//! Auto-generated API clients for gcp/gkeonprem.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/gkeonprem`

#![cfg(feature = "gcp/gkeonprem")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_get_execute()` to send, or `gkeonprem_projects_locations_get` for simplest API.

pub fn gkeonprem_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_get_builder()` + `gkeonprem_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_get_builder(client, name)?;
    gkeonprem_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_list_execute()` to send, or `gkeonprem_projects_locations_list` for simplest API.

pub fn gkeonprem_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_list_builder()` + `gkeonprem_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    gkeonprem_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Creates a new bare metal admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work. See: <https://cloud.google.`com/anthos/clusters/docs/bare-metal/latest/installing/creating-clusters/create-admin-cluster-api`#prepare_bootstrap_environment>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_create_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_create` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    allowPreflightFailure: Option<bool>,
    bareMetalAdminClusterId: Option<&str>,
    validateOnly: Option<bool>,
    body: &BareMetalAdminCluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalAdminClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowPreflightFailure {
        query_parts.push(format!("allowPreflightFailure={}", val));
    }
    if let Some(val) = bareMetalAdminClusterId {
        query_parts.push(format!("bareMetalAdminClusterId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Creates a new bare metal admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work. See: <https://cloud.google.`com/anthos/clusters/docs/bare-metal/latest/installing/creating-clusters/create-admin-cluster-api`#prepare_bootstrap_environment>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_admin_clusters_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Creates a new bare metal admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work. See: <https://cloud.google.`com/anthos/clusters/docs/bare-metal/latest/installing/creating-clusters/create-admin-cluster-api`#prepare_bootstrap_environment>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_create_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_create(
    client: &SimpleHttpClient,
    parent: &str,
    allowPreflightFailure: Option<bool>,
    bareMetalAdminClusterId: Option<&str>,
    validateOnly: Option<bool>,
    body: &BareMetalAdminCluster,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_create_builder(
        client,
        parent,
        allowPreflightFailure,
        bareMetalAdminClusterId,
        validateOnly,
        body,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:enroll
/// Enrolls an existing bare metal admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollBareMetalAdminClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalAdminClusters:enroll",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:enroll
/// Enrolls an existing bare metal admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:enroll
/// Enrolls an existing bare metal admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_enroll(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollBareMetalAdminClusterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_builder(
        client, parent, body,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Gets details of a single bare metal admin cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_get` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalAdminClusters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Gets details of a single bare metal admin cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_admin_clusters_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BareMetalAdminCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BareMetalAdminCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Gets details of a single bare metal admin cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BareMetalAdminCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_get_builder(
        client,
        name,
        allowMissing,
        view,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalAdminClusters/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Lists bare metal admin clusters in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_list_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_list` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    allowMissing: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalAdminClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Lists bare metal admin clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_admin_clusters_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBareMetalAdminClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBareMetalAdminClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Lists bare metal admin clusters in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_list_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_list(
    client: &SimpleHttpClient,
    parent: &str,
    allowMissing: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBareMetalAdminClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_list_builder(
        client,
        parent,
        allowMissing,
        pageSize,
        pageToken,
        view,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Updates the parameters of a single bare metal admin cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_patch` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &BareMetalAdminCluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalAdminClusters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Updates the parameters of a single bare metal admin cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_admin_clusters_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Updates the parameters of a single bare metal admin cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &BareMetalAdminCluster,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_patch_builder(
        client,
        name,
        updateMask,
        validateOnly,
        body,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:queryVersionConfig
/// Queries the bare metal admin cluster version config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_builder(
    client: &SimpleHttpClient,
    parent: &str,
    upgradeConfig_clusterName: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalAdminClusters:queryVersionConfig",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = upgradeConfig_clusterName {
        query_parts.push(format!("upgradeConfig_clusterName={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:queryVersionConfig
/// Queries the bare metal admin cluster version config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryBareMetalAdminVersionConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryBareMetalAdminVersionConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:queryVersionConfig
/// Queries the bare metal admin cluster version config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config(
    client: &SimpleHttpClient,
    parent: &str,
    upgradeConfig_clusterName: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryBareMetalAdminVersionConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_builder(
            client,
            parent,
            upgradeConfig_clusterName,
        )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalAdminClusters/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_builder(
        client, resource, body,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalAdminClusters/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_builder(
            client, resource, body,
        )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:unenroll
/// Unenrolls an existing bare metal admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    ignoreErrors: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalAdminClusters/{}:unenroll",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = ignoreErrors {
        query_parts.push(format!("ignoreErrors={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:unenroll
/// Unenrolls an existing bare metal admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:unenroll
/// Unenrolls an existing bare metal admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    ignoreErrors: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_builder(
        client,
        name,
        allowMissing,
        etag,
        ignoreErrors,
        validateOnly,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalAdminClusters/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_builder(
        client, name,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalAdminClusters/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Creates a new bare metal cluster in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_create_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_create` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    allowPreflightFailure: Option<bool>,
    bareMetalClusterId: Option<&str>,
    validateOnly: Option<bool>,
    body: &BareMetalCluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowPreflightFailure {
        query_parts.push(format!("allowPreflightFailure={}", val));
    }
    if let Some(val) = bareMetalClusterId {
        query_parts.push(format!("bareMetalClusterId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Creates a new bare metal cluster in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Creates a new bare metal cluster in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_create_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_create(
    client: &SimpleHttpClient,
    parent: &str,
    allowPreflightFailure: Option<bool>,
    bareMetalClusterId: Option<&str>,
    validateOnly: Option<bool>,
    body: &BareMetalCluster,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_create_builder(
        client,
        parent,
        allowPreflightFailure,
        bareMetalClusterId,
        validateOnly,
        body,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Deletes a single bare metal Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_delete_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_delete` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    force: Option<bool>,
    ignoreErrors: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = ignoreErrors {
        query_parts.push(format!("ignoreErrors={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Deletes a single bare metal Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Deletes a single bare metal Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_delete_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_delete(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    force: Option<bool>,
    ignoreErrors: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_delete_builder(
        client,
        name,
        allowMissing,
        etag,
        force,
        ignoreErrors,
        validateOnly,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:enroll
/// Enrolls an existing bare metal user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_enroll_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_enroll` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_enroll_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollBareMetalClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters:enroll",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:enroll
/// Enrolls an existing bare metal user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_enroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_enroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:enroll
/// Enrolls an existing bare metal user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_enroll_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_enroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_enroll(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollBareMetalClusterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_enroll_builder(client, parent, body)?;
    gkeonprem_projects_locations_bare_metal_clusters_enroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Gets details of a single bare metal Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_get_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_get` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Gets details of a single bare metal Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BareMetalCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BareMetalCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Gets details of a single bare metal Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_get_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BareMetalCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_get_builder(
        client,
        name,
        allowMissing,
        view,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Lists bare metal clusters in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_list_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_list` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    allowMissing: Option<bool>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Lists bare metal clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBareMetalClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBareMetalClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Lists bare metal clusters in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_list_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_list(
    client: &SimpleHttpClient,
    parent: &str,
    allowMissing: Option<bool>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBareMetalClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_list_builder(
        client,
        parent,
        allowMissing,
        filter,
        pageSize,
        pageToken,
        view,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Updates the parameters of a single bare metal Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_patch_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_patch` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &BareMetalCluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Updates the parameters of a single bare metal Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Updates the parameters of a single bare metal Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_patch_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &BareMetalCluster,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_patch_builder(
        client,
        name,
        allowMissing,
        updateMask,
        validateOnly,
        body,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:queryVersionConfig
/// Queries the bare metal user cluster version config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_query_version_config` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_query_version_config_builder(
    client: &SimpleHttpClient,
    parent: &str,
    createConfig_adminClusterMembership: Option<&str>,
    createConfig_adminClusterName: Option<&str>,
    upgradeConfig_clusterName: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters:queryVersionConfig",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = createConfig_adminClusterMembership {
        query_parts.push(format!("createConfig_adminClusterMembership={}", val));
    }
    if let Some(val) = createConfig_adminClusterName {
        query_parts.push(format!("createConfig_adminClusterName={}", val));
    }
    if let Some(val) = upgradeConfig_clusterName {
        query_parts.push(format!("upgradeConfig_clusterName={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:queryVersionConfig
/// Queries the bare metal user cluster version config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_query_version_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_query_version_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryBareMetalVersionConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryBareMetalVersionConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:queryVersionConfig
/// Queries the bare metal user cluster version config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_query_version_config(
    client: &SimpleHttpClient,
    parent: &str,
    createConfig_adminClusterMembership: Option<&str>,
    createConfig_adminClusterName: Option<&str>,
    upgradeConfig_clusterName: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryBareMetalVersionConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_query_version_config_builder(
        client,
        parent,
        createConfig_adminClusterMembership,
        createConfig_adminClusterName,
        upgradeConfig_clusterName,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_query_version_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_builder(
        client, resource, body,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_builder(
        client, resource, body,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:unenroll
/// Unenrolls an existing bare metal user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_unenroll_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_unenroll` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_unenroll_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    force: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}:unenroll",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:unenroll
/// Unenrolls an existing bare metal user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_unenroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_unenroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:unenroll
/// Unenrolls an existing bare metal user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_unenroll_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_unenroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_unenroll(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    force: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_unenroll_builder(
        client,
        name,
        allowMissing,
        etag,
        force,
        validateOnly,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_unenroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Creates a new bare metal node pool in a given project, location and Bare Metal cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bareMetalNodePoolId: Option<&str>,
    validateOnly: Option<bool>,
    body: &BareMetalNodePool,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/bareMetalNodePools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bareMetalNodePoolId {
        query_parts.push(format!("bareMetalNodePoolId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Creates a new bare metal node pool in a given project, location and Bare Metal cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Creates a new bare metal node pool in a given project, location and Bare Metal cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create(
    client: &SimpleHttpClient,
    parent: &str,
    bareMetalNodePoolId: Option<&str>,
    validateOnly: Option<bool>,
    body: &BareMetalNodePool,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_builder(
            client,
            parent,
            bareMetalNodePoolId,
            validateOnly,
            body,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Deletes a single bare metal node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    ignoreErrors: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/bareMetalNodePools/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = ignoreErrors {
        query_parts.push(format!("ignoreErrors={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Deletes a single bare metal node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Deletes a single bare metal node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    ignoreErrors: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_builder(
            client,
            name,
            allowMissing,
            etag,
            ignoreErrors,
            validateOnly,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools:enroll
/// Enrolls an existing bare metal node pool to the Anthos On-Prem API within a given project and location. Through enrollment, an existing node pool will become Anthos On-Prem API managed. The corresponding GCP resources will be created.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollBareMetalNodePoolRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/bareMetalNodePools:enroll",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools:enroll
/// Enrolls an existing bare metal node pool to the Anthos On-Prem API within a given project and location. Through enrollment, an existing node pool will become Anthos On-Prem API managed. The corresponding GCP resources will be created.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools:enroll
/// Enrolls an existing bare metal node pool to the Anthos On-Prem API within a given project and location. Through enrollment, an existing node pool will become Anthos On-Prem API managed. The corresponding GCP resources will be created.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollBareMetalNodePoolRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_builder(
            client, parent, body,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Gets details of a single bare metal node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/bareMetalNodePools/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Gets details of a single bare metal node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BareMetalNodePool>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BareMetalNodePool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Gets details of a single bare metal node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BareMetalNodePool>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_builder(
            client, name, view,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/bareMetalNodePools/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_builder(client, resource, options_requestedPolicyVersion)?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Lists bare metal node pools in a given project, location and bare metal cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/bareMetalNodePools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Lists bare metal node pools in a given project, location and bare metal cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBareMetalNodePoolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBareMetalNodePoolsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Lists bare metal node pools in a given project, location and bare metal cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBareMetalNodePoolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_builder(
            client, parent, pageSize, pageToken, view,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Updates the parameters of a single bare metal node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &BareMetalNodePool,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/bareMetalNodePools/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Updates the parameters of a single bare metal node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Updates the parameters of a single bare metal node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &BareMetalNodePool,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_builder(
            client,
            name,
            allowMissing,
            updateMask,
            validateOnly,
            body,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/bareMetalNodePools/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_builder(client, resource, body)?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/bareMetalNodePools/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_builder(client, resource, body)?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:unenroll
/// Unenrolls a bare metal node pool from Anthos On-Prem API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/bareMetalNodePools/{}:unenroll",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:unenroll
/// Unenrolls a bare metal node pool from Anthos On-Prem API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:unenroll
/// Unenrolls a bare metal node pool from Anthos On-Prem API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_builder(
            client,
            name,
            allowMissing,
            etag,
            validateOnly,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/bareMetalNodePools/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_builder(client, name)?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/bareMetalNodePools/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_builder(client, name, filter, pageSize, pageToken, returnPartialSuccess)?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_operations_get_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_operations_get_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_operations_get_builder(client, name)?;
    gkeonprem_projects_locations_bare_metal_clusters_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_operations_list_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/bareMetalClusters/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_bare_metal_clusters_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_operations_list_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_operations_cancel_execute()` to send, or `gkeonprem_projects_locations_operations_cancel` for simplest API.

pub fn gkeonprem_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_operations_cancel_builder()` + `gkeonprem_projects_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_operations_cancel_builder(client, name, body)?;
    gkeonprem_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_operations_delete_execute()` to send, or `gkeonprem_projects_locations_operations_delete` for simplest API.

pub fn gkeonprem_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_operations_delete_builder()` + `gkeonprem_projects_locations_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_operations_delete_builder(client, name)?;
    gkeonprem_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_operations_get_execute()` to send, or `gkeonprem_projects_locations_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_operations_get_builder()` + `gkeonprem_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_operations_get_builder(client, name)?;
    gkeonprem_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_operations_list_execute()` to send, or `gkeonprem_projects_locations_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_operations_list_builder()` + `gkeonprem_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    gkeonprem_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Creates a new VMware admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_create_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_create` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    allowPreflightFailure: Option<bool>,
    skipValidations: Option<&str>,
    validateOnly: Option<bool>,
    vmwareAdminClusterId: Option<&str>,
    body: &VmwareAdminCluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareAdminClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowPreflightFailure {
        query_parts.push(format!("allowPreflightFailure={}", val));
    }
    if let Some(val) = skipValidations {
        query_parts.push(format!("skipValidations={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }
    if let Some(val) = vmwareAdminClusterId {
        query_parts.push(format!("vmwareAdminClusterId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Creates a new VMware admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_admin_clusters_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Creates a new VMware admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_create_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_create(
    client: &SimpleHttpClient,
    parent: &str,
    allowPreflightFailure: Option<bool>,
    skipValidations: Option<&str>,
    validateOnly: Option<bool>,
    vmwareAdminClusterId: Option<&str>,
    body: &VmwareAdminCluster,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_create_builder(
        client,
        parent,
        allowPreflightFailure,
        skipValidations,
        validateOnly,
        vmwareAdminClusterId,
        body,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters:enroll
/// Enrolls an existing VMware admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_enroll_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_enroll` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_enroll_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollVmwareAdminClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareAdminClusters:enroll",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters:enroll
/// Enrolls an existing VMware admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_enroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_admin_clusters_enroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters:enroll
/// Enrolls an existing VMware admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_enroll_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_enroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_enroll(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollVmwareAdminClusterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_admin_clusters_enroll_builder(client, parent, body)?;
    gkeonprem_projects_locations_vmware_admin_clusters_enroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Gets details of a single VMware admin cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_get_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_get` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareAdminClusters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Gets details of a single VMware admin cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_admin_clusters_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmwareAdminCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VmwareAdminCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Gets details of a single VMware admin cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_get_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmwareAdminCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_get_builder(
        client,
        name,
        allowMissing,
        view,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareAdminClusters/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Lists VMware admin clusters in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_list_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_list` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    allowMissing: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareAdminClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Lists VMware admin clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_admin_clusters_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVmwareAdminClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVmwareAdminClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Lists VMware admin clusters in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_list_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_list(
    client: &SimpleHttpClient,
    parent: &str,
    allowMissing: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVmwareAdminClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_list_builder(
        client,
        parent,
        allowMissing,
        pageSize,
        pageToken,
        view,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Updates the parameters of a single VMware admin cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_patch_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_patch` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    skipValidations: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &VmwareAdminCluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareAdminClusters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = skipValidations {
        query_parts.push(format!("skipValidations={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Updates the parameters of a single VMware admin cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_admin_clusters_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Updates the parameters of a single VMware admin cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_patch_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_patch(
    client: &SimpleHttpClient,
    name: &str,
    skipValidations: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &VmwareAdminCluster,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_patch_builder(
        client,
        name,
        skipValidations,
        updateMask,
        validateOnly,
        body,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareAdminClusters/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_builder(
        client, resource, body,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareAdminClusters/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_builder(
        client, resource, body,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:unenroll
/// Unenrolls an existing VMware admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_unenroll` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_unenroll_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    ignoreErrors: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareAdminClusters/{}:unenroll",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = ignoreErrors {
        query_parts.push(format!("ignoreErrors={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:unenroll
/// Unenrolls an existing VMware admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_admin_clusters_unenroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:unenroll
/// Unenrolls an existing VMware admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_unenroll(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    ignoreErrors: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_unenroll_builder(
        client,
        name,
        allowMissing,
        etag,
        ignoreErrors,
        validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_unenroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareAdminClusters/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_admin_clusters_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_admin_clusters_operations_get_builder(client, name)?;
    gkeonprem_projects_locations_vmware_admin_clusters_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareAdminClusters/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_admin_clusters_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Creates a new VMware user cluster in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_create_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_create` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    allowPreflightFailure: Option<bool>,
    skipValidations: Option<&str>,
    validateOnly: Option<bool>,
    vmwareClusterId: Option<&str>,
    body: &VmwareCluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowPreflightFailure {
        query_parts.push(format!("allowPreflightFailure={}", val));
    }
    if let Some(val) = skipValidations {
        query_parts.push(format!("skipValidations={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }
    if let Some(val) = vmwareClusterId {
        query_parts.push(format!("vmwareClusterId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Creates a new VMware user cluster in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Creates a new VMware user cluster in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_create_builder()` + `gkeonprem_projects_locations_vmware_clusters_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_create(
    client: &SimpleHttpClient,
    parent: &str,
    allowPreflightFailure: Option<bool>,
    skipValidations: Option<&str>,
    validateOnly: Option<bool>,
    vmwareClusterId: Option<&str>,
    body: &VmwareCluster,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_create_builder(
        client,
        parent,
        allowPreflightFailure,
        skipValidations,
        validateOnly,
        vmwareClusterId,
        body,
    )?;
    gkeonprem_projects_locations_vmware_clusters_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Deletes a single VMware Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_delete_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_delete` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    force: Option<bool>,
    ignoreErrors: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = ignoreErrors {
        query_parts.push(format!("ignoreErrors={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Deletes a single VMware Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Deletes a single VMware Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_delete_builder()` + `gkeonprem_projects_locations_vmware_clusters_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_delete(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    force: Option<bool>,
    ignoreErrors: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_delete_builder(
        client,
        name,
        allowMissing,
        etag,
        force,
        ignoreErrors,
        validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_clusters_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:enroll
/// Enrolls an existing VMware user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_enroll_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_enroll` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_enroll_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollVmwareClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters:enroll",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:enroll
/// Enrolls an existing VMware user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_enroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_enroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:enroll
/// Enrolls an existing VMware user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_enroll_builder()` + `gkeonprem_projects_locations_vmware_clusters_enroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_enroll(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollVmwareClusterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_enroll_builder(client, parent, body)?;
    gkeonprem_projects_locations_vmware_clusters_enroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Gets details of a single VMware Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_get_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_get` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Gets details of a single VMware Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmwareCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VmwareCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Gets details of a single VMware Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_get_builder()` + `gkeonprem_projects_locations_vmware_clusters_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_get(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmwareCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_get_builder(client, name, allowMissing, view)?;
    gkeonprem_projects_locations_vmware_clusters_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_get_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_builder()` + `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    gkeonprem_projects_locations_vmware_clusters_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Lists VMware Clusters in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_list_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_list` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    allowMissing: Option<bool>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Lists VMware Clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVmwareClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVmwareClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Lists VMware Clusters in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_list_builder()` + `gkeonprem_projects_locations_vmware_clusters_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_list(
    client: &SimpleHttpClient,
    parent: &str,
    allowMissing: Option<bool>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVmwareClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_list_builder(
        client,
        parent,
        allowMissing,
        filter,
        pageSize,
        pageToken,
        view,
    )?;
    gkeonprem_projects_locations_vmware_clusters_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Updates the parameters of a single VMware cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_patch_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_patch` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    skipValidations: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &VmwareCluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = skipValidations {
        query_parts.push(format!("skipValidations={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Updates the parameters of a single VMware cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Updates the parameters of a single VMware cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_patch_builder()` + `gkeonprem_projects_locations_vmware_clusters_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_patch(
    client: &SimpleHttpClient,
    name: &str,
    skipValidations: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &VmwareCluster,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_patch_builder(
        client,
        name,
        skipValidations,
        updateMask,
        validateOnly,
        body,
    )?;
    gkeonprem_projects_locations_vmware_clusters_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:queryVersionConfig
/// Queries the VMware user cluster version config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_query_version_config_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_query_version_config` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_query_version_config_builder(
    client: &SimpleHttpClient,
    parent: &str,
    createConfig_adminClusterMembership: Option<&str>,
    createConfig_adminClusterName: Option<&str>,
    upgradeConfig_clusterName: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters:queryVersionConfig",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = createConfig_adminClusterMembership {
        query_parts.push(format!("createConfig_adminClusterMembership={}", val));
    }
    if let Some(val) = createConfig_adminClusterName {
        query_parts.push(format!("createConfig_adminClusterName={}", val));
    }
    if let Some(val) = upgradeConfig_clusterName {
        query_parts.push(format!("upgradeConfig_clusterName={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:queryVersionConfig
/// Queries the VMware user cluster version config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_query_version_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_query_version_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_query_version_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryVmwareVersionConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryVmwareVersionConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:queryVersionConfig
/// Queries the VMware user cluster version config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_query_version_config_builder()` + `gkeonprem_projects_locations_vmware_clusters_query_version_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_query_version_config(
    client: &SimpleHttpClient,
    parent: &str,
    createConfig_adminClusterMembership: Option<&str>,
    createConfig_adminClusterName: Option<&str>,
    upgradeConfig_clusterName: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryVmwareVersionConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_query_version_config_builder(
        client,
        parent,
        createConfig_adminClusterMembership,
        createConfig_adminClusterName,
        upgradeConfig_clusterName,
    )?;
    gkeonprem_projects_locations_vmware_clusters_query_version_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_set_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_builder()` + `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_set_iam_policy_builder(
        client, resource, body,
    )?;
    gkeonprem_projects_locations_vmware_clusters_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_builder()` + `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_builder(
        client, resource, body,
    )?;
    gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:unenroll
/// Unenrolls an existing VMware user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or UI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_unenroll_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_unenroll` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_unenroll_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    force: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}:unenroll",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:unenroll
/// Unenrolls an existing VMware user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or UI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_unenroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_unenroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:unenroll
/// Unenrolls an existing VMware user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or UI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_unenroll_builder()` + `gkeonprem_projects_locations_vmware_clusters_unenroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_unenroll(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    force: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_unenroll_builder(
        client,
        name,
        allowMissing,
        etag,
        force,
        validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_clusters_unenroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_operations_get_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_operations_get_builder()` + `gkeonprem_projects_locations_vmware_clusters_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_operations_get_builder(client, name)?;
    gkeonprem_projects_locations_vmware_clusters_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_operations_list_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_operations_list_builder()` + `gkeonprem_projects_locations_vmware_clusters_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    gkeonprem_projects_locations_vmware_clusters_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Creates a new VMware node pool in a given project, location and VMWare cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    validateOnly: Option<bool>,
    vmwareNodePoolId: Option<&str>,
    body: &VmwareNodePool,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/vmwareNodePools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }
    if let Some(val) = vmwareNodePoolId {
        query_parts.push(format!("vmwareNodePoolId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Creates a new VMware node pool in a given project, location and VMWare cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Creates a new VMware node pool in a given project, location and VMWare cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create(
    client: &SimpleHttpClient,
    parent: &str,
    validateOnly: Option<bool>,
    vmwareNodePoolId: Option<&str>,
    body: &VmwareNodePool,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_builder(
        client,
        parent,
        validateOnly,
        vmwareNodePoolId,
        body,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Deletes a single VMware node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    ignoreErrors: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/vmwareNodePools/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = ignoreErrors {
        query_parts.push(format!("ignoreErrors={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Deletes a single VMware node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Deletes a single VMware node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    ignoreErrors: Option<bool>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_builder(
        client,
        name,
        allowMissing,
        etag,
        ignoreErrors,
        validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools:enroll
/// Enrolls a VMware node pool to Anthos On-Prem API
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollVmwareNodePoolRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/vmwareNodePools:enroll",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools:enroll
/// Enrolls a VMware node pool to Anthos On-Prem API
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools:enroll
/// Enrolls a VMware node pool to Anthos On-Prem API
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollVmwareNodePoolRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_builder(
        client, parent, body,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Gets details of a single VMware node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/vmwareNodePools/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Gets details of a single VMware node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmwareNodePool>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VmwareNodePool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Gets details of a single VMware node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmwareNodePool>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_builder(
        client, name, view,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/vmwareNodePools/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_builder(
            client,
            resource,
            options_requestedPolicyVersion,
        )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Lists VMware node pools in a given project, location and VMWare cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/vmwareNodePools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Lists VMware node pools in a given project, location and VMWare cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVmwareNodePoolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVmwareNodePoolsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Lists VMware node pools in a given project, location and VMWare cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVmwareNodePoolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_builder(
        client, parent, pageSize, pageToken, view,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Updates the parameters of a single VMware node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &VmwareNodePool,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/vmwareNodePools/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Updates the parameters of a single VMware node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Updates the parameters of a single VMware node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &VmwareNodePool,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_builder(
        client,
        name,
        updateMask,
        validateOnly,
        body,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/vmwareNodePools/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_builder(
            client, resource, body,
        )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/vmwareNodePools/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_builder(client, resource, body)?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:unenroll
/// Unenrolls a VMware node pool to Anthos On-Prem API
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/vmwareNodePools/{}:unenroll",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:unenroll
/// Unenrolls a VMware node pool to Anthos On-Prem API
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:unenroll
/// Unenrolls a VMware node pool to Anthos On-Prem API
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_builder(
        client,
        name,
        allowMissing,
        etag,
        validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/vmwareNodePools/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_builder(
            client, name,
        )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{}/vmwareClusters/{}/vmwareNodePools/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_builder(
            client,
            name,
            filter,
            pageSize,
            pageToken,
            returnPartialSuccess,
        )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_execute(builder)
}
