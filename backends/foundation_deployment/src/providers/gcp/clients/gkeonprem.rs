//! Auto-generated API clients for gcp/gkeonprem.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_get_execute()` to send, or `gkeonprem_projects_locations_get` for simplest API.

pub fn gkeonprem_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_get_execute()` or `gkeonprem_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_get_builder()` + `gkeonprem_projects_locations_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_get_builder(client, &args.name)?;
    gkeonprem_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_list_execute()` to send, or `gkeonprem_projects_locations_list` for simplest API.

pub fn gkeonprem_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_list_execute()` or `gkeonprem_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_list_builder()` + `gkeonprem_projects_locations_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    gkeonprem_projects_locations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Creates a new bare metal admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work. See: <https://cloud.google.`com/anthos/clusters/docs/bare-metal/latest/installing/creating-clusters/create-admin-cluster-api`#prepare_bootstrap_environment>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_create_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_create` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    allowPreflightFailure: &Option<String>,
    bareMetalAdminClusterId: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalAdminClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowPreflightFailure.as_ref() {
        query_parts.push(format!("allowPreflightFailure={}", val));
    }
    if let Some(val) = bareMetalAdminClusterId.as_ref() {
        query_parts.push(format!("bareMetalAdminClusterId={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Creates a new bare metal admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work. See: <https://cloud.google.`com/anthos/clusters/docs/bare-metal/latest/installing/creating-clusters/create-admin-cluster-api`#prepare_bootstrap_environment>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_admin_clusters_create_execute()` or `gkeonprem_projects_locations_bare_metal_admin_clusters_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Creates a new bare metal admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work. See: <https://cloud.google.`com/anthos/clusters/docs/bare-metal/latest/installing/creating-clusters/create-admin-cluster-api`#prepare_bootstrap_environment>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_create_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_admin_clusters_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_admin_clusters_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_admin_clusters_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalAdminClustersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: allowPreflightFailure
    pub allowPreflightFailure: Option<String>,
    /// Query parameter: bareMetalAdminClusterId
    pub bareMetalAdminClusterId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Creates a new bare metal admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work. See: <https://cloud.google.`com/anthos/clusters/docs/bare-metal/latest/installing/creating-clusters/create-admin-cluster-api`#prepare_bootstrap_environment>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_create_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_create_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_create(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalAdminClustersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_create_builder(
        client,
        &args.parent,
        &args.allowPreflightFailure,
        &args.bareMetalAdminClusterId,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_create_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:enroll
/// Enrolls an existing bare metal admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalAdminClusters:enroll",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:enroll
/// Enrolls an existing bare metal admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_execute()` or `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:enroll
/// Enrolls an existing bare metal admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_admin_clusters_enroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalAdminClustersEnrollArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:enroll
/// Enrolls an existing bare metal admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_enroll(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalAdminClustersEnrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_builder(
        client,
        &args.parent,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_enroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Gets details of a single bare metal admin cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_get` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Gets details of a single bare metal admin cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_execute()` or `gkeonprem_projects_locations_bare_metal_admin_clusters_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BareMetalAdminCluster>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BareMetalAdminCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Gets details of a single bare metal admin cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BareMetalAdminCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_admin_clusters_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_admin_clusters_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalAdminClustersGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Gets details of a single bare metal admin cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalAdminClustersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BareMetalAdminCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_get_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.view,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_execute()` or `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalAdminClustersGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalAdminClustersGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Lists bare metal admin clusters in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_list_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_list` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    allowMissing: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalAdminClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Lists bare metal admin clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_admin_clusters_list_execute()` or `gkeonprem_projects_locations_bare_metal_admin_clusters_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBareMetalAdminClustersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBareMetalAdminClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Lists bare metal admin clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_admin_clusters_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBareMetalAdminClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_admin_clusters_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_admin_clusters_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalAdminClustersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters
/// Lists bare metal admin clusters in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_list_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalAdminClustersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBareMetalAdminClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_list_builder(
        client,
        &args.parent,
        &args.allowMissing,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Updates the parameters of a single bare metal admin cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_patch` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Updates the parameters of a single bare metal admin cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_execute()` or `gkeonprem_projects_locations_bare_metal_admin_clusters_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Updates the parameters of a single bare metal admin cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_admin_clusters_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_admin_clusters_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_admin_clusters_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalAdminClustersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}
/// Updates the parameters of a single bare metal admin cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_patch(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalAdminClustersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_patch_builder(
        client,
        &args.name,
        &args.updateMask,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:queryVersionConfig
/// Queries the bare metal admin cluster version config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    upgradeConfig_clusterName: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalAdminClusters:queryVersionConfig",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = upgradeConfig_clusterName.as_ref() {
        query_parts.push(format!("upgradeConfig.clusterName={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:queryVersionConfig
/// Queries the bare metal admin cluster version config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_execute()` or `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryBareMetalAdminVersionConfigResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryBareMetalAdminVersionConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:queryVersionConfig
/// Queries the bare metal admin cluster version config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryBareMetalAdminVersionConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalAdminClustersQueryVersionConfigArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: upgradeConfig_clusterName
    pub upgradeConfig_clusterName: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters:queryVersionConfig
/// Queries the bare metal admin cluster version config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalAdminClustersQueryVersionConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryBareMetalAdminVersionConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_builder(
            client,
            &args.parent,
            &args.upgradeConfig_clusterName,
        )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_query_version_config_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_execute()` or `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalAdminClustersSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalAdminClustersSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_execute()` or `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalAdminClustersTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalAdminClustersTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_builder(
            client,
            &args.resource,
        )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_test_iam_permissions_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:unenroll
/// Unenrolls an existing bare metal admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    etag: &Option<String>,
    ignoreErrors: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:unenroll",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = ignoreErrors.as_ref() {
        query_parts.push(format!("ignoreErrors={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:unenroll
/// Unenrolls an existing bare metal admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_execute()` or `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:unenroll
/// Unenrolls an existing bare metal admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalAdminClustersUnenrollArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: ignoreErrors
    pub ignoreErrors: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}:unenroll
/// Unenrolls an existing bare metal admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalAdminClustersUnenrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.etag,
        &args.ignoreErrors,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_unenroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_execute()` or `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalAdminClustersOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalAdminClustersOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_builder(
        client, &args.name,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_execute()` to send, or `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_execute()` or `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalAdminClustersOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalAdminClusters/{bareMetalAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_builder()` + `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalAdminClustersOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    gkeonprem_projects_locations_bare_metal_admin_clusters_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Creates a new bare metal cluster in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_create_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_create` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    allowPreflightFailure: &Option<String>,
    bareMetalClusterId: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowPreflightFailure.as_ref() {
        query_parts.push(format!("allowPreflightFailure={}", val));
    }
    if let Some(val) = bareMetalClusterId.as_ref() {
        query_parts.push(format!("bareMetalClusterId={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Creates a new bare metal cluster in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_create_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Creates a new bare metal cluster in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_create_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: allowPreflightFailure
    pub allowPreflightFailure: Option<String>,
    /// Query parameter: bareMetalClusterId
    pub bareMetalClusterId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Creates a new bare metal cluster in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_create_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_create_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_create(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_create_builder(
        client,
        &args.parent,
        &args.allowPreflightFailure,
        &args.bareMetalClusterId,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Deletes a single bare metal Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_delete_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_delete` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    etag: &Option<String>,
    force: &Option<String>,
    ignoreErrors: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = ignoreErrors.as_ref() {
        query_parts.push(format!("ignoreErrors={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Deletes a single bare metal Cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_delete_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Deletes a single bare metal Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_delete_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: force
    pub force: Option<String>,
    /// Query parameter: ignoreErrors
    pub ignoreErrors: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Deletes a single bare metal Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_delete_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_delete_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_delete(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_delete_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.etag,
        &args.force,
        &args.ignoreErrors,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:enroll
/// Enrolls an existing bare metal user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_enroll_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_enroll` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_enroll_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters:enroll",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:enroll
/// Enrolls an existing bare metal user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_enroll_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_enroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_enroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:enroll
/// Enrolls an existing bare metal user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_enroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_enroll_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_enroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_enroll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_enroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersEnrollArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:enroll
/// Enrolls an existing bare metal user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_enroll_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_enroll_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_enroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_enroll(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersEnrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_enroll_builder(client, &args.parent)?;
    gkeonprem_projects_locations_bare_metal_clusters_enroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Gets details of a single bare metal Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_get_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_get` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Gets details of a single bare metal Cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_get_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BareMetalCluster>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BareMetalCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Gets details of a single bare metal Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BareMetalCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Gets details of a single bare metal Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_get_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BareMetalCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_get_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.view,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Lists bare metal clusters in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_list_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_list` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    allowMissing: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Lists bare metal clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_list_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBareMetalClustersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBareMetalClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Lists bare metal clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBareMetalClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters
/// Lists bare metal clusters in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_list_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBareMetalClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_list_builder(
        client,
        &args.parent,
        &args.allowMissing,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Updates the parameters of a single bare metal Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_patch_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_patch` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    updateMask: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Updates the parameters of a single bare metal Cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_patch_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Updates the parameters of a single bare metal Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_patch_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}
/// Updates the parameters of a single bare metal Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_patch_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_patch_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_patch(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_patch_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.updateMask,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:queryVersionConfig
/// Queries the bare metal user cluster version config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_query_version_config` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_query_version_config_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    createConfig_adminClusterMembership: &Option<String>,
    createConfig_adminClusterName: &Option<String>,
    upgradeConfig_clusterName: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters:queryVersionConfig",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = createConfig_adminClusterMembership.as_ref() {
        query_parts.push(format!("createConfig.adminClusterMembership={}", val));
    }
    if let Some(val) = createConfig_adminClusterName.as_ref() {
        query_parts.push(format!("createConfig.adminClusterName={}", val));
    }
    if let Some(val) = upgradeConfig_clusterName.as_ref() {
        query_parts.push(format!("upgradeConfig.clusterName={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:queryVersionConfig
/// Queries the bare metal user cluster version config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_query_version_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_query_version_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryBareMetalVersionConfigResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryBareMetalVersionConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:queryVersionConfig
/// Queries the bare metal user cluster version config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_query_version_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_query_version_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryBareMetalVersionConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_query_version_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_query_version_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersQueryVersionConfigArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: createConfig_adminClusterMembership
    pub createConfig_adminClusterMembership: Option<String>,
    /// Query parameter: createConfig_adminClusterName
    pub createConfig_adminClusterName: Option<String>,
    /// Query parameter: upgradeConfig_clusterName
    pub upgradeConfig_clusterName: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters:queryVersionConfig
/// Queries the bare metal user cluster version config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_query_version_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_query_version_config(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersQueryVersionConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryBareMetalVersionConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_query_version_config_builder(
        client,
        &args.parent,
        &args.createConfig_adminClusterMembership,
        &args.createConfig_adminClusterName,
        &args.upgradeConfig_clusterName,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_query_version_config_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_test_iam_permissions_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:unenroll
/// Unenrolls an existing bare metal user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_unenroll_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_unenroll` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_unenroll_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    etag: &Option<String>,
    force: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:unenroll",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:unenroll
/// Unenrolls an existing bare metal user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_unenroll_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_unenroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_unenroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:unenroll
/// Unenrolls an existing bare metal user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_unenroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_unenroll_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_unenroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_unenroll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_unenroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersUnenrollArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: force
    pub force: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}:unenroll
/// Unenrolls an existing bare metal user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_unenroll_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_unenroll_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_unenroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_unenroll(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersUnenrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_unenroll_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.etag,
        &args.force,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_unenroll_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Creates a new bare metal node pool in a given project, location and Bare Metal cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bareMetalNodePoolId: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bareMetalNodePoolId.as_ref() {
        query_parts.push(format!("bareMetalNodePoolId={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Creates a new bare metal node pool in a given project, location and Bare Metal cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Creates a new bare metal node pool in a given project, location and Bare Metal cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bareMetalNodePoolId
    pub bareMetalNodePoolId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Creates a new bare metal node pool in a given project, location and Bare Metal cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_builder(
            client,
            &args.parent,
            &args.bareMetalNodePoolId,
            &args.validateOnly,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Deletes a single bare metal node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    etag: &Option<String>,
    ignoreErrors: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = ignoreErrors.as_ref() {
        query_parts.push(format!("ignoreErrors={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Deletes a single bare metal node pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Deletes a single bare metal node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: ignoreErrors
    pub ignoreErrors: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Deletes a single bare metal node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_builder(
            client,
            &args.name,
            &args.allowMissing,
            &args.etag,
            &args.ignoreErrors,
            &args.validateOnly,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools:enroll
/// Enrolls an existing bare metal node pool to the Anthos On-Prem API within a given project and location. Through enrollment, an existing node pool will become Anthos On-Prem API managed. The corresponding GCP resources will be created.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools:enroll",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools:enroll
/// Enrolls an existing bare metal node pool to the Anthos On-Prem API within a given project and location. Through enrollment, an existing node pool will become Anthos On-Prem API managed. The corresponding GCP resources will be created.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools:enroll
/// Enrolls an existing bare metal node pool to the Anthos On-Prem API within a given project and location. Through enrollment, an existing node pool will become Anthos On-Prem API managed. The corresponding GCP resources will be created.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsEnrollArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools:enroll
/// Enrolls an existing bare metal node pool to the Anthos On-Prem API within a given project and location. Through enrollment, an existing node pool will become Anthos On-Prem API managed. The corresponding GCP resources will be created.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsEnrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_builder(
            client,
            &args.parent,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_enroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Gets details of a single bare metal node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Gets details of a single bare metal node pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BareMetalNodePool>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BareMetalNodePool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Gets details of a single bare metal node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BareMetalNodePool>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Gets details of a single bare metal node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BareMetalNodePool>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_builder(
            client, &args.name, &args.view,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_builder(client, &args.resource, &args.options_requestedPolicyVersion)?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_get_iam_policy_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Lists bare metal node pools in a given project, location and bare metal cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Lists bare metal node pools in a given project, location and bare metal cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBareMetalNodePoolsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBareMetalNodePoolsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Lists bare metal node pools in a given project, location and bare metal cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBareMetalNodePoolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools
/// Lists bare metal node pools in a given project, location and bare metal cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBareMetalNodePoolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_builder(
            client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
            &args.view,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Updates the parameters of a single bare metal node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    updateMask: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Updates the parameters of a single bare metal node pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Updates the parameters of a single bare metal node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}
/// Updates the parameters of a single bare metal node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_builder(
            client,
            &args.name,
            &args.allowMissing,
            &args.updateMask,
            &args.validateOnly,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_builder(client, &args.resource)?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_set_iam_policy_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_builder(client, &args.resource)?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_test_iam_permissions_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:unenroll
/// Unenrolls a bare metal node pool from Anthos On-Prem API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    etag: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:unenroll",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:unenroll
/// Unenrolls a bare metal node pool from Anthos On-Prem API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:unenroll
/// Unenrolls a bare metal node pool from Anthos On-Prem API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsUnenrollArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}:unenroll
/// Unenrolls a bare metal node pool from Anthos On-Prem API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsUnenrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_builder(
            client,
            &args.name,
            &args.allowMissing,
            &args.etag,
            &args.validateOnly,
        )?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_unenroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_builder(client, &args.name)?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/bareMetalNodePools/{bareMetalNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_builder(client, &args.name, &args.filter, &args.pageSize, &args.pageToken, &args.returnPartialSuccess)?;
    gkeonprem_projects_locations_bare_metal_clusters_bare_metal_node_pools_operations_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_operations_get_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_operations_get_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_operations_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_operations_get_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_operations_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_operations_get_builder(
        client, &args.name,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_bare_metal_clusters_operations_list_execute()` to send, or `gkeonprem_projects_locations_bare_metal_clusters_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_bare_metal_clusters_operations_list_execute()` or `gkeonprem_projects_locations_bare_metal_clusters_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_bare_metal_clusters_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_operations_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_bare_metal_clusters_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_bare_metal_clusters_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_bare_metal_clusters_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_bare_metal_clusters_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsBareMetalClustersOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/bareMetalClusters/{bareMetalClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_bare_metal_clusters_operations_list_builder()` + `gkeonprem_projects_locations_bare_metal_clusters_operations_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_bare_metal_clusters_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_bare_metal_clusters_operations_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsBareMetalClustersOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_bare_metal_clusters_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    gkeonprem_projects_locations_bare_metal_clusters_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_operations_cancel_execute()` to send, or `gkeonprem_projects_locations_operations_cancel` for simplest API.

pub fn gkeonprem_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_operations_cancel_execute()` or `gkeonprem_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_operations_cancel_builder()` + `gkeonprem_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_operations_cancel_builder(client, &args.name)?;
    gkeonprem_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_operations_delete_execute()` to send, or `gkeonprem_projects_locations_operations_delete` for simplest API.

pub fn gkeonprem_projects_locations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_operations_delete_execute()` or `gkeonprem_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_operations_delete_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_operations_delete_builder()` + `gkeonprem_projects_locations_operations_delete_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_operations_delete_builder(client, &args.name)?;
    gkeonprem_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_operations_get_execute()` to send, or `gkeonprem_projects_locations_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_operations_get_execute()` or `gkeonprem_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_operations_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_operations_get_builder()` + `gkeonprem_projects_locations_operations_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_operations_get_builder(client, &args.name)?;
    gkeonprem_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_operations_list_execute()` to send, or `gkeonprem_projects_locations_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_operations_list_execute()` or `gkeonprem_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_operations_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_operations_list_builder()` + `gkeonprem_projects_locations_operations_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    gkeonprem_projects_locations_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Creates a new VMware admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_create_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_create` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    allowPreflightFailure: &Option<String>,
    skipValidations: &Option<String>,
    validateOnly: &Option<String>,
    vmwareAdminClusterId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareAdminClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowPreflightFailure.as_ref() {
        query_parts.push(format!("allowPreflightFailure={}", val));
    }
    if let Some(val) = skipValidations.as_ref() {
        query_parts.push(format!("skipValidations={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }
    if let Some(val) = vmwareAdminClusterId.as_ref() {
        query_parts.push(format!("vmwareAdminClusterId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Creates a new VMware admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_admin_clusters_create_execute()` or `gkeonprem_projects_locations_vmware_admin_clusters_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Creates a new VMware admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_admin_clusters_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_create_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_admin_clusters_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_admin_clusters_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_admin_clusters_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareAdminClustersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: allowPreflightFailure
    pub allowPreflightFailure: Option<String>,
    /// Query parameter: skipValidations
    pub skipValidations: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
    /// Query parameter: vmwareAdminClusterId
    pub vmwareAdminClusterId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Creates a new VMware admin cluster in a given project and location. The API needs to be combined with creating a bootstrap cluster to work.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_create_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_create_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_create(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareAdminClustersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_create_builder(
        client,
        &args.parent,
        &args.allowPreflightFailure,
        &args.skipValidations,
        &args.validateOnly,
        &args.vmwareAdminClusterId,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_create_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters:enroll
/// Enrolls an existing VMware admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_enroll_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_enroll` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_enroll_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareAdminClusters:enroll",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters:enroll
/// Enrolls an existing VMware admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_admin_clusters_enroll_execute()` or `gkeonprem_projects_locations_vmware_admin_clusters_enroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_enroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters:enroll
/// Enrolls an existing VMware admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_admin_clusters_enroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_enroll_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_admin_clusters_enroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_admin_clusters_enroll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_admin_clusters_enroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareAdminClustersEnrollArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters:enroll
/// Enrolls an existing VMware admin cluster to the Anthos On-Prem API within a given project and location. Through enrollment, an existing admin cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster will be expected to be performed through the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_enroll_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_enroll_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_enroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_enroll(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareAdminClustersEnrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_admin_clusters_enroll_builder(client, &args.parent)?;
    gkeonprem_projects_locations_vmware_admin_clusters_enroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Gets details of a single VMware admin cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_get_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_get` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Gets details of a single VMware admin cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_admin_clusters_get_execute()` or `gkeonprem_projects_locations_vmware_admin_clusters_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VmwareAdminCluster>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VmwareAdminCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Gets details of a single VMware admin cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_admin_clusters_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_admin_clusters_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmwareAdminCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_admin_clusters_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_admin_clusters_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareAdminClustersGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Gets details of a single VMware admin cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_get_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareAdminClustersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmwareAdminCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_get_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.view,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_execute()` or `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareAdminClustersGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareAdminClustersGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Lists VMware admin clusters in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_list_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_list` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    allowMissing: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareAdminClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Lists VMware admin clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_admin_clusters_list_execute()` or `gkeonprem_projects_locations_vmware_admin_clusters_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListVmwareAdminClustersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVmwareAdminClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Lists VMware admin clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_admin_clusters_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_admin_clusters_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVmwareAdminClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_admin_clusters_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_admin_clusters_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareAdminClustersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters
/// Lists VMware admin clusters in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_list_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareAdminClustersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVmwareAdminClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_list_builder(
        client,
        &args.parent,
        &args.allowMissing,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Updates the parameters of a single VMware admin cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_patch_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_patch` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    skipValidations: &Option<String>,
    updateMask: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = skipValidations.as_ref() {
        query_parts.push(format!("skipValidations={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Updates the parameters of a single VMware admin cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_admin_clusters_patch_execute()` or `gkeonprem_projects_locations_vmware_admin_clusters_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Updates the parameters of a single VMware admin cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_admin_clusters_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_patch_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_admin_clusters_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_admin_clusters_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_admin_clusters_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareAdminClustersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: skipValidations
    pub skipValidations: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}
/// Updates the parameters of a single VMware admin cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_patch_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_patch_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_patch(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareAdminClustersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_patch_builder(
        client,
        &args.name,
        &args.skipValidations,
        &args.updateMask,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_execute()` or `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareAdminClustersSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareAdminClustersSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_execute()` or `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareAdminClustersTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareAdminClustersTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_test_iam_permissions_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:unenroll
/// Unenrolls an existing VMware admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_unenroll` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_unenroll_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    etag: &Option<String>,
    ignoreErrors: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:unenroll",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = ignoreErrors.as_ref() {
        query_parts.push(format!("ignoreErrors={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:unenroll
/// Unenrolls an existing VMware admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_execute()` or `gkeonprem_projects_locations_vmware_admin_clusters_unenroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_unenroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:unenroll
/// Unenrolls an existing VMware admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_admin_clusters_unenroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_admin_clusters_unenroll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_admin_clusters_unenroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareAdminClustersUnenrollArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: ignoreErrors
    pub ignoreErrors: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}:unenroll
/// Unenrolls an existing VMware admin cluster from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or its clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_unenroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_unenroll(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareAdminClustersUnenrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_unenroll_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.etag,
        &args.ignoreErrors,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_unenroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_execute()` or `gkeonprem_projects_locations_vmware_admin_clusters_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_admin_clusters_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_admin_clusters_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareAdminClustersOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareAdminClustersOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_operations_get_builder(
        client, &args.name,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_execute()` to send, or `gkeonprem_projects_locations_vmware_admin_clusters_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_execute()` or `gkeonprem_projects_locations_vmware_admin_clusters_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_admin_clusters_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_admin_clusters_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareAdminClustersOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareAdminClusters/{vmwareAdminClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_builder()` + `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_admin_clusters_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_admin_clusters_operations_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareAdminClustersOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_admin_clusters_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    gkeonprem_projects_locations_vmware_admin_clusters_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Creates a new VMware user cluster in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_create_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_create` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    allowPreflightFailure: &Option<String>,
    skipValidations: &Option<String>,
    validateOnly: &Option<String>,
    vmwareClusterId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowPreflightFailure.as_ref() {
        query_parts.push(format!("allowPreflightFailure={}", val));
    }
    if let Some(val) = skipValidations.as_ref() {
        query_parts.push(format!("skipValidations={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }
    if let Some(val) = vmwareClusterId.as_ref() {
        query_parts.push(format!("vmwareClusterId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Creates a new VMware user cluster in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_create_execute()` or `gkeonprem_projects_locations_vmware_clusters_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Creates a new VMware user cluster in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_create_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: allowPreflightFailure
    pub allowPreflightFailure: Option<String>,
    /// Query parameter: skipValidations
    pub skipValidations: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
    /// Query parameter: vmwareClusterId
    pub vmwareClusterId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Creates a new VMware user cluster in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_create_builder()` + `gkeonprem_projects_locations_vmware_clusters_create_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_create(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_create_builder(
        client,
        &args.parent,
        &args.allowPreflightFailure,
        &args.skipValidations,
        &args.validateOnly,
        &args.vmwareClusterId,
    )?;
    gkeonprem_projects_locations_vmware_clusters_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Deletes a single VMware Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_delete_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_delete` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    etag: &Option<String>,
    force: &Option<String>,
    ignoreErrors: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = ignoreErrors.as_ref() {
        query_parts.push(format!("ignoreErrors={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Deletes a single VMware Cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_delete_execute()` or `gkeonprem_projects_locations_vmware_clusters_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Deletes a single VMware Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_delete_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: force
    pub force: Option<String>,
    /// Query parameter: ignoreErrors
    pub ignoreErrors: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Deletes a single VMware Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_delete_builder()` + `gkeonprem_projects_locations_vmware_clusters_delete_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_delete(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_delete_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.etag,
        &args.force,
        &args.ignoreErrors,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_clusters_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:enroll
/// Enrolls an existing VMware user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_enroll_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_enroll` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_enroll_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters:enroll",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:enroll
/// Enrolls an existing VMware user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_enroll_execute()` or `gkeonprem_projects_locations_vmware_clusters_enroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_enroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:enroll
/// Enrolls an existing VMware user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_enroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_enroll_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_enroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_enroll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_enroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersEnrollArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:enroll
/// Enrolls an existing VMware user cluster and its node pools to the Anthos On-Prem API within a given project and location. Through enrollment, an existing cluster will become Anthos On-Prem API managed. The corresponding GCP resources will be created and all future modifications to the cluster `and/or` its node pools will be expected to be performed through the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_enroll_builder()` + `gkeonprem_projects_locations_vmware_clusters_enroll_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_enroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_enroll(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersEnrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_enroll_builder(client, &args.parent)?;
    gkeonprem_projects_locations_vmware_clusters_enroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Gets details of a single VMware Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_get_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_get` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Gets details of a single VMware Cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_get_execute()` or `gkeonprem_projects_locations_vmware_clusters_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VmwareCluster>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VmwareCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Gets details of a single VMware Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmwareCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Gets details of a single VMware Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_get_builder()` + `gkeonprem_projects_locations_vmware_clusters_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmwareCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_get_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.view,
    )?;
    gkeonprem_projects_locations_vmware_clusters_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_get_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_execute()` or `gkeonprem_projects_locations_vmware_clusters_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_builder()` + `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_get_iam_policy(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    gkeonprem_projects_locations_vmware_clusters_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Lists VMware Clusters in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_list_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_list` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    allowMissing: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Lists VMware Clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_list_execute()` or `gkeonprem_projects_locations_vmware_clusters_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListVmwareClustersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVmwareClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Lists VMware Clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVmwareClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters
/// Lists VMware Clusters in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_list_builder()` + `gkeonprem_projects_locations_vmware_clusters_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVmwareClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_list_builder(
        client,
        &args.parent,
        &args.allowMissing,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    gkeonprem_projects_locations_vmware_clusters_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Updates the parameters of a single VMware cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_patch_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_patch` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    skipValidations: &Option<String>,
    updateMask: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = skipValidations.as_ref() {
        query_parts.push(format!("skipValidations={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Updates the parameters of a single VMware cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_patch_execute()` or `gkeonprem_projects_locations_vmware_clusters_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Updates the parameters of a single VMware cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_patch_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: skipValidations
    pub skipValidations: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}
/// Updates the parameters of a single VMware cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_patch_builder()` + `gkeonprem_projects_locations_vmware_clusters_patch_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_patch(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_patch_builder(
        client,
        &args.name,
        &args.skipValidations,
        &args.updateMask,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_clusters_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:queryVersionConfig
/// Queries the VMware user cluster version config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_query_version_config_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_query_version_config` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_query_version_config_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    createConfig_adminClusterMembership: &Option<String>,
    createConfig_adminClusterName: &Option<String>,
    upgradeConfig_clusterName: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters:queryVersionConfig",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = createConfig_adminClusterMembership.as_ref() {
        query_parts.push(format!("createConfig.adminClusterMembership={}", val));
    }
    if let Some(val) = createConfig_adminClusterName.as_ref() {
        query_parts.push(format!("createConfig.adminClusterName={}", val));
    }
    if let Some(val) = upgradeConfig_clusterName.as_ref() {
        query_parts.push(format!("upgradeConfig.clusterName={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:queryVersionConfig
/// Queries the VMware user cluster version config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_query_version_config_execute()` or `gkeonprem_projects_locations_vmware_clusters_query_version_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_query_version_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_query_version_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryVmwareVersionConfigResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryVmwareVersionConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:queryVersionConfig
/// Queries the VMware user cluster version config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_query_version_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_query_version_config_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_query_version_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_query_version_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_query_version_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryVmwareVersionConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_query_version_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_query_version_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersQueryVersionConfigArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: createConfig_adminClusterMembership
    pub createConfig_adminClusterMembership: Option<String>,
    /// Query parameter: createConfig_adminClusterName
    pub createConfig_adminClusterName: Option<String>,
    /// Query parameter: upgradeConfig_clusterName
    pub upgradeConfig_clusterName: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters:queryVersionConfig
/// Queries the VMware user cluster version config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_query_version_config_builder()` + `gkeonprem_projects_locations_vmware_clusters_query_version_config_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_query_version_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_query_version_config(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersQueryVersionConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryVmwareVersionConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_query_version_config_builder(
        client,
        &args.parent,
        &args.createConfig_adminClusterMembership,
        &args.createConfig_adminClusterName,
        &args.upgradeConfig_clusterName,
    )?;
    gkeonprem_projects_locations_vmware_clusters_query_version_config_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_set_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_execute()` or `gkeonprem_projects_locations_vmware_clusters_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_builder()` + `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_set_iam_policy(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    gkeonprem_projects_locations_vmware_clusters_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_execute()` or `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_builder()` + `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    gkeonprem_projects_locations_vmware_clusters_test_iam_permissions_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:unenroll
/// Unenrolls an existing VMware user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or UI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_unenroll_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_unenroll` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_unenroll_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    etag: &Option<String>,
    force: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:unenroll",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:unenroll
/// Unenrolls an existing VMware user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or UI.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_unenroll_execute()` or `gkeonprem_projects_locations_vmware_clusters_unenroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_unenroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:unenroll
/// Unenrolls an existing VMware user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or UI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_unenroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_unenroll_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_unenroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_unenroll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_unenroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersUnenrollArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: force
    pub force: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}:unenroll
/// Unenrolls an existing VMware user cluster and its node pools from the Anthos On-Prem API within a given project and location. Unenrollment removes the Cloud reference to the cluster without modifying the underlying OnPrem Resources. Clusters and node pools will continue to run; however, they will no longer be accessible through the Anthos On-Prem API or UI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_unenroll_builder()` + `gkeonprem_projects_locations_vmware_clusters_unenroll_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_unenroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_unenroll(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersUnenrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_unenroll_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.etag,
        &args.force,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_clusters_unenroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_operations_get_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_operations_get_execute()` or `gkeonprem_projects_locations_vmware_clusters_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_operations_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_operations_get_builder()` + `gkeonprem_projects_locations_vmware_clusters_operations_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_operations_get_builder(client, &args.name)?;
    gkeonprem_projects_locations_vmware_clusters_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_operations_list_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_operations_list_execute()` or `gkeonprem_projects_locations_vmware_clusters_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_operations_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_operations_list_builder()` + `gkeonprem_projects_locations_vmware_clusters_operations_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_operations_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    gkeonprem_projects_locations_vmware_clusters_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Creates a new VMware node pool in a given project, location and VMWare cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    validateOnly: &Option<String>,
    vmwareNodePoolId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }
    if let Some(val) = vmwareNodePoolId.as_ref() {
        query_parts.push(format!("vmwareNodePoolId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Creates a new VMware node pool in a given project, location and VMWare cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_execute()` or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Creates a new VMware node pool in a given project, location and VMWare cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
    /// Query parameter: vmwareNodePoolId
    pub vmwareNodePoolId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Creates a new VMware node pool in a given project, location and VMWare cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_builder(
        client,
        &args.parent,
        &args.validateOnly,
        &args.vmwareNodePoolId,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Deletes a single VMware node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    etag: &Option<String>,
    ignoreErrors: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = ignoreErrors.as_ref() {
        query_parts.push(format!("ignoreErrors={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Deletes a single VMware node pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_execute()` or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Deletes a single VMware node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: ignoreErrors
    pub ignoreErrors: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Deletes a single VMware node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.etag,
        &args.ignoreErrors,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools:enroll
/// Enrolls a VMware node pool to Anthos On-Prem API
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools:enroll",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools:enroll
/// Enrolls a VMware node pool to Anthos On-Prem API
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_execute()` or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools:enroll
/// Enrolls a VMware node pool to Anthos On-Prem API
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsEnrollArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools:enroll
/// Enrolls a VMware node pool to Anthos On-Prem API
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsEnrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_builder(
        client,
        &args.parent,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_enroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Gets details of a single VMware node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Gets details of a single VMware node pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_execute()` or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VmwareNodePool>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VmwareNodePool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Gets details of a single VMware node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmwareNodePool>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Gets details of a single VMware node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmwareNodePool>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_builder(
        client, &args.name, &args.view,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_execute()` or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_builder(
            client,
            &args.resource,
            &args.options_requestedPolicyVersion,
        )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Lists VMware node pools in a given project, location and VMWare cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Lists VMware node pools in a given project, location and VMWare cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_execute()` or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListVmwareNodePoolsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVmwareNodePoolsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Lists VMware node pools in a given project, location and VMWare cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVmwareNodePoolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools
/// Lists VMware node pools in a given project, location and VMWare cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVmwareNodePoolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Updates the parameters of a single VMware node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Updates the parameters of a single VMware node pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_execute()` or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Updates the parameters of a single VMware node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}
/// Updates the parameters of a single VMware node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_builder(
        client,
        &args.name,
        &args.updateMask,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_execute()` or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_builder(
            client,
            &args.resource,
        )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_execute()` or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_builder(client, &args.resource)?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_test_iam_permissions_execute(
        builder,
    )
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:unenroll
/// Unenrolls a VMware node pool to Anthos On-Prem API
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    etag: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:unenroll",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:unenroll
/// Unenrolls a VMware node pool to Anthos On-Prem API
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_execute()` or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:unenroll
/// Unenrolls a VMware node pool to Anthos On-Prem API
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsUnenrollArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}:unenroll
/// Unenrolls a VMware node pool to Anthos On-Prem API
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsUnenrollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.etag,
        &args.validateOnly,
    )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_unenroll_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_execute()` or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_builder(
            client, &args.name,
        )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_execute()` to send, or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list` for simplest API.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gkeonprem.googleapis.com/v1/projects/{}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_execute()` or `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_task()`.
/// For the simplest API, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/vmwareClusters/{vmwareClustersId}/vmwareNodePools/{vmwareNodePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_builder()` + `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_execute()`.
/// For task-level control, use `gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list(
    client: &SimpleHttpClient,
    args: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_builder(
            client,
            &args.name,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
            &args.returnPartialSuccess,
        )?;
    gkeonprem_projects_locations_vmware_clusters_vmware_node_pools_operations_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with GkeonpremProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &GkeonpremProjectsLocationsGetArgs) -> String {
        format!("gcp::gkeonprem::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with GkeonpremProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &GkeonpremProjectsLocationsListArgs) -> String {
        format!("gcp::gkeonprem::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalAdminClustersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalAdminClustersCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalAdminClustersCreateArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalAdminClustersEnrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalAdminClustersEnrollArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalAdminClustersEnrollArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BareMetalAdminCluster
// =============================================================================

/// ResourceIdentifier implementation for BareMetalAdminCluster with GkeonpremProjectsLocationsBareMetalAdminClustersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalAdminClustersGetArgs>
    for BareMetalAdminCluster
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalAdminClustersGetArgs,
    ) -> String {
        format!("gcp::gkeonprem::BareMetalAdminCluster/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::BareMetalAdminCluster"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with GkeonpremProjectsLocationsBareMetalAdminClustersGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalAdminClustersGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalAdminClustersGetIamPolicyArgs,
    ) -> String {
        format!("gcp::gkeonprem::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBareMetalAdminClustersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBareMetalAdminClustersResponse with GkeonpremProjectsLocationsBareMetalAdminClustersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalAdminClustersListArgs>
    for ListBareMetalAdminClustersResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalAdminClustersListArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::ListBareMetalAdminClustersResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListBareMetalAdminClustersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalAdminClustersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalAdminClustersPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalAdminClustersPatchArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryBareMetalAdminVersionConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryBareMetalAdminVersionConfigResponse with GkeonpremProjectsLocationsBareMetalAdminClustersQueryVersionConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalAdminClustersQueryVersionConfigArgs>
    for QueryBareMetalAdminVersionConfigResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalAdminClustersQueryVersionConfigArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::QueryBareMetalAdminVersionConfigResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::QueryBareMetalAdminVersionConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with GkeonpremProjectsLocationsBareMetalAdminClustersSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalAdminClustersSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalAdminClustersSetIamPolicyArgs,
    ) -> String {
        format!("gcp::gkeonprem::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with GkeonpremProjectsLocationsBareMetalAdminClustersTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalAdminClustersTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalAdminClustersTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalAdminClustersUnenrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalAdminClustersUnenrollArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalAdminClustersUnenrollArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalAdminClustersOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalAdminClustersOperationsGetArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalAdminClustersOperationsGetArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with GkeonpremProjectsLocationsBareMetalAdminClustersOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalAdminClustersOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalAdminClustersOperationsListArgs,
    ) -> String {
        format!("gcp::gkeonprem::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalClustersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersCreateArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalClustersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersDeleteArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalClustersEnrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersEnrollArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersEnrollArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BareMetalCluster
// =============================================================================

/// ResourceIdentifier implementation for BareMetalCluster with GkeonpremProjectsLocationsBareMetalClustersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersGetArgs> for BareMetalCluster {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersGetArgs,
    ) -> String {
        format!("gcp::gkeonprem::BareMetalCluster/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::BareMetalCluster"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with GkeonpremProjectsLocationsBareMetalClustersGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersGetIamPolicyArgs,
    ) -> String {
        format!("gcp::gkeonprem::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBareMetalClustersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBareMetalClustersResponse with GkeonpremProjectsLocationsBareMetalClustersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersListArgs>
    for ListBareMetalClustersResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersListArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::ListBareMetalClustersResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListBareMetalClustersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalClustersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersPatchArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryBareMetalVersionConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryBareMetalVersionConfigResponse with GkeonpremProjectsLocationsBareMetalClustersQueryVersionConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersQueryVersionConfigArgs>
    for QueryBareMetalVersionConfigResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersQueryVersionConfigArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::QueryBareMetalVersionConfigResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::QueryBareMetalVersionConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with GkeonpremProjectsLocationsBareMetalClustersSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersSetIamPolicyArgs,
    ) -> String {
        format!("gcp::gkeonprem::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with GkeonpremProjectsLocationsBareMetalClustersTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalClustersUnenrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersUnenrollArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersUnenrollArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsCreateArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsDeleteArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsEnrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsEnrollArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsEnrollArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BareMetalNodePool
// =============================================================================

/// ResourceIdentifier implementation for BareMetalNodePool with GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsGetArgs>
    for BareMetalNodePool
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsGetArgs,
    ) -> String {
        format!("gcp::gkeonprem::BareMetalNodePool/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::BareMetalNodePool"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsGetIamPolicyArgs,
    > for Policy
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::gkeonprem::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBareMetalNodePoolsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBareMetalNodePoolsResponse with GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsListArgs>
    for ListBareMetalNodePoolsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsListArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::ListBareMetalNodePoolsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListBareMetalNodePoolsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsPatchArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsSetIamPolicyArgs,
    > for Policy
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::gkeonprem::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsTestIamPermissionsArgs,
    > for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsUnenrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsUnenrollArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsUnenrollArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsOperationsGetArgs,
    > for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsOperationsGetArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsOperationsListArgs,
    > for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersBareMetalNodePoolsOperationsListArgs,
    ) -> String {
        format!("gcp::gkeonprem::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsBareMetalClustersOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersOperationsGetArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersOperationsGetArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with GkeonpremProjectsLocationsBareMetalClustersOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsBareMetalClustersOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsBareMetalClustersOperationsListArgs,
    ) -> String {
        format!("gcp::gkeonprem::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with GkeonpremProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::gkeonprem::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with GkeonpremProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::gkeonprem::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &GkeonpremProjectsLocationsOperationsGetArgs) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with GkeonpremProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &GkeonpremProjectsLocationsOperationsListArgs) -> String {
        format!("gcp::gkeonprem::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareAdminClustersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareAdminClustersCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareAdminClustersCreateArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareAdminClustersEnrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareAdminClustersEnrollArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareAdminClustersEnrollArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VmwareAdminCluster
// =============================================================================

/// ResourceIdentifier implementation for VmwareAdminCluster with GkeonpremProjectsLocationsVmwareAdminClustersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareAdminClustersGetArgs>
    for VmwareAdminCluster
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareAdminClustersGetArgs,
    ) -> String {
        format!("gcp::gkeonprem::VmwareAdminCluster/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::VmwareAdminCluster"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with GkeonpremProjectsLocationsVmwareAdminClustersGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareAdminClustersGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareAdminClustersGetIamPolicyArgs,
    ) -> String {
        format!("gcp::gkeonprem::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListVmwareAdminClustersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListVmwareAdminClustersResponse with GkeonpremProjectsLocationsVmwareAdminClustersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareAdminClustersListArgs>
    for ListVmwareAdminClustersResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareAdminClustersListArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::ListVmwareAdminClustersResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListVmwareAdminClustersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareAdminClustersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareAdminClustersPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareAdminClustersPatchArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with GkeonpremProjectsLocationsVmwareAdminClustersSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareAdminClustersSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareAdminClustersSetIamPolicyArgs,
    ) -> String {
        format!("gcp::gkeonprem::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with GkeonpremProjectsLocationsVmwareAdminClustersTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareAdminClustersTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareAdminClustersTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareAdminClustersUnenrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareAdminClustersUnenrollArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareAdminClustersUnenrollArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareAdminClustersOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareAdminClustersOperationsGetArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareAdminClustersOperationsGetArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with GkeonpremProjectsLocationsVmwareAdminClustersOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareAdminClustersOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareAdminClustersOperationsListArgs,
    ) -> String {
        format!("gcp::gkeonprem::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareClustersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersCreateArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareClustersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersDeleteArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareClustersEnrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersEnrollArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersEnrollArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VmwareCluster
// =============================================================================

/// ResourceIdentifier implementation for VmwareCluster with GkeonpremProjectsLocationsVmwareClustersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersGetArgs> for VmwareCluster {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersGetArgs,
    ) -> String {
        format!("gcp::gkeonprem::VmwareCluster/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::VmwareCluster"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with GkeonpremProjectsLocationsVmwareClustersGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersGetIamPolicyArgs,
    ) -> String {
        format!("gcp::gkeonprem::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListVmwareClustersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListVmwareClustersResponse with GkeonpremProjectsLocationsVmwareClustersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersListArgs>
    for ListVmwareClustersResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersListArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::ListVmwareClustersResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListVmwareClustersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareClustersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersPatchArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryVmwareVersionConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryVmwareVersionConfigResponse with GkeonpremProjectsLocationsVmwareClustersQueryVersionConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersQueryVersionConfigArgs>
    for QueryVmwareVersionConfigResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersQueryVersionConfigArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::QueryVmwareVersionConfigResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::QueryVmwareVersionConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with GkeonpremProjectsLocationsVmwareClustersSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersSetIamPolicyArgs,
    ) -> String {
        format!("gcp::gkeonprem::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with GkeonpremProjectsLocationsVmwareClustersTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareClustersUnenrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersUnenrollArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersUnenrollArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareClustersOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersOperationsGetArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with GkeonpremProjectsLocationsVmwareClustersOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersOperationsListArgs,
    ) -> String {
        format!("gcp::gkeonprem::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsCreateArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsDeleteArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsEnrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsEnrollArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsEnrollArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VmwareNodePool
// =============================================================================

/// ResourceIdentifier implementation for VmwareNodePool with GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsGetArgs>
    for VmwareNodePool
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsGetArgs,
    ) -> String {
        format!("gcp::gkeonprem::VmwareNodePool/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::VmwareNodePool"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::gkeonprem::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListVmwareNodePoolsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListVmwareNodePoolsResponse with GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsListArgs>
    for ListVmwareNodePoolsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsListArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::ListVmwareNodePoolsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListVmwareNodePoolsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsPatchArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::gkeonprem::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsTestIamPermissionsArgs,
    > for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::gkeonprem::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsUnenrollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsUnenrollArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsUnenrollArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsOperationsGetArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsOperationsGetArgs,
    ) -> String {
        format!("gcp::gkeonprem::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &GkeonpremProjectsLocationsVmwareClustersVmwareNodePoolsOperationsListArgs,
    ) -> String {
        format!("gcp::gkeonprem::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gkeonprem::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
