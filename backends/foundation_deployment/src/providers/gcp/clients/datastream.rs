//! Auto-generated API clients for Google Cloud Platform - datastream.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}:fetchStaticIps
/// The FetchStaticIps API call exposes the static IP addresses used by Datastream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_fetch_static_ips_execute()` to send, or `datastream_projects_locations_fetch_static_ips` for simplest API.

pub fn datastream_projects_locations_fetch_static_ips_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:fetchStaticIps", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:fetchStaticIps
/// The FetchStaticIps API call exposes the static IP addresses used by Datastream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_fetch_static_ips_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_fetch_static_ips_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_fetch_static_ips_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchStaticIpsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchStaticIpsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:fetchStaticIps
/// The FetchStaticIps API call exposes the static IP addresses used by Datastream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_fetch_static_ips_builder()` + `datastream_projects_locations_fetch_static_ips_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_fetch_static_ips(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchStaticIpsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        datastream_projects_locations_fetch_static_ips_builder(client, name, pageSize, pageToken)?;
    datastream_projects_locations_fetch_static_ips_execute(builder)
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_get_execute()` to send, or `datastream_projects_locations_get` for simplest API.

pub fn datastream_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_get_builder()` + `datastream_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_get_builder(client, name)?;
    datastream_projects_locations_get_execute(builder)
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_list_execute()` to send, or `datastream_projects_locations_list` for simplest API.

pub fn datastream_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_list_builder()` + `datastream_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    datastream_projects_locations_list_execute(builder)
}

/// GET v1/{+parent}/connectionProfiles
/// Use this method to create a connection profile in a project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_connection_profiles_create_execute()` to send, or `datastream_projects_locations_connection_profiles_create` for simplest API.

pub fn datastream_projects_locations_connection_profiles_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    connectionProfileId: Option<&str>,
    force: Option<bool>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &ConnectionProfile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/connectionProfiles",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "connectionProfileId",
            connectionProfileId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "force",
            force
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/connectionProfiles
/// Use this method to create a connection profile in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_connection_profiles_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_connection_profiles_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_connection_profiles_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/connectionProfiles
/// Use this method to create a connection profile in a project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_connection_profiles_create_builder()` + `datastream_projects_locations_connection_profiles_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_connection_profiles_create(
    client: &SimpleHttpClient,
    parent: &str,
    connectionProfileId: Option<&str>,
    force: Option<bool>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &ConnectionProfile,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_connection_profiles_create_builder(
        client,
        parent,
        connectionProfileId,
        force,
        requestId,
        validateOnly,
        body,
    )?;
    datastream_projects_locations_connection_profiles_create_execute(builder)
}

/// GET v1/{+name}
/// Use this method to delete a connection profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_connection_profiles_delete_execute()` to send, or `datastream_projects_locations_connection_profiles_delete` for simplest API.

pub fn datastream_projects_locations_connection_profiles_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Use this method to delete a connection profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_connection_profiles_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_connection_profiles_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_connection_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to delete a connection profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_connection_profiles_delete_builder()` + `datastream_projects_locations_connection_profiles_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_connection_profiles_delete(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        datastream_projects_locations_connection_profiles_delete_builder(client, name, requestId)?;
    datastream_projects_locations_connection_profiles_delete_execute(builder)
}

/// GET v1/{+parent}/connectionProfiles:discover
/// Use this method to discover a connection profile. The discover API call exposes the data objects and metadata belonging to the profile. Typically, a request returns children data objects of a parent data object that's optionally supplied in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_connection_profiles_discover_execute()` to send, or `datastream_projects_locations_connection_profiles_discover` for simplest API.

pub fn datastream_projects_locations_connection_profiles_discover_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &DiscoverConnectionProfileRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/connectionProfiles:discover",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/connectionProfiles:discover
/// Use this method to discover a connection profile. The discover API call exposes the data objects and metadata belonging to the profile. Typically, a request returns children data objects of a parent data object that's optionally supplied in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_connection_profiles_discover_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_connection_profiles_discover_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_connection_profiles_discover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DiscoverConnectionProfileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiscoverConnectionProfileResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/connectionProfiles:discover
/// Use this method to discover a connection profile. The discover API call exposes the data objects and metadata belonging to the profile. Typically, a request returns children data objects of a parent data object that's optionally supplied in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_connection_profiles_discover_builder()` + `datastream_projects_locations_connection_profiles_discover_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_connection_profiles_discover(
    client: &SimpleHttpClient,
    parent: &str,
    body: &DiscoverConnectionProfileRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DiscoverConnectionProfileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        datastream_projects_locations_connection_profiles_discover_builder(client, parent, body)?;
    datastream_projects_locations_connection_profiles_discover_execute(builder)
}

/// GET v1/{+name}
/// Use this method to get details about a connection profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_connection_profiles_get_execute()` to send, or `datastream_projects_locations_connection_profiles_get` for simplest API.

pub fn datastream_projects_locations_connection_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to get details about a connection profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_connection_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_connection_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_connection_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConnectionProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConnectionProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to get details about a connection profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_connection_profiles_get_builder()` + `datastream_projects_locations_connection_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_connection_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConnectionProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_connection_profiles_get_builder(client, name)?;
    datastream_projects_locations_connection_profiles_get_execute(builder)
}

/// GET v1/{+parent}/connectionProfiles
/// Use this method to list connection profiles created in a project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_connection_profiles_list_execute()` to send, or `datastream_projects_locations_connection_profiles_list` for simplest API.

pub fn datastream_projects_locations_connection_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/connectionProfiles",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/connectionProfiles
/// Use this method to list connection profiles created in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_connection_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_connection_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_connection_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConnectionProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConnectionProfilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/connectionProfiles
/// Use this method to list connection profiles created in a project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_connection_profiles_list_builder()` + `datastream_projects_locations_connection_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_connection_profiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConnectionProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_connection_profiles_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    datastream_projects_locations_connection_profiles_list_execute(builder)
}

/// GET v1/{+name}
/// Use this method to update the parameters of a connection profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_connection_profiles_patch_execute()` to send, or `datastream_projects_locations_connection_profiles_patch` for simplest API.

pub fn datastream_projects_locations_connection_profiles_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &ConnectionProfile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "force",
            force
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Use this method to update the parameters of a connection profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_connection_profiles_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_connection_profiles_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_connection_profiles_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to update the parameters of a connection profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_connection_profiles_patch_builder()` + `datastream_projects_locations_connection_profiles_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_connection_profiles_patch(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &ConnectionProfile,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_connection_profiles_patch_builder(
        client,
        name,
        force,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    datastream_projects_locations_connection_profiles_patch_execute(builder)
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_operations_cancel_execute()` to send, or `datastream_projects_locations_operations_cancel` for simplest API.

pub fn datastream_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_operations_cancel_builder()` + `datastream_projects_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_operations_cancel_builder(client, name, body)?;
    datastream_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_operations_delete_execute()` to send, or `datastream_projects_locations_operations_delete` for simplest API.

pub fn datastream_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_operations_delete_builder()` + `datastream_projects_locations_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_operations_delete_builder(client, name)?;
    datastream_projects_locations_operations_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_operations_get_execute()` to send, or `datastream_projects_locations_operations_get` for simplest API.

pub fn datastream_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_operations_get_builder()` + `datastream_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_operations_get_builder(client, name)?;
    datastream_projects_locations_operations_get_execute(builder)
}

/// GET v1/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_operations_list_execute()` to send, or `datastream_projects_locations_operations_list` for simplest API.

pub fn datastream_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/operations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_operations_list_builder()` + `datastream_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    datastream_projects_locations_operations_list_execute(builder)
}

/// GET v1/{+parent}/privateConnections
/// Use this method to create a private connectivity configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_private_connections_create_execute()` to send, or `datastream_projects_locations_private_connections_create` for simplest API.

pub fn datastream_projects_locations_private_connections_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    force: Option<bool>,
    privateConnectionId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &PrivateConnection,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/privateConnections",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "force",
            force
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "privateConnectionId",
            privateConnectionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/privateConnections
/// Use this method to create a private connectivity configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_private_connections_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_private_connections_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/privateConnections
/// Use this method to create a private connectivity configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_private_connections_create_builder()` + `datastream_projects_locations_private_connections_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_create(
    client: &SimpleHttpClient,
    parent: &str,
    force: Option<bool>,
    privateConnectionId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &PrivateConnection,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_private_connections_create_builder(
        client,
        parent,
        force,
        privateConnectionId,
        requestId,
        validateOnly,
        body,
    )?;
    datastream_projects_locations_private_connections_create_execute(builder)
}

/// GET v1/{+name}
/// Use this method to delete a private connectivity configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_private_connections_delete_execute()` to send, or `datastream_projects_locations_private_connections_delete` for simplest API.

pub fn datastream_projects_locations_private_connections_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "force",
            force
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Use this method to delete a private connectivity configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_private_connections_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_private_connections_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to delete a private connectivity configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_private_connections_delete_builder()` + `datastream_projects_locations_private_connections_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_delete(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_private_connections_delete_builder(
        client, name, force, requestId,
    )?;
    datastream_projects_locations_private_connections_delete_execute(builder)
}

/// GET v1/{+name}
/// Use this method to get details about a private connectivity configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_private_connections_get_execute()` to send, or `datastream_projects_locations_private_connections_get` for simplest API.

pub fn datastream_projects_locations_private_connections_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to get details about a private connectivity configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_private_connections_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_private_connections_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PrivateConnection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PrivateConnection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to get details about a private connectivity configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_private_connections_get_builder()` + `datastream_projects_locations_private_connections_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PrivateConnection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_private_connections_get_builder(client, name)?;
    datastream_projects_locations_private_connections_get_execute(builder)
}

/// GET v1/{+parent}/privateConnections
/// Use this method to list private connectivity configurations in a project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_private_connections_list_execute()` to send, or `datastream_projects_locations_private_connections_list` for simplest API.

pub fn datastream_projects_locations_private_connections_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/privateConnections",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/privateConnections
/// Use this method to list private connectivity configurations in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_private_connections_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_private_connections_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPrivateConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPrivateConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/privateConnections
/// Use this method to list private connectivity configurations in a project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_private_connections_list_builder()` + `datastream_projects_locations_private_connections_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPrivateConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_private_connections_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    datastream_projects_locations_private_connections_list_execute(builder)
}

/// GET v1/{+parent}/routes
/// Use this method to create a route for a private connectivity configuration in a project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_private_connections_routes_create_execute()` to send, or `datastream_projects_locations_private_connections_routes_create` for simplest API.

pub fn datastream_projects_locations_private_connections_routes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    routeId: Option<&str>,
    body: &Route,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/routes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "routeId",
            routeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/routes
/// Use this method to create a route for a private connectivity configuration in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_private_connections_routes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_private_connections_routes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_routes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/routes
/// Use this method to create a route for a private connectivity configuration in a project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_private_connections_routes_create_builder()` + `datastream_projects_locations_private_connections_routes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_routes_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    routeId: Option<&str>,
    body: &Route,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_private_connections_routes_create_builder(
        client, parent, requestId, routeId, body,
    )?;
    datastream_projects_locations_private_connections_routes_create_execute(builder)
}

/// GET v1/{+name}
/// Use this method to delete a route.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_private_connections_routes_delete_execute()` to send, or `datastream_projects_locations_private_connections_routes_delete` for simplest API.

pub fn datastream_projects_locations_private_connections_routes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Use this method to delete a route.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_private_connections_routes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_private_connections_routes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_routes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to delete a route.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_private_connections_routes_delete_builder()` + `datastream_projects_locations_private_connections_routes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_routes_delete(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_private_connections_routes_delete_builder(
        client, name, requestId,
    )?;
    datastream_projects_locations_private_connections_routes_delete_execute(builder)
}

/// GET v1/{+name}
/// Use this method to get details about a route.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_private_connections_routes_get_execute()` to send, or `datastream_projects_locations_private_connections_routes_get` for simplest API.

pub fn datastream_projects_locations_private_connections_routes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to get details about a route.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_private_connections_routes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_private_connections_routes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_routes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Route>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Route = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to get details about a route.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_private_connections_routes_get_builder()` + `datastream_projects_locations_private_connections_routes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_routes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Route>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        datastream_projects_locations_private_connections_routes_get_builder(client, name)?;
    datastream_projects_locations_private_connections_routes_get_execute(builder)
}

/// GET v1/{+parent}/routes
/// Use this method to list routes created for a private connectivity configuration in a project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_private_connections_routes_list_execute()` to send, or `datastream_projects_locations_private_connections_routes_list` for simplest API.

pub fn datastream_projects_locations_private_connections_routes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/routes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/routes
/// Use this method to list routes created for a private connectivity configuration in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_private_connections_routes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_private_connections_routes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_routes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRoutesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRoutesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/routes
/// Use this method to list routes created for a private connectivity configuration in a project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_private_connections_routes_list_builder()` + `datastream_projects_locations_private_connections_routes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_private_connections_routes_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRoutesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_private_connections_routes_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    datastream_projects_locations_private_connections_routes_list_execute(builder)
}

/// GET v1/{+parent}/streams
/// Use this method to create a stream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_streams_create_execute()` to send, or `datastream_projects_locations_streams_create` for simplest API.

pub fn datastream_projects_locations_streams_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    force: Option<bool>,
    requestId: Option<&str>,
    streamId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Stream,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/streams", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "force",
            force
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "streamId",
            streamId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/streams
/// Use this method to create a stream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_streams_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_streams_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/streams
/// Use this method to create a stream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_streams_create_builder()` + `datastream_projects_locations_streams_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_create(
    client: &SimpleHttpClient,
    parent: &str,
    force: Option<bool>,
    requestId: Option<&str>,
    streamId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Stream,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_streams_create_builder(
        client,
        parent,
        force,
        requestId,
        streamId,
        validateOnly,
        body,
    )?;
    datastream_projects_locations_streams_create_execute(builder)
}

/// GET v1/{+name}
/// Use this method to delete a stream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_streams_delete_execute()` to send, or `datastream_projects_locations_streams_delete` for simplest API.

pub fn datastream_projects_locations_streams_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Use this method to delete a stream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_streams_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_streams_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to delete a stream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_streams_delete_builder()` + `datastream_projects_locations_streams_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_delete(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_streams_delete_builder(client, name, requestId)?;
    datastream_projects_locations_streams_delete_execute(builder)
}

/// GET v1/{+name}
/// Use this method to get details about a stream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_streams_get_execute()` to send, or `datastream_projects_locations_streams_get` for simplest API.

pub fn datastream_projects_locations_streams_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to get details about a stream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_streams_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_streams_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Stream>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Stream = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to get details about a stream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_streams_get_builder()` + `datastream_projects_locations_streams_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Stream>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_streams_get_builder(client, name)?;
    datastream_projects_locations_streams_get_execute(builder)
}

/// GET v1/{+parent}/streams
/// Use this method to list streams in a project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_streams_list_execute()` to send, or `datastream_projects_locations_streams_list` for simplest API.

pub fn datastream_projects_locations_streams_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/streams", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/streams
/// Use this method to list streams in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_streams_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_streams_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListStreamsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListStreamsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/streams
/// Use this method to list streams in a project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_streams_list_builder()` + `datastream_projects_locations_streams_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListStreamsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_streams_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    datastream_projects_locations_streams_list_execute(builder)
}

/// GET v1/{+name}
/// Use this method to update the configuration of a stream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_streams_patch_execute()` to send, or `datastream_projects_locations_streams_patch` for simplest API.

pub fn datastream_projects_locations_streams_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Stream,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "force",
            force
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Use this method to update the configuration of a stream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_streams_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_streams_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to update the configuration of a stream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_streams_patch_builder()` + `datastream_projects_locations_streams_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_patch(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Stream,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_streams_patch_builder(
        client,
        name,
        force,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    datastream_projects_locations_streams_patch_execute(builder)
}

/// GET v1/{+name}:run
/// Use this method to start, resume or recover a stream with a non default CDC strategy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_streams_run_execute()` to send, or `datastream_projects_locations_streams_run` for simplest API.

pub fn datastream_projects_locations_streams_run_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RunStreamRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:run", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:run
/// Use this method to start, resume or recover a stream with a non default CDC strategy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_streams_run_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_streams_run_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:run
/// Use this method to start, resume or recover a stream with a non default CDC strategy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_streams_run_builder()` + `datastream_projects_locations_streams_run_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_run(
    client: &SimpleHttpClient,
    name: &str,
    body: &RunStreamRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_streams_run_builder(client, name, body)?;
    datastream_projects_locations_streams_run_execute(builder)
}

/// GET v1/{+name}
/// Use this method to get details about a stream object.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_streams_objects_get_execute()` to send, or `datastream_projects_locations_streams_objects_get` for simplest API.

pub fn datastream_projects_locations_streams_objects_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to get details about a stream object.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_streams_objects_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_streams_objects_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_objects_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StreamObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StreamObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Use this method to get details about a stream object.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_streams_objects_get_builder()` + `datastream_projects_locations_streams_objects_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_objects_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StreamObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_streams_objects_get_builder(client, name)?;
    datastream_projects_locations_streams_objects_get_execute(builder)
}

/// GET v1/{+parent}/objects
/// Use this method to list the objects of a specific stream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_streams_objects_list_execute()` to send, or `datastream_projects_locations_streams_objects_list` for simplest API.

pub fn datastream_projects_locations_streams_objects_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/objects", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/objects
/// Use this method to list the objects of a specific stream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_streams_objects_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_streams_objects_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_objects_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListStreamObjectsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListStreamObjectsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/objects
/// Use this method to list the objects of a specific stream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_streams_objects_list_builder()` + `datastream_projects_locations_streams_objects_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_objects_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListStreamObjectsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_streams_objects_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    datastream_projects_locations_streams_objects_list_execute(builder)
}

/// GET v1/{+parent}/objects:lookup
/// Use this method to look up a stream object by its source object identifier.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_streams_objects_lookup_execute()` to send, or `datastream_projects_locations_streams_objects_lookup` for simplest API.

pub fn datastream_projects_locations_streams_objects_lookup_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LookupStreamObjectRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/objects:lookup", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/objects:lookup
/// Use this method to look up a stream object by its source object identifier.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_streams_objects_lookup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_streams_objects_lookup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_objects_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StreamObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StreamObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/objects:lookup
/// Use this method to look up a stream object by its source object identifier.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_streams_objects_lookup_builder()` + `datastream_projects_locations_streams_objects_lookup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_objects_lookup(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LookupStreamObjectRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StreamObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        datastream_projects_locations_streams_objects_lookup_builder(client, parent, body)?;
    datastream_projects_locations_streams_objects_lookup_execute(builder)
}

/// GET v1/{+object}:startBackfillJob
/// Use this method to start a backfill job for the specified stream object.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_streams_objects_start_backfill_job_execute()` to send, or `datastream_projects_locations_streams_objects_start_backfill_job` for simplest API.

pub fn datastream_projects_locations_streams_objects_start_backfill_job_builder(
    client: &SimpleHttpClient,
    object: &str,
    body: &StartBackfillJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+object}:startBackfillJob",
        object,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+object}:startBackfillJob
/// Use this method to start a backfill job for the specified stream object.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_streams_objects_start_backfill_job_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_streams_objects_start_backfill_job_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_objects_start_backfill_job_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StartBackfillJobResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StartBackfillJobResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+object}:startBackfillJob
/// Use this method to start a backfill job for the specified stream object.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_streams_objects_start_backfill_job_builder()` + `datastream_projects_locations_streams_objects_start_backfill_job_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_objects_start_backfill_job(
    client: &SimpleHttpClient,
    object: &str,
    body: &StartBackfillJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StartBackfillJobResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_streams_objects_start_backfill_job_builder(
        client, object, body,
    )?;
    datastream_projects_locations_streams_objects_start_backfill_job_execute(builder)
}

/// GET v1/{+object}:stopBackfillJob
/// Use this method to stop a backfill job for the specified stream object.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastream_projects_locations_streams_objects_stop_backfill_job_execute()` to send, or `datastream_projects_locations_streams_objects_stop_backfill_job` for simplest API.

pub fn datastream_projects_locations_streams_objects_stop_backfill_job_builder(
    client: &SimpleHttpClient,
    object: &str,
    body: &StopBackfillJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+object}:stopBackfillJob",
        object,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+object}:stopBackfillJob
/// Use this method to stop a backfill job for the specified stream object.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datastream_projects_locations_streams_objects_stop_backfill_job_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datastream_projects_locations_streams_objects_stop_backfill_job_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_objects_stop_backfill_job_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StopBackfillJobResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StopBackfillJobResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+object}:stopBackfillJob
/// Use this method to stop a backfill job for the specified stream object.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastream_projects_locations_streams_objects_stop_backfill_job_builder()` + `datastream_projects_locations_streams_objects_stop_backfill_job_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastream_projects_locations_streams_objects_stop_backfill_job(
    client: &SimpleHttpClient,
    object: &str,
    body: &StopBackfillJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StopBackfillJobResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastream_projects_locations_streams_objects_stop_backfill_job_builder(
        client, object, body,
    )?;
    datastream_projects_locations_streams_objects_stop_backfill_job_execute(builder)
}
