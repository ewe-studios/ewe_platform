//! Auto-generated API clients for gcp/pubsub.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/schemas
/// Creates a schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsub_projects_schemas_create_execute()` to send, or `pubsub_projects_schemas_create` for simplest API.

pub fn pubsub_projects_schemas_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    schemaId: &Option<String>,
    body: &Schema,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://pubsub.googleapis.com/v1/projects/{}/schemas",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = schemaId.as_ref() {
        query_parts.push(format!("schemaId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/schemas
/// Creates a schema.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsub_projects_schemas_create_execute()` or `pubsub_projects_schemas_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsub_projects_schemas_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsub_projects_schemas_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Schema>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Schema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/schemas
/// Creates a schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsub_projects_schemas_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsub_projects_schemas_create_task()`.
/// For the simplest API, use `pubsub_projects_schemas_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsub_projects_schemas_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsub_projects_schemas_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Schema>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsub_projects_schemas_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsub_projects_schemas_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubProjectsSchemasCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: schemaId
    pub schemaId: Option<String>,
    /// Request body.
    pub body: Schema,
}

/// GET v1/projects/{projectsId}/schemas
/// Creates a schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsub_projects_schemas_create_builder()` + `pubsub_projects_schemas_create_execute()`.
/// For task-level control, use `pubsub_projects_schemas_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsub_projects_schemas_create(
    client: &SimpleHttpClient,
    args: &PubsubProjectsSchemasCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Schema>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        pubsub_projects_schemas_create_builder(client, &args.parent, &args.schemaId, &args.body)?;
    pubsub_projects_schemas_create_execute(builder)
}

/// GET v1/projects/{projectsId}/schemas:validate
/// Validates a schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsub_projects_schemas_validate_execute()` to send, or `pubsub_projects_schemas_validate` for simplest API.

pub fn pubsub_projects_schemas_validate_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &ValidateSchemaRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://pubsub.googleapis.com/v1/projects/{}/schemas:validate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/schemas:validate
/// Validates a schema.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsub_projects_schemas_validate_execute()` or `pubsub_projects_schemas_validate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsub_projects_schemas_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsub_projects_schemas_validate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ValidateSchemaResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateSchemaResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/schemas:validate
/// Validates a schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsub_projects_schemas_validate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsub_projects_schemas_validate_task()`.
/// For the simplest API, use `pubsub_projects_schemas_validate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsub_projects_schemas_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsub_projects_schemas_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ValidateSchemaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsub_projects_schemas_validate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsub_projects_schemas_validate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubProjectsSchemasValidateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: ValidateSchemaRequest,
}

/// GET v1/projects/{projectsId}/schemas:validate
/// Validates a schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsub_projects_schemas_validate_builder()` + `pubsub_projects_schemas_validate_execute()`.
/// For task-level control, use `pubsub_projects_schemas_validate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsub_projects_schemas_validate(
    client: &SimpleHttpClient,
    args: &PubsubProjectsSchemasValidateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ValidateSchemaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsub_projects_schemas_validate_builder(client, &args.parent, &args.body)?;
    pubsub_projects_schemas_validate_execute(builder)
}

/// GET v1/projects/{projectsId}/schemas:validateMessage
/// Validates a message against a schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsub_projects_schemas_validate_message_execute()` to send, or `pubsub_projects_schemas_validate_message` for simplest API.

pub fn pubsub_projects_schemas_validate_message_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &ValidateMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://pubsub.googleapis.com/v1/projects/{}/schemas:validateMessage",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/schemas:validateMessage
/// Validates a message against a schema.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsub_projects_schemas_validate_message_execute()` or `pubsub_projects_schemas_validate_message`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsub_projects_schemas_validate_message_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsub_projects_schemas_validate_message_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ValidateMessageResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/schemas:validateMessage
/// Validates a message against a schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsub_projects_schemas_validate_message_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsub_projects_schemas_validate_message_task()`.
/// For the simplest API, use `pubsub_projects_schemas_validate_message()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsub_projects_schemas_validate_message_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsub_projects_schemas_validate_message_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ValidateMessageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsub_projects_schemas_validate_message_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsub_projects_schemas_validate_message`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubProjectsSchemasValidateMessageArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: ValidateMessageRequest,
}

/// GET v1/projects/{projectsId}/schemas:validateMessage
/// Validates a message against a schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsub_projects_schemas_validate_message_builder()` + `pubsub_projects_schemas_validate_message_execute()`.
/// For task-level control, use `pubsub_projects_schemas_validate_message_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsub_projects_schemas_validate_message(
    client: &SimpleHttpClient,
    args: &PubsubProjectsSchemasValidateMessageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ValidateMessageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        pubsub_projects_schemas_validate_message_builder(client, &args.parent, &args.body)?;
    pubsub_projects_schemas_validate_message_execute(builder)
}

/// GET v1/projects/{projectsId}/snapshots
/// Lists the existing snapshots. Snapshots are used in [Seek]( <https://cloud.google.`com/pubsub/docs/replay-overview`>) operations, which allow you to manage message acknowledgments in bulk. That is, you can set the acknowledgment state of messages in an existing subscription to the state captured by a snapshot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsub_projects_snapshots_list_execute()` to send, or `pubsub_projects_snapshots_list` for simplest API.

pub fn pubsub_projects_snapshots_list_builder(
    client: &SimpleHttpClient,
    project: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://pubsub.googleapis.com/v1/projects/{}/snapshots",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/snapshots
/// Lists the existing snapshots. Snapshots are used in [Seek]( <https://cloud.google.`com/pubsub/docs/replay-overview`>) operations, which allow you to manage message acknowledgments in bulk. That is, you can set the acknowledgment state of messages in an existing subscription to the state captured by a snapshot.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsub_projects_snapshots_list_execute()` or `pubsub_projects_snapshots_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsub_projects_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsub_projects_snapshots_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSnapshotsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSnapshotsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/snapshots
/// Lists the existing snapshots. Snapshots are used in [Seek]( <https://cloud.google.`com/pubsub/docs/replay-overview`>) operations, which allow you to manage message acknowledgments in bulk. That is, you can set the acknowledgment state of messages in an existing subscription to the state captured by a snapshot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsub_projects_snapshots_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsub_projects_snapshots_list_task()`.
/// For the simplest API, use `pubsub_projects_snapshots_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsub_projects_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsub_projects_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnapshotsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsub_projects_snapshots_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsub_projects_snapshots_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubProjectsSnapshotsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/snapshots
/// Lists the existing snapshots. Snapshots are used in [Seek]( <https://cloud.google.`com/pubsub/docs/replay-overview`>) operations, which allow you to manage message acknowledgments in bulk. That is, you can set the acknowledgment state of messages in an existing subscription to the state captured by a snapshot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsub_projects_snapshots_list_builder()` + `pubsub_projects_snapshots_list_execute()`.
/// For task-level control, use `pubsub_projects_snapshots_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsub_projects_snapshots_list(
    client: &SimpleHttpClient,
    args: &PubsubProjectsSnapshotsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnapshotsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsub_projects_snapshots_list_builder(
        client,
        &args.project,
        &args.pageSize,
        &args.pageToken,
    )?;
    pubsub_projects_snapshots_list_execute(builder)
}

/// GET v1/projects/{projectsId}/subscriptions
/// Lists matching subscriptions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsub_projects_subscriptions_list_execute()` to send, or `pubsub_projects_subscriptions_list` for simplest API.

pub fn pubsub_projects_subscriptions_list_builder(
    client: &SimpleHttpClient,
    project: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://pubsub.googleapis.com/v1/projects/{}/subscriptions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/subscriptions
/// Lists matching subscriptions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsub_projects_subscriptions_list_execute()` or `pubsub_projects_subscriptions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsub_projects_subscriptions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsub_projects_subscriptions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSubscriptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSubscriptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/subscriptions
/// Lists matching subscriptions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsub_projects_subscriptions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsub_projects_subscriptions_list_task()`.
/// For the simplest API, use `pubsub_projects_subscriptions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsub_projects_subscriptions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsub_projects_subscriptions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSubscriptionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsub_projects_subscriptions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsub_projects_subscriptions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubProjectsSubscriptionsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/subscriptions
/// Lists matching subscriptions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsub_projects_subscriptions_list_builder()` + `pubsub_projects_subscriptions_list_execute()`.
/// For task-level control, use `pubsub_projects_subscriptions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsub_projects_subscriptions_list(
    client: &SimpleHttpClient,
    args: &PubsubProjectsSubscriptionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSubscriptionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsub_projects_subscriptions_list_builder(
        client,
        &args.project,
        &args.pageSize,
        &args.pageToken,
    )?;
    pubsub_projects_subscriptions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/topics
/// Lists matching topics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsub_projects_topics_list_execute()` to send, or `pubsub_projects_topics_list` for simplest API.

pub fn pubsub_projects_topics_list_builder(
    client: &SimpleHttpClient,
    project: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://pubsub.googleapis.com/v1/projects/{}/topics",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/topics
/// Lists matching topics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsub_projects_topics_list_execute()` or `pubsub_projects_topics_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsub_projects_topics_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsub_projects_topics_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTopicsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTopicsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/topics
/// Lists matching topics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsub_projects_topics_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsub_projects_topics_list_task()`.
/// For the simplest API, use `pubsub_projects_topics_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsub_projects_topics_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsub_projects_topics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTopicsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsub_projects_topics_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsub_projects_topics_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubProjectsTopicsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/topics
/// Lists matching topics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsub_projects_topics_list_builder()` + `pubsub_projects_topics_list_execute()`.
/// For task-level control, use `pubsub_projects_topics_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsub_projects_topics_list(
    client: &SimpleHttpClient,
    args: &PubsubProjectsTopicsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTopicsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsub_projects_topics_list_builder(
        client,
        &args.project,
        &args.pageSize,
        &args.pageToken,
    )?;
    pubsub_projects_topics_list_execute(builder)
}
