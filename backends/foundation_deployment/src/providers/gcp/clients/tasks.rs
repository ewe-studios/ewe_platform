//! Auto-generated API clients for gcp/tasks.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Deletes the authenticated user's specified task list. If the list contains assigned tasks, both the assigned tasks and the original tasks in the assignment surface (Docs, Chat Spaces) are deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasklists_delete_execute()` to send, or `tasks_tasklists_delete` for simplest API.

pub fn tasks_tasklists_delete_builder(
    client: &SimpleHttpClient,
    tasklist: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tasks.googleapis.com/tasks/v1/users/@me/lists/{}",
        tasklist,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Deletes the authenticated user's specified task list. If the list contains assigned tasks, both the assigned tasks and the original tasks in the assignment surface (Docs, Chat Spaces) are deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tasks_tasklists_delete_execute()` or `tasks_tasklists_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tasks_tasklists_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Deletes the authenticated user's specified task list. If the list contains assigned tasks, both the assigned tasks and the original tasks in the assignment surface (Docs, Chat Spaces) are deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tasks_tasklists_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tasks_tasklists_delete_task()`.
/// For the simplest API, use `tasks_tasklists_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tasks_tasklists_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tasks_tasklists_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tasks_tasklists_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tasks_tasklists_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TasksTasklistsDeleteArgs {
    /// Path parameter: tasklist
    pub tasklist: String,
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Deletes the authenticated user's specified task list. If the list contains assigned tasks, both the assigned tasks and the original tasks in the assignment surface (Docs, Chat Spaces) are deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasklists_delete_builder()` + `tasks_tasklists_delete_execute()`.
/// For task-level control, use `tasks_tasklists_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_delete(
    client: &SimpleHttpClient,
    args: &TasksTasklistsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasklists_delete_builder(client, &args.tasklist)?;
    tasks_tasklists_delete_execute(builder)
}

/// GET tasks/v1/users/@me/lists
/// Creates a new task list and adds it to the authenticated user's task lists. A user can have up to 2000 lists at a time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasklists_insert_execute()` to send, or `tasks_tasklists_insert` for simplest API.

pub fn tasks_tasklists_insert_builder(
    client: &SimpleHttpClient,
    body: &TaskList,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://tasks.googleapis.com/tasks/v1/users/@me/lists",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/users/@me/lists
/// Creates a new task list and adds it to the authenticated user's task lists. A user can have up to 2000 lists at a time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tasks_tasklists_insert_execute()` or `tasks_tasklists_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tasks_tasklists_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TaskList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaskList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET tasks/v1/users/@me/lists
/// Creates a new task list and adds it to the authenticated user's task lists. A user can have up to 2000 lists at a time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tasks_tasklists_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tasks_tasklists_insert_task()`.
/// For the simplest API, use `tasks_tasklists_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tasks_tasklists_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tasks_tasklists_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tasks_tasklists_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tasks_tasklists_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TasksTasklistsInsertArgs {
    /// Request body.
    pub body: TaskList,
}

/// GET tasks/v1/users/@me/lists
/// Creates a new task list and adds it to the authenticated user's task lists. A user can have up to 2000 lists at a time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasklists_insert_builder()` + `tasks_tasklists_insert_execute()`.
/// For task-level control, use `tasks_tasklists_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_insert(
    client: &SimpleHttpClient,
    args: &TasksTasklistsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasklists_insert_builder(client, &args.body)?;
    tasks_tasklists_insert_execute(builder)
}

/// GET tasks/v1/lists/{tasklist}/clear
/// Clears all completed tasks from the specified task list. The affected tasks will be marked as 'hidden' and no longer be returned by default when retrieving all tasks for a task list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasks_clear_execute()` to send, or `tasks_tasks_clear` for simplest API.

pub fn tasks_tasks_clear_builder(
    client: &SimpleHttpClient,
    tasklist: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tasks.googleapis.com/tasks/v1/lists/{}/clear",
        tasklist,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tasks/v1/lists/{tasklist}/clear
/// Clears all completed tasks from the specified task list. The affected tasks will be marked as 'hidden' and no longer be returned by default when retrieving all tasks for a task list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tasks_tasks_clear_execute()` or `tasks_tasks_clear`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tasks_tasks_clear_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_clear_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET tasks/v1/lists/{tasklist}/clear
/// Clears all completed tasks from the specified task list. The affected tasks will be marked as 'hidden' and no longer be returned by default when retrieving all tasks for a task list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tasks_tasks_clear_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tasks_tasks_clear_task()`.
/// For the simplest API, use `tasks_tasks_clear()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tasks_tasks_clear_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tasks_tasks_clear_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tasks_tasks_clear_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tasks_tasks_clear`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TasksTasksClearArgs {
    /// Path parameter: tasklist
    pub tasklist: String,
}

/// GET tasks/v1/lists/{tasklist}/clear
/// Clears all completed tasks from the specified task list. The affected tasks will be marked as 'hidden' and no longer be returned by default when retrieving all tasks for a task list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasks_clear_builder()` + `tasks_tasks_clear_execute()`.
/// For task-level control, use `tasks_tasks_clear_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_clear(
    client: &SimpleHttpClient,
    args: &TasksTasksClearArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasks_clear_builder(client, &args.tasklist)?;
    tasks_tasks_clear_execute(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Deletes the specified task from the task list. If the task is assigned, both the assigned task and the original task (in Docs, Chat Spaces) are deleted. To delete the assigned task only, navigate to the assignment surface and unassign the task from there.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasks_delete_execute()` to send, or `tasks_tasks_delete` for simplest API.

pub fn tasks_tasks_delete_builder(
    client: &SimpleHttpClient,
    tasklist: &String,
    task: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tasks.googleapis.com/tasks/v1/lists/{}/tasks/{}",
        tasklist, task,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Deletes the specified task from the task list. If the task is assigned, both the assigned task and the original task (in Docs, Chat Spaces) are deleted. To delete the assigned task only, navigate to the assignment surface and unassign the task from there.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tasks_tasks_delete_execute()` or `tasks_tasks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tasks_tasks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Deletes the specified task from the task list. If the task is assigned, both the assigned task and the original task (in Docs, Chat Spaces) are deleted. To delete the assigned task only, navigate to the assignment surface and unassign the task from there.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tasks_tasks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tasks_tasks_delete_task()`.
/// For the simplest API, use `tasks_tasks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tasks_tasks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tasks_tasks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tasks_tasks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tasks_tasks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TasksTasksDeleteArgs {
    /// Path parameter: tasklist
    pub tasklist: String,
    /// Path parameter: task
    pub task: String,
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Deletes the specified task from the task list. If the task is assigned, both the assigned task and the original task (in Docs, Chat Spaces) are deleted. To delete the assigned task only, navigate to the assignment surface and unassign the task from there.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasks_delete_builder()` + `tasks_tasks_delete_execute()`.
/// For task-level control, use `tasks_tasks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_delete(
    client: &SimpleHttpClient,
    args: &TasksTasksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasks_delete_builder(client, &args.tasklist, &args.task)?;
    tasks_tasks_delete_execute(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks
/// Creates a new task on the specified task list. Tasks assigned from Docs or Chat Spaces cannot be inserted from Tasks Public API; they can only be created by assigning them from Docs or Chat Spaces. A user can have up to 20,000 non-hidden tasks per list and up to 100,000 tasks in total at a time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasks_insert_execute()` to send, or `tasks_tasks_insert` for simplest API.

pub fn tasks_tasks_insert_builder(
    client: &SimpleHttpClient,
    tasklist: &String,
    parent: &Option<String>,
    previous: &Option<String>,
    body: &Task,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tasks.googleapis.com/tasks/v1/lists/{}/tasks",
        tasklist,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent.as_ref() {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = previous.as_ref() {
        query_parts.push(format!("previous={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/lists/{tasklist}/tasks
/// Creates a new task on the specified task list. Tasks assigned from Docs or Chat Spaces cannot be inserted from Tasks Public API; they can only be created by assigning them from Docs or Chat Spaces. A user can have up to 20,000 non-hidden tasks per list and up to 100,000 tasks in total at a time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tasks_tasks_insert_execute()` or `tasks_tasks_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tasks_tasks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Task>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Task = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET tasks/v1/lists/{tasklist}/tasks
/// Creates a new task on the specified task list. Tasks assigned from Docs or Chat Spaces cannot be inserted from Tasks Public API; they can only be created by assigning them from Docs or Chat Spaces. A user can have up to 20,000 non-hidden tasks per list and up to 100,000 tasks in total at a time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tasks_tasks_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tasks_tasks_insert_task()`.
/// For the simplest API, use `tasks_tasks_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tasks_tasks_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tasks_tasks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tasks_tasks_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tasks_tasks_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TasksTasksInsertArgs {
    /// Path parameter: tasklist
    pub tasklist: String,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: previous
    pub previous: Option<String>,
    /// Request body.
    pub body: Task,
}

/// GET tasks/v1/lists/{tasklist}/tasks
/// Creates a new task on the specified task list. Tasks assigned from Docs or Chat Spaces cannot be inserted from Tasks Public API; they can only be created by assigning them from Docs or Chat Spaces. A user can have up to 20,000 non-hidden tasks per list and up to 100,000 tasks in total at a time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasks_insert_builder()` + `tasks_tasks_insert_execute()`.
/// For task-level control, use `tasks_tasks_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_insert(
    client: &SimpleHttpClient,
    args: &TasksTasksInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasks_insert_builder(
        client,
        &args.tasklist,
        &args.parent,
        &args.previous,
        &args.body,
    )?;
    tasks_tasks_insert_execute(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}/move
/// Moves the specified task to another position in the destination task list. If the destination list is not specified, the task is moved within its current list. This can include putting it as a child task under a new parent `and/or` move it to a different position among its sibling tasks. A user can have up to 2,000 subtasks per task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasks_move_execute()` to send, or `tasks_tasks_move` for simplest API.

pub fn tasks_tasks_move_builder(
    client: &SimpleHttpClient,
    tasklist: &String,
    task: &String,
    destinationTasklist: &Option<String>,
    parent: &Option<String>,
    previous: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tasks.googleapis.com/tasks/v1/lists/{}/tasks/{}/move",
        tasklist, task,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = destinationTasklist.as_ref() {
        query_parts.push(format!("destinationTasklist={}", val));
    }
    if let Some(val) = parent.as_ref() {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = previous.as_ref() {
        query_parts.push(format!("previous={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}/move
/// Moves the specified task to another position in the destination task list. If the destination list is not specified, the task is moved within its current list. This can include putting it as a child task under a new parent `and/or` move it to a different position among its sibling tasks. A user can have up to 2,000 subtasks per task.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tasks_tasks_move_execute()` or `tasks_tasks_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tasks_tasks_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Task>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Task = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}/move
/// Moves the specified task to another position in the destination task list. If the destination list is not specified, the task is moved within its current list. This can include putting it as a child task under a new parent `and/or` move it to a different position among its sibling tasks. A user can have up to 2,000 subtasks per task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tasks_tasks_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tasks_tasks_move_task()`.
/// For the simplest API, use `tasks_tasks_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tasks_tasks_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tasks_tasks_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tasks_tasks_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tasks_tasks_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TasksTasksMoveArgs {
    /// Path parameter: tasklist
    pub tasklist: String,
    /// Path parameter: task
    pub task: String,
    /// Query parameter: destinationTasklist
    pub destinationTasklist: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: previous
    pub previous: Option<String>,
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}/move
/// Moves the specified task to another position in the destination task list. If the destination list is not specified, the task is moved within its current list. This can include putting it as a child task under a new parent `and/or` move it to a different position among its sibling tasks. A user can have up to 2,000 subtasks per task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasks_move_builder()` + `tasks_tasks_move_execute()`.
/// For task-level control, use `tasks_tasks_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_move(
    client: &SimpleHttpClient,
    args: &TasksTasksMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasks_move_builder(
        client,
        &args.tasklist,
        &args.task,
        &args.destinationTasklist,
        &args.parent,
        &args.previous,
    )?;
    tasks_tasks_move_execute(builder)
}
