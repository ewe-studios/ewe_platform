//! Auto-generated API clients for gcp/tasks.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/tasks`

#![cfg(feature = "gcp/tasks")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Deletes the authenticated user's specified task list. If the list contains assigned tasks, both the assigned tasks and the original tasks in the assignment surface (Docs, Chat Spaces) are deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasklists_delete_execute()` to send, or `tasks_tasklists_delete` for simplest API.

pub fn tasks_tasklists_delete_builder(
    client: &SimpleHttpClient,
    tasklist: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://tasks.googleapis.com/tasks/v1/users/@me/lists/{}",
        tasklist,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Deletes the authenticated user's specified task list. If the list contains assigned tasks, both the assigned tasks and the original tasks in the assignment surface (Docs, Chat Spaces) are deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasklists_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasklists_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Deletes the authenticated user's specified task list. If the list contains assigned tasks, both the assigned tasks and the original tasks in the assignment surface (Docs, Chat Spaces) are deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasklists_delete_builder()` + `tasks_tasklists_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_delete(
    client: &SimpleHttpClient,
    tasklist: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasklists_delete_builder(client, tasklist)?;
    tasks_tasklists_delete_execute(builder)
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Returns the authenticated user's specified task list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasklists_get_execute()` to send, or `tasks_tasklists_get` for simplest API.

pub fn tasks_tasklists_get_builder(
    client: &SimpleHttpClient,
    tasklist: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://tasks.googleapis.com/tasks/v1/users/@me/lists/{}",
        tasklist,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Returns the authenticated user's specified task list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasklists_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasklists_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaskList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Returns the authenticated user's specified task list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasklists_get_builder()` + `tasks_tasklists_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_get(
    client: &SimpleHttpClient,
    tasklist: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasklists_get_builder(client, tasklist)?;
    tasks_tasklists_get_execute(builder)
}

/// GET tasks/v1/users/@me/lists
/// Creates a new task list and adds it to the authenticated user's task lists. A user can have up to 2000 lists at a time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasklists_insert_execute()` to send, or `tasks_tasklists_insert` for simplest API.

pub fn tasks_tasklists_insert_builder(
    client: &SimpleHttpClient,
    body: &TaskList,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://tasks.googleapis.com/tasks/v1/users/@me/lists",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/users/@me/lists
/// Creates a new task list and adds it to the authenticated user's task lists. A user can have up to 2000 lists at a time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasklists_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasklists_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaskList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/users/@me/lists
/// Creates a new task list and adds it to the authenticated user's task lists. A user can have up to 2000 lists at a time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasklists_insert_builder()` + `tasks_tasklists_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_insert(
    client: &SimpleHttpClient,
    body: &TaskList,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasklists_insert_builder(client, body)?;
    tasks_tasklists_insert_execute(builder)
}

/// GET tasks/v1/users/@me/lists
/// Returns all the authenticated user's task lists. A user can have up to 2000 lists at a time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasklists_list_execute()` to send, or `tasks_tasklists_list` for simplest API.

pub fn tasks_tasklists_list_builder(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://tasks.googleapis.com/tasks/v1/users/@me/lists",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tasks/v1/users/@me/lists
/// Returns all the authenticated user's task lists. A user can have up to 2000 lists at a time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasklists_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasklists_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaskLists>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaskLists = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/users/@me/lists
/// Returns all the authenticated user's task lists. A user can have up to 2000 lists at a time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasklists_list_builder()` + `tasks_tasklists_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_list(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaskLists>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasklists_list_builder(client, maxResults, pageToken)?;
    tasks_tasklists_list_execute(builder)
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Updates the authenticated user's specified task list. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasklists_patch_execute()` to send, or `tasks_tasklists_patch` for simplest API.

pub fn tasks_tasklists_patch_builder(
    client: &SimpleHttpClient,
    tasklist: &str,
    body: &TaskList,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://tasks.googleapis.com/tasks/v1/users/@me/lists/{}",
        tasklist,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Updates the authenticated user's specified task list. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasklists_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasklists_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaskList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Updates the authenticated user's specified task list. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasklists_patch_builder()` + `tasks_tasklists_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_patch(
    client: &SimpleHttpClient,
    tasklist: &str,
    body: &TaskList,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasklists_patch_builder(client, tasklist, body)?;
    tasks_tasklists_patch_execute(builder)
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Updates the authenticated user's specified task list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasklists_update_execute()` to send, or `tasks_tasklists_update` for simplest API.

pub fn tasks_tasklists_update_builder(
    client: &SimpleHttpClient,
    tasklist: &str,
    body: &TaskList,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://tasks.googleapis.com/tasks/v1/users/@me/lists/{}",
        tasklist,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Updates the authenticated user's specified task list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasklists_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasklists_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaskList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/users/@me/lists/{tasklist}
/// Updates the authenticated user's specified task list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasklists_update_builder()` + `tasks_tasklists_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasklists_update(
    client: &SimpleHttpClient,
    tasklist: &str,
    body: &TaskList,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasklists_update_builder(client, tasklist, body)?;
    tasks_tasklists_update_execute(builder)
}

/// GET tasks/v1/lists/{tasklist}/clear
/// Clears all completed tasks from the specified task list. The affected tasks will be marked as 'hidden' and no longer be returned by default when retrieving all tasks for a task list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasks_clear_execute()` to send, or `tasks_tasks_clear` for simplest API.

pub fn tasks_tasks_clear_builder(
    client: &SimpleHttpClient,
    tasklist: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://tasks.googleapis.com/tasks/v1/lists/{}/clear",
        tasklist,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tasks/v1/lists/{tasklist}/clear
/// Clears all completed tasks from the specified task list. The affected tasks will be marked as 'hidden' and no longer be returned by default when retrieving all tasks for a task list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasks_clear_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasks_clear_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_clear_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/lists/{tasklist}/clear
/// Clears all completed tasks from the specified task list. The affected tasks will be marked as 'hidden' and no longer be returned by default when retrieving all tasks for a task list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasks_clear_builder()` + `tasks_tasks_clear_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_clear(
    client: &SimpleHttpClient,
    tasklist: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasks_clear_builder(client, tasklist)?;
    tasks_tasks_clear_execute(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Deletes the specified task from the task list. If the task is assigned, both the assigned task and the original task (in Docs, Chat Spaces) are deleted. To delete the assigned task only, navigate to the assignment surface and unassign the task from there.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasks_delete_execute()` to send, or `tasks_tasks_delete` for simplest API.

pub fn tasks_tasks_delete_builder(
    client: &SimpleHttpClient,
    tasklist: &str,
    task: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://tasks.googleapis.com/tasks/v1/lists/{}/tasks/{}",
        tasklist, task,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Deletes the specified task from the task list. If the task is assigned, both the assigned task and the original task (in Docs, Chat Spaces) are deleted. To delete the assigned task only, navigate to the assignment surface and unassign the task from there.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Deletes the specified task from the task list. If the task is assigned, both the assigned task and the original task (in Docs, Chat Spaces) are deleted. To delete the assigned task only, navigate to the assignment surface and unassign the task from there.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasks_delete_builder()` + `tasks_tasks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_delete(
    client: &SimpleHttpClient,
    tasklist: &str,
    task: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasks_delete_builder(client, tasklist, task)?;
    tasks_tasks_delete_execute(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Returns the specified task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasks_get_execute()` to send, or `tasks_tasks_get` for simplest API.

pub fn tasks_tasks_get_builder(
    client: &SimpleHttpClient,
    tasklist: &str,
    task: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://tasks.googleapis.com/tasks/v1/lists/{}/tasks/{}",
        tasklist, task,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Returns the specified task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Task = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Returns the specified task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasks_get_builder()` + `tasks_tasks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_get(
    client: &SimpleHttpClient,
    tasklist: &str,
    task: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasks_get_builder(client, tasklist, task)?;
    tasks_tasks_get_execute(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks
/// Creates a new task on the specified task list. Tasks assigned from Docs or Chat Spaces cannot be inserted from Tasks Public API; they can only be created by assigning them from Docs or Chat Spaces. A user can have up to 20,000 non-hidden tasks per list and up to 100,000 tasks in total at a time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasks_insert_execute()` to send, or `tasks_tasks_insert` for simplest API.

pub fn tasks_tasks_insert_builder(
    client: &SimpleHttpClient,
    tasklist: &str,
    parent: Option<&str>,
    previous: Option<&str>,
    body: &Task,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://tasks.googleapis.com/tasks/v1/lists/{}/tasks",
        tasklist,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = previous {
        query_parts.push(format!("previous={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/lists/{tasklist}/tasks
/// Creates a new task on the specified task list. Tasks assigned from Docs or Chat Spaces cannot be inserted from Tasks Public API; they can only be created by assigning them from Docs or Chat Spaces. A user can have up to 20,000 non-hidden tasks per list and up to 100,000 tasks in total at a time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasks_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasks_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Task = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/lists/{tasklist}/tasks
/// Creates a new task on the specified task list. Tasks assigned from Docs or Chat Spaces cannot be inserted from Tasks Public API; they can only be created by assigning them from Docs or Chat Spaces. A user can have up to 20,000 non-hidden tasks per list and up to 100,000 tasks in total at a time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasks_insert_builder()` + `tasks_tasks_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_insert(
    client: &SimpleHttpClient,
    tasklist: &str,
    parent: Option<&str>,
    previous: Option<&str>,
    body: &Task,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasks_insert_builder(client, tasklist, parent, previous, body)?;
    tasks_tasks_insert_execute(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks
/// Returns all tasks in the specified task list. Doesn't return assigned tasks by default (from Docs, Chat Spaces). A user can have up to 20,000 non-hidden tasks per list and up to 100,000 tasks in total at a time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasks_list_execute()` to send, or `tasks_tasks_list` for simplest API.

pub fn tasks_tasks_list_builder(
    client: &SimpleHttpClient,
    tasklist: &str,
    completedMax: Option<&str>,
    completedMin: Option<&str>,
    dueMax: Option<&str>,
    dueMin: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    showAssigned: Option<bool>,
    showCompleted: Option<bool>,
    showDeleted: Option<bool>,
    showHidden: Option<bool>,
    updatedMin: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://tasks.googleapis.com/tasks/v1/lists/{}/tasks",
        tasklist,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = completedMax {
        query_parts.push(format!("completedMax={}", val));
    }
    if let Some(val) = completedMin {
        query_parts.push(format!("completedMin={}", val));
    }
    if let Some(val) = dueMax {
        query_parts.push(format!("dueMax={}", val));
    }
    if let Some(val) = dueMin {
        query_parts.push(format!("dueMin={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showAssigned {
        query_parts.push(format!("showAssigned={}", val));
    }
    if let Some(val) = showCompleted {
        query_parts.push(format!("showCompleted={}", val));
    }
    if let Some(val) = showDeleted {
        query_parts.push(format!("showDeleted={}", val));
    }
    if let Some(val) = showHidden {
        query_parts.push(format!("showHidden={}", val));
    }
    if let Some(val) = updatedMin {
        query_parts.push(format!("updatedMin={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks
/// Returns all tasks in the specified task list. Doesn't return assigned tasks by default (from Docs, Chat Spaces). A user can have up to 20,000 non-hidden tasks per list and up to 100,000 tasks in total at a time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tasks>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tasks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/lists/{tasklist}/tasks
/// Returns all tasks in the specified task list. Doesn't return assigned tasks by default (from Docs, Chat Spaces). A user can have up to 20,000 non-hidden tasks per list and up to 100,000 tasks in total at a time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasks_list_builder()` + `tasks_tasks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_list(
    client: &SimpleHttpClient,
    tasklist: &str,
    completedMax: Option<&str>,
    completedMin: Option<&str>,
    dueMax: Option<&str>,
    dueMin: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    showAssigned: Option<bool>,
    showCompleted: Option<bool>,
    showDeleted: Option<bool>,
    showHidden: Option<bool>,
    updatedMin: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tasks>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasks_list_builder(
        client,
        tasklist,
        completedMax,
        completedMin,
        dueMax,
        dueMin,
        maxResults,
        pageToken,
        showAssigned,
        showCompleted,
        showDeleted,
        showHidden,
        updatedMin,
    )?;
    tasks_tasks_list_execute(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}/move
/// Moves the specified task to another position in the destination task list. If the destination list is not specified, the task is moved within its current list. This can include putting it as a child task under a new parent `and/or` move it to a different position among its sibling tasks. A user can have up to 2,000 subtasks per task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasks_move_execute()` to send, or `tasks_tasks_move` for simplest API.

pub fn tasks_tasks_move_builder(
    client: &SimpleHttpClient,
    tasklist: &str,
    task: &str,
    destinationTasklist: Option<&str>,
    parent: Option<&str>,
    previous: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://tasks.googleapis.com/tasks/v1/lists/{}/tasks/{}/move",
        tasklist, task,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = destinationTasklist {
        query_parts.push(format!("destinationTasklist={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = previous {
        query_parts.push(format!("previous={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}/move
/// Moves the specified task to another position in the destination task list. If the destination list is not specified, the task is moved within its current list. This can include putting it as a child task under a new parent `and/or` move it to a different position among its sibling tasks. A user can have up to 2,000 subtasks per task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasks_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasks_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Task = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}/move
/// Moves the specified task to another position in the destination task list. If the destination list is not specified, the task is moved within its current list. This can include putting it as a child task under a new parent `and/or` move it to a different position among its sibling tasks. A user can have up to 2,000 subtasks per task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasks_move_builder()` + `tasks_tasks_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_move(
    client: &SimpleHttpClient,
    tasklist: &str,
    task: &str,
    destinationTasklist: Option<&str>,
    parent: Option<&str>,
    previous: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasks_move_builder(
        client,
        tasklist,
        task,
        destinationTasklist,
        parent,
        previous,
    )?;
    tasks_tasks_move_execute(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Updates the specified task. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasks_patch_execute()` to send, or `tasks_tasks_patch` for simplest API.

pub fn tasks_tasks_patch_builder(
    client: &SimpleHttpClient,
    tasklist: &str,
    task: &str,
    body: &Task,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://tasks.googleapis.com/tasks/v1/lists/{}/tasks/{}",
        tasklist, task,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Updates the specified task. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Task = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Updates the specified task. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasks_patch_builder()` + `tasks_tasks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_patch(
    client: &SimpleHttpClient,
    tasklist: &str,
    task: &str,
    body: &Task,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasks_patch_builder(client, tasklist, task, body)?;
    tasks_tasks_patch_execute(builder)
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Updates the specified task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tasks_tasks_update_execute()` to send, or `tasks_tasks_update` for simplest API.

pub fn tasks_tasks_update_builder(
    client: &SimpleHttpClient,
    tasklist: &str,
    task: &str,
    body: &Task,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://tasks.googleapis.com/tasks/v1/lists/{}/tasks/{}",
        tasklist, task,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Updates the specified task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `tasks_tasks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from tasks_tasks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Task = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tasks/v1/lists/{tasklist}/tasks/{task}
/// Updates the specified task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tasks_tasks_update_builder()` + `tasks_tasks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tasks_tasks_update(
    client: &SimpleHttpClient,
    tasklist: &str,
    task: &str,
    body: &Task,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tasks_tasks_update_builder(client, tasklist, task, body)?;
    tasks_tasks_update_execute(builder)
}
