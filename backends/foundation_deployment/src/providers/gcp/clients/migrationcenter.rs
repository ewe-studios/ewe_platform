//! Auto-generated API clients for gcp/migrationcenter.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_get_execute()` to send, or `migrationcenter_projects_locations_get` for simplest API.

pub fn migrationcenter_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_get_execute()` or `migrationcenter_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_get_builder()` + `migrationcenter_projects_locations_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_get_builder(client, &args.name)?;
    migrationcenter_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Gets the details of regional settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_get_settings_execute()` to send, or `migrationcenter_projects_locations_get_settings` for simplest API.

pub fn migrationcenter_projects_locations_get_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/settings",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Gets the details of regional settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_get_settings_execute()` or `migrationcenter_projects_locations_get_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_get_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Gets the details of regional settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_get_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_get_settings_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_get_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_get_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_get_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsGetSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Gets the details of regional settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_get_settings_builder()` + `migrationcenter_projects_locations_get_settings_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_get_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_get_settings(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsGetSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_get_settings_builder(client, &args.name)?;
    migrationcenter_projects_locations_get_settings_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_list_execute()` to send, or `migrationcenter_projects_locations_list` for simplest API.

pub fn migrationcenter_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_list_execute()` or `migrationcenter_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_list_builder()` + `migrationcenter_projects_locations_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_list_builder(
        client,
        &args.name,
        args.extraLocationTypes.as_deref(),
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    migrationcenter_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Updates the regional-level project settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_update_settings_execute()` to send, or `migrationcenter_projects_locations_update_settings` for simplest API.

pub fn migrationcenter_projects_locations_update_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Settings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/settings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Updates the regional-level project settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_update_settings_execute()` or `migrationcenter_projects_locations_update_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_update_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Updates the regional-level project settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_update_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_update_settings_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_update_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_update_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_update_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsUpdateSettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Settings,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Updates the regional-level project settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_update_settings_builder()` + `migrationcenter_projects_locations_update_settings_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_update_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_update_settings(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsUpdateSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_update_settings_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_update_settings_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:aggregateValues
/// Aggregates the requested fields based on provided function.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_aggregate_values_execute()` to send, or `migrationcenter_projects_locations_assets_aggregate_values` for simplest API.

pub fn migrationcenter_projects_locations_assets_aggregate_values_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AggregateAssetsValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assets:aggregateValues",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:aggregateValues
/// Aggregates the requested fields based on provided function.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_aggregate_values_execute()` or `migrationcenter_projects_locations_assets_aggregate_values`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_aggregate_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_aggregate_values_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AggregateAssetsValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AggregateAssetsValuesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:aggregateValues
/// Aggregates the requested fields based on provided function.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_aggregate_values_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_aggregate_values_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_aggregate_values()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_aggregate_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_aggregate_values_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AggregateAssetsValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_aggregate_values_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_aggregate_values`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsAggregateValuesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: AggregateAssetsValuesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:aggregateValues
/// Aggregates the requested fields based on provided function.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_aggregate_values_builder()` + `migrationcenter_projects_locations_assets_aggregate_values_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_aggregate_values_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_aggregate_values(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsAggregateValuesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AggregateAssetsValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_assets_aggregate_values_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    migrationcenter_projects_locations_assets_aggregate_values_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:batchDelete
/// Deletes list of Assets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_batch_delete_execute()` to send, or `migrationcenter_projects_locations_assets_batch_delete` for simplest API.

pub fn migrationcenter_projects_locations_assets_batch_delete_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchDeleteAssetsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assets:batchDelete",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:batchDelete
/// Deletes list of Assets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_batch_delete_execute()` or `migrationcenter_projects_locations_assets_batch_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_batch_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:batchDelete
/// Deletes list of Assets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_batch_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_batch_delete_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_batch_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_batch_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_batch_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsBatchDeleteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: BatchDeleteAssetsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:batchDelete
/// Deletes list of Assets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_batch_delete_builder()` + `migrationcenter_projects_locations_assets_batch_delete_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_batch_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_batch_delete(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsBatchDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_assets_batch_delete_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    migrationcenter_projects_locations_assets_batch_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:batchUpdate
/// Updates the parameters of a list of assets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_batch_update_execute()` to send, or `migrationcenter_projects_locations_assets_batch_update` for simplest API.

pub fn migrationcenter_projects_locations_assets_batch_update_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchUpdateAssetsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assets:batchUpdate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:batchUpdate
/// Updates the parameters of a list of assets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_batch_update_execute()` or `migrationcenter_projects_locations_assets_batch_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_batch_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BatchUpdateAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:batchUpdate
/// Updates the parameters of a list of assets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_batch_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_batch_update_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_batch_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchUpdateAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_batch_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_batch_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsBatchUpdateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: BatchUpdateAssetsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:batchUpdate
/// Updates the parameters of a list of assets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_batch_update_builder()` + `migrationcenter_projects_locations_assets_batch_update_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_batch_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_batch_update(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsBatchUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchUpdateAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_assets_batch_update_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    migrationcenter_projects_locations_assets_batch_update_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets/{assetsId}
/// Deletes an asset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_delete_execute()` to send, or `migrationcenter_projects_locations_assets_delete` for simplest API.

pub fn migrationcenter_projects_locations_assets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets/{assetsId}
/// Deletes an asset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_delete_execute()` or `migrationcenter_projects_locations_assets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets/{assetsId}
/// Deletes an asset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_delete_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets/{assetsId}
/// Deletes an asset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_delete_builder()` + `migrationcenter_projects_locations_assets_delete_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_delete(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_assets_delete_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
    )?;
    migrationcenter_projects_locations_assets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets/{assetsId}
/// Gets the details of an asset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_get_execute()` to send, or `migrationcenter_projects_locations_assets_get` for simplest API.

pub fn migrationcenter_projects_locations_assets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets/{assetsId}
/// Gets the details of an asset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_get_execute()` or `migrationcenter_projects_locations_assets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Asset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Asset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets/{assetsId}
/// Gets the details of an asset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Asset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets/{assetsId}
/// Gets the details of an asset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_get_builder()` + `migrationcenter_projects_locations_assets_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Asset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_assets_get_builder(
        client,
        &args.name,
        args.view.as_deref(),
    )?;
    migrationcenter_projects_locations_assets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets
/// Lists all the assets in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_list_execute()` to send, or `migrationcenter_projects_locations_assets_list` for simplest API.

pub fn migrationcenter_projects_locations_assets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    showHidden: Option<bool>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showHidden {
        query_parts.push(format!("showHidden={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets
/// Lists all the assets in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_list_execute()` or `migrationcenter_projects_locations_assets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets
/// Lists all the assets in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: showHidden
    pub showHidden: Option<bool>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets
/// Lists all the assets in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_list_builder()` + `migrationcenter_projects_locations_assets_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_assets_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.showHidden,
        args.view.as_deref(),
    )?;
    migrationcenter_projects_locations_assets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets/{assetsId}
/// Updates the parameters of an asset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_patch_execute()` to send, or `migrationcenter_projects_locations_assets_patch` for simplest API.

pub fn migrationcenter_projects_locations_assets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Asset,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets/{assetsId}
/// Updates the parameters of an asset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_patch_execute()` or `migrationcenter_projects_locations_assets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Asset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Asset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets/{assetsId}
/// Updates the parameters of an asset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_patch_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Asset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Asset,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets/{assetsId}
/// Updates the parameters of an asset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_patch_builder()` + `migrationcenter_projects_locations_assets_patch_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_patch(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Asset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_assets_patch_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_assets_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:reportAssetFrames
/// Reports a set of frames.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_report_asset_frames_execute()` to send, or `migrationcenter_projects_locations_assets_report_asset_frames` for simplest API.

pub fn migrationcenter_projects_locations_assets_report_asset_frames_builder(
    client: &SimpleHttpClient,
    parent: &str,
    source: Option<&str>,
    body: &Frames,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assets:reportAssetFrames",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = source {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:reportAssetFrames
/// Reports a set of frames.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_report_asset_frames_execute()` or `migrationcenter_projects_locations_assets_report_asset_frames`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_report_asset_frames_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_report_asset_frames_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ReportAssetFramesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportAssetFramesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:reportAssetFrames
/// Reports a set of frames.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_report_asset_frames_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_report_asset_frames_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_report_asset_frames()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_report_asset_frames_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_report_asset_frames_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportAssetFramesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_report_asset_frames_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_report_asset_frames`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsReportAssetFramesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: source
    pub source: Option<String>,
    /// Request body.
    pub body: Frames,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assets:reportAssetFrames
/// Reports a set of frames.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_report_asset_frames_builder()` + `migrationcenter_projects_locations_assets_report_asset_frames_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_report_asset_frames_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_report_asset_frames(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsReportAssetFramesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportAssetFramesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_assets_report_asset_frames_builder(
        client,
        &args.parent,
        args.source.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_assets_report_asset_frames_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs
/// Creates a new assets export job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_export_jobs_create_execute()` to send, or `migrationcenter_projects_locations_assets_export_jobs_create` for simplest API.

pub fn migrationcenter_projects_locations_assets_export_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    assetsExportJobId: Option<&str>,
    requestId: Option<&str>,
    body: &AssetsExportJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assetsExportJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = assetsExportJobId {
        query_parts.push(format!("assetsExportJobId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs
/// Creates a new assets export job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_export_jobs_create_execute()` or `migrationcenter_projects_locations_assets_export_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_export_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_export_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs
/// Creates a new assets export job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_export_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_export_jobs_create_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_export_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_export_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_export_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_export_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_export_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsExportJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: assetsExportJobId
    pub assetsExportJobId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: AssetsExportJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs
/// Creates a new assets export job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_export_jobs_create_builder()` + `migrationcenter_projects_locations_assets_export_jobs_create_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_export_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_export_jobs_create(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsExportJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_assets_export_jobs_create_builder(
        client,
        &args.parent,
        args.assetsExportJobId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_assets_export_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs/{assetsExportJobsId}
/// Deletes an assets export job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_export_jobs_delete_execute()` to send, or `migrationcenter_projects_locations_assets_export_jobs_delete` for simplest API.

pub fn migrationcenter_projects_locations_assets_export_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assetsExportJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs/{assetsExportJobsId}
/// Deletes an assets export job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_export_jobs_delete_execute()` or `migrationcenter_projects_locations_assets_export_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_export_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_export_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs/{assetsExportJobsId}
/// Deletes an assets export job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_export_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_export_jobs_delete_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_export_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_export_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_export_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_export_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_export_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsExportJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs/{assetsExportJobsId}
/// Deletes an assets export job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_export_jobs_delete_builder()` + `migrationcenter_projects_locations_assets_export_jobs_delete_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_export_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_export_jobs_delete(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsExportJobsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        migrationcenter_projects_locations_assets_export_jobs_delete_builder(client, &args.name)?;
    migrationcenter_projects_locations_assets_export_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs/{assetsExportJobsId}
/// Gets the details of an assets export job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_export_jobs_get_execute()` to send, or `migrationcenter_projects_locations_assets_export_jobs_get` for simplest API.

pub fn migrationcenter_projects_locations_assets_export_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assetsExportJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs/{assetsExportJobsId}
/// Gets the details of an assets export job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_export_jobs_get_execute()` or `migrationcenter_projects_locations_assets_export_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_export_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_export_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AssetsExportJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AssetsExportJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs/{assetsExportJobsId}
/// Gets the details of an assets export job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_export_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_export_jobs_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_export_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_export_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_export_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssetsExportJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_export_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_export_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsExportJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs/{assetsExportJobsId}
/// Gets the details of an assets export job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_export_jobs_get_builder()` + `migrationcenter_projects_locations_assets_export_jobs_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_export_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_export_jobs_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsExportJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssetsExportJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        migrationcenter_projects_locations_assets_export_jobs_get_builder(client, &args.name)?;
    migrationcenter_projects_locations_assets_export_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs
/// Lists all the assets export jobs in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_export_jobs_list_execute()` to send, or `migrationcenter_projects_locations_assets_export_jobs_list` for simplest API.

pub fn migrationcenter_projects_locations_assets_export_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assetsExportJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs
/// Lists all the assets export jobs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_export_jobs_list_execute()` or `migrationcenter_projects_locations_assets_export_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_export_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_export_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListAssetsExportJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssetsExportJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs
/// Lists all the assets export jobs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_export_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_export_jobs_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_export_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_export_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_export_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAssetsExportJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_export_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_export_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsExportJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs
/// Lists all the assets export jobs in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_export_jobs_list_builder()` + `migrationcenter_projects_locations_assets_export_jobs_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_export_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_export_jobs_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsExportJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAssetsExportJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_assets_export_jobs_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    migrationcenter_projects_locations_assets_export_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs/{assetsExportJobsId}:run
/// Runs an assets export job, returning an AssetsExportJobExecution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_assets_export_jobs_run_execute()` to send, or `migrationcenter_projects_locations_assets_export_jobs_run` for simplest API.

pub fn migrationcenter_projects_locations_assets_export_jobs_run_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RunAssetsExportJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/assetsExportJobs/{}:run",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs/{assetsExportJobsId}:run
/// Runs an assets export job, returning an AssetsExportJobExecution.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_assets_export_jobs_run_execute()` or `migrationcenter_projects_locations_assets_export_jobs_run`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_export_jobs_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_export_jobs_run_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs/{assetsExportJobsId}:run
/// Runs an assets export job, returning an AssetsExportJobExecution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_assets_export_jobs_run_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_assets_export_jobs_run_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_assets_export_jobs_run()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_assets_export_jobs_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_assets_export_jobs_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_assets_export_jobs_run_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_assets_export_jobs_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsAssetsExportJobsRunArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RunAssetsExportJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assetsExportJobs/{assetsExportJobsId}:run
/// Runs an assets export job, returning an AssetsExportJobExecution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_assets_export_jobs_run_builder()` + `migrationcenter_projects_locations_assets_export_jobs_run_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_assets_export_jobs_run_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_assets_export_jobs_run(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsAssetsExportJobsRunArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_assets_export_jobs_run_builder(
        client, &args.name, &args.body,
    )?;
    migrationcenter_projects_locations_assets_export_jobs_run_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients
/// Creates a new discovery client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_discovery_clients_create_execute()` to send, or `migrationcenter_projects_locations_discovery_clients_create` for simplest API.

pub fn migrationcenter_projects_locations_discovery_clients_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    discoveryClientId: Option<&str>,
    requestId: Option<&str>,
    body: &DiscoveryClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/discoveryClients",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = discoveryClientId {
        query_parts.push(format!("discoveryClientId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients
/// Creates a new discovery client.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_discovery_clients_create_execute()` or `migrationcenter_projects_locations_discovery_clients_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_discovery_clients_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_discovery_clients_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients
/// Creates a new discovery client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_discovery_clients_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_discovery_clients_create_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_discovery_clients_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_discovery_clients_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_discovery_clients_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_discovery_clients_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_discovery_clients_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsDiscoveryClientsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: discoveryClientId
    pub discoveryClientId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: DiscoveryClient,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients
/// Creates a new discovery client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_discovery_clients_create_builder()` + `migrationcenter_projects_locations_discovery_clients_create_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_discovery_clients_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_discovery_clients_create(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsDiscoveryClientsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_discovery_clients_create_builder(
        client,
        &args.parent,
        args.discoveryClientId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_discovery_clients_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}
/// Deletes a discovery client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_discovery_clients_delete_execute()` to send, or `migrationcenter_projects_locations_discovery_clients_delete` for simplest API.

pub fn migrationcenter_projects_locations_discovery_clients_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/discoveryClients/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}
/// Deletes a discovery client.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_discovery_clients_delete_execute()` or `migrationcenter_projects_locations_discovery_clients_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_discovery_clients_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_discovery_clients_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}
/// Deletes a discovery client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_discovery_clients_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_discovery_clients_delete_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_discovery_clients_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_discovery_clients_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_discovery_clients_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_discovery_clients_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_discovery_clients_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsDiscoveryClientsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}
/// Deletes a discovery client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_discovery_clients_delete_builder()` + `migrationcenter_projects_locations_discovery_clients_delete_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_discovery_clients_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_discovery_clients_delete(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsDiscoveryClientsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_discovery_clients_delete_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
    )?;
    migrationcenter_projects_locations_discovery_clients_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}
/// Gets the details of a discovery client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_discovery_clients_get_execute()` to send, or `migrationcenter_projects_locations_discovery_clients_get` for simplest API.

pub fn migrationcenter_projects_locations_discovery_clients_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/discoveryClients/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}
/// Gets the details of a discovery client.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_discovery_clients_get_execute()` or `migrationcenter_projects_locations_discovery_clients_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_discovery_clients_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_discovery_clients_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<DiscoveryClient>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiscoveryClient = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}
/// Gets the details of a discovery client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_discovery_clients_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_discovery_clients_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_discovery_clients_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_discovery_clients_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_discovery_clients_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiscoveryClient>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_discovery_clients_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_discovery_clients_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsDiscoveryClientsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}
/// Gets the details of a discovery client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_discovery_clients_get_builder()` + `migrationcenter_projects_locations_discovery_clients_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_discovery_clients_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_discovery_clients_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsDiscoveryClientsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiscoveryClient>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        migrationcenter_projects_locations_discovery_clients_get_builder(client, &args.name)?;
    migrationcenter_projects_locations_discovery_clients_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients
/// Lists all the discovery clients in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_discovery_clients_list_execute()` to send, or `migrationcenter_projects_locations_discovery_clients_list` for simplest API.

pub fn migrationcenter_projects_locations_discovery_clients_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/discoveryClients",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients
/// Lists all the discovery clients in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_discovery_clients_list_execute()` or `migrationcenter_projects_locations_discovery_clients_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_discovery_clients_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_discovery_clients_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListDiscoveryClientsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDiscoveryClientsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients
/// Lists all the discovery clients in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_discovery_clients_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_discovery_clients_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_discovery_clients_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_discovery_clients_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_discovery_clients_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDiscoveryClientsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_discovery_clients_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_discovery_clients_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsDiscoveryClientsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients
/// Lists all the discovery clients in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_discovery_clients_list_builder()` + `migrationcenter_projects_locations_discovery_clients_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_discovery_clients_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_discovery_clients_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsDiscoveryClientsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDiscoveryClientsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_discovery_clients_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    migrationcenter_projects_locations_discovery_clients_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}
/// Updates a discovery client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_discovery_clients_patch_execute()` to send, or `migrationcenter_projects_locations_discovery_clients_patch` for simplest API.

pub fn migrationcenter_projects_locations_discovery_clients_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &DiscoveryClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/discoveryClients/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}
/// Updates a discovery client.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_discovery_clients_patch_execute()` or `migrationcenter_projects_locations_discovery_clients_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_discovery_clients_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_discovery_clients_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}
/// Updates a discovery client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_discovery_clients_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_discovery_clients_patch_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_discovery_clients_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_discovery_clients_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_discovery_clients_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_discovery_clients_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_discovery_clients_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsDiscoveryClientsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: DiscoveryClient,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}
/// Updates a discovery client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_discovery_clients_patch_builder()` + `migrationcenter_projects_locations_discovery_clients_patch_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_discovery_clients_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_discovery_clients_patch(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsDiscoveryClientsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_discovery_clients_patch_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_discovery_clients_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}:sendHeartbeat
/// Sends a discovery client heartbeat. Healthy clients are expected to send heartbeats regularly (normally every few minutes).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_discovery_clients_send_heartbeat_execute()` to send, or `migrationcenter_projects_locations_discovery_clients_send_heartbeat` for simplest API.

pub fn migrationcenter_projects_locations_discovery_clients_send_heartbeat_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SendDiscoveryClientHeartbeatRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/discoveryClients/{}:sendHeartbeat",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}:sendHeartbeat
/// Sends a discovery client heartbeat. Healthy clients are expected to send heartbeats regularly (normally every few minutes).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_discovery_clients_send_heartbeat_execute()` or `migrationcenter_projects_locations_discovery_clients_send_heartbeat`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_discovery_clients_send_heartbeat_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_discovery_clients_send_heartbeat_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}:sendHeartbeat
/// Sends a discovery client heartbeat. Healthy clients are expected to send heartbeats regularly (normally every few minutes).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_discovery_clients_send_heartbeat_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_discovery_clients_send_heartbeat_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_discovery_clients_send_heartbeat()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_discovery_clients_send_heartbeat_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_discovery_clients_send_heartbeat_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_discovery_clients_send_heartbeat_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_discovery_clients_send_heartbeat`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsDiscoveryClientsSendHeartbeatArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SendDiscoveryClientHeartbeatRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveryClients/{discoveryClientsId}:sendHeartbeat
/// Sends a discovery client heartbeat. Healthy clients are expected to send heartbeats regularly (normally every few minutes).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_discovery_clients_send_heartbeat_builder()` + `migrationcenter_projects_locations_discovery_clients_send_heartbeat_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_discovery_clients_send_heartbeat_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_discovery_clients_send_heartbeat(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsDiscoveryClientsSendHeartbeatArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_discovery_clients_send_heartbeat_builder(
        client, &args.name, &args.body,
    )?;
    migrationcenter_projects_locations_discovery_clients_send_heartbeat_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:addAssets
/// Adds assets to a group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_groups_add_assets_execute()` to send, or `migrationcenter_projects_locations_groups_add_assets` for simplest API.

pub fn migrationcenter_projects_locations_groups_add_assets_builder(
    client: &SimpleHttpClient,
    group: &str,
    body: &AddAssetsToGroupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/groups/{}:addAssets",
        group,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:addAssets
/// Adds assets to a group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_groups_add_assets_execute()` or `migrationcenter_projects_locations_groups_add_assets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_add_assets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_add_assets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:addAssets
/// Adds assets to a group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_groups_add_assets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_groups_add_assets_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_groups_add_assets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_add_assets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_groups_add_assets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_groups_add_assets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_groups_add_assets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsGroupsAddAssetsArgs {
    /// Path parameter: group
    pub group: String,
    /// Request body.
    pub body: AddAssetsToGroupRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:addAssets
/// Adds assets to a group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_groups_add_assets_builder()` + `migrationcenter_projects_locations_groups_add_assets_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_groups_add_assets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_add_assets(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsGroupsAddAssetsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_groups_add_assets_builder(
        client,
        &args.group,
        &args.body,
    )?;
    migrationcenter_projects_locations_groups_add_assets_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups
/// Creates a new group in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_groups_create_execute()` to send, or `migrationcenter_projects_locations_groups_create` for simplest API.

pub fn migrationcenter_projects_locations_groups_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    groupId: Option<&str>,
    requestId: Option<&str>,
    body: &Group,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/groups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = groupId {
        query_parts.push(format!("groupId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups
/// Creates a new group in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_groups_create_execute()` or `migrationcenter_projects_locations_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups
/// Creates a new group in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_groups_create_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: groupId
    pub groupId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: Group,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups
/// Creates a new group in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_groups_create_builder()` + `migrationcenter_projects_locations_groups_create_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_create(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_groups_create_builder(
        client,
        &args.parent,
        args.groupId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_groups_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Deletes a group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_groups_delete_execute()` to send, or `migrationcenter_projects_locations_groups_delete` for simplest API.

pub fn migrationcenter_projects_locations_groups_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/groups/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Deletes a group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_groups_delete_execute()` or `migrationcenter_projects_locations_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Deletes a group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_groups_delete_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Deletes a group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_groups_delete_builder()` + `migrationcenter_projects_locations_groups_delete_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_delete(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_groups_delete_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
    )?;
    migrationcenter_projects_locations_groups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Gets the details of a group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_groups_get_execute()` to send, or `migrationcenter_projects_locations_groups_get` for simplest API.

pub fn migrationcenter_projects_locations_groups_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/groups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Gets the details of a group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_groups_get_execute()` or `migrationcenter_projects_locations_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Group = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Gets the details of a group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_groups_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Gets the details of a group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_groups_get_builder()` + `migrationcenter_projects_locations_groups_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_groups_get_builder(client, &args.name)?;
    migrationcenter_projects_locations_groups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups
/// Lists all groups in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_groups_list_execute()` to send, or `migrationcenter_projects_locations_groups_list` for simplest API.

pub fn migrationcenter_projects_locations_groups_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/groups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups
/// Lists all groups in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_groups_list_execute()` or `migrationcenter_projects_locations_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups
/// Lists all groups in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_groups_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups
/// Lists all groups in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_groups_list_builder()` + `migrationcenter_projects_locations_groups_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_groups_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    migrationcenter_projects_locations_groups_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Updates the parameters of a group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_groups_patch_execute()` to send, or `migrationcenter_projects_locations_groups_patch` for simplest API.

pub fn migrationcenter_projects_locations_groups_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Group,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/groups/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Updates the parameters of a group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_groups_patch_execute()` or `migrationcenter_projects_locations_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Updates the parameters of a group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_groups_patch_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Group,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Updates the parameters of a group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_groups_patch_builder()` + `migrationcenter_projects_locations_groups_patch_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_patch(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_groups_patch_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_groups_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:removeAssets
/// Removes assets from a group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_groups_remove_assets_execute()` to send, or `migrationcenter_projects_locations_groups_remove_assets` for simplest API.

pub fn migrationcenter_projects_locations_groups_remove_assets_builder(
    client: &SimpleHttpClient,
    group: &str,
    body: &RemoveAssetsFromGroupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/groups/{}:removeAssets",
        group,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:removeAssets
/// Removes assets from a group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_groups_remove_assets_execute()` or `migrationcenter_projects_locations_groups_remove_assets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_remove_assets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_remove_assets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:removeAssets
/// Removes assets from a group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_groups_remove_assets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_groups_remove_assets_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_groups_remove_assets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_groups_remove_assets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_groups_remove_assets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_groups_remove_assets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_groups_remove_assets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsGroupsRemoveAssetsArgs {
    /// Path parameter: group
    pub group: String,
    /// Request body.
    pub body: RemoveAssetsFromGroupRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:removeAssets
/// Removes assets from a group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_groups_remove_assets_builder()` + `migrationcenter_projects_locations_groups_remove_assets_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_groups_remove_assets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_groups_remove_assets(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsGroupsRemoveAssetsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_groups_remove_assets_builder(
        client,
        &args.group,
        &args.body,
    )?;
    migrationcenter_projects_locations_groups_remove_assets_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs
/// Creates an import job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_import_jobs_create_execute()` to send, or `migrationcenter_projects_locations_import_jobs_create` for simplest API.

pub fn migrationcenter_projects_locations_import_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    importJobId: Option<&str>,
    requestId: Option<&str>,
    body: &ImportJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/importJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = importJobId {
        query_parts.push(format!("importJobId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs
/// Creates an import job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_import_jobs_create_execute()` or `migrationcenter_projects_locations_import_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs
/// Creates an import job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_import_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_create_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_import_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_import_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_import_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_import_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsImportJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: importJobId
    pub importJobId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: ImportJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs
/// Creates an import job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_import_jobs_create_builder()` + `migrationcenter_projects_locations_import_jobs_create_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_create(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsImportJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_import_jobs_create_builder(
        client,
        &args.parent,
        args.importJobId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_import_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}
/// Deletes an import job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_import_jobs_delete_execute()` to send, or `migrationcenter_projects_locations_import_jobs_delete` for simplest API.

pub fn migrationcenter_projects_locations_import_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/importJobs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}
/// Deletes an import job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_import_jobs_delete_execute()` or `migrationcenter_projects_locations_import_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}
/// Deletes an import job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_import_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_delete_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_import_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_import_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_import_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_import_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsImportJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}
/// Deletes an import job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_import_jobs_delete_builder()` + `migrationcenter_projects_locations_import_jobs_delete_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_delete(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsImportJobsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_import_jobs_delete_builder(
        client,
        &args.name,
        args.force,
        args.requestId.as_deref(),
    )?;
    migrationcenter_projects_locations_import_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}
/// Gets the details of an import job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_import_jobs_get_execute()` to send, or `migrationcenter_projects_locations_import_jobs_get` for simplest API.

pub fn migrationcenter_projects_locations_import_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/importJobs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}
/// Gets the details of an import job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_import_jobs_get_execute()` or `migrationcenter_projects_locations_import_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ImportJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImportJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}
/// Gets the details of an import job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_import_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_import_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_import_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImportJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_import_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_import_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsImportJobsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}
/// Gets the details of an import job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_import_jobs_get_builder()` + `migrationcenter_projects_locations_import_jobs_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsImportJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImportJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_import_jobs_get_builder(
        client,
        &args.name,
        args.view.as_deref(),
    )?;
    migrationcenter_projects_locations_import_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs
/// Lists all import jobs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_import_jobs_list_execute()` to send, or `migrationcenter_projects_locations_import_jobs_list` for simplest API.

pub fn migrationcenter_projects_locations_import_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/importJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs
/// Lists all import jobs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_import_jobs_list_execute()` or `migrationcenter_projects_locations_import_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListImportJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListImportJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs
/// Lists all import jobs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_import_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_import_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_import_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListImportJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_import_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_import_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsImportJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs
/// Lists all import jobs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_import_jobs_list_builder()` + `migrationcenter_projects_locations_import_jobs_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsImportJobsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListImportJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_import_jobs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.view.as_deref(),
    )?;
    migrationcenter_projects_locations_import_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}
/// Updates an import job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_import_jobs_patch_execute()` to send, or `migrationcenter_projects_locations_import_jobs_patch` for simplest API.

pub fn migrationcenter_projects_locations_import_jobs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &ImportJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/importJobs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}
/// Updates an import job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_import_jobs_patch_execute()` or `migrationcenter_projects_locations_import_jobs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}
/// Updates an import job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_import_jobs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_patch_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_import_jobs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_import_jobs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_import_jobs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_import_jobs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsImportJobsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: ImportJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}
/// Updates an import job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_import_jobs_patch_builder()` + `migrationcenter_projects_locations_import_jobs_patch_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_patch(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsImportJobsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_import_jobs_patch_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_import_jobs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}:run
/// Runs an import job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_import_jobs_run_execute()` to send, or `migrationcenter_projects_locations_import_jobs_run` for simplest API.

pub fn migrationcenter_projects_locations_import_jobs_run_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RunImportJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/importJobs/{}:run",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}:run
/// Runs an import job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_import_jobs_run_execute()` or `migrationcenter_projects_locations_import_jobs_run`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_run_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}:run
/// Runs an import job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_import_jobs_run_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_run_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_import_jobs_run()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_import_jobs_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_import_jobs_run_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_import_jobs_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsImportJobsRunArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RunImportJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}:run
/// Runs an import job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_import_jobs_run_builder()` + `migrationcenter_projects_locations_import_jobs_run_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_run_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_run(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsImportJobsRunArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        migrationcenter_projects_locations_import_jobs_run_builder(client, &args.name, &args.body)?;
    migrationcenter_projects_locations_import_jobs_run_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}:validate
/// Validates an import job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_import_jobs_validate_execute()` to send, or `migrationcenter_projects_locations_import_jobs_validate` for simplest API.

pub fn migrationcenter_projects_locations_import_jobs_validate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ValidateImportJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/importJobs/{}:validate",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}:validate
/// Validates an import job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_import_jobs_validate_execute()` or `migrationcenter_projects_locations_import_jobs_validate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_validate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}:validate
/// Validates an import job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_import_jobs_validate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_validate_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_import_jobs_validate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_import_jobs_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_import_jobs_validate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_import_jobs_validate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsImportJobsValidateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ValidateImportJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}:validate
/// Validates an import job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_import_jobs_validate_builder()` + `migrationcenter_projects_locations_import_jobs_validate_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_validate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_validate(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsImportJobsValidateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_import_jobs_validate_builder(
        client, &args.name, &args.body,
    )?;
    migrationcenter_projects_locations_import_jobs_validate_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles
/// Creates an import data file.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_import_jobs_import_data_files_create_execute()` to send, or `migrationcenter_projects_locations_import_jobs_import_data_files_create` for simplest API.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    importDataFileId: Option<&str>,
    requestId: Option<&str>,
    body: &ImportDataFile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/importJobs/{}/importDataFiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = importDataFileId {
        query_parts.push(format!("importDataFileId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles
/// Creates an import data file.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_import_jobs_import_data_files_create_execute()` or `migrationcenter_projects_locations_import_jobs_import_data_files_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_import_data_files_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles
/// Creates an import data file.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_import_jobs_import_data_files_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_import_data_files_create_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_import_jobs_import_data_files_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_import_data_files_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        migrationcenter_projects_locations_import_jobs_import_data_files_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_import_jobs_import_data_files_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsImportJobsImportDataFilesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: importDataFileId
    pub importDataFileId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: ImportDataFile,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles
/// Creates an import data file.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_import_jobs_import_data_files_create_builder()` + `migrationcenter_projects_locations_import_jobs_import_data_files_create_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_import_data_files_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_create(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsImportJobsImportDataFilesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_import_jobs_import_data_files_create_builder(
        client,
        &args.parent,
        args.importDataFileId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_import_jobs_import_data_files_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles/{importDataFilesId}
/// Delete an import data file.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_import_jobs_import_data_files_delete_execute()` to send, or `migrationcenter_projects_locations_import_jobs_import_data_files_delete` for simplest API.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/importJobs/{}/importDataFiles/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles/{importDataFilesId}
/// Delete an import data file.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_import_jobs_import_data_files_delete_execute()` or `migrationcenter_projects_locations_import_jobs_import_data_files_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_import_data_files_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles/{importDataFilesId}
/// Delete an import data file.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_import_jobs_import_data_files_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_import_data_files_delete_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_import_jobs_import_data_files_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_import_data_files_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        migrationcenter_projects_locations_import_jobs_import_data_files_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_import_jobs_import_data_files_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsImportJobsImportDataFilesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles/{importDataFilesId}
/// Delete an import data file.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_import_jobs_import_data_files_delete_builder()` + `migrationcenter_projects_locations_import_jobs_import_data_files_delete_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_import_data_files_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_delete(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsImportJobsImportDataFilesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_import_jobs_import_data_files_delete_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
    )?;
    migrationcenter_projects_locations_import_jobs_import_data_files_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles/{importDataFilesId}
/// Gets an import data file.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_import_jobs_import_data_files_get_execute()` to send, or `migrationcenter_projects_locations_import_jobs_import_data_files_get` for simplest API.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/importJobs/{}/importDataFiles/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles/{importDataFilesId}
/// Gets an import data file.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_import_jobs_import_data_files_get_execute()` or `migrationcenter_projects_locations_import_jobs_import_data_files_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_import_data_files_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ImportDataFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImportDataFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles/{importDataFilesId}
/// Gets an import data file.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_import_jobs_import_data_files_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_import_data_files_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_import_jobs_import_data_files_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_import_data_files_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImportDataFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_import_jobs_import_data_files_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_import_jobs_import_data_files_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsImportJobsImportDataFilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles/{importDataFilesId}
/// Gets an import data file.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_import_jobs_import_data_files_get_builder()` + `migrationcenter_projects_locations_import_jobs_import_data_files_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_import_data_files_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsImportJobsImportDataFilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImportDataFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_import_jobs_import_data_files_get_builder(
        client, &args.name,
    )?;
    migrationcenter_projects_locations_import_jobs_import_data_files_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles
/// List import data files.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_import_jobs_import_data_files_list_execute()` to send, or `migrationcenter_projects_locations_import_jobs_import_data_files_list` for simplest API.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/importJobs/{}/importDataFiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles
/// List import data files.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_import_jobs_import_data_files_list_execute()` or `migrationcenter_projects_locations_import_jobs_import_data_files_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_import_data_files_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListImportDataFilesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListImportDataFilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles
/// List import data files.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_import_jobs_import_data_files_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_import_data_files_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_import_jobs_import_data_files_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_import_jobs_import_data_files_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListImportDataFilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_import_jobs_import_data_files_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_import_jobs_import_data_files_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsImportJobsImportDataFilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/importJobs/{importJobsId}/importDataFiles
/// List import data files.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_import_jobs_import_data_files_list_builder()` + `migrationcenter_projects_locations_import_jobs_import_data_files_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_import_jobs_import_data_files_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_import_jobs_import_data_files_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsImportJobsImportDataFilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListImportDataFilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_import_jobs_import_data_files_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    migrationcenter_projects_locations_import_jobs_import_data_files_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_operations_cancel_execute()` to send, or `migrationcenter_projects_locations_operations_cancel` for simplest API.

pub fn migrationcenter_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_operations_cancel_execute()` or `migrationcenter_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_operations_cancel_builder()` + `migrationcenter_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_operations_cancel_builder(
        client, &args.name, &args.body,
    )?;
    migrationcenter_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_operations_delete_execute()` to send, or `migrationcenter_projects_locations_operations_delete` for simplest API.

pub fn migrationcenter_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_operations_delete_execute()` or `migrationcenter_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_operations_delete_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_operations_delete_builder()` + `migrationcenter_projects_locations_operations_delete_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_operations_delete_builder(client, &args.name)?;
    migrationcenter_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_operations_get_execute()` to send, or `migrationcenter_projects_locations_operations_get` for simplest API.

pub fn migrationcenter_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_operations_get_execute()` or `migrationcenter_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_operations_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_operations_get_builder()` + `migrationcenter_projects_locations_operations_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_operations_get_builder(client, &args.name)?;
    migrationcenter_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_operations_list_execute()` to send, or `migrationcenter_projects_locations_operations_list` for simplest API.

pub fn migrationcenter_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_operations_list_execute()` or `migrationcenter_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_operations_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_operations_list_builder()` + `migrationcenter_projects_locations_operations_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    migrationcenter_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets
/// Creates a new preference set in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_preference_sets_create_execute()` to send, or `migrationcenter_projects_locations_preference_sets_create` for simplest API.

pub fn migrationcenter_projects_locations_preference_sets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    preferenceSetId: Option<&str>,
    requestId: Option<&str>,
    body: &PreferenceSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/preferenceSets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = preferenceSetId {
        query_parts.push(format!("preferenceSetId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets
/// Creates a new preference set in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_preference_sets_create_execute()` or `migrationcenter_projects_locations_preference_sets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_preference_sets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_preference_sets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets
/// Creates a new preference set in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_preference_sets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_preference_sets_create_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_preference_sets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_preference_sets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_preference_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_preference_sets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_preference_sets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsPreferenceSetsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: preferenceSetId
    pub preferenceSetId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: PreferenceSet,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets
/// Creates a new preference set in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_preference_sets_create_builder()` + `migrationcenter_projects_locations_preference_sets_create_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_preference_sets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_preference_sets_create(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsPreferenceSetsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_preference_sets_create_builder(
        client,
        &args.parent,
        args.preferenceSetId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_preference_sets_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets/{preferenceSetsId}
/// Deletes a preference set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_preference_sets_delete_execute()` to send, or `migrationcenter_projects_locations_preference_sets_delete` for simplest API.

pub fn migrationcenter_projects_locations_preference_sets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/preferenceSets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets/{preferenceSetsId}
/// Deletes a preference set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_preference_sets_delete_execute()` or `migrationcenter_projects_locations_preference_sets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_preference_sets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_preference_sets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets/{preferenceSetsId}
/// Deletes a preference set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_preference_sets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_preference_sets_delete_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_preference_sets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_preference_sets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_preference_sets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_preference_sets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_preference_sets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsPreferenceSetsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets/{preferenceSetsId}
/// Deletes a preference set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_preference_sets_delete_builder()` + `migrationcenter_projects_locations_preference_sets_delete_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_preference_sets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_preference_sets_delete(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsPreferenceSetsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_preference_sets_delete_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
    )?;
    migrationcenter_projects_locations_preference_sets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets/{preferenceSetsId}
/// Gets the details of a preference set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_preference_sets_get_execute()` to send, or `migrationcenter_projects_locations_preference_sets_get` for simplest API.

pub fn migrationcenter_projects_locations_preference_sets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/preferenceSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets/{preferenceSetsId}
/// Gets the details of a preference set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_preference_sets_get_execute()` or `migrationcenter_projects_locations_preference_sets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_preference_sets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_preference_sets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<PreferenceSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PreferenceSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets/{preferenceSetsId}
/// Gets the details of a preference set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_preference_sets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_preference_sets_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_preference_sets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_preference_sets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_preference_sets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PreferenceSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_preference_sets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_preference_sets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsPreferenceSetsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets/{preferenceSetsId}
/// Gets the details of a preference set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_preference_sets_get_builder()` + `migrationcenter_projects_locations_preference_sets_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_preference_sets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_preference_sets_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsPreferenceSetsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PreferenceSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        migrationcenter_projects_locations_preference_sets_get_builder(client, &args.name)?;
    migrationcenter_projects_locations_preference_sets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets
/// Lists all the preference sets in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_preference_sets_list_execute()` to send, or `migrationcenter_projects_locations_preference_sets_list` for simplest API.

pub fn migrationcenter_projects_locations_preference_sets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/preferenceSets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets
/// Lists all the preference sets in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_preference_sets_list_execute()` or `migrationcenter_projects_locations_preference_sets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_preference_sets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_preference_sets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListPreferenceSetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPreferenceSetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets
/// Lists all the preference sets in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_preference_sets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_preference_sets_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_preference_sets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_preference_sets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_preference_sets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPreferenceSetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_preference_sets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_preference_sets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsPreferenceSetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets
/// Lists all the preference sets in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_preference_sets_list_builder()` + `migrationcenter_projects_locations_preference_sets_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_preference_sets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_preference_sets_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsPreferenceSetsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPreferenceSetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_preference_sets_list_builder(
        client,
        &args.parent,
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    migrationcenter_projects_locations_preference_sets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets/{preferenceSetsId}
/// Updates the parameters of a preference set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_preference_sets_patch_execute()` to send, or `migrationcenter_projects_locations_preference_sets_patch` for simplest API.

pub fn migrationcenter_projects_locations_preference_sets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &PreferenceSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/preferenceSets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets/{preferenceSetsId}
/// Updates the parameters of a preference set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_preference_sets_patch_execute()` or `migrationcenter_projects_locations_preference_sets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_preference_sets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_preference_sets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets/{preferenceSetsId}
/// Updates the parameters of a preference set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_preference_sets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_preference_sets_patch_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_preference_sets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_preference_sets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_preference_sets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_preference_sets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_preference_sets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsPreferenceSetsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: PreferenceSet,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/preferenceSets/{preferenceSetsId}
/// Updates the parameters of a preference set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_preference_sets_patch_builder()` + `migrationcenter_projects_locations_preference_sets_patch_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_preference_sets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_preference_sets_patch(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsPreferenceSetsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_preference_sets_patch_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_preference_sets_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/relations/{relationsId}
/// Gets the details of an relation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_relations_get_execute()` to send, or `migrationcenter_projects_locations_relations_get` for simplest API.

pub fn migrationcenter_projects_locations_relations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/relations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/relations/{relationsId}
/// Gets the details of an relation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_relations_get_execute()` or `migrationcenter_projects_locations_relations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_relations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_relations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Relation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Relation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/relations/{relationsId}
/// Gets the details of an relation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_relations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_relations_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_relations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_relations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_relations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Relation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_relations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_relations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsRelationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/relations/{relationsId}
/// Gets the details of an relation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_relations_get_builder()` + `migrationcenter_projects_locations_relations_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_relations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_relations_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsRelationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Relation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_relations_get_builder(client, &args.name)?;
    migrationcenter_projects_locations_relations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/relations
/// Lists all the relations in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_relations_list_execute()` to send, or `migrationcenter_projects_locations_relations_list` for simplest API.

pub fn migrationcenter_projects_locations_relations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/relations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/relations
/// Lists all the relations in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_relations_list_execute()` or `migrationcenter_projects_locations_relations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_relations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_relations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListRelationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRelationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/relations
/// Lists all the relations in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_relations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_relations_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_relations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_relations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_relations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRelationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_relations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_relations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsRelationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/relations
/// Lists all the relations in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_relations_list_builder()` + `migrationcenter_projects_locations_relations_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_relations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_relations_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsRelationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRelationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_relations_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    migrationcenter_projects_locations_relations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs
/// Creates a report configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_report_configs_create_execute()` to send, or `migrationcenter_projects_locations_report_configs_create` for simplest API.

pub fn migrationcenter_projects_locations_report_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    reportConfigId: Option<&str>,
    requestId: Option<&str>,
    body: &ReportConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/reportConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reportConfigId {
        query_parts.push(format!("reportConfigId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs
/// Creates a report configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_report_configs_create_execute()` or `migrationcenter_projects_locations_report_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs
/// Creates a report configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_report_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_create_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_report_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_report_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_report_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_report_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsReportConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: reportConfigId
    pub reportConfigId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: ReportConfig,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs
/// Creates a report configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_report_configs_create_builder()` + `migrationcenter_projects_locations_report_configs_create_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_create(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsReportConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_report_configs_create_builder(
        client,
        &args.parent,
        args.reportConfigId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_report_configs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}
/// Deletes a ReportConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_report_configs_delete_execute()` to send, or `migrationcenter_projects_locations_report_configs_delete` for simplest API.

pub fn migrationcenter_projects_locations_report_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/reportConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}
/// Deletes a ReportConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_report_configs_delete_execute()` or `migrationcenter_projects_locations_report_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}
/// Deletes a ReportConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_report_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_delete_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_report_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_report_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_report_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_report_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsReportConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}
/// Deletes a ReportConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_report_configs_delete_builder()` + `migrationcenter_projects_locations_report_configs_delete_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_delete(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsReportConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_report_configs_delete_builder(
        client,
        &args.name,
        args.force,
        args.requestId.as_deref(),
    )?;
    migrationcenter_projects_locations_report_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}
/// Gets details of a single ReportConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_report_configs_get_execute()` to send, or `migrationcenter_projects_locations_report_configs_get` for simplest API.

pub fn migrationcenter_projects_locations_report_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/reportConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}
/// Gets details of a single ReportConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_report_configs_get_execute()` or `migrationcenter_projects_locations_report_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ReportConfig>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}
/// Gets details of a single ReportConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_report_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_report_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_report_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_report_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_report_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsReportConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}
/// Gets details of a single ReportConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_report_configs_get_builder()` + `migrationcenter_projects_locations_report_configs_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsReportConfigsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        migrationcenter_projects_locations_report_configs_get_builder(client, &args.name)?;
    migrationcenter_projects_locations_report_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs
/// Lists ReportConfigs in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_report_configs_list_execute()` to send, or `migrationcenter_projects_locations_report_configs_list` for simplest API.

pub fn migrationcenter_projects_locations_report_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/reportConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs
/// Lists ReportConfigs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_report_configs_list_execute()` or `migrationcenter_projects_locations_report_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListReportConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReportConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs
/// Lists ReportConfigs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_report_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_report_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_report_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_report_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_report_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsReportConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs
/// Lists ReportConfigs in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_report_configs_list_builder()` + `migrationcenter_projects_locations_report_configs_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsReportConfigsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_report_configs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    migrationcenter_projects_locations_report_configs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports
/// Creates a report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_report_configs_reports_create_execute()` to send, or `migrationcenter_projects_locations_report_configs_reports_create` for simplest API.

pub fn migrationcenter_projects_locations_report_configs_reports_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    reportId: Option<&str>,
    requestId: Option<&str>,
    body: &Report,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/reportConfigs/{}/reports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reportId {
        query_parts.push(format!("reportId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports
/// Creates a report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_report_configs_reports_create_execute()` or `migrationcenter_projects_locations_report_configs_reports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_reports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_reports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports
/// Creates a report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_report_configs_reports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_reports_create_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_report_configs_reports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_reports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_report_configs_reports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_report_configs_reports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_report_configs_reports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsReportConfigsReportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: reportId
    pub reportId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: Report,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports
/// Creates a report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_report_configs_reports_create_builder()` + `migrationcenter_projects_locations_report_configs_reports_create_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_reports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_reports_create(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsReportConfigsReportsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_report_configs_reports_create_builder(
        client,
        &args.parent,
        args.reportId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_report_configs_reports_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports/{reportsId}
/// Deletes a Report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_report_configs_reports_delete_execute()` to send, or `migrationcenter_projects_locations_report_configs_reports_delete` for simplest API.

pub fn migrationcenter_projects_locations_report_configs_reports_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/reportConfigs/{}/reports/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports/{reportsId}
/// Deletes a Report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_report_configs_reports_delete_execute()` or `migrationcenter_projects_locations_report_configs_reports_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_reports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_reports_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports/{reportsId}
/// Deletes a Report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_report_configs_reports_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_reports_delete_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_report_configs_reports_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_reports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_report_configs_reports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_report_configs_reports_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_report_configs_reports_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsReportConfigsReportsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports/{reportsId}
/// Deletes a Report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_report_configs_reports_delete_builder()` + `migrationcenter_projects_locations_report_configs_reports_delete_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_reports_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_reports_delete(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsReportConfigsReportsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_report_configs_reports_delete_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
    )?;
    migrationcenter_projects_locations_report_configs_reports_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports/{reportsId}
/// Gets details of a single Report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_report_configs_reports_get_execute()` to send, or `migrationcenter_projects_locations_report_configs_reports_get` for simplest API.

pub fn migrationcenter_projects_locations_report_configs_reports_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/reportConfigs/{}/reports/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports/{reportsId}
/// Gets details of a single Report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_report_configs_reports_get_execute()` or `migrationcenter_projects_locations_report_configs_reports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_reports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports/{reportsId}
/// Gets details of a single Report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_report_configs_reports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_reports_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_report_configs_reports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_report_configs_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_report_configs_reports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_report_configs_reports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsReportConfigsReportsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports/{reportsId}
/// Gets details of a single Report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_report_configs_reports_get_builder()` + `migrationcenter_projects_locations_report_configs_reports_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_reports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_reports_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsReportConfigsReportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_report_configs_reports_get_builder(
        client,
        &args.name,
        args.view.as_deref(),
    )?;
    migrationcenter_projects_locations_report_configs_reports_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports
/// Lists Reports in a given ReportConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_report_configs_reports_list_execute()` to send, or `migrationcenter_projects_locations_report_configs_reports_list` for simplest API.

pub fn migrationcenter_projects_locations_report_configs_reports_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/reportConfigs/{}/reports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports
/// Lists Reports in a given ReportConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_report_configs_reports_list_execute()` or `migrationcenter_projects_locations_report_configs_reports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_reports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports
/// Lists Reports in a given ReportConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_report_configs_reports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_reports_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_report_configs_reports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_report_configs_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_report_configs_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_report_configs_reports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_report_configs_reports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsReportConfigsReportsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports
/// Lists Reports in a given ReportConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_report_configs_reports_list_builder()` + `migrationcenter_projects_locations_report_configs_reports_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_report_configs_reports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_report_configs_reports_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsReportConfigsReportsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_report_configs_reports_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.view.as_deref(),
    )?;
    migrationcenter_projects_locations_report_configs_reports_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources
/// Creates a new source in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_sources_create_execute()` to send, or `migrationcenter_projects_locations_sources_create` for simplest API.

pub fn migrationcenter_projects_locations_sources_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    sourceId: Option<&str>,
    body: &Source,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/sources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sourceId {
        query_parts.push(format!("sourceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources
/// Creates a new source in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_sources_create_execute()` or `migrationcenter_projects_locations_sources_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources
/// Creates a new source in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_sources_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_sources_create_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_sources_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_sources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_sources_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_sources_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsSourcesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: sourceId
    pub sourceId: Option<String>,
    /// Request body.
    pub body: Source,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources
/// Creates a new source in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_sources_create_builder()` + `migrationcenter_projects_locations_sources_create_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_sources_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_create(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsSourcesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_sources_create_builder(
        client,
        &args.parent,
        args.requestId.as_deref(),
        args.sourceId.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_sources_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Deletes a source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_sources_delete_execute()` to send, or `migrationcenter_projects_locations_sources_delete` for simplest API.

pub fn migrationcenter_projects_locations_sources_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/sources/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Deletes a source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_sources_delete_execute()` or `migrationcenter_projects_locations_sources_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Deletes a source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_sources_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_sources_delete_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_sources_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_sources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_sources_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_sources_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsSourcesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Deletes a source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_sources_delete_builder()` + `migrationcenter_projects_locations_sources_delete_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_sources_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_delete(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsSourcesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_sources_delete_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
    )?;
    migrationcenter_projects_locations_sources_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Gets the details of a source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_sources_get_execute()` to send, or `migrationcenter_projects_locations_sources_get` for simplest API.

pub fn migrationcenter_projects_locations_sources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/sources/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Gets the details of a source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_sources_get_execute()` or `migrationcenter_projects_locations_sources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Gets the details of a source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_sources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_sources_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_sources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_sources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_sources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_sources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsSourcesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Gets the details of a source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_sources_get_builder()` + `migrationcenter_projects_locations_sources_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_sources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsSourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_sources_get_builder(client, &args.name)?;
    migrationcenter_projects_locations_sources_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources
/// Lists all the sources in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_sources_list_execute()` to send, or `migrationcenter_projects_locations_sources_list` for simplest API.

pub fn migrationcenter_projects_locations_sources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/sources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources
/// Lists all the sources in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_sources_list_execute()` or `migrationcenter_projects_locations_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources
/// Lists all the sources in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_sources_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsSourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources
/// Lists all the sources in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_sources_list_builder()` + `migrationcenter_projects_locations_sources_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsSourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_sources_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    migrationcenter_projects_locations_sources_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Updates the parameters of a source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_sources_patch_execute()` to send, or `migrationcenter_projects_locations_sources_patch` for simplest API.

pub fn migrationcenter_projects_locations_sources_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Source,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/sources/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Updates the parameters of a source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_sources_patch_execute()` or `migrationcenter_projects_locations_sources_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Updates the parameters of a source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_sources_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_sources_patch_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_sources_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_sources_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_sources_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_sources_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsSourcesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Source,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Updates the parameters of a source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_sources_patch_builder()` + `migrationcenter_projects_locations_sources_patch_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_sources_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_patch(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsSourcesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_sources_patch_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        &args.body,
    )?;
    migrationcenter_projects_locations_sources_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/errorFrames/{errorFramesId}
/// Gets the details of an error frame.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_sources_error_frames_get_execute()` to send, or `migrationcenter_projects_locations_sources_error_frames_get` for simplest API.

pub fn migrationcenter_projects_locations_sources_error_frames_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/sources/{}/errorFrames/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/errorFrames/{errorFramesId}
/// Gets the details of an error frame.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_sources_error_frames_get_execute()` or `migrationcenter_projects_locations_sources_error_frames_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_error_frames_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_error_frames_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ErrorFrame>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ErrorFrame = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/errorFrames/{errorFramesId}
/// Gets the details of an error frame.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_sources_error_frames_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_sources_error_frames_get_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_sources_error_frames_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_error_frames_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_sources_error_frames_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ErrorFrame>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_sources_error_frames_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_sources_error_frames_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsSourcesErrorFramesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/errorFrames/{errorFramesId}
/// Gets the details of an error frame.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_sources_error_frames_get_builder()` + `migrationcenter_projects_locations_sources_error_frames_get_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_sources_error_frames_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_error_frames_get(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsSourcesErrorFramesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ErrorFrame>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_sources_error_frames_get_builder(
        client,
        &args.name,
        args.view.as_deref(),
    )?;
    migrationcenter_projects_locations_sources_error_frames_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/errorFrames
/// Lists all error frames in a given source and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `migrationcenter_projects_locations_sources_error_frames_list_execute()` to send, or `migrationcenter_projects_locations_sources_error_frames_list` for simplest API.

pub fn migrationcenter_projects_locations_sources_error_frames_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{}/locations/{}/sources/{}/errorFrames",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/errorFrames
/// Lists all error frames in a given source and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `migrationcenter_projects_locations_sources_error_frames_list_execute()` or `migrationcenter_projects_locations_sources_error_frames_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_error_frames_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_error_frames_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListErrorFramesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListErrorFramesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/errorFrames
/// Lists all error frames in a given source and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `migrationcenter_projects_locations_sources_error_frames_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `migrationcenter_projects_locations_sources_error_frames_list_task()`.
/// For the simplest API, use `migrationcenter_projects_locations_sources_error_frames_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `migrationcenter_projects_locations_sources_error_frames_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn migrationcenter_projects_locations_sources_error_frames_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListErrorFramesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = migrationcenter_projects_locations_sources_error_frames_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`migrationcenter_projects_locations_sources_error_frames_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsSourcesErrorFramesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/errorFrames
/// Lists all error frames in a given source and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `migrationcenter_projects_locations_sources_error_frames_list_builder()` + `migrationcenter_projects_locations_sources_error_frames_list_execute()`.
/// For task-level control, use `migrationcenter_projects_locations_sources_error_frames_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn migrationcenter_projects_locations_sources_error_frames_list(
    client: &SimpleHttpClient,
    args: &MigrationcenterProjectsLocationsSourcesErrorFramesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListErrorFramesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = migrationcenter_projects_locations_sources_error_frames_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
        args.view.as_deref(),
    )?;
    migrationcenter_projects_locations_sources_error_frames_list_execute(builder)
}
