//! Auto-generated API clients for gcp/adexchangebuyer2.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/adexchangebuyer2`

#![cfg(feature = "gcp/adexchangebuyer2")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2beta1/accounts/{accountId}/clients
/// Creates a new client buyer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_create_execute()` to send, or `adexchangebuyer2_accounts_clients_create` for simplest API.

pub fn adexchangebuyer2_accounts_clients_create_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    body: &Client,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients",
        accountId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/clients
/// Creates a new client buyer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_clients_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_clients_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsCreateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Request body.
    pub body: Client,
}

/// GET v2beta1/accounts/{accountId}/clients
/// Creates a new client buyer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_create_builder()` + `adexchangebuyer2_accounts_clients_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        adexchangebuyer2_accounts_clients_create_builder(client, &args.accountId, &args.body)?;
    adexchangebuyer2_accounts_clients_create_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}
/// Gets a client buyer with a given client account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_get_execute()` to send, or `adexchangebuyer2_accounts_clients_get` for simplest API.

pub fn adexchangebuyer2_accounts_clients_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    clientAccountId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}",
        accountId, clientAccountId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}
/// Gets a client buyer with a given client account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_clients_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_clients_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}
/// Gets a client buyer with a given client account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_get_builder()` + `adexchangebuyer2_accounts_clients_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_get_builder(
        client,
        &args.accountId,
        &args.clientAccountId,
    )?;
    adexchangebuyer2_accounts_clients_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients
/// Lists all the clients for the current sponsor buyer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_list_execute()` to send, or `adexchangebuyer2_accounts_clients_list` for simplest API.

pub fn adexchangebuyer2_accounts_clients_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    partnerClientId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients",
        accountId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerClientId {
        query_parts.push(format!("partnerClientId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/clients
/// Lists all the clients for the current sponsor buyer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_clients_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_clients_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClientsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerClientId
    pub partnerClientId: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/clients
/// Lists all the clients for the current sponsor buyer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_list_builder()` + `adexchangebuyer2_accounts_clients_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_list_builder(
        client,
        &args.accountId,
        args.pageSize,
        args.pageToken.as_deref(),
        args.partnerClientId.as_deref(),
    )?;
    adexchangebuyer2_accounts_clients_list_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}
/// Updates an existing client buyer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_update_execute()` to send, or `adexchangebuyer2_accounts_clients_update` for simplest API.

pub fn adexchangebuyer2_accounts_clients_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    clientAccountId: &str,
    body: &Client,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}",
        accountId, clientAccountId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}
/// Updates an existing client buyer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_clients_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_clients_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsUpdateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
    /// Request body.
    pub body: Client,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}
/// Updates an existing client buyer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_update_builder()` + `adexchangebuyer2_accounts_clients_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_update(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_update_builder(
        client,
        &args.accountId,
        &args.clientAccountId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_clients_update_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations
/// Creates and sends out an email invitation to access an Ad Exchange client buyer account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_invitations_create_execute()` to send, or `adexchangebuyer2_accounts_clients_invitations_create` for simplest API.

pub fn adexchangebuyer2_accounts_clients_invitations_create_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    clientAccountId: &str,
    body: &ClientUserInvitation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/invitations",
        accountId, clientAccountId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations
/// Creates and sends out an email invitation to access an Ad Exchange client buyer account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_clients_invitations_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_clients_invitations_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_invitations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUserInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUserInvitation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_invitations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsInvitationsCreateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
    /// Request body.
    pub body: ClientUserInvitation,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations
/// Creates and sends out an email invitation to access an Ad Exchange client buyer account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_invitations_create_builder()` + `adexchangebuyer2_accounts_clients_invitations_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_invitations_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsInvitationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUserInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_invitations_create_builder(
        client,
        &args.accountId,
        &args.clientAccountId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_clients_invitations_create_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations/{invitationId}
/// Retrieves an existing client user invitation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_invitations_get_execute()` to send, or `adexchangebuyer2_accounts_clients_invitations_get` for simplest API.

pub fn adexchangebuyer2_accounts_clients_invitations_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    clientAccountId: &str,
    invitationId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/invitations/{}",
        accountId, clientAccountId, invitationId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations/{invitationId}
/// Retrieves an existing client user invitation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_clients_invitations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_clients_invitations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_invitations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUserInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUserInvitation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_invitations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsInvitationsGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
    /// Path parameter: invitationId
    pub invitationId: String,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations/{invitationId}
/// Retrieves an existing client user invitation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_invitations_get_builder()` + `adexchangebuyer2_accounts_clients_invitations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_invitations_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsInvitationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUserInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_invitations_get_builder(
        client,
        &args.accountId,
        &args.clientAccountId,
        &args.invitationId,
    )?;
    adexchangebuyer2_accounts_clients_invitations_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations
/// Lists all the client users invitations for a client with a given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_invitations_list_execute()` to send, or `adexchangebuyer2_accounts_clients_invitations_list` for simplest API.

pub fn adexchangebuyer2_accounts_clients_invitations_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    clientAccountId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/invitations",
        accountId, clientAccountId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations
/// Lists all the client users invitations for a client with a given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_clients_invitations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_clients_invitations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_invitations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListClientUserInvitationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClientUserInvitationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_invitations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsInvitationsListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations
/// Lists all the client users invitations for a client with a given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_invitations_list_builder()` + `adexchangebuyer2_accounts_clients_invitations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_invitations_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsInvitationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListClientUserInvitationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_invitations_list_builder(
        client,
        &args.accountId,
        &args.clientAccountId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_accounts_clients_invitations_list_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}
/// Retrieves an existing client user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_users_get_execute()` to send, or `adexchangebuyer2_accounts_clients_users_get` for simplest API.

pub fn adexchangebuyer2_accounts_clients_users_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    clientAccountId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/users/{}",
        accountId, clientAccountId, userId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}
/// Retrieves an existing client user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_clients_users_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_clients_users_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_users_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsUsersGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}
/// Retrieves an existing client user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_users_get_builder()` + `adexchangebuyer2_accounts_clients_users_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_users_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsUsersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_users_get_builder(
        client,
        &args.accountId,
        &args.clientAccountId,
        &args.userId,
    )?;
    adexchangebuyer2_accounts_clients_users_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users
/// Lists all the known client users for a specified sponsor buyer account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_users_list_execute()` to send, or `adexchangebuyer2_accounts_clients_users_list` for simplest API.

pub fn adexchangebuyer2_accounts_clients_users_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    clientAccountId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/users",
        accountId, clientAccountId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users
/// Lists all the known client users for a specified sponsor buyer account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_clients_users_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_clients_users_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClientUsersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_users_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsUsersListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users
/// Lists all the known client users for a specified sponsor buyer account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_users_list_builder()` + `adexchangebuyer2_accounts_clients_users_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_users_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsUsersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_users_list_builder(
        client,
        &args.accountId,
        &args.clientAccountId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_accounts_clients_users_list_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}
/// Updates an existing client user. Only the user status can be changed on update.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_users_update_execute()` to send, or `adexchangebuyer2_accounts_clients_users_update` for simplest API.

pub fn adexchangebuyer2_accounts_clients_users_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    clientAccountId: &str,
    userId: &str,
    body: &ClientUser,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/users/{}",
        accountId, clientAccountId, userId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}
/// Updates an existing client user. Only the user status can be changed on update.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_clients_users_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_clients_users_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_users_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_users_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsUsersUpdateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
    /// Path parameter: userId
    pub userId: String,
    /// Request body.
    pub body: ClientUser,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}
/// Updates an existing client user. Only the user status can be changed on update.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_users_update_builder()` + `adexchangebuyer2_accounts_clients_users_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_users_update(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsUsersUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_users_update_builder(
        client,
        &args.accountId,
        &args.clientAccountId,
        &args.userId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_clients_users_update_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives
/// Creates a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_create_execute()` to send, or `adexchangebuyer2_accounts_creatives_create` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_create_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    duplicateIdMode: Option<&str>,
    body: &Creative,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives",
        accountId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = duplicateIdMode {
        query_parts.push(format!("duplicateIdMode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/creatives
/// Creates a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_creatives_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_creatives_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesCreateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: duplicateIdMode
    pub duplicateIdMode: Option<String>,
    /// Request body.
    pub body: Creative,
}

/// GET v2beta1/accounts/{accountId}/creatives
/// Creates a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_create_builder()` + `adexchangebuyer2_accounts_creatives_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_create_builder(
        client,
        &args.accountId,
        args.duplicateIdMode.as_deref(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_creatives_create_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}
/// Gets a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_get_execute()` to send, or `adexchangebuyer2_accounts_creatives_get` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    creativeId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}",
        accountId, creativeId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}
/// Gets a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_creatives_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_creatives_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}
/// Gets a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_get_builder()` + `adexchangebuyer2_accounts_creatives_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        adexchangebuyer2_accounts_creatives_get_builder(client, &args.accountId, &args.creativeId)?;
    adexchangebuyer2_accounts_creatives_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives
/// Lists creatives.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_list_execute()` to send, or `adexchangebuyer2_accounts_creatives_list` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives",
        accountId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives
/// Lists creatives.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_creatives_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_creatives_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCreativesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCreativesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/creatives
/// Lists creatives.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_list_builder()` + `adexchangebuyer2_accounts_creatives_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCreativesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_list_builder(
        client,
        &args.accountId,
        args.pageSize,
        args.pageToken.as_deref(),
        args.query.as_deref(),
    )?;
    adexchangebuyer2_accounts_creatives_list_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:stopWatching
/// Stops watching a creative. Will stop push notifications being sent to the topics when the creative changes status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_stop_watching_execute()` to send, or `adexchangebuyer2_accounts_creatives_stop_watching` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_stop_watching_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    creativeId: &str,
    body: &StopWatchingCreativeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}:stopWatching",
        accountId, creativeId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:stopWatching
/// Stops watching a creative. Will stop push notifications being sent to the topics when the creative changes status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_creatives_stop_watching_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_creatives_stop_watching_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_stop_watching_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_stop_watching`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesStopWatchingArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
    /// Request body.
    pub body: StopWatchingCreativeRequest,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:stopWatching
/// Stops watching a creative. Will stop push notifications being sent to the topics when the creative changes status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_stop_watching_builder()` + `adexchangebuyer2_accounts_creatives_stop_watching_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_stop_watching(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesStopWatchingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_stop_watching_builder(
        client,
        &args.accountId,
        &args.creativeId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_creatives_stop_watching_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}
/// Updates a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_update_execute()` to send, or `adexchangebuyer2_accounts_creatives_update` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    creativeId: &str,
    body: &Creative,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}",
        accountId, creativeId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}
/// Updates a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_creatives_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_creatives_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesUpdateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
    /// Request body.
    pub body: Creative,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}
/// Updates a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_update_builder()` + `adexchangebuyer2_accounts_creatives_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_update(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_update_builder(
        client,
        &args.accountId,
        &args.creativeId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_creatives_update_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:watch
/// Watches a creative. Will result in push notifications being sent to the topic when the creative changes status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_watch_execute()` to send, or `adexchangebuyer2_accounts_creatives_watch` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_watch_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    creativeId: &str,
    body: &WatchCreativeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}:watch",
        accountId, creativeId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:watch
/// Watches a creative. Will result in push notifications being sent to the topic when the creative changes status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_creatives_watch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_creatives_watch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_watch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesWatchArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
    /// Request body.
    pub body: WatchCreativeRequest,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:watch
/// Watches a creative. Will result in push notifications being sent to the topic when the creative changes status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_watch_builder()` + `adexchangebuyer2_accounts_creatives_watch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_watch(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesWatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_watch_builder(
        client,
        &args.accountId,
        &args.creativeId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_creatives_watch_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:add
/// Associate an existing deal with a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_deal_associations_add_execute()` to send, or `adexchangebuyer2_accounts_creatives_deal_associations_add` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_add_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    creativeId: &str,
    body: &AddDealAssociationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}/dealAssociations:add",
        accountId,
        creativeId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:add
/// Associate an existing deal with a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_creatives_deal_associations_add_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_creatives_deal_associations_add_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_add_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_deal_associations_add`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesDealAssociationsAddArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
    /// Request body.
    pub body: AddDealAssociationRequest,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:add
/// Associate an existing deal with a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_deal_associations_add_builder()` + `adexchangebuyer2_accounts_creatives_deal_associations_add_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_add(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesDealAssociationsAddArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_deal_associations_add_builder(
        client,
        &args.accountId,
        &args.creativeId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_creatives_deal_associations_add_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations
/// List all creative-deal associations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_deal_associations_list_execute()` to send, or `adexchangebuyer2_accounts_creatives_deal_associations_list` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    creativeId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}/dealAssociations",
        accountId, creativeId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations
/// List all creative-deal associations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_creatives_deal_associations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_creatives_deal_associations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDealAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDealAssociationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_deal_associations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesDealAssociationsListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations
/// List all creative-deal associations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_deal_associations_list_builder()` + `adexchangebuyer2_accounts_creatives_deal_associations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesDealAssociationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDealAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_deal_associations_list_builder(
        client,
        &args.accountId,
        &args.creativeId,
        args.pageSize,
        args.pageToken.as_deref(),
        args.query.as_deref(),
    )?;
    adexchangebuyer2_accounts_creatives_deal_associations_list_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:remove
/// Remove the association between a deal and a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_deal_associations_remove_execute()` to send, or `adexchangebuyer2_accounts_creatives_deal_associations_remove` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_remove_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    creativeId: &str,
    body: &RemoveDealAssociationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}/dealAssociations:remove",
        accountId,
        creativeId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:remove
/// Remove the association between a deal and a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_creatives_deal_associations_remove_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_creatives_deal_associations_remove_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_remove_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_deal_associations_remove`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesDealAssociationsRemoveArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
    /// Request body.
    pub body: RemoveDealAssociationRequest,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:remove
/// Remove the association between a deal and a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_deal_associations_remove_builder()` + `adexchangebuyer2_accounts_creatives_deal_associations_remove_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_remove(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesDealAssociationsRemoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_deal_associations_remove_builder(
        client,
        &args.accountId,
        &args.creativeId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_creatives_deal_associations_remove_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals
/// List finalized proposals, regardless if a proposal is being renegotiated. A filter expression (PQL query) may be specified to filter the results. The notes will not be returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_finalized_proposals_list_execute()` to send, or `adexchangebuyer2_accounts_finalized_proposals_list` for simplest API.

pub fn adexchangebuyer2_accounts_finalized_proposals_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    filter: Option<&str>,
    filterSyntax: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/finalizedProposals",
        accountId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = filterSyntax {
        query_parts.push(format!("filterSyntax={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals
/// List finalized proposals, regardless if a proposal is being renegotiated. A filter expression (PQL query) may be specified to filter the results. The notes will not be returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_finalized_proposals_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_finalized_proposals_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_finalized_proposals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProposalsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProposalsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_finalized_proposals_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsFinalizedProposalsListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: filterSyntax
    pub filterSyntax: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals
/// List finalized proposals, regardless if a proposal is being renegotiated. A filter expression (PQL query) may be specified to filter the results. The notes will not be returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_finalized_proposals_list_builder()` + `adexchangebuyer2_accounts_finalized_proposals_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_finalized_proposals_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsFinalizedProposalsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProposalsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_finalized_proposals_list_builder(
        client,
        &args.accountId,
        args.filter.as_deref(),
        args.filterSyntax.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_accounts_finalized_proposals_list_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:pause
/// Update given deals to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.pause endpoint. It is a no-op to pause already-paused deals. It is an error to call PauseProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_finalized_proposals_pause_execute()` to send, or `adexchangebuyer2_accounts_finalized_proposals_pause` for simplest API.

pub fn adexchangebuyer2_accounts_finalized_proposals_pause_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    proposalId: &str,
    body: &PauseProposalDealsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/finalizedProposals/{}:pause",
        accountId, proposalId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:pause
/// Update given deals to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.pause endpoint. It is a no-op to pause already-paused deals. It is an error to call PauseProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_finalized_proposals_pause_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_finalized_proposals_pause_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_finalized_proposals_pause_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_finalized_proposals_pause`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsFinalizedProposalsPauseArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: PauseProposalDealsRequest,
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:pause
/// Update given deals to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.pause endpoint. It is a no-op to pause already-paused deals. It is an error to call PauseProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_finalized_proposals_pause_builder()` + `adexchangebuyer2_accounts_finalized_proposals_pause_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_finalized_proposals_pause(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsFinalizedProposalsPauseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_finalized_proposals_pause_builder(
        client,
        &args.accountId,
        &args.proposalId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_finalized_proposals_pause_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:resume
/// Update given deals to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.resume endpoint. It is a no-op to resume running deals or deals paused by the other party. It is an error to call ResumeProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_finalized_proposals_resume_execute()` to send, or `adexchangebuyer2_accounts_finalized_proposals_resume` for simplest API.

pub fn adexchangebuyer2_accounts_finalized_proposals_resume_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    proposalId: &str,
    body: &ResumeProposalDealsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/finalizedProposals/{}:resume",
        accountId, proposalId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:resume
/// Update given deals to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.resume endpoint. It is a no-op to resume running deals or deals paused by the other party. It is an error to call ResumeProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_finalized_proposals_resume_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_finalized_proposals_resume_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_finalized_proposals_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_finalized_proposals_resume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsFinalizedProposalsResumeArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: ResumeProposalDealsRequest,
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:resume
/// Update given deals to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.resume endpoint. It is a no-op to resume running deals or deals paused by the other party. It is an error to call ResumeProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_finalized_proposals_resume_builder()` + `adexchangebuyer2_accounts_finalized_proposals_resume_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_finalized_proposals_resume(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsFinalizedProposalsResumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_finalized_proposals_resume_builder(
        client,
        &args.accountId,
        &args.proposalId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_finalized_proposals_resume_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/products/{productId}
/// Gets the requested product by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_products_get_execute()` to send, or `adexchangebuyer2_accounts_products_get` for simplest API.

pub fn adexchangebuyer2_accounts_products_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    productId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/products/{}",
        accountId, productId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/products/{productId}
/// Gets the requested product by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_products_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_products_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_products_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Product = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_products_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProductsGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: productId
    pub productId: String,
}

/// GET v2beta1/accounts/{accountId}/products/{productId}
/// Gets the requested product by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_products_get_builder()` + `adexchangebuyer2_accounts_products_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_products_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProductsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        adexchangebuyer2_accounts_products_get_builder(client, &args.accountId, &args.productId)?;
    adexchangebuyer2_accounts_products_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/products
/// List all products visible to the buyer (optionally filtered by the specified PQL query).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_products_list_execute()` to send, or `adexchangebuyer2_accounts_products_list` for simplest API.

pub fn adexchangebuyer2_accounts_products_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/products",
        accountId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/products
/// List all products visible to the buyer (optionally filtered by the specified PQL query).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_products_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_products_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_products_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProductsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProductsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_products_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProductsListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/products
/// List all products visible to the buyer (optionally filtered by the specified PQL query).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_products_list_builder()` + `adexchangebuyer2_accounts_products_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_products_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProductsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProductsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_products_list_builder(
        client,
        &args.accountId,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_accounts_products_list_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:accept
/// Mark the proposal as accepted at the given revision number. If the number does not match the server's revision number an ABORTED error message will be returned. This call updates the proposal_state from PROPOSED to BUYER_ACCEPTED, or from SELLER_ACCEPTED to FINALIZED. Upon calling this endpoint, the buyer implicitly agrees to the terms and conditions optionally set within the proposal by the publisher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_accept_execute()` to send, or `adexchangebuyer2_accounts_proposals_accept` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_accept_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    proposalId: &str,
    body: &AcceptProposalRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:accept",
        accountId, proposalId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:accept
/// Mark the proposal as accepted at the given revision number. If the number does not match the server's revision number an ABORTED error message will be returned. This call updates the proposal_state from PROPOSED to BUYER_ACCEPTED, or from SELLER_ACCEPTED to FINALIZED. Upon calling this endpoint, the buyer implicitly agrees to the terms and conditions optionally set within the proposal by the publisher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_proposals_accept_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_proposals_accept_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_accept_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_accept`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsAcceptArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: AcceptProposalRequest,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:accept
/// Mark the proposal as accepted at the given revision number. If the number does not match the server's revision number an ABORTED error message will be returned. This call updates the proposal_state from PROPOSED to BUYER_ACCEPTED, or from SELLER_ACCEPTED to FINALIZED. Upon calling this endpoint, the buyer implicitly agrees to the terms and conditions optionally set within the proposal by the publisher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_accept_builder()` + `adexchangebuyer2_accounts_proposals_accept_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_accept(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsAcceptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_accept_builder(
        client,
        &args.accountId,
        &args.proposalId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_accept_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:addNote
/// Create a new note and attach it to the proposal. The note is assigned a unique ID by the server. The proposal revision number will not increase when associated with a new note.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_add_note_execute()` to send, or `adexchangebuyer2_accounts_proposals_add_note` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_add_note_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    proposalId: &str,
    body: &AddNoteRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:addNote",
        accountId, proposalId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:addNote
/// Create a new note and attach it to the proposal. The note is assigned a unique ID by the server. The proposal revision number will not increase when associated with a new note.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_proposals_add_note_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_proposals_add_note_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_add_note_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Note = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_add_note`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsAddNoteArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: AddNoteRequest,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:addNote
/// Create a new note and attach it to the proposal. The note is assigned a unique ID by the server. The proposal revision number will not increase when associated with a new note.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_add_note_builder()` + `adexchangebuyer2_accounts_proposals_add_note_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_add_note(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsAddNoteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_add_note_builder(
        client,
        &args.accountId,
        &args.proposalId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_add_note_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:cancelNegotiation
/// Cancel an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized, but only cancels a negotiation unilaterally.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_cancel_negotiation_execute()` to send, or `adexchangebuyer2_accounts_proposals_cancel_negotiation` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_cancel_negotiation_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    proposalId: &str,
    body: &CancelNegotiationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:cancelNegotiation",
        accountId, proposalId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:cancelNegotiation
/// Cancel an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized, but only cancels a negotiation unilaterally.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_proposals_cancel_negotiation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_proposals_cancel_negotiation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_cancel_negotiation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_cancel_negotiation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsCancelNegotiationArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: CancelNegotiationRequest,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:cancelNegotiation
/// Cancel an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized, but only cancels a negotiation unilaterally.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_cancel_negotiation_builder()` + `adexchangebuyer2_accounts_proposals_cancel_negotiation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_cancel_negotiation(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsCancelNegotiationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_cancel_negotiation_builder(
        client,
        &args.accountId,
        &args.proposalId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_cancel_negotiation_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:completeSetup
/// You can opt-in to manually update proposals to indicate that setup is complete. By default, proposal setup is automatically completed after their deals are finalized. Contact your Technical Account Manager to opt in. Buyers can call this method when the proposal has been finalized, and all the required creatives have been uploaded using the Creatives API. This call updates the is_setup_completed field on the deals in the proposal, and notifies the seller. The server then advances the revision number of the most recent proposal. To mark an individual deal as ready to serve, call buyers.`finalizedDeals`.`setReadyToServe` in the Marketplace API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_complete_setup_execute()` to send, or `adexchangebuyer2_accounts_proposals_complete_setup` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_complete_setup_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    proposalId: &str,
    body: &CompleteSetupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:completeSetup",
        accountId, proposalId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:completeSetup
/// You can opt-in to manually update proposals to indicate that setup is complete. By default, proposal setup is automatically completed after their deals are finalized. Contact your Technical Account Manager to opt in. Buyers can call this method when the proposal has been finalized, and all the required creatives have been uploaded using the Creatives API. This call updates the is_setup_completed field on the deals in the proposal, and notifies the seller. The server then advances the revision number of the most recent proposal. To mark an individual deal as ready to serve, call buyers.`finalizedDeals`.`setReadyToServe` in the Marketplace API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_proposals_complete_setup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_proposals_complete_setup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_complete_setup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_complete_setup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsCompleteSetupArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: CompleteSetupRequest,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:completeSetup
/// You can opt-in to manually update proposals to indicate that setup is complete. By default, proposal setup is automatically completed after their deals are finalized. Contact your Technical Account Manager to opt in. Buyers can call this method when the proposal has been finalized, and all the required creatives have been uploaded using the Creatives API. This call updates the is_setup_completed field on the deals in the proposal, and notifies the seller. The server then advances the revision number of the most recent proposal. To mark an individual deal as ready to serve, call buyers.`finalizedDeals`.`setReadyToServe` in the Marketplace API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_complete_setup_builder()` + `adexchangebuyer2_accounts_proposals_complete_setup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_complete_setup(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsCompleteSetupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_complete_setup_builder(
        client,
        &args.accountId,
        &args.proposalId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_complete_setup_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals
/// Create the given proposal. Each created proposal and any deals it contains are assigned a unique ID by the server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_create_execute()` to send, or `adexchangebuyer2_accounts_proposals_create` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_create_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    body: &Proposal,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals",
        accountId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals
/// Create the given proposal. Each created proposal and any deals it contains are assigned a unique ID by the server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_proposals_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_proposals_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsCreateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Request body.
    pub body: Proposal,
}

/// GET v2beta1/accounts/{accountId}/proposals
/// Create the given proposal. Each created proposal and any deals it contains are assigned a unique ID by the server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_create_builder()` + `adexchangebuyer2_accounts_proposals_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        adexchangebuyer2_accounts_proposals_create_builder(client, &args.accountId, &args.body)?;
    adexchangebuyer2_accounts_proposals_create_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}
/// Gets a proposal given its ID. The proposal is returned at its head revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_get_execute()` to send, or `adexchangebuyer2_accounts_proposals_get` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    proposalId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}",
        accountId, proposalId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}
/// Gets a proposal given its ID. The proposal is returned at its head revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_proposals_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_proposals_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}
/// Gets a proposal given its ID. The proposal is returned at its head revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_get_builder()` + `adexchangebuyer2_accounts_proposals_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        adexchangebuyer2_accounts_proposals_get_builder(client, &args.accountId, &args.proposalId)?;
    adexchangebuyer2_accounts_proposals_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals
/// List proposals. A filter expression (PQL query) may be specified to filter the results. To retrieve all finalized proposals, regardless if a proposal is being renegotiated, see the FinalizedProposals resource. Note that B`idder/ChildSeat` relationships differ from the usual behavior. A Bidder account can only see its child seats' proposals by specifying the ChildSeat's `accountId` in the request path.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_list_execute()` to send, or `adexchangebuyer2_accounts_proposals_list` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    filter: Option<&str>,
    filterSyntax: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals",
        accountId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = filterSyntax {
        query_parts.push(format!("filterSyntax={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals
/// List proposals. A filter expression (PQL query) may be specified to filter the results. To retrieve all finalized proposals, regardless if a proposal is being renegotiated, see the FinalizedProposals resource. Note that B`idder/ChildSeat` relationships differ from the usual behavior. A Bidder account can only see its child seats' proposals by specifying the ChildSeat's `accountId` in the request path.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_proposals_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_proposals_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProposalsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProposalsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: filterSyntax
    pub filterSyntax: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/proposals
/// List proposals. A filter expression (PQL query) may be specified to filter the results. To retrieve all finalized proposals, regardless if a proposal is being renegotiated, see the FinalizedProposals resource. Note that B`idder/ChildSeat` relationships differ from the usual behavior. A Bidder account can only see its child seats' proposals by specifying the ChildSeat's `accountId` in the request path.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_list_builder()` + `adexchangebuyer2_accounts_proposals_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProposalsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_list_builder(
        client,
        &args.accountId,
        args.filter.as_deref(),
        args.filterSyntax.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_accounts_proposals_list_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:pause
/// Update the given proposal to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all deals in the proposal. It is a no-op to pause an already-paused proposal. It is an error to call PauseProposal for a proposal that is not finalized or renegotiating.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_pause_execute()` to send, or `adexchangebuyer2_accounts_proposals_pause` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_pause_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    proposalId: &str,
    body: &PauseProposalRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:pause",
        accountId, proposalId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:pause
/// Update the given proposal to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all deals in the proposal. It is a no-op to pause an already-paused proposal. It is an error to call PauseProposal for a proposal that is not finalized or renegotiating.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_proposals_pause_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_proposals_pause_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_pause_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_pause`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsPauseArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: PauseProposalRequest,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:pause
/// Update the given proposal to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all deals in the proposal. It is a no-op to pause an already-paused proposal. It is an error to call PauseProposal for a proposal that is not finalized or renegotiating.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_pause_builder()` + `adexchangebuyer2_accounts_proposals_pause_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_pause(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsPauseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_pause_builder(
        client,
        &args.accountId,
        &args.proposalId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_pause_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:resume
/// Update the given proposal to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all deals in the proposal. Note that if the has_seller_paused bit is also set, serving will not resume until the seller also resumes. It is a no-op to resume an already-running proposal. It is an error to call ResumeProposal for a proposal that is not finalized or renegotiating.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_resume_execute()` to send, or `adexchangebuyer2_accounts_proposals_resume` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_resume_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    proposalId: &str,
    body: &ResumeProposalRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:resume",
        accountId, proposalId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:resume
/// Update the given proposal to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all deals in the proposal. Note that if the has_seller_paused bit is also set, serving will not resume until the seller also resumes. It is a no-op to resume an already-running proposal. It is an error to call ResumeProposal for a proposal that is not finalized or renegotiating.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_proposals_resume_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_proposals_resume_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_resume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsResumeArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: ResumeProposalRequest,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:resume
/// Update the given proposal to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all deals in the proposal. Note that if the has_seller_paused bit is also set, serving will not resume until the seller also resumes. It is a no-op to resume an already-running proposal. It is an error to call ResumeProposal for a proposal that is not finalized or renegotiating.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_resume_builder()` + `adexchangebuyer2_accounts_proposals_resume_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_resume(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsResumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_resume_builder(
        client,
        &args.accountId,
        &args.proposalId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_resume_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}
/// Update the given proposal at the client known revision number. If the server revision has advanced since the passed-in proposal.proposal_revision, an ABORTED error message will be returned. Only the buyer-modifiable fields of the proposal will be updated. Note that the deals in the proposal will be updated to match the passed-in copy. If a passed-in deal does not have a deal_id, the server will assign a new unique ID and create the deal. If passed-in deal has a deal_id, it will be updated to match the passed-in copy. Any existing deals not present in the passed-in proposal will be deleted. It is an error to pass in a deal with a deal_id not present at head.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_update_execute()` to send, or `adexchangebuyer2_accounts_proposals_update` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_update_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    proposalId: &str,
    body: &Proposal,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}",
        accountId, proposalId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}
/// Update the given proposal at the client known revision number. If the server revision has advanced since the passed-in proposal.proposal_revision, an ABORTED error message will be returned. Only the buyer-modifiable fields of the proposal will be updated. Note that the deals in the proposal will be updated to match the passed-in copy. If a passed-in deal does not have a deal_id, the server will assign a new unique ID and create the deal. If passed-in deal has a deal_id, it will be updated to match the passed-in copy. Any existing deals not present in the passed-in proposal will be deleted. It is an error to pass in a deal with a deal_id not present at head.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_proposals_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_proposals_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsUpdateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: Proposal,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}
/// Update the given proposal at the client known revision number. If the server revision has advanced since the passed-in proposal.proposal_revision, an ABORTED error message will be returned. Only the buyer-modifiable fields of the proposal will be updated. Note that the deals in the proposal will be updated to match the passed-in copy. If a passed-in deal does not have a deal_id, the server will assign a new unique ID and create the deal. If passed-in deal has a deal_id, it will be updated to match the passed-in copy. Any existing deals not present in the passed-in proposal will be deleted. It is an error to pass in a deal with a deal_id not present at head.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_update_builder()` + `adexchangebuyer2_accounts_proposals_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_update(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_update_builder(
        client,
        &args.accountId,
        &args.proposalId,
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_update_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles/{publisherProfileId}
/// Gets the requested publisher profile by id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_publisher_profiles_get_execute()` to send, or `adexchangebuyer2_accounts_publisher_profiles_get` for simplest API.

pub fn adexchangebuyer2_accounts_publisher_profiles_get_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    publisherProfileId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/publisherProfiles/{}",
        accountId, publisherProfileId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles/{publisherProfileId}
/// Gets the requested publisher profile by id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_publisher_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_publisher_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_publisher_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublisherProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublisherProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_publisher_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsPublisherProfilesGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: publisherProfileId
    pub publisherProfileId: String,
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles/{publisherProfileId}
/// Gets the requested publisher profile by id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_publisher_profiles_get_builder()` + `adexchangebuyer2_accounts_publisher_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_publisher_profiles_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsPublisherProfilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublisherProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_publisher_profiles_get_builder(
        client,
        &args.accountId,
        &args.publisherProfileId,
    )?;
    adexchangebuyer2_accounts_publisher_profiles_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles
/// List all publisher profiles visible to the buyer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_publisher_profiles_list_execute()` to send, or `adexchangebuyer2_accounts_publisher_profiles_list` for simplest API.

pub fn adexchangebuyer2_accounts_publisher_profiles_list_builder(
    client: &SimpleHttpClient,
    accountId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/publisherProfiles",
        accountId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles
/// List all publisher profiles visible to the buyer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_accounts_publisher_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_accounts_publisher_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_publisher_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPublisherProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPublisherProfilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_publisher_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsPublisherProfilesListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles
/// List all publisher profiles visible to the buyer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_publisher_profiles_list_builder()` + `adexchangebuyer2_accounts_publisher_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_publisher_profiles_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsPublisherProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPublisherProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_publisher_profiles_list_builder(
        client,
        &args.accountId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_accounts_publisher_profiles_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_create_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_create` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_create_builder(
    client: &SimpleHttpClient,
    ownerName: &str,
    isTransient: Option<bool>,
    body: &FilterSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets",
        ownerName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = isTransient {
        query_parts.push(format!("isTransient={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FilterSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsCreateArgs {
    /// Path parameter: ownerName
    pub ownerName: String,
    /// Query parameter: isTransient
    pub isTransient: Option<bool>,
    /// Request body.
    pub body: FilterSet,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_create_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_accounts_filter_sets_create_builder(
        client,
        &args.ownerName,
        args.isTransient,
        &args.body,
    )?;
    adexchangebuyer2_bidders_accounts_filter_sets_create_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}
/// Deletes the requested filter set from the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_delete_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_delete` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}
/// Deletes the requested filter set from the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}
/// Deletes the requested filter set from the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_delete_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_delete(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_accounts_filter_sets_delete_builder(client, &args.name)?;
    adexchangebuyer2_bidders_accounts_filter_sets_delete_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}
/// Retrieves the requested filter set for the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_get_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_get` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}
/// Retrieves the requested filter set for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FilterSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}
/// Retrieves the requested filter set for the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_get_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_accounts_filter_sets_get_builder(client, &args.name)?;
    adexchangebuyer2_bidders_accounts_filter_sets_get_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets
/// Lists all filter sets for the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_list_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_list` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_list_builder(
    client: &SimpleHttpClient,
    ownerName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets",
        ownerName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets
/// Lists all filter sets for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFilterSetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFilterSetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsListArgs {
    /// Path parameter: ownerName
    pub ownerName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets
/// Lists all filter sets for the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_list_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFilterSetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_accounts_filter_sets_list_builder(
        client,
        &args.ownerName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_accounts_filter_sets_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/bidMetrics
/// Lists all metrics that are measured in terms of number of bids.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets/{}/bidMetrics",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/bidMetrics
/// Lists all metrics that are measured in terms of number of bids.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBidMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBidMetricsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsBidMetricsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/bidMetrics
/// Lists all metrics that are measured in terms of number of bids.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsBidMetricsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBidMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/bidResponseErrors
/// List all errors that occurred in bid responses, with the number of bid responses affected for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets/{}/bidResponseErrors",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/bidResponseErrors
/// List all errors that occurred in bid responses, with the number of bid responses affected for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBidResponseErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBidResponseErrorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsBidResponseErrorsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/bidResponseErrors
/// List all errors that occurred in bid responses, with the number of bid responses affected for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsBidResponseErrorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBidResponseErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/bidResponsesWithoutBids
/// List all reasons for which bid responses were considered to have no applicable bids, with the number of bid responses affected for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets/{}/bidResponsesWithoutBids",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/bidResponsesWithoutBids
/// List all reasons for which bid responses were considered to have no applicable bids, with the number of bid responses affected for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBidResponsesWithoutBidsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBidResponsesWithoutBidsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsBidResponsesWithoutBidsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/bidResponsesWithoutBids
/// List all reasons for which bid responses were considered to have no applicable bids, with the number of bid responses affected for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsBidResponsesWithoutBidsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBidResponsesWithoutBidsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list_builder(
            client,
            &args.filterSetName,
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/filteredBidRequests
/// List all reasons that caused a bid request not to be sent for an impression, with the number of bid requests not sent for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets/{}/filteredBidRequests",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/filteredBidRequests
/// List all reasons that caused a bid request not to be sent for an impression, with the number of bid requests not sent for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFilteredBidRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFilteredBidRequestsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidRequestsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/filteredBidRequests
/// List all reasons that caused a bid request not to be sent for an impression, with the number of bid requests not sent for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidRequestsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFilteredBidRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/filteredBids
/// List all reasons for which bids were filtered, with the number of bids filtered for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets/{}/filteredBids",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/filteredBids
/// List all reasons for which bids were filtered, with the number of bids filtered for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFilteredBidsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFilteredBidsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/filteredBids
/// List all reasons for which bids were filtered, with the number of bids filtered for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFilteredBidsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/creatives
/// List all creatives associated with a specific reason for which bids were filtered, with the number of bids filtered for each creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    creativeStatusId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets/{}/filteredBids/{}/creatives",
        filterSetName,
        creativeStatusId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/creatives
/// List all creatives associated with a specific reason for which bids were filtered, with the number of bids filtered for each creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCreativeStatusBreakdownByCreativeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCreativeStatusBreakdownByCreativeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidsCreativesListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Path parameter: creativeStatusId
    pub creativeStatusId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/creatives
/// List all creatives associated with a specific reason for which bids were filtered, with the number of bids filtered for each creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidsCreativesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCreativeStatusBreakdownByCreativeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list_builder(
            client,
            &args.filterSetName,
            &args.creativeStatusId,
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/details
/// List all details associated with a specific reason for which bids were filtered, with the number of bids filtered for each detail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    creativeStatusId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets/{}/filteredBids/{}/details",
        filterSetName,
        creativeStatusId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/details
/// List all details associated with a specific reason for which bids were filtered, with the number of bids filtered for each detail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCreativeStatusBreakdownByDetailResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCreativeStatusBreakdownByDetailResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidsDetailsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Path parameter: creativeStatusId
    pub creativeStatusId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/details
/// List all details associated with a specific reason for which bids were filtered, with the number of bids filtered for each detail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidsDetailsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCreativeStatusBreakdownByDetailResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list_builder(
        client,
        &args.filterSetName,
        &args.creativeStatusId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/impressionMetrics
/// Lists all metrics that are measured in terms of number of impressions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets/{}/impressionMetrics",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/impressionMetrics
/// Lists all metrics that are measured in terms of number of impressions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListImpressionMetricsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListImpressionMetricsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsImpressionMetricsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/impressionMetrics
/// Lists all metrics that are measured in terms of number of impressions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsImpressionMetricsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListImpressionMetricsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/losingBids
/// List all reasons for which bids lost in the auction, with the number of bids that lost for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets/{}/losingBids",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/losingBids
/// List all reasons for which bids lost in the auction, with the number of bids that lost for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLosingBidsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLosingBidsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsLosingBidsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/losingBids
/// List all reasons for which bids lost in the auction, with the number of bids that lost for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsLosingBidsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLosingBidsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/nonBillableWinningBids
/// List all reasons for which winning bids were not billable, with the number of bids not billed for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list_execute()` to send, or `adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list` for simplest API.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/accounts/{}/filterSets/{}/nonBillableWinningBids",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/nonBillableWinningBids
/// List all reasons for which winning bids were not billable, with the number of bids not billed for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNonBillableWinningBidsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNonBillableWinningBidsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsNonBillableWinningBidsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/accounts/{accountsId}/filterSets/{filterSetsId}/nonBillableWinningBids
/// List all reasons for which winning bids were not billable, with the number of bids not billed for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list_builder()` + `adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsNonBillableWinningBidsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNonBillableWinningBidsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list_builder(
            client,
            &args.filterSetName,
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_create_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_create` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_create_builder(
    client: &SimpleHttpClient,
    ownerName: &str,
    isTransient: Option<bool>,
    body: &FilterSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets",
        ownerName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = isTransient {
        query_parts.push(format!("isTransient={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/bidders/{biddersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FilterSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsCreateArgs {
    /// Path parameter: ownerName
    pub ownerName: String,
    /// Query parameter: isTransient
    pub isTransient: Option<bool>,
    /// Request body.
    pub body: FilterSet,
}

/// GET v2beta1/bidders/{biddersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_create_builder()` + `adexchangebuyer2_bidders_filter_sets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_create_builder(
        client,
        &args.ownerName,
        args.isTransient,
        &args.body,
    )?;
    adexchangebuyer2_bidders_filter_sets_create_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}
/// Deletes the requested filter set from the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_delete_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_delete` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}
/// Deletes the requested filter set from the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}
/// Deletes the requested filter set from the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_delete_builder()` + `adexchangebuyer2_bidders_filter_sets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_delete(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_delete_builder(client, &args.name)?;
    adexchangebuyer2_bidders_filter_sets_delete_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}
/// Retrieves the requested filter set for the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_get_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_get` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}
/// Retrieves the requested filter set for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FilterSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}
/// Retrieves the requested filter set for the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_get_builder()` + `adexchangebuyer2_bidders_filter_sets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_get_builder(client, &args.name)?;
    adexchangebuyer2_bidders_filter_sets_get_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets
/// Lists all filter sets for the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_list_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_list` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_list_builder(
    client: &SimpleHttpClient,
    ownerName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets",
        ownerName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets
/// Lists all filter sets for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFilterSetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFilterSetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsListArgs {
    /// Path parameter: ownerName
    pub ownerName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/filterSets
/// Lists all filter sets for the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_list_builder()` + `adexchangebuyer2_bidders_filter_sets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFilterSetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_list_builder(
        client,
        &args.ownerName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_filter_sets_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/bidMetrics
/// Lists all metrics that are measured in terms of number of bids.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_bid_metrics_list_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_bid_metrics_list` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_bid_metrics_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets/{}/bidMetrics",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/bidMetrics
/// Lists all metrics that are measured in terms of number of bids.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_bid_metrics_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_bid_metrics_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_bid_metrics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBidMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBidMetricsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_bid_metrics_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsBidMetricsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/bidMetrics
/// Lists all metrics that are measured in terms of number of bids.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_bid_metrics_list_builder()` + `adexchangebuyer2_bidders_filter_sets_bid_metrics_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_bid_metrics_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsBidMetricsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBidMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_bid_metrics_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_filter_sets_bid_metrics_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/bidResponseErrors
/// List all errors that occurred in bid responses, with the number of bid responses affected for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_bid_response_errors_list_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_bid_response_errors_list` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_bid_response_errors_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets/{}/bidResponseErrors",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/bidResponseErrors
/// List all errors that occurred in bid responses, with the number of bid responses affected for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_bid_response_errors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_bid_response_errors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_bid_response_errors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBidResponseErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBidResponseErrorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_bid_response_errors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsBidResponseErrorsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/bidResponseErrors
/// List all errors that occurred in bid responses, with the number of bid responses affected for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_bid_response_errors_list_builder()` + `adexchangebuyer2_bidders_filter_sets_bid_response_errors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_bid_response_errors_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsBidResponseErrorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBidResponseErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_bid_response_errors_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_filter_sets_bid_response_errors_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/bidResponsesWithoutBids
/// List all reasons for which bid responses were considered to have no applicable bids, with the number of bid responses affected for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets/{}/bidResponsesWithoutBids",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/bidResponsesWithoutBids
/// List all reasons for which bid responses were considered to have no applicable bids, with the number of bid responses affected for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBidResponsesWithoutBidsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBidResponsesWithoutBidsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsBidResponsesWithoutBidsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/bidResponsesWithoutBids
/// List all reasons for which bid responses were considered to have no applicable bids, with the number of bid responses affected for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list_builder()` + `adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsBidResponsesWithoutBidsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBidResponsesWithoutBidsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/filteredBidRequests
/// List all reasons that caused a bid request not to be sent for an impression, with the number of bid requests not sent for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets/{}/filteredBidRequests",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/filteredBidRequests
/// List all reasons that caused a bid request not to be sent for an impression, with the number of bid requests not sent for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFilteredBidRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFilteredBidRequestsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsFilteredBidRequestsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/filteredBidRequests
/// List all reasons that caused a bid request not to be sent for an impression, with the number of bid requests not sent for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list_builder()` + `adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsFilteredBidRequestsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFilteredBidRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/filteredBids
/// List all reasons for which bids were filtered, with the number of bids filtered for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_filtered_bids_list_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_filtered_bids_list` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_filtered_bids_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets/{}/filteredBids",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/filteredBids
/// List all reasons for which bids were filtered, with the number of bids filtered for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_filtered_bids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_filtered_bids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_filtered_bids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFilteredBidsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFilteredBidsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_filtered_bids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsFilteredBidsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/filteredBids
/// List all reasons for which bids were filtered, with the number of bids filtered for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_filtered_bids_list_builder()` + `adexchangebuyer2_bidders_filter_sets_filtered_bids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_filtered_bids_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsFilteredBidsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFilteredBidsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_filtered_bids_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_filter_sets_filtered_bids_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/creatives
/// List all creatives associated with a specific reason for which bids were filtered, with the number of bids filtered for each creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    creativeStatusId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets/{}/filteredBids/{}/creatives",
        filterSetName,
        creativeStatusId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/creatives
/// List all creatives associated with a specific reason for which bids were filtered, with the number of bids filtered for each creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCreativeStatusBreakdownByCreativeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCreativeStatusBreakdownByCreativeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsFilteredBidsCreativesListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Path parameter: creativeStatusId
    pub creativeStatusId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/creatives
/// List all creatives associated with a specific reason for which bids were filtered, with the number of bids filtered for each creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list_builder()` + `adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsFilteredBidsCreativesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCreativeStatusBreakdownByCreativeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list_builder(
        client,
        &args.filterSetName,
        &args.creativeStatusId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/details
/// List all details associated with a specific reason for which bids were filtered, with the number of bids filtered for each detail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    creativeStatusId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets/{}/filteredBids/{}/details",
        filterSetName,
        creativeStatusId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/details
/// List all details associated with a specific reason for which bids were filtered, with the number of bids filtered for each detail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCreativeStatusBreakdownByDetailResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCreativeStatusBreakdownByDetailResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsFilteredBidsDetailsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Path parameter: creativeStatusId
    pub creativeStatusId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/details
/// List all details associated with a specific reason for which bids were filtered, with the number of bids filtered for each detail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list_builder()` + `adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsFilteredBidsDetailsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCreativeStatusBreakdownByDetailResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list_builder(
        client,
        &args.filterSetName,
        &args.creativeStatusId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/impressionMetrics
/// Lists all metrics that are measured in terms of number of impressions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_impression_metrics_list_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_impression_metrics_list` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_impression_metrics_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets/{}/impressionMetrics",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/impressionMetrics
/// Lists all metrics that are measured in terms of number of impressions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_impression_metrics_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_impression_metrics_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_impression_metrics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListImpressionMetricsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListImpressionMetricsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_impression_metrics_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsImpressionMetricsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/impressionMetrics
/// Lists all metrics that are measured in terms of number of impressions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_impression_metrics_list_builder()` + `adexchangebuyer2_bidders_filter_sets_impression_metrics_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_impression_metrics_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsImpressionMetricsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListImpressionMetricsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_impression_metrics_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_filter_sets_impression_metrics_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/losingBids
/// List all reasons for which bids lost in the auction, with the number of bids that lost for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_losing_bids_list_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_losing_bids_list` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_losing_bids_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets/{}/losingBids",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/losingBids
/// List all reasons for which bids lost in the auction, with the number of bids that lost for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_losing_bids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_losing_bids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_losing_bids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLosingBidsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLosingBidsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_losing_bids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsLosingBidsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/losingBids
/// List all reasons for which bids lost in the auction, with the number of bids that lost for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_losing_bids_list_builder()` + `adexchangebuyer2_bidders_filter_sets_losing_bids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_losing_bids_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsLosingBidsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLosingBidsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_losing_bids_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_filter_sets_losing_bids_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/nonBillableWinningBids
/// List all reasons for which winning bids were not billable, with the number of bids not billed for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets/{}/nonBillableWinningBids",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/nonBillableWinningBids
/// List all reasons for which winning bids were not billable, with the number of bids not billed for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNonBillableWinningBidsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNonBillableWinningBidsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsNonBillableWinningBidsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/bidders/{biddersId}/filterSets/{filterSetsId}/nonBillableWinningBids
/// List all reasons for which winning bids were not billable, with the number of bids not billed for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list_builder()` + `adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsNonBillableWinningBidsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNonBillableWinningBidsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_create_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_create` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_create_builder(
    client: &SimpleHttpClient,
    ownerName: &str,
    isTransient: Option<bool>,
    body: &FilterSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets",
        ownerName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = isTransient {
        query_parts.push(format!("isTransient={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/buyers/{buyersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FilterSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsCreateArgs {
    /// Path parameter: ownerName
    pub ownerName: String,
    /// Query parameter: isTransient
    pub isTransient: Option<bool>,
    /// Request body.
    pub body: FilterSet,
}

/// GET v2beta1/buyers/{buyersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_create_builder()` + `adexchangebuyer2_buyers_filter_sets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_create_builder(
        client,
        &args.ownerName,
        args.isTransient,
        &args.body,
    )?;
    adexchangebuyer2_buyers_filter_sets_create_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}
/// Deletes the requested filter set from the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_delete_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_delete` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}
/// Deletes the requested filter set from the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}
/// Deletes the requested filter set from the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_delete_builder()` + `adexchangebuyer2_buyers_filter_sets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_delete(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_delete_builder(client, &args.name)?;
    adexchangebuyer2_buyers_filter_sets_delete_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}
/// Retrieves the requested filter set for the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_get_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_get` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}
/// Retrieves the requested filter set for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FilterSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}
/// Retrieves the requested filter set for the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_get_builder()` + `adexchangebuyer2_buyers_filter_sets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_get_builder(client, &args.name)?;
    adexchangebuyer2_buyers_filter_sets_get_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets
/// Lists all filter sets for the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_list_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_list` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_list_builder(
    client: &SimpleHttpClient,
    ownerName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets",
        ownerName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets
/// Lists all filter sets for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFilterSetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFilterSetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsListArgs {
    /// Path parameter: ownerName
    pub ownerName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/buyers/{buyersId}/filterSets
/// Lists all filter sets for the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_list_builder()` + `adexchangebuyer2_buyers_filter_sets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFilterSetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_list_builder(
        client,
        &args.ownerName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_buyers_filter_sets_list_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/bidMetrics
/// Lists all metrics that are measured in terms of number of bids.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_bid_metrics_list_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_bid_metrics_list` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_bid_metrics_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets/{}/bidMetrics",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/bidMetrics
/// Lists all metrics that are measured in terms of number of bids.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_bid_metrics_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_bid_metrics_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_bid_metrics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBidMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBidMetricsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_bid_metrics_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsBidMetricsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/bidMetrics
/// Lists all metrics that are measured in terms of number of bids.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_bid_metrics_list_builder()` + `adexchangebuyer2_buyers_filter_sets_bid_metrics_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_bid_metrics_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsBidMetricsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBidMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_bid_metrics_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_buyers_filter_sets_bid_metrics_list_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/bidResponseErrors
/// List all errors that occurred in bid responses, with the number of bid responses affected for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_bid_response_errors_list_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_bid_response_errors_list` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_bid_response_errors_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets/{}/bidResponseErrors",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/bidResponseErrors
/// List all errors that occurred in bid responses, with the number of bid responses affected for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_bid_response_errors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_bid_response_errors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_bid_response_errors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBidResponseErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBidResponseErrorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_bid_response_errors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsBidResponseErrorsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/bidResponseErrors
/// List all errors that occurred in bid responses, with the number of bid responses affected for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_bid_response_errors_list_builder()` + `adexchangebuyer2_buyers_filter_sets_bid_response_errors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_bid_response_errors_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsBidResponseErrorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBidResponseErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_bid_response_errors_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_buyers_filter_sets_bid_response_errors_list_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/bidResponsesWithoutBids
/// List all reasons for which bid responses were considered to have no applicable bids, with the number of bid responses affected for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets/{}/bidResponsesWithoutBids",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/bidResponsesWithoutBids
/// List all reasons for which bid responses were considered to have no applicable bids, with the number of bid responses affected for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBidResponsesWithoutBidsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBidResponsesWithoutBidsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsBidResponsesWithoutBidsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/bidResponsesWithoutBids
/// List all reasons for which bid responses were considered to have no applicable bids, with the number of bid responses affected for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list_builder()` + `adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsBidResponsesWithoutBidsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBidResponsesWithoutBidsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/filteredBidRequests
/// List all reasons that caused a bid request not to be sent for an impression, with the number of bid requests not sent for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets/{}/filteredBidRequests",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/filteredBidRequests
/// List all reasons that caused a bid request not to be sent for an impression, with the number of bid requests not sent for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFilteredBidRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFilteredBidRequestsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsFilteredBidRequestsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/filteredBidRequests
/// List all reasons that caused a bid request not to be sent for an impression, with the number of bid requests not sent for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list_builder()` + `adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsFilteredBidRequestsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFilteredBidRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/filteredBids
/// List all reasons for which bids were filtered, with the number of bids filtered for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_filtered_bids_list_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_filtered_bids_list` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_filtered_bids_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets/{}/filteredBids",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/filteredBids
/// List all reasons for which bids were filtered, with the number of bids filtered for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_filtered_bids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_filtered_bids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_filtered_bids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFilteredBidsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFilteredBidsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_filtered_bids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsFilteredBidsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/filteredBids
/// List all reasons for which bids were filtered, with the number of bids filtered for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_filtered_bids_list_builder()` + `adexchangebuyer2_buyers_filter_sets_filtered_bids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_filtered_bids_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsFilteredBidsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFilteredBidsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_filtered_bids_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_buyers_filter_sets_filtered_bids_list_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/creatives
/// List all creatives associated with a specific reason for which bids were filtered, with the number of bids filtered for each creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    creativeStatusId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets/{}/filteredBids/{}/creatives",
        filterSetName,
        creativeStatusId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/creatives
/// List all creatives associated with a specific reason for which bids were filtered, with the number of bids filtered for each creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCreativeStatusBreakdownByCreativeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCreativeStatusBreakdownByCreativeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsFilteredBidsCreativesListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Path parameter: creativeStatusId
    pub creativeStatusId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/creatives
/// List all creatives associated with a specific reason for which bids were filtered, with the number of bids filtered for each creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list_builder()` + `adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsFilteredBidsCreativesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCreativeStatusBreakdownByCreativeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list_builder(
        client,
        &args.filterSetName,
        &args.creativeStatusId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/details
/// List all details associated with a specific reason for which bids were filtered, with the number of bids filtered for each detail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    creativeStatusId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets/{}/filteredBids/{}/details",
        filterSetName,
        creativeStatusId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/details
/// List all details associated with a specific reason for which bids were filtered, with the number of bids filtered for each detail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCreativeStatusBreakdownByDetailResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCreativeStatusBreakdownByDetailResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsFilteredBidsDetailsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Path parameter: creativeStatusId
    pub creativeStatusId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/filteredBids/{creativeStatusId}/details
/// List all details associated with a specific reason for which bids were filtered, with the number of bids filtered for each detail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list_builder()` + `adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsFilteredBidsDetailsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCreativeStatusBreakdownByDetailResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list_builder(
        client,
        &args.filterSetName,
        &args.creativeStatusId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/impressionMetrics
/// Lists all metrics that are measured in terms of number of impressions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_impression_metrics_list_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_impression_metrics_list` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_impression_metrics_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets/{}/impressionMetrics",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/impressionMetrics
/// Lists all metrics that are measured in terms of number of impressions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_impression_metrics_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_impression_metrics_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_impression_metrics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListImpressionMetricsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListImpressionMetricsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_impression_metrics_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsImpressionMetricsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/impressionMetrics
/// Lists all metrics that are measured in terms of number of impressions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_impression_metrics_list_builder()` + `adexchangebuyer2_buyers_filter_sets_impression_metrics_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_impression_metrics_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsImpressionMetricsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListImpressionMetricsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_impression_metrics_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_buyers_filter_sets_impression_metrics_list_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/losingBids
/// List all reasons for which bids lost in the auction, with the number of bids that lost for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_losing_bids_list_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_losing_bids_list` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_losing_bids_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets/{}/losingBids",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/losingBids
/// List all reasons for which bids lost in the auction, with the number of bids that lost for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_losing_bids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_losing_bids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_losing_bids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLosingBidsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLosingBidsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_losing_bids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsLosingBidsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/losingBids
/// List all reasons for which bids lost in the auction, with the number of bids that lost for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_losing_bids_list_builder()` + `adexchangebuyer2_buyers_filter_sets_losing_bids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_losing_bids_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsLosingBidsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLosingBidsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_losing_bids_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_buyers_filter_sets_losing_bids_list_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/nonBillableWinningBids
/// List all reasons for which winning bids were not billable, with the number of bids not billed for each reason.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list_builder(
    client: &SimpleHttpClient,
    filterSetName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets/{}/nonBillableWinningBids",
        filterSetName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/nonBillableWinningBids
/// List all reasons for which winning bids were not billable, with the number of bids not billed for each reason.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNonBillableWinningBidsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNonBillableWinningBidsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsNonBillableWinningBidsListArgs {
    /// Path parameter: filterSetName
    pub filterSetName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/buyers/{buyersId}/filterSets/{filterSetsId}/nonBillableWinningBids
/// List all reasons for which winning bids were not billable, with the number of bids not billed for each reason.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list_builder()` + `adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsNonBillableWinningBidsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNonBillableWinningBidsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list_builder(
        client,
        &args.filterSetName,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list_execute(builder)
}
