//! Auto-generated API clients for gcp/adexchangebuyer2.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2beta1/accounts/{accountId}/clients
/// Creates a new client buyer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_create_execute()` to send, or `adexchangebuyer2_accounts_clients_create` for simplest API.

pub fn adexchangebuyer2_accounts_clients_create_builder(
    client: &SimpleHttpClient,
    accountId: String,
    body: &Client,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients",
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/clients
/// Creates a new client buyer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_clients_create_execute()` or `adexchangebuyer2_accounts_clients_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_clients_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Client>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/clients
/// Creates a new client buyer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_clients_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_clients_create_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_clients_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_clients_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_clients_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_clients_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsCreateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Request body.
    pub body: Client,
}

/// GET v2beta1/accounts/{accountId}/clients
/// Creates a new client buyer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_create_builder()` + `adexchangebuyer2_accounts_clients_create_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_clients_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_create_builder(
        client,
        args.accountId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_clients_create_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}
/// Gets a client buyer with a given client account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_get_execute()` to send, or `adexchangebuyer2_accounts_clients_get` for simplest API.

pub fn adexchangebuyer2_accounts_clients_get_builder(
    client: &SimpleHttpClient,
    accountId: String,
    clientAccountId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}",
        accountId.as_str(),
        clientAccountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}
/// Gets a client buyer with a given client account ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_clients_get_execute()` or `adexchangebuyer2_accounts_clients_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_clients_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Client>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}
/// Gets a client buyer with a given client account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_clients_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_clients_get_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_clients_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_clients_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_clients_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_clients_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}
/// Gets a client buyer with a given client account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_get_builder()` + `adexchangebuyer2_accounts_clients_get_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_clients_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_get_builder(
        client,
        args.accountId.clone(),
        args.clientAccountId.clone(),
    )?;
    adexchangebuyer2_accounts_clients_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations
/// Creates and sends out an email invitation to access an Ad Exchange client buyer account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_invitations_create_execute()` to send, or `adexchangebuyer2_accounts_clients_invitations_create` for simplest API.

pub fn adexchangebuyer2_accounts_clients_invitations_create_builder(
    client: &SimpleHttpClient,
    accountId: String,
    clientAccountId: String,
    body: &ClientUserInvitation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/invitations",
        accountId.as_str(),
        clientAccountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations
/// Creates and sends out an email invitation to access an Ad Exchange client buyer account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_clients_invitations_create_execute()` or `adexchangebuyer2_accounts_clients_invitations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_clients_invitations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_invitations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClientUserInvitation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUserInvitation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations
/// Creates and sends out an email invitation to access an Ad Exchange client buyer account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_clients_invitations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_clients_invitations_create_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_clients_invitations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_clients_invitations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_clients_invitations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUserInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_clients_invitations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_invitations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsInvitationsCreateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
    /// Request body.
    pub body: ClientUserInvitation,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations
/// Creates and sends out an email invitation to access an Ad Exchange client buyer account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_invitations_create_builder()` + `adexchangebuyer2_accounts_clients_invitations_create_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_clients_invitations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_invitations_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsInvitationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUserInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_invitations_create_builder(
        client,
        args.accountId.clone(),
        args.clientAccountId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_clients_invitations_create_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations/{invitationId}
/// Retrieves an existing client user invitation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_invitations_get_execute()` to send, or `adexchangebuyer2_accounts_clients_invitations_get` for simplest API.

pub fn adexchangebuyer2_accounts_clients_invitations_get_builder(
    client: &SimpleHttpClient,
    accountId: String,
    clientAccountId: String,
    invitationId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/invitations/{}",
        accountId.as_str(),
        clientAccountId.as_str(),
        invitationId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations/{invitationId}
/// Retrieves an existing client user invitation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_clients_invitations_get_execute()` or `adexchangebuyer2_accounts_clients_invitations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_clients_invitations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_invitations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClientUserInvitation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUserInvitation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations/{invitationId}
/// Retrieves an existing client user invitation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_clients_invitations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_clients_invitations_get_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_clients_invitations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_clients_invitations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_clients_invitations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUserInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_clients_invitations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_invitations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsInvitationsGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
    /// Path parameter: invitationId
    pub invitationId: String,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations/{invitationId}
/// Retrieves an existing client user invitation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_invitations_get_builder()` + `adexchangebuyer2_accounts_clients_invitations_get_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_clients_invitations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_invitations_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsInvitationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUserInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_invitations_get_builder(
        client,
        args.accountId.clone(),
        args.clientAccountId.clone(),
        args.invitationId.clone(),
    )?;
    adexchangebuyer2_accounts_clients_invitations_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}
/// Retrieves an existing client user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_users_get_execute()` to send, or `adexchangebuyer2_accounts_clients_users_get` for simplest API.

pub fn adexchangebuyer2_accounts_clients_users_get_builder(
    client: &SimpleHttpClient,
    accountId: String,
    clientAccountId: String,
    userId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/users/{}",
        accountId.as_str(),
        clientAccountId.as_str(),
        userId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}
/// Retrieves an existing client user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_clients_users_get_execute()` or `adexchangebuyer2_accounts_clients_users_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_clients_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_users_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClientUser>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}
/// Retrieves an existing client user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_clients_users_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_clients_users_get_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_clients_users_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_clients_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_clients_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_clients_users_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_users_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsUsersGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}
/// Retrieves an existing client user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_users_get_builder()` + `adexchangebuyer2_accounts_clients_users_get_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_clients_users_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_users_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsUsersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_users_get_builder(
        client,
        args.accountId.clone(),
        args.clientAccountId.clone(),
        args.userId.clone(),
    )?;
    adexchangebuyer2_accounts_clients_users_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users
/// Lists all the known client users for a specified sponsor buyer account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_clients_users_list_execute()` to send, or `adexchangebuyer2_accounts_clients_users_list` for simplest API.

pub fn adexchangebuyer2_accounts_clients_users_list_builder(
    client: &SimpleHttpClient,
    accountId: String,
    clientAccountId: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/users",
        accountId.as_str(),
        clientAccountId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users
/// Lists all the known client users for a specified sponsor buyer account ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_clients_users_list_execute()` or `adexchangebuyer2_accounts_clients_users_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_clients_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_users_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClientUsersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClientUsersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users
/// Lists all the known client users for a specified sponsor buyer account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_clients_users_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_clients_users_list_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_clients_users_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_clients_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_clients_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_clients_users_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_clients_users_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsUsersListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: clientAccountId
    pub clientAccountId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users
/// Lists all the known client users for a specified sponsor buyer account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_clients_users_list_builder()` + `adexchangebuyer2_accounts_clients_users_list_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_clients_users_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_clients_users_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsClientsUsersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_clients_users_list_builder(
        client,
        args.accountId.clone(),
        args.clientAccountId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    adexchangebuyer2_accounts_clients_users_list_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives
/// Creates a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_create_execute()` to send, or `adexchangebuyer2_accounts_creatives_create` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_create_builder(
    client: &SimpleHttpClient,
    accountId: String,
    duplicateIdMode: Option<String>,
    body: &Creative,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives",
        accountId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = duplicateIdMode {
        query_parts.push(format!("duplicateIdMode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/creatives
/// Creates a creative.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_creatives_create_execute()` or `adexchangebuyer2_accounts_creatives_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/creatives
/// Creates a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_creatives_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_create_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_creatives_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_creatives_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_creatives_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesCreateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: duplicateIdMode
    pub duplicateIdMode: Option<String>,
    /// Request body.
    pub body: Creative,
}

/// GET v2beta1/accounts/{accountId}/creatives
/// Creates a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_create_builder()` + `adexchangebuyer2_accounts_creatives_create_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_create_builder(
        client,
        args.accountId.clone(),
        args.duplicateIdMode.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_creatives_create_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}
/// Gets a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_get_execute()` to send, or `adexchangebuyer2_accounts_creatives_get` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_get_builder(
    client: &SimpleHttpClient,
    accountId: String,
    creativeId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}",
        accountId.as_str(),
        creativeId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}
/// Gets a creative.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_creatives_get_execute()` or `adexchangebuyer2_accounts_creatives_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}
/// Gets a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_creatives_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_get_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_creatives_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_creatives_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_creatives_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}
/// Gets a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_get_builder()` + `adexchangebuyer2_accounts_creatives_get_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_get_builder(
        client,
        args.accountId.clone(),
        args.creativeId.clone(),
    )?;
    adexchangebuyer2_accounts_creatives_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:stopWatching
/// Stops watching a creative. Will stop push notifications being sent to the topics when the creative changes status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_stop_watching_execute()` to send, or `adexchangebuyer2_accounts_creatives_stop_watching` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_stop_watching_builder(
    client: &SimpleHttpClient,
    accountId: String,
    creativeId: String,
    body: &StopWatchingCreativeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}:stopWatching",
        accountId.as_str(),
        creativeId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:stopWatching
/// Stops watching a creative. Will stop push notifications being sent to the topics when the creative changes status.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_creatives_stop_watching_execute()` or `adexchangebuyer2_accounts_creatives_stop_watching`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_stop_watching_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_stop_watching_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:stopWatching
/// Stops watching a creative. Will stop push notifications being sent to the topics when the creative changes status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_creatives_stop_watching_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_stop_watching_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_creatives_stop_watching()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_stop_watching_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_creatives_stop_watching_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_creatives_stop_watching_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_stop_watching`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesStopWatchingArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
    /// Request body.
    pub body: StopWatchingCreativeRequest,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:stopWatching
/// Stops watching a creative. Will stop push notifications being sent to the topics when the creative changes status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_stop_watching_builder()` + `adexchangebuyer2_accounts_creatives_stop_watching_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_stop_watching_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_stop_watching(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesStopWatchingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_stop_watching_builder(
        client,
        args.accountId.clone(),
        args.creativeId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_creatives_stop_watching_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:watch
/// Watches a creative. Will result in push notifications being sent to the topic when the creative changes status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_watch_execute()` to send, or `adexchangebuyer2_accounts_creatives_watch` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_watch_builder(
    client: &SimpleHttpClient,
    accountId: String,
    creativeId: String,
    body: &WatchCreativeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}:watch",
        accountId.as_str(),
        creativeId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:watch
/// Watches a creative. Will result in push notifications being sent to the topic when the creative changes status.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_creatives_watch_execute()` or `adexchangebuyer2_accounts_creatives_watch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_watch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:watch
/// Watches a creative. Will result in push notifications being sent to the topic when the creative changes status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_creatives_watch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_watch_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_creatives_watch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_creatives_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_creatives_watch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_watch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesWatchArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
    /// Request body.
    pub body: WatchCreativeRequest,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}:watch
/// Watches a creative. Will result in push notifications being sent to the topic when the creative changes status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_watch_builder()` + `adexchangebuyer2_accounts_creatives_watch_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_watch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_watch(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesWatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_watch_builder(
        client,
        args.accountId.clone(),
        args.creativeId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_creatives_watch_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:add
/// Associate an existing deal with a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_deal_associations_add_execute()` to send, or `adexchangebuyer2_accounts_creatives_deal_associations_add` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_add_builder(
    client: &SimpleHttpClient,
    accountId: String,
    creativeId: String,
    body: &AddDealAssociationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}/dealAssociations:add",
        accountId.as_str(),
        creativeId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:add
/// Associate an existing deal with a creative.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_creatives_deal_associations_add_execute()` or `adexchangebuyer2_accounts_creatives_deal_associations_add`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_deal_associations_add_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_add_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:add
/// Associate an existing deal with a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_creatives_deal_associations_add_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_deal_associations_add_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_creatives_deal_associations_add()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_deal_associations_add_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_add_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_creatives_deal_associations_add_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_deal_associations_add`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesDealAssociationsAddArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
    /// Request body.
    pub body: AddDealAssociationRequest,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:add
/// Associate an existing deal with a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_deal_associations_add_builder()` + `adexchangebuyer2_accounts_creatives_deal_associations_add_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_deal_associations_add_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_add(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesDealAssociationsAddArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_deal_associations_add_builder(
        client,
        args.accountId.clone(),
        args.creativeId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_creatives_deal_associations_add_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations
/// List all creative-deal associations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_deal_associations_list_execute()` to send, or `adexchangebuyer2_accounts_creatives_deal_associations_list` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_list_builder(
    client: &SimpleHttpClient,
    accountId: String,
    creativeId: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    query: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}/dealAssociations",
        accountId.as_str(),
        creativeId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations
/// List all creative-deal associations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_creatives_deal_associations_list_execute()` or `adexchangebuyer2_accounts_creatives_deal_associations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_deal_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDealAssociationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDealAssociationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations
/// List all creative-deal associations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_creatives_deal_associations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_deal_associations_list_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_creatives_deal_associations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_deal_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDealAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_creatives_deal_associations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_deal_associations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesDealAssociationsListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations
/// List all creative-deal associations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_deal_associations_list_builder()` + `adexchangebuyer2_accounts_creatives_deal_associations_list_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_deal_associations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesDealAssociationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDealAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_deal_associations_list_builder(
        client,
        args.accountId.clone(),
        args.creativeId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.query.clone(),
    )?;
    adexchangebuyer2_accounts_creatives_deal_associations_list_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:remove
/// Remove the association between a deal and a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_creatives_deal_associations_remove_execute()` to send, or `adexchangebuyer2_accounts_creatives_deal_associations_remove` for simplest API.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_remove_builder(
    client: &SimpleHttpClient,
    accountId: String,
    creativeId: String,
    body: &RemoveDealAssociationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}/dealAssociations:remove",
        accountId.as_str(),
        creativeId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:remove
/// Remove the association between a deal and a creative.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_creatives_deal_associations_remove_execute()` or `adexchangebuyer2_accounts_creatives_deal_associations_remove`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_deal_associations_remove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_remove_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:remove
/// Remove the association between a deal and a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_creatives_deal_associations_remove_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_deal_associations_remove_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_creatives_deal_associations_remove()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_creatives_deal_associations_remove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_remove_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_creatives_deal_associations_remove_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_creatives_deal_associations_remove`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesDealAssociationsRemoveArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
    /// Request body.
    pub body: RemoveDealAssociationRequest,
}

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:remove
/// Remove the association between a deal and a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_creatives_deal_associations_remove_builder()` + `adexchangebuyer2_accounts_creatives_deal_associations_remove_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_creatives_deal_associations_remove_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_creatives_deal_associations_remove(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsCreativesDealAssociationsRemoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_creatives_deal_associations_remove_builder(
        client,
        args.accountId.clone(),
        args.creativeId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_creatives_deal_associations_remove_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals
/// List finalized proposals, regardless if a proposal is being renegotiated. A filter expression (PQL query) may be specified to filter the results. The notes will not be returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_finalized_proposals_list_execute()` to send, or `adexchangebuyer2_accounts_finalized_proposals_list` for simplest API.

pub fn adexchangebuyer2_accounts_finalized_proposals_list_builder(
    client: &SimpleHttpClient,
    accountId: String,
    filter: Option<String>,
    filterSyntax: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/finalizedProposals",
        accountId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = filterSyntax {
        query_parts.push(format!("filterSyntax={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals
/// List finalized proposals, regardless if a proposal is being renegotiated. A filter expression (PQL query) may be specified to filter the results. The notes will not be returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_finalized_proposals_list_execute()` or `adexchangebuyer2_accounts_finalized_proposals_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_finalized_proposals_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_finalized_proposals_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProposalsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProposalsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals
/// List finalized proposals, regardless if a proposal is being renegotiated. A filter expression (PQL query) may be specified to filter the results. The notes will not be returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_finalized_proposals_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_finalized_proposals_list_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_finalized_proposals_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_finalized_proposals_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_finalized_proposals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProposalsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_finalized_proposals_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_finalized_proposals_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsFinalizedProposalsListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: filterSyntax
    pub filterSyntax: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals
/// List finalized proposals, regardless if a proposal is being renegotiated. A filter expression (PQL query) may be specified to filter the results. The notes will not be returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_finalized_proposals_list_builder()` + `adexchangebuyer2_accounts_finalized_proposals_list_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_finalized_proposals_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_finalized_proposals_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsFinalizedProposalsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProposalsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_finalized_proposals_list_builder(
        client,
        args.accountId.clone(),
        args.filter.clone(),
        args.filterSyntax.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    adexchangebuyer2_accounts_finalized_proposals_list_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:pause
/// Update given deals to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.pause endpoint. It is a no-op to pause already-paused deals. It is an error to call PauseProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_finalized_proposals_pause_execute()` to send, or `adexchangebuyer2_accounts_finalized_proposals_pause` for simplest API.

pub fn adexchangebuyer2_accounts_finalized_proposals_pause_builder(
    client: &SimpleHttpClient,
    accountId: String,
    proposalId: String,
    body: &PauseProposalDealsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/finalizedProposals/{}:pause",
        accountId.as_str(),
        proposalId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:pause
/// Update given deals to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.pause endpoint. It is a no-op to pause already-paused deals. It is an error to call PauseProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_finalized_proposals_pause_execute()` or `adexchangebuyer2_accounts_finalized_proposals_pause`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_finalized_proposals_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_finalized_proposals_pause_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:pause
/// Update given deals to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.pause endpoint. It is a no-op to pause already-paused deals. It is an error to call PauseProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_finalized_proposals_pause_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_finalized_proposals_pause_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_finalized_proposals_pause()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_finalized_proposals_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_finalized_proposals_pause_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_finalized_proposals_pause_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_finalized_proposals_pause`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsFinalizedProposalsPauseArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: PauseProposalDealsRequest,
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:pause
/// Update given deals to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.pause endpoint. It is a no-op to pause already-paused deals. It is an error to call PauseProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_finalized_proposals_pause_builder()` + `adexchangebuyer2_accounts_finalized_proposals_pause_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_finalized_proposals_pause_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_finalized_proposals_pause(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsFinalizedProposalsPauseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_finalized_proposals_pause_builder(
        client,
        args.accountId.clone(),
        args.proposalId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_finalized_proposals_pause_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:resume
/// Update given deals to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.resume endpoint. It is a no-op to resume running deals or deals paused by the other party. It is an error to call ResumeProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_finalized_proposals_resume_execute()` to send, or `adexchangebuyer2_accounts_finalized_proposals_resume` for simplest API.

pub fn adexchangebuyer2_accounts_finalized_proposals_resume_builder(
    client: &SimpleHttpClient,
    accountId: String,
    proposalId: String,
    body: &ResumeProposalDealsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/finalizedProposals/{}:resume",
        accountId.as_str(),
        proposalId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:resume
/// Update given deals to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.resume endpoint. It is a no-op to resume running deals or deals paused by the other party. It is an error to call ResumeProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_finalized_proposals_resume_execute()` or `adexchangebuyer2_accounts_finalized_proposals_resume`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_finalized_proposals_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_finalized_proposals_resume_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:resume
/// Update given deals to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.resume endpoint. It is a no-op to resume running deals or deals paused by the other party. It is an error to call ResumeProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_finalized_proposals_resume_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_finalized_proposals_resume_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_finalized_proposals_resume()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_finalized_proposals_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_finalized_proposals_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_finalized_proposals_resume_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_finalized_proposals_resume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsFinalizedProposalsResumeArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: ResumeProposalDealsRequest,
}

/// GET v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:resume
/// Update given deals to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all listed deals in the request. Currently, this method only applies to PG and PD deals. For PA deals, call accounts.proposals.resume endpoint. It is a no-op to resume running deals or deals paused by the other party. It is an error to call ResumeProposalDeals for deals which are not part of the proposal of proposal_id or which are not finalized or renegotiating.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_finalized_proposals_resume_builder()` + `adexchangebuyer2_accounts_finalized_proposals_resume_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_finalized_proposals_resume_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_finalized_proposals_resume(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsFinalizedProposalsResumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_finalized_proposals_resume_builder(
        client,
        args.accountId.clone(),
        args.proposalId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_finalized_proposals_resume_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/products/{productId}
/// Gets the requested product by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_products_get_execute()` to send, or `adexchangebuyer2_accounts_products_get` for simplest API.

pub fn adexchangebuyer2_accounts_products_get_builder(
    client: &SimpleHttpClient,
    accountId: String,
    productId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/products/{}",
        accountId.as_str(),
        productId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/products/{productId}
/// Gets the requested product by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_products_get_execute()` or `adexchangebuyer2_accounts_products_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_products_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_products_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Product>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Product = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/products/{productId}
/// Gets the requested product by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_products_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_products_get_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_products_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_products_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_products_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_products_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_products_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProductsGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: productId
    pub productId: String,
}

/// GET v2beta1/accounts/{accountId}/products/{productId}
/// Gets the requested product by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_products_get_builder()` + `adexchangebuyer2_accounts_products_get_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_products_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_products_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProductsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_products_get_builder(
        client,
        args.accountId.clone(),
        args.productId.clone(),
    )?;
    adexchangebuyer2_accounts_products_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/products
/// List all products visible to the buyer (optionally filtered by the specified PQL query).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_products_list_execute()` to send, or `adexchangebuyer2_accounts_products_list` for simplest API.

pub fn adexchangebuyer2_accounts_products_list_builder(
    client: &SimpleHttpClient,
    accountId: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/products",
        accountId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/products
/// List all products visible to the buyer (optionally filtered by the specified PQL query).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_products_list_execute()` or `adexchangebuyer2_accounts_products_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_products_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_products_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProductsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProductsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/products
/// List all products visible to the buyer (optionally filtered by the specified PQL query).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_products_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_products_list_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_products_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_products_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_products_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProductsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_products_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_products_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProductsListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/products
/// List all products visible to the buyer (optionally filtered by the specified PQL query).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_products_list_builder()` + `adexchangebuyer2_accounts_products_list_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_products_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_products_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProductsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProductsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_products_list_builder(
        client,
        args.accountId.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    adexchangebuyer2_accounts_products_list_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:accept
/// Mark the proposal as accepted at the given revision number. If the number does not match the server's revision number an ABORTED error message will be returned. This call updates the proposal_state from PROPOSED to BUYER_ACCEPTED, or from SELLER_ACCEPTED to FINALIZED. Upon calling this endpoint, the buyer implicitly agrees to the terms and conditions optionally set within the proposal by the publisher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_accept_execute()` to send, or `adexchangebuyer2_accounts_proposals_accept` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_accept_builder(
    client: &SimpleHttpClient,
    accountId: String,
    proposalId: String,
    body: &AcceptProposalRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:accept",
        accountId.as_str(),
        proposalId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:accept
/// Mark the proposal as accepted at the given revision number. If the number does not match the server's revision number an ABORTED error message will be returned. This call updates the proposal_state from PROPOSED to BUYER_ACCEPTED, or from SELLER_ACCEPTED to FINALIZED. Upon calling this endpoint, the buyer implicitly agrees to the terms and conditions optionally set within the proposal by the publisher.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_proposals_accept_execute()` or `adexchangebuyer2_accounts_proposals_accept`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_accept_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_accept_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:accept
/// Mark the proposal as accepted at the given revision number. If the number does not match the server's revision number an ABORTED error message will be returned. This call updates the proposal_state from PROPOSED to BUYER_ACCEPTED, or from SELLER_ACCEPTED to FINALIZED. Upon calling this endpoint, the buyer implicitly agrees to the terms and conditions optionally set within the proposal by the publisher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_proposals_accept_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_accept_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_proposals_accept()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_accept_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_proposals_accept_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_proposals_accept_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_accept`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsAcceptArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: AcceptProposalRequest,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:accept
/// Mark the proposal as accepted at the given revision number. If the number does not match the server's revision number an ABORTED error message will be returned. This call updates the proposal_state from PROPOSED to BUYER_ACCEPTED, or from SELLER_ACCEPTED to FINALIZED. Upon calling this endpoint, the buyer implicitly agrees to the terms and conditions optionally set within the proposal by the publisher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_accept_builder()` + `adexchangebuyer2_accounts_proposals_accept_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_accept_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_accept(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsAcceptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_accept_builder(
        client,
        args.accountId.clone(),
        args.proposalId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_accept_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:addNote
/// Create a new note and attach it to the proposal. The note is assigned a unique ID by the server. The proposal revision number will not increase when associated with a new note.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_add_note_execute()` to send, or `adexchangebuyer2_accounts_proposals_add_note` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_add_note_builder(
    client: &SimpleHttpClient,
    accountId: String,
    proposalId: String,
    body: &AddNoteRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:addNote",
        accountId.as_str(),
        proposalId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:addNote
/// Create a new note and attach it to the proposal. The note is assigned a unique ID by the server. The proposal revision number will not increase when associated with a new note.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_proposals_add_note_execute()` or `adexchangebuyer2_accounts_proposals_add_note`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_add_note_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_add_note_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Note>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Note = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:addNote
/// Create a new note and attach it to the proposal. The note is assigned a unique ID by the server. The proposal revision number will not increase when associated with a new note.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_proposals_add_note_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_add_note_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_proposals_add_note()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_add_note_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_proposals_add_note_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_proposals_add_note_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_add_note`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsAddNoteArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: AddNoteRequest,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:addNote
/// Create a new note and attach it to the proposal. The note is assigned a unique ID by the server. The proposal revision number will not increase when associated with a new note.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_add_note_builder()` + `adexchangebuyer2_accounts_proposals_add_note_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_add_note_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_add_note(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsAddNoteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_add_note_builder(
        client,
        args.accountId.clone(),
        args.proposalId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_add_note_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:cancelNegotiation
/// Cancel an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized, but only cancels a negotiation unilaterally.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_cancel_negotiation_execute()` to send, or `adexchangebuyer2_accounts_proposals_cancel_negotiation` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_cancel_negotiation_builder(
    client: &SimpleHttpClient,
    accountId: String,
    proposalId: String,
    body: &CancelNegotiationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:cancelNegotiation",
        accountId.as_str(),
        proposalId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:cancelNegotiation
/// Cancel an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized, but only cancels a negotiation unilaterally.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_proposals_cancel_negotiation_execute()` or `adexchangebuyer2_accounts_proposals_cancel_negotiation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_cancel_negotiation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_cancel_negotiation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:cancelNegotiation
/// Cancel an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized, but only cancels a negotiation unilaterally.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_proposals_cancel_negotiation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_cancel_negotiation_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_proposals_cancel_negotiation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_cancel_negotiation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_proposals_cancel_negotiation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_proposals_cancel_negotiation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_cancel_negotiation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsCancelNegotiationArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: CancelNegotiationRequest,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:cancelNegotiation
/// Cancel an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized, but only cancels a negotiation unilaterally.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_cancel_negotiation_builder()` + `adexchangebuyer2_accounts_proposals_cancel_negotiation_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_cancel_negotiation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_cancel_negotiation(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsCancelNegotiationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_cancel_negotiation_builder(
        client,
        args.accountId.clone(),
        args.proposalId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_cancel_negotiation_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:completeSetup
/// You can opt-in to manually update proposals to indicate that setup is complete. By default, proposal setup is automatically completed after their deals are finalized. Contact your Technical Account Manager to opt in. Buyers can call this method when the proposal has been finalized, and all the required creatives have been uploaded using the Creatives API. This call updates the is_setup_completed field on the deals in the proposal, and notifies the seller. The server then advances the revision number of the most recent proposal. To mark an individual deal as ready to serve, call buyers.`finalizedDeals`.`setReadyToServe` in the Marketplace API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_complete_setup_execute()` to send, or `adexchangebuyer2_accounts_proposals_complete_setup` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_complete_setup_builder(
    client: &SimpleHttpClient,
    accountId: String,
    proposalId: String,
    body: &CompleteSetupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:completeSetup",
        accountId.as_str(),
        proposalId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:completeSetup
/// You can opt-in to manually update proposals to indicate that setup is complete. By default, proposal setup is automatically completed after their deals are finalized. Contact your Technical Account Manager to opt in. Buyers can call this method when the proposal has been finalized, and all the required creatives have been uploaded using the Creatives API. This call updates the is_setup_completed field on the deals in the proposal, and notifies the seller. The server then advances the revision number of the most recent proposal. To mark an individual deal as ready to serve, call buyers.`finalizedDeals`.`setReadyToServe` in the Marketplace API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_proposals_complete_setup_execute()` or `adexchangebuyer2_accounts_proposals_complete_setup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_complete_setup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_complete_setup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:completeSetup
/// You can opt-in to manually update proposals to indicate that setup is complete. By default, proposal setup is automatically completed after their deals are finalized. Contact your Technical Account Manager to opt in. Buyers can call this method when the proposal has been finalized, and all the required creatives have been uploaded using the Creatives API. This call updates the is_setup_completed field on the deals in the proposal, and notifies the seller. The server then advances the revision number of the most recent proposal. To mark an individual deal as ready to serve, call buyers.`finalizedDeals`.`setReadyToServe` in the Marketplace API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_proposals_complete_setup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_complete_setup_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_proposals_complete_setup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_complete_setup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_proposals_complete_setup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_proposals_complete_setup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_complete_setup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsCompleteSetupArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: CompleteSetupRequest,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:completeSetup
/// You can opt-in to manually update proposals to indicate that setup is complete. By default, proposal setup is automatically completed after their deals are finalized. Contact your Technical Account Manager to opt in. Buyers can call this method when the proposal has been finalized, and all the required creatives have been uploaded using the Creatives API. This call updates the is_setup_completed field on the deals in the proposal, and notifies the seller. The server then advances the revision number of the most recent proposal. To mark an individual deal as ready to serve, call buyers.`finalizedDeals`.`setReadyToServe` in the Marketplace API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_complete_setup_builder()` + `adexchangebuyer2_accounts_proposals_complete_setup_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_complete_setup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_complete_setup(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsCompleteSetupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_complete_setup_builder(
        client,
        args.accountId.clone(),
        args.proposalId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_complete_setup_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals
/// Create the given proposal. Each created proposal and any deals it contains are assigned a unique ID by the server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_create_execute()` to send, or `adexchangebuyer2_accounts_proposals_create` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_create_builder(
    client: &SimpleHttpClient,
    accountId: String,
    body: &Proposal,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals",
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals
/// Create the given proposal. Each created proposal and any deals it contains are assigned a unique ID by the server.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_proposals_create_execute()` or `adexchangebuyer2_accounts_proposals_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/proposals
/// Create the given proposal. Each created proposal and any deals it contains are assigned a unique ID by the server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_proposals_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_create_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_proposals_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_proposals_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_proposals_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsCreateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Request body.
    pub body: Proposal,
}

/// GET v2beta1/accounts/{accountId}/proposals
/// Create the given proposal. Each created proposal and any deals it contains are assigned a unique ID by the server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_create_builder()` + `adexchangebuyer2_accounts_proposals_create_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_create_builder(
        client,
        args.accountId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_create_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}
/// Gets a proposal given its ID. The proposal is returned at its head revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_get_execute()` to send, or `adexchangebuyer2_accounts_proposals_get` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_get_builder(
    client: &SimpleHttpClient,
    accountId: String,
    proposalId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}",
        accountId.as_str(),
        proposalId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}
/// Gets a proposal given its ID. The proposal is returned at its head revision.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_proposals_get_execute()` or `adexchangebuyer2_accounts_proposals_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}
/// Gets a proposal given its ID. The proposal is returned at its head revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_proposals_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_get_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_proposals_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_proposals_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_proposals_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}
/// Gets a proposal given its ID. The proposal is returned at its head revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_get_builder()` + `adexchangebuyer2_accounts_proposals_get_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_get_builder(
        client,
        args.accountId.clone(),
        args.proposalId.clone(),
    )?;
    adexchangebuyer2_accounts_proposals_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:pause
/// Update the given proposal to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all deals in the proposal. It is a no-op to pause an already-paused proposal. It is an error to call PauseProposal for a proposal that is not finalized or renegotiating.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_pause_execute()` to send, or `adexchangebuyer2_accounts_proposals_pause` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_pause_builder(
    client: &SimpleHttpClient,
    accountId: String,
    proposalId: String,
    body: &PauseProposalRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:pause",
        accountId.as_str(),
        proposalId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:pause
/// Update the given proposal to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all deals in the proposal. It is a no-op to pause an already-paused proposal. It is an error to call PauseProposal for a proposal that is not finalized or renegotiating.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_proposals_pause_execute()` or `adexchangebuyer2_accounts_proposals_pause`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_pause_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:pause
/// Update the given proposal to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all deals in the proposal. It is a no-op to pause an already-paused proposal. It is an error to call PauseProposal for a proposal that is not finalized or renegotiating.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_proposals_pause_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_pause_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_proposals_pause()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_proposals_pause_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_proposals_pause_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_pause`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsPauseArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: PauseProposalRequest,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:pause
/// Update the given proposal to pause serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `true` for all deals in the proposal. It is a no-op to pause an already-paused proposal. It is an error to call PauseProposal for a proposal that is not finalized or renegotiating.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_pause_builder()` + `adexchangebuyer2_accounts_proposals_pause_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_pause_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_pause(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsPauseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_pause_builder(
        client,
        args.accountId.clone(),
        args.proposalId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_pause_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:resume
/// Update the given proposal to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all deals in the proposal. Note that if the has_seller_paused bit is also set, serving will not resume until the seller also resumes. It is a no-op to resume an already-running proposal. It is an error to call ResumeProposal for a proposal that is not finalized or renegotiating.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_proposals_resume_execute()` to send, or `adexchangebuyer2_accounts_proposals_resume` for simplest API.

pub fn adexchangebuyer2_accounts_proposals_resume_builder(
    client: &SimpleHttpClient,
    accountId: String,
    proposalId: String,
    body: &ResumeProposalRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:resume",
        accountId.as_str(),
        proposalId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:resume
/// Update the given proposal to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all deals in the proposal. Note that if the has_seller_paused bit is also set, serving will not resume until the seller also resumes. It is a no-op to resume an already-running proposal. It is an error to call ResumeProposal for a proposal that is not finalized or renegotiating.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_proposals_resume_execute()` or `adexchangebuyer2_accounts_proposals_resume`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_resume_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:resume
/// Update the given proposal to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all deals in the proposal. Note that if the has_seller_paused bit is also set, serving will not resume until the seller also resumes. It is a no-op to resume an already-running proposal. It is an error to call ResumeProposal for a proposal that is not finalized or renegotiating.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_proposals_resume_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_resume_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_proposals_resume()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_proposals_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_proposals_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_proposals_resume_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_proposals_resume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsResumeArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: proposalId
    pub proposalId: String,
    /// Request body.
    pub body: ResumeProposalRequest,
}

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}:resume
/// Update the given proposal to resume serving. This method will set the DealServingMetadata.DealPauseStatus.has_buyer_paused bit to `false` for all deals in the proposal. Note that if the has_seller_paused bit is also set, serving will not resume until the seller also resumes. It is a no-op to resume an already-running proposal. It is an error to call ResumeProposal for a proposal that is not finalized or renegotiating.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_proposals_resume_builder()` + `adexchangebuyer2_accounts_proposals_resume_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_proposals_resume_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_proposals_resume(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsProposalsResumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_proposals_resume_builder(
        client,
        args.accountId.clone(),
        args.proposalId.clone(),
        &args.body,
    )?;
    adexchangebuyer2_accounts_proposals_resume_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles/{publisherProfileId}
/// Gets the requested publisher profile by id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_publisher_profiles_get_execute()` to send, or `adexchangebuyer2_accounts_publisher_profiles_get` for simplest API.

pub fn adexchangebuyer2_accounts_publisher_profiles_get_builder(
    client: &SimpleHttpClient,
    accountId: String,
    publisherProfileId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/publisherProfiles/{}",
        accountId.as_str(),
        publisherProfileId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles/{publisherProfileId}
/// Gets the requested publisher profile by id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_publisher_profiles_get_execute()` or `adexchangebuyer2_accounts_publisher_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_publisher_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_publisher_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublisherProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublisherProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles/{publisherProfileId}
/// Gets the requested publisher profile by id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_publisher_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_publisher_profiles_get_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_publisher_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_publisher_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_publisher_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublisherProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_publisher_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_publisher_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsPublisherProfilesGetArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: publisherProfileId
    pub publisherProfileId: String,
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles/{publisherProfileId}
/// Gets the requested publisher profile by id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_publisher_profiles_get_builder()` + `adexchangebuyer2_accounts_publisher_profiles_get_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_publisher_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_publisher_profiles_get(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsPublisherProfilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublisherProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_publisher_profiles_get_builder(
        client,
        args.accountId.clone(),
        args.publisherProfileId.clone(),
    )?;
    adexchangebuyer2_accounts_publisher_profiles_get_execute(builder)
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles
/// List all publisher profiles visible to the buyer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_accounts_publisher_profiles_list_execute()` to send, or `adexchangebuyer2_accounts_publisher_profiles_list` for simplest API.

pub fn adexchangebuyer2_accounts_publisher_profiles_list_builder(
    client: &SimpleHttpClient,
    accountId: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/publisherProfiles",
        accountId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles
/// List all publisher profiles visible to the buyer
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_accounts_publisher_profiles_list_execute()` or `adexchangebuyer2_accounts_publisher_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_publisher_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_publisher_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPublisherProfilesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPublisherProfilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles
/// List all publisher profiles visible to the buyer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_accounts_publisher_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_accounts_publisher_profiles_list_task()`.
/// For the simplest API, use `adexchangebuyer2_accounts_publisher_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_accounts_publisher_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_accounts_publisher_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPublisherProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_accounts_publisher_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_accounts_publisher_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsPublisherProfilesListArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2beta1/accounts/{accountId}/publisherProfiles
/// List all publisher profiles visible to the buyer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_accounts_publisher_profiles_list_builder()` + `adexchangebuyer2_accounts_publisher_profiles_list_execute()`.
/// For task-level control, use `adexchangebuyer2_accounts_publisher_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_accounts_publisher_profiles_list(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2AccountsPublisherProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPublisherProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_accounts_publisher_profiles_list_builder(
        client,
        args.accountId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    adexchangebuyer2_accounts_publisher_profiles_list_execute(builder)
}

/// GET v2beta1/bidders/{biddersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_bidders_filter_sets_create_execute()` to send, or `adexchangebuyer2_bidders_filter_sets_create` for simplest API.

pub fn adexchangebuyer2_bidders_filter_sets_create_builder(
    client: &SimpleHttpClient,
    ownerName: String,
    isTransient: Option<bool>,
    body: &FilterSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://adexchangebuyer.googleapis.com/v2beta1/bidders/{}/filterSets",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = isTransient {
        query_parts.push(format!("isTransient={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/bidders/{biddersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_bidders_filter_sets_create_execute()` or `adexchangebuyer2_bidders_filter_sets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_bidders_filter_sets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FilterSet>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FilterSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/bidders/{biddersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_bidders_filter_sets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_bidders_filter_sets_create_task()`.
/// For the simplest API, use `adexchangebuyer2_bidders_filter_sets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_bidders_filter_sets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_bidders_filter_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_bidders_filter_sets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_bidders_filter_sets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsCreateArgs {
    /// Path parameter: ownerName
    pub ownerName: String,
    /// Query parameter: isTransient
    pub isTransient: Option<bool>,
    /// Request body.
    pub body: FilterSet,
}

/// GET v2beta1/bidders/{biddersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_bidders_filter_sets_create_builder()` + `adexchangebuyer2_bidders_filter_sets_create_execute()`.
/// For task-level control, use `adexchangebuyer2_bidders_filter_sets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_bidders_filter_sets_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BiddersFilterSetsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_bidders_filter_sets_create_builder(
        client,
        args.ownerName.clone(),
        args.isTransient.clone(),
        &args.body,
    )?;
    adexchangebuyer2_bidders_filter_sets_create_execute(builder)
}

/// GET v2beta1/buyers/{buyersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexchangebuyer2_buyers_filter_sets_create_execute()` to send, or `adexchangebuyer2_buyers_filter_sets_create` for simplest API.

pub fn adexchangebuyer2_buyers_filter_sets_create_builder(
    client: &SimpleHttpClient,
    ownerName: String,
    isTransient: Option<bool>,
    body: &FilterSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://adexchangebuyer.googleapis.com/v2beta1/buyers/{}/filterSets",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = isTransient {
        query_parts.push(format!("isTransient={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2beta1/buyers/{buyersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adexchangebuyer2_buyers_filter_sets_create_execute()` or `adexchangebuyer2_buyers_filter_sets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_buyers_filter_sets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FilterSet>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FilterSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2beta1/buyers/{buyersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adexchangebuyer2_buyers_filter_sets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adexchangebuyer2_buyers_filter_sets_create_task()`.
/// For the simplest API, use `adexchangebuyer2_buyers_filter_sets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adexchangebuyer2_buyers_filter_sets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adexchangebuyer2_buyers_filter_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adexchangebuyer2_buyers_filter_sets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adexchangebuyer2_buyers_filter_sets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsCreateArgs {
    /// Path parameter: ownerName
    pub ownerName: String,
    /// Query parameter: isTransient
    pub isTransient: Option<bool>,
    /// Request body.
    pub body: FilterSet,
}

/// GET v2beta1/buyers/{buyersId}/filterSets
/// Creates the specified filter set for the account with the given account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexchangebuyer2_buyers_filter_sets_create_builder()` + `adexchangebuyer2_buyers_filter_sets_create_execute()`.
/// For task-level control, use `adexchangebuyer2_buyers_filter_sets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexchangebuyer2_buyers_filter_sets_create(
    client: &SimpleHttpClient,
    args: &Adexchangebuyer2BuyersFilterSetsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FilterSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adexchangebuyer2_buyers_filter_sets_create_builder(
        client,
        args.ownerName.clone(),
        args.isTransient.clone(),
        &args.body,
    )?;
    adexchangebuyer2_buyers_filter_sets_create_execute(builder)
}
