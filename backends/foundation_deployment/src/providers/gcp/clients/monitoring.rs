//! Auto-generated API clients for gcp/monitoring.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v3/folders/{foldersId}/timeSeries
/// Lists time series that match a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_folders_time_series_list_execute()` to send, or `monitoring_folders_time_series_list` for simplest API.

pub fn monitoring_folders_time_series_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    aggregation_alignmentPeriod: &Option<Option<String>>,
    aggregation_crossSeriesReducer: &Option<Option<String>>,
    aggregation_groupByFields: &Option<Option<String>>,
    aggregation_perSeriesAligner: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    interval_endTime: &Option<Option<String>>,
    interval_startTime: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    secondaryAggregation_alignmentPeriod: &Option<Option<String>>,
    secondaryAggregation_crossSeriesReducer: &Option<Option<String>>,
    secondaryAggregation_groupByFields: &Option<Option<String>>,
    secondaryAggregation_perSeriesAligner: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/folders/{}/timeSeries",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = aggregation_alignmentPeriod.as_ref() {
        query_parts.push(format!("aggregation.alignmentPeriod={}", val));
    }
    if let Some(val) = aggregation_crossSeriesReducer.as_ref() {
        query_parts.push(format!("aggregation.crossSeriesReducer={}", val));
    }
    if let Some(val) = aggregation_groupByFields.as_ref() {
        query_parts.push(format!("aggregation.groupByFields={}", val));
    }
    if let Some(val) = aggregation_perSeriesAligner.as_ref() {
        query_parts.push(format!("aggregation.perSeriesAligner={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = interval_endTime.as_ref() {
        query_parts.push(format!("interval.endTime={}", val));
    }
    if let Some(val) = interval_startTime.as_ref() {
        query_parts.push(format!("interval.startTime={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = secondaryAggregation_alignmentPeriod.as_ref() {
        query_parts.push(format!("secondaryAggregation.alignmentPeriod={}", val));
    }
    if let Some(val) = secondaryAggregation_crossSeriesReducer.as_ref() {
        query_parts.push(format!("secondaryAggregation.crossSeriesReducer={}", val));
    }
    if let Some(val) = secondaryAggregation_groupByFields.as_ref() {
        query_parts.push(format!("secondaryAggregation.groupByFields={}", val));
    }
    if let Some(val) = secondaryAggregation_perSeriesAligner.as_ref() {
        query_parts.push(format!("secondaryAggregation.perSeriesAligner={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/folders/{foldersId}/timeSeries
/// Lists time series that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_folders_time_series_list_execute()` or `monitoring_folders_time_series_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_folders_time_series_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_folders_time_series_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/folders/{foldersId}/timeSeries
/// Lists time series that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_folders_time_series_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_folders_time_series_list_task()`.
/// For the simplest API, use `monitoring_folders_time_series_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_folders_time_series_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_folders_time_series_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_folders_time_series_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_folders_time_series_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringFoldersTimeSeriesListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: aggregation_alignmentPeriod
    pub aggregation_alignmentPeriod: Option<Option<String>>,
    /// Query parameter: aggregation_crossSeriesReducer
    pub aggregation_crossSeriesReducer: Option<Option<String>>,
    /// Query parameter: aggregation_groupByFields
    pub aggregation_groupByFields: Option<Option<String>>,
    /// Query parameter: aggregation_perSeriesAligner
    pub aggregation_perSeriesAligner: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: interval_endTime
    pub interval_endTime: Option<Option<String>>,
    /// Query parameter: interval_startTime
    pub interval_startTime: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: secondaryAggregation_alignmentPeriod
    pub secondaryAggregation_alignmentPeriod: Option<Option<String>>,
    /// Query parameter: secondaryAggregation_crossSeriesReducer
    pub secondaryAggregation_crossSeriesReducer: Option<Option<String>>,
    /// Query parameter: secondaryAggregation_groupByFields
    pub secondaryAggregation_groupByFields: Option<Option<String>>,
    /// Query parameter: secondaryAggregation_perSeriesAligner
    pub secondaryAggregation_perSeriesAligner: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/folders/{foldersId}/timeSeries
/// Lists time series that match a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_folders_time_series_list_builder()` + `monitoring_folders_time_series_list_execute()`.
/// For task-level control, use `monitoring_folders_time_series_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_folders_time_series_list(
    client: &SimpleHttpClient,
    args: &MonitoringFoldersTimeSeriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_folders_time_series_list_builder(
        client,
        &args.name,
        &args.aggregation_alignmentPeriod,
        &args.aggregation_crossSeriesReducer,
        &args.aggregation_groupByFields,
        &args.aggregation_perSeriesAligner,
        &args.filter,
        &args.interval_endTime,
        &args.interval_startTime,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.secondaryAggregation_alignmentPeriod,
        &args.secondaryAggregation_crossSeriesReducer,
        &args.secondaryAggregation_groupByFields,
        &args.secondaryAggregation_perSeriesAligner,
        &args.view,
    )?;
    monitoring_folders_time_series_list_execute(builder)
}

/// GET v3/organizations/{organizationsId}/timeSeries
/// Lists time series that match a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_organizations_time_series_list_execute()` to send, or `monitoring_organizations_time_series_list` for simplest API.

pub fn monitoring_organizations_time_series_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    aggregation_alignmentPeriod: &Option<Option<String>>,
    aggregation_crossSeriesReducer: &Option<Option<String>>,
    aggregation_groupByFields: &Option<Option<String>>,
    aggregation_perSeriesAligner: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    interval_endTime: &Option<Option<String>>,
    interval_startTime: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    secondaryAggregation_alignmentPeriod: &Option<Option<String>>,
    secondaryAggregation_crossSeriesReducer: &Option<Option<String>>,
    secondaryAggregation_groupByFields: &Option<Option<String>>,
    secondaryAggregation_perSeriesAligner: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/organizations/{}/timeSeries",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = aggregation_alignmentPeriod.as_ref() {
        query_parts.push(format!("aggregation.alignmentPeriod={}", val));
    }
    if let Some(val) = aggregation_crossSeriesReducer.as_ref() {
        query_parts.push(format!("aggregation.crossSeriesReducer={}", val));
    }
    if let Some(val) = aggregation_groupByFields.as_ref() {
        query_parts.push(format!("aggregation.groupByFields={}", val));
    }
    if let Some(val) = aggregation_perSeriesAligner.as_ref() {
        query_parts.push(format!("aggregation.perSeriesAligner={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = interval_endTime.as_ref() {
        query_parts.push(format!("interval.endTime={}", val));
    }
    if let Some(val) = interval_startTime.as_ref() {
        query_parts.push(format!("interval.startTime={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = secondaryAggregation_alignmentPeriod.as_ref() {
        query_parts.push(format!("secondaryAggregation.alignmentPeriod={}", val));
    }
    if let Some(val) = secondaryAggregation_crossSeriesReducer.as_ref() {
        query_parts.push(format!("secondaryAggregation.crossSeriesReducer={}", val));
    }
    if let Some(val) = secondaryAggregation_groupByFields.as_ref() {
        query_parts.push(format!("secondaryAggregation.groupByFields={}", val));
    }
    if let Some(val) = secondaryAggregation_perSeriesAligner.as_ref() {
        query_parts.push(format!("secondaryAggregation.perSeriesAligner={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/organizations/{organizationsId}/timeSeries
/// Lists time series that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_organizations_time_series_list_execute()` or `monitoring_organizations_time_series_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_organizations_time_series_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_organizations_time_series_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/organizations/{organizationsId}/timeSeries
/// Lists time series that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_organizations_time_series_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_organizations_time_series_list_task()`.
/// For the simplest API, use `monitoring_organizations_time_series_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_organizations_time_series_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_organizations_time_series_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_organizations_time_series_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_organizations_time_series_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringOrganizationsTimeSeriesListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: aggregation_alignmentPeriod
    pub aggregation_alignmentPeriod: Option<Option<String>>,
    /// Query parameter: aggregation_crossSeriesReducer
    pub aggregation_crossSeriesReducer: Option<Option<String>>,
    /// Query parameter: aggregation_groupByFields
    pub aggregation_groupByFields: Option<Option<String>>,
    /// Query parameter: aggregation_perSeriesAligner
    pub aggregation_perSeriesAligner: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: interval_endTime
    pub interval_endTime: Option<Option<String>>,
    /// Query parameter: interval_startTime
    pub interval_startTime: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: secondaryAggregation_alignmentPeriod
    pub secondaryAggregation_alignmentPeriod: Option<Option<String>>,
    /// Query parameter: secondaryAggregation_crossSeriesReducer
    pub secondaryAggregation_crossSeriesReducer: Option<Option<String>>,
    /// Query parameter: secondaryAggregation_groupByFields
    pub secondaryAggregation_groupByFields: Option<Option<String>>,
    /// Query parameter: secondaryAggregation_perSeriesAligner
    pub secondaryAggregation_perSeriesAligner: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/organizations/{organizationsId}/timeSeries
/// Lists time series that match a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_organizations_time_series_list_builder()` + `monitoring_organizations_time_series_list_execute()`.
/// For task-level control, use `monitoring_organizations_time_series_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_organizations_time_series_list(
    client: &SimpleHttpClient,
    args: &MonitoringOrganizationsTimeSeriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_organizations_time_series_list_builder(
        client,
        &args.name,
        &args.aggregation_alignmentPeriod,
        &args.aggregation_crossSeriesReducer,
        &args.aggregation_groupByFields,
        &args.aggregation_perSeriesAligner,
        &args.filter,
        &args.interval_endTime,
        &args.interval_startTime,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.secondaryAggregation_alignmentPeriod,
        &args.secondaryAggregation_crossSeriesReducer,
        &args.secondaryAggregation_groupByFields,
        &args.secondaryAggregation_perSeriesAligner,
        &args.view,
    )?;
    monitoring_organizations_time_series_list_execute(builder)
}

/// POST v3/projects/{projectsId}/alertPolicies
/// Creates a new alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alert_policies_create_execute()` to send, or `monitoring_projects_alert_policies_create` for simplest API.

pub fn monitoring_projects_alert_policies_create_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/alertPolicies",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/alertPolicies
/// Creates a new alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_alert_policies_create_execute()` or `monitoring_projects_alert_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alert_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AlertPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AlertPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/alertPolicies
/// Creates a new alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_alert_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_alert_policies_create_task()`.
/// For the simplest API, use `monitoring_projects_alert_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alert_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_alert_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_alert_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_alert_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsAlertPoliciesCreateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/alertPolicies
/// Creates a new alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alert_policies_create_builder()` + `monitoring_projects_alert_policies_create_execute()`.
/// For task-level control, use `monitoring_projects_alert_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_create(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsAlertPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alert_policies_create_builder(client, &args.name)?;
    monitoring_projects_alert_policies_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/alertPolicies/{alertPoliciesId}
/// Deletes an alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alert_policies_delete_execute()` to send, or `monitoring_projects_alert_policies_delete` for simplest API.

pub fn monitoring_projects_alert_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/alertPolicies/{alertPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/alertPolicies/{alertPoliciesId}
/// Deletes an alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_alert_policies_delete_execute()` or `monitoring_projects_alert_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alert_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/alertPolicies/{alertPoliciesId}
/// Deletes an alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_alert_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_alert_policies_delete_task()`.
/// For the simplest API, use `monitoring_projects_alert_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alert_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_alert_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_alert_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_alert_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsAlertPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/alertPolicies/{alertPoliciesId}
/// Deletes an alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alert_policies_delete_builder()` + `monitoring_projects_alert_policies_delete_execute()`.
/// For task-level control, use `monitoring_projects_alert_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_delete(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsAlertPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alert_policies_delete_builder(client, &args.name)?;
    monitoring_projects_alert_policies_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/alertPolicies/{alertPoliciesId}
/// Gets a single alerting policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alert_policies_get_execute()` to send, or `monitoring_projects_alert_policies_get` for simplest API.

pub fn monitoring_projects_alert_policies_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/alertPolicies/{alertPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/alertPolicies/{alertPoliciesId}
/// Gets a single alerting policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_alert_policies_get_execute()` or `monitoring_projects_alert_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alert_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AlertPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AlertPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/alertPolicies/{alertPoliciesId}
/// Gets a single alerting policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_alert_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_alert_policies_get_task()`.
/// For the simplest API, use `monitoring_projects_alert_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alert_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_alert_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_alert_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_alert_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsAlertPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/alertPolicies/{alertPoliciesId}
/// Gets a single alerting policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alert_policies_get_builder()` + `monitoring_projects_alert_policies_get_execute()`.
/// For task-level control, use `monitoring_projects_alert_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_get(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsAlertPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alert_policies_get_builder(client, &args.name)?;
    monitoring_projects_alert_policies_get_execute(builder)
}

/// GET v3/projects/{projectsId}/alertPolicies
/// Lists the existing alerting policies for the workspace.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alert_policies_list_execute()` to send, or `monitoring_projects_alert_policies_list` for simplest API.

pub fn monitoring_projects_alert_policies_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/alertPolicies",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/alertPolicies
/// Lists the existing alerting policies for the workspace.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_alert_policies_list_execute()` or `monitoring_projects_alert_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alert_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAlertPoliciesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAlertPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/alertPolicies
/// Lists the existing alerting policies for the workspace.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_alert_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_alert_policies_list_task()`.
/// For the simplest API, use `monitoring_projects_alert_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alert_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_alert_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_alert_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_alert_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsAlertPoliciesListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v3/projects/{projectsId}/alertPolicies
/// Lists the existing alerting policies for the workspace.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alert_policies_list_builder()` + `monitoring_projects_alert_policies_list_execute()`.
/// For task-level control, use `monitoring_projects_alert_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_list(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsAlertPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alert_policies_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    monitoring_projects_alert_policies_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/alertPolicies/{alertPoliciesId}
/// Updates an alerting policy. You can either replace the entire policy with a new one or replace only certain fields in the current alerting policy by specifying the fields to be updated via `updateMask`. Returns the updated alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alert_policies_patch_execute()` to send, or `monitoring_projects_alert_policies_patch` for simplest API.

pub fn monitoring_projects_alert_policies_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/alertPolicies/{alertPoliciesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/alertPolicies/{alertPoliciesId}
/// Updates an alerting policy. You can either replace the entire policy with a new one or replace only certain fields in the current alerting policy by specifying the fields to be updated via `updateMask`. Returns the updated alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_alert_policies_patch_execute()` or `monitoring_projects_alert_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alert_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AlertPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AlertPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/alertPolicies/{alertPoliciesId}
/// Updates an alerting policy. You can either replace the entire policy with a new one or replace only certain fields in the current alerting policy by specifying the fields to be updated via `updateMask`. Returns the updated alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_alert_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_alert_policies_patch_task()`.
/// For the simplest API, use `monitoring_projects_alert_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alert_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_alert_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_alert_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_alert_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsAlertPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v3/projects/{projectsId}/alertPolicies/{alertPoliciesId}
/// Updates an alerting policy. You can either replace the entire policy with a new one or replace only certain fields in the current alerting policy by specifying the fields to be updated via `updateMask`. Returns the updated alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alert_policies_patch_builder()` + `monitoring_projects_alert_policies_patch_execute()`.
/// For task-level control, use `monitoring_projects_alert_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_patch(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsAlertPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        monitoring_projects_alert_policies_patch_builder(client, &args.name, &args.updateMask)?;
    monitoring_projects_alert_policies_patch_execute(builder)
}

/// GET v3/projects/{projectsId}/alerts/{alertsId}
/// Gets a single alert.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alerts_get_execute()` to send, or `monitoring_projects_alerts_get` for simplest API.

pub fn monitoring_projects_alerts_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/alerts/{alertsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/alerts/{alertsId}
/// Gets a single alert.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_alerts_get_execute()` or `monitoring_projects_alerts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alerts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alerts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Alert>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/alerts/{alertsId}
/// Gets a single alert.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_alerts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_alerts_get_task()`.
/// For the simplest API, use `monitoring_projects_alerts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alerts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_alerts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_alerts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_alerts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsAlertsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/alerts/{alertsId}
/// Gets a single alert.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alerts_get_builder()` + `monitoring_projects_alerts_get_execute()`.
/// For task-level control, use `monitoring_projects_alerts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alerts_get(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsAlertsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alerts_get_builder(client, &args.name)?;
    monitoring_projects_alerts_get_execute(builder)
}

/// GET v3/projects/{projectsId}/alerts
/// Lists the existing alerts for the metrics scope of the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alerts_list_execute()` to send, or `monitoring_projects_alerts_list` for simplest API.

pub fn monitoring_projects_alerts_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/alerts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/alerts
/// Lists the existing alerts for the metrics scope of the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_alerts_list_execute()` or `monitoring_projects_alerts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alerts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alerts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAlertsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAlertsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/alerts
/// Lists the existing alerts for the metrics scope of the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_alerts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_alerts_list_task()`.
/// For the simplest API, use `monitoring_projects_alerts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_alerts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_alerts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_alerts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_alerts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsAlertsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v3/projects/{projectsId}/alerts
/// Lists the existing alerts for the metrics scope of the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alerts_list_builder()` + `monitoring_projects_alerts_list_execute()`.
/// For task-level control, use `monitoring_projects_alerts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alerts_list(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsAlertsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alerts_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    monitoring_projects_alerts_list_execute(builder)
}

/// POST v3/projects/{projectsId}/collectdTimeSeries
/// Cloud Monitoring Agent only: Creates a new time series.This method is only for use by the Cloud Monitoring Agent. Use projects.`timeSeries`.create instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_collectd_time_series_create_execute()` to send, or `monitoring_projects_collectd_time_series_create` for simplest API.

pub fn monitoring_projects_collectd_time_series_create_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/collectdTimeSeries",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/collectdTimeSeries
/// Cloud Monitoring Agent only: Creates a new time series.This method is only for use by the Cloud Monitoring Agent. Use projects.`timeSeries`.create instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_collectd_time_series_create_execute()` or `monitoring_projects_collectd_time_series_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_collectd_time_series_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_collectd_time_series_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreateCollectdTimeSeriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateCollectdTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/collectdTimeSeries
/// Cloud Monitoring Agent only: Creates a new time series.This method is only for use by the Cloud Monitoring Agent. Use projects.`timeSeries`.create instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_collectd_time_series_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_collectd_time_series_create_task()`.
/// For the simplest API, use `monitoring_projects_collectd_time_series_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_collectd_time_series_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_collectd_time_series_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateCollectdTimeSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_collectd_time_series_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_collectd_time_series_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsCollectdTimeSeriesCreateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/collectdTimeSeries
/// Cloud Monitoring Agent only: Creates a new time series.This method is only for use by the Cloud Monitoring Agent. Use projects.`timeSeries`.create instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_collectd_time_series_create_builder()` + `monitoring_projects_collectd_time_series_create_execute()`.
/// For task-level control, use `monitoring_projects_collectd_time_series_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_collectd_time_series_create(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsCollectdTimeSeriesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateCollectdTimeSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_collectd_time_series_create_builder(client, &args.name)?;
    monitoring_projects_collectd_time_series_create_execute(builder)
}

/// POST v3/projects/{projectsId}/groups
/// Creates a new group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_groups_create_execute()` to send, or `monitoring_projects_groups_create` for simplest API.

pub fn monitoring_projects_groups_create_builder(
    client: &SimpleHttpClient,
    name: &String,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/groups",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/groups
/// Creates a new group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_groups_create_execute()` or `monitoring_projects_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Group>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Group = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/groups
/// Creates a new group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_groups_create_task()`.
/// For the simplest API, use `monitoring_projects_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsGroupsCreateArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// POST v3/projects/{projectsId}/groups
/// Creates a new group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_groups_create_builder()` + `monitoring_projects_groups_create_execute()`.
/// For task-level control, use `monitoring_projects_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_create(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        monitoring_projects_groups_create_builder(client, &args.name, &args.validateOnly)?;
    monitoring_projects_groups_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/groups/{groupsId}
/// Deletes an existing group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_groups_delete_execute()` to send, or `monitoring_projects_groups_delete` for simplest API.

pub fn monitoring_projects_groups_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    recursive: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/groups/{groupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = recursive.as_ref() {
        query_parts.push(format!("recursive={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/groups/{groupsId}
/// Deletes an existing group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_groups_delete_execute()` or `monitoring_projects_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/groups/{groupsId}
/// Deletes an existing group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_groups_delete_task()`.
/// For the simplest API, use `monitoring_projects_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: recursive
    pub recursive: Option<Option<String>>,
}

/// DELETE v3/projects/{projectsId}/groups/{groupsId}
/// Deletes an existing group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_groups_delete_builder()` + `monitoring_projects_groups_delete_execute()`.
/// For task-level control, use `monitoring_projects_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_delete(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_groups_delete_builder(client, &args.name, &args.recursive)?;
    monitoring_projects_groups_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/groups/{groupsId}
/// Gets a single group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_groups_get_execute()` to send, or `monitoring_projects_groups_get` for simplest API.

pub fn monitoring_projects_groups_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/groups/{groupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/groups/{groupsId}
/// Gets a single group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_groups_get_execute()` or `monitoring_projects_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Group>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Group = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/groups/{groupsId}
/// Gets a single group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_groups_get_task()`.
/// For the simplest API, use `monitoring_projects_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/groups/{groupsId}
/// Gets a single group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_groups_get_builder()` + `monitoring_projects_groups_get_execute()`.
/// For task-level control, use `monitoring_projects_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_get(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_groups_get_builder(client, &args.name)?;
    monitoring_projects_groups_get_execute(builder)
}

/// GET v3/projects/{projectsId}/groups
/// Lists the existing groups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_groups_list_execute()` to send, or `monitoring_projects_groups_list` for simplest API.

pub fn monitoring_projects_groups_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    ancestorsOfGroup: &Option<Option<String>>,
    childrenOfGroup: &Option<Option<String>>,
    descendantsOfGroup: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/groups",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ancestorsOfGroup.as_ref() {
        query_parts.push(format!("ancestorsOfGroup={}", val));
    }
    if let Some(val) = childrenOfGroup.as_ref() {
        query_parts.push(format!("childrenOfGroup={}", val));
    }
    if let Some(val) = descendantsOfGroup.as_ref() {
        query_parts.push(format!("descendantsOfGroup={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/groups
/// Lists the existing groups.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_groups_list_execute()` or `monitoring_projects_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/groups
/// Lists the existing groups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_groups_list_task()`.
/// For the simplest API, use `monitoring_projects_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsGroupsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: ancestorsOfGroup
    pub ancestorsOfGroup: Option<Option<String>>,
    /// Query parameter: childrenOfGroup
    pub childrenOfGroup: Option<Option<String>>,
    /// Query parameter: descendantsOfGroup
    pub descendantsOfGroup: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v3/projects/{projectsId}/groups
/// Lists the existing groups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_groups_list_builder()` + `monitoring_projects_groups_list_execute()`.
/// For task-level control, use `monitoring_projects_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_list(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_groups_list_builder(
        client,
        &args.name,
        &args.ancestorsOfGroup,
        &args.childrenOfGroup,
        &args.descendantsOfGroup,
        &args.pageSize,
        &args.pageToken,
    )?;
    monitoring_projects_groups_list_execute(builder)
}

/// PUT v3/projects/{projectsId}/groups/{groupsId}
/// Updates an existing group. You can change any group attributes except name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_groups_update_execute()` to send, or `monitoring_projects_groups_update` for simplest API.

pub fn monitoring_projects_groups_update_builder(
    client: &SimpleHttpClient,
    name: &String,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/groups/{groupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v3/projects/{projectsId}/groups/{groupsId}
/// Updates an existing group. You can change any group attributes except name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_groups_update_execute()` or `monitoring_projects_groups_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_groups_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Group>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Group = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v3/projects/{projectsId}/groups/{groupsId}
/// Updates an existing group. You can change any group attributes except name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_groups_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_groups_update_task()`.
/// For the simplest API, use `monitoring_projects_groups_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_groups_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_groups_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_groups_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_groups_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsGroupsUpdateArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// PUT v3/projects/{projectsId}/groups/{groupsId}
/// Updates an existing group. You can change any group attributes except name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_groups_update_builder()` + `monitoring_projects_groups_update_execute()`.
/// For task-level control, use `monitoring_projects_groups_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_update(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsGroupsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        monitoring_projects_groups_update_builder(client, &args.name, &args.validateOnly)?;
    monitoring_projects_groups_update_execute(builder)
}

/// GET v3/projects/{projectsId}/groups/{groupsId}/members
/// Lists the monitored resources that are members of a group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_groups_members_list_execute()` to send, or `monitoring_projects_groups_members_list` for simplest API.

pub fn monitoring_projects_groups_members_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<Option<String>>,
    interval_endTime: &Option<Option<String>>,
    interval_startTime: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/groups/{groupsId}/members",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = interval_endTime.as_ref() {
        query_parts.push(format!("interval.endTime={}", val));
    }
    if let Some(val) = interval_startTime.as_ref() {
        query_parts.push(format!("interval.startTime={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/groups/{groupsId}/members
/// Lists the monitored resources that are members of a group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_groups_members_list_execute()` or `monitoring_projects_groups_members_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_groups_members_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_members_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListGroupMembersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGroupMembersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/groups/{groupsId}/members
/// Lists the monitored resources that are members of a group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_groups_members_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_groups_members_list_task()`.
/// For the simplest API, use `monitoring_projects_groups_members_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_groups_members_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_groups_members_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupMembersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_groups_members_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_groups_members_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsGroupsMembersListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: interval_endTime
    pub interval_endTime: Option<Option<String>>,
    /// Query parameter: interval_startTime
    pub interval_startTime: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v3/projects/{projectsId}/groups/{groupsId}/members
/// Lists the monitored resources that are members of a group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_groups_members_list_builder()` + `monitoring_projects_groups_members_list_execute()`.
/// For task-level control, use `monitoring_projects_groups_members_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_members_list(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsGroupsMembersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupMembersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_groups_members_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.interval_endTime,
        &args.interval_startTime,
        &args.pageSize,
        &args.pageToken,
    )?;
    monitoring_projects_groups_members_list_execute(builder)
}

/// POST v3/projects/{projectsId}/metricDescriptors
/// Creates a new metric descriptor. The creation is executed asynchronously. User-created metric descriptors define custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>). The metric descriptor is updated if it already exists, except that metric labels are never removed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_metric_descriptors_create_execute()` to send, or `monitoring_projects_metric_descriptors_create` for simplest API.

pub fn monitoring_projects_metric_descriptors_create_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/metricDescriptors",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/metricDescriptors
/// Creates a new metric descriptor. The creation is executed asynchronously. User-created metric descriptors define custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>). The metric descriptor is updated if it already exists, except that metric labels are never removed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_metric_descriptors_create_execute()` or `monitoring_projects_metric_descriptors_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_metric_descriptors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MetricDescriptor>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MetricDescriptor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/metricDescriptors
/// Creates a new metric descriptor. The creation is executed asynchronously. User-created metric descriptors define custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>). The metric descriptor is updated if it already exists, except that metric labels are never removed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_metric_descriptors_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_metric_descriptors_create_task()`.
/// For the simplest API, use `monitoring_projects_metric_descriptors_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_metric_descriptors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_metric_descriptors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetricDescriptor>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_metric_descriptors_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_metric_descriptors_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsMetricDescriptorsCreateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/metricDescriptors
/// Creates a new metric descriptor. The creation is executed asynchronously. User-created metric descriptors define custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>). The metric descriptor is updated if it already exists, except that metric labels are never removed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_metric_descriptors_create_builder()` + `monitoring_projects_metric_descriptors_create_execute()`.
/// For task-level control, use `monitoring_projects_metric_descriptors_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_create(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsMetricDescriptorsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetricDescriptor>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_metric_descriptors_create_builder(client, &args.name)?;
    monitoring_projects_metric_descriptors_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/metricDescriptors/{metricDescriptorsId}
/// Deletes a metric descriptor. Only user-created custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>) can be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_metric_descriptors_delete_execute()` to send, or `monitoring_projects_metric_descriptors_delete` for simplest API.

pub fn monitoring_projects_metric_descriptors_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/metricDescriptors/{metricDescriptorsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/metricDescriptors/{metricDescriptorsId}
/// Deletes a metric descriptor. Only user-created custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>) can be deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_metric_descriptors_delete_execute()` or `monitoring_projects_metric_descriptors_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_metric_descriptors_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/metricDescriptors/{metricDescriptorsId}
/// Deletes a metric descriptor. Only user-created custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>) can be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_metric_descriptors_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_metric_descriptors_delete_task()`.
/// For the simplest API, use `monitoring_projects_metric_descriptors_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_metric_descriptors_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_metric_descriptors_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_metric_descriptors_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_metric_descriptors_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsMetricDescriptorsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/metricDescriptors/{metricDescriptorsId}
/// Deletes a metric descriptor. Only user-created custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>) can be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_metric_descriptors_delete_builder()` + `monitoring_projects_metric_descriptors_delete_execute()`.
/// For task-level control, use `monitoring_projects_metric_descriptors_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_delete(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsMetricDescriptorsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_metric_descriptors_delete_builder(client, &args.name)?;
    monitoring_projects_metric_descriptors_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/metricDescriptors/{metricDescriptorsId}
/// Gets a single metric descriptor.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_metric_descriptors_get_execute()` to send, or `monitoring_projects_metric_descriptors_get` for simplest API.

pub fn monitoring_projects_metric_descriptors_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/metricDescriptors/{metricDescriptorsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/metricDescriptors/{metricDescriptorsId}
/// Gets a single metric descriptor.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_metric_descriptors_get_execute()` or `monitoring_projects_metric_descriptors_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_metric_descriptors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MetricDescriptor>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MetricDescriptor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/metricDescriptors/{metricDescriptorsId}
/// Gets a single metric descriptor.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_metric_descriptors_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_metric_descriptors_get_task()`.
/// For the simplest API, use `monitoring_projects_metric_descriptors_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_metric_descriptors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_metric_descriptors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetricDescriptor>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_metric_descriptors_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_metric_descriptors_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsMetricDescriptorsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/metricDescriptors/{metricDescriptorsId}
/// Gets a single metric descriptor.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_metric_descriptors_get_builder()` + `monitoring_projects_metric_descriptors_get_execute()`.
/// For task-level control, use `monitoring_projects_metric_descriptors_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_get(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsMetricDescriptorsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetricDescriptor>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_metric_descriptors_get_builder(client, &args.name)?;
    monitoring_projects_metric_descriptors_get_execute(builder)
}

/// GET v3/projects/{projectsId}/metricDescriptors
/// Lists metric descriptors that match a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_metric_descriptors_list_execute()` to send, or `monitoring_projects_metric_descriptors_list` for simplest API.

pub fn monitoring_projects_metric_descriptors_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    activeOnly: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/metricDescriptors",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = activeOnly.as_ref() {
        query_parts.push(format!("activeOnly={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/metricDescriptors
/// Lists metric descriptors that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_metric_descriptors_list_execute()` or `monitoring_projects_metric_descriptors_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_metric_descriptors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMetricDescriptorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMetricDescriptorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/metricDescriptors
/// Lists metric descriptors that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_metric_descriptors_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_metric_descriptors_list_task()`.
/// For the simplest API, use `monitoring_projects_metric_descriptors_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_metric_descriptors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_metric_descriptors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMetricDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_metric_descriptors_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_metric_descriptors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsMetricDescriptorsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: activeOnly
    pub activeOnly: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v3/projects/{projectsId}/metricDescriptors
/// Lists metric descriptors that match a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_metric_descriptors_list_builder()` + `monitoring_projects_metric_descriptors_list_execute()`.
/// For task-level control, use `monitoring_projects_metric_descriptors_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_list(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsMetricDescriptorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMetricDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_metric_descriptors_list_builder(
        client,
        &args.name,
        &args.activeOnly,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    monitoring_projects_metric_descriptors_list_execute(builder)
}

/// GET v3/projects/{projectsId}/monitoredResourceDescriptors/{monitoredResourceDescriptorsId}
/// Gets a single monitored resource descriptor.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_monitored_resource_descriptors_get_execute()` to send, or `monitoring_projects_monitored_resource_descriptors_get` for simplest API.

pub fn monitoring_projects_monitored_resource_descriptors_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/monitoredResourceDescriptors/{monitoredResourceDescriptorsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/monitoredResourceDescriptors/{monitoredResourceDescriptorsId}
/// Gets a single monitored resource descriptor.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_monitored_resource_descriptors_get_execute()` or `monitoring_projects_monitored_resource_descriptors_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_monitored_resource_descriptors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_monitored_resource_descriptors_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MonitoredResourceDescriptor>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MonitoredResourceDescriptor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/monitoredResourceDescriptors/{monitoredResourceDescriptorsId}
/// Gets a single monitored resource descriptor.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_monitored_resource_descriptors_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_monitored_resource_descriptors_get_task()`.
/// For the simplest API, use `monitoring_projects_monitored_resource_descriptors_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_monitored_resource_descriptors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_monitored_resource_descriptors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MonitoredResourceDescriptor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_monitored_resource_descriptors_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_monitored_resource_descriptors_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsMonitoredResourceDescriptorsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/monitoredResourceDescriptors/{monitoredResourceDescriptorsId}
/// Gets a single monitored resource descriptor.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_monitored_resource_descriptors_get_builder()` + `monitoring_projects_monitored_resource_descriptors_get_execute()`.
/// For task-level control, use `monitoring_projects_monitored_resource_descriptors_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_monitored_resource_descriptors_get(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsMonitoredResourceDescriptorsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MonitoredResourceDescriptor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        monitoring_projects_monitored_resource_descriptors_get_builder(client, &args.name)?;
    monitoring_projects_monitored_resource_descriptors_get_execute(builder)
}

/// GET v3/projects/{projectsId}/monitoredResourceDescriptors
/// Lists monitored resource descriptors that match a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_monitored_resource_descriptors_list_execute()` to send, or `monitoring_projects_monitored_resource_descriptors_list` for simplest API.

pub fn monitoring_projects_monitored_resource_descriptors_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/monitoredResourceDescriptors",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/monitoredResourceDescriptors
/// Lists monitored resource descriptors that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_monitored_resource_descriptors_list_execute()` or `monitoring_projects_monitored_resource_descriptors_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_monitored_resource_descriptors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_monitored_resource_descriptors_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMonitoredResourceDescriptorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMonitoredResourceDescriptorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/monitoredResourceDescriptors
/// Lists monitored resource descriptors that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_monitored_resource_descriptors_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_monitored_resource_descriptors_list_task()`.
/// For the simplest API, use `monitoring_projects_monitored_resource_descriptors_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_monitored_resource_descriptors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_monitored_resource_descriptors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMonitoredResourceDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_monitored_resource_descriptors_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_monitored_resource_descriptors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsMonitoredResourceDescriptorsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v3/projects/{projectsId}/monitoredResourceDescriptors
/// Lists monitored resource descriptors that match a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_monitored_resource_descriptors_list_builder()` + `monitoring_projects_monitored_resource_descriptors_list_execute()`.
/// For task-level control, use `monitoring_projects_monitored_resource_descriptors_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_monitored_resource_descriptors_list(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsMonitoredResourceDescriptorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMonitoredResourceDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_monitored_resource_descriptors_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    monitoring_projects_monitored_resource_descriptors_list_execute(builder)
}

/// GET v3/projects/{projectsId}/notificationChannelDescriptors/{notificationChannelDescriptorsId}
/// Gets a single channel descriptor. The descriptor indicates which fields are expected / permitted for a notification channel of the given type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channel_descriptors_get_execute()` to send, or `monitoring_projects_notification_channel_descriptors_get` for simplest API.

pub fn monitoring_projects_notification_channel_descriptors_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/notificationChannelDescriptors/{notificationChannelDescriptorsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/notificationChannelDescriptors/{notificationChannelDescriptorsId}
/// Gets a single channel descriptor. The descriptor indicates which fields are expected / permitted for a notification channel of the given type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_notification_channel_descriptors_get_execute()` or `monitoring_projects_notification_channel_descriptors_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channel_descriptors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channel_descriptors_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationChannelDescriptor>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationChannelDescriptor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/notificationChannelDescriptors/{notificationChannelDescriptorsId}
/// Gets a single channel descriptor. The descriptor indicates which fields are expected / permitted for a notification channel of the given type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_notification_channel_descriptors_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_notification_channel_descriptors_get_task()`.
/// For the simplest API, use `monitoring_projects_notification_channel_descriptors_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channel_descriptors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_notification_channel_descriptors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NotificationChannelDescriptor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_notification_channel_descriptors_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_notification_channel_descriptors_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsNotificationChannelDescriptorsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/notificationChannelDescriptors/{notificationChannelDescriptorsId}
/// Gets a single channel descriptor. The descriptor indicates which fields are expected / permitted for a notification channel of the given type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channel_descriptors_get_builder()` + `monitoring_projects_notification_channel_descriptors_get_execute()`.
/// For task-level control, use `monitoring_projects_notification_channel_descriptors_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channel_descriptors_get(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsNotificationChannelDescriptorsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NotificationChannelDescriptor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        monitoring_projects_notification_channel_descriptors_get_builder(client, &args.name)?;
    monitoring_projects_notification_channel_descriptors_get_execute(builder)
}

/// GET v3/projects/{projectsId}/notificationChannelDescriptors
/// Lists the descriptors for supported channel types. The use of descriptors makes it possible for new channel types to be dynamically added.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channel_descriptors_list_execute()` to send, or `monitoring_projects_notification_channel_descriptors_list` for simplest API.

pub fn monitoring_projects_notification_channel_descriptors_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/notificationChannelDescriptors",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/notificationChannelDescriptors
/// Lists the descriptors for supported channel types. The use of descriptors makes it possible for new channel types to be dynamically added.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_notification_channel_descriptors_list_execute()` or `monitoring_projects_notification_channel_descriptors_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channel_descriptors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channel_descriptors_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNotificationChannelDescriptorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNotificationChannelDescriptorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/notificationChannelDescriptors
/// Lists the descriptors for supported channel types. The use of descriptors makes it possible for new channel types to be dynamically added.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_notification_channel_descriptors_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_notification_channel_descriptors_list_task()`.
/// For the simplest API, use `monitoring_projects_notification_channel_descriptors_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channel_descriptors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_notification_channel_descriptors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationChannelDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_notification_channel_descriptors_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_notification_channel_descriptors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsNotificationChannelDescriptorsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v3/projects/{projectsId}/notificationChannelDescriptors
/// Lists the descriptors for supported channel types. The use of descriptors makes it possible for new channel types to be dynamically added.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channel_descriptors_list_builder()` + `monitoring_projects_notification_channel_descriptors_list_execute()`.
/// For task-level control, use `monitoring_projects_notification_channel_descriptors_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channel_descriptors_list(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsNotificationChannelDescriptorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationChannelDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channel_descriptors_list_builder(
        client,
        &args.name,
        &args.pageSize,
        &args.pageToken,
    )?;
    monitoring_projects_notification_channel_descriptors_list_execute(builder)
}

/// POST v3/projects/{projectsId}/notificationChannels
/// Creates a new notification channel, representing a single notification endpoint such as an email address, SMS number, or PagerDuty service.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_create_execute()` to send, or `monitoring_projects_notification_channels_create` for simplest API.

pub fn monitoring_projects_notification_channels_create_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/notificationChannels",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/notificationChannels
/// Creates a new notification channel, representing a single notification endpoint such as an email address, SMS number, or PagerDuty service.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_notification_channels_create_execute()` or `monitoring_projects_notification_channels_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationChannel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/notificationChannels
/// Creates a new notification channel, representing a single notification endpoint such as an email address, SMS number, or PagerDuty service.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_notification_channels_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_notification_channels_create_task()`.
/// For the simplest API, use `monitoring_projects_notification_channels_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_notification_channels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_notification_channels_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_notification_channels_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsNotificationChannelsCreateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/notificationChannels
/// Creates a new notification channel, representing a single notification endpoint such as an email address, SMS number, or PagerDuty service.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_create_builder()` + `monitoring_projects_notification_channels_create_execute()`.
/// For task-level control, use `monitoring_projects_notification_channels_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_create(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsNotificationChannelsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_create_builder(client, &args.name)?;
    monitoring_projects_notification_channels_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}
/// Deletes a notification channel.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_delete_execute()` to send, or `monitoring_projects_notification_channels_delete` for simplest API.

pub fn monitoring_projects_notification_channels_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    force: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/notificationChannels/{notificationChannelsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}
/// Deletes a notification channel.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_notification_channels_delete_execute()` or `monitoring_projects_notification_channels_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}
/// Deletes a notification channel.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_notification_channels_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_notification_channels_delete_task()`.
/// For the simplest API, use `monitoring_projects_notification_channels_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_notification_channels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_notification_channels_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_notification_channels_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsNotificationChannelsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<Option<String>>,
}

/// DELETE v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}
/// Deletes a notification channel.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_delete_builder()` + `monitoring_projects_notification_channels_delete_execute()`.
/// For task-level control, use `monitoring_projects_notification_channels_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_delete(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsNotificationChannelsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        monitoring_projects_notification_channels_delete_builder(client, &args.name, &args.force)?;
    monitoring_projects_notification_channels_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}
/// Gets a single notification channel. The channel includes the relevant configuration details with which the channel was created. However, the response may truncate or omit passwords, API keys, or other private key matter and thus the response may not be 100% identical to the information that was supplied in the call to the create method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_get_execute()` to send, or `monitoring_projects_notification_channels_get` for simplest API.

pub fn monitoring_projects_notification_channels_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/notificationChannels/{notificationChannelsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}
/// Gets a single notification channel. The channel includes the relevant configuration details with which the channel was created. However, the response may truncate or omit passwords, API keys, or other private key matter and thus the response may not be 100% identical to the information that was supplied in the call to the create method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_notification_channels_get_execute()` or `monitoring_projects_notification_channels_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationChannel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}
/// Gets a single notification channel. The channel includes the relevant configuration details with which the channel was created. However, the response may truncate or omit passwords, API keys, or other private key matter and thus the response may not be 100% identical to the information that was supplied in the call to the create method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_notification_channels_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_notification_channels_get_task()`.
/// For the simplest API, use `monitoring_projects_notification_channels_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_notification_channels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_notification_channels_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_notification_channels_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsNotificationChannelsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}
/// Gets a single notification channel. The channel includes the relevant configuration details with which the channel was created. However, the response may truncate or omit passwords, API keys, or other private key matter and thus the response may not be 100% identical to the information that was supplied in the call to the create method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_get_builder()` + `monitoring_projects_notification_channels_get_execute()`.
/// For task-level control, use `monitoring_projects_notification_channels_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_get(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsNotificationChannelsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_get_builder(client, &args.name)?;
    monitoring_projects_notification_channels_get_execute(builder)
}

/// POST v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}:getVerificationCode
/// Requests a verification code for an already verified channel that can then be used in a call to VerifyNotificationChannel() on a different channel with an equivalent identity in the same or in a different project. This makes it possible to copy a channel between projects without requiring manual reverification of the channel. If the channel is not in the verified state, this method will fail (in other words, this may only be used if the SendNotificationChannelVerificationCode and VerifyNotificationChannel paths have already been used to put the given channel into the verified state).There is no guarantee that the verification codes returned by this method will be of a similar structure or form as the ones that are delivered to the channel via SendNotificationChannelVerificationCode; while VerifyNotificationChannel() will recognize both the codes delivered via SendNotificationChannelVerificationCode() and returned from GetNotificationChannelVerificationCode(), it is typically the case that the verification codes delivered via SendNotificationChannelVerificationCode() will be shorter and also have a shorter expiration (e.g. codes such as "G-123456") whereas GetVerificationCode() will typically return a much longer, websafe base 64 encoded string that has a longer expiration time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_get_verification_code_execute()` to send, or `monitoring_projects_notification_channels_get_verification_code` for simplest API.

pub fn monitoring_projects_notification_channels_get_verification_code_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/notificationChannels/{notificationChannelsId}:getVerificationCode",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}:getVerificationCode
/// Requests a verification code for an already verified channel that can then be used in a call to VerifyNotificationChannel() on a different channel with an equivalent identity in the same or in a different project. This makes it possible to copy a channel between projects without requiring manual reverification of the channel. If the channel is not in the verified state, this method will fail (in other words, this may only be used if the SendNotificationChannelVerificationCode and VerifyNotificationChannel paths have already been used to put the given channel into the verified state).There is no guarantee that the verification codes returned by this method will be of a similar structure or form as the ones that are delivered to the channel via SendNotificationChannelVerificationCode; while VerifyNotificationChannel() will recognize both the codes delivered via SendNotificationChannelVerificationCode() and returned from GetNotificationChannelVerificationCode(), it is typically the case that the verification codes delivered via SendNotificationChannelVerificationCode() will be shorter and also have a shorter expiration (e.g. codes such as "G-123456") whereas GetVerificationCode() will typically return a much longer, websafe base 64 encoded string that has a longer expiration time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_notification_channels_get_verification_code_execute()` or `monitoring_projects_notification_channels_get_verification_code`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_get_verification_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_get_verification_code_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetNotificationChannelVerificationCodeResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetNotificationChannelVerificationCodeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}:getVerificationCode
/// Requests a verification code for an already verified channel that can then be used in a call to VerifyNotificationChannel() on a different channel with an equivalent identity in the same or in a different project. This makes it possible to copy a channel between projects without requiring manual reverification of the channel. If the channel is not in the verified state, this method will fail (in other words, this may only be used if the SendNotificationChannelVerificationCode and VerifyNotificationChannel paths have already been used to put the given channel into the verified state).There is no guarantee that the verification codes returned by this method will be of a similar structure or form as the ones that are delivered to the channel via SendNotificationChannelVerificationCode; while VerifyNotificationChannel() will recognize both the codes delivered via SendNotificationChannelVerificationCode() and returned from GetNotificationChannelVerificationCode(), it is typically the case that the verification codes delivered via SendNotificationChannelVerificationCode() will be shorter and also have a shorter expiration (e.g. codes such as "G-123456") whereas GetVerificationCode() will typically return a much longer, websafe base 64 encoded string that has a longer expiration time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_notification_channels_get_verification_code_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_notification_channels_get_verification_code_task()`.
/// For the simplest API, use `monitoring_projects_notification_channels_get_verification_code()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_get_verification_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_notification_channels_get_verification_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetNotificationChannelVerificationCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_notification_channels_get_verification_code_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_notification_channels_get_verification_code`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsNotificationChannelsGetVerificationCodeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}:getVerificationCode
/// Requests a verification code for an already verified channel that can then be used in a call to VerifyNotificationChannel() on a different channel with an equivalent identity in the same or in a different project. This makes it possible to copy a channel between projects without requiring manual reverification of the channel. If the channel is not in the verified state, this method will fail (in other words, this may only be used if the SendNotificationChannelVerificationCode and VerifyNotificationChannel paths have already been used to put the given channel into the verified state).There is no guarantee that the verification codes returned by this method will be of a similar structure or form as the ones that are delivered to the channel via SendNotificationChannelVerificationCode; while VerifyNotificationChannel() will recognize both the codes delivered via SendNotificationChannelVerificationCode() and returned from GetNotificationChannelVerificationCode(), it is typically the case that the verification codes delivered via SendNotificationChannelVerificationCode() will be shorter and also have a shorter expiration (e.g. codes such as "G-123456") whereas GetVerificationCode() will typically return a much longer, websafe base 64 encoded string that has a longer expiration time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_get_verification_code_builder()` + `monitoring_projects_notification_channels_get_verification_code_execute()`.
/// For task-level control, use `monitoring_projects_notification_channels_get_verification_code_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_get_verification_code(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsNotificationChannelsGetVerificationCodeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetNotificationChannelVerificationCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_get_verification_code_builder(
        client, &args.name,
    )?;
    monitoring_projects_notification_channels_get_verification_code_execute(builder)
}

/// GET v3/projects/{projectsId}/notificationChannels
/// Lists the notification channels that have been created for the project. To list the types of notification channels that are supported, use the ListNotificationChannelDescriptors method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_list_execute()` to send, or `monitoring_projects_notification_channels_list` for simplest API.

pub fn monitoring_projects_notification_channels_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/notificationChannels",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/notificationChannels
/// Lists the notification channels that have been created for the project. To list the types of notification channels that are supported, use the ListNotificationChannelDescriptors method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_notification_channels_list_execute()` or `monitoring_projects_notification_channels_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNotificationChannelsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNotificationChannelsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/notificationChannels
/// Lists the notification channels that have been created for the project. To list the types of notification channels that are supported, use the ListNotificationChannelDescriptors method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_notification_channels_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_notification_channels_list_task()`.
/// For the simplest API, use `monitoring_projects_notification_channels_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_notification_channels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationChannelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_notification_channels_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_notification_channels_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsNotificationChannelsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v3/projects/{projectsId}/notificationChannels
/// Lists the notification channels that have been created for the project. To list the types of notification channels that are supported, use the ListNotificationChannelDescriptors method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_list_builder()` + `monitoring_projects_notification_channels_list_execute()`.
/// For task-level control, use `monitoring_projects_notification_channels_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_list(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsNotificationChannelsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationChannelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    monitoring_projects_notification_channels_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}
/// Updates a notification channel. Fields not specified in the field mask remain unchanged.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_patch_execute()` to send, or `monitoring_projects_notification_channels_patch` for simplest API.

pub fn monitoring_projects_notification_channels_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/notificationChannels/{notificationChannelsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}
/// Updates a notification channel. Fields not specified in the field mask remain unchanged.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_notification_channels_patch_execute()` or `monitoring_projects_notification_channels_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationChannel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}
/// Updates a notification channel. Fields not specified in the field mask remain unchanged.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_notification_channels_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_notification_channels_patch_task()`.
/// For the simplest API, use `monitoring_projects_notification_channels_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_notification_channels_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_notification_channels_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_notification_channels_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsNotificationChannelsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}
/// Updates a notification channel. Fields not specified in the field mask remain unchanged.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_patch_builder()` + `monitoring_projects_notification_channels_patch_execute()`.
/// For task-level control, use `monitoring_projects_notification_channels_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_patch(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsNotificationChannelsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    monitoring_projects_notification_channels_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}:sendVerificationCode
/// Causes a verification code to be delivered to the channel. The code can then be supplied in VerifyNotificationChannel to verify the channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_send_verification_code_execute()` to send, or `monitoring_projects_notification_channels_send_verification_code` for simplest API.

pub fn monitoring_projects_notification_channels_send_verification_code_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/notificationChannels/{notificationChannelsId}:sendVerificationCode",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}:sendVerificationCode
/// Causes a verification code to be delivered to the channel. The code can then be supplied in VerifyNotificationChannel to verify the channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_notification_channels_send_verification_code_execute()` or `monitoring_projects_notification_channels_send_verification_code`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_send_verification_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_send_verification_code_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}:sendVerificationCode
/// Causes a verification code to be delivered to the channel. The code can then be supplied in VerifyNotificationChannel to verify the channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_notification_channels_send_verification_code_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_notification_channels_send_verification_code_task()`.
/// For the simplest API, use `monitoring_projects_notification_channels_send_verification_code()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_send_verification_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_notification_channels_send_verification_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_notification_channels_send_verification_code_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_notification_channels_send_verification_code`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsNotificationChannelsSendVerificationCodeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}:sendVerificationCode
/// Causes a verification code to be delivered to the channel. The code can then be supplied in VerifyNotificationChannel to verify the channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_send_verification_code_builder()` + `monitoring_projects_notification_channels_send_verification_code_execute()`.
/// For task-level control, use `monitoring_projects_notification_channels_send_verification_code_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_send_verification_code(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsNotificationChannelsSendVerificationCodeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_send_verification_code_builder(
        client, &args.name,
    )?;
    monitoring_projects_notification_channels_send_verification_code_execute(builder)
}

/// POST v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}:verify
/// Verifies a NotificationChannel by proving receipt of the code delivered to the channel as a result of calling SendNotificationChannelVerificationCode.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_verify_execute()` to send, or `monitoring_projects_notification_channels_verify` for simplest API.

pub fn monitoring_projects_notification_channels_verify_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/notificationChannels/{notificationChannelsId}:verify",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}:verify
/// Verifies a NotificationChannel by proving receipt of the code delivered to the channel as a result of calling SendNotificationChannelVerificationCode.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_notification_channels_verify_execute()` or `monitoring_projects_notification_channels_verify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_verify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_verify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationChannel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}:verify
/// Verifies a NotificationChannel by proving receipt of the code delivered to the channel as a result of calling SendNotificationChannelVerificationCode.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_notification_channels_verify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_notification_channels_verify_task()`.
/// For the simplest API, use `monitoring_projects_notification_channels_verify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_notification_channels_verify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_notification_channels_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_notification_channels_verify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_notification_channels_verify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsNotificationChannelsVerifyArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/notificationChannels/{notificationChannelsId}:verify
/// Verifies a NotificationChannel by proving receipt of the code delivered to the channel as a result of calling SendNotificationChannelVerificationCode.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_verify_builder()` + `monitoring_projects_notification_channels_verify_execute()`.
/// For task-level control, use `monitoring_projects_notification_channels_verify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_verify(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsNotificationChannelsVerifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_verify_builder(client, &args.name)?;
    monitoring_projects_notification_channels_verify_execute(builder)
}

/// POST v3/projects/{projectsId}/snoozes
/// Creates a Snooze that will prevent alerts, which match the provided criteria, from being opened. The Snooze applies for a specific time interval.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_snoozes_create_execute()` to send, or `monitoring_projects_snoozes_create` for simplest API.

pub fn monitoring_projects_snoozes_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/snoozes",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/snoozes
/// Creates a Snooze that will prevent alerts, which match the provided criteria, from being opened. The Snooze applies for a specific time interval.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_snoozes_create_execute()` or `monitoring_projects_snoozes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_snoozes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snooze>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snooze = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/snoozes
/// Creates a Snooze that will prevent alerts, which match the provided criteria, from being opened. The Snooze applies for a specific time interval.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_snoozes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_snoozes_create_task()`.
/// For the simplest API, use `monitoring_projects_snoozes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_snoozes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_snoozes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snooze>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_snoozes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_snoozes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsSnoozesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/snoozes
/// Creates a Snooze that will prevent alerts, which match the provided criteria, from being opened. The Snooze applies for a specific time interval.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_snoozes_create_builder()` + `monitoring_projects_snoozes_create_execute()`.
/// For task-level control, use `monitoring_projects_snoozes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_create(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsSnoozesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snooze>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_snoozes_create_builder(client, &args.parent)?;
    monitoring_projects_snoozes_create_execute(builder)
}

/// GET v3/projects/{projectsId}/snoozes/{snoozesId}
/// Retrieves a Snooze by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_snoozes_get_execute()` to send, or `monitoring_projects_snoozes_get` for simplest API.

pub fn monitoring_projects_snoozes_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/snoozes/{snoozesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/snoozes/{snoozesId}
/// Retrieves a Snooze by name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_snoozes_get_execute()` or `monitoring_projects_snoozes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_snoozes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snooze>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snooze = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/snoozes/{snoozesId}
/// Retrieves a Snooze by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_snoozes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_snoozes_get_task()`.
/// For the simplest API, use `monitoring_projects_snoozes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_snoozes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_snoozes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snooze>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_snoozes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_snoozes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsSnoozesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/snoozes/{snoozesId}
/// Retrieves a Snooze by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_snoozes_get_builder()` + `monitoring_projects_snoozes_get_execute()`.
/// For task-level control, use `monitoring_projects_snoozes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_get(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsSnoozesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snooze>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_snoozes_get_builder(client, &args.name)?;
    monitoring_projects_snoozes_get_execute(builder)
}

/// GET v3/projects/{projectsId}/snoozes
/// Lists the Snoozes associated with a project. Can optionally pass in filter, which specifies predicates to match Snoozes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_snoozes_list_execute()` to send, or `monitoring_projects_snoozes_list` for simplest API.

pub fn monitoring_projects_snoozes_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/snoozes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/snoozes
/// Lists the Snoozes associated with a project. Can optionally pass in filter, which specifies predicates to match Snoozes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_snoozes_list_execute()` or `monitoring_projects_snoozes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_snoozes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSnoozesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSnoozesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/snoozes
/// Lists the Snoozes associated with a project. Can optionally pass in filter, which specifies predicates to match Snoozes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_snoozes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_snoozes_list_task()`.
/// For the simplest API, use `monitoring_projects_snoozes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_snoozes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_snoozes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnoozesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_snoozes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_snoozes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsSnoozesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v3/projects/{projectsId}/snoozes
/// Lists the Snoozes associated with a project. Can optionally pass in filter, which specifies predicates to match Snoozes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_snoozes_list_builder()` + `monitoring_projects_snoozes_list_execute()`.
/// For task-level control, use `monitoring_projects_snoozes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_list(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsSnoozesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnoozesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_snoozes_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    monitoring_projects_snoozes_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/snoozes/{snoozesId}
/// Updates a Snooze, identified by its name, with the parameters in the given Snooze object.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_snoozes_patch_execute()` to send, or `monitoring_projects_snoozes_patch` for simplest API.

pub fn monitoring_projects_snoozes_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/snoozes/{snoozesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/snoozes/{snoozesId}
/// Updates a Snooze, identified by its name, with the parameters in the given Snooze object.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_snoozes_patch_execute()` or `monitoring_projects_snoozes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_snoozes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snooze>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snooze = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/snoozes/{snoozesId}
/// Updates a Snooze, identified by its name, with the parameters in the given Snooze object.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_snoozes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_snoozes_patch_task()`.
/// For the simplest API, use `monitoring_projects_snoozes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_snoozes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_snoozes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snooze>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_snoozes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_snoozes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsSnoozesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v3/projects/{projectsId}/snoozes/{snoozesId}
/// Updates a Snooze, identified by its name, with the parameters in the given Snooze object.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_snoozes_patch_builder()` + `monitoring_projects_snoozes_patch_execute()`.
/// For task-level control, use `monitoring_projects_snoozes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_patch(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsSnoozesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snooze>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_snoozes_patch_builder(client, &args.name, &args.updateMask)?;
    monitoring_projects_snoozes_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/timeSeries
/// Creates or adds data to one or more time series. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This method does not support resource locations constraint of an organization policy (<https://cloud.google.`com/resource-manager/docs/organization-policy/defining-locations`#setting_the_organization_policy>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_time_series_create_execute()` to send, or `monitoring_projects_time_series_create` for simplest API.

pub fn monitoring_projects_time_series_create_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/timeSeries",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/timeSeries
/// Creates or adds data to one or more time series. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This method does not support resource locations constraint of an organization policy (<https://cloud.google.`com/resource-manager/docs/organization-policy/defining-locations`#setting_the_organization_policy>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_time_series_create_execute()` or `monitoring_projects_time_series_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_time_series_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/timeSeries
/// Creates or adds data to one or more time series. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This method does not support resource locations constraint of an organization policy (<https://cloud.google.`com/resource-manager/docs/organization-policy/defining-locations`#setting_the_organization_policy>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_time_series_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_time_series_create_task()`.
/// For the simplest API, use `monitoring_projects_time_series_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_time_series_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_time_series_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_time_series_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_time_series_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsTimeSeriesCreateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/timeSeries
/// Creates or adds data to one or more time series. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This method does not support resource locations constraint of an organization policy (<https://cloud.google.`com/resource-manager/docs/organization-policy/defining-locations`#setting_the_organization_policy>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_time_series_create_builder()` + `monitoring_projects_time_series_create_execute()`.
/// For task-level control, use `monitoring_projects_time_series_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_create(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsTimeSeriesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_time_series_create_builder(client, &args.name)?;
    monitoring_projects_time_series_create_execute(builder)
}

/// POST v3/projects/{projectsId}/timeSeries:createService
/// Creates or adds data to one or more service time series. A service time series is a time series for a metric from a Google Cloud service. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This endpoint rejects writes to user-defined metrics. This method is only for use by Google Cloud services. Use projects.`timeSeries`.create instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_time_series_create_service_execute()` to send, or `monitoring_projects_time_series_create_service` for simplest API.

pub fn monitoring_projects_time_series_create_service_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/timeSeries:createService",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/timeSeries:createService
/// Creates or adds data to one or more service time series. A service time series is a time series for a metric from a Google Cloud service. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This endpoint rejects writes to user-defined metrics. This method is only for use by Google Cloud services. Use projects.`timeSeries`.create instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_time_series_create_service_execute()` or `monitoring_projects_time_series_create_service`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_time_series_create_service_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_create_service_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/timeSeries:createService
/// Creates or adds data to one or more service time series. A service time series is a time series for a metric from a Google Cloud service. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This endpoint rejects writes to user-defined metrics. This method is only for use by Google Cloud services. Use projects.`timeSeries`.create instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_time_series_create_service_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_time_series_create_service_task()`.
/// For the simplest API, use `monitoring_projects_time_series_create_service()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_time_series_create_service_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_time_series_create_service_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_time_series_create_service_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_time_series_create_service`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsTimeSeriesCreateServiceArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/timeSeries:createService
/// Creates or adds data to one or more service time series. A service time series is a time series for a metric from a Google Cloud service. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This endpoint rejects writes to user-defined metrics. This method is only for use by Google Cloud services. Use projects.`timeSeries`.create instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_time_series_create_service_builder()` + `monitoring_projects_time_series_create_service_execute()`.
/// For task-level control, use `monitoring_projects_time_series_create_service_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_create_service(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsTimeSeriesCreateServiceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_time_series_create_service_builder(client, &args.name)?;
    monitoring_projects_time_series_create_service_execute(builder)
}

/// GET v3/projects/{projectsId}/timeSeries
/// Lists time series that match a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_time_series_list_execute()` to send, or `monitoring_projects_time_series_list` for simplest API.

pub fn monitoring_projects_time_series_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    aggregation_alignmentPeriod: &Option<Option<String>>,
    aggregation_crossSeriesReducer: &Option<Option<String>>,
    aggregation_groupByFields: &Option<Option<String>>,
    aggregation_perSeriesAligner: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    interval_endTime: &Option<Option<String>>,
    interval_startTime: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    secondaryAggregation_alignmentPeriod: &Option<Option<String>>,
    secondaryAggregation_crossSeriesReducer: &Option<Option<String>>,
    secondaryAggregation_groupByFields: &Option<Option<String>>,
    secondaryAggregation_perSeriesAligner: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/timeSeries",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = aggregation_alignmentPeriod.as_ref() {
        query_parts.push(format!("aggregation.alignmentPeriod={}", val));
    }
    if let Some(val) = aggregation_crossSeriesReducer.as_ref() {
        query_parts.push(format!("aggregation.crossSeriesReducer={}", val));
    }
    if let Some(val) = aggregation_groupByFields.as_ref() {
        query_parts.push(format!("aggregation.groupByFields={}", val));
    }
    if let Some(val) = aggregation_perSeriesAligner.as_ref() {
        query_parts.push(format!("aggregation.perSeriesAligner={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = interval_endTime.as_ref() {
        query_parts.push(format!("interval.endTime={}", val));
    }
    if let Some(val) = interval_startTime.as_ref() {
        query_parts.push(format!("interval.startTime={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = secondaryAggregation_alignmentPeriod.as_ref() {
        query_parts.push(format!("secondaryAggregation.alignmentPeriod={}", val));
    }
    if let Some(val) = secondaryAggregation_crossSeriesReducer.as_ref() {
        query_parts.push(format!("secondaryAggregation.crossSeriesReducer={}", val));
    }
    if let Some(val) = secondaryAggregation_groupByFields.as_ref() {
        query_parts.push(format!("secondaryAggregation.groupByFields={}", val));
    }
    if let Some(val) = secondaryAggregation_perSeriesAligner.as_ref() {
        query_parts.push(format!("secondaryAggregation.perSeriesAligner={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/timeSeries
/// Lists time series that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_time_series_list_execute()` or `monitoring_projects_time_series_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_time_series_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/timeSeries
/// Lists time series that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_time_series_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_time_series_list_task()`.
/// For the simplest API, use `monitoring_projects_time_series_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_time_series_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_time_series_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_time_series_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_time_series_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsTimeSeriesListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: aggregation_alignmentPeriod
    pub aggregation_alignmentPeriod: Option<Option<String>>,
    /// Query parameter: aggregation_crossSeriesReducer
    pub aggregation_crossSeriesReducer: Option<Option<String>>,
    /// Query parameter: aggregation_groupByFields
    pub aggregation_groupByFields: Option<Option<String>>,
    /// Query parameter: aggregation_perSeriesAligner
    pub aggregation_perSeriesAligner: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: interval_endTime
    pub interval_endTime: Option<Option<String>>,
    /// Query parameter: interval_startTime
    pub interval_startTime: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: secondaryAggregation_alignmentPeriod
    pub secondaryAggregation_alignmentPeriod: Option<Option<String>>,
    /// Query parameter: secondaryAggregation_crossSeriesReducer
    pub secondaryAggregation_crossSeriesReducer: Option<Option<String>>,
    /// Query parameter: secondaryAggregation_groupByFields
    pub secondaryAggregation_groupByFields: Option<Option<String>>,
    /// Query parameter: secondaryAggregation_perSeriesAligner
    pub secondaryAggregation_perSeriesAligner: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/projects/{projectsId}/timeSeries
/// Lists time series that match a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_time_series_list_builder()` + `monitoring_projects_time_series_list_execute()`.
/// For task-level control, use `monitoring_projects_time_series_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_list(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsTimeSeriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_time_series_list_builder(
        client,
        &args.name,
        &args.aggregation_alignmentPeriod,
        &args.aggregation_crossSeriesReducer,
        &args.aggregation_groupByFields,
        &args.aggregation_perSeriesAligner,
        &args.filter,
        &args.interval_endTime,
        &args.interval_startTime,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.secondaryAggregation_alignmentPeriod,
        &args.secondaryAggregation_crossSeriesReducer,
        &args.secondaryAggregation_groupByFields,
        &args.secondaryAggregation_perSeriesAligner,
        &args.view,
    )?;
    monitoring_projects_time_series_list_execute(builder)
}

/// POST v3/projects/{projectsId}/timeSeries:query
/// Queries time series by using Monitoring Query Language (MQL). We recommend using PromQL instead of MQL. For more information about the status of MQL, see the MQL deprecation notice (<https://cloud.google.`com/stackdriver/docs/deprecations/mql`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_time_series_query_execute()` to send, or `monitoring_projects_time_series_query` for simplest API.

pub fn monitoring_projects_time_series_query_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/timeSeries:query",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/timeSeries:query
/// Queries time series by using Monitoring Query Language (MQL). We recommend using PromQL instead of MQL. For more information about the status of MQL, see the MQL deprecation notice (<https://cloud.google.`com/stackdriver/docs/deprecations/mql`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_time_series_query_execute()` or `monitoring_projects_time_series_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_time_series_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryTimeSeriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/timeSeries:query
/// Queries time series by using Monitoring Query Language (MQL). We recommend using PromQL instead of MQL. For more information about the status of MQL, see the MQL deprecation notice (<https://cloud.google.`com/stackdriver/docs/deprecations/mql`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_time_series_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_time_series_query_task()`.
/// For the simplest API, use `monitoring_projects_time_series_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_time_series_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_time_series_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_time_series_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_time_series_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsTimeSeriesQueryArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/timeSeries:query
/// Queries time series by using Monitoring Query Language (MQL). We recommend using PromQL instead of MQL. For more information about the status of MQL, see the MQL deprecation notice (<https://cloud.google.`com/stackdriver/docs/deprecations/mql`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_time_series_query_builder()` + `monitoring_projects_time_series_query_execute()`.
/// For task-level control, use `monitoring_projects_time_series_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_query(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsTimeSeriesQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_time_series_query_builder(client, &args.name)?;
    monitoring_projects_time_series_query_execute(builder)
}

/// POST v3/projects/{projectsId}/uptimeCheckConfigs
/// Creates a new Uptime check configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_uptime_check_configs_create_execute()` to send, or `monitoring_projects_uptime_check_configs_create` for simplest API.

pub fn monitoring_projects_uptime_check_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/uptimeCheckConfigs",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/uptimeCheckConfigs
/// Creates a new Uptime check configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_uptime_check_configs_create_execute()` or `monitoring_projects_uptime_check_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_uptime_check_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UptimeCheckConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UptimeCheckConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/uptimeCheckConfigs
/// Creates a new Uptime check configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_uptime_check_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_uptime_check_configs_create_task()`.
/// For the simplest API, use `monitoring_projects_uptime_check_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_uptime_check_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_uptime_check_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UptimeCheckConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_uptime_check_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_uptime_check_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsUptimeCheckConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/uptimeCheckConfigs
/// Creates a new Uptime check configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_uptime_check_configs_create_builder()` + `monitoring_projects_uptime_check_configs_create_execute()`.
/// For task-level control, use `monitoring_projects_uptime_check_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_create(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsUptimeCheckConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UptimeCheckConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_uptime_check_configs_create_builder(client, &args.parent)?;
    monitoring_projects_uptime_check_configs_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/uptimeCheckConfigs/{uptimeCheckConfigsId}
/// Deletes an Uptime check configuration. Note that this method will fail if the Uptime check configuration is referenced by an alert policy or other dependent configs that would be rendered invalid by the deletion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_uptime_check_configs_delete_execute()` to send, or `monitoring_projects_uptime_check_configs_delete` for simplest API.

pub fn monitoring_projects_uptime_check_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/uptimeCheckConfigs/{uptimeCheckConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/uptimeCheckConfigs/{uptimeCheckConfigsId}
/// Deletes an Uptime check configuration. Note that this method will fail if the Uptime check configuration is referenced by an alert policy or other dependent configs that would be rendered invalid by the deletion.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_uptime_check_configs_delete_execute()` or `monitoring_projects_uptime_check_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_uptime_check_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/uptimeCheckConfigs/{uptimeCheckConfigsId}
/// Deletes an Uptime check configuration. Note that this method will fail if the Uptime check configuration is referenced by an alert policy or other dependent configs that would be rendered invalid by the deletion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_uptime_check_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_uptime_check_configs_delete_task()`.
/// For the simplest API, use `monitoring_projects_uptime_check_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_uptime_check_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_uptime_check_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_projects_uptime_check_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_uptime_check_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsUptimeCheckConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/uptimeCheckConfigs/{uptimeCheckConfigsId}
/// Deletes an Uptime check configuration. Note that this method will fail if the Uptime check configuration is referenced by an alert policy or other dependent configs that would be rendered invalid by the deletion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_uptime_check_configs_delete_builder()` + `monitoring_projects_uptime_check_configs_delete_execute()`.
/// For task-level control, use `monitoring_projects_uptime_check_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_delete(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsUptimeCheckConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_uptime_check_configs_delete_builder(client, &args.name)?;
    monitoring_projects_uptime_check_configs_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/uptimeCheckConfigs/{uptimeCheckConfigsId}
/// Gets a single Uptime check configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_uptime_check_configs_get_execute()` to send, or `monitoring_projects_uptime_check_configs_get` for simplest API.

pub fn monitoring_projects_uptime_check_configs_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/uptimeCheckConfigs/{uptimeCheckConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/uptimeCheckConfigs/{uptimeCheckConfigsId}
/// Gets a single Uptime check configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_uptime_check_configs_get_execute()` or `monitoring_projects_uptime_check_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_uptime_check_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UptimeCheckConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UptimeCheckConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/uptimeCheckConfigs/{uptimeCheckConfigsId}
/// Gets a single Uptime check configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_uptime_check_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_uptime_check_configs_get_task()`.
/// For the simplest API, use `monitoring_projects_uptime_check_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_uptime_check_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_uptime_check_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UptimeCheckConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_uptime_check_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_uptime_check_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsUptimeCheckConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/uptimeCheckConfigs/{uptimeCheckConfigsId}
/// Gets a single Uptime check configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_uptime_check_configs_get_builder()` + `monitoring_projects_uptime_check_configs_get_execute()`.
/// For task-level control, use `monitoring_projects_uptime_check_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_get(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsUptimeCheckConfigsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UptimeCheckConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_uptime_check_configs_get_builder(client, &args.name)?;
    monitoring_projects_uptime_check_configs_get_execute(builder)
}

/// GET v3/projects/{projectsId}/uptimeCheckConfigs
/// Lists the existing valid Uptime check configurations for the project (leaving out any invalid configurations).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_uptime_check_configs_list_execute()` to send, or `monitoring_projects_uptime_check_configs_list` for simplest API.

pub fn monitoring_projects_uptime_check_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/uptimeCheckConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/uptimeCheckConfigs
/// Lists the existing valid Uptime check configurations for the project (leaving out any invalid configurations).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_uptime_check_configs_list_execute()` or `monitoring_projects_uptime_check_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_uptime_check_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUptimeCheckConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUptimeCheckConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/uptimeCheckConfigs
/// Lists the existing valid Uptime check configurations for the project (leaving out any invalid configurations).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_uptime_check_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_uptime_check_configs_list_task()`.
/// For the simplest API, use `monitoring_projects_uptime_check_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_uptime_check_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_uptime_check_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUptimeCheckConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_uptime_check_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_uptime_check_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsUptimeCheckConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v3/projects/{projectsId}/uptimeCheckConfigs
/// Lists the existing valid Uptime check configurations for the project (leaving out any invalid configurations).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_uptime_check_configs_list_builder()` + `monitoring_projects_uptime_check_configs_list_execute()`.
/// For task-level control, use `monitoring_projects_uptime_check_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_list(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsUptimeCheckConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUptimeCheckConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_uptime_check_configs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    monitoring_projects_uptime_check_configs_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/uptimeCheckConfigs/{uptimeCheckConfigsId}
/// Updates an Uptime check configuration. You can either replace the entire configuration with a new one or replace only certain fields in the current configuration by specifying the fields to be updated via `updateMask`. Returns the updated configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_uptime_check_configs_patch_execute()` to send, or `monitoring_projects_uptime_check_configs_patch` for simplest API.

pub fn monitoring_projects_uptime_check_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/projects/{}/uptimeCheckConfigs/{uptimeCheckConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/uptimeCheckConfigs/{uptimeCheckConfigsId}
/// Updates an Uptime check configuration. You can either replace the entire configuration with a new one or replace only certain fields in the current configuration by specifying the fields to be updated via `updateMask`. Returns the updated configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_projects_uptime_check_configs_patch_execute()` or `monitoring_projects_uptime_check_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_uptime_check_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UptimeCheckConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UptimeCheckConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/uptimeCheckConfigs/{uptimeCheckConfigsId}
/// Updates an Uptime check configuration. You can either replace the entire configuration with a new one or replace only certain fields in the current configuration by specifying the fields to be updated via `updateMask`. Returns the updated configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_projects_uptime_check_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_projects_uptime_check_configs_patch_task()`.
/// For the simplest API, use `monitoring_projects_uptime_check_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_projects_uptime_check_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_projects_uptime_check_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UptimeCheckConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_projects_uptime_check_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_projects_uptime_check_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsUptimeCheckConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v3/projects/{projectsId}/uptimeCheckConfigs/{uptimeCheckConfigsId}
/// Updates an Uptime check configuration. You can either replace the entire configuration with a new one or replace only certain fields in the current configuration by specifying the fields to be updated via `updateMask`. Returns the updated configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_uptime_check_configs_patch_builder()` + `monitoring_projects_uptime_check_configs_patch_execute()`.
/// For task-level control, use `monitoring_projects_uptime_check_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_patch(
    client: &SimpleHttpClient,
    args: &MonitoringProjectsUptimeCheckConfigsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UptimeCheckConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_uptime_check_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    monitoring_projects_uptime_check_configs_patch_execute(builder)
}

/// POST v3/{v3Id}/{v3Id1}/services
/// Create a Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_create_execute()` to send, or `monitoring_services_create` for simplest API.

pub fn monitoring_services_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    serviceId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{}/{v3Id1}/services",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = serviceId.as_ref() {
        query_parts.push(format!("serviceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/{v3Id}/{v3Id1}/services
/// Create a Service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_services_create_execute()` or `monitoring_services_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Service>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Service = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/{v3Id}/{v3Id1}/services
/// Create a Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_services_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_services_create_task()`.
/// For the simplest API, use `monitoring_services_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_services_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_services_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_services_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: serviceId
    pub serviceId: Option<Option<String>>,
}

/// POST v3/{v3Id}/{v3Id1}/services
/// Create a Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_create_builder()` + `monitoring_services_create_execute()`.
/// For task-level control, use `monitoring_services_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_create(
    client: &SimpleHttpClient,
    args: &MonitoringServicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_services_create_builder(client, &args.parent, &args.serviceId)?;
    monitoring_services_create_execute(builder)
}

/// DELETE v3/{v3Id}/{v3Id1}/services/{servicesId}
/// Soft delete this Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_delete_execute()` to send, or `monitoring_services_delete` for simplest API.

pub fn monitoring_services_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{}/{v3Id1}/services/{servicesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/{v3Id}/{v3Id1}/services/{servicesId}
/// Soft delete this Service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_services_delete_execute()` or `monitoring_services_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/{v3Id}/{v3Id1}/services/{servicesId}
/// Soft delete this Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_services_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_services_delete_task()`.
/// For the simplest API, use `monitoring_services_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_services_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_services_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/{v3Id}/{v3Id1}/services/{servicesId}
/// Soft delete this Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_delete_builder()` + `monitoring_services_delete_execute()`.
/// For task-level control, use `monitoring_services_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_delete(
    client: &SimpleHttpClient,
    args: &MonitoringServicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_services_delete_builder(client, &args.name)?;
    monitoring_services_delete_execute(builder)
}

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}
/// Get the named Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_get_execute()` to send, or `monitoring_services_get` for simplest API.

pub fn monitoring_services_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{}/{v3Id1}/services/{servicesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}
/// Get the named Service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_services_get_execute()` or `monitoring_services_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Service>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Service = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}
/// Get the named Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_services_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_services_get_task()`.
/// For the simplest API, use `monitoring_services_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_services_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_services_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}
/// Get the named Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_get_builder()` + `monitoring_services_get_execute()`.
/// For task-level control, use `monitoring_services_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_get(
    client: &SimpleHttpClient,
    args: &MonitoringServicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_services_get_builder(client, &args.name)?;
    monitoring_services_get_execute(builder)
}

/// GET v3/{v3Id}/{v3Id1}/services
/// List Services for this Metrics Scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_list_execute()` to send, or `monitoring_services_list` for simplest API.

pub fn monitoring_services_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{}/{v3Id1}/services",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/{v3Id}/{v3Id1}/services
/// List Services for this Metrics Scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_services_list_execute()` or `monitoring_services_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListServicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/{v3Id}/{v3Id1}/services
/// List Services for this Metrics Scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_services_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_services_list_task()`.
/// For the simplest API, use `monitoring_services_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_services_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_services_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v3/{v3Id}/{v3Id1}/services
/// List Services for this Metrics Scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_list_builder()` + `monitoring_services_list_execute()`.
/// For task-level control, use `monitoring_services_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_list(
    client: &SimpleHttpClient,
    args: &MonitoringServicesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_services_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    monitoring_services_list_execute(builder)
}

/// PATCH v3/{v3Id}/{v3Id1}/services/{servicesId}
/// Update this Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_patch_execute()` to send, or `monitoring_services_patch` for simplest API.

pub fn monitoring_services_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{}/{v3Id1}/services/{servicesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/{v3Id}/{v3Id1}/services/{servicesId}
/// Update this Service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_services_patch_execute()` or `monitoring_services_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Service>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Service = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/{v3Id}/{v3Id1}/services/{servicesId}
/// Update this Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_services_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_services_patch_task()`.
/// For the simplest API, use `monitoring_services_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_services_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_services_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v3/{v3Id}/{v3Id1}/services/{servicesId}
/// Update this Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_patch_builder()` + `monitoring_services_patch_execute()`.
/// For task-level control, use `monitoring_services_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_patch(
    client: &SimpleHttpClient,
    args: &MonitoringServicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_services_patch_builder(client, &args.name, &args.updateMask)?;
    monitoring_services_patch_execute(builder)
}

/// POST v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives
/// Create a ServiceLevelObjective for the given Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_service_level_objectives_create_execute()` to send, or `monitoring_services_service_level_objectives_create` for simplest API.

pub fn monitoring_services_service_level_objectives_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    serviceLevelObjectiveId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{}/{v3Id1}/services/{servicesId}/serviceLevelObjectives",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = serviceLevelObjectiveId.as_ref() {
        query_parts.push(format!("serviceLevelObjectiveId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives
/// Create a ServiceLevelObjective for the given Service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_services_service_level_objectives_create_execute()` or `monitoring_services_service_level_objectives_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_service_level_objectives_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceLevelObjective>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceLevelObjective = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives
/// Create a ServiceLevelObjective for the given Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_services_service_level_objectives_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_services_service_level_objectives_create_task()`.
/// For the simplest API, use `monitoring_services_service_level_objectives_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_service_level_objectives_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_services_service_level_objectives_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceLevelObjective>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_services_service_level_objectives_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_services_service_level_objectives_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesServiceLevelObjectivesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: serviceLevelObjectiveId
    pub serviceLevelObjectiveId: Option<Option<String>>,
}

/// POST v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives
/// Create a ServiceLevelObjective for the given Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_service_level_objectives_create_builder()` + `monitoring_services_service_level_objectives_create_execute()`.
/// For task-level control, use `monitoring_services_service_level_objectives_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_create(
    client: &SimpleHttpClient,
    args: &MonitoringServicesServiceLevelObjectivesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceLevelObjective>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_services_service_level_objectives_create_builder(
        client,
        &args.parent,
        &args.serviceLevelObjectiveId,
    )?;
    monitoring_services_service_level_objectives_create_execute(builder)
}

/// DELETE v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
/// Delete the given ServiceLevelObjective.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_service_level_objectives_delete_execute()` to send, or `monitoring_services_service_level_objectives_delete` for simplest API.

pub fn monitoring_services_service_level_objectives_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
/// Delete the given ServiceLevelObjective.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_services_service_level_objectives_delete_execute()` or `monitoring_services_service_level_objectives_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_service_level_objectives_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
/// Delete the given ServiceLevelObjective.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_services_service_level_objectives_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_services_service_level_objectives_delete_task()`.
/// For the simplest API, use `monitoring_services_service_level_objectives_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_service_level_objectives_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_services_service_level_objectives_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = monitoring_services_service_level_objectives_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_services_service_level_objectives_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesServiceLevelObjectivesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
/// Delete the given ServiceLevelObjective.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_service_level_objectives_delete_builder()` + `monitoring_services_service_level_objectives_delete_execute()`.
/// For task-level control, use `monitoring_services_service_level_objectives_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_delete(
    client: &SimpleHttpClient,
    args: &MonitoringServicesServiceLevelObjectivesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_services_service_level_objectives_delete_builder(client, &args.name)?;
    monitoring_services_service_level_objectives_delete_execute(builder)
}

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
/// Get a ServiceLevelObjective by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_service_level_objectives_get_execute()` to send, or `monitoring_services_service_level_objectives_get` for simplest API.

pub fn monitoring_services_service_level_objectives_get_builder(
    client: &SimpleHttpClient,
    name: &String,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
/// Get a ServiceLevelObjective by name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_services_service_level_objectives_get_execute()` or `monitoring_services_service_level_objectives_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_service_level_objectives_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceLevelObjective>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceLevelObjective = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
/// Get a ServiceLevelObjective by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_services_service_level_objectives_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_services_service_level_objectives_get_task()`.
/// For the simplest API, use `monitoring_services_service_level_objectives_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_service_level_objectives_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_services_service_level_objectives_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceLevelObjective>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_services_service_level_objectives_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_services_service_level_objectives_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesServiceLevelObjectivesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
/// Get a ServiceLevelObjective by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_service_level_objectives_get_builder()` + `monitoring_services_service_level_objectives_get_execute()`.
/// For task-level control, use `monitoring_services_service_level_objectives_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_get(
    client: &SimpleHttpClient,
    args: &MonitoringServicesServiceLevelObjectivesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceLevelObjective>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        monitoring_services_service_level_objectives_get_builder(client, &args.name, &args.view)?;
    monitoring_services_service_level_objectives_get_execute(builder)
}

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives
/// List the ServiceLevelObjectives for the given Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_service_level_objectives_list_execute()` to send, or `monitoring_services_service_level_objectives_list` for simplest API.

pub fn monitoring_services_service_level_objectives_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{}/{v3Id1}/services/{servicesId}/serviceLevelObjectives",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives
/// List the ServiceLevelObjectives for the given Service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_services_service_level_objectives_list_execute()` or `monitoring_services_service_level_objectives_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_service_level_objectives_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListServiceLevelObjectivesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServiceLevelObjectivesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives
/// List the ServiceLevelObjectives for the given Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_services_service_level_objectives_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_services_service_level_objectives_list_task()`.
/// For the simplest API, use `monitoring_services_service_level_objectives_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_service_level_objectives_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_services_service_level_objectives_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServiceLevelObjectivesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_services_service_level_objectives_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_services_service_level_objectives_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesServiceLevelObjectivesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives
/// List the ServiceLevelObjectives for the given Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_service_level_objectives_list_builder()` + `monitoring_services_service_level_objectives_list_execute()`.
/// For task-level control, use `monitoring_services_service_level_objectives_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_list(
    client: &SimpleHttpClient,
    args: &MonitoringServicesServiceLevelObjectivesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServiceLevelObjectivesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_services_service_level_objectives_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    monitoring_services_service_level_objectives_list_execute(builder)
}

/// PATCH v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
/// Update the given ServiceLevelObjective.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_service_level_objectives_patch_execute()` to send, or `monitoring_services_service_level_objectives_patch` for simplest API.

pub fn monitoring_services_service_level_objectives_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
/// Update the given ServiceLevelObjective.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_services_service_level_objectives_patch_execute()` or `monitoring_services_service_level_objectives_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_service_level_objectives_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceLevelObjective>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceLevelObjective = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
/// Update the given ServiceLevelObjective.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_services_service_level_objectives_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_services_service_level_objectives_patch_task()`.
/// For the simplest API, use `monitoring_services_service_level_objectives_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_services_service_level_objectives_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_services_service_level_objectives_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceLevelObjective>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_services_service_level_objectives_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_services_service_level_objectives_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesServiceLevelObjectivesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
/// Update the given ServiceLevelObjective.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_service_level_objectives_patch_builder()` + `monitoring_services_service_level_objectives_patch_execute()`.
/// For task-level control, use `monitoring_services_service_level_objectives_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_patch(
    client: &SimpleHttpClient,
    args: &MonitoringServicesServiceLevelObjectivesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceLevelObjective>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_services_service_level_objectives_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    monitoring_services_service_level_objectives_patch_execute(builder)
}

/// GET v3/uptimeCheckIps
/// Returns the list of IP addresses that checkers run from.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_uptime_check_ips_list_execute()` to send, or `monitoring_uptime_check_ips_list` for simplest API.

pub fn monitoring_uptime_check_ips_list_builder(
    client: &SimpleHttpClient,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://monitoring.googleapis.com/v3/uptimeCheckIps",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/uptimeCheckIps
/// Returns the list of IP addresses that checkers run from.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `monitoring_uptime_check_ips_list_execute()` or `monitoring_uptime_check_ips_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_uptime_check_ips_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_uptime_check_ips_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUptimeCheckIpsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUptimeCheckIpsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/uptimeCheckIps
/// Returns the list of IP addresses that checkers run from.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `monitoring_uptime_check_ips_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `monitoring_uptime_check_ips_list_task()`.
/// For the simplest API, use `monitoring_uptime_check_ips_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `monitoring_uptime_check_ips_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn monitoring_uptime_check_ips_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUptimeCheckIpsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = monitoring_uptime_check_ips_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`monitoring_uptime_check_ips_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringUptimeCheckIpsListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v3/uptimeCheckIps
/// Returns the list of IP addresses that checkers run from.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_uptime_check_ips_list_builder()` + `monitoring_uptime_check_ips_list_execute()`.
/// For task-level control, use `monitoring_uptime_check_ips_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_uptime_check_ips_list(
    client: &SimpleHttpClient,
    args: &MonitoringUptimeCheckIpsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUptimeCheckIpsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        monitoring_uptime_check_ips_list_builder(client, &args.pageSize, &args.pageToken)?;
    monitoring_uptime_check_ips_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for ListTimeSeriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListTimeSeriesResponse with MonitoringFoldersTimeSeriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringFoldersTimeSeriesListArgs> for ListTimeSeriesResponse {
    fn generate_resource_id(&self, input: &MonitoringFoldersTimeSeriesListArgs) -> String {
        format!("gcp::monitoring::ListTimeSeriesResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListTimeSeriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListTimeSeriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListTimeSeriesResponse with MonitoringOrganizationsTimeSeriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringOrganizationsTimeSeriesListArgs> for ListTimeSeriesResponse {
    fn generate_resource_id(&self, input: &MonitoringOrganizationsTimeSeriesListArgs) -> String {
        format!("gcp::monitoring::ListTimeSeriesResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListTimeSeriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AlertPolicy
// =============================================================================

/// ResourceIdentifier implementation for AlertPolicy with MonitoringProjectsAlertPoliciesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsAlertPoliciesCreateArgs> for AlertPolicy {
    fn generate_resource_id(&self, input: &MonitoringProjectsAlertPoliciesCreateArgs) -> String {
        format!("gcp::monitoring::AlertPolicy/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::AlertPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with MonitoringProjectsAlertPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsAlertPoliciesDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &MonitoringProjectsAlertPoliciesDeleteArgs) -> String {
        format!("gcp::monitoring::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AlertPolicy
// =============================================================================

/// ResourceIdentifier implementation for AlertPolicy with MonitoringProjectsAlertPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsAlertPoliciesGetArgs> for AlertPolicy {
    fn generate_resource_id(&self, input: &MonitoringProjectsAlertPoliciesGetArgs) -> String {
        format!("gcp::monitoring::AlertPolicy/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::AlertPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAlertPoliciesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAlertPoliciesResponse with MonitoringProjectsAlertPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsAlertPoliciesListArgs> for ListAlertPoliciesResponse {
    fn generate_resource_id(&self, input: &MonitoringProjectsAlertPoliciesListArgs) -> String {
        format!("gcp::monitoring::ListAlertPoliciesResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListAlertPoliciesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AlertPolicy
// =============================================================================

/// ResourceIdentifier implementation for AlertPolicy with MonitoringProjectsAlertPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsAlertPoliciesPatchArgs> for AlertPolicy {
    fn generate_resource_id(&self, input: &MonitoringProjectsAlertPoliciesPatchArgs) -> String {
        format!("gcp::monitoring::AlertPolicy/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::AlertPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Alert
// =============================================================================

/// ResourceIdentifier implementation for Alert with MonitoringProjectsAlertsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsAlertsGetArgs> for Alert {
    fn generate_resource_id(&self, input: &MonitoringProjectsAlertsGetArgs) -> String {
        format!("gcp::monitoring::Alert/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Alert"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAlertsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAlertsResponse with MonitoringProjectsAlertsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsAlertsListArgs> for ListAlertsResponse {
    fn generate_resource_id(&self, input: &MonitoringProjectsAlertsListArgs) -> String {
        format!("gcp::monitoring::ListAlertsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListAlertsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreateCollectdTimeSeriesResponse
// =============================================================================

/// ResourceIdentifier implementation for CreateCollectdTimeSeriesResponse with MonitoringProjectsCollectdTimeSeriesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsCollectdTimeSeriesCreateArgs>
    for CreateCollectdTimeSeriesResponse
{
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsCollectdTimeSeriesCreateArgs,
    ) -> String {
        format!(
            "gcp::monitoring::CreateCollectdTimeSeriesResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::CreateCollectdTimeSeriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Group
// =============================================================================

/// ResourceIdentifier implementation for Group with MonitoringProjectsGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsGroupsCreateArgs> for Group {
    fn generate_resource_id(&self, input: &MonitoringProjectsGroupsCreateArgs) -> String {
        format!("gcp::monitoring::Group/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Group"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with MonitoringProjectsGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsGroupsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &MonitoringProjectsGroupsDeleteArgs) -> String {
        format!("gcp::monitoring::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Group
// =============================================================================

/// ResourceIdentifier implementation for Group with MonitoringProjectsGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsGroupsGetArgs> for Group {
    fn generate_resource_id(&self, input: &MonitoringProjectsGroupsGetArgs) -> String {
        format!("gcp::monitoring::Group/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Group"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListGroupsResponse with MonitoringProjectsGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsGroupsListArgs> for ListGroupsResponse {
    fn generate_resource_id(&self, input: &MonitoringProjectsGroupsListArgs) -> String {
        format!("gcp::monitoring::ListGroupsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Group
// =============================================================================

/// ResourceIdentifier implementation for Group with MonitoringProjectsGroupsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsGroupsUpdateArgs> for Group {
    fn generate_resource_id(&self, input: &MonitoringProjectsGroupsUpdateArgs) -> String {
        format!("gcp::monitoring::Group/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Group"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListGroupMembersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListGroupMembersResponse with MonitoringProjectsGroupsMembersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsGroupsMembersListArgs> for ListGroupMembersResponse {
    fn generate_resource_id(&self, input: &MonitoringProjectsGroupsMembersListArgs) -> String {
        format!("gcp::monitoring::ListGroupMembersResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListGroupMembersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MetricDescriptor
// =============================================================================

/// ResourceIdentifier implementation for MetricDescriptor with MonitoringProjectsMetricDescriptorsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsMetricDescriptorsCreateArgs> for MetricDescriptor {
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsMetricDescriptorsCreateArgs,
    ) -> String {
        format!("gcp::monitoring::MetricDescriptor/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::MetricDescriptor"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with MonitoringProjectsMetricDescriptorsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsMetricDescriptorsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsMetricDescriptorsDeleteArgs,
    ) -> String {
        format!("gcp::monitoring::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MetricDescriptor
// =============================================================================

/// ResourceIdentifier implementation for MetricDescriptor with MonitoringProjectsMetricDescriptorsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsMetricDescriptorsGetArgs> for MetricDescriptor {
    fn generate_resource_id(&self, input: &MonitoringProjectsMetricDescriptorsGetArgs) -> String {
        format!("gcp::monitoring::MetricDescriptor/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::MetricDescriptor"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMetricDescriptorsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMetricDescriptorsResponse with MonitoringProjectsMetricDescriptorsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsMetricDescriptorsListArgs>
    for ListMetricDescriptorsResponse
{
    fn generate_resource_id(&self, input: &MonitoringProjectsMetricDescriptorsListArgs) -> String {
        format!(
            "gcp::monitoring::ListMetricDescriptorsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListMetricDescriptorsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MonitoredResourceDescriptor
// =============================================================================

/// ResourceIdentifier implementation for MonitoredResourceDescriptor with MonitoringProjectsMonitoredResourceDescriptorsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsMonitoredResourceDescriptorsGetArgs>
    for MonitoredResourceDescriptor
{
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsMonitoredResourceDescriptorsGetArgs,
    ) -> String {
        format!(
            "gcp::monitoring::MonitoredResourceDescriptor/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::MonitoredResourceDescriptor"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMonitoredResourceDescriptorsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMonitoredResourceDescriptorsResponse with MonitoringProjectsMonitoredResourceDescriptorsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsMonitoredResourceDescriptorsListArgs>
    for ListMonitoredResourceDescriptorsResponse
{
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsMonitoredResourceDescriptorsListArgs,
    ) -> String {
        format!(
            "gcp::monitoring::ListMonitoredResourceDescriptorsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListMonitoredResourceDescriptorsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationChannelDescriptor
// =============================================================================

/// ResourceIdentifier implementation for NotificationChannelDescriptor with MonitoringProjectsNotificationChannelDescriptorsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsNotificationChannelDescriptorsGetArgs>
    for NotificationChannelDescriptor
{
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsNotificationChannelDescriptorsGetArgs,
    ) -> String {
        format!(
            "gcp::monitoring::NotificationChannelDescriptor/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::NotificationChannelDescriptor"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListNotificationChannelDescriptorsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListNotificationChannelDescriptorsResponse with MonitoringProjectsNotificationChannelDescriptorsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsNotificationChannelDescriptorsListArgs>
    for ListNotificationChannelDescriptorsResponse
{
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsNotificationChannelDescriptorsListArgs,
    ) -> String {
        format!(
            "gcp::monitoring::ListNotificationChannelDescriptorsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListNotificationChannelDescriptorsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationChannel
// =============================================================================

/// ResourceIdentifier implementation for NotificationChannel with MonitoringProjectsNotificationChannelsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsNotificationChannelsCreateArgs> for NotificationChannel {
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsNotificationChannelsCreateArgs,
    ) -> String {
        format!("gcp::monitoring::NotificationChannel/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::NotificationChannel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with MonitoringProjectsNotificationChannelsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsNotificationChannelsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsNotificationChannelsDeleteArgs,
    ) -> String {
        format!("gcp::monitoring::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationChannel
// =============================================================================

/// ResourceIdentifier implementation for NotificationChannel with MonitoringProjectsNotificationChannelsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsNotificationChannelsGetArgs> for NotificationChannel {
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsNotificationChannelsGetArgs,
    ) -> String {
        format!("gcp::monitoring::NotificationChannel/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::NotificationChannel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetNotificationChannelVerificationCodeResponse
// =============================================================================

/// ResourceIdentifier implementation for GetNotificationChannelVerificationCodeResponse with MonitoringProjectsNotificationChannelsGetVerificationCodeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsNotificationChannelsGetVerificationCodeArgs>
    for GetNotificationChannelVerificationCodeResponse
{
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsNotificationChannelsGetVerificationCodeArgs,
    ) -> String {
        format!(
            "gcp::monitoring::GetNotificationChannelVerificationCodeResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::GetNotificationChannelVerificationCodeResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListNotificationChannelsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListNotificationChannelsResponse with MonitoringProjectsNotificationChannelsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsNotificationChannelsListArgs>
    for ListNotificationChannelsResponse
{
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsNotificationChannelsListArgs,
    ) -> String {
        format!(
            "gcp::monitoring::ListNotificationChannelsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListNotificationChannelsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationChannel
// =============================================================================

/// ResourceIdentifier implementation for NotificationChannel with MonitoringProjectsNotificationChannelsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsNotificationChannelsPatchArgs> for NotificationChannel {
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsNotificationChannelsPatchArgs,
    ) -> String {
        format!("gcp::monitoring::NotificationChannel/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::NotificationChannel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with MonitoringProjectsNotificationChannelsSendVerificationCodeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsNotificationChannelsSendVerificationCodeArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsNotificationChannelsSendVerificationCodeArgs,
    ) -> String {
        format!("gcp::monitoring::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NotificationChannel
// =============================================================================

/// ResourceIdentifier implementation for NotificationChannel with MonitoringProjectsNotificationChannelsVerifyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsNotificationChannelsVerifyArgs> for NotificationChannel {
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsNotificationChannelsVerifyArgs,
    ) -> String {
        format!("gcp::monitoring::NotificationChannel/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::NotificationChannel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Snooze
// =============================================================================

/// ResourceIdentifier implementation for Snooze with MonitoringProjectsSnoozesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsSnoozesCreateArgs> for Snooze {
    fn generate_resource_id(&self, input: &MonitoringProjectsSnoozesCreateArgs) -> String {
        format!("gcp::monitoring::Snooze/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Snooze"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Snooze
// =============================================================================

/// ResourceIdentifier implementation for Snooze with MonitoringProjectsSnoozesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsSnoozesGetArgs> for Snooze {
    fn generate_resource_id(&self, input: &MonitoringProjectsSnoozesGetArgs) -> String {
        format!("gcp::monitoring::Snooze/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Snooze"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSnoozesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSnoozesResponse with MonitoringProjectsSnoozesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsSnoozesListArgs> for ListSnoozesResponse {
    fn generate_resource_id(&self, input: &MonitoringProjectsSnoozesListArgs) -> String {
        format!("gcp::monitoring::ListSnoozesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListSnoozesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Snooze
// =============================================================================

/// ResourceIdentifier implementation for Snooze with MonitoringProjectsSnoozesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsSnoozesPatchArgs> for Snooze {
    fn generate_resource_id(&self, input: &MonitoringProjectsSnoozesPatchArgs) -> String {
        format!("gcp::monitoring::Snooze/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Snooze"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with MonitoringProjectsTimeSeriesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsTimeSeriesCreateArgs> for Empty {
    fn generate_resource_id(&self, input: &MonitoringProjectsTimeSeriesCreateArgs) -> String {
        format!("gcp::monitoring::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with MonitoringProjectsTimeSeriesCreateServiceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsTimeSeriesCreateServiceArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsTimeSeriesCreateServiceArgs,
    ) -> String {
        format!("gcp::monitoring::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListTimeSeriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListTimeSeriesResponse with MonitoringProjectsTimeSeriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsTimeSeriesListArgs> for ListTimeSeriesResponse {
    fn generate_resource_id(&self, input: &MonitoringProjectsTimeSeriesListArgs) -> String {
        format!("gcp::monitoring::ListTimeSeriesResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListTimeSeriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryTimeSeriesResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryTimeSeriesResponse with MonitoringProjectsTimeSeriesQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsTimeSeriesQueryArgs> for QueryTimeSeriesResponse {
    fn generate_resource_id(&self, input: &MonitoringProjectsTimeSeriesQueryArgs) -> String {
        format!("gcp::monitoring::QueryTimeSeriesResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::QueryTimeSeriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UptimeCheckConfig
// =============================================================================

/// ResourceIdentifier implementation for UptimeCheckConfig with MonitoringProjectsUptimeCheckConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsUptimeCheckConfigsCreateArgs> for UptimeCheckConfig {
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsUptimeCheckConfigsCreateArgs,
    ) -> String {
        format!("gcp::monitoring::UptimeCheckConfig/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::UptimeCheckConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with MonitoringProjectsUptimeCheckConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsUptimeCheckConfigsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsUptimeCheckConfigsDeleteArgs,
    ) -> String {
        format!("gcp::monitoring::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UptimeCheckConfig
// =============================================================================

/// ResourceIdentifier implementation for UptimeCheckConfig with MonitoringProjectsUptimeCheckConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsUptimeCheckConfigsGetArgs> for UptimeCheckConfig {
    fn generate_resource_id(&self, input: &MonitoringProjectsUptimeCheckConfigsGetArgs) -> String {
        format!("gcp::monitoring::UptimeCheckConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::UptimeCheckConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListUptimeCheckConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListUptimeCheckConfigsResponse with MonitoringProjectsUptimeCheckConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsUptimeCheckConfigsListArgs>
    for ListUptimeCheckConfigsResponse
{
    fn generate_resource_id(&self, input: &MonitoringProjectsUptimeCheckConfigsListArgs) -> String {
        format!(
            "gcp::monitoring::ListUptimeCheckConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListUptimeCheckConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UptimeCheckConfig
// =============================================================================

/// ResourceIdentifier implementation for UptimeCheckConfig with MonitoringProjectsUptimeCheckConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringProjectsUptimeCheckConfigsPatchArgs> for UptimeCheckConfig {
    fn generate_resource_id(
        &self,
        input: &MonitoringProjectsUptimeCheckConfigsPatchArgs,
    ) -> String {
        format!("gcp::monitoring::UptimeCheckConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::UptimeCheckConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Service
// =============================================================================

/// ResourceIdentifier implementation for Service with MonitoringServicesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringServicesCreateArgs> for Service {
    fn generate_resource_id(&self, input: &MonitoringServicesCreateArgs) -> String {
        format!("gcp::monitoring::Service/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Service"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with MonitoringServicesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringServicesDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &MonitoringServicesDeleteArgs) -> String {
        format!("gcp::monitoring::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Service
// =============================================================================

/// ResourceIdentifier implementation for Service with MonitoringServicesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringServicesGetArgs> for Service {
    fn generate_resource_id(&self, input: &MonitoringServicesGetArgs) -> String {
        format!("gcp::monitoring::Service/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Service"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListServicesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListServicesResponse with MonitoringServicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringServicesListArgs> for ListServicesResponse {
    fn generate_resource_id(&self, input: &MonitoringServicesListArgs) -> String {
        format!("gcp::monitoring::ListServicesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListServicesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Service
// =============================================================================

/// ResourceIdentifier implementation for Service with MonitoringServicesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringServicesPatchArgs> for Service {
    fn generate_resource_id(&self, input: &MonitoringServicesPatchArgs) -> String {
        format!("gcp::monitoring::Service/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Service"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceLevelObjective
// =============================================================================

/// ResourceIdentifier implementation for ServiceLevelObjective with MonitoringServicesServiceLevelObjectivesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringServicesServiceLevelObjectivesCreateArgs>
    for ServiceLevelObjective
{
    fn generate_resource_id(
        &self,
        input: &MonitoringServicesServiceLevelObjectivesCreateArgs,
    ) -> String {
        format!("gcp::monitoring::ServiceLevelObjective/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ServiceLevelObjective"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with MonitoringServicesServiceLevelObjectivesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringServicesServiceLevelObjectivesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &MonitoringServicesServiceLevelObjectivesDeleteArgs,
    ) -> String {
        format!("gcp::monitoring::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceLevelObjective
// =============================================================================

/// ResourceIdentifier implementation for ServiceLevelObjective with MonitoringServicesServiceLevelObjectivesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringServicesServiceLevelObjectivesGetArgs> for ServiceLevelObjective {
    fn generate_resource_id(
        &self,
        input: &MonitoringServicesServiceLevelObjectivesGetArgs,
    ) -> String {
        format!("gcp::monitoring::ServiceLevelObjective/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ServiceLevelObjective"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListServiceLevelObjectivesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListServiceLevelObjectivesResponse with MonitoringServicesServiceLevelObjectivesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringServicesServiceLevelObjectivesListArgs>
    for ListServiceLevelObjectivesResponse
{
    fn generate_resource_id(
        &self,
        input: &MonitoringServicesServiceLevelObjectivesListArgs,
    ) -> String {
        format!(
            "gcp::monitoring::ListServiceLevelObjectivesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListServiceLevelObjectivesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceLevelObjective
// =============================================================================

/// ResourceIdentifier implementation for ServiceLevelObjective with MonitoringServicesServiceLevelObjectivesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringServicesServiceLevelObjectivesPatchArgs>
    for ServiceLevelObjective
{
    fn generate_resource_id(
        &self,
        input: &MonitoringServicesServiceLevelObjectivesPatchArgs,
    ) -> String {
        format!("gcp::monitoring::ServiceLevelObjective/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ServiceLevelObjective"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListUptimeCheckIpsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListUptimeCheckIpsResponse with MonitoringUptimeCheckIpsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MonitoringUptimeCheckIpsListArgs> for ListUptimeCheckIpsResponse {
    fn generate_resource_id(&self, input: &MonitoringUptimeCheckIpsListArgs) -> String {
        "gcp::monitoring::ListUptimeCheckIpsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::monitoring::ListUptimeCheckIpsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
