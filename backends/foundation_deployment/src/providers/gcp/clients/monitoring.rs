//! Auto-generated API clients for Google Cloud Platform - monitoring.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v3/{+name}/timeSeries
/// Lists time series that match a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_folders_time_series_list_execute()` to send, or `monitoring_folders_time_series_list` for simplest API.

pub fn monitoring_folders_time_series_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    aggregation_alignmentPeriod: Option<&str>,
    aggregation_crossSeriesReducer: Option<&str>,
    aggregation_groupByFields: Option<&str>,
    aggregation_perSeriesAligner: Option<&str>,
    filter: Option<&str>,
    interval_endTime: Option<&str>,
    interval_startTime: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    secondaryAggregation_alignmentPeriod: Option<&str>,
    secondaryAggregation_crossSeriesReducer: Option<&str>,
    secondaryAggregation_groupByFields: Option<&str>,
    secondaryAggregation_perSeriesAligner: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/timeSeries", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "aggregation_alignmentPeriod",
            aggregation_alignmentPeriod
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "aggregation_crossSeriesReducer",
            aggregation_crossSeriesReducer
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "aggregation_groupByFields",
            aggregation_groupByFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "aggregation_perSeriesAligner",
            aggregation_perSeriesAligner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "interval_endTime",
            interval_endTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "interval_startTime",
            interval_startTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "secondaryAggregation_alignmentPeriod",
            secondaryAggregation_alignmentPeriod
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "secondaryAggregation_crossSeriesReducer",
            secondaryAggregation_crossSeriesReducer
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "secondaryAggregation_groupByFields",
            secondaryAggregation_groupByFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "secondaryAggregation_perSeriesAligner",
            secondaryAggregation_perSeriesAligner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}/timeSeries
/// Lists time series that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_folders_time_series_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_folders_time_series_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_folders_time_series_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/timeSeries
/// Lists time series that match a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_folders_time_series_list_builder()` + `monitoring_folders_time_series_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_folders_time_series_list(
    client: &SimpleHttpClient,
    name: &str,
    aggregation_alignmentPeriod: Option<&str>,
    aggregation_crossSeriesReducer: Option<&str>,
    aggregation_groupByFields: Option<&str>,
    aggregation_perSeriesAligner: Option<&str>,
    filter: Option<&str>,
    interval_endTime: Option<&str>,
    interval_startTime: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    secondaryAggregation_alignmentPeriod: Option<&str>,
    secondaryAggregation_crossSeriesReducer: Option<&str>,
    secondaryAggregation_groupByFields: Option<&str>,
    secondaryAggregation_perSeriesAligner: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_folders_time_series_list_builder(
        client,
        name,
        aggregation_alignmentPeriod,
        aggregation_crossSeriesReducer,
        aggregation_groupByFields,
        aggregation_perSeriesAligner,
        filter,
        interval_endTime,
        interval_startTime,
        orderBy,
        pageSize,
        pageToken,
        secondaryAggregation_alignmentPeriod,
        secondaryAggregation_crossSeriesReducer,
        secondaryAggregation_groupByFields,
        secondaryAggregation_perSeriesAligner,
        view,
    )?;
    monitoring_folders_time_series_list_execute(builder)
}

/// GET v3/{+name}/timeSeries
/// Lists time series that match a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_organizations_time_series_list_execute()` to send, or `monitoring_organizations_time_series_list` for simplest API.

pub fn monitoring_organizations_time_series_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    aggregation_alignmentPeriod: Option<&str>,
    aggregation_crossSeriesReducer: Option<&str>,
    aggregation_groupByFields: Option<&str>,
    aggregation_perSeriesAligner: Option<&str>,
    filter: Option<&str>,
    interval_endTime: Option<&str>,
    interval_startTime: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    secondaryAggregation_alignmentPeriod: Option<&str>,
    secondaryAggregation_crossSeriesReducer: Option<&str>,
    secondaryAggregation_groupByFields: Option<&str>,
    secondaryAggregation_perSeriesAligner: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/timeSeries", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "aggregation_alignmentPeriod",
            aggregation_alignmentPeriod
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "aggregation_crossSeriesReducer",
            aggregation_crossSeriesReducer
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "aggregation_groupByFields",
            aggregation_groupByFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "aggregation_perSeriesAligner",
            aggregation_perSeriesAligner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "interval_endTime",
            interval_endTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "interval_startTime",
            interval_startTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "secondaryAggregation_alignmentPeriod",
            secondaryAggregation_alignmentPeriod
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "secondaryAggregation_crossSeriesReducer",
            secondaryAggregation_crossSeriesReducer
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "secondaryAggregation_groupByFields",
            secondaryAggregation_groupByFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "secondaryAggregation_perSeriesAligner",
            secondaryAggregation_perSeriesAligner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}/timeSeries
/// Lists time series that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_organizations_time_series_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_organizations_time_series_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_organizations_time_series_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/timeSeries
/// Lists time series that match a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_organizations_time_series_list_builder()` + `monitoring_organizations_time_series_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_organizations_time_series_list(
    client: &SimpleHttpClient,
    name: &str,
    aggregation_alignmentPeriod: Option<&str>,
    aggregation_crossSeriesReducer: Option<&str>,
    aggregation_groupByFields: Option<&str>,
    aggregation_perSeriesAligner: Option<&str>,
    filter: Option<&str>,
    interval_endTime: Option<&str>,
    interval_startTime: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    secondaryAggregation_alignmentPeriod: Option<&str>,
    secondaryAggregation_crossSeriesReducer: Option<&str>,
    secondaryAggregation_groupByFields: Option<&str>,
    secondaryAggregation_perSeriesAligner: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_organizations_time_series_list_builder(
        client,
        name,
        aggregation_alignmentPeriod,
        aggregation_crossSeriesReducer,
        aggregation_groupByFields,
        aggregation_perSeriesAligner,
        filter,
        interval_endTime,
        interval_startTime,
        orderBy,
        pageSize,
        pageToken,
        secondaryAggregation_alignmentPeriod,
        secondaryAggregation_crossSeriesReducer,
        secondaryAggregation_groupByFields,
        secondaryAggregation_perSeriesAligner,
        view,
    )?;
    monitoring_organizations_time_series_list_execute(builder)
}

/// GET v3/{+name}/alertPolicies
/// Creates a new alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alert_policies_create_execute()` to send, or `monitoring_projects_alert_policies_create` for simplest API.

pub fn monitoring_projects_alert_policies_create_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AlertPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/alertPolicies", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}/alertPolicies
/// Creates a new alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_alert_policies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_alert_policies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AlertPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/alertPolicies
/// Creates a new alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alert_policies_create_builder()` + `monitoring_projects_alert_policies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_create(
    client: &SimpleHttpClient,
    name: &str,
    body: &AlertPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alert_policies_create_builder(client, name, body)?;
    monitoring_projects_alert_policies_create_execute(builder)
}

/// GET v3/{+name}
/// Deletes an alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alert_policies_delete_execute()` to send, or `monitoring_projects_alert_policies_delete` for simplest API.

pub fn monitoring_projects_alert_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Deletes an alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_alert_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_alert_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Deletes an alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alert_policies_delete_builder()` + `monitoring_projects_alert_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alert_policies_delete_builder(client, name)?;
    monitoring_projects_alert_policies_delete_execute(builder)
}

/// GET v3/{+name}
/// Gets a single alerting policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alert_policies_get_execute()` to send, or `monitoring_projects_alert_policies_get` for simplest API.

pub fn monitoring_projects_alert_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single alerting policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_alert_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_alert_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AlertPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single alerting policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alert_policies_get_builder()` + `monitoring_projects_alert_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alert_policies_get_builder(client, name)?;
    monitoring_projects_alert_policies_get_execute(builder)
}

/// GET v3/{+name}/alertPolicies
/// Lists the existing alerting policies for the workspace.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alert_policies_list_execute()` to send, or `monitoring_projects_alert_policies_list` for simplest API.

pub fn monitoring_projects_alert_policies_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/alertPolicies", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}/alertPolicies
/// Lists the existing alerting policies for the workspace.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_alert_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_alert_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAlertPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/alertPolicies
/// Lists the existing alerting policies for the workspace.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alert_policies_list_builder()` + `monitoring_projects_alert_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alert_policies_list_builder(
        client, name, filter, orderBy, pageSize, pageToken,
    )?;
    monitoring_projects_alert_policies_list_execute(builder)
}

/// GET v3/{+name}
/// Updates an alerting policy. You can either replace the entire policy with a new one or replace only certain fields in the current alerting policy by specifying the fields to be updated via `updateMask`. Returns the updated alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alert_policies_patch_execute()` to send, or `monitoring_projects_alert_policies_patch` for simplest API.

pub fn monitoring_projects_alert_policies_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &AlertPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}
/// Updates an alerting policy. You can either replace the entire policy with a new one or replace only certain fields in the current alerting policy by specifying the fields to be updated via `updateMask`. Returns the updated alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_alert_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_alert_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AlertPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Updates an alerting policy. You can either replace the entire policy with a new one or replace only certain fields in the current alerting policy by specifying the fields to be updated via `updateMask`. Returns the updated alerting policy.Design your application to single-thread API calls that modify the state of alerting policies in a single project. This includes calls to CreateAlertPolicy, DeleteAlertPolicy and UpdateAlertPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alert_policies_patch_builder()` + `monitoring_projects_alert_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alert_policies_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &AlertPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alert_policies_patch_builder(client, name, updateMask, body)?;
    monitoring_projects_alert_policies_patch_execute(builder)
}

/// GET v3/{+name}
/// Gets a single alert.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alerts_get_execute()` to send, or `monitoring_projects_alerts_get` for simplest API.

pub fn monitoring_projects_alerts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single alert.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_alerts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_alerts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alerts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single alert.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alerts_get_builder()` + `monitoring_projects_alerts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alerts_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alerts_get_builder(client, name)?;
    monitoring_projects_alerts_get_execute(builder)
}

/// GET v3/{+parent}/alerts
/// Lists the existing alerts for the metrics scope of the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_alerts_list_execute()` to send, or `monitoring_projects_alerts_list` for simplest API.

pub fn monitoring_projects_alerts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+parent}/alerts", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+parent}/alerts
/// Lists the existing alerts for the metrics scope of the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_alerts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_alerts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alerts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAlertsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+parent}/alerts
/// Lists the existing alerts for the metrics scope of the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_alerts_list_builder()` + `monitoring_projects_alerts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_alerts_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_alerts_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    monitoring_projects_alerts_list_execute(builder)
}

/// GET v3/{+name}/collectdTimeSeries
/// Cloud Monitoring Agent only: Creates a new time series.This method is only for use by the Cloud Monitoring Agent. Use projects.`timeSeries`.create instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_collectd_time_series_create_execute()` to send, or `monitoring_projects_collectd_time_series_create` for simplest API.

pub fn monitoring_projects_collectd_time_series_create_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CreateCollectdTimeSeriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/collectdTimeSeries", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}/collectdTimeSeries
/// Cloud Monitoring Agent only: Creates a new time series.This method is only for use by the Cloud Monitoring Agent. Use projects.`timeSeries`.create instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_collectd_time_series_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_collectd_time_series_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_collectd_time_series_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateCollectdTimeSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateCollectdTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/collectdTimeSeries
/// Cloud Monitoring Agent only: Creates a new time series.This method is only for use by the Cloud Monitoring Agent. Use projects.`timeSeries`.create instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_collectd_time_series_create_builder()` + `monitoring_projects_collectd_time_series_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_collectd_time_series_create(
    client: &SimpleHttpClient,
    name: &str,
    body: &CreateCollectdTimeSeriesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateCollectdTimeSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_collectd_time_series_create_builder(client, name, body)?;
    monitoring_projects_collectd_time_series_create_execute(builder)
}

/// GET v3/{+name}/groups
/// Creates a new group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_groups_create_execute()` to send, or `monitoring_projects_groups_create` for simplest API.

pub fn monitoring_projects_groups_create_builder(
    client: &SimpleHttpClient,
    name: &str,
    validateOnly: Option<bool>,
    body: &Group,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/groups", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}/groups
/// Creates a new group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_groups_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_groups_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Group = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/groups
/// Creates a new group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_groups_create_builder()` + `monitoring_projects_groups_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_create(
    client: &SimpleHttpClient,
    name: &str,
    validateOnly: Option<bool>,
    body: &Group,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_groups_create_builder(client, name, validateOnly, body)?;
    monitoring_projects_groups_create_execute(builder)
}

/// GET v3/{+name}
/// Deletes an existing group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_groups_delete_execute()` to send, or `monitoring_projects_groups_delete` for simplest API.

pub fn monitoring_projects_groups_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    recursive: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "recursive",
            recursive
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}
/// Deletes an existing group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Deletes an existing group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_groups_delete_builder()` + `monitoring_projects_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_delete(
    client: &SimpleHttpClient,
    name: &str,
    recursive: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_groups_delete_builder(client, name, recursive)?;
    monitoring_projects_groups_delete_execute(builder)
}

/// GET v3/{+name}
/// Gets a single group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_groups_get_execute()` to send, or `monitoring_projects_groups_get` for simplest API.

pub fn monitoring_projects_groups_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Group = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_groups_get_builder()` + `monitoring_projects_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_groups_get_builder(client, name)?;
    monitoring_projects_groups_get_execute(builder)
}

/// GET v3/{+name}/groups
/// Lists the existing groups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_groups_list_execute()` to send, or `monitoring_projects_groups_list` for simplest API.

pub fn monitoring_projects_groups_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    ancestorsOfGroup: Option<&str>,
    childrenOfGroup: Option<&str>,
    descendantsOfGroup: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/groups", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "ancestorsOfGroup",
            ancestorsOfGroup
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "childrenOfGroup",
            childrenOfGroup
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "descendantsOfGroup",
            descendantsOfGroup
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}/groups
/// Lists the existing groups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/groups
/// Lists the existing groups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_groups_list_builder()` + `monitoring_projects_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_list(
    client: &SimpleHttpClient,
    name: &str,
    ancestorsOfGroup: Option<&str>,
    childrenOfGroup: Option<&str>,
    descendantsOfGroup: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_groups_list_builder(
        client,
        name,
        ancestorsOfGroup,
        childrenOfGroup,
        descendantsOfGroup,
        pageSize,
        pageToken,
    )?;
    monitoring_projects_groups_list_execute(builder)
}

/// GET v3/{+name}
/// Updates an existing group. You can change any group attributes except name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_groups_update_execute()` to send, or `monitoring_projects_groups_update` for simplest API.

pub fn monitoring_projects_groups_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    validateOnly: Option<bool>,
    body: &Group,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}
/// Updates an existing group. You can change any group attributes except name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_groups_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_groups_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Group = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Updates an existing group. You can change any group attributes except name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_groups_update_builder()` + `monitoring_projects_groups_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_update(
    client: &SimpleHttpClient,
    name: &str,
    validateOnly: Option<bool>,
    body: &Group,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_groups_update_builder(client, name, validateOnly, body)?;
    monitoring_projects_groups_update_execute(builder)
}

/// GET v3/{+name}/members
/// Lists the monitored resources that are members of a group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_groups_members_list_execute()` to send, or `monitoring_projects_groups_members_list` for simplest API.

pub fn monitoring_projects_groups_members_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    interval_endTime: Option<&str>,
    interval_startTime: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/members", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "interval_endTime",
            interval_endTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "interval_startTime",
            interval_startTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}/members
/// Lists the monitored resources that are members of a group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_groups_members_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_groups_members_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_members_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupMembersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGroupMembersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/members
/// Lists the monitored resources that are members of a group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_groups_members_list_builder()` + `monitoring_projects_groups_members_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_groups_members_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    interval_endTime: Option<&str>,
    interval_startTime: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupMembersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_groups_members_list_builder(
        client,
        name,
        filter,
        interval_endTime,
        interval_startTime,
        pageSize,
        pageToken,
    )?;
    monitoring_projects_groups_members_list_execute(builder)
}

/// GET v3/{+name}/metricDescriptors
/// Creates a new metric descriptor. The creation is executed asynchronously. User-created metric descriptors define custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>). The metric descriptor is updated if it already exists, except that metric labels are never removed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_metric_descriptors_create_execute()` to send, or `monitoring_projects_metric_descriptors_create` for simplest API.

pub fn monitoring_projects_metric_descriptors_create_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MetricDescriptor,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/metricDescriptors", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}/metricDescriptors
/// Creates a new metric descriptor. The creation is executed asynchronously. User-created metric descriptors define custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>). The metric descriptor is updated if it already exists, except that metric labels are never removed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_metric_descriptors_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_metric_descriptors_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetricDescriptor>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MetricDescriptor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/metricDescriptors
/// Creates a new metric descriptor. The creation is executed asynchronously. User-created metric descriptors define custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>). The metric descriptor is updated if it already exists, except that metric labels are never removed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_metric_descriptors_create_builder()` + `monitoring_projects_metric_descriptors_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_create(
    client: &SimpleHttpClient,
    name: &str,
    body: &MetricDescriptor,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetricDescriptor>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_metric_descriptors_create_builder(client, name, body)?;
    monitoring_projects_metric_descriptors_create_execute(builder)
}

/// GET v3/{+name}
/// Deletes a metric descriptor. Only user-created custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>) can be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_metric_descriptors_delete_execute()` to send, or `monitoring_projects_metric_descriptors_delete` for simplest API.

pub fn monitoring_projects_metric_descriptors_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Deletes a metric descriptor. Only user-created custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>) can be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_metric_descriptors_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_metric_descriptors_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Deletes a metric descriptor. Only user-created custom metrics (<https://cloud.google.`com/monitoring/custom-metrics`>) can be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_metric_descriptors_delete_builder()` + `monitoring_projects_metric_descriptors_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_metric_descriptors_delete_builder(client, name)?;
    monitoring_projects_metric_descriptors_delete_execute(builder)
}

/// GET v3/{+name}
/// Gets a single metric descriptor.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_metric_descriptors_get_execute()` to send, or `monitoring_projects_metric_descriptors_get` for simplest API.

pub fn monitoring_projects_metric_descriptors_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single metric descriptor.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_metric_descriptors_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_metric_descriptors_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetricDescriptor>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MetricDescriptor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single metric descriptor.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_metric_descriptors_get_builder()` + `monitoring_projects_metric_descriptors_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetricDescriptor>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_metric_descriptors_get_builder(client, name)?;
    monitoring_projects_metric_descriptors_get_execute(builder)
}

/// GET v3/{+name}/metricDescriptors
/// Lists metric descriptors that match a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_metric_descriptors_list_execute()` to send, or `monitoring_projects_metric_descriptors_list` for simplest API.

pub fn monitoring_projects_metric_descriptors_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    activeOnly: Option<bool>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/metricDescriptors", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "activeOnly",
            activeOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}/metricDescriptors
/// Lists metric descriptors that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_metric_descriptors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_metric_descriptors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMetricDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMetricDescriptorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/metricDescriptors
/// Lists metric descriptors that match a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_metric_descriptors_list_builder()` + `monitoring_projects_metric_descriptors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_metric_descriptors_list(
    client: &SimpleHttpClient,
    name: &str,
    activeOnly: Option<bool>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMetricDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_metric_descriptors_list_builder(
        client, name, activeOnly, filter, pageSize, pageToken,
    )?;
    monitoring_projects_metric_descriptors_list_execute(builder)
}

/// GET v3/{+name}
/// Gets a single monitored resource descriptor.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_monitored_resource_descriptors_get_execute()` to send, or `monitoring_projects_monitored_resource_descriptors_get` for simplest API.

pub fn monitoring_projects_monitored_resource_descriptors_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single monitored resource descriptor.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_monitored_resource_descriptors_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_monitored_resource_descriptors_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_monitored_resource_descriptors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MonitoredResourceDescriptor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MonitoredResourceDescriptor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single monitored resource descriptor.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_monitored_resource_descriptors_get_builder()` + `monitoring_projects_monitored_resource_descriptors_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_monitored_resource_descriptors_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MonitoredResourceDescriptor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_monitored_resource_descriptors_get_builder(client, name)?;
    monitoring_projects_monitored_resource_descriptors_get_execute(builder)
}

/// GET v3/{+name}/monitoredResourceDescriptors
/// Lists monitored resource descriptors that match a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_monitored_resource_descriptors_list_execute()` to send, or `monitoring_projects_monitored_resource_descriptors_list` for simplest API.

pub fn monitoring_projects_monitored_resource_descriptors_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv3/{+name}/monitoredResourceDescriptors",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}/monitoredResourceDescriptors
/// Lists monitored resource descriptors that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_monitored_resource_descriptors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_monitored_resource_descriptors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_monitored_resource_descriptors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMonitoredResourceDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMonitoredResourceDescriptorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/monitoredResourceDescriptors
/// Lists monitored resource descriptors that match a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_monitored_resource_descriptors_list_builder()` + `monitoring_projects_monitored_resource_descriptors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_monitored_resource_descriptors_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMonitoredResourceDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_monitored_resource_descriptors_list_builder(
        client, name, filter, pageSize, pageToken,
    )?;
    monitoring_projects_monitored_resource_descriptors_list_execute(builder)
}

/// GET v3/{+name}
/// Gets a single channel descriptor. The descriptor indicates which fields are expected / permitted for a notification channel of the given type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channel_descriptors_get_execute()` to send, or `monitoring_projects_notification_channel_descriptors_get` for simplest API.

pub fn monitoring_projects_notification_channel_descriptors_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single channel descriptor. The descriptor indicates which fields are expected / permitted for a notification channel of the given type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_notification_channel_descriptors_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_notification_channel_descriptors_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channel_descriptors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NotificationChannelDescriptor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationChannelDescriptor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single channel descriptor. The descriptor indicates which fields are expected / permitted for a notification channel of the given type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channel_descriptors_get_builder()` + `monitoring_projects_notification_channel_descriptors_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channel_descriptors_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NotificationChannelDescriptor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channel_descriptors_get_builder(client, name)?;
    monitoring_projects_notification_channel_descriptors_get_execute(builder)
}

/// GET v3/{+name}/notificationChannelDescriptors
/// Lists the descriptors for supported channel types. The use of descriptors makes it possible for new channel types to be dynamically added.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channel_descriptors_list_execute()` to send, or `monitoring_projects_notification_channel_descriptors_list` for simplest API.

pub fn monitoring_projects_notification_channel_descriptors_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv3/{+name}/notificationChannelDescriptors",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}/notificationChannelDescriptors
/// Lists the descriptors for supported channel types. The use of descriptors makes it possible for new channel types to be dynamically added.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_notification_channel_descriptors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_notification_channel_descriptors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channel_descriptors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationChannelDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNotificationChannelDescriptorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/notificationChannelDescriptors
/// Lists the descriptors for supported channel types. The use of descriptors makes it possible for new channel types to be dynamically added.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channel_descriptors_list_builder()` + `monitoring_projects_notification_channel_descriptors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channel_descriptors_list(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationChannelDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channel_descriptors_list_builder(
        client, name, pageSize, pageToken,
    )?;
    monitoring_projects_notification_channel_descriptors_list_execute(builder)
}

/// GET v3/{+name}/notificationChannels
/// Creates a new notification channel, representing a single notification endpoint such as an email address, SMS number, or PagerDuty service.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_create_execute()` to send, or `monitoring_projects_notification_channels_create` for simplest API.

pub fn monitoring_projects_notification_channels_create_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &NotificationChannel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv3/{+name}/notificationChannels",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}/notificationChannels
/// Creates a new notification channel, representing a single notification endpoint such as an email address, SMS number, or PagerDuty service.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_notification_channels_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_notification_channels_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/notificationChannels
/// Creates a new notification channel, representing a single notification endpoint such as an email address, SMS number, or PagerDuty service.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_create_builder()` + `monitoring_projects_notification_channels_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_create(
    client: &SimpleHttpClient,
    name: &str,
    body: &NotificationChannel,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_create_builder(client, name, body)?;
    monitoring_projects_notification_channels_create_execute(builder)
}

/// GET v3/{+name}
/// Deletes a notification channel.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_delete_execute()` to send, or `monitoring_projects_notification_channels_delete` for simplest API.

pub fn monitoring_projects_notification_channels_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "force",
            force
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}
/// Deletes a notification channel.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_notification_channels_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_notification_channels_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Deletes a notification channel.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_delete_builder()` + `monitoring_projects_notification_channels_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_delete(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_delete_builder(client, name, force)?;
    monitoring_projects_notification_channels_delete_execute(builder)
}

/// GET v3/{+name}
/// Gets a single notification channel. The channel includes the relevant configuration details with which the channel was created. However, the response may truncate or omit passwords, API keys, or other private key matter and thus the response may not be 100% identical to the information that was supplied in the call to the create method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_get_execute()` to send, or `monitoring_projects_notification_channels_get` for simplest API.

pub fn monitoring_projects_notification_channels_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single notification channel. The channel includes the relevant configuration details with which the channel was created. However, the response may truncate or omit passwords, API keys, or other private key matter and thus the response may not be 100% identical to the information that was supplied in the call to the create method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_notification_channels_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_notification_channels_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single notification channel. The channel includes the relevant configuration details with which the channel was created. However, the response may truncate or omit passwords, API keys, or other private key matter and thus the response may not be 100% identical to the information that was supplied in the call to the create method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_get_builder()` + `monitoring_projects_notification_channels_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_get_builder(client, name)?;
    monitoring_projects_notification_channels_get_execute(builder)
}

/// GET v3/{+name}:getVerificationCode
/// Requests a verification code for an already verified channel that can then be used in a call to VerifyNotificationChannel() on a different channel with an equivalent identity in the same or in a different project. This makes it possible to copy a channel between projects without requiring manual reverification of the channel. If the channel is not in the verified state, this method will fail (in other words, this may only be used if the SendNotificationChannelVerificationCode and VerifyNotificationChannel paths have already been used to put the given channel into the verified state).There is no guarantee that the verification codes returned by this method will be of a similar structure or form as the ones that are delivered to the channel via SendNotificationChannelVerificationCode; while VerifyNotificationChannel() will recognize both the codes delivered via SendNotificationChannelVerificationCode() and returned from GetNotificationChannelVerificationCode(), it is typically the case that the verification codes delivered via SendNotificationChannelVerificationCode() will be shorter and also have a shorter expiration (e.g. codes such as "G-123456") whereas GetVerificationCode() will typically return a much longer, websafe base 64 encoded string that has a longer expiration time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_get_verification_code_execute()` to send, or `monitoring_projects_notification_channels_get_verification_code` for simplest API.

pub fn monitoring_projects_notification_channels_get_verification_code_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GetNotificationChannelVerificationCodeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv3/{+name}:getVerificationCode",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}:getVerificationCode
/// Requests a verification code for an already verified channel that can then be used in a call to VerifyNotificationChannel() on a different channel with an equivalent identity in the same or in a different project. This makes it possible to copy a channel between projects without requiring manual reverification of the channel. If the channel is not in the verified state, this method will fail (in other words, this may only be used if the SendNotificationChannelVerificationCode and VerifyNotificationChannel paths have already been used to put the given channel into the verified state).There is no guarantee that the verification codes returned by this method will be of a similar structure or form as the ones that are delivered to the channel via SendNotificationChannelVerificationCode; while VerifyNotificationChannel() will recognize both the codes delivered via SendNotificationChannelVerificationCode() and returned from GetNotificationChannelVerificationCode(), it is typically the case that the verification codes delivered via SendNotificationChannelVerificationCode() will be shorter and also have a shorter expiration (e.g. codes such as "G-123456") whereas GetVerificationCode() will typically return a much longer, websafe base 64 encoded string that has a longer expiration time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_notification_channels_get_verification_code_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_notification_channels_get_verification_code_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_get_verification_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetNotificationChannelVerificationCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetNotificationChannelVerificationCodeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}:getVerificationCode
/// Requests a verification code for an already verified channel that can then be used in a call to VerifyNotificationChannel() on a different channel with an equivalent identity in the same or in a different project. This makes it possible to copy a channel between projects without requiring manual reverification of the channel. If the channel is not in the verified state, this method will fail (in other words, this may only be used if the SendNotificationChannelVerificationCode and VerifyNotificationChannel paths have already been used to put the given channel into the verified state).There is no guarantee that the verification codes returned by this method will be of a similar structure or form as the ones that are delivered to the channel via SendNotificationChannelVerificationCode; while VerifyNotificationChannel() will recognize both the codes delivered via SendNotificationChannelVerificationCode() and returned from GetNotificationChannelVerificationCode(), it is typically the case that the verification codes delivered via SendNotificationChannelVerificationCode() will be shorter and also have a shorter expiration (e.g. codes such as "G-123456") whereas GetVerificationCode() will typically return a much longer, websafe base 64 encoded string that has a longer expiration time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_get_verification_code_builder()` + `monitoring_projects_notification_channels_get_verification_code_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_get_verification_code(
    client: &SimpleHttpClient,
    name: &str,
    body: &GetNotificationChannelVerificationCodeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetNotificationChannelVerificationCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_get_verification_code_builder(
        client, name, body,
    )?;
    monitoring_projects_notification_channels_get_verification_code_execute(builder)
}

/// GET v3/{+name}/notificationChannels
/// Lists the notification channels that have been created for the project. To list the types of notification channels that are supported, use the ListNotificationChannelDescriptors method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_list_execute()` to send, or `monitoring_projects_notification_channels_list` for simplest API.

pub fn monitoring_projects_notification_channels_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv3/{+name}/notificationChannels",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}/notificationChannels
/// Lists the notification channels that have been created for the project. To list the types of notification channels that are supported, use the ListNotificationChannelDescriptors method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_notification_channels_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_notification_channels_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationChannelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNotificationChannelsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/notificationChannels
/// Lists the notification channels that have been created for the project. To list the types of notification channels that are supported, use the ListNotificationChannelDescriptors method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_list_builder()` + `monitoring_projects_notification_channels_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNotificationChannelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_list_builder(
        client, name, filter, orderBy, pageSize, pageToken,
    )?;
    monitoring_projects_notification_channels_list_execute(builder)
}

/// GET v3/{+name}
/// Updates a notification channel. Fields not specified in the field mask remain unchanged.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_patch_execute()` to send, or `monitoring_projects_notification_channels_patch` for simplest API.

pub fn monitoring_projects_notification_channels_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &NotificationChannel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}
/// Updates a notification channel. Fields not specified in the field mask remain unchanged.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_notification_channels_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_notification_channels_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Updates a notification channel. Fields not specified in the field mask remain unchanged.Design your application to single-thread API calls that modify the state of notification channels in a single project. This includes calls to CreateNotificationChannel, DeleteNotificationChannel and UpdateNotificationChannel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_patch_builder()` + `monitoring_projects_notification_channels_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &NotificationChannel,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        monitoring_projects_notification_channels_patch_builder(client, name, updateMask, body)?;
    monitoring_projects_notification_channels_patch_execute(builder)
}

/// GET v3/{+name}:sendVerificationCode
/// Causes a verification code to be delivered to the channel. The code can then be supplied in VerifyNotificationChannel to verify the channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_send_verification_code_execute()` to send, or `monitoring_projects_notification_channels_send_verification_code` for simplest API.

pub fn monitoring_projects_notification_channels_send_verification_code_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SendNotificationChannelVerificationCodeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv3/{+name}:sendVerificationCode",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}:sendVerificationCode
/// Causes a verification code to be delivered to the channel. The code can then be supplied in VerifyNotificationChannel to verify the channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_notification_channels_send_verification_code_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_notification_channels_send_verification_code_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_send_verification_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}:sendVerificationCode
/// Causes a verification code to be delivered to the channel. The code can then be supplied in VerifyNotificationChannel to verify the channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_send_verification_code_builder()` + `monitoring_projects_notification_channels_send_verification_code_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_send_verification_code(
    client: &SimpleHttpClient,
    name: &str,
    body: &SendNotificationChannelVerificationCodeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_send_verification_code_builder(
        client, name, body,
    )?;
    monitoring_projects_notification_channels_send_verification_code_execute(builder)
}

/// GET v3/{+name}:verify
/// Verifies a NotificationChannel by proving receipt of the code delivered to the channel as a result of calling SendNotificationChannelVerificationCode.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_notification_channels_verify_execute()` to send, or `monitoring_projects_notification_channels_verify` for simplest API.

pub fn monitoring_projects_notification_channels_verify_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &VerifyNotificationChannelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}:verify", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}:verify
/// Verifies a NotificationChannel by proving receipt of the code delivered to the channel as a result of calling SendNotificationChannelVerificationCode.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_notification_channels_verify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_notification_channels_verify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}:verify
/// Verifies a NotificationChannel by proving receipt of the code delivered to the channel as a result of calling SendNotificationChannelVerificationCode.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_notification_channels_verify_builder()` + `monitoring_projects_notification_channels_verify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_notification_channels_verify(
    client: &SimpleHttpClient,
    name: &str,
    body: &VerifyNotificationChannelRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationChannel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_notification_channels_verify_builder(client, name, body)?;
    monitoring_projects_notification_channels_verify_execute(builder)
}

/// GET v3/{+parent}/snoozes
/// Creates a Snooze that will prevent alerts, which match the provided criteria, from being opened. The Snooze applies for a specific time interval.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_snoozes_create_execute()` to send, or `monitoring_projects_snoozes_create` for simplest API.

pub fn monitoring_projects_snoozes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Snooze,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+parent}/snoozes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+parent}/snoozes
/// Creates a Snooze that will prevent alerts, which match the provided criteria, from being opened. The Snooze applies for a specific time interval.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_snoozes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_snoozes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snooze>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snooze = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+parent}/snoozes
/// Creates a Snooze that will prevent alerts, which match the provided criteria, from being opened. The Snooze applies for a specific time interval.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_snoozes_create_builder()` + `monitoring_projects_snoozes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Snooze,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snooze>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_snoozes_create_builder(client, parent, body)?;
    monitoring_projects_snoozes_create_execute(builder)
}

/// GET v3/{+name}
/// Retrieves a Snooze by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_snoozes_get_execute()` to send, or `monitoring_projects_snoozes_get` for simplest API.

pub fn monitoring_projects_snoozes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Retrieves a Snooze by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_snoozes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_snoozes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snooze>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snooze = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Retrieves a Snooze by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_snoozes_get_builder()` + `monitoring_projects_snoozes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snooze>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_snoozes_get_builder(client, name)?;
    monitoring_projects_snoozes_get_execute(builder)
}

/// GET v3/{+parent}/snoozes
/// Lists the Snoozes associated with a project. Can optionally pass in filter, which specifies predicates to match Snoozes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_snoozes_list_execute()` to send, or `monitoring_projects_snoozes_list` for simplest API.

pub fn monitoring_projects_snoozes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+parent}/snoozes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+parent}/snoozes
/// Lists the Snoozes associated with a project. Can optionally pass in filter, which specifies predicates to match Snoozes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_snoozes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_snoozes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnoozesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSnoozesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+parent}/snoozes
/// Lists the Snoozes associated with a project. Can optionally pass in filter, which specifies predicates to match Snoozes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_snoozes_list_builder()` + `monitoring_projects_snoozes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnoozesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        monitoring_projects_snoozes_list_builder(client, parent, filter, pageSize, pageToken)?;
    monitoring_projects_snoozes_list_execute(builder)
}

/// GET v3/{+name}
/// Updates a Snooze, identified by its name, with the parameters in the given Snooze object.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_snoozes_patch_execute()` to send, or `monitoring_projects_snoozes_patch` for simplest API.

pub fn monitoring_projects_snoozes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Snooze,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}
/// Updates a Snooze, identified by its name, with the parameters in the given Snooze object.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_snoozes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_snoozes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snooze>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snooze = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Updates a Snooze, identified by its name, with the parameters in the given Snooze object.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_snoozes_patch_builder()` + `monitoring_projects_snoozes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_snoozes_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Snooze,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snooze>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_snoozes_patch_builder(client, name, updateMask, body)?;
    monitoring_projects_snoozes_patch_execute(builder)
}

/// GET v3/{+name}/timeSeries
/// Creates or adds data to one or more time series. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This method does not support resource locations constraint of an organization policy (<https://cloud.google.`com/resource-manager/docs/organization-policy/defining-locations`#setting_the_organization_policy>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_time_series_create_execute()` to send, or `monitoring_projects_time_series_create` for simplest API.

pub fn monitoring_projects_time_series_create_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CreateTimeSeriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/timeSeries", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}/timeSeries
/// Creates or adds data to one or more time series. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This method does not support resource locations constraint of an organization policy (<https://cloud.google.`com/resource-manager/docs/organization-policy/defining-locations`#setting_the_organization_policy>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_time_series_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_time_series_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/timeSeries
/// Creates or adds data to one or more time series. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This method does not support resource locations constraint of an organization policy (<https://cloud.google.`com/resource-manager/docs/organization-policy/defining-locations`#setting_the_organization_policy>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_time_series_create_builder()` + `monitoring_projects_time_series_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_create(
    client: &SimpleHttpClient,
    name: &str,
    body: &CreateTimeSeriesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_time_series_create_builder(client, name, body)?;
    monitoring_projects_time_series_create_execute(builder)
}

/// GET v3/{+name}/timeSeries:createService
/// Creates or adds data to one or more service time series. A service time series is a time series for a metric from a Google Cloud service. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This endpoint rejects writes to user-defined metrics. This method is only for use by Google Cloud services. Use projects.`timeSeries`.create instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_time_series_create_service_execute()` to send, or `monitoring_projects_time_series_create_service` for simplest API.

pub fn monitoring_projects_time_series_create_service_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CreateTimeSeriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv3/{+name}/timeSeries:createService",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}/timeSeries:createService
/// Creates or adds data to one or more service time series. A service time series is a time series for a metric from a Google Cloud service. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This endpoint rejects writes to user-defined metrics. This method is only for use by Google Cloud services. Use projects.`timeSeries`.create instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_time_series_create_service_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_time_series_create_service_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_create_service_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/timeSeries:createService
/// Creates or adds data to one or more service time series. A service time series is a time series for a metric from a Google Cloud service. The response is empty if all time series in the request were written. If any time series could not be written, a corresponding failure message is included in the error response. This endpoint rejects writes to user-defined metrics. This method is only for use by Google Cloud services. Use projects.`timeSeries`.create instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_time_series_create_service_builder()` + `monitoring_projects_time_series_create_service_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_create_service(
    client: &SimpleHttpClient,
    name: &str,
    body: &CreateTimeSeriesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_time_series_create_service_builder(client, name, body)?;
    monitoring_projects_time_series_create_service_execute(builder)
}

/// GET v3/{+name}/timeSeries
/// Lists time series that match a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_time_series_list_execute()` to send, or `monitoring_projects_time_series_list` for simplest API.

pub fn monitoring_projects_time_series_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    aggregation_alignmentPeriod: Option<&str>,
    aggregation_crossSeriesReducer: Option<&str>,
    aggregation_groupByFields: Option<&str>,
    aggregation_perSeriesAligner: Option<&str>,
    filter: Option<&str>,
    interval_endTime: Option<&str>,
    interval_startTime: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    secondaryAggregation_alignmentPeriod: Option<&str>,
    secondaryAggregation_crossSeriesReducer: Option<&str>,
    secondaryAggregation_groupByFields: Option<&str>,
    secondaryAggregation_perSeriesAligner: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/timeSeries", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "aggregation_alignmentPeriod",
            aggregation_alignmentPeriod
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "aggregation_crossSeriesReducer",
            aggregation_crossSeriesReducer
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "aggregation_groupByFields",
            aggregation_groupByFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "aggregation_perSeriesAligner",
            aggregation_perSeriesAligner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "interval_endTime",
            interval_endTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "interval_startTime",
            interval_startTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "secondaryAggregation_alignmentPeriod",
            secondaryAggregation_alignmentPeriod
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "secondaryAggregation_crossSeriesReducer",
            secondaryAggregation_crossSeriesReducer
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "secondaryAggregation_groupByFields",
            secondaryAggregation_groupByFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "secondaryAggregation_perSeriesAligner",
            secondaryAggregation_perSeriesAligner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}/timeSeries
/// Lists time series that match a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_time_series_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_time_series_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/timeSeries
/// Lists time series that match a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_time_series_list_builder()` + `monitoring_projects_time_series_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_list(
    client: &SimpleHttpClient,
    name: &str,
    aggregation_alignmentPeriod: Option<&str>,
    aggregation_crossSeriesReducer: Option<&str>,
    aggregation_groupByFields: Option<&str>,
    aggregation_perSeriesAligner: Option<&str>,
    filter: Option<&str>,
    interval_endTime: Option<&str>,
    interval_startTime: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    secondaryAggregation_alignmentPeriod: Option<&str>,
    secondaryAggregation_crossSeriesReducer: Option<&str>,
    secondaryAggregation_groupByFields: Option<&str>,
    secondaryAggregation_perSeriesAligner: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_time_series_list_builder(
        client,
        name,
        aggregation_alignmentPeriod,
        aggregation_crossSeriesReducer,
        aggregation_groupByFields,
        aggregation_perSeriesAligner,
        filter,
        interval_endTime,
        interval_startTime,
        orderBy,
        pageSize,
        pageToken,
        secondaryAggregation_alignmentPeriod,
        secondaryAggregation_crossSeriesReducer,
        secondaryAggregation_groupByFields,
        secondaryAggregation_perSeriesAligner,
        view,
    )?;
    monitoring_projects_time_series_list_execute(builder)
}

/// GET v3/{+name}/timeSeries:query
/// Queries time series by using Monitoring Query Language (MQL). We recommend using PromQL instead of MQL. For more information about the status of MQL, see the MQL deprecation notice (<https://cloud.google.`com/stackdriver/docs/deprecations/mql`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_time_series_query_execute()` to send, or `monitoring_projects_time_series_query` for simplest API.

pub fn monitoring_projects_time_series_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &QueryTimeSeriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}/timeSeries:query", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}/timeSeries:query
/// Queries time series by using Monitoring Query Language (MQL). We recommend using PromQL instead of MQL. For more information about the status of MQL, see the MQL deprecation notice (<https://cloud.google.`com/stackdriver/docs/deprecations/mql`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_time_series_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_time_series_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}/timeSeries:query
/// Queries time series by using Monitoring Query Language (MQL). We recommend using PromQL instead of MQL. For more information about the status of MQL, see the MQL deprecation notice (<https://cloud.google.`com/stackdriver/docs/deprecations/mql`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_time_series_query_builder()` + `monitoring_projects_time_series_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_time_series_query(
    client: &SimpleHttpClient,
    name: &str,
    body: &QueryTimeSeriesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryTimeSeriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_time_series_query_builder(client, name, body)?;
    monitoring_projects_time_series_query_execute(builder)
}

/// GET v3/{+parent}/uptimeCheckConfigs
/// Creates a new Uptime check configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_uptime_check_configs_create_execute()` to send, or `monitoring_projects_uptime_check_configs_create` for simplest API.

pub fn monitoring_projects_uptime_check_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &UptimeCheckConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv3/{+parent}/uptimeCheckConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+parent}/uptimeCheckConfigs
/// Creates a new Uptime check configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_uptime_check_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_uptime_check_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UptimeCheckConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UptimeCheckConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+parent}/uptimeCheckConfigs
/// Creates a new Uptime check configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_uptime_check_configs_create_builder()` + `monitoring_projects_uptime_check_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &UptimeCheckConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UptimeCheckConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_uptime_check_configs_create_builder(client, parent, body)?;
    monitoring_projects_uptime_check_configs_create_execute(builder)
}

/// GET v3/{+name}
/// Deletes an Uptime check configuration. Note that this method will fail if the Uptime check configuration is referenced by an alert policy or other dependent configs that would be rendered invalid by the deletion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_uptime_check_configs_delete_execute()` to send, or `monitoring_projects_uptime_check_configs_delete` for simplest API.

pub fn monitoring_projects_uptime_check_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Deletes an Uptime check configuration. Note that this method will fail if the Uptime check configuration is referenced by an alert policy or other dependent configs that would be rendered invalid by the deletion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_uptime_check_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_uptime_check_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Deletes an Uptime check configuration. Note that this method will fail if the Uptime check configuration is referenced by an alert policy or other dependent configs that would be rendered invalid by the deletion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_uptime_check_configs_delete_builder()` + `monitoring_projects_uptime_check_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_projects_uptime_check_configs_delete_builder(client, name)?;
    monitoring_projects_uptime_check_configs_delete_execute(builder)
}

/// GET v3/{+name}
/// Gets a single Uptime check configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_uptime_check_configs_get_execute()` to send, or `monitoring_projects_uptime_check_configs_get` for simplest API.

pub fn monitoring_projects_uptime_check_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single Uptime check configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_uptime_check_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_uptime_check_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UptimeCheckConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UptimeCheckConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Gets a single Uptime check configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_uptime_check_configs_get_builder()` + `monitoring_projects_uptime_check_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UptimeCheckConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_uptime_check_configs_get_builder(client, name)?;
    monitoring_projects_uptime_check_configs_get_execute(builder)
}

/// GET v3/{+parent}/uptimeCheckConfigs
/// Lists the existing valid Uptime check configurations for the project (leaving out any invalid configurations).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_uptime_check_configs_list_execute()` to send, or `monitoring_projects_uptime_check_configs_list` for simplest API.

pub fn monitoring_projects_uptime_check_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv3/{+parent}/uptimeCheckConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+parent}/uptimeCheckConfigs
/// Lists the existing valid Uptime check configurations for the project (leaving out any invalid configurations).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_uptime_check_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_uptime_check_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUptimeCheckConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUptimeCheckConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+parent}/uptimeCheckConfigs
/// Lists the existing valid Uptime check configurations for the project (leaving out any invalid configurations).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_uptime_check_configs_list_builder()` + `monitoring_projects_uptime_check_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUptimeCheckConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_projects_uptime_check_configs_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    monitoring_projects_uptime_check_configs_list_execute(builder)
}

/// GET v3/{+name}
/// Updates an Uptime check configuration. You can either replace the entire configuration with a new one or replace only certain fields in the current configuration by specifying the fields to be updated via `updateMask`. Returns the updated configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_projects_uptime_check_configs_patch_execute()` to send, or `monitoring_projects_uptime_check_configs_patch` for simplest API.

pub fn monitoring_projects_uptime_check_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &UptimeCheckConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}
/// Updates an Uptime check configuration. You can either replace the entire configuration with a new one or replace only certain fields in the current configuration by specifying the fields to be updated via `updateMask`. Returns the updated configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_projects_uptime_check_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_projects_uptime_check_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UptimeCheckConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UptimeCheckConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Updates an Uptime check configuration. You can either replace the entire configuration with a new one or replace only certain fields in the current configuration by specifying the fields to be updated via `updateMask`. Returns the updated configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_projects_uptime_check_configs_patch_builder()` + `monitoring_projects_uptime_check_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_projects_uptime_check_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &UptimeCheckConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UptimeCheckConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        monitoring_projects_uptime_check_configs_patch_builder(client, name, updateMask, body)?;
    monitoring_projects_uptime_check_configs_patch_execute(builder)
}

/// GET v3/{+parent}/services
/// Create a Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_create_execute()` to send, or `monitoring_services_create` for simplest API.

pub fn monitoring_services_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    serviceId: Option<&str>,
    body: &Service,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+parent}/services", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "serviceId",
            serviceId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+parent}/services
/// Create a Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_services_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_services_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Service = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+parent}/services
/// Create a Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_create_builder()` + `monitoring_services_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_create(
    client: &SimpleHttpClient,
    parent: &str,
    serviceId: Option<&str>,
    body: &Service,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_services_create_builder(client, parent, serviceId, body)?;
    monitoring_services_create_execute(builder)
}

/// GET v3/{+name}
/// Soft delete this Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_delete_execute()` to send, or `monitoring_services_delete` for simplest API.

pub fn monitoring_services_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Soft delete this Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_services_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_services_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Soft delete this Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_delete_builder()` + `monitoring_services_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_services_delete_builder(client, name)?;
    monitoring_services_delete_execute(builder)
}

/// GET v3/{+name}
/// Get the named Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_get_execute()` to send, or `monitoring_services_get` for simplest API.

pub fn monitoring_services_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Get the named Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_services_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_services_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Service = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Get the named Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_get_builder()` + `monitoring_services_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_services_get_builder(client, name)?;
    monitoring_services_get_execute(builder)
}

/// GET v3/{+parent}/services
/// List Services for this Metrics Scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_list_execute()` to send, or `monitoring_services_list` for simplest API.

pub fn monitoring_services_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+parent}/services", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+parent}/services
/// List Services for this Metrics Scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_services_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_services_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+parent}/services
/// List Services for this Metrics Scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_list_builder()` + `monitoring_services_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_services_list_builder(client, parent, filter, pageSize, pageToken)?;
    monitoring_services_list_execute(builder)
}

/// GET v3/{+name}
/// Update this Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_patch_execute()` to send, or `monitoring_services_patch` for simplest API.

pub fn monitoring_services_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Service,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}
/// Update this Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_services_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_services_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Service = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Update this Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_patch_builder()` + `monitoring_services_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Service,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_services_patch_builder(client, name, updateMask, body)?;
    monitoring_services_patch_execute(builder)
}

/// GET v3/{+parent}/serviceLevelObjectives
/// Create a ServiceLevelObjective for the given Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_service_level_objectives_create_execute()` to send, or `monitoring_services_service_level_objectives_create` for simplest API.

pub fn monitoring_services_service_level_objectives_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    serviceLevelObjectiveId: Option<&str>,
    body: &ServiceLevelObjective,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv3/{+parent}/serviceLevelObjectives",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "serviceLevelObjectiveId",
            serviceLevelObjectiveId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+parent}/serviceLevelObjectives
/// Create a ServiceLevelObjective for the given Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_services_service_level_objectives_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_services_service_level_objectives_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceLevelObjective>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceLevelObjective = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+parent}/serviceLevelObjectives
/// Create a ServiceLevelObjective for the given Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_service_level_objectives_create_builder()` + `monitoring_services_service_level_objectives_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_create(
    client: &SimpleHttpClient,
    parent: &str,
    serviceLevelObjectiveId: Option<&str>,
    body: &ServiceLevelObjective,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceLevelObjective>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_services_service_level_objectives_create_builder(
        client,
        parent,
        serviceLevelObjectiveId,
        body,
    )?;
    monitoring_services_service_level_objectives_create_execute(builder)
}

/// GET v3/{+name}
/// Delete the given ServiceLevelObjective.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_service_level_objectives_delete_execute()` to send, or `monitoring_services_service_level_objectives_delete` for simplest API.

pub fn monitoring_services_service_level_objectives_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Delete the given ServiceLevelObjective.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_services_service_level_objectives_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_services_service_level_objectives_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Delete the given ServiceLevelObjective.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_service_level_objectives_delete_builder()` + `monitoring_services_service_level_objectives_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = monitoring_services_service_level_objectives_delete_builder(client, name)?;
    monitoring_services_service_level_objectives_delete_execute(builder)
}

/// GET v3/{+name}
/// Get a ServiceLevelObjective by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_service_level_objectives_get_execute()` to send, or `monitoring_services_service_level_objectives_get` for simplest API.

pub fn monitoring_services_service_level_objectives_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+name}
/// Get a ServiceLevelObjective by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_services_service_level_objectives_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_services_service_level_objectives_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceLevelObjective>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceLevelObjective = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Get a ServiceLevelObjective by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_service_level_objectives_get_builder()` + `monitoring_services_service_level_objectives_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_get(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceLevelObjective>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_services_service_level_objectives_get_builder(client, name, view)?;
    monitoring_services_service_level_objectives_get_execute(builder)
}

/// GET v3/{+parent}/serviceLevelObjectives
/// List the ServiceLevelObjectives for the given Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_service_level_objectives_list_execute()` to send, or `monitoring_services_service_level_objectives_list` for simplest API.

pub fn monitoring_services_service_level_objectives_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv3/{+parent}/serviceLevelObjectives",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/{+parent}/serviceLevelObjectives
/// List the ServiceLevelObjectives for the given Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_services_service_level_objectives_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_services_service_level_objectives_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServiceLevelObjectivesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServiceLevelObjectivesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+parent}/serviceLevelObjectives
/// List the ServiceLevelObjectives for the given Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_service_level_objectives_list_builder()` + `monitoring_services_service_level_objectives_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServiceLevelObjectivesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_services_service_level_objectives_list_builder(
        client, parent, filter, pageSize, pageToken, view,
    )?;
    monitoring_services_service_level_objectives_list_execute(builder)
}

/// GET v3/{+name}
/// Update the given ServiceLevelObjective.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_services_service_level_objectives_patch_execute()` to send, or `monitoring_services_service_level_objectives_patch` for simplest API.

pub fn monitoring_services_service_level_objectives_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &ServiceLevelObjective,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v3/{+name}
/// Update the given ServiceLevelObjective.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_services_service_level_objectives_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_services_service_level_objectives_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceLevelObjective>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceLevelObjective = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/{+name}
/// Update the given ServiceLevelObjective.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_services_service_level_objectives_patch_builder()` + `monitoring_services_service_level_objectives_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_services_service_level_objectives_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &ServiceLevelObjective,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceLevelObjective>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        monitoring_services_service_level_objectives_patch_builder(client, name, updateMask, body)?;
    monitoring_services_service_level_objectives_patch_execute(builder)
}

/// GET v3/uptimeCheckIps
/// Returns the list of IP addresses that checkers run from.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `monitoring_uptime_check_ips_list_execute()` to send, or `monitoring_uptime_check_ips_list` for simplest API.

pub fn monitoring_uptime_check_ips_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv3/uptimeCheckIps",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v3/uptimeCheckIps
/// Returns the list of IP addresses that checkers run from.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `monitoring_uptime_check_ips_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from monitoring_uptime_check_ips_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_uptime_check_ips_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUptimeCheckIpsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUptimeCheckIpsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/uptimeCheckIps
/// Returns the list of IP addresses that checkers run from.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `monitoring_uptime_check_ips_list_builder()` + `monitoring_uptime_check_ips_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn monitoring_uptime_check_ips_list(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUptimeCheckIpsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = monitoring_uptime_check_ips_list_builder(client, pageSize, pageToken)?;
    monitoring_uptime_check_ips_list_execute(builder)
}
