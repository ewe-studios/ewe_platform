//! Auto-generated API clients for gcp/developerconnect.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_get_execute()` to send, or `developerconnect_projects_locations_get` for simplest API.

pub fn developerconnect_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_get_builder()` + `developerconnect_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_get_builder(client, name)?;
    developerconnect_projects_locations_get_execute(builder)
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_list_execute()` to send, or `developerconnect_projects_locations_list` for simplest API.

pub fn developerconnect_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_list_builder()` + `developerconnect_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    developerconnect_projects_locations_list_execute(builder)
}

/// GET v1/{+parent}/accountConnectors
/// Creates a new AccountConnector in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_create_execute()` to send, or `developerconnect_projects_locations_account_connectors_create` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    accountConnectorId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &AccountConnector,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/accountConnectors",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "accountConnectorId",
            accountConnectorId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/accountConnectors
/// Creates a new AccountConnector in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/accountConnectors
/// Creates a new AccountConnector in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_create_builder()` + `developerconnect_projects_locations_account_connectors_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_create(
    client: &SimpleHttpClient,
    parent: &str,
    accountConnectorId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &AccountConnector,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_account_connectors_create_builder(
        client,
        parent,
        accountConnectorId,
        requestId,
        validateOnly,
        body,
    )?;
    developerconnect_projects_locations_account_connectors_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single AccountConnector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_delete_execute()` to send, or `developerconnect_projects_locations_account_connectors_delete` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    force: Option<bool>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "force",
            force
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a single AccountConnector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single AccountConnector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_delete_builder()` + `developerconnect_projects_locations_account_connectors_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    force: Option<bool>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_account_connectors_delete_builder(
        client,
        name,
        etag,
        force,
        requestId,
        validateOnly,
    )?;
    developerconnect_projects_locations_account_connectors_delete_execute(builder)
}

/// GET v1/{+accountConnector}:fetchUserRepositories
/// FetchUserRepositories returns a list of UserRepos that are available for an account connector resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_fetch_user_repositories_execute()` to send, or `developerconnect_projects_locations_account_connectors_fetch_user_repositories` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_fetch_user_repositories_builder(
    client: &SimpleHttpClient,
    accountConnector: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    repository: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+accountConnector}:fetchUserRepositories",
        accountConnector,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "repository",
            repository
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+accountConnector}:fetchUserRepositories
/// FetchUserRepositories returns a list of UserRepos that are available for an account connector resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_fetch_user_repositories_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_fetch_user_repositories_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_fetch_user_repositories_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchUserRepositoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchUserRepositoriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+accountConnector}:fetchUserRepositories
/// FetchUserRepositories returns a list of UserRepos that are available for an account connector resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_fetch_user_repositories_builder()` + `developerconnect_projects_locations_account_connectors_fetch_user_repositories_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_fetch_user_repositories(
    client: &SimpleHttpClient,
    accountConnector: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    repository: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchUserRepositoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_account_connectors_fetch_user_repositories_builder(
            client,
            accountConnector,
            pageSize,
            pageToken,
            repository,
        )?;
    developerconnect_projects_locations_account_connectors_fetch_user_repositories_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single AccountConnector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_get_execute()` to send, or `developerconnect_projects_locations_account_connectors_get` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single AccountConnector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountConnector>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountConnector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single AccountConnector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_get_builder()` + `developerconnect_projects_locations_account_connectors_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountConnector>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_account_connectors_get_builder(client, name)?;
    developerconnect_projects_locations_account_connectors_get_execute(builder)
}

/// GET v1/{+parent}/accountConnectors
/// Lists AccountConnectors in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_list_execute()` to send, or `developerconnect_projects_locations_account_connectors_list` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/accountConnectors",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/accountConnectors
/// Lists AccountConnectors in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAccountConnectorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAccountConnectorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/accountConnectors
/// Lists AccountConnectors in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_list_builder()` + `developerconnect_projects_locations_account_connectors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAccountConnectorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_account_connectors_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    developerconnect_projects_locations_account_connectors_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the parameters of a single AccountConnector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_patch_execute()` to send, or `developerconnect_projects_locations_account_connectors_patch` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &AccountConnector,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allowMissing",
            allowMissing
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the parameters of a single AccountConnector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the parameters of a single AccountConnector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_patch_builder()` + `developerconnect_projects_locations_account_connectors_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &AccountConnector,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_account_connectors_patch_builder(
        client,
        name,
        allowMissing,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    developerconnect_projects_locations_account_connectors_patch_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single User.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_users_delete_execute()` to send, or `developerconnect_projects_locations_account_connectors_users_delete` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_users_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a single User.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_users_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single User.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_delete_builder()` + `developerconnect_projects_locations_account_connectors_users_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_account_connectors_users_delete_builder(
        client,
        name,
        etag,
        requestId,
        validateOnly,
    )?;
    developerconnect_projects_locations_account_connectors_users_delete_execute(builder)
}

/// GET v1/{+name}/users:deleteSelf
/// Delete the User based on the user credentials.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_users_delete_self_execute()` to send, or `developerconnect_projects_locations_account_connectors_users_delete_self` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_users_delete_self_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/users:deleteSelf", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/users:deleteSelf
/// Delete the User based on the user credentials.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_delete_self_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_users_delete_self_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_delete_self_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/users:deleteSelf
/// Delete the User based on the user credentials.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_delete_self_builder()` + `developerconnect_projects_locations_account_connectors_users_delete_self_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_delete_self(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_account_connectors_users_delete_self_builder(
        client, name,
    )?;
    developerconnect_projects_locations_account_connectors_users_delete_self_execute(builder)
}

/// GET v1/{+accountConnector}/users:fetchAccessToken
/// Fetches OAuth access token based on end user credentials.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_users_fetch_access_token_execute()` to send, or `developerconnect_projects_locations_account_connectors_users_fetch_access_token` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_users_fetch_access_token_builder(
    client: &SimpleHttpClient,
    accountConnector: &str,
    body: &FetchAccessTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+accountConnector}/users:fetchAccessToken",
        accountConnector,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+accountConnector}/users:fetchAccessToken
/// Fetches OAuth access token based on end user credentials.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_fetch_access_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_users_fetch_access_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_fetch_access_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchAccessTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchAccessTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+accountConnector}/users:fetchAccessToken
/// Fetches OAuth access token based on end user credentials.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_fetch_access_token_builder()` + `developerconnect_projects_locations_account_connectors_users_fetch_access_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_fetch_access_token(
    client: &SimpleHttpClient,
    accountConnector: &str,
    body: &FetchAccessTokenRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchAccessTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_account_connectors_users_fetch_access_token_builder(
            client,
            accountConnector,
            body,
        )?;
    developerconnect_projects_locations_account_connectors_users_fetch_access_token_execute(builder)
}

/// GET v1/{+name}/users:fetchSelf
/// Fetch the User based on the user credentials.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_users_fetch_self_execute()` to send, or `developerconnect_projects_locations_account_connectors_users_fetch_self` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_users_fetch_self_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/users:fetchSelf", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/users:fetchSelf
/// Fetch the User based on the user credentials.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_fetch_self_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_users_fetch_self_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_fetch_self_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/users:fetchSelf
/// Fetch the User based on the user credentials.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_fetch_self_builder()` + `developerconnect_projects_locations_account_connectors_users_fetch_self_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_fetch_self(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_account_connectors_users_fetch_self_builder(
        client, name,
    )?;
    developerconnect_projects_locations_account_connectors_users_fetch_self_execute(builder)
}

/// GET v1/{+accountConnector}/users:finishOAuthFlow
/// Finishes OAuth flow for an account connector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_users_finish_oauth_flow_execute()` to send, or `developerconnect_projects_locations_account_connectors_users_finish_oauth_flow` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_users_finish_oauth_flow_builder(
    client: &SimpleHttpClient,
    accountConnector: &str,
    googleOauthParams_scopes: Option<&str>,
    googleOauthParams_ticket: Option<&str>,
    googleOauthParams_versionInfo: Option<&str>,
    oauthParams_code: Option<&str>,
    oauthParams_ticket: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+accountConnector}/users:finishOAuthFlow",
        accountConnector,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "googleOauthParams_scopes",
            googleOauthParams_scopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "googleOauthParams_ticket",
            googleOauthParams_ticket
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "googleOauthParams_versionInfo",
            googleOauthParams_versionInfo
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "oauthParams_code",
            oauthParams_code
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "oauthParams_ticket",
            oauthParams_ticket
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+accountConnector}/users:finishOAuthFlow
/// Finishes OAuth flow for an account connector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_finish_oauth_flow_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_users_finish_oauth_flow_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_finish_oauth_flow_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinishOAuthResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinishOAuthResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+accountConnector}/users:finishOAuthFlow
/// Finishes OAuth flow for an account connector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_finish_oauth_flow_builder()` + `developerconnect_projects_locations_account_connectors_users_finish_oauth_flow_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_finish_oauth_flow(
    client: &SimpleHttpClient,
    accountConnector: &str,
    googleOauthParams_scopes: Option<&str>,
    googleOauthParams_ticket: Option<&str>,
    googleOauthParams_versionInfo: Option<&str>,
    oauthParams_code: Option<&str>,
    oauthParams_ticket: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinishOAuthResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_account_connectors_users_finish_oauth_flow_builder(
            client,
            accountConnector,
            googleOauthParams_scopes,
            googleOauthParams_ticket,
            googleOauthParams_versionInfo,
            oauthParams_code,
            oauthParams_ticket,
        )?;
    developerconnect_projects_locations_account_connectors_users_finish_oauth_flow_execute(builder)
}

/// GET v1/{+parent}/users
/// Lists Users in a given project, location, and account_connector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_users_list_execute()` to send, or `developerconnect_projects_locations_account_connectors_users_list` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_users_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/users", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/users
/// Lists Users in a given project, location, and account_connector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_users_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUsersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/users
/// Lists Users in a given project, location, and account_connector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_list_builder()` + `developerconnect_projects_locations_account_connectors_users_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_account_connectors_users_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    developerconnect_projects_locations_account_connectors_users_list_execute(builder)
}

/// GET v1/{+accountConnector}/users:startOAuthFlow
/// Starts OAuth flow for an account connector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_account_connectors_users_start_oauth_flow_execute()` to send, or `developerconnect_projects_locations_account_connectors_users_start_oauth_flow` for simplest API.

pub fn developerconnect_projects_locations_account_connectors_users_start_oauth_flow_builder(
    client: &SimpleHttpClient,
    accountConnector: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+accountConnector}/users:startOAuthFlow",
        accountConnector,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+accountConnector}/users:startOAuthFlow
/// Starts OAuth flow for an account connector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_start_oauth_flow_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_account_connectors_users_start_oauth_flow_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_start_oauth_flow_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StartOAuthResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StartOAuthResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+accountConnector}/users:startOAuthFlow
/// Starts OAuth flow for an account connector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_account_connectors_users_start_oauth_flow_builder()` + `developerconnect_projects_locations_account_connectors_users_start_oauth_flow_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_account_connectors_users_start_oauth_flow(
    client: &SimpleHttpClient,
    accountConnector: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StartOAuthResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_account_connectors_users_start_oauth_flow_builder(
            client,
            accountConnector,
        )?;
    developerconnect_projects_locations_account_connectors_users_start_oauth_flow_execute(builder)
}

/// GET v1/{+parent}/connections
/// Creates a new Connection in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_create_execute()` to send, or `developerconnect_projects_locations_connections_create` for simplest API.

pub fn developerconnect_projects_locations_connections_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    connectionId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Connection,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/connections", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "connectionId",
            connectionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/connections
/// Creates a new Connection in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/connections
/// Creates a new Connection in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_create_builder()` + `developerconnect_projects_locations_connections_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_create(
    client: &SimpleHttpClient,
    parent: &str,
    connectionId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Connection,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_create_builder(
        client,
        parent,
        connectionId,
        requestId,
        validateOnly,
        body,
    )?;
    developerconnect_projects_locations_connections_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_delete_execute()` to send, or `developerconnect_projects_locations_connections_delete` for simplest API.

pub fn developerconnect_projects_locations_connections_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a single Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_delete_builder()` + `developerconnect_projects_locations_connections_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_delete_builder(
        client,
        name,
        etag,
        requestId,
        validateOnly,
    )?;
    developerconnect_projects_locations_connections_delete_execute(builder)
}

/// GET v1/{+connection}:fetchGitHubInstallations
/// FetchGitHubInstallations returns the list of GitHub Installations that are available to be added to a Connection. For github.com, only installations accessible to the authorizer token are returned. For GitHub Enterprise, all installations are returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_fetch_git_hub_installations_execute()` to send, or `developerconnect_projects_locations_connections_fetch_git_hub_installations` for simplest API.

pub fn developerconnect_projects_locations_connections_fetch_git_hub_installations_builder(
    client: &SimpleHttpClient,
    connection: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+connection}:fetchGitHubInstallations",
        connection,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+connection}:fetchGitHubInstallations
/// FetchGitHubInstallations returns the list of GitHub Installations that are available to be added to a Connection. For github.com, only installations accessible to the authorizer token are returned. For GitHub Enterprise, all installations are returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_fetch_git_hub_installations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_fetch_git_hub_installations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_fetch_git_hub_installations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchGitHubInstallationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchGitHubInstallationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+connection}:fetchGitHubInstallations
/// FetchGitHubInstallations returns the list of GitHub Installations that are available to be added to a Connection. For github.com, only installations accessible to the authorizer token are returned. For GitHub Enterprise, all installations are returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_fetch_git_hub_installations_builder()` + `developerconnect_projects_locations_connections_fetch_git_hub_installations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_fetch_git_hub_installations(
    client: &SimpleHttpClient,
    connection: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchGitHubInstallationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_connections_fetch_git_hub_installations_builder(
            client, connection,
        )?;
    developerconnect_projects_locations_connections_fetch_git_hub_installations_execute(builder)
}

/// GET v1/{+connection}:fetchLinkableGitRepositories
/// FetchLinkableGitRepositories returns a list of git repositories from an SCM that are available to be added to a Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_fetch_linkable_git_repositories_execute()` to send, or `developerconnect_projects_locations_connections_fetch_linkable_git_repositories` for simplest API.

pub fn developerconnect_projects_locations_connections_fetch_linkable_git_repositories_builder(
    client: &SimpleHttpClient,
    connection: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+connection}:fetchLinkableGitRepositories",
        connection,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+connection}:fetchLinkableGitRepositories
/// FetchLinkableGitRepositories returns a list of git repositories from an SCM that are available to be added to a Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_fetch_linkable_git_repositories_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_fetch_linkable_git_repositories_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_fetch_linkable_git_repositories_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchLinkableGitRepositoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchLinkableGitRepositoriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+connection}:fetchLinkableGitRepositories
/// FetchLinkableGitRepositories returns a list of git repositories from an SCM that are available to be added to a Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_fetch_linkable_git_repositories_builder()` + `developerconnect_projects_locations_connections_fetch_linkable_git_repositories_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_fetch_linkable_git_repositories(
    client: &SimpleHttpClient,
    connection: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchLinkableGitRepositoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_connections_fetch_linkable_git_repositories_builder(
            client, connection, pageSize, pageToken,
        )?;
    developerconnect_projects_locations_connections_fetch_linkable_git_repositories_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_get_execute()` to send, or `developerconnect_projects_locations_connections_get` for simplest API.

pub fn developerconnect_projects_locations_connections_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Connection>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_get_builder()` + `developerconnect_projects_locations_connections_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Connection>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_get_builder(client, name)?;
    developerconnect_projects_locations_connections_get_execute(builder)
}

/// GET v1/{+parent}/connections
/// Lists Connections in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_list_execute()` to send, or `developerconnect_projects_locations_connections_list` for simplest API.

pub fn developerconnect_projects_locations_connections_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/connections", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/connections
/// Lists Connections in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConnectionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/connections
/// Lists Connections in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_list_builder()` + `developerconnect_projects_locations_connections_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConnectionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    developerconnect_projects_locations_connections_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the parameters of a single Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_patch_execute()` to send, or `developerconnect_projects_locations_connections_patch` for simplest API.

pub fn developerconnect_projects_locations_connections_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Connection,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allowMissing",
            allowMissing
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the parameters of a single Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the parameters of a single Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_patch_builder()` + `developerconnect_projects_locations_connections_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Connection,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_patch_builder(
        client,
        name,
        allowMissing,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    developerconnect_projects_locations_connections_patch_execute(builder)
}

/// GET v1/{+parent}/connections:processGitHubEnterpriseWebhook
/// ProcessGitHubEnterpriseWebhook is called by the external GitHub Enterprise instances for notifying events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_process_git_hub_enterprise_webhook_execute()` to send, or `developerconnect_projects_locations_connections_process_git_hub_enterprise_webhook` for simplest API.

pub fn developerconnect_projects_locations_connections_process_git_hub_enterprise_webhook_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ProcessGitHubEnterpriseWebhookRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/connections:processGitHubEnterpriseWebhook",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/connections:processGitHubEnterpriseWebhook
/// ProcessGitHubEnterpriseWebhook is called by the external GitHub Enterprise instances for notifying events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_process_git_hub_enterprise_webhook_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_process_git_hub_enterprise_webhook_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_process_git_hub_enterprise_webhook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/connections:processGitHubEnterpriseWebhook
/// ProcessGitHubEnterpriseWebhook is called by the external GitHub Enterprise instances for notifying events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_process_git_hub_enterprise_webhook_builder()` + `developerconnect_projects_locations_connections_process_git_hub_enterprise_webhook_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_process_git_hub_enterprise_webhook(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ProcessGitHubEnterpriseWebhookRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_connections_process_git_hub_enterprise_webhook_builder(
            client, parent, body,
        )?;
    developerconnect_projects_locations_connections_process_git_hub_enterprise_webhook_execute(
        builder,
    )
}

/// GET v1/{+parent}/gitRepositoryLinks
/// Creates a GitRepositoryLink. Upon linking a Git Repository, Developer Connect will configure the Git Repository to send webhook events to Developer Connect. Connections that use Firebase GitHub Application will have events forwarded to the Firebase service. Connections that use Gemini Code Assist will have events forwarded to Gemini Code Assist service. All other Connections will have events forwarded to Cloud Build.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_git_repository_links_create_execute()` to send, or `developerconnect_projects_locations_connections_git_repository_links_create` for simplest API.

pub fn developerconnect_projects_locations_connections_git_repository_links_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    gitRepositoryLinkId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &GitRepositoryLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/gitRepositoryLinks",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "gitRepositoryLinkId",
            gitRepositoryLinkId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/gitRepositoryLinks
/// Creates a GitRepositoryLink. Upon linking a Git Repository, Developer Connect will configure the Git Repository to send webhook events to Developer Connect. Connections that use Firebase GitHub Application will have events forwarded to the Firebase service. Connections that use Gemini Code Assist will have events forwarded to Gemini Code Assist service. All other Connections will have events forwarded to Cloud Build.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_git_repository_links_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/gitRepositoryLinks
/// Creates a GitRepositoryLink. Upon linking a Git Repository, Developer Connect will configure the Git Repository to send webhook events to Developer Connect. Connections that use Firebase GitHub Application will have events forwarded to the Firebase service. Connections that use Gemini Code Assist will have events forwarded to Gemini Code Assist service. All other Connections will have events forwarded to Cloud Build.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_create_builder()` + `developerconnect_projects_locations_connections_git_repository_links_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_create(
    client: &SimpleHttpClient,
    parent: &str,
    gitRepositoryLinkId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &GitRepositoryLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_connections_git_repository_links_create_builder(
            client,
            parent,
            gitRepositoryLinkId,
            requestId,
            validateOnly,
            body,
        )?;
    developerconnect_projects_locations_connections_git_repository_links_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single GitRepositoryLink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_git_repository_links_delete_execute()` to send, or `developerconnect_projects_locations_connections_git_repository_links_delete` for simplest API.

pub fn developerconnect_projects_locations_connections_git_repository_links_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a single GitRepositoryLink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_git_repository_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single GitRepositoryLink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_delete_builder()` + `developerconnect_projects_locations_connections_git_repository_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_connections_git_repository_links_delete_builder(
            client,
            name,
            etag,
            requestId,
            validateOnly,
        )?;
    developerconnect_projects_locations_connections_git_repository_links_delete_execute(builder)
}

/// GET v1/{+gitRepositoryLink}:fetchGitRefs
/// Fetch the list of branches or tags for a given repository.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_git_repository_links_fetch_git_refs_execute()` to send, or `developerconnect_projects_locations_connections_git_repository_links_fetch_git_refs` for simplest API.

pub fn developerconnect_projects_locations_connections_git_repository_links_fetch_git_refs_builder(
    client: &SimpleHttpClient,
    gitRepositoryLink: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    refType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+gitRepositoryLink}:fetchGitRefs",
        gitRepositoryLink,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "refType",
            refType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+gitRepositoryLink}:fetchGitRefs
/// Fetch the list of branches or tags for a given repository.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_fetch_git_refs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_git_repository_links_fetch_git_refs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_fetch_git_refs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchGitRefsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchGitRefsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+gitRepositoryLink}:fetchGitRefs
/// Fetch the list of branches or tags for a given repository.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_fetch_git_refs_builder()` + `developerconnect_projects_locations_connections_git_repository_links_fetch_git_refs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_fetch_git_refs(
    client: &SimpleHttpClient,
    gitRepositoryLink: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    refType: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchGitRefsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_git_repository_links_fetch_git_refs_builder(client, gitRepositoryLink, pageSize, pageToken, refType)?;
    developerconnect_projects_locations_connections_git_repository_links_fetch_git_refs_execute(
        builder,
    )
}

/// GET v1/{+gitRepositoryLink}:fetchReadToken
/// Fetches read token of a given `gitRepositoryLink`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_git_repository_links_fetch_read_token_execute()` to send, or `developerconnect_projects_locations_connections_git_repository_links_fetch_read_token` for simplest API.

pub fn developerconnect_projects_locations_connections_git_repository_links_fetch_read_token_builder(
    client: &SimpleHttpClient,
    gitRepositoryLink: &str,
    body: &FetchReadTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+gitRepositoryLink}:fetchReadToken",
        gitRepositoryLink,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+gitRepositoryLink}:fetchReadToken
/// Fetches read token of a given `gitRepositoryLink`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_fetch_read_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_git_repository_links_fetch_read_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_fetch_read_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchReadTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchReadTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+gitRepositoryLink}:fetchReadToken
/// Fetches read token of a given `gitRepositoryLink`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_fetch_read_token_builder()` + `developerconnect_projects_locations_connections_git_repository_links_fetch_read_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_fetch_read_token(
    client: &SimpleHttpClient,
    gitRepositoryLink: &str,
    body: &FetchReadTokenRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchReadTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_git_repository_links_fetch_read_token_builder(client, gitRepositoryLink, body)?;
    developerconnect_projects_locations_connections_git_repository_links_fetch_read_token_execute(
        builder,
    )
}

/// GET v1/{+gitRepositoryLink}:fetchReadWriteToken
/// Fetches `read/write` token of a given `gitRepositoryLink`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_git_repository_links_fetch_read_write_token_execute()` to send, or `developerconnect_projects_locations_connections_git_repository_links_fetch_read_write_token` for simplest API.

pub fn developerconnect_projects_locations_connections_git_repository_links_fetch_read_write_token_builder(
    client: &SimpleHttpClient,
    gitRepositoryLink: &str,
    body: &FetchReadWriteTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+gitRepositoryLink}:fetchReadWriteToken",
        gitRepositoryLink,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+gitRepositoryLink}:fetchReadWriteToken
/// Fetches `read/write` token of a given `gitRepositoryLink`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_fetch_read_write_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_git_repository_links_fetch_read_write_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_fetch_read_write_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchReadWriteTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchReadWriteTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+gitRepositoryLink}:fetchReadWriteToken
/// Fetches `read/write` token of a given `gitRepositoryLink`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_fetch_read_write_token_builder()` + `developerconnect_projects_locations_connections_git_repository_links_fetch_read_write_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_fetch_read_write_token(
    client: &SimpleHttpClient,
    gitRepositoryLink: &str,
    body: &FetchReadWriteTokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchReadWriteTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_git_repository_links_fetch_read_write_token_builder(client, gitRepositoryLink, body)?;
    developerconnect_projects_locations_connections_git_repository_links_fetch_read_write_token_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single GitRepositoryLink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_git_repository_links_get_execute()` to send, or `developerconnect_projects_locations_connections_git_repository_links_get` for simplest API.

pub fn developerconnect_projects_locations_connections_git_repository_links_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single GitRepositoryLink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_git_repository_links_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GitRepositoryLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GitRepositoryLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single GitRepositoryLink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_get_builder()` + `developerconnect_projects_locations_connections_git_repository_links_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GitRepositoryLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_git_repository_links_get_builder(
        client, name,
    )?;
    developerconnect_projects_locations_connections_git_repository_links_get_execute(builder)
}

/// GET v1/{+parent}/gitRepositoryLinks
/// Lists GitRepositoryLinks in a given project, location, and connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_git_repository_links_list_execute()` to send, or `developerconnect_projects_locations_connections_git_repository_links_list` for simplest API.

pub fn developerconnect_projects_locations_connections_git_repository_links_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/gitRepositoryLinks",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/gitRepositoryLinks
/// Lists GitRepositoryLinks in a given project, location, and connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_git_repository_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListGitRepositoryLinksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGitRepositoryLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/gitRepositoryLinks
/// Lists GitRepositoryLinks in a given project, location, and connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_list_builder()` + `developerconnect_projects_locations_connections_git_repository_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListGitRepositoryLinksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_connections_git_repository_links_list_builder(
            client, parent, filter, orderBy, pageSize, pageToken,
        )?;
    developerconnect_projects_locations_connections_git_repository_links_list_execute(builder)
}

/// GET v1/{+name}:processBitbucketCloudWebhook
/// ProcessBitbucketCloudWebhook is called by the external Bitbucket Cloud instances for notifying events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_cloud_webhook_execute()` to send, or `developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_cloud_webhook` for simplest API.

pub fn developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_cloud_webhook_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ProcessBitbucketCloudWebhookRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:processBitbucketCloudWebhook",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:processBitbucketCloudWebhook
/// ProcessBitbucketCloudWebhook is called by the external Bitbucket Cloud instances for notifying events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_cloud_webhook_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_cloud_webhook_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_cloud_webhook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:processBitbucketCloudWebhook
/// ProcessBitbucketCloudWebhook is called by the external Bitbucket Cloud instances for notifying events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_cloud_webhook_builder()` + `developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_cloud_webhook_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_cloud_webhook(
    client: &SimpleHttpClient,
    name: &str,
    body: &ProcessBitbucketCloudWebhookRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_cloud_webhook_builder(client, name, body)?;
    developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_cloud_webhook_execute(builder)
}

/// GET v1/{+name}:processBitbucketDataCenterWebhook
/// ProcessBitbucketDataCenterWebhook is called by the external Bitbucket Data Center instances for notifying events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_data_center_webhook_execute()` to send, or `developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_data_center_webhook` for simplest API.

pub fn developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_data_center_webhook_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ProcessBitbucketDataCenterWebhookRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:processBitbucketDataCenterWebhook",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:processBitbucketDataCenterWebhook
/// ProcessBitbucketDataCenterWebhook is called by the external Bitbucket Data Center instances for notifying events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_data_center_webhook_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_data_center_webhook_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_data_center_webhook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:processBitbucketDataCenterWebhook
/// ProcessBitbucketDataCenterWebhook is called by the external Bitbucket Data Center instances for notifying events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_data_center_webhook_builder()` + `developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_data_center_webhook_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_data_center_webhook(
    client: &SimpleHttpClient,
    name: &str,
    body: &ProcessBitbucketDataCenterWebhookRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_data_center_webhook_builder(client, name, body)?;
    developerconnect_projects_locations_connections_git_repository_links_process_bitbucket_data_center_webhook_execute(builder)
}

/// GET v1/{+name}:processGitLabEnterpriseWebhook
/// ProcessGitLabEnterpriseWebhook is called by the external GitLab Enterprise instances for notifying events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_git_repository_links_process_git_lab_enterprise_webhook_execute()` to send, or `developerconnect_projects_locations_connections_git_repository_links_process_git_lab_enterprise_webhook` for simplest API.

pub fn developerconnect_projects_locations_connections_git_repository_links_process_git_lab_enterprise_webhook_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ProcessGitLabEnterpriseWebhookRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:processGitLabEnterpriseWebhook",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:processGitLabEnterpriseWebhook
/// ProcessGitLabEnterpriseWebhook is called by the external GitLab Enterprise instances for notifying events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_process_git_lab_enterprise_webhook_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_git_repository_links_process_git_lab_enterprise_webhook_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_process_git_lab_enterprise_webhook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:processGitLabEnterpriseWebhook
/// ProcessGitLabEnterpriseWebhook is called by the external GitLab Enterprise instances for notifying events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_process_git_lab_enterprise_webhook_builder()` + `developerconnect_projects_locations_connections_git_repository_links_process_git_lab_enterprise_webhook_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_process_git_lab_enterprise_webhook(
    client: &SimpleHttpClient,
    name: &str,
    body: &ProcessGitLabEnterpriseWebhookRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_git_repository_links_process_git_lab_enterprise_webhook_builder(client, name, body)?;
    developerconnect_projects_locations_connections_git_repository_links_process_git_lab_enterprise_webhook_execute(builder)
}

/// GET v1/{+name}:processGitLabWebhook
/// ProcessGitLabWebhook is called by the GitLab.com for notifying events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_connections_git_repository_links_process_git_lab_webhook_execute()` to send, or `developerconnect_projects_locations_connections_git_repository_links_process_git_lab_webhook` for simplest API.

pub fn developerconnect_projects_locations_connections_git_repository_links_process_git_lab_webhook_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ProcessGitLabWebhookRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:processGitLabWebhook",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:processGitLabWebhook
/// ProcessGitLabWebhook is called by the GitLab.com for notifying events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_process_git_lab_webhook_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_connections_git_repository_links_process_git_lab_webhook_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_process_git_lab_webhook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:processGitLabWebhook
/// ProcessGitLabWebhook is called by the GitLab.com for notifying events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_connections_git_repository_links_process_git_lab_webhook_builder()` + `developerconnect_projects_locations_connections_git_repository_links_process_git_lab_webhook_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_connections_git_repository_links_process_git_lab_webhook(
    client: &SimpleHttpClient,
    name: &str,
    body: &ProcessGitLabWebhookRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_connections_git_repository_links_process_git_lab_webhook_builder(client, name, body)?;
    developerconnect_projects_locations_connections_git_repository_links_process_git_lab_webhook_execute(builder)
}

/// GET v1/{+parent}/insightsConfigs
/// Creates a new InsightsConfig in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_insights_configs_create_execute()` to send, or `developerconnect_projects_locations_insights_configs_create` for simplest API.

pub fn developerconnect_projects_locations_insights_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    insightsConfigId: Option<&str>,
    validateOnly: Option<bool>,
    body: &InsightsConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/insightsConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "insightsConfigId",
            insightsConfigId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/insightsConfigs
/// Creates a new InsightsConfig in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_insights_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_insights_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/insightsConfigs
/// Creates a new InsightsConfig in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_insights_configs_create_builder()` + `developerconnect_projects_locations_insights_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    insightsConfigId: Option<&str>,
    validateOnly: Option<bool>,
    body: &InsightsConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_insights_configs_create_builder(
        client,
        parent,
        insightsConfigId,
        validateOnly,
        body,
    )?;
    developerconnect_projects_locations_insights_configs_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single Insight.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_insights_configs_delete_execute()` to send, or `developerconnect_projects_locations_insights_configs_delete` for simplest API.

pub fn developerconnect_projects_locations_insights_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a single Insight.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_insights_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_insights_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single Insight.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_insights_configs_delete_builder()` + `developerconnect_projects_locations_insights_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_insights_configs_delete_builder(
        client,
        name,
        etag,
        requestId,
        validateOnly,
    )?;
    developerconnect_projects_locations_insights_configs_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single Insight.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_insights_configs_get_execute()` to send, or `developerconnect_projects_locations_insights_configs_get` for simplest API.

pub fn developerconnect_projects_locations_insights_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single Insight.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_insights_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_insights_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InsightsConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InsightsConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single Insight.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_insights_configs_get_builder()` + `developerconnect_projects_locations_insights_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InsightsConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_insights_configs_get_builder(client, name)?;
    developerconnect_projects_locations_insights_configs_get_execute(builder)
}

/// GET v1/{+parent}/insightsConfigs
/// Lists InsightsConfigs in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_insights_configs_list_execute()` to send, or `developerconnect_projects_locations_insights_configs_list` for simplest API.

pub fn developerconnect_projects_locations_insights_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/insightsConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/insightsConfigs
/// Lists InsightsConfigs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_insights_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_insights_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInsightsConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInsightsConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/insightsConfigs
/// Lists InsightsConfigs in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_insights_configs_list_builder()` + `developerconnect_projects_locations_insights_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInsightsConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_insights_configs_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    developerconnect_projects_locations_insights_configs_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the parameters of a single InsightsConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_insights_configs_patch_execute()` to send, or `developerconnect_projects_locations_insights_configs_patch` for simplest API.

pub fn developerconnect_projects_locations_insights_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &InsightsConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allowMissing",
            allowMissing
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the parameters of a single InsightsConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_insights_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_insights_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the parameters of a single InsightsConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_insights_configs_patch_builder()` + `developerconnect_projects_locations_insights_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &InsightsConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_insights_configs_patch_builder(
        client,
        name,
        allowMissing,
        requestId,
        validateOnly,
        body,
    )?;
    developerconnect_projects_locations_insights_configs_patch_execute(builder)
}

/// GET v1/{+name}
/// Gets a single Deployment Event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_insights_configs_deployment_events_get_execute()` to send, or `developerconnect_projects_locations_insights_configs_deployment_events_get` for simplest API.

pub fn developerconnect_projects_locations_insights_configs_deployment_events_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a single Deployment Event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_insights_configs_deployment_events_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_insights_configs_deployment_events_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_deployment_events_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeploymentEvent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeploymentEvent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a single Deployment Event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_insights_configs_deployment_events_get_builder()` + `developerconnect_projects_locations_insights_configs_deployment_events_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_deployment_events_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeploymentEvent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_insights_configs_deployment_events_get_builder(
            client, name,
        )?;
    developerconnect_projects_locations_insights_configs_deployment_events_get_execute(builder)
}

/// GET v1/{+parent}/deploymentEvents
/// Lists Deployment Events in a given insights config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_insights_configs_deployment_events_list_execute()` to send, or `developerconnect_projects_locations_insights_configs_deployment_events_list` for simplest API.

pub fn developerconnect_projects_locations_insights_configs_deployment_events_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/deploymentEvents",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/deploymentEvents
/// Lists Deployment Events in a given insights config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_insights_configs_deployment_events_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_insights_configs_deployment_events_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_deployment_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDeploymentEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDeploymentEventsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/deploymentEvents
/// Lists Deployment Events in a given insights config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_insights_configs_deployment_events_list_builder()` + `developerconnect_projects_locations_insights_configs_deployment_events_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_insights_configs_deployment_events_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDeploymentEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_insights_configs_deployment_events_list_builder(
            client, parent, filter, pageSize, pageToken,
        )?;
    developerconnect_projects_locations_insights_configs_deployment_events_list_execute(builder)
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_operations_cancel_execute()` to send, or `developerconnect_projects_locations_operations_cancel` for simplest API.

pub fn developerconnect_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_operations_cancel_builder()` + `developerconnect_projects_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        developerconnect_projects_locations_operations_cancel_builder(client, name, body)?;
    developerconnect_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_operations_delete_execute()` to send, or `developerconnect_projects_locations_operations_delete` for simplest API.

pub fn developerconnect_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_operations_delete_builder()` + `developerconnect_projects_locations_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_operations_delete_builder(client, name)?;
    developerconnect_projects_locations_operations_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_operations_get_execute()` to send, or `developerconnect_projects_locations_operations_get` for simplest API.

pub fn developerconnect_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_operations_get_builder()` + `developerconnect_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_operations_get_builder(client, name)?;
    developerconnect_projects_locations_operations_get_execute(builder)
}

/// GET v1/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `developerconnect_projects_locations_operations_list_execute()` to send, or `developerconnect_projects_locations_operations_list` for simplest API.

pub fn developerconnect_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/operations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `developerconnect_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from developerconnect_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `developerconnect_projects_locations_operations_list_builder()` + `developerconnect_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn developerconnect_projects_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = developerconnect_projects_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    developerconnect_projects_locations_operations_list_execute(builder)
}
