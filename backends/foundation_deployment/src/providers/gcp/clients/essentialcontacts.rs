//! Auto-generated API clients for gcp/essentialcontacts.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/folders/{foldersId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_folders_contacts_compute_execute()` to send, or `essentialcontacts_folders_contacts_compute` for simplest API.

pub fn essentialcontacts_folders_contacts_compute_builder(
    client: &SimpleHttpClient,
    parent: String,
    notificationCategories: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://essentialcontacts.googleapis.com/v1/folders/{}/contacts:compute",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = notificationCategories {
        query_parts.push(format!("notificationCategories={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `essentialcontacts_folders_contacts_compute_execute()` or `essentialcontacts_folders_contacts_compute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_folders_contacts_compute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_compute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1ComputeContactsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `essentialcontacts_folders_contacts_compute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `essentialcontacts_folders_contacts_compute_task()`.
/// For the simplest API, use `essentialcontacts_folders_contacts_compute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_folders_contacts_compute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn essentialcontacts_folders_contacts_compute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = essentialcontacts_folders_contacts_compute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_folders_contacts_compute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsFoldersContactsComputeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: notificationCategories
    pub notificationCategories: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_folders_contacts_compute_builder()` + `essentialcontacts_folders_contacts_compute_execute()`.
/// For task-level control, use `essentialcontacts_folders_contacts_compute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_compute(
    client: &SimpleHttpClient,
    args: &EssentialcontactsFoldersContactsComputeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_folders_contacts_compute_builder(
        client,
        args.parent.clone(),
        args.notificationCategories.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    essentialcontacts_folders_contacts_compute_execute(builder)
}

/// GET v1/folders/{foldersId}/contacts
/// Adds a new contact for a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_folders_contacts_create_execute()` to send, or `essentialcontacts_folders_contacts_create` for simplest API.

pub fn essentialcontacts_folders_contacts_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudEssentialcontactsV1Contact,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://essentialcontacts.googleapis.com/v1/folders/{}/contacts",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/contacts
/// Adds a new contact for a resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `essentialcontacts_folders_contacts_create_execute()` or `essentialcontacts_folders_contacts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_folders_contacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1Contact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/contacts
/// Adds a new contact for a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `essentialcontacts_folders_contacts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `essentialcontacts_folders_contacts_create_task()`.
/// For the simplest API, use `essentialcontacts_folders_contacts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_folders_contacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn essentialcontacts_folders_contacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = essentialcontacts_folders_contacts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_folders_contacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsFoldersContactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1Contact,
}

/// GET v1/folders/{foldersId}/contacts
/// Adds a new contact for a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_folders_contacts_create_builder()` + `essentialcontacts_folders_contacts_create_execute()`.
/// For task-level control, use `essentialcontacts_folders_contacts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_create(
    client: &SimpleHttpClient,
    args: &EssentialcontactsFoldersContactsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        essentialcontacts_folders_contacts_create_builder(client, args.parent.clone(), &args.body)?;
    essentialcontacts_folders_contacts_create_execute(builder)
}

/// GET v1/folders/{foldersId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_folders_contacts_send_test_message_execute()` to send, or `essentialcontacts_folders_contacts_send_test_message` for simplest API.

pub fn essentialcontacts_folders_contacts_send_test_message_builder(
    client: &SimpleHttpClient,
    resource: String,
    body: &GoogleCloudEssentialcontactsV1SendTestMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://essentialcontacts.googleapis.com/v1/folders/{}/contacts:sendTestMessage",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `essentialcontacts_folders_contacts_send_test_message_execute()` or `essentialcontacts_folders_contacts_send_test_message`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_folders_contacts_send_test_message_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_send_test_message_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `essentialcontacts_folders_contacts_send_test_message_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `essentialcontacts_folders_contacts_send_test_message_task()`.
/// For the simplest API, use `essentialcontacts_folders_contacts_send_test_message()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_folders_contacts_send_test_message_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn essentialcontacts_folders_contacts_send_test_message_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = essentialcontacts_folders_contacts_send_test_message_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_folders_contacts_send_test_message`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsFoldersContactsSendTestMessageArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1SendTestMessageRequest,
}

/// GET v1/folders/{foldersId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_folders_contacts_send_test_message_builder()` + `essentialcontacts_folders_contacts_send_test_message_execute()`.
/// For task-level control, use `essentialcontacts_folders_contacts_send_test_message_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_send_test_message(
    client: &SimpleHttpClient,
    args: &EssentialcontactsFoldersContactsSendTestMessageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_folders_contacts_send_test_message_builder(
        client,
        args.resource.clone(),
        &args.body,
    )?;
    essentialcontacts_folders_contacts_send_test_message_execute(builder)
}

/// GET v1/organizations/{organizationsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_organizations_contacts_compute_execute()` to send, or `essentialcontacts_organizations_contacts_compute` for simplest API.

pub fn essentialcontacts_organizations_contacts_compute_builder(
    client: &SimpleHttpClient,
    parent: String,
    notificationCategories: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://essentialcontacts.googleapis.com/v1/organizations/{}/contacts:compute",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = notificationCategories {
        query_parts.push(format!("notificationCategories={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `essentialcontacts_organizations_contacts_compute_execute()` or `essentialcontacts_organizations_contacts_compute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_organizations_contacts_compute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_compute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1ComputeContactsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `essentialcontacts_organizations_contacts_compute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `essentialcontacts_organizations_contacts_compute_task()`.
/// For the simplest API, use `essentialcontacts_organizations_contacts_compute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_organizations_contacts_compute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn essentialcontacts_organizations_contacts_compute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = essentialcontacts_organizations_contacts_compute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_organizations_contacts_compute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsOrganizationsContactsComputeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: notificationCategories
    pub notificationCategories: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_organizations_contacts_compute_builder()` + `essentialcontacts_organizations_contacts_compute_execute()`.
/// For task-level control, use `essentialcontacts_organizations_contacts_compute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_compute(
    client: &SimpleHttpClient,
    args: &EssentialcontactsOrganizationsContactsComputeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_organizations_contacts_compute_builder(
        client,
        args.parent.clone(),
        args.notificationCategories.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    essentialcontacts_organizations_contacts_compute_execute(builder)
}

/// GET v1/organizations/{organizationsId}/contacts
/// Adds a new contact for a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_organizations_contacts_create_execute()` to send, or `essentialcontacts_organizations_contacts_create` for simplest API.

pub fn essentialcontacts_organizations_contacts_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudEssentialcontactsV1Contact,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://essentialcontacts.googleapis.com/v1/organizations/{}/contacts",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/contacts
/// Adds a new contact for a resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `essentialcontacts_organizations_contacts_create_execute()` or `essentialcontacts_organizations_contacts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_organizations_contacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1Contact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/contacts
/// Adds a new contact for a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `essentialcontacts_organizations_contacts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `essentialcontacts_organizations_contacts_create_task()`.
/// For the simplest API, use `essentialcontacts_organizations_contacts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_organizations_contacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn essentialcontacts_organizations_contacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = essentialcontacts_organizations_contacts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_organizations_contacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsOrganizationsContactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1Contact,
}

/// GET v1/organizations/{organizationsId}/contacts
/// Adds a new contact for a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_organizations_contacts_create_builder()` + `essentialcontacts_organizations_contacts_create_execute()`.
/// For task-level control, use `essentialcontacts_organizations_contacts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_create(
    client: &SimpleHttpClient,
    args: &EssentialcontactsOrganizationsContactsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_organizations_contacts_create_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    essentialcontacts_organizations_contacts_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_organizations_contacts_send_test_message_execute()` to send, or `essentialcontacts_organizations_contacts_send_test_message` for simplest API.

pub fn essentialcontacts_organizations_contacts_send_test_message_builder(
    client: &SimpleHttpClient,
    resource: String,
    body: &GoogleCloudEssentialcontactsV1SendTestMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://essentialcontacts.googleapis.com/v1/organizations/{}/contacts:sendTestMessage",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `essentialcontacts_organizations_contacts_send_test_message_execute()` or `essentialcontacts_organizations_contacts_send_test_message`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_organizations_contacts_send_test_message_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_send_test_message_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `essentialcontacts_organizations_contacts_send_test_message_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `essentialcontacts_organizations_contacts_send_test_message_task()`.
/// For the simplest API, use `essentialcontacts_organizations_contacts_send_test_message()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_organizations_contacts_send_test_message_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn essentialcontacts_organizations_contacts_send_test_message_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = essentialcontacts_organizations_contacts_send_test_message_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_organizations_contacts_send_test_message`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsOrganizationsContactsSendTestMessageArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1SendTestMessageRequest,
}

/// GET v1/organizations/{organizationsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_organizations_contacts_send_test_message_builder()` + `essentialcontacts_organizations_contacts_send_test_message_execute()`.
/// For task-level control, use `essentialcontacts_organizations_contacts_send_test_message_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_send_test_message(
    client: &SimpleHttpClient,
    args: &EssentialcontactsOrganizationsContactsSendTestMessageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_organizations_contacts_send_test_message_builder(
        client,
        args.resource.clone(),
        &args.body,
    )?;
    essentialcontacts_organizations_contacts_send_test_message_execute(builder)
}

/// GET v1/projects/{projectsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_projects_contacts_compute_execute()` to send, or `essentialcontacts_projects_contacts_compute` for simplest API.

pub fn essentialcontacts_projects_contacts_compute_builder(
    client: &SimpleHttpClient,
    parent: String,
    notificationCategories: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://essentialcontacts.googleapis.com/v1/projects/{}/contacts:compute",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = notificationCategories {
        query_parts.push(format!("notificationCategories={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `essentialcontacts_projects_contacts_compute_execute()` or `essentialcontacts_projects_contacts_compute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_projects_contacts_compute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_compute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1ComputeContactsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `essentialcontacts_projects_contacts_compute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `essentialcontacts_projects_contacts_compute_task()`.
/// For the simplest API, use `essentialcontacts_projects_contacts_compute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_projects_contacts_compute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn essentialcontacts_projects_contacts_compute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = essentialcontacts_projects_contacts_compute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_projects_contacts_compute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsProjectsContactsComputeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: notificationCategories
    pub notificationCategories: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_projects_contacts_compute_builder()` + `essentialcontacts_projects_contacts_compute_execute()`.
/// For task-level control, use `essentialcontacts_projects_contacts_compute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_compute(
    client: &SimpleHttpClient,
    args: &EssentialcontactsProjectsContactsComputeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_projects_contacts_compute_builder(
        client,
        args.parent.clone(),
        args.notificationCategories.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    essentialcontacts_projects_contacts_compute_execute(builder)
}

/// GET v1/projects/{projectsId}/contacts
/// Adds a new contact for a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_projects_contacts_create_execute()` to send, or `essentialcontacts_projects_contacts_create` for simplest API.

pub fn essentialcontacts_projects_contacts_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudEssentialcontactsV1Contact,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://essentialcontacts.googleapis.com/v1/projects/{}/contacts",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/contacts
/// Adds a new contact for a resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `essentialcontacts_projects_contacts_create_execute()` or `essentialcontacts_projects_contacts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_projects_contacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1Contact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/contacts
/// Adds a new contact for a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `essentialcontacts_projects_contacts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `essentialcontacts_projects_contacts_create_task()`.
/// For the simplest API, use `essentialcontacts_projects_contacts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_projects_contacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn essentialcontacts_projects_contacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = essentialcontacts_projects_contacts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_projects_contacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsProjectsContactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1Contact,
}

/// GET v1/projects/{projectsId}/contacts
/// Adds a new contact for a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_projects_contacts_create_builder()` + `essentialcontacts_projects_contacts_create_execute()`.
/// For task-level control, use `essentialcontacts_projects_contacts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_create(
    client: &SimpleHttpClient,
    args: &EssentialcontactsProjectsContactsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_projects_contacts_create_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    essentialcontacts_projects_contacts_create_execute(builder)
}

/// GET v1/projects/{projectsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_projects_contacts_send_test_message_execute()` to send, or `essentialcontacts_projects_contacts_send_test_message` for simplest API.

pub fn essentialcontacts_projects_contacts_send_test_message_builder(
    client: &SimpleHttpClient,
    resource: String,
    body: &GoogleCloudEssentialcontactsV1SendTestMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://essentialcontacts.googleapis.com/v1/projects/{}/contacts:sendTestMessage",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `essentialcontacts_projects_contacts_send_test_message_execute()` or `essentialcontacts_projects_contacts_send_test_message`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_projects_contacts_send_test_message_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_send_test_message_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `essentialcontacts_projects_contacts_send_test_message_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `essentialcontacts_projects_contacts_send_test_message_task()`.
/// For the simplest API, use `essentialcontacts_projects_contacts_send_test_message()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `essentialcontacts_projects_contacts_send_test_message_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn essentialcontacts_projects_contacts_send_test_message_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = essentialcontacts_projects_contacts_send_test_message_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_projects_contacts_send_test_message`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsProjectsContactsSendTestMessageArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1SendTestMessageRequest,
}

/// GET v1/projects/{projectsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_projects_contacts_send_test_message_builder()` + `essentialcontacts_projects_contacts_send_test_message_execute()`.
/// For task-level control, use `essentialcontacts_projects_contacts_send_test_message_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_send_test_message(
    client: &SimpleHttpClient,
    args: &EssentialcontactsProjectsContactsSendTestMessageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_projects_contacts_send_test_message_builder(
        client,
        args.resource.clone(),
        &args.body,
    )?;
    essentialcontacts_projects_contacts_send_test_message_execute(builder)
}
