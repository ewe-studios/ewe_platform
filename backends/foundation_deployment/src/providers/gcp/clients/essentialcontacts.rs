//! Auto-generated API clients for gcp/essentialcontacts.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/essentialcontacts`

#![cfg(feature = "gcp/essentialcontacts")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/folders/{foldersId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_folders_contacts_compute_execute()` to send, or `essentialcontacts_folders_contacts_compute` for simplest API.

pub fn essentialcontacts_folders_contacts_compute_builder(
    client: &SimpleHttpClient,
    parent: &str,
    notificationCategories: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/folders/{}/contacts:compute",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = notificationCategories {
        query_parts.push(format!("notificationCategories={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_folders_contacts_compute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_folders_contacts_compute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_compute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1ComputeContactsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_folders_contacts_compute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsFoldersContactsComputeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: notificationCategories
    pub notificationCategories: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_folders_contacts_compute_builder()` + `essentialcontacts_folders_contacts_compute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_compute(
    client: &SimpleHttpClient,
    args: &EssentialcontactsFoldersContactsComputeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_folders_contacts_compute_builder(
        client,
        &args.parent,
        args.notificationCategories.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    essentialcontacts_folders_contacts_compute_execute(builder)
}

/// GET v1/folders/{foldersId}/contacts
/// Adds a new contact for a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_folders_contacts_create_execute()` to send, or `essentialcontacts_folders_contacts_create` for simplest API.

pub fn essentialcontacts_folders_contacts_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudEssentialcontactsV1Contact,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/folders/{}/contacts",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/contacts
/// Adds a new contact for a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_folders_contacts_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_folders_contacts_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1Contact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_folders_contacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsFoldersContactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1Contact,
}

/// GET v1/folders/{foldersId}/contacts
/// Adds a new contact for a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_folders_contacts_create_builder()` + `essentialcontacts_folders_contacts_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_create(
    client: &SimpleHttpClient,
    args: &EssentialcontactsFoldersContactsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        essentialcontacts_folders_contacts_create_builder(client, &args.parent, &args.body)?;
    essentialcontacts_folders_contacts_create_execute(builder)
}

/// GET v1/folders/{foldersId}/contacts/{contactsId}
/// Deletes a contact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_folders_contacts_delete_execute()` to send, or `essentialcontacts_folders_contacts_delete` for simplest API.

pub fn essentialcontacts_folders_contacts_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/folders/{}/contacts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/contacts/{contactsId}
/// Deletes a contact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_folders_contacts_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_folders_contacts_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_folders_contacts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsFoldersContactsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/contacts/{contactsId}
/// Deletes a contact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_folders_contacts_delete_builder()` + `essentialcontacts_folders_contacts_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_delete(
    client: &SimpleHttpClient,
    args: &EssentialcontactsFoldersContactsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_folders_contacts_delete_builder(client, &args.name)?;
    essentialcontacts_folders_contacts_delete_execute(builder)
}

/// GET v1/folders/{foldersId}/contacts/{contactsId}
/// Gets a single contact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_folders_contacts_get_execute()` to send, or `essentialcontacts_folders_contacts_get` for simplest API.

pub fn essentialcontacts_folders_contacts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/folders/{}/contacts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/contacts/{contactsId}
/// Gets a single contact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_folders_contacts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_folders_contacts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1Contact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_folders_contacts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsFoldersContactsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/contacts/{contactsId}
/// Gets a single contact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_folders_contacts_get_builder()` + `essentialcontacts_folders_contacts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_get(
    client: &SimpleHttpClient,
    args: &EssentialcontactsFoldersContactsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_folders_contacts_get_builder(client, &args.name)?;
    essentialcontacts_folders_contacts_get_execute(builder)
}

/// GET v1/folders/{foldersId}/contacts
/// Lists the contacts that have been set on a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_folders_contacts_list_execute()` to send, or `essentialcontacts_folders_contacts_list` for simplest API.

pub fn essentialcontacts_folders_contacts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/folders/{}/contacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/contacts
/// Lists the contacts that have been set on a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_folders_contacts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_folders_contacts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1ListContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1ListContactsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_folders_contacts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsFoldersContactsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/contacts
/// Lists the contacts that have been set on a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_folders_contacts_list_builder()` + `essentialcontacts_folders_contacts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_list(
    client: &SimpleHttpClient,
    args: &EssentialcontactsFoldersContactsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1ListContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_folders_contacts_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    essentialcontacts_folders_contacts_list_execute(builder)
}

/// GET v1/folders/{foldersId}/contacts/{contactsId}
/// Updates a contact. Note: A contact's email address cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_folders_contacts_patch_execute()` to send, or `essentialcontacts_folders_contacts_patch` for simplest API.

pub fn essentialcontacts_folders_contacts_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudEssentialcontactsV1Contact,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/folders/{}/contacts/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/contacts/{contactsId}
/// Updates a contact. Note: A contact's email address cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_folders_contacts_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_folders_contacts_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1Contact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_folders_contacts_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsFoldersContactsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1Contact,
}

/// GET v1/folders/{foldersId}/contacts/{contactsId}
/// Updates a contact. Note: A contact's email address cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_folders_contacts_patch_builder()` + `essentialcontacts_folders_contacts_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_patch(
    client: &SimpleHttpClient,
    args: &EssentialcontactsFoldersContactsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_folders_contacts_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    essentialcontacts_folders_contacts_patch_execute(builder)
}

/// GET v1/folders/{foldersId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_folders_contacts_send_test_message_execute()` to send, or `essentialcontacts_folders_contacts_send_test_message` for simplest API.

pub fn essentialcontacts_folders_contacts_send_test_message_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleCloudEssentialcontactsV1SendTestMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/folders/{}/contacts:sendTestMessage",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_folders_contacts_send_test_message_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_folders_contacts_send_test_message_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_send_test_message_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_folders_contacts_send_test_message`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsFoldersContactsSendTestMessageArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1SendTestMessageRequest,
}

/// GET v1/folders/{foldersId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_folders_contacts_send_test_message_builder()` + `essentialcontacts_folders_contacts_send_test_message_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_folders_contacts_send_test_message(
    client: &SimpleHttpClient,
    args: &EssentialcontactsFoldersContactsSendTestMessageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_folders_contacts_send_test_message_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    essentialcontacts_folders_contacts_send_test_message_execute(builder)
}

/// GET v1/organizations/{organizationsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_organizations_contacts_compute_execute()` to send, or `essentialcontacts_organizations_contacts_compute` for simplest API.

pub fn essentialcontacts_organizations_contacts_compute_builder(
    client: &SimpleHttpClient,
    parent: &str,
    notificationCategories: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/organizations/{}/contacts:compute",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = notificationCategories {
        query_parts.push(format!("notificationCategories={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_organizations_contacts_compute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_organizations_contacts_compute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_compute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1ComputeContactsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_organizations_contacts_compute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsOrganizationsContactsComputeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: notificationCategories
    pub notificationCategories: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_organizations_contacts_compute_builder()` + `essentialcontacts_organizations_contacts_compute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_compute(
    client: &SimpleHttpClient,
    args: &EssentialcontactsOrganizationsContactsComputeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_organizations_contacts_compute_builder(
        client,
        &args.parent,
        args.notificationCategories.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    essentialcontacts_organizations_contacts_compute_execute(builder)
}

/// GET v1/organizations/{organizationsId}/contacts
/// Adds a new contact for a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_organizations_contacts_create_execute()` to send, or `essentialcontacts_organizations_contacts_create` for simplest API.

pub fn essentialcontacts_organizations_contacts_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudEssentialcontactsV1Contact,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/organizations/{}/contacts",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/contacts
/// Adds a new contact for a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_organizations_contacts_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_organizations_contacts_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1Contact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_organizations_contacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsOrganizationsContactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1Contact,
}

/// GET v1/organizations/{organizationsId}/contacts
/// Adds a new contact for a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_organizations_contacts_create_builder()` + `essentialcontacts_organizations_contacts_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_create(
    client: &SimpleHttpClient,
    args: &EssentialcontactsOrganizationsContactsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        essentialcontacts_organizations_contacts_create_builder(client, &args.parent, &args.body)?;
    essentialcontacts_organizations_contacts_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/contacts/{contactsId}
/// Deletes a contact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_organizations_contacts_delete_execute()` to send, or `essentialcontacts_organizations_contacts_delete` for simplest API.

pub fn essentialcontacts_organizations_contacts_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/organizations/{}/contacts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/contacts/{contactsId}
/// Deletes a contact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_organizations_contacts_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_organizations_contacts_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_organizations_contacts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsOrganizationsContactsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/contacts/{contactsId}
/// Deletes a contact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_organizations_contacts_delete_builder()` + `essentialcontacts_organizations_contacts_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_delete(
    client: &SimpleHttpClient,
    args: &EssentialcontactsOrganizationsContactsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_organizations_contacts_delete_builder(client, &args.name)?;
    essentialcontacts_organizations_contacts_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/contacts/{contactsId}
/// Gets a single contact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_organizations_contacts_get_execute()` to send, or `essentialcontacts_organizations_contacts_get` for simplest API.

pub fn essentialcontacts_organizations_contacts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/organizations/{}/contacts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/contacts/{contactsId}
/// Gets a single contact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_organizations_contacts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_organizations_contacts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1Contact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_organizations_contacts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsOrganizationsContactsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/contacts/{contactsId}
/// Gets a single contact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_organizations_contacts_get_builder()` + `essentialcontacts_organizations_contacts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_get(
    client: &SimpleHttpClient,
    args: &EssentialcontactsOrganizationsContactsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_organizations_contacts_get_builder(client, &args.name)?;
    essentialcontacts_organizations_contacts_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/contacts
/// Lists the contacts that have been set on a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_organizations_contacts_list_execute()` to send, or `essentialcontacts_organizations_contacts_list` for simplest API.

pub fn essentialcontacts_organizations_contacts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/organizations/{}/contacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/contacts
/// Lists the contacts that have been set on a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_organizations_contacts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_organizations_contacts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1ListContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1ListContactsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_organizations_contacts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsOrganizationsContactsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/contacts
/// Lists the contacts that have been set on a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_organizations_contacts_list_builder()` + `essentialcontacts_organizations_contacts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_list(
    client: &SimpleHttpClient,
    args: &EssentialcontactsOrganizationsContactsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1ListContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_organizations_contacts_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    essentialcontacts_organizations_contacts_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/contacts/{contactsId}
/// Updates a contact. Note: A contact's email address cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_organizations_contacts_patch_execute()` to send, or `essentialcontacts_organizations_contacts_patch` for simplest API.

pub fn essentialcontacts_organizations_contacts_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudEssentialcontactsV1Contact,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/organizations/{}/contacts/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/contacts/{contactsId}
/// Updates a contact. Note: A contact's email address cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_organizations_contacts_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_organizations_contacts_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1Contact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_organizations_contacts_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsOrganizationsContactsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1Contact,
}

/// GET v1/organizations/{organizationsId}/contacts/{contactsId}
/// Updates a contact. Note: A contact's email address cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_organizations_contacts_patch_builder()` + `essentialcontacts_organizations_contacts_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_patch(
    client: &SimpleHttpClient,
    args: &EssentialcontactsOrganizationsContactsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_organizations_contacts_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    essentialcontacts_organizations_contacts_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_organizations_contacts_send_test_message_execute()` to send, or `essentialcontacts_organizations_contacts_send_test_message` for simplest API.

pub fn essentialcontacts_organizations_contacts_send_test_message_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleCloudEssentialcontactsV1SendTestMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/organizations/{}/contacts:sendTestMessage",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_organizations_contacts_send_test_message_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_organizations_contacts_send_test_message_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_send_test_message_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_organizations_contacts_send_test_message`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsOrganizationsContactsSendTestMessageArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1SendTestMessageRequest,
}

/// GET v1/organizations/{organizationsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_organizations_contacts_send_test_message_builder()` + `essentialcontacts_organizations_contacts_send_test_message_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_organizations_contacts_send_test_message(
    client: &SimpleHttpClient,
    args: &EssentialcontactsOrganizationsContactsSendTestMessageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_organizations_contacts_send_test_message_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    essentialcontacts_organizations_contacts_send_test_message_execute(builder)
}

/// GET v1/projects/{projectsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_projects_contacts_compute_execute()` to send, or `essentialcontacts_projects_contacts_compute` for simplest API.

pub fn essentialcontacts_projects_contacts_compute_builder(
    client: &SimpleHttpClient,
    parent: &str,
    notificationCategories: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/projects/{}/contacts:compute",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = notificationCategories {
        query_parts.push(format!("notificationCategories={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_projects_contacts_compute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_projects_contacts_compute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_compute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1ComputeContactsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_projects_contacts_compute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsProjectsContactsComputeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: notificationCategories
    pub notificationCategories: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/contacts:compute
/// Lists all contacts for the resource that are subscribed to the specified notification categories, including contacts inherited from any parent resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_projects_contacts_compute_builder()` + `essentialcontacts_projects_contacts_compute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_compute(
    client: &SimpleHttpClient,
    args: &EssentialcontactsProjectsContactsComputeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudEssentialcontactsV1ComputeContactsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_projects_contacts_compute_builder(
        client,
        &args.parent,
        args.notificationCategories.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    essentialcontacts_projects_contacts_compute_execute(builder)
}

/// GET v1/projects/{projectsId}/contacts
/// Adds a new contact for a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_projects_contacts_create_execute()` to send, or `essentialcontacts_projects_contacts_create` for simplest API.

pub fn essentialcontacts_projects_contacts_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudEssentialcontactsV1Contact,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/projects/{}/contacts",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/contacts
/// Adds a new contact for a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_projects_contacts_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_projects_contacts_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1Contact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_projects_contacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsProjectsContactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1Contact,
}

/// GET v1/projects/{projectsId}/contacts
/// Adds a new contact for a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_projects_contacts_create_builder()` + `essentialcontacts_projects_contacts_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_create(
    client: &SimpleHttpClient,
    args: &EssentialcontactsProjectsContactsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        essentialcontacts_projects_contacts_create_builder(client, &args.parent, &args.body)?;
    essentialcontacts_projects_contacts_create_execute(builder)
}

/// GET v1/projects/{projectsId}/contacts/{contactsId}
/// Deletes a contact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_projects_contacts_delete_execute()` to send, or `essentialcontacts_projects_contacts_delete` for simplest API.

pub fn essentialcontacts_projects_contacts_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/projects/{}/contacts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/contacts/{contactsId}
/// Deletes a contact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_projects_contacts_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_projects_contacts_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_projects_contacts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsProjectsContactsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/contacts/{contactsId}
/// Deletes a contact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_projects_contacts_delete_builder()` + `essentialcontacts_projects_contacts_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_delete(
    client: &SimpleHttpClient,
    args: &EssentialcontactsProjectsContactsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_projects_contacts_delete_builder(client, &args.name)?;
    essentialcontacts_projects_contacts_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/contacts/{contactsId}
/// Gets a single contact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_projects_contacts_get_execute()` to send, or `essentialcontacts_projects_contacts_get` for simplest API.

pub fn essentialcontacts_projects_contacts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/projects/{}/contacts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/contacts/{contactsId}
/// Gets a single contact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_projects_contacts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_projects_contacts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1Contact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_projects_contacts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsProjectsContactsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/contacts/{contactsId}
/// Gets a single contact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_projects_contacts_get_builder()` + `essentialcontacts_projects_contacts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_get(
    client: &SimpleHttpClient,
    args: &EssentialcontactsProjectsContactsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_projects_contacts_get_builder(client, &args.name)?;
    essentialcontacts_projects_contacts_get_execute(builder)
}

/// GET v1/projects/{projectsId}/contacts
/// Lists the contacts that have been set on a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_projects_contacts_list_execute()` to send, or `essentialcontacts_projects_contacts_list` for simplest API.

pub fn essentialcontacts_projects_contacts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/projects/{}/contacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/contacts
/// Lists the contacts that have been set on a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_projects_contacts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_projects_contacts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1ListContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1ListContactsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_projects_contacts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsProjectsContactsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/contacts
/// Lists the contacts that have been set on a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_projects_contacts_list_builder()` + `essentialcontacts_projects_contacts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_list(
    client: &SimpleHttpClient,
    args: &EssentialcontactsProjectsContactsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1ListContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_projects_contacts_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    essentialcontacts_projects_contacts_list_execute(builder)
}

/// GET v1/projects/{projectsId}/contacts/{contactsId}
/// Updates a contact. Note: A contact's email address cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_projects_contacts_patch_execute()` to send, or `essentialcontacts_projects_contacts_patch` for simplest API.

pub fn essentialcontacts_projects_contacts_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudEssentialcontactsV1Contact,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/projects/{}/contacts/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/contacts/{contactsId}
/// Updates a contact. Note: A contact's email address cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_projects_contacts_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_projects_contacts_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudEssentialcontactsV1Contact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_projects_contacts_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsProjectsContactsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1Contact,
}

/// GET v1/projects/{projectsId}/contacts/{contactsId}
/// Updates a contact. Note: A contact's email address cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_projects_contacts_patch_builder()` + `essentialcontacts_projects_contacts_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_patch(
    client: &SimpleHttpClient,
    args: &EssentialcontactsProjectsContactsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudEssentialcontactsV1Contact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_projects_contacts_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    essentialcontacts_projects_contacts_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `essentialcontacts_projects_contacts_send_test_message_execute()` to send, or `essentialcontacts_projects_contacts_send_test_message` for simplest API.

pub fn essentialcontacts_projects_contacts_send_test_message_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleCloudEssentialcontactsV1SendTestMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://essentialcontacts.googleapis.com/v1/projects/{}/contacts:sendTestMessage",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `essentialcontacts_projects_contacts_send_test_message_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from essentialcontacts_projects_contacts_send_test_message_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_send_test_message_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`essentialcontacts_projects_contacts_send_test_message`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EssentialcontactsProjectsContactsSendTestMessageArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleCloudEssentialcontactsV1SendTestMessageRequest,
}

/// GET v1/projects/{projectsId}/contacts:sendTestMessage
/// Allows a contact admin to send a test message to contact to verify that it has been configured correctly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `essentialcontacts_projects_contacts_send_test_message_builder()` + `essentialcontacts_projects_contacts_send_test_message_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn essentialcontacts_projects_contacts_send_test_message(
    client: &SimpleHttpClient,
    args: &EssentialcontactsProjectsContactsSendTestMessageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = essentialcontacts_projects_contacts_send_test_message_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    essentialcontacts_projects_contacts_send_test_message_execute(builder)
}
