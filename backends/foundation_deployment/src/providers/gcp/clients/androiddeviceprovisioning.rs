//! Auto-generated API clients for gcp/androiddeviceprovisioning.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/customers
/// Lists the user's customer accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_list_execute()` to send, or `androiddeviceprovisioning_customers_list` for simplest API.

pub fn androiddeviceprovisioning_customers_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://androiddeviceprovisioning.googleapis.com/v1/customers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers
/// Lists the user's customer accounts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_customers_list_execute()` or `androiddeviceprovisioning_customers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomerListCustomersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomerListCustomersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers
/// Lists the user's customer accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_customers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_customers_list_task()`.
/// For the simplest API, use `androiddeviceprovisioning_customers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_customers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CustomerListCustomersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_customers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_customers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningCustomersListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/customers
/// Lists the user's customer accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_list_builder()` + `androiddeviceprovisioning_customers_list_execute()`.
/// For task-level control, use `androiddeviceprovisioning_customers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_list(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningCustomersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CustomerListCustomersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_customers_list_builder(
        client,
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    androiddeviceprovisioning_customers_list_execute(builder)
}

/// GET v1/customers/{customersId}/configurations
/// Creates a new configuration. Once created, a customer can apply the configuration to devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_configurations_create_execute()` to send, or `androiddeviceprovisioning_customers_configurations_create` for simplest API.

pub fn androiddeviceprovisioning_customers_configurations_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &Configuration,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androiddeviceprovisioning.googleapis.com/v1/customers/{}/configurations",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers/{customersId}/configurations
/// Creates a new configuration. Once created, a customer can apply the configuration to devices.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_customers_configurations_create_execute()` or `androiddeviceprovisioning_customers_configurations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_configurations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_configurations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Configuration>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Configuration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/configurations
/// Creates a new configuration. Once created, a customer can apply the configuration to devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_customers_configurations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_customers_configurations_create_task()`.
/// For the simplest API, use `androiddeviceprovisioning_customers_configurations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_configurations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_customers_configurations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Configuration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_customers_configurations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_customers_configurations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningCustomersConfigurationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Configuration,
}

/// GET v1/customers/{customersId}/configurations
/// Creates a new configuration. Once created, a customer can apply the configuration to devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_configurations_create_builder()` + `androiddeviceprovisioning_customers_configurations_create_execute()`.
/// For task-level control, use `androiddeviceprovisioning_customers_configurations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_configurations_create(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningCustomersConfigurationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Configuration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_customers_configurations_create_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_customers_configurations_create_execute(builder)
}

/// GET v1/customers/{customersId}/devices:applyConfiguration
/// Applies a Configuration to the device to register the device for zero-touch enrollment. After applying a configuration to a device, the device automatically provisions itself on first boot, or next factory reset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_devices_apply_configuration_execute()` to send, or `androiddeviceprovisioning_customers_devices_apply_configuration` for simplest API.

pub fn androiddeviceprovisioning_customers_devices_apply_configuration_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &CustomerApplyConfigurationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androiddeviceprovisioning.googleapis.com/v1/customers/{}/devices:applyConfiguration",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers/{customersId}/devices:applyConfiguration
/// Applies a Configuration to the device to register the device for zero-touch enrollment. After applying a configuration to a device, the device automatically provisions itself on first boot, or next factory reset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_customers_devices_apply_configuration_execute()` or `androiddeviceprovisioning_customers_devices_apply_configuration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_devices_apply_configuration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_apply_configuration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/devices:applyConfiguration
/// Applies a Configuration to the device to register the device for zero-touch enrollment. After applying a configuration to a device, the device automatically provisions itself on first boot, or next factory reset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_customers_devices_apply_configuration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_customers_devices_apply_configuration_task()`.
/// For the simplest API, use `androiddeviceprovisioning_customers_devices_apply_configuration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_devices_apply_configuration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_customers_devices_apply_configuration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_customers_devices_apply_configuration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_customers_devices_apply_configuration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningCustomersDevicesApplyConfigurationArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: CustomerApplyConfigurationRequest,
}

/// GET v1/customers/{customersId}/devices:applyConfiguration
/// Applies a Configuration to the device to register the device for zero-touch enrollment. After applying a configuration to a device, the device automatically provisions itself on first boot, or next factory reset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_devices_apply_configuration_builder()` + `androiddeviceprovisioning_customers_devices_apply_configuration_execute()`.
/// For task-level control, use `androiddeviceprovisioning_customers_devices_apply_configuration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_apply_configuration(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningCustomersDevicesApplyConfigurationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_customers_devices_apply_configuration_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_customers_devices_apply_configuration_execute(builder)
}

/// GET v1/customers/{customersId}/devices
/// Lists a customer's devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_devices_list_execute()` to send, or `androiddeviceprovisioning_customers_devices_list` for simplest API.

pub fn androiddeviceprovisioning_customers_devices_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<String>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androiddeviceprovisioning.googleapis.com/v1/customers/{}/devices",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/devices
/// Lists a customer's devices.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_customers_devices_list_execute()` or `androiddeviceprovisioning_customers_devices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomerListDevicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomerListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/devices
/// Lists a customer's devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_customers_devices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_customers_devices_list_task()`.
/// For the simplest API, use `androiddeviceprovisioning_customers_devices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_customers_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CustomerListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_customers_devices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_customers_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningCustomersDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/customers/{customersId}/devices
/// Lists a customer's devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_devices_list_builder()` + `androiddeviceprovisioning_customers_devices_list_execute()`.
/// For task-level control, use `androiddeviceprovisioning_customers_devices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_list(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningCustomersDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CustomerListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_customers_devices_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    androiddeviceprovisioning_customers_devices_list_execute(builder)
}

/// GET v1/customers/{customersId}/devices:removeConfiguration
/// Removes a configuration from device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_devices_remove_configuration_execute()` to send, or `androiddeviceprovisioning_customers_devices_remove_configuration` for simplest API.

pub fn androiddeviceprovisioning_customers_devices_remove_configuration_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &CustomerRemoveConfigurationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androiddeviceprovisioning.googleapis.com/v1/customers/{}/devices:removeConfiguration",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers/{customersId}/devices:removeConfiguration
/// Removes a configuration from device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_customers_devices_remove_configuration_execute()` or `androiddeviceprovisioning_customers_devices_remove_configuration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_devices_remove_configuration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_remove_configuration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/devices:removeConfiguration
/// Removes a configuration from device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_customers_devices_remove_configuration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_customers_devices_remove_configuration_task()`.
/// For the simplest API, use `androiddeviceprovisioning_customers_devices_remove_configuration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_devices_remove_configuration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_customers_devices_remove_configuration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_customers_devices_remove_configuration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_customers_devices_remove_configuration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningCustomersDevicesRemoveConfigurationArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: CustomerRemoveConfigurationRequest,
}

/// GET v1/customers/{customersId}/devices:removeConfiguration
/// Removes a configuration from device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_devices_remove_configuration_builder()` + `androiddeviceprovisioning_customers_devices_remove_configuration_execute()`.
/// For task-level control, use `androiddeviceprovisioning_customers_devices_remove_configuration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_remove_configuration(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningCustomersDevicesRemoveConfigurationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_customers_devices_remove_configuration_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_customers_devices_remove_configuration_execute(builder)
}

/// GET v1/customers/{customersId}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment. After removing a device, a customer must contact their reseller to register the device into zero-touch enrollment again.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_devices_unclaim_execute()` to send, or `androiddeviceprovisioning_customers_devices_unclaim` for simplest API.

pub fn androiddeviceprovisioning_customers_devices_unclaim_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &CustomerUnclaimDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androiddeviceprovisioning.googleapis.com/v1/customers/{}/devices:unclaim",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers/{customersId}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment. After removing a device, a customer must contact their reseller to register the device into zero-touch enrollment again.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_customers_devices_unclaim_execute()` or `androiddeviceprovisioning_customers_devices_unclaim`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_devices_unclaim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_unclaim_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment. After removing a device, a customer must contact their reseller to register the device into zero-touch enrollment again.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_customers_devices_unclaim_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_customers_devices_unclaim_task()`.
/// For the simplest API, use `androiddeviceprovisioning_customers_devices_unclaim()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_devices_unclaim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_customers_devices_unclaim_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_customers_devices_unclaim_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_customers_devices_unclaim`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningCustomersDevicesUnclaimArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: CustomerUnclaimDeviceRequest,
}

/// GET v1/customers/{customersId}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment. After removing a device, a customer must contact their reseller to register the device into zero-touch enrollment again.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_devices_unclaim_builder()` + `androiddeviceprovisioning_customers_devices_unclaim_execute()`.
/// For task-level control, use `androiddeviceprovisioning_customers_devices_unclaim_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_unclaim(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningCustomersDevicesUnclaimArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_customers_devices_unclaim_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_customers_devices_unclaim_execute(builder)
}

/// GET v1/customers/{customersId}/dpcs
/// Lists the DPCs (device policy controllers) that support zero-touch enrollment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_dpcs_list_execute()` to send, or `androiddeviceprovisioning_customers_dpcs_list` for simplest API.

pub fn androiddeviceprovisioning_customers_dpcs_list_builder(
    client: &SimpleHttpClient,
    parent: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androiddeviceprovisioning.googleapis.com/v1/customers/{}/dpcs",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/dpcs
/// Lists the DPCs (device policy controllers) that support zero-touch enrollment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_customers_dpcs_list_execute()` or `androiddeviceprovisioning_customers_dpcs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_dpcs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_dpcs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomerListDpcsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomerListDpcsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/dpcs
/// Lists the DPCs (device policy controllers) that support zero-touch enrollment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_customers_dpcs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_customers_dpcs_list_task()`.
/// For the simplest API, use `androiddeviceprovisioning_customers_dpcs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_customers_dpcs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_customers_dpcs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomerListDpcsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_customers_dpcs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_customers_dpcs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningCustomersDpcsListArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/customers/{customersId}/dpcs
/// Lists the DPCs (device policy controllers) that support zero-touch enrollment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_dpcs_list_builder()` + `androiddeviceprovisioning_customers_dpcs_list_execute()`.
/// For task-level control, use `androiddeviceprovisioning_customers_dpcs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_dpcs_list(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningCustomersDpcsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomerListDpcsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androiddeviceprovisioning_customers_dpcs_list_builder(client, args.parent.clone())?;
    androiddeviceprovisioning_customers_dpcs_list_execute(builder)
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_operations_get_execute()` to send, or `androiddeviceprovisioning_operations_get` for simplest API.

pub fn androiddeviceprovisioning_operations_get_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androiddeviceprovisioning.googleapis.com/v1/operations/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_operations_get_execute()` or `androiddeviceprovisioning_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_operations_get_task()`.
/// For the simplest API, use `androiddeviceprovisioning_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_operations_get_builder()` + `androiddeviceprovisioning_operations_get_execute()`.
/// For task-level control, use `androiddeviceprovisioning_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_operations_get(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_operations_get_builder(client, args.name.clone())?;
    androiddeviceprovisioning_operations_get_execute(builder)
}

/// GET v1/partners/{partnersId}/customers
/// Creates a customer for zero-touch enrollment. After the method returns successfully, admin and owner roles can manage devices and EMM configs by calling API methods or using their zero-touch enrollment portal. The customer receives an email that welcomes them to zero-touch enrollment and explains how to sign into the portal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_customers_create_execute()` to send, or `androiddeviceprovisioning_partners_customers_create` for simplest API.

pub fn androiddeviceprovisioning_partners_customers_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &CreateCustomerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androiddeviceprovisioning.googleapis.com/v1/partners/{}/customers",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{partnersId}/customers
/// Creates a customer for zero-touch enrollment. After the method returns successfully, admin and owner roles can manage devices and EMM configs by calling API methods or using their zero-touch enrollment portal. The customer receives an email that welcomes them to zero-touch enrollment and explains how to sign into the portal.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_partners_customers_create_execute()` or `androiddeviceprovisioning_partners_customers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_customers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_customers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Company>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Company = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/partners/{partnersId}/customers
/// Creates a customer for zero-touch enrollment. After the method returns successfully, admin and owner roles can manage devices and EMM configs by calling API methods or using their zero-touch enrollment portal. The customer receives an email that welcomes them to zero-touch enrollment and explains how to sign into the portal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_partners_customers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_partners_customers_create_task()`.
/// For the simplest API, use `androiddeviceprovisioning_partners_customers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_customers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_partners_customers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Company>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_partners_customers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_partners_customers_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningPartnersCustomersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: CreateCustomerRequest,
}

/// GET v1/partners/{partnersId}/customers
/// Creates a customer for zero-touch enrollment. After the method returns successfully, admin and owner roles can manage devices and EMM configs by calling API methods or using their zero-touch enrollment portal. The customer receives an email that welcomes them to zero-touch enrollment and explains how to sign into the portal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_customers_create_builder()` + `androiddeviceprovisioning_partners_customers_create_execute()`.
/// For task-level control, use `androiddeviceprovisioning_partners_customers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_customers_create(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningPartnersCustomersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Company>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_partners_customers_create_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_partners_customers_create_execute(builder)
}

/// GET v1/partners/{partnersId}/devices:claim
/// Claims a device for a customer and adds it to zero-touch enrollment. If the device is already claimed by another customer, the call returns an error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_claim_execute()` to send, or `androiddeviceprovisioning_partners_devices_claim` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_claim_builder(
    client: &SimpleHttpClient,
    partnerId: String,
    body: &ClaimDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androiddeviceprovisioning.googleapis.com/v1/partners/{}/devices:claim",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{partnersId}/devices:claim
/// Claims a device for a customer and adds it to zero-touch enrollment. If the device is already claimed by another customer, the call returns an error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_partners_devices_claim_execute()` or `androiddeviceprovisioning_partners_devices_claim`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_claim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_claim_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClaimDeviceResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClaimDeviceResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/partners/{partnersId}/devices:claim
/// Claims a device for a customer and adds it to zero-touch enrollment. If the device is already claimed by another customer, the call returns an error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_partners_devices_claim_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_claim_task()`.
/// For the simplest API, use `androiddeviceprovisioning_partners_devices_claim()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_claim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_partners_devices_claim_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClaimDeviceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_partners_devices_claim_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_partners_devices_claim`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningPartnersDevicesClaimArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Request body.
    pub body: ClaimDeviceRequest,
}

/// GET v1/partners/{partnersId}/devices:claim
/// Claims a device for a customer and adds it to zero-touch enrollment. If the device is already claimed by another customer, the call returns an error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_claim_builder()` + `androiddeviceprovisioning_partners_devices_claim_execute()`.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_claim_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_claim(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningPartnersDevicesClaimArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClaimDeviceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_partners_devices_claim_builder(
        client,
        args.partnerId.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_partners_devices_claim_execute(builder)
}

/// GET v1/partners/{partnersId}/devices:claimAsync
/// Claims a batch of devices for a customer asynchronously. Adds the devices to zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_claim_async_execute()` to send, or `androiddeviceprovisioning_partners_devices_claim_async` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_claim_async_builder(
    client: &SimpleHttpClient,
    partnerId: String,
    body: &ClaimDevicesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androiddeviceprovisioning.googleapis.com/v1/partners/{}/devices:claimAsync",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{partnersId}/devices:claimAsync
/// Claims a batch of devices for a customer asynchronously. Adds the devices to zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_partners_devices_claim_async_execute()` or `androiddeviceprovisioning_partners_devices_claim_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_claim_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_claim_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/partners/{partnersId}/devices:claimAsync
/// Claims a batch of devices for a customer asynchronously. Adds the devices to zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_partners_devices_claim_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_claim_async_task()`.
/// For the simplest API, use `androiddeviceprovisioning_partners_devices_claim_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_claim_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_partners_devices_claim_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_partners_devices_claim_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_partners_devices_claim_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningPartnersDevicesClaimAsyncArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Request body.
    pub body: ClaimDevicesRequest,
}

/// GET v1/partners/{partnersId}/devices:claimAsync
/// Claims a batch of devices for a customer asynchronously. Adds the devices to zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_claim_async_builder()` + `androiddeviceprovisioning_partners_devices_claim_async_execute()`.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_claim_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_claim_async(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningPartnersDevicesClaimAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_partners_devices_claim_async_builder(
        client,
        args.partnerId.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_partners_devices_claim_async_execute(builder)
}

/// GET v1/partners/{partnersId}/devices:findByIdentifier
/// Finds devices by hardware identifiers, such as IMEI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_find_by_identifier_execute()` to send, or `androiddeviceprovisioning_partners_devices_find_by_identifier` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_find_by_identifier_builder(
    client: &SimpleHttpClient,
    partnerId: String,
    body: &FindDevicesByDeviceIdentifierRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androiddeviceprovisioning.googleapis.com/v1/partners/{}/devices:findByIdentifier",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{partnersId}/devices:findByIdentifier
/// Finds devices by hardware identifiers, such as IMEI.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_partners_devices_find_by_identifier_execute()` or `androiddeviceprovisioning_partners_devices_find_by_identifier`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_find_by_identifier_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_find_by_identifier_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FindDevicesByDeviceIdentifierResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FindDevicesByDeviceIdentifierResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/partners/{partnersId}/devices:findByIdentifier
/// Finds devices by hardware identifiers, such as IMEI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_partners_devices_find_by_identifier_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_find_by_identifier_task()`.
/// For the simplest API, use `androiddeviceprovisioning_partners_devices_find_by_identifier()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_find_by_identifier_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_partners_devices_find_by_identifier_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FindDevicesByDeviceIdentifierResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_partners_devices_find_by_identifier_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_partners_devices_find_by_identifier`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningPartnersDevicesFindByIdentifierArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Request body.
    pub body: FindDevicesByDeviceIdentifierRequest,
}

/// GET v1/partners/{partnersId}/devices:findByIdentifier
/// Finds devices by hardware identifiers, such as IMEI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_find_by_identifier_builder()` + `androiddeviceprovisioning_partners_devices_find_by_identifier_execute()`.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_find_by_identifier_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_find_by_identifier(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningPartnersDevicesFindByIdentifierArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FindDevicesByDeviceIdentifierResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_partners_devices_find_by_identifier_builder(
        client,
        args.partnerId.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_partners_devices_find_by_identifier_execute(builder)
}

/// GET v1/partners/{partnersId}/devices:findByOwner
/// Finds devices claimed for customers. The results only contain devices registered to the reseller that's identified by the `partnerId` argument. The customer's devices purchased from other resellers don't appear in the results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_find_by_owner_execute()` to send, or `androiddeviceprovisioning_partners_devices_find_by_owner` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_find_by_owner_builder(
    client: &SimpleHttpClient,
    partnerId: String,
    body: &FindDevicesByOwnerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androiddeviceprovisioning.googleapis.com/v1/partners/{}/devices:findByOwner",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{partnersId}/devices:findByOwner
/// Finds devices claimed for customers. The results only contain devices registered to the reseller that's identified by the `partnerId` argument. The customer's devices purchased from other resellers don't appear in the results.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_partners_devices_find_by_owner_execute()` or `androiddeviceprovisioning_partners_devices_find_by_owner`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_find_by_owner_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_find_by_owner_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FindDevicesByOwnerResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FindDevicesByOwnerResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/partners/{partnersId}/devices:findByOwner
/// Finds devices claimed for customers. The results only contain devices registered to the reseller that's identified by the `partnerId` argument. The customer's devices purchased from other resellers don't appear in the results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_partners_devices_find_by_owner_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_find_by_owner_task()`.
/// For the simplest API, use `androiddeviceprovisioning_partners_devices_find_by_owner()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_find_by_owner_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_partners_devices_find_by_owner_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FindDevicesByOwnerResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_partners_devices_find_by_owner_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_partners_devices_find_by_owner`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningPartnersDevicesFindByOwnerArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Request body.
    pub body: FindDevicesByOwnerRequest,
}

/// GET v1/partners/{partnersId}/devices:findByOwner
/// Finds devices claimed for customers. The results only contain devices registered to the reseller that's identified by the `partnerId` argument. The customer's devices purchased from other resellers don't appear in the results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_find_by_owner_builder()` + `androiddeviceprovisioning_partners_devices_find_by_owner_execute()`.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_find_by_owner_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_find_by_owner(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningPartnersDevicesFindByOwnerArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FindDevicesByOwnerResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_partners_devices_find_by_owner_builder(
        client,
        args.partnerId.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_partners_devices_find_by_owner_execute(builder)
}

/// GET v1/partners/{partnersId}/devices:getSimLockState
/// Gets a device's SIM lock state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_get_sim_lock_state_execute()` to send, or `androiddeviceprovisioning_partners_devices_get_sim_lock_state` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_get_sim_lock_state_builder(
    client: &SimpleHttpClient,
    partnerId: String,
    body: &GetDeviceSimLockStateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androiddeviceprovisioning.googleapis.com/v1/partners/{}/devices:getSimLockState",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{partnersId}/devices:getSimLockState
/// Gets a device's SIM lock state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_partners_devices_get_sim_lock_state_execute()` or `androiddeviceprovisioning_partners_devices_get_sim_lock_state`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_get_sim_lock_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_get_sim_lock_state_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetDeviceSimLockStateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetDeviceSimLockStateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/partners/{partnersId}/devices:getSimLockState
/// Gets a device's SIM lock state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_partners_devices_get_sim_lock_state_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_get_sim_lock_state_task()`.
/// For the simplest API, use `androiddeviceprovisioning_partners_devices_get_sim_lock_state()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_get_sim_lock_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_partners_devices_get_sim_lock_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetDeviceSimLockStateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_partners_devices_get_sim_lock_state_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_partners_devices_get_sim_lock_state`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningPartnersDevicesGetSimLockStateArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Request body.
    pub body: GetDeviceSimLockStateRequest,
}

/// GET v1/partners/{partnersId}/devices:getSimLockState
/// Gets a device's SIM lock state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_get_sim_lock_state_builder()` + `androiddeviceprovisioning_partners_devices_get_sim_lock_state_execute()`.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_get_sim_lock_state_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_get_sim_lock_state(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningPartnersDevicesGetSimLockStateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetDeviceSimLockStateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_partners_devices_get_sim_lock_state_builder(
        client,
        args.partnerId.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_partners_devices_get_sim_lock_state_execute(builder)
}

/// GET v1/partners/{partnersId}/devices/{devicesId}/metadata
/// Updates reseller metadata associated with the device. Android devices only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_metadata_execute()` to send, or `androiddeviceprovisioning_partners_devices_metadata` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_metadata_builder(
    client: &SimpleHttpClient,
    metadataOwnerId: String,
    deviceId: String,
    body: &UpdateDeviceMetadataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androiddeviceprovisioning.googleapis.com/v1/partners/{}/devices/{}/metadata",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{partnersId}/devices/{devicesId}/metadata
/// Updates reseller metadata associated with the device. Android devices only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_partners_devices_metadata_execute()` or `androiddeviceprovisioning_partners_devices_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeviceMetadata>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceMetadata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/partners/{partnersId}/devices/{devicesId}/metadata
/// Updates reseller metadata associated with the device. Android devices only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_partners_devices_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_metadata_task()`.
/// For the simplest API, use `androiddeviceprovisioning_partners_devices_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_partners_devices_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceMetadata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_partners_devices_metadata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_partners_devices_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningPartnersDevicesMetadataArgs {
    /// Path parameter: metadataOwnerId
    pub metadataOwnerId: String,
    /// Path parameter: deviceId
    pub deviceId: String,
    /// Request body.
    pub body: UpdateDeviceMetadataRequest,
}

/// GET v1/partners/{partnersId}/devices/{devicesId}/metadata
/// Updates reseller metadata associated with the device. Android devices only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_metadata_builder()` + `androiddeviceprovisioning_partners_devices_metadata_execute()`.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_metadata(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningPartnersDevicesMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceMetadata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_partners_devices_metadata_builder(
        client,
        args.metadataOwnerId.clone(),
        args.deviceId.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_partners_devices_metadata_execute(builder)
}

/// GET v1/partners/{partnersId}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_unclaim_execute()` to send, or `androiddeviceprovisioning_partners_devices_unclaim` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_unclaim_builder(
    client: &SimpleHttpClient,
    partnerId: String,
    body: &UnclaimDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androiddeviceprovisioning.googleapis.com/v1/partners/{}/devices:unclaim",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{partnersId}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_partners_devices_unclaim_execute()` or `androiddeviceprovisioning_partners_devices_unclaim`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_unclaim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_unclaim_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/partners/{partnersId}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_partners_devices_unclaim_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_unclaim_task()`.
/// For the simplest API, use `androiddeviceprovisioning_partners_devices_unclaim()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_unclaim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_partners_devices_unclaim_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_partners_devices_unclaim_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_partners_devices_unclaim`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningPartnersDevicesUnclaimArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Request body.
    pub body: UnclaimDeviceRequest,
}

/// GET v1/partners/{partnersId}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_unclaim_builder()` + `androiddeviceprovisioning_partners_devices_unclaim_execute()`.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_unclaim_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_unclaim(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningPartnersDevicesUnclaimArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_partners_devices_unclaim_builder(
        client,
        args.partnerId.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_partners_devices_unclaim_execute(builder)
}

/// GET v1/partners/{partnersId}/devices:unclaimAsync
/// Unclaims a batch of devices for a customer asynchronously. Removes the devices from zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_unclaim_async_execute()` to send, or `androiddeviceprovisioning_partners_devices_unclaim_async` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_unclaim_async_builder(
    client: &SimpleHttpClient,
    partnerId: String,
    body: &UnclaimDevicesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androiddeviceprovisioning.googleapis.com/v1/partners/{}/devices:unclaimAsync",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{partnersId}/devices:unclaimAsync
/// Unclaims a batch of devices for a customer asynchronously. Removes the devices from zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_partners_devices_unclaim_async_execute()` or `androiddeviceprovisioning_partners_devices_unclaim_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_unclaim_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_unclaim_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/partners/{partnersId}/devices:unclaimAsync
/// Unclaims a batch of devices for a customer asynchronously. Removes the devices from zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_partners_devices_unclaim_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_unclaim_async_task()`.
/// For the simplest API, use `androiddeviceprovisioning_partners_devices_unclaim_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_unclaim_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_partners_devices_unclaim_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_partners_devices_unclaim_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_partners_devices_unclaim_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningPartnersDevicesUnclaimAsyncArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Request body.
    pub body: UnclaimDevicesRequest,
}

/// GET v1/partners/{partnersId}/devices:unclaimAsync
/// Unclaims a batch of devices for a customer asynchronously. Removes the devices from zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_unclaim_async_builder()` + `androiddeviceprovisioning_partners_devices_unclaim_async_execute()`.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_unclaim_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_unclaim_async(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningPartnersDevicesUnclaimAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_partners_devices_unclaim_async_builder(
        client,
        args.partnerId.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_partners_devices_unclaim_async_execute(builder)
}

/// GET v1/partners/{partnersId}/devices:updateMetadataAsync
/// Updates the reseller metadata attached to a batch of devices. This method updates devices asynchronously and returns an Operation that can be used to track progress. Read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations). Android Devices only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_update_metadata_async_execute()` to send, or `androiddeviceprovisioning_partners_devices_update_metadata_async` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_update_metadata_async_builder(
    client: &SimpleHttpClient,
    partnerId: String,
    body: &UpdateDeviceMetadataInBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androiddeviceprovisioning.googleapis.com/v1/partners/{}/devices:updateMetadataAsync",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{partnersId}/devices:updateMetadataAsync
/// Updates the reseller metadata attached to a batch of devices. This method updates devices asynchronously and returns an Operation that can be used to track progress. Read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations). Android Devices only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_partners_devices_update_metadata_async_execute()` or `androiddeviceprovisioning_partners_devices_update_metadata_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_update_metadata_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_update_metadata_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/partners/{partnersId}/devices:updateMetadataAsync
/// Updates the reseller metadata attached to a batch of devices. This method updates devices asynchronously and returns an Operation that can be used to track progress. Read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations). Android Devices only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_partners_devices_update_metadata_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_update_metadata_async_task()`.
/// For the simplest API, use `androiddeviceprovisioning_partners_devices_update_metadata_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_devices_update_metadata_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_partners_devices_update_metadata_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_partners_devices_update_metadata_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_partners_devices_update_metadata_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningPartnersDevicesUpdateMetadataAsyncArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Request body.
    pub body: UpdateDeviceMetadataInBatchRequest,
}

/// GET v1/partners/{partnersId}/devices:updateMetadataAsync
/// Updates the reseller metadata attached to a batch of devices. This method updates devices asynchronously and returns an Operation that can be used to track progress. Read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations). Android Devices only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_update_metadata_async_builder()` + `androiddeviceprovisioning_partners_devices_update_metadata_async_execute()`.
/// For task-level control, use `androiddeviceprovisioning_partners_devices_update_metadata_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_update_metadata_async(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningPartnersDevicesUpdateMetadataAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_partners_devices_update_metadata_async_builder(
        client,
        args.partnerId.clone(),
        &args.body,
    )?;
    androiddeviceprovisioning_partners_devices_update_metadata_async_execute(builder)
}

/// GET v1/partners/{partnersId}/vendors
/// Lists the vendors of the partner.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_vendors_list_execute()` to send, or `androiddeviceprovisioning_partners_vendors_list` for simplest API.

pub fn androiddeviceprovisioning_partners_vendors_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androiddeviceprovisioning.googleapis.com/v1/partners/{}/vendors",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/partners/{partnersId}/vendors
/// Lists the vendors of the partner.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androiddeviceprovisioning_partners_vendors_list_execute()` or `androiddeviceprovisioning_partners_vendors_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_vendors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_vendors_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListVendorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVendorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/partners/{partnersId}/vendors
/// Lists the vendors of the partner.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androiddeviceprovisioning_partners_vendors_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androiddeviceprovisioning_partners_vendors_list_task()`.
/// For the simplest API, use `androiddeviceprovisioning_partners_vendors_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androiddeviceprovisioning_partners_vendors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androiddeviceprovisioning_partners_vendors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVendorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androiddeviceprovisioning_partners_vendors_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androiddeviceprovisioning_partners_vendors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroiddeviceprovisioningPartnersVendorsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/partners/{partnersId}/vendors
/// Lists the vendors of the partner.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_vendors_list_builder()` + `androiddeviceprovisioning_partners_vendors_list_execute()`.
/// For task-level control, use `androiddeviceprovisioning_partners_vendors_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_vendors_list(
    client: &SimpleHttpClient,
    args: &AndroiddeviceprovisioningPartnersVendorsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVendorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androiddeviceprovisioning_partners_vendors_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    androiddeviceprovisioning_partners_vendors_list_execute(builder)
}
