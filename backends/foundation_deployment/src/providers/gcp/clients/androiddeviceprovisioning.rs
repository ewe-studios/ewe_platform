//! Auto-generated API clients for Google Cloud Platform - androiddeviceprovisioning.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`
//!

#![cfg(feature = "gcp")]

//! Feature flag: `gcp/androiddeviceprovisioning`

#![cfg(feature = "gcp/androiddeviceprovisioning")]

use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;

/// GET v1/customers
/// Lists the user's customer accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_list_execute()` to send, or `androiddeviceprovisioning_customers_list` for simplest API.

pub fn androiddeviceprovisioning_customers_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/customers",
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "pageSize",
            pageSize.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "pageToken",
            pageToken.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )
}

/// GET v1/customers
/// Lists the user's customer accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_customers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_customers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CustomerListCustomersResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomerListCustomersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers
/// Lists the user's customer accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_list_builder()` + `androiddeviceprovisioning_customers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_list(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CustomerListCustomersResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_customers_list_builder(client, pageSize, pageToken)?;
    androiddeviceprovisioning_customers_list_execute(builder)
}

/// GET v1/{+parent}/configurations
/// Creates a new configuration. Once created, a customer can apply the configuration to devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_configurations_create_execute()` to send, or `androiddeviceprovisioning_customers_configurations_create` for simplest API.

pub fn androiddeviceprovisioning_customers_configurations_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Configuration
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/configurations",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/configurations
/// Creates a new configuration. Once created, a customer can apply the configuration to devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_customers_configurations_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_customers_configurations_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_configurations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Configuration>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Configuration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/configurations
/// Creates a new configuration. Once created, a customer can apply the configuration to devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_configurations_create_builder()` + `androiddeviceprovisioning_customers_configurations_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_configurations_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Configuration,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Configuration>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_customers_configurations_create_builder(client, parent, body)?;
    androiddeviceprovisioning_customers_configurations_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes an unused configuration. The API call fails if the customer has devices with the configuration applied.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_configurations_delete_execute()` to send, or `androiddeviceprovisioning_customers_configurations_delete` for simplest API.

pub fn androiddeviceprovisioning_customers_configurations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an unused configuration. The API call fails if the customer has devices with the configuration applied.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_customers_configurations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_customers_configurations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_configurations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Empty>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an unused configuration. The API call fails if the customer has devices with the configuration applied.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_configurations_delete_builder()` + `androiddeviceprovisioning_customers_configurations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_configurations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Empty>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_customers_configurations_delete_builder(client, name)?;
    androiddeviceprovisioning_customers_configurations_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the details of a configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_configurations_get_execute()` to send, or `androiddeviceprovisioning_customers_configurations_get` for simplest API.

pub fn androiddeviceprovisioning_customers_configurations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the details of a configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_customers_configurations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_customers_configurations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_configurations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Configuration>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Configuration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the details of a configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_configurations_get_builder()` + `androiddeviceprovisioning_customers_configurations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_configurations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Configuration>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_customers_configurations_get_builder(client, name)?;
    androiddeviceprovisioning_customers_configurations_get_execute(builder)
}

/// GET v1/{+parent}/configurations
/// Lists a customer's configurations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_configurations_list_execute()` to send, or `androiddeviceprovisioning_customers_configurations_list` for simplest API.

pub fn androiddeviceprovisioning_customers_configurations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/configurations",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/configurations
/// Lists a customer's configurations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_customers_configurations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_customers_configurations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_configurations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CustomerListConfigurationsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomerListConfigurationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/configurations
/// Lists a customer's configurations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_configurations_list_builder()` + `androiddeviceprovisioning_customers_configurations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_configurations_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CustomerListConfigurationsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_customers_configurations_list_builder(client, parent)?;
    androiddeviceprovisioning_customers_configurations_list_execute(builder)
}

/// GET v1/{+name}
/// Updates a configuration's field values.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_configurations_patch_execute()` to send, or `androiddeviceprovisioning_customers_configurations_patch` for simplest API.

pub fn androiddeviceprovisioning_customers_configurations_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Configuration
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "updateMask",
            updateMask.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates a configuration's field values.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_customers_configurations_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_customers_configurations_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_configurations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Configuration>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Configuration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates a configuration's field values.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_configurations_patch_builder()` + `androiddeviceprovisioning_customers_configurations_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_configurations_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Configuration,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Configuration>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_customers_configurations_patch_builder(client, name, updateMask, body)?;
    androiddeviceprovisioning_customers_configurations_patch_execute(builder)
}

/// GET v1/{+parent}/devices:applyConfiguration
/// Applies a Configuration to the device to register the device for zero-touch enrollment. After applying a configuration to a device, the device automatically provisions itself on first boot, or next factory reset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_devices_apply_configuration_execute()` to send, or `androiddeviceprovisioning_customers_devices_apply_configuration` for simplest API.

pub fn androiddeviceprovisioning_customers_devices_apply_configuration_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CustomerApplyConfigurationRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/devices:applyConfiguration",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/devices:applyConfiguration
/// Applies a Configuration to the device to register the device for zero-touch enrollment. After applying a configuration to a device, the device automatically provisions itself on first boot, or next factory reset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_customers_devices_apply_configuration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_customers_devices_apply_configuration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_apply_configuration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Empty>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/devices:applyConfiguration
/// Applies a Configuration to the device to register the device for zero-touch enrollment. After applying a configuration to a device, the device automatically provisions itself on first boot, or next factory reset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_devices_apply_configuration_builder()` + `androiddeviceprovisioning_customers_devices_apply_configuration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_apply_configuration(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CustomerApplyConfigurationRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Empty>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_customers_devices_apply_configuration_builder(client, parent, body)?;
    androiddeviceprovisioning_customers_devices_apply_configuration_execute(builder)
}

/// GET v1/{+name}
/// Gets the details of a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_devices_get_execute()` to send, or `androiddeviceprovisioning_customers_devices_get` for simplest API.

pub fn androiddeviceprovisioning_customers_devices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the details of a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_customers_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_customers_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Device>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Device = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the details of a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_devices_get_builder()` + `androiddeviceprovisioning_customers_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Device>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_customers_devices_get_builder(client, name)?;
    androiddeviceprovisioning_customers_devices_get_execute(builder)
}

/// GET v1/{+parent}/devices
/// Lists a customer's devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_devices_list_execute()` to send, or `androiddeviceprovisioning_customers_devices_list` for simplest API.

pub fn androiddeviceprovisioning_customers_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<&str>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/devices",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "pageSize",
            pageSize.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "pageToken",
            pageToken.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )
}

/// GET v1/{+parent}/devices
/// Lists a customer's devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_customers_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_customers_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CustomerListDevicesResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomerListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/devices
/// Lists a customer's devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_devices_list_builder()` + `androiddeviceprovisioning_customers_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<&str>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CustomerListDevicesResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_customers_devices_list_builder(client, parent, pageSize, pageToken)?;
    androiddeviceprovisioning_customers_devices_list_execute(builder)
}

/// GET v1/{+parent}/devices:removeConfiguration
/// Removes a configuration from device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_devices_remove_configuration_execute()` to send, or `androiddeviceprovisioning_customers_devices_remove_configuration` for simplest API.

pub fn androiddeviceprovisioning_customers_devices_remove_configuration_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CustomerRemoveConfigurationRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/devices:removeConfiguration",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/devices:removeConfiguration
/// Removes a configuration from device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_customers_devices_remove_configuration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_customers_devices_remove_configuration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_remove_configuration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Empty>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/devices:removeConfiguration
/// Removes a configuration from device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_devices_remove_configuration_builder()` + `androiddeviceprovisioning_customers_devices_remove_configuration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_remove_configuration(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CustomerRemoveConfigurationRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Empty>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_customers_devices_remove_configuration_builder(client, parent, body)?;
    androiddeviceprovisioning_customers_devices_remove_configuration_execute(builder)
}

/// GET v1/{+parent}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment. After removing a device, a customer must contact their reseller to register the device into zero-touch enrollment again.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_devices_unclaim_execute()` to send, or `androiddeviceprovisioning_customers_devices_unclaim` for simplest API.

pub fn androiddeviceprovisioning_customers_devices_unclaim_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CustomerUnclaimDeviceRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/devices:unclaim",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment. After removing a device, a customer must contact their reseller to register the device into zero-touch enrollment again.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_customers_devices_unclaim_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_customers_devices_unclaim_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_unclaim_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Empty>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment. After removing a device, a customer must contact their reseller to register the device into zero-touch enrollment again.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_devices_unclaim_builder()` + `androiddeviceprovisioning_customers_devices_unclaim_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_devices_unclaim(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CustomerUnclaimDeviceRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Empty>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_customers_devices_unclaim_builder(client, parent, body)?;
    androiddeviceprovisioning_customers_devices_unclaim_execute(builder)
}

/// GET v1/{+parent}/dpcs
/// Lists the DPCs (device policy controllers) that support zero-touch enrollment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_customers_dpcs_list_execute()` to send, or `androiddeviceprovisioning_customers_dpcs_list` for simplest API.

pub fn androiddeviceprovisioning_customers_dpcs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/dpcs",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dpcs
/// Lists the DPCs (device policy controllers) that support zero-touch enrollment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_customers_dpcs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_customers_dpcs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_dpcs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CustomerListDpcsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomerListDpcsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dpcs
/// Lists the DPCs (device policy controllers) that support zero-touch enrollment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_customers_dpcs_list_builder()` + `androiddeviceprovisioning_customers_dpcs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_customers_dpcs_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CustomerListDpcsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_customers_dpcs_list_builder(client, parent)?;
    androiddeviceprovisioning_customers_dpcs_list_execute(builder)
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_operations_get_execute()` to send, or `androiddeviceprovisioning_operations_get` for simplest API.

pub fn androiddeviceprovisioning_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Operation>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_operations_get_builder()` + `androiddeviceprovisioning_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Operation>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_operations_get_builder(client, name)?;
    androiddeviceprovisioning_operations_get_execute(builder)
}

/// GET v1/{+parent}/customers
/// Creates a customer for zero-touch enrollment. After the method returns successfully, admin and owner roles can manage devices and EMM configs by calling API methods or using their zero-touch enrollment portal. The customer receives an email that welcomes them to zero-touch enrollment and explains how to sign into the portal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_customers_create_execute()` to send, or `androiddeviceprovisioning_partners_customers_create` for simplest API.

pub fn androiddeviceprovisioning_partners_customers_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreateCustomerRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/customers",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/customers
/// Creates a customer for zero-touch enrollment. After the method returns successfully, admin and owner roles can manage devices and EMM configs by calling API methods or using their zero-touch enrollment portal. The customer receives an email that welcomes them to zero-touch enrollment and explains how to sign into the portal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_customers_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_customers_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_customers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Company>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Company = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/customers
/// Creates a customer for zero-touch enrollment. After the method returns successfully, admin and owner roles can manage devices and EMM configs by calling API methods or using their zero-touch enrollment portal. The customer receives an email that welcomes them to zero-touch enrollment and explains how to sign into the portal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_customers_create_builder()` + `androiddeviceprovisioning_partners_customers_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_customers_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreateCustomerRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Company>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_customers_create_builder(client, parent, body)?;
    androiddeviceprovisioning_partners_customers_create_execute(builder)
}

/// GET v1/partners/{+partnerId}/customers
/// Lists the customers that are enrolled to the reseller identified by the `partnerId` argument. This list includes customers that the reseller created and customers that enrolled themselves using the portal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_customers_list_execute()` to send, or `androiddeviceprovisioning_partners_customers_list` for simplest API.

pub fn androiddeviceprovisioning_partners_customers_list_builder(
    client: &SimpleHttpClient,
    partnerId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/partners/{+partnerId}/customers",
        partnerId,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "pageSize",
            pageSize.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "pageToken",
            pageToken.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )
}

/// GET v1/partners/{+partnerId}/customers
/// Lists the customers that are enrolled to the reseller identified by the `partnerId` argument. This list includes customers that the reseller created and customers that enrolled themselves using the portal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_customers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_customers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_customers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListCustomersResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCustomersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{+partnerId}/customers
/// Lists the customers that are enrolled to the reseller identified by the `partnerId` argument. This list includes customers that the reseller created and customers that enrolled themselves using the portal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_customers_list_builder()` + `androiddeviceprovisioning_partners_customers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_customers_list(
    client: &SimpleHttpClient,
    partnerId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListCustomersResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_customers_list_builder(client, partnerId, pageSize, pageToken)?;
    androiddeviceprovisioning_partners_customers_list_execute(builder)
}

/// GET v1/partners/{+partnerId}/devices:claim
/// Claims a device for a customer and adds it to zero-touch enrollment. If the device is already claimed by another customer, the call returns an error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_claim_execute()` to send, or `androiddeviceprovisioning_partners_devices_claim` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_claim_builder(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &ClaimDeviceRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/partners/{+partnerId}/devices:claim",
        partnerId,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/partners/{+partnerId}/devices:claim
/// Claims a device for a customer and adds it to zero-touch enrollment. If the device is already claimed by another customer, the call returns an error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_devices_claim_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_devices_claim_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_claim_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ClaimDeviceResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClaimDeviceResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{+partnerId}/devices:claim
/// Claims a device for a customer and adds it to zero-touch enrollment. If the device is already claimed by another customer, the call returns an error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_claim_builder()` + `androiddeviceprovisioning_partners_devices_claim_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_claim(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &ClaimDeviceRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ClaimDeviceResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_devices_claim_builder(client, partnerId, body)?;
    androiddeviceprovisioning_partners_devices_claim_execute(builder)
}

/// GET v1/partners/{+partnerId}/devices:claimAsync
/// Claims a batch of devices for a customer asynchronously. Adds the devices to zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_claim_async_execute()` to send, or `androiddeviceprovisioning_partners_devices_claim_async` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_claim_async_builder(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &ClaimDevicesRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/partners/{+partnerId}/devices:claimAsync",
        partnerId,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/partners/{+partnerId}/devices:claimAsync
/// Claims a batch of devices for a customer asynchronously. Adds the devices to zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_devices_claim_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_devices_claim_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_claim_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Operation>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{+partnerId}/devices:claimAsync
/// Claims a batch of devices for a customer asynchronously. Adds the devices to zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_claim_async_builder()` + `androiddeviceprovisioning_partners_devices_claim_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_claim_async(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &ClaimDevicesRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Operation>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_devices_claim_async_builder(client, partnerId, body)?;
    androiddeviceprovisioning_partners_devices_claim_async_execute(builder)
}

/// GET v1/partners/{+partnerId}/devices:findByIdentifier
/// Finds devices by hardware identifiers, such as IMEI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_find_by_identifier_execute()` to send, or `androiddeviceprovisioning_partners_devices_find_by_identifier` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_find_by_identifier_builder(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &FindDevicesByDeviceIdentifierRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/partners/{+partnerId}/devices:findByIdentifier",
        partnerId,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/partners/{+partnerId}/devices:findByIdentifier
/// Finds devices by hardware identifiers, such as IMEI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_devices_find_by_identifier_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_devices_find_by_identifier_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_find_by_identifier_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<FindDevicesByDeviceIdentifierResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FindDevicesByDeviceIdentifierResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{+partnerId}/devices:findByIdentifier
/// Finds devices by hardware identifiers, such as IMEI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_find_by_identifier_builder()` + `androiddeviceprovisioning_partners_devices_find_by_identifier_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_find_by_identifier(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &FindDevicesByDeviceIdentifierRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<FindDevicesByDeviceIdentifierResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_devices_find_by_identifier_builder(client, partnerId, body)?;
    androiddeviceprovisioning_partners_devices_find_by_identifier_execute(builder)
}

/// GET v1/partners/{+partnerId}/devices:findByOwner
/// Finds devices claimed for customers. The results only contain devices registered to the reseller that's identified by the `partnerId` argument. The customer's devices purchased from other resellers don't appear in the results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_find_by_owner_execute()` to send, or `androiddeviceprovisioning_partners_devices_find_by_owner` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_find_by_owner_builder(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &FindDevicesByOwnerRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/partners/{+partnerId}/devices:findByOwner",
        partnerId,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/partners/{+partnerId}/devices:findByOwner
/// Finds devices claimed for customers. The results only contain devices registered to the reseller that's identified by the `partnerId` argument. The customer's devices purchased from other resellers don't appear in the results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_devices_find_by_owner_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_devices_find_by_owner_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_find_by_owner_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<FindDevicesByOwnerResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FindDevicesByOwnerResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{+partnerId}/devices:findByOwner
/// Finds devices claimed for customers. The results only contain devices registered to the reseller that's identified by the `partnerId` argument. The customer's devices purchased from other resellers don't appear in the results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_find_by_owner_builder()` + `androiddeviceprovisioning_partners_devices_find_by_owner_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_find_by_owner(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &FindDevicesByOwnerRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<FindDevicesByOwnerResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_devices_find_by_owner_builder(client, partnerId, body)?;
    androiddeviceprovisioning_partners_devices_find_by_owner_execute(builder)
}

/// GET v1/{+name}
/// Gets a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_get_execute()` to send, or `androiddeviceprovisioning_partners_devices_get` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Device>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Device = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_get_builder()` + `androiddeviceprovisioning_partners_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Device>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_devices_get_builder(client, name)?;
    androiddeviceprovisioning_partners_devices_get_execute(builder)
}

/// GET v1/partners/{+partnerId}/devices:getSimLockState
/// Gets a device's SIM lock state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_get_sim_lock_state_execute()` to send, or `androiddeviceprovisioning_partners_devices_get_sim_lock_state` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_get_sim_lock_state_builder(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &GetDeviceSimLockStateRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/partners/{+partnerId}/devices:getSimLockState",
        partnerId,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/partners/{+partnerId}/devices:getSimLockState
/// Gets a device's SIM lock state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_devices_get_sim_lock_state_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_devices_get_sim_lock_state_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_get_sim_lock_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GetDeviceSimLockStateResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetDeviceSimLockStateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{+partnerId}/devices:getSimLockState
/// Gets a device's SIM lock state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_get_sim_lock_state_builder()` + `androiddeviceprovisioning_partners_devices_get_sim_lock_state_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_get_sim_lock_state(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &GetDeviceSimLockStateRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GetDeviceSimLockStateResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_devices_get_sim_lock_state_builder(client, partnerId, body)?;
    androiddeviceprovisioning_partners_devices_get_sim_lock_state_execute(builder)
}

/// GET v1/partners/{+metadataOwnerId}/devices/{+deviceId}/metadata
/// Updates reseller metadata associated with the device. Android devices only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_metadata_execute()` to send, or `androiddeviceprovisioning_partners_devices_metadata` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_metadata_builder(
    client: &SimpleHttpClient,
    deviceId: &str,
    metadataOwnerId: &str,
    body: &UpdateDeviceMetadataRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/partners/{+metadataOwnerId}/devices/{+deviceId}/metadata",
        deviceId,
        metadataOwnerId,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/partners/{+metadataOwnerId}/devices/{+deviceId}/metadata
/// Updates reseller metadata associated with the device. Android devices only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_devices_metadata_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_devices_metadata_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DeviceMetadata>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceMetadata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{+metadataOwnerId}/devices/{+deviceId}/metadata
/// Updates reseller metadata associated with the device. Android devices only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_metadata_builder()` + `androiddeviceprovisioning_partners_devices_metadata_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_metadata(
    client: &SimpleHttpClient,
    deviceId: &str,
    metadataOwnerId: &str,
    body: &UpdateDeviceMetadataRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DeviceMetadata>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_devices_metadata_builder(client, deviceId, metadataOwnerId, body)?;
    androiddeviceprovisioning_partners_devices_metadata_execute(builder)
}

/// GET v1/partners/{+partnerId}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_unclaim_execute()` to send, or `androiddeviceprovisioning_partners_devices_unclaim` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_unclaim_builder(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &UnclaimDeviceRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/partners/{+partnerId}/devices:unclaim",
        partnerId,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/partners/{+partnerId}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_devices_unclaim_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_devices_unclaim_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_unclaim_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Empty>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{+partnerId}/devices:unclaim
/// Unclaims a device from a customer and removes it from zero-touch enrollment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_unclaim_builder()` + `androiddeviceprovisioning_partners_devices_unclaim_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_unclaim(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &UnclaimDeviceRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Empty>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_devices_unclaim_builder(client, partnerId, body)?;
    androiddeviceprovisioning_partners_devices_unclaim_execute(builder)
}

/// GET v1/partners/{+partnerId}/devices:unclaimAsync
/// Unclaims a batch of devices for a customer asynchronously. Removes the devices from zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_unclaim_async_execute()` to send, or `androiddeviceprovisioning_partners_devices_unclaim_async` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_unclaim_async_builder(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &UnclaimDevicesRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/partners/{+partnerId}/devices:unclaimAsync",
        partnerId,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/partners/{+partnerId}/devices:unclaimAsync
/// Unclaims a batch of devices for a customer asynchronously. Removes the devices from zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_devices_unclaim_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_devices_unclaim_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_unclaim_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Operation>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{+partnerId}/devices:unclaimAsync
/// Unclaims a batch of devices for a customer asynchronously. Removes the devices from zero-touch enrollment. To learn more, read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_unclaim_async_builder()` + `androiddeviceprovisioning_partners_devices_unclaim_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_unclaim_async(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &UnclaimDevicesRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Operation>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_devices_unclaim_async_builder(client, partnerId, body)?;
    androiddeviceprovisioning_partners_devices_unclaim_async_execute(builder)
}

/// GET v1/partners/{+partnerId}/devices:updateMetadataAsync
/// Updates the reseller metadata attached to a batch of devices. This method updates devices asynchronously and returns an Operation that can be used to track progress. Read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations). Android Devices only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_devices_update_metadata_async_execute()` to send, or `androiddeviceprovisioning_partners_devices_update_metadata_async` for simplest API.

pub fn androiddeviceprovisioning_partners_devices_update_metadata_async_builder(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &UpdateDeviceMetadataInBatchRequest
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/partners/{+partnerId}/devices:updateMetadataAsync",
        partnerId,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/partners/{+partnerId}/devices:updateMetadataAsync
/// Updates the reseller metadata attached to a batch of devices. This method updates devices asynchronously and returns an Operation that can be used to track progress. Read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations). Android Devices only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_devices_update_metadata_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_devices_update_metadata_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_update_metadata_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Operation>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/partners/{+partnerId}/devices:updateMetadataAsync
/// Updates the reseller metadata attached to a batch of devices. This method updates devices asynchronously and returns an Operation that can be used to track progress. Read [Long‑running batch operations](/zero-`touch/guides/how-it-works`#operations). Android Devices only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_devices_update_metadata_async_builder()` + `androiddeviceprovisioning_partners_devices_update_metadata_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_devices_update_metadata_async(
    client: &SimpleHttpClient,
    partnerId: &str,
    body: &UpdateDeviceMetadataInBatchRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<Operation>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_devices_update_metadata_async_builder(client, partnerId, body)?;
    androiddeviceprovisioning_partners_devices_update_metadata_async_execute(builder)
}

/// GET v1/{+parent}/vendors
/// Lists the vendors of the partner.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_vendors_list_execute()` to send, or `androiddeviceprovisioning_partners_vendors_list` for simplest API.

pub fn androiddeviceprovisioning_partners_vendors_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/vendors",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "pageSize",
            pageSize.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "pageToken",
            pageToken.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )
}

/// GET v1/{+parent}/vendors
/// Lists the vendors of the partner.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_vendors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_vendors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_vendors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListVendorsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVendorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/vendors
/// Lists the vendors of the partner.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_vendors_list_builder()` + `androiddeviceprovisioning_partners_vendors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_vendors_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListVendorsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_vendors_list_builder(client, parent, pageSize, pageToken)?;
    androiddeviceprovisioning_partners_vendors_list_execute(builder)
}

/// GET v1/{+parent}/customers
/// Lists the customers of the vendor.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androiddeviceprovisioning_partners_vendors_customers_list_execute()` to send, or `androiddeviceprovisioning_partners_vendors_customers_list` for simplest API.

pub fn androiddeviceprovisioning_partners_vendors_customers_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/customers",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "pageSize",
            pageSize.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "pageToken",
            pageToken.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )
}

/// GET v1/{+parent}/customers
/// Lists the customers of the vendor.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androiddeviceprovisioning_partners_vendors_customers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androiddeviceprovisioning_partners_vendors_customers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_vendors_customers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListVendorCustomersResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVendorCustomersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/customers
/// Lists the customers of the vendor.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androiddeviceprovisioning_partners_vendors_customers_list_builder()` + `androiddeviceprovisioning_partners_vendors_customers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androiddeviceprovisioning_partners_vendors_customers_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListVendorCustomersResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = androiddeviceprovisioning_partners_vendors_customers_list_builder(client, parent, pageSize, pageToken)?;
    androiddeviceprovisioning_partners_vendors_customers_list_execute(builder)
}

