//! Auto-generated API clients for gcp/firebasedatabase.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/firebasedatabase`

#![cfg(feature = "gcp/firebasedatabase")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances
/// Requests that a new DatabaseInstance be created. The state of a successfully created DatabaseInstance is `ACTIVE`. Only available for projects on the Blaze plan. Projects can be upgraded using the Cloud Billing API <https://cloud.google.`com/billing/reference/rest/v1/projects/`updateBillingInfo``.> Note that it might take a few minutes for billing enablement state to propagate to Firebase systems.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedatabase_projects_locations_instances_create_execute()` to send, or `firebasedatabase_projects_locations_instances_create` for simplest API.

pub fn firebasedatabase_projects_locations_instances_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    databaseId: Option<&str>,
    validateOnly: Option<bool>,
    body: &DatabaseInstance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedatabase.googleapis.com/v1beta/projects/{}/locations/{}/instances",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = databaseId {
        query_parts.push(format!("databaseId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances
/// Requests that a new DatabaseInstance be created. The state of a successfully created DatabaseInstance is `ACTIVE`. Only available for projects on the Blaze plan. Projects can be upgraded using the Cloud Billing API <https://cloud.google.`com/billing/reference/rest/v1/projects/`updateBillingInfo``.> Note that it might take a few minutes for billing enablement state to propagate to Firebase systems.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebasedatabase_projects_locations_instances_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebasedatabase_projects_locations_instances_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatabaseInstance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances
/// Requests that a new DatabaseInstance be created. The state of a successfully created DatabaseInstance is `ACTIVE`. Only available for projects on the Blaze plan. Projects can be upgraded using the Cloud Billing API <https://cloud.google.`com/billing/reference/rest/v1/projects/`updateBillingInfo``.> Note that it might take a few minutes for billing enablement state to propagate to Firebase systems.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedatabase_projects_locations_instances_create_builder()` + `firebasedatabase_projects_locations_instances_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_create(
    client: &SimpleHttpClient,
    parent: &str,
    databaseId: Option<&str>,
    validateOnly: Option<bool>,
    body: &DatabaseInstance,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedatabase_projects_locations_instances_create_builder(
        client,
        parent,
        databaseId,
        validateOnly,
        body,
    )?;
    firebasedatabase_projects_locations_instances_create_execute(builder)
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Marks a DatabaseInstance to be deleted. The DatabaseInstance will be set to the DELETED state for 20 days, and will be purged within 30 days. The default database cannot be deleted. IDs for deleted database instances may never be recovered or re-used. The Database may only be deleted if it is already in a DISABLED state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedatabase_projects_locations_instances_delete_execute()` to send, or `firebasedatabase_projects_locations_instances_delete` for simplest API.

pub fn firebasedatabase_projects_locations_instances_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedatabase.googleapis.com/v1beta/projects/{}/locations/{}/instances/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Marks a DatabaseInstance to be deleted. The DatabaseInstance will be set to the DELETED state for 20 days, and will be purged within 30 days. The default database cannot be deleted. IDs for deleted database instances may never be recovered or re-used. The Database may only be deleted if it is already in a DISABLED state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebasedatabase_projects_locations_instances_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebasedatabase_projects_locations_instances_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatabaseInstance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Marks a DatabaseInstance to be deleted. The DatabaseInstance will be set to the DELETED state for 20 days, and will be purged within 30 days. The default database cannot be deleted. IDs for deleted database instances may never be recovered or re-used. The Database may only be deleted if it is already in a DISABLED state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedatabase_projects_locations_instances_delete_builder()` + `firebasedatabase_projects_locations_instances_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedatabase_projects_locations_instances_delete_builder(client, name)?;
    firebasedatabase_projects_locations_instances_delete_execute(builder)
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:disable
/// Disables a DatabaseInstance. The database can be re-enabled later using ReenableDatabaseInstance. When a database is disabled, all reads and writes are denied, including view access in the Firebase console.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedatabase_projects_locations_instances_disable_execute()` to send, or `firebasedatabase_projects_locations_instances_disable` for simplest API.

pub fn firebasedatabase_projects_locations_instances_disable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DisableDatabaseInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedatabase.googleapis.com/v1beta/projects/{}/locations/{}/instances/{}:disable",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:disable
/// Disables a DatabaseInstance. The database can be re-enabled later using ReenableDatabaseInstance. When a database is disabled, all reads and writes are denied, including view access in the Firebase console.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebasedatabase_projects_locations_instances_disable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebasedatabase_projects_locations_instances_disable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_disable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatabaseInstance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:disable
/// Disables a DatabaseInstance. The database can be re-enabled later using ReenableDatabaseInstance. When a database is disabled, all reads and writes are denied, including view access in the Firebase console.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedatabase_projects_locations_instances_disable_builder()` + `firebasedatabase_projects_locations_instances_disable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_disable(
    client: &SimpleHttpClient,
    name: &str,
    body: &DisableDatabaseInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebasedatabase_projects_locations_instances_disable_builder(client, name, body)?;
    firebasedatabase_projects_locations_instances_disable_execute(builder)
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Gets the DatabaseInstance identified by the specified resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedatabase_projects_locations_instances_get_execute()` to send, or `firebasedatabase_projects_locations_instances_get` for simplest API.

pub fn firebasedatabase_projects_locations_instances_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedatabase.googleapis.com/v1beta/projects/{}/locations/{}/instances/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Gets the DatabaseInstance identified by the specified resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebasedatabase_projects_locations_instances_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebasedatabase_projects_locations_instances_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatabaseInstance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Gets the DatabaseInstance identified by the specified resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedatabase_projects_locations_instances_get_builder()` + `firebasedatabase_projects_locations_instances_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedatabase_projects_locations_instances_get_builder(client, name)?;
    firebasedatabase_projects_locations_instances_get_execute(builder)
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances
/// Lists each DatabaseInstance associated with the specified parent project. The list items are returned in no particular order, but will be a consistent view of the database instances when additional requests are made with a `pageToken`. The resulting list contains instances in any STATE. The list results may be stale by a few seconds. Use GetDatabaseInstance for consistent reads.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedatabase_projects_locations_instances_list_execute()` to send, or `firebasedatabase_projects_locations_instances_list` for simplest API.

pub fn firebasedatabase_projects_locations_instances_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedatabase.googleapis.com/v1beta/projects/{}/locations/{}/instances",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances
/// Lists each DatabaseInstance associated with the specified parent project. The list items are returned in no particular order, but will be a consistent view of the database instances when additional requests are made with a `pageToken`. The resulting list contains instances in any STATE. The list results may be stale by a few seconds. Use GetDatabaseInstance for consistent reads.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebasedatabase_projects_locations_instances_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebasedatabase_projects_locations_instances_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDatabaseInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDatabaseInstancesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances
/// Lists each DatabaseInstance associated with the specified parent project. The list items are returned in no particular order, but will be a consistent view of the database instances when additional requests are made with a `pageToken`. The resulting list contains instances in any STATE. The list results may be stale by a few seconds. Use GetDatabaseInstance for consistent reads.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedatabase_projects_locations_instances_list_builder()` + `firebasedatabase_projects_locations_instances_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDatabaseInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedatabase_projects_locations_instances_list_builder(
        client,
        parent,
        pageSize,
        pageToken,
        showDeleted,
    )?;
    firebasedatabase_projects_locations_instances_list_execute(builder)
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:reenable
/// Enables a DatabaseInstance. The database must have been disabled previously using DisableDatabaseInstance. The state of a successfully reenabled DatabaseInstance is `ACTIVE`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedatabase_projects_locations_instances_reenable_execute()` to send, or `firebasedatabase_projects_locations_instances_reenable` for simplest API.

pub fn firebasedatabase_projects_locations_instances_reenable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ReenableDatabaseInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedatabase.googleapis.com/v1beta/projects/{}/locations/{}/instances/{}:reenable",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:reenable
/// Enables a DatabaseInstance. The database must have been disabled previously using DisableDatabaseInstance. The state of a successfully reenabled DatabaseInstance is `ACTIVE`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebasedatabase_projects_locations_instances_reenable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebasedatabase_projects_locations_instances_reenable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_reenable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatabaseInstance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:reenable
/// Enables a DatabaseInstance. The database must have been disabled previously using DisableDatabaseInstance. The state of a successfully reenabled DatabaseInstance is `ACTIVE`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedatabase_projects_locations_instances_reenable_builder()` + `firebasedatabase_projects_locations_instances_reenable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_reenable(
    client: &SimpleHttpClient,
    name: &str,
    body: &ReenableDatabaseInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebasedatabase_projects_locations_instances_reenable_builder(client, name, body)?;
    firebasedatabase_projects_locations_instances_reenable_execute(builder)
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:undelete
/// Restores a DatabaseInstance that was previously marked to be deleted. After the delete method is used, DatabaseInstances are set to the DELETED state for 20 days, and will be purged within 30 days. Databases in the DELETED state can be undeleted without losing any data. This method may only be used on a DatabaseInstance in the DELETED state. Purged DatabaseInstances may not be recovered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedatabase_projects_locations_instances_undelete_execute()` to send, or `firebasedatabase_projects_locations_instances_undelete` for simplest API.

pub fn firebasedatabase_projects_locations_instances_undelete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteDatabaseInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedatabase.googleapis.com/v1beta/projects/{}/locations/{}/instances/{}:undelete",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:undelete
/// Restores a DatabaseInstance that was previously marked to be deleted. After the delete method is used, DatabaseInstances are set to the DELETED state for 20 days, and will be purged within 30 days. Databases in the DELETED state can be undeleted without losing any data. This method may only be used on a DatabaseInstance in the DELETED state. Purged DatabaseInstances may not be recovered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebasedatabase_projects_locations_instances_undelete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebasedatabase_projects_locations_instances_undelete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatabaseInstance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:undelete
/// Restores a DatabaseInstance that was previously marked to be deleted. After the delete method is used, DatabaseInstances are set to the DELETED state for 20 days, and will be purged within 30 days. Databases in the DELETED state can be undeleted without losing any data. This method may only be used on a DatabaseInstance in the DELETED state. Purged DatabaseInstances may not be recovered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedatabase_projects_locations_instances_undelete_builder()` + `firebasedatabase_projects_locations_instances_undelete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedatabase_projects_locations_instances_undelete(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteDatabaseInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebasedatabase_projects_locations_instances_undelete_builder(client, name, body)?;
    firebasedatabase_projects_locations_instances_undelete_execute(builder)
}
