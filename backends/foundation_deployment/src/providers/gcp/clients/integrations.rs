//! Auto-generated API clients for gcp/integrations.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/callback:generateToken
/// Receives the auth code and auth config id to combine that with the client id and secret to retrieve access tokens from the token endpoint. Returns either a success or error message when it's done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_callback_generate_token_execute()` to send, or `integrations_callback_generate_token` for simplest API.

pub fn integrations_callback_generate_token_builder(
    client: &SimpleHttpClient,
    code: Option<&str>,
    gcpProjectId: Option<&str>,
    product: Option<&str>,
    redirectUri: Option<&str>,
    state: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/callback:generateToken",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "code",
            code.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "gcpProjectId",
            gcpProjectId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "product",
            product
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "redirectUri",
            redirectUri
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "state",
            state
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/callback:generateToken
/// Receives the auth code and auth config id to combine that with the client id and secret to retrieve access tokens from the token endpoint. Returns either a success or error message when it's done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_callback_generate_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_callback_generate_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_callback_generate_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaGenerateTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaGenerateTokenResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/callback:generateToken
/// Receives the auth code and auth config id to combine that with the client id and secret to retrieve access tokens from the token endpoint. Returns either a success or error message when it's done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_callback_generate_token_builder()` + `integrations_callback_generate_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_callback_generate_token(
    client: &SimpleHttpClient,
    code: Option<&str>,
    gcpProjectId: Option<&str>,
    product: Option<&str>,
    redirectUri: Option<&str>,
    state: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaGenerateTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_callback_generate_token_builder(
        client,
        code,
        gcpProjectId,
        product,
        redirectUri,
        state,
    )?;
    integrations_callback_generate_token_execute(builder)
}

/// GET v1/connectorPlatformRegions:enumerate
/// Enumerates the regions for which Connector Platform is provisioned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_connector_platform_regions_enumerate_execute()` to send, or `integrations_connector_platform_regions_enumerate` for simplest API.

pub fn integrations_connector_platform_regions_enumerate_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/connectorPlatformRegions:enumerate",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/connectorPlatformRegions:enumerate
/// Enumerates the regions for which Connector Platform is provisioned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_connector_platform_regions_enumerate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_connector_platform_regions_enumerate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_connector_platform_regions_enumerate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudIntegrationsV1alphaEnumerateConnectorPlatformRegionsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaEnumerateConnectorPlatformRegionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/connectorPlatformRegions:enumerate
/// Enumerates the regions for which Connector Platform is provisioned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_connector_platform_regions_enumerate_builder()` + `integrations_connector_platform_regions_enumerate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_connector_platform_regions_enumerate(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudIntegrationsV1alphaEnumerateConnectorPlatformRegionsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_connector_platform_regions_enumerate_builder(client)?;
    integrations_connector_platform_regions_enumerate_execute(builder)
}

/// GET v1/{+parent}/clientmetadata
/// Gets the metadata info for the requested client
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_get_clientmetadata_execute()` to send, or `integrations_projects_get_clientmetadata` for simplest API.

pub fn integrations_projects_get_clientmetadata_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/clientmetadata", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clientmetadata
/// Gets the metadata info for the requested client
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_get_clientmetadata_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_get_clientmetadata_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_get_clientmetadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaGetClientMetadataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaGetClientMetadataResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clientmetadata
/// Gets the metadata info for the requested client
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_get_clientmetadata_builder()` + `integrations_projects_get_clientmetadata_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_get_clientmetadata(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaGetClientMetadataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_get_clientmetadata_builder(client, parent)?;
    integrations_projects_get_clientmetadata_execute(builder)
}

/// GET v1/{+name}:generateOpenApiSpec
/// Generate OpenAPI spec for the requested integrations and api triggers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_generate_open_api_spec_execute()` to send, or `integrations_projects_locations_generate_open_api_spec` for simplest API.

pub fn integrations_projects_locations_generate_open_api_spec_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:generateOpenApiSpec",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:generateOpenApiSpec
/// Generate OpenAPI spec for the requested integrations and api triggers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_generate_open_api_spec_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_generate_open_api_spec_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_generate_open_api_spec_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:generateOpenApiSpec
/// Generate OpenAPI spec for the requested integrations and api triggers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_generate_open_api_spec_builder()` + `integrations_projects_locations_generate_open_api_spec_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_generate_open_api_spec(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_generate_open_api_spec_builder(client, name, body)?;
    integrations_projects_locations_generate_open_api_spec_execute(builder)
}

/// GET v1/{+parent}/clients
/// Gets the client configuration for the given project and location resource name
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_get_clients_execute()` to send, or `integrations_projects_locations_get_clients` for simplest API.

pub fn integrations_projects_locations_get_clients_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/clients", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clients
/// Gets the client configuration for the given project and location resource name
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_get_clients_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_get_clients_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_get_clients_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaGetClientResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaGetClientResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clients
/// Gets the client configuration for the given project and location resource name
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_get_clients_builder()` + `integrations_projects_locations_get_clients_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_get_clients(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaGetClientResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_get_clients_builder(client, parent)?;
    integrations_projects_locations_get_clients_execute(builder)
}

/// GET v1/{+parent}/appsScriptProjects
/// Creates an Apps Script project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_apps_script_projects_create_execute()` to send, or `integrations_projects_locations_apps_script_projects_create` for simplest API.

pub fn integrations_projects_locations_apps_script_projects_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/appsScriptProjects",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/appsScriptProjects
/// Creates an Apps Script project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_apps_script_projects_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_apps_script_projects_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_apps_script_projects_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/appsScriptProjects
/// Creates an Apps Script project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_apps_script_projects_create_builder()` + `integrations_projects_locations_apps_script_projects_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_apps_script_projects_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_apps_script_projects_create_builder(client, parent, body)?;
    integrations_projects_locations_apps_script_projects_create_execute(builder)
}

/// GET v1/{+parent}/appsScriptProjects:link
/// Links a existing Apps Script project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_apps_script_projects_link_execute()` to send, or `integrations_projects_locations_apps_script_projects_link` for simplest API.

pub fn integrations_projects_locations_apps_script_projects_link_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/appsScriptProjects:link",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/appsScriptProjects:link
/// Links a existing Apps Script project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_apps_script_projects_link_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_apps_script_projects_link_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_apps_script_projects_link_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/appsScriptProjects:link
/// Links a existing Apps Script project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_apps_script_projects_link_builder()` + `integrations_projects_locations_apps_script_projects_link_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_apps_script_projects_link(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_apps_script_projects_link_builder(client, parent, body)?;
    integrations_projects_locations_apps_script_projects_link_execute(builder)
}

/// GET v1/{+parent}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_auth_configs_create_execute()` to send, or `integrations_projects_locations_auth_configs_create` for simplest API.

pub fn integrations_projects_locations_auth_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    clientCertificate_encryptedPrivateKey: Option<&str>,
    clientCertificate_passphrase: Option<&str>,
    clientCertificate_sslCertificate: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaAuthConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/authConfigs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "clientCertificate_encryptedPrivateKey",
            clientCertificate_encryptedPrivateKey
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "clientCertificate_passphrase",
            clientCertificate_passphrase
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "clientCertificate_sslCertificate",
            clientCertificate_sslCertificate
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_auth_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_auth_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaAuthConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_auth_configs_create_builder()` + `integrations_projects_locations_auth_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    clientCertificate_encryptedPrivateKey: Option<&str>,
    clientCertificate_passphrase: Option<&str>,
    clientCertificate_sslCertificate: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaAuthConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_auth_configs_create_builder(
        client,
        parent,
        clientCertificate_encryptedPrivateKey,
        clientCertificate_passphrase,
        clientCertificate_sslCertificate,
        body,
    )?;
    integrations_projects_locations_auth_configs_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes an auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_auth_configs_delete_execute()` to send, or `integrations_projects_locations_auth_configs_delete` for simplest API.

pub fn integrations_projects_locations_auth_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_auth_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_auth_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_auth_configs_delete_builder()` + `integrations_projects_locations_auth_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_auth_configs_delete_builder(client, name)?;
    integrations_projects_locations_auth_configs_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets a complete auth config. If the auth config doesn't exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_auth_configs_get_execute()` to send, or `integrations_projects_locations_auth_configs_get` for simplest API.

pub fn integrations_projects_locations_auth_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a complete auth config. If the auth config doesn't exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_auth_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_auth_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaAuthConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a complete auth config. If the auth config doesn't exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_auth_configs_get_builder()` + `integrations_projects_locations_auth_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_auth_configs_get_builder(client, name)?;
    integrations_projects_locations_auth_configs_get_execute(builder)
}

/// GET v1/{+parent}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_auth_configs_list_execute()` to send, or `integrations_projects_locations_auth_configs_list` for simplest API.

pub fn integrations_projects_locations_auth_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/authConfigs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_auth_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_auth_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListAuthConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListAuthConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_auth_configs_list_builder()` + `integrations_projects_locations_auth_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListAuthConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_auth_configs_list_builder(
        client, parent, filter, pageSize, pageToken, readMask,
    )?;
    integrations_projects_locations_auth_configs_list_execute(builder)
}

/// GET v1/{+name}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_auth_configs_patch_execute()` to send, or `integrations_projects_locations_auth_configs_patch` for simplest API.

pub fn integrations_projects_locations_auth_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    clientCertificate_encryptedPrivateKey: Option<&str>,
    clientCertificate_passphrase: Option<&str>,
    clientCertificate_sslCertificate: Option<&str>,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaAuthConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "clientCertificate_encryptedPrivateKey",
            clientCertificate_encryptedPrivateKey
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "clientCertificate_passphrase",
            clientCertificate_passphrase
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "clientCertificate_sslCertificate",
            clientCertificate_sslCertificate
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_auth_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_auth_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaAuthConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_auth_configs_patch_builder()` + `integrations_projects_locations_auth_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    clientCertificate_encryptedPrivateKey: Option<&str>,
    clientCertificate_passphrase: Option<&str>,
    clientCertificate_sslCertificate: Option<&str>,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaAuthConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_auth_configs_patch_builder(
        client,
        name,
        clientCertificate_encryptedPrivateKey,
        clientCertificate_passphrase,
        clientCertificate_sslCertificate,
        updateMask,
        body,
    )?;
    integrations_projects_locations_auth_configs_patch_execute(builder)
}

/// GET v1/{+parent}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_certificates_create_execute()` to send, or `integrations_projects_locations_certificates_create` for simplest API.

pub fn integrations_projects_locations_certificates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaCertificate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/certificates", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_certificates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_certificates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_certificates_create_builder()` + `integrations_projects_locations_certificates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaCertificate,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_certificates_create_builder(client, parent, body)?;
    integrations_projects_locations_certificates_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a certificate
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_certificates_delete_execute()` to send, or `integrations_projects_locations_certificates_delete` for simplest API.

pub fn integrations_projects_locations_certificates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a certificate
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_certificates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_certificates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a certificate
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_certificates_delete_builder()` + `integrations_projects_locations_certificates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_certificates_delete_builder(client, name)?;
    integrations_projects_locations_certificates_delete_execute(builder)
}

/// GET v1/{+name}
/// Get a certificates in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_certificates_get_execute()` to send, or `integrations_projects_locations_certificates_get` for simplest API.

pub fn integrations_projects_locations_certificates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a certificates in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_certificates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_certificates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a certificates in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_certificates_get_builder()` + `integrations_projects_locations_certificates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_certificates_get_builder(client, name)?;
    integrations_projects_locations_certificates_get_execute(builder)
}

/// GET v1/{+parent}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_certificates_list_execute()` to send, or `integrations_projects_locations_certificates_list` for simplest API.

pub fn integrations_projects_locations_certificates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/certificates", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_certificates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_certificates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListCertificatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListCertificatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_certificates_list_builder()` + `integrations_projects_locations_certificates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListCertificatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_certificates_list_builder(
        client, parent, filter, pageSize, pageToken, readMask,
    )?;
    integrations_projects_locations_certificates_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_certificates_patch_execute()` to send, or `integrations_projects_locations_certificates_patch` for simplest API.

pub fn integrations_projects_locations_certificates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaCertificate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_certificates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_certificates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_certificates_patch_builder()` + `integrations_projects_locations_certificates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaCertificate,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_certificates_patch_builder(client, name, updateMask, body)?;
    integrations_projects_locations_certificates_patch_execute(builder)
}

/// GET v1/{+parent}/clients:changeConfig
/// Updates the client customer configuration for the given project and location resource name
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_change_config_execute()` to send, or `integrations_projects_locations_clients_change_config` for simplest API.

pub fn integrations_projects_locations_clients_change_config_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaChangeCustomerConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/clients:changeConfig",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/clients:changeConfig
/// Updates the client customer configuration for the given project and location resource name
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_clients_change_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_clients_change_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_change_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaChangeCustomerConfigResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaChangeCustomerConfigResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clients:changeConfig
/// Updates the client customer configuration for the given project and location resource name
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_change_config_builder()` + `integrations_projects_locations_clients_change_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_change_config(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaChangeCustomerConfigRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaChangeCustomerConfigResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_clients_change_config_builder(client, parent, body)?;
    integrations_projects_locations_clients_change_config_execute(builder)
}

/// GET v1/{+parent}/clients:deprovision
/// Perform the deprovisioning steps to disable a user GCP project to use IP and purge all related data in a wipeout-compliant way.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_deprovision_execute()` to send, or `integrations_projects_locations_clients_deprovision` for simplest API.

pub fn integrations_projects_locations_clients_deprovision_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaDeprovisionClientRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/clients:deprovision",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/clients:deprovision
/// Perform the deprovisioning steps to disable a user GCP project to use IP and purge all related data in a wipeout-compliant way.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_clients_deprovision_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_clients_deprovision_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_deprovision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clients:deprovision
/// Perform the deprovisioning steps to disable a user GCP project to use IP and purge all related data in a wipeout-compliant way.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_deprovision_builder()` + `integrations_projects_locations_clients_deprovision_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_deprovision(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaDeprovisionClientRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_clients_deprovision_builder(client, parent, body)?;
    integrations_projects_locations_clients_deprovision_execute(builder)
}

/// GET v1/{+parent}/clients:provision
/// Perform the provisioning steps to enable a user GCP project to use IP. If GCP project already registered on IP end via Apigee Integration, provisioning will fail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_provision_execute()` to send, or `integrations_projects_locations_clients_provision` for simplest API.

pub fn integrations_projects_locations_clients_provision_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaProvisionClientRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/clients:provision",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/clients:provision
/// Perform the provisioning steps to enable a user GCP project to use IP. If GCP project already registered on IP end via Apigee Integration, provisioning will fail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_clients_provision_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_clients_provision_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_provision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clients:provision
/// Perform the provisioning steps to enable a user GCP project to use IP. If GCP project already registered on IP end via Apigee Integration, provisioning will fail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_provision_builder()` + `integrations_projects_locations_clients_provision_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_provision(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaProvisionClientRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_clients_provision_builder(client, parent, body)?;
    integrations_projects_locations_clients_provision_execute(builder)
}

/// GET v1/{+parent}/clients:provisionClientPostProcessor
/// Perform post provisioning steps after client is provisioned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_provision_client_post_processor_execute()` to send, or `integrations_projects_locations_clients_provision_client_post_processor` for simplest API.

pub fn integrations_projects_locations_clients_provision_client_post_processor_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/clients:provisionClientPostProcessor",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/clients:provisionClientPostProcessor
/// Perform post provisioning steps after client is provisioned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_clients_provision_client_post_processor_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_clients_provision_client_post_processor_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_provision_client_post_processor_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clients:provisionClientPostProcessor
/// Perform post provisioning steps after client is provisioned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_provision_client_post_processor_builder()` + `integrations_projects_locations_clients_provision_client_post_processor_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_provision_client_post_processor(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_clients_provision_client_post_processor_builder(
        client, parent, body,
    )?;
    integrations_projects_locations_clients_provision_client_post_processor_execute(builder)
}

/// GET v1/{+parent}/clients:replace
/// Update run-as service account for provisioned client
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_replace_execute()` to send, or `integrations_projects_locations_clients_replace` for simplest API.

pub fn integrations_projects_locations_clients_replace_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaReplaceServiceAccountRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/clients:replace",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/clients:replace
/// Update run-as service account for provisioned client
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_clients_replace_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_clients_replace_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_replace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clients:replace
/// Update run-as service account for provisioned client
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_replace_builder()` + `integrations_projects_locations_clients_replace_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_replace(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaReplaceServiceAccountRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_clients_replace_builder(client, parent, body)?;
    integrations_projects_locations_clients_replace_execute(builder)
}

/// GET v1/{+parent}/clients:switch
/// Update client from GMEK to CMEK
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_switch_execute()` to send, or `integrations_projects_locations_clients_switch` for simplest API.

pub fn integrations_projects_locations_clients_switch_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaSwitchEncryptionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/clients:switch", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/clients:switch
/// Update client from GMEK to CMEK
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_clients_switch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_clients_switch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_switch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clients:switch
/// Update client from GMEK to CMEK
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_switch_builder()` + `integrations_projects_locations_clients_switch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_switch(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaSwitchEncryptionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_clients_switch_builder(client, parent, body)?;
    integrations_projects_locations_clients_switch_execute(builder)
}

/// GET v1/{+parent}/clients:switchVariableMasking
/// Update variable masking for provisioned client
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_switch_variable_masking_execute()` to send, or `integrations_projects_locations_clients_switch_variable_masking` for simplest API.

pub fn integrations_projects_locations_clients_switch_variable_masking_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaSwitchVariableMaskingRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/clients:switchVariableMasking",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/clients:switchVariableMasking
/// Update variable masking for provisioned client
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_clients_switch_variable_masking_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_clients_switch_variable_masking_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_switch_variable_masking_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clients:switchVariableMasking
/// Update variable masking for provisioned client
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_switch_variable_masking_builder()` + `integrations_projects_locations_clients_switch_variable_masking_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_switch_variable_masking(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaSwitchVariableMaskingRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_clients_switch_variable_masking_builder(
        client, parent, body,
    )?;
    integrations_projects_locations_clients_switch_variable_masking_execute(builder)
}

/// GET v1/{+parent}/clients:toggleHttp
/// E`nable/Disable` http call for provisioned client
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_toggle_http_execute()` to send, or `integrations_projects_locations_clients_toggle_http` for simplest API.

pub fn integrations_projects_locations_clients_toggle_http_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaToggleHttpRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/clients:toggleHttp",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/clients:toggleHttp
/// E`nable/Disable` http call for provisioned client
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_clients_toggle_http_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_clients_toggle_http_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_toggle_http_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clients:toggleHttp
/// E`nable/Disable` http call for provisioned client
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_toggle_http_builder()` + `integrations_projects_locations_clients_toggle_http_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_toggle_http(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaToggleHttpRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_clients_toggle_http_builder(client, parent, body)?;
    integrations_projects_locations_clients_toggle_http_execute(builder)
}

/// GET v1/{+parent}/cloudFunctions
/// Creates a cloud function project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_cloud_functions_create_execute()` to send, or `integrations_projects_locations_cloud_functions_create` for simplest API.

pub fn integrations_projects_locations_cloud_functions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaCreateCloudFunctionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/cloudFunctions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/cloudFunctions
/// Creates a cloud function project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_cloud_functions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_cloud_functions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_cloud_functions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/cloudFunctions
/// Creates a cloud function project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_cloud_functions_create_builder()` + `integrations_projects_locations_cloud_functions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_cloud_functions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaCreateCloudFunctionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_cloud_functions_create_builder(client, parent, body)?;
    integrations_projects_locations_cloud_functions_create_execute(builder)
}

/// GET v1/{+name}
/// Lists the available entities and actions associated with a Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_connections_get_connection_schema_metadata_execute()` to send, or `integrations_projects_locations_connections_get_connection_schema_metadata` for simplest API.

pub fn integrations_projects_locations_connections_get_connection_schema_metadata_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Lists the available entities and actions associated with a Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_connections_get_connection_schema_metadata_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_connections_get_connection_schema_metadata_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_get_connection_schema_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaConnectionSchemaMetadata>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaConnectionSchemaMetadata =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Lists the available entities and actions associated with a Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_connections_get_connection_schema_metadata_builder()` + `integrations_projects_locations_connections_get_connection_schema_metadata_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_get_connection_schema_metadata(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaConnectionSchemaMetadata>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_connections_get_connection_schema_metadata_builder(
            client, name,
        )?;
    integrations_projects_locations_connections_get_connection_schema_metadata_execute(builder)
}

/// GET v1/{+parent}/connections
/// Lists Connections in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_connections_list_execute()` to send, or `integrations_projects_locations_connections_list` for simplest API.

pub fn integrations_projects_locations_connections_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/connections", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/connections
/// Lists Connections in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_connections_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_connections_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListConnectionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListConnectionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/connections
/// Lists Connections in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_connections_list_builder()` + `integrations_projects_locations_connections_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListConnectionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_connections_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    integrations_projects_locations_connections_list_execute(builder)
}

/// GET v1/{+parent}/runtimeActionSchemas
/// Lists the JSON schemas for the inputs and outputs of actions, filtered by action name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_connections_runtime_action_schemas_list_execute()` to send, or `integrations_projects_locations_connections_runtime_action_schemas_list` for simplest API.

pub fn integrations_projects_locations_connections_runtime_action_schemas_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/runtimeActionSchemas",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/runtimeActionSchemas
/// Lists the JSON schemas for the inputs and outputs of actions, filtered by action name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_connections_runtime_action_schemas_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_connections_runtime_action_schemas_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_runtime_action_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListRuntimeActionSchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListRuntimeActionSchemasResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/runtimeActionSchemas
/// Lists the JSON schemas for the inputs and outputs of actions, filtered by action name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_connections_runtime_action_schemas_list_builder()` + `integrations_projects_locations_connections_runtime_action_schemas_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_runtime_action_schemas_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListRuntimeActionSchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_connections_runtime_action_schemas_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    integrations_projects_locations_connections_runtime_action_schemas_list_execute(builder)
}

/// GET v1/{+parent}/runtimeEntitySchemas
/// Lists the JSON schemas for the properties of runtime entities, filtered by entity name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_connections_runtime_entity_schemas_list_execute()` to send, or `integrations_projects_locations_connections_runtime_entity_schemas_list` for simplest API.

pub fn integrations_projects_locations_connections_runtime_entity_schemas_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/runtimeEntitySchemas",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/runtimeEntitySchemas
/// Lists the JSON schemas for the properties of runtime entities, filtered by entity name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_connections_runtime_entity_schemas_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_connections_runtime_entity_schemas_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_runtime_entity_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListRuntimeEntitySchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListRuntimeEntitySchemasResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/runtimeEntitySchemas
/// Lists the JSON schemas for the properties of runtime entities, filtered by entity name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_connections_runtime_entity_schemas_list_builder()` + `integrations_projects_locations_connections_runtime_entity_schemas_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_runtime_entity_schemas_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListRuntimeEntitySchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_connections_runtime_entity_schemas_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    integrations_projects_locations_connections_runtime_entity_schemas_list_execute(builder)
}

/// GET v1/{+name}
/// Delete the selected integration and all versions inside
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_delete_execute()` to send, or `integrations_projects_locations_integrations_delete` for simplest API.

pub fn integrations_projects_locations_integrations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete the selected integration and all versions inside
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete the selected integration and all versions inside
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_delete_builder()` + `integrations_projects_locations_integrations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_delete_builder(client, name)?;
    integrations_projects_locations_integrations_delete_execute(builder)
}

/// GET v1/{+name}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_execute_execute()` to send, or `integrations_projects_locations_integrations_execute` for simplest API.

pub fn integrations_projects_locations_integrations_execute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaExecuteIntegrationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:execute", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_execute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_execute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_execute_builder()` + `integrations_projects_locations_integrations_execute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_execute(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaExecuteIntegrationsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_execute_builder(client, name, body)?;
    integrations_projects_locations_integrations_execute_execute(builder)
}

/// GET v1/{+name}:executeEvent
/// Executes an integration on receiving events from Integration Connector triggers, Eventarc or CPS Trigger. Input data to integration is received in body in json format
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_execute_event_execute()` to send, or `integrations_projects_locations_integrations_execute_event` for simplest API.

pub fn integrations_projects_locations_integrations_execute_event_builder(
    client: &SimpleHttpClient,
    name: &str,
    triggerId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:executeEvent", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "triggerId",
            triggerId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:executeEvent
/// Executes an integration on receiving events from Integration Connector triggers, Eventarc or CPS Trigger. Input data to integration is received in body in json format
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_execute_event_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_execute_event_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_execute_event_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecuteEventResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecuteEventResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:executeEvent
/// Executes an integration on receiving events from Integration Connector triggers, Eventarc or CPS Trigger. Input data to integration is received in body in json format
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_execute_event_builder()` + `integrations_projects_locations_integrations_execute_event_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_execute_event(
    client: &SimpleHttpClient,
    name: &str,
    triggerId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecuteEventResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_execute_event_builder(
        client, name, triggerId,
    )?;
    integrations_projects_locations_integrations_execute_event_execute(builder)
}

/// GET v1/{+parent}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_list_execute()` to send, or `integrations_projects_locations_integrations_list` for simplest API.

pub fn integrations_projects_locations_integrations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/integrations", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_list_builder()` + `integrations_projects_locations_integrations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    integrations_projects_locations_integrations_list_execute(builder)
}

/// GET v1/{+name}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_schedule_execute()` to send, or `integrations_projects_locations_integrations_schedule` for simplest API.

pub fn integrations_projects_locations_integrations_schedule_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaScheduleIntegrationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:schedule", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_schedule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_schedule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_schedule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_schedule_builder()` + `integrations_projects_locations_integrations_schedule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_schedule(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaScheduleIntegrationsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_schedule_builder(client, name, body)?;
    integrations_projects_locations_integrations_schedule_execute(builder)
}

/// GET v1/{+parent}/integrations:search
/// Searches and returns the list of integrations in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_search_execute()` to send, or `integrations_projects_locations_integrations_search` for simplest API.

pub fn integrations_projects_locations_integrations_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    enableNaturalLanguageQueryUnderstanding: Option<bool>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/integrations:search",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "enableNaturalLanguageQueryUnderstanding",
            enableNaturalLanguageQueryUnderstanding
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "query",
            query
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/integrations:search
/// Searches and returns the list of integrations in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaSearchIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSearchIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/integrations:search
/// Searches and returns the list of integrations in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_search_builder()` + `integrations_projects_locations_integrations_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_search(
    client: &SimpleHttpClient,
    parent: &str,
    enableNaturalLanguageQueryUnderstanding: Option<bool>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaSearchIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_search_builder(
        client,
        parent,
        enableNaturalLanguageQueryUnderstanding,
        filter,
        pageSize,
        pageToken,
        query,
    )?;
    integrations_projects_locations_integrations_search_execute(builder)
}

/// GET v1/{+name}:test
/// Execute the integration in draft state
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_test_execute()` to send, or `integrations_projects_locations_integrations_test` for simplest API.

pub fn integrations_projects_locations_integrations_test_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaTestIntegrationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:test", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:test
/// Execute the integration in draft state
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_test_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_test_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:test
/// Execute the integration in draft state
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_test_builder()` + `integrations_projects_locations_integrations_test_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_test(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaTestIntegrationsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_test_builder(client, name, body)?;
    integrations_projects_locations_integrations_test_execute(builder)
}

/// GET v1/{+name}:cancel
/// Cancellation of an execution and associated sub-executions. This will not cancel an IN_PROCESS or completed(SUCCESSFUL, FAILED or CANCELLED) executions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_cancel_execute()` to send, or `integrations_projects_locations_integrations_executions_cancel` for simplest API.

pub fn integrations_projects_locations_integrations_executions_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaCancelExecutionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Cancellation of an execution and associated sub-executions. This will not cancel an IN_PROCESS or completed(SUCCESSFUL, FAILED or CANCELLED) executions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_executions_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_executions_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCancelExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCancelExecutionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Cancellation of an execution and associated sub-executions. This will not cancel an IN_PROCESS or completed(SUCCESSFUL, FAILED or CANCELLED) executions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_cancel_builder()` + `integrations_projects_locations_integrations_executions_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaCancelExecutionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCancelExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_executions_cancel_builder(client, name, body)?;
    integrations_projects_locations_integrations_executions_cancel_execute(builder)
}

/// GET v1/{+name}:download
/// Download the execution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_download_execute()` to send, or `integrations_projects_locations_integrations_executions_download` for simplest API.

pub fn integrations_projects_locations_integrations_executions_download_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:download", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:download
/// Download the execution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_executions_download_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_executions_download_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadExecutionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:download
/// Download the execution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_download_builder()` + `integrations_projects_locations_integrations_executions_download_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_download(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_executions_download_builder(client, name)?;
    integrations_projects_locations_integrations_executions_download_execute(builder)
}

/// GET v1/{+name}
/// Get an execution in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_get_execute()` to send, or `integrations_projects_locations_integrations_executions_get` for simplest API.

pub fn integrations_projects_locations_integrations_executions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get an execution in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_executions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_executions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecution =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get an execution in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_get_builder()` + `integrations_projects_locations_integrations_executions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_executions_get_builder(client, name)?;
    integrations_projects_locations_integrations_executions_get_execute(builder)
}

/// GET v1/{+parent}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_list_execute()` to send, or `integrations_projects_locations_integrations_executions_list` for simplest API.

pub fn integrations_projects_locations_integrations_executions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    filterParams_customFilter: Option<&str>,
    filterParams_endTime: Option<&str>,
    filterParams_eventStatuses: Option<&str>,
    filterParams_executionId: Option<&str>,
    filterParams_parameterKey: Option<&str>,
    filterParams_parameterPairKey: Option<&str>,
    filterParams_parameterPairValue: Option<&str>,
    filterParams_parameterType: Option<&str>,
    filterParams_parameterValue: Option<&str>,
    filterParams_startTime: Option<&str>,
    filterParams_taskStatuses: Option<&str>,
    filterParams_workflowName: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
    refreshAcl: Option<bool>,
    snapshotMetadataWithoutParams: Option<bool>,
    truncateParams: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/executions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_customFilter",
            filterParams_customFilter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_endTime",
            filterParams_endTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_eventStatuses",
            filterParams_eventStatuses
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_executionId",
            filterParams_executionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_parameterKey",
            filterParams_parameterKey
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_parameterPairKey",
            filterParams_parameterPairKey
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_parameterPairValue",
            filterParams_parameterPairValue
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_parameterType",
            filterParams_parameterType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_parameterValue",
            filterParams_parameterValue
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_startTime",
            filterParams_startTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_taskStatuses",
            filterParams_taskStatuses
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_workflowName",
            filterParams_workflowName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "refreshAcl",
            refreshAcl
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "snapshotMetadataWithoutParams",
            snapshotMetadataWithoutParams
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "truncateParams",
            truncateParams
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_executions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_executions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListExecutionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_list_builder()` + `integrations_projects_locations_integrations_executions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    filterParams_customFilter: Option<&str>,
    filterParams_endTime: Option<&str>,
    filterParams_eventStatuses: Option<&str>,
    filterParams_executionId: Option<&str>,
    filterParams_parameterKey: Option<&str>,
    filterParams_parameterPairKey: Option<&str>,
    filterParams_parameterPairValue: Option<&str>,
    filterParams_parameterType: Option<&str>,
    filterParams_parameterValue: Option<&str>,
    filterParams_startTime: Option<&str>,
    filterParams_taskStatuses: Option<&str>,
    filterParams_workflowName: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
    refreshAcl: Option<bool>,
    snapshotMetadataWithoutParams: Option<bool>,
    truncateParams: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_executions_list_builder(
        client,
        parent,
        filter,
        filterParams_customFilter,
        filterParams_endTime,
        filterParams_eventStatuses,
        filterParams_executionId,
        filterParams_parameterKey,
        filterParams_parameterPairKey,
        filterParams_parameterPairValue,
        filterParams_parameterType,
        filterParams_parameterValue,
        filterParams_startTime,
        filterParams_taskStatuses,
        filterParams_workflowName,
        orderBy,
        pageSize,
        pageToken,
        readMask,
        refreshAcl,
        snapshotMetadataWithoutParams,
        truncateParams,
    )?;
    integrations_projects_locations_integrations_executions_list_execute(builder)
}

/// GET v1/{+name}:replay
/// Re-execute an existing execution, with same request parameters and execution strategy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_replay_execute()` to send, or `integrations_projects_locations_integrations_executions_replay` for simplest API.

pub fn integrations_projects_locations_integrations_executions_replay_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaReplayExecutionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:replay", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:replay
/// Re-execute an existing execution, with same request parameters and execution strategy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_executions_replay_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_executions_replay_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_replay_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaReplayExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaReplayExecutionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:replay
/// Re-execute an existing execution, with same request parameters and execution strategy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_replay_builder()` + `integrations_projects_locations_integrations_executions_replay_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_replay(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaReplayExecutionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaReplayExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_executions_replay_builder(client, name, body)?;
    integrations_projects_locations_integrations_executions_replay_execute(builder)
}

/// GET v1/{+name}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_suspensions_lift_execute()` to send, or `integrations_projects_locations_integrations_executions_suspensions_lift` for simplest API.

pub fn integrations_projects_locations_integrations_executions_suspensions_lift_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaLiftSuspensionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:lift", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_executions_suspensions_lift_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_executions_suspensions_lift_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_suspensions_lift_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaLiftSuspensionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaLiftSuspensionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_suspensions_lift_builder()` + `integrations_projects_locations_integrations_executions_suspensions_lift_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_suspensions_lift(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaLiftSuspensionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaLiftSuspensionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_executions_suspensions_lift_builder(
        client, name, body,
    )?;
    integrations_projects_locations_integrations_executions_suspensions_lift_execute(builder)
}

/// GET v1/{+parent}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_suspensions_list_execute()` to send, or `integrations_projects_locations_integrations_executions_suspensions_list` for simplest API.

pub fn integrations_projects_locations_integrations_executions_suspensions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/suspensions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_executions_suspensions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_executions_suspensions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_suspensions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSuspensionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListSuspensionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_suspensions_list_builder()` + `integrations_projects_locations_integrations_executions_suspensions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_suspensions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSuspensionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_executions_suspensions_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    integrations_projects_locations_integrations_executions_suspensions_list_execute(builder)
}

/// GET v1/{+name}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_suspensions_resolve_execute()` to send, or `integrations_projects_locations_integrations_executions_suspensions_resolve` for simplest API.

pub fn integrations_projects_locations_integrations_executions_suspensions_resolve_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaResolveSuspensionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:resolve", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_executions_suspensions_resolve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_executions_suspensions_resolve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_suspensions_resolve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaResolveSuspensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaResolveSuspensionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_suspensions_resolve_builder()` + `integrations_projects_locations_integrations_executions_suspensions_resolve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_suspensions_resolve(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaResolveSuspensionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaResolveSuspensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_executions_suspensions_resolve_builder(
            client, name, body,
        )?;
    integrations_projects_locations_integrations_executions_suspensions_resolve_execute(builder)
}

/// GET v1/{+parent}/versions
/// Create a integration with a draft version in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_create_execute()` to send, or `integrations_projects_locations_integrations_versions_create` for simplest API.

pub fn integrations_projects_locations_integrations_versions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    createSampleIntegrations: Option<bool>,
    newIntegration: Option<bool>,
    body: &GoogleCloudIntegrationsV1alphaIntegrationVersion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/versions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "createSampleIntegrations",
            createSampleIntegrations
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "newIntegration",
            newIntegration
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/versions
/// Create a integration with a draft version in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaIntegrationVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/versions
/// Create a integration with a draft version in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_create_builder()` + `integrations_projects_locations_integrations_versions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_create(
    client: &SimpleHttpClient,
    parent: &str,
    createSampleIntegrations: Option<bool>,
    newIntegration: Option<bool>,
    body: &GoogleCloudIntegrationsV1alphaIntegrationVersion,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_create_builder(
        client,
        parent,
        createSampleIntegrations,
        newIntegration,
        body,
    )?;
    integrations_projects_locations_integrations_versions_create_execute(builder)
}

/// GET v1/{+name}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_delete_execute()` to send, or `integrations_projects_locations_integrations_versions_delete` for simplest API.

pub fn integrations_projects_locations_integrations_versions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_delete_builder()` + `integrations_projects_locations_integrations_versions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_delete_builder(client, name)?;
    integrations_projects_locations_integrations_versions_delete_execute(builder)
}

/// GET v1/{+name}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_download_execute()` to send, or `integrations_projects_locations_integrations_versions_download` for simplest API.

pub fn integrations_projects_locations_integrations_versions_download_builder(
    client: &SimpleHttpClient,
    name: &str,
    fileFormat: Option<&str>,
    files: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:download", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fileFormat",
            fileFormat
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "files",
            files
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_download_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_download_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_download_builder()` + `integrations_projects_locations_integrations_versions_download_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_download(
    client: &SimpleHttpClient,
    name: &str,
    fileFormat: Option<&str>,
    files: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_download_builder(
        client, name, fileFormat, files,
    )?;
    integrations_projects_locations_integrations_versions_download_execute(builder)
}

/// GET v1/{+name}:downloadJsonPackage
/// Downloads an Integration version package like IntegrationVersion,Integration Config etc. Retrieves the IntegrationVersion package for a given integration_id and returns the response as a JSON.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_download_json_package_execute()` to send, or `integrations_projects_locations_integrations_versions_download_json_package` for simplest API.

pub fn integrations_projects_locations_integrations_versions_download_json_package_builder(
    client: &SimpleHttpClient,
    name: &str,
    files: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:downloadJsonPackage",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "files",
            files
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:downloadJsonPackage
/// Downloads an Integration version package like IntegrationVersion,Integration Config etc. Retrieves the IntegrationVersion package for a given integration_id and returns the response as a JSON.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_download_json_package_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_download_json_package_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_download_json_package_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadJsonPackageResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadJsonPackageResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:downloadJsonPackage
/// Downloads an Integration version package like IntegrationVersion,Integration Config etc. Retrieves the IntegrationVersion package for a given integration_id and returns the response as a JSON.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_download_json_package_builder()` + `integrations_projects_locations_integrations_versions_download_json_package_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_download_json_package(
    client: &SimpleHttpClient,
    name: &str,
    files: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadJsonPackageResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_download_json_package_builder(
            client, name, files,
        )?;
    integrations_projects_locations_integrations_versions_download_json_package_execute(builder)
}

/// GET v1/{+name}
/// Get a integration in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_get_execute()` to send, or `integrations_projects_locations_integrations_versions_get` for simplest API.

pub fn integrations_projects_locations_integrations_versions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a integration in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaIntegrationVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a integration in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_get_builder()` + `integrations_projects_locations_integrations_versions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_get_builder(client, name)?;
    integrations_projects_locations_integrations_versions_get_execute(builder)
}

/// GET v1/{+parent}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_list_execute()` to send, or `integrations_projects_locations_integrations_versions_list` for simplest API.

pub fn integrations_projects_locations_integrations_versions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/versions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fieldMask",
            fieldMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_list_builder()` + `integrations_projects_locations_integrations_versions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_list(
    client: &SimpleHttpClient,
    parent: &str,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_list_builder(
        client, parent, fieldMask, filter, orderBy, pageSize, pageToken,
    )?;
    integrations_projects_locations_integrations_versions_list_execute(builder)
}

/// GET v1/{+name}
/// Update a integration with a draft version in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_patch_execute()` to send, or `integrations_projects_locations_integrations_versions_patch` for simplest API.

pub fn integrations_projects_locations_integrations_versions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaIntegrationVersion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update a integration with a draft version in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaIntegrationVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update a integration with a draft version in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_patch_builder()` + `integrations_projects_locations_integrations_versions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaIntegrationVersion,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_patch_builder(
        client, name, updateMask, body,
    )?;
    integrations_projects_locations_integrations_versions_patch_execute(builder)
}

/// GET v1/{+name}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_publish_execute()` to send, or `integrations_projects_locations_integrations_versions_publish` for simplest API.

pub fn integrations_projects_locations_integrations_versions_publish_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaPublishIntegrationVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:publish", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_publish_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_publish_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_publish_builder()` + `integrations_projects_locations_integrations_versions_publish_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_publish(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaPublishIntegrationVersionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_publish_builder(client, name, body)?;
    integrations_projects_locations_integrations_versions_publish_execute(builder)
}

/// GET v1/{+name}:test
/// Execute the integration in draft state
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_execute()` to send, or `integrations_projects_locations_integrations_versions_test` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaTestIntegrationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:test", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:test
/// Execute the integration in draft state
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_test_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_test_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:test
/// Execute the integration in draft state
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_builder()` + `integrations_projects_locations_integrations_versions_test_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaTestIntegrationsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_test_builder(client, name, body)?;
    integrations_projects_locations_integrations_versions_test_execute(builder)
}

/// GET v1/{+name}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_unpublish_execute()` to send, or `integrations_projects_locations_integrations_versions_unpublish` for simplest API.

pub fn integrations_projects_locations_integrations_versions_unpublish_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaUnpublishIntegrationVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:unpublish", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_unpublish_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_unpublish_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_unpublish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_unpublish_builder()` + `integrations_projects_locations_integrations_versions_unpublish_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_unpublish(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaUnpublishIntegrationVersionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_unpublish_builder(
        client, name, body,
    )?;
    integrations_projects_locations_integrations_versions_unpublish_execute(builder)
}

/// GET v1/{+parent}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_upload_execute()` to send, or `integrations_projects_locations_integrations_versions_upload` for simplest API.

pub fn integrations_projects_locations_integrations_versions_upload_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaUploadIntegrationVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/versions:upload",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_upload_builder()` + `integrations_projects_locations_integrations_versions_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_upload(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaUploadIntegrationVersionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_upload_builder(client, parent, body)?;
    integrations_projects_locations_integrations_versions_upload_execute(builder)
}

/// GET v1/{+parent}/testCases
/// Creates a new test case
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_create_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_create` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    testCaseId: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaTestCase,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/testCases", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "testCaseId",
            testCaseId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/testCases
/// Creates a new test case
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_test_cases_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestCase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/testCases
/// Creates a new test case
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_create_builder()` + `integrations_projects_locations_integrations_versions_test_cases_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_create(
    client: &SimpleHttpClient,
    parent: &str,
    testCaseId: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaTestCase,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_create_builder(
        client, parent, testCaseId, body,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a test case
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_delete_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_delete` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a test case
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_test_cases_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a test case
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_delete_builder()` + `integrations_projects_locations_integrations_versions_test_cases_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_delete_builder(
        client, name,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_delete_execute(builder)
}

/// GET v1/{+name}:download
/// Downloads a test case. Retrieves the TestCase for a given test_case_id and returns the response as a string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_download_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_download` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_download_builder(
    client: &SimpleHttpClient,
    name: &str,
    fileFormat: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:download", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fileFormat",
            fileFormat
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:download
/// Downloads a test case. Retrieves the TestCase for a given test_case_id and returns the response as a string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_download_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_test_cases_download_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadTestCaseResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadTestCaseResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:download
/// Downloads a test case. Retrieves the TestCase for a given test_case_id and returns the response as a string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_download_builder()` + `integrations_projects_locations_integrations_versions_test_cases_download_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_download(
    client: &SimpleHttpClient,
    name: &str,
    fileFormat: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadTestCaseResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_test_cases_download_builder(
            client, name, fileFormat,
        )?;
    integrations_projects_locations_integrations_versions_test_cases_download_execute(builder)
}

/// GET v1/{+parent}/testCases:execute
/// Executes all test cases in an integration version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_execute_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_execute` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaExecuteTestCasesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/testCases:execute",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/testCases:execute
/// Executes all test cases in an integration version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_execute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_test_cases_execute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteTestCasesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecuteTestCasesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/testCases:execute
/// Executes all test cases in an integration version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_execute_builder()` + `integrations_projects_locations_integrations_versions_test_cases_execute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaExecuteTestCasesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteTestCasesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_execute_builder(
        client, parent, body,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_execute_execute(builder)
}

/// GET v1/{+testCaseName}:executeTest
/// Executes functional test
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_execute_test_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_execute_test` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute_test_builder(
    client: &SimpleHttpClient,
    testCaseName: &str,
    body: &GoogleCloudIntegrationsV1alphaExecuteTestCaseRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+testCaseName}:executeTest",
        testCaseName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+testCaseName}:executeTest
/// Executes functional test
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_execute_test_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_test_cases_execute_test_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteTestCaseResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecuteTestCaseResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+testCaseName}:executeTest
/// Executes functional test
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_execute_test_builder()` + `integrations_projects_locations_integrations_versions_test_cases_execute_test_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute_test(
    client: &SimpleHttpClient,
    testCaseName: &str,
    body: &GoogleCloudIntegrationsV1alphaExecuteTestCaseRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteTestCaseResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_test_cases_execute_test_builder(
            client,
            testCaseName,
            body,
        )?;
    integrations_projects_locations_integrations_versions_test_cases_execute_test_execute(builder)
}

/// GET v1/{+name}
/// Get a test case
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_get_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_get` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a test case
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_test_cases_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestCase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a test case
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_get_builder()` + `integrations_projects_locations_integrations_versions_test_cases_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_test_cases_get_builder(client, name)?;
    integrations_projects_locations_integrations_versions_test_cases_get_execute(builder)
}

/// GET v1/{+parent}/testCases
/// Lists all the test cases that satisfy the filters.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_list_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_list` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/testCases", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/testCases
/// Lists all the test cases that satisfy the filters.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_test_cases_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListTestCasesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListTestCasesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/testCases
/// Lists all the test cases that satisfy the filters.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_list_builder()` + `integrations_projects_locations_integrations_versions_test_cases_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListTestCasesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken, readMask,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_list_execute(builder)
}

/// GET v1/{+name}
/// Updates a test case
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_patch_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_patch` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaTestCase,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates a test case
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_test_cases_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestCase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates a test case
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_patch_builder()` + `integrations_projects_locations_integrations_versions_test_cases_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaTestCase,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_patch_builder(
        client, name, updateMask, body,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_patch_execute(builder)
}

/// GET v1/{+name}:takeoverEditLock
/// Clear the lock fields and assign them to current user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaTakeoverTestCaseEditLockRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:takeoverEditLock", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:takeoverEditLock
/// Clear the lock fields and assign them to current user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestCase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:takeoverEditLock
/// Clear the lock fields and assign them to current user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_builder()` + `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaTakeoverTestCaseEditLockRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_builder(client, name, body)?;
    integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_execute(
        builder,
    )
}

/// GET v1/{+parent}/testCases:upload
/// Uploads a test case. The content can be a previously downloaded test case. Performs the same function as CreateTestCase, but accepts input in a string format, which holds the complete representation of the TestCase content.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_upload_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_upload` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_upload_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaUploadTestCaseRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/testCases:upload",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/testCases:upload
/// Uploads a test case. The content can be a previously downloaded test case. Performs the same function as CreateTestCase, but accepts input in a string format, which holds the complete representation of the TestCase content.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_integrations_versions_test_cases_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaUploadTestCaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaUploadTestCaseResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/testCases:upload
/// Uploads a test case. The content can be a previously downloaded test case. Performs the same function as CreateTestCase, but accepts input in a string format, which holds the complete representation of the TestCase content.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_upload_builder()` + `integrations_projects_locations_integrations_versions_test_cases_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_upload(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaUploadTestCaseRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaUploadTestCaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_upload_builder(
        client, parent, body,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_upload_execute(builder)
}

/// GET v1/{+parent}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_auth_configs_create_execute()` to send, or `integrations_projects_locations_products_auth_configs_create` for simplest API.

pub fn integrations_projects_locations_products_auth_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    clientCertificate_encryptedPrivateKey: Option<&str>,
    clientCertificate_passphrase: Option<&str>,
    clientCertificate_sslCertificate: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaAuthConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/authConfigs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "clientCertificate_encryptedPrivateKey",
            clientCertificate_encryptedPrivateKey
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "clientCertificate_passphrase",
            clientCertificate_passphrase
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "clientCertificate_sslCertificate",
            clientCertificate_sslCertificate
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_auth_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_auth_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaAuthConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_auth_configs_create_builder()` + `integrations_projects_locations_products_auth_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    clientCertificate_encryptedPrivateKey: Option<&str>,
    clientCertificate_passphrase: Option<&str>,
    clientCertificate_sslCertificate: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaAuthConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_auth_configs_create_builder(
        client,
        parent,
        clientCertificate_encryptedPrivateKey,
        clientCertificate_passphrase,
        clientCertificate_sslCertificate,
        body,
    )?;
    integrations_projects_locations_products_auth_configs_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes an auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_auth_configs_delete_execute()` to send, or `integrations_projects_locations_products_auth_configs_delete` for simplest API.

pub fn integrations_projects_locations_products_auth_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_auth_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_auth_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_auth_configs_delete_builder()` + `integrations_projects_locations_products_auth_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_auth_configs_delete_builder(client, name)?;
    integrations_projects_locations_products_auth_configs_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets a complete auth config. If the auth config doesn't exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_auth_configs_get_execute()` to send, or `integrations_projects_locations_products_auth_configs_get` for simplest API.

pub fn integrations_projects_locations_products_auth_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a complete auth config. If the auth config doesn't exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_auth_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_auth_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaAuthConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a complete auth config. If the auth config doesn't exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_auth_configs_get_builder()` + `integrations_projects_locations_products_auth_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_auth_configs_get_builder(client, name)?;
    integrations_projects_locations_products_auth_configs_get_execute(builder)
}

/// GET v1/{+parent}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_auth_configs_list_execute()` to send, or `integrations_projects_locations_products_auth_configs_list` for simplest API.

pub fn integrations_projects_locations_products_auth_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/authConfigs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_auth_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_auth_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListAuthConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListAuthConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_auth_configs_list_builder()` + `integrations_projects_locations_products_auth_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListAuthConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_auth_configs_list_builder(
        client, parent, filter, pageSize, pageToken, readMask,
    )?;
    integrations_projects_locations_products_auth_configs_list_execute(builder)
}

/// GET v1/{+name}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_auth_configs_patch_execute()` to send, or `integrations_projects_locations_products_auth_configs_patch` for simplest API.

pub fn integrations_projects_locations_products_auth_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    clientCertificate_encryptedPrivateKey: Option<&str>,
    clientCertificate_passphrase: Option<&str>,
    clientCertificate_sslCertificate: Option<&str>,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaAuthConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "clientCertificate_encryptedPrivateKey",
            clientCertificate_encryptedPrivateKey
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "clientCertificate_passphrase",
            clientCertificate_passphrase
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "clientCertificate_sslCertificate",
            clientCertificate_sslCertificate
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_auth_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_auth_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaAuthConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_auth_configs_patch_builder()` + `integrations_projects_locations_products_auth_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    clientCertificate_encryptedPrivateKey: Option<&str>,
    clientCertificate_passphrase: Option<&str>,
    clientCertificate_sslCertificate: Option<&str>,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaAuthConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_auth_configs_patch_builder(
        client,
        name,
        clientCertificate_encryptedPrivateKey,
        clientCertificate_passphrase,
        clientCertificate_sslCertificate,
        updateMask,
        body,
    )?;
    integrations_projects_locations_products_auth_configs_patch_execute(builder)
}

/// GET v1/{+parent}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_certificates_create_execute()` to send, or `integrations_projects_locations_products_certificates_create` for simplest API.

pub fn integrations_projects_locations_products_certificates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaCertificate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/certificates", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_certificates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_certificates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_certificates_create_builder()` + `integrations_projects_locations_products_certificates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaCertificate,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_certificates_create_builder(client, parent, body)?;
    integrations_projects_locations_products_certificates_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a certificate
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_certificates_delete_execute()` to send, or `integrations_projects_locations_products_certificates_delete` for simplest API.

pub fn integrations_projects_locations_products_certificates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a certificate
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_certificates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_certificates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a certificate
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_certificates_delete_builder()` + `integrations_projects_locations_products_certificates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_certificates_delete_builder(client, name)?;
    integrations_projects_locations_products_certificates_delete_execute(builder)
}

/// GET v1/{+name}
/// Get a certificates in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_certificates_get_execute()` to send, or `integrations_projects_locations_products_certificates_get` for simplest API.

pub fn integrations_projects_locations_products_certificates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a certificates in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_certificates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_certificates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a certificates in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_certificates_get_builder()` + `integrations_projects_locations_products_certificates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_certificates_get_builder(client, name)?;
    integrations_projects_locations_products_certificates_get_execute(builder)
}

/// GET v1/{+parent}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_certificates_list_execute()` to send, or `integrations_projects_locations_products_certificates_list` for simplest API.

pub fn integrations_projects_locations_products_certificates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/certificates", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_certificates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_certificates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListCertificatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListCertificatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_certificates_list_builder()` + `integrations_projects_locations_products_certificates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListCertificatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_certificates_list_builder(
        client, parent, filter, pageSize, pageToken, readMask,
    )?;
    integrations_projects_locations_products_certificates_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_certificates_patch_execute()` to send, or `integrations_projects_locations_products_certificates_patch` for simplest API.

pub fn integrations_projects_locations_products_certificates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaCertificate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_certificates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_certificates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_certificates_patch_builder()` + `integrations_projects_locations_products_certificates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaCertificate,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_certificates_patch_builder(
        client, name, updateMask, body,
    )?;
    integrations_projects_locations_products_certificates_patch_execute(builder)
}

/// GET v1/{+parent}/cloudFunctions
/// Creates a cloud function project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_cloud_functions_create_execute()` to send, or `integrations_projects_locations_products_cloud_functions_create` for simplest API.

pub fn integrations_projects_locations_products_cloud_functions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaCreateCloudFunctionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/cloudFunctions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/cloudFunctions
/// Creates a cloud function project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_cloud_functions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_cloud_functions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_cloud_functions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/cloudFunctions
/// Creates a cloud function project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_cloud_functions_create_builder()` + `integrations_projects_locations_products_cloud_functions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_cloud_functions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaCreateCloudFunctionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_cloud_functions_create_builder(
        client, parent, body,
    )?;
    integrations_projects_locations_products_cloud_functions_create_execute(builder)
}

/// GET v1/{+name}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_execute_execute()` to send, or `integrations_projects_locations_products_integrations_execute` for simplest API.

pub fn integrations_projects_locations_products_integrations_execute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaExecuteIntegrationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:execute", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_execute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_execute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_execute_builder()` + `integrations_projects_locations_products_integrations_execute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_execute(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaExecuteIntegrationsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_execute_builder(client, name, body)?;
    integrations_projects_locations_products_integrations_execute_execute(builder)
}

/// GET v1/{+parent}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_list_execute()` to send, or `integrations_projects_locations_products_integrations_list` for simplest API.

pub fn integrations_projects_locations_products_integrations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/integrations", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_list_builder()` + `integrations_projects_locations_products_integrations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    integrations_projects_locations_products_integrations_list_execute(builder)
}

/// GET v1/{+name}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_schedule_execute()` to send, or `integrations_projects_locations_products_integrations_schedule` for simplest API.

pub fn integrations_projects_locations_products_integrations_schedule_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaScheduleIntegrationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:schedule", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_schedule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_schedule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_schedule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_schedule_builder()` + `integrations_projects_locations_products_integrations_schedule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_schedule(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaScheduleIntegrationsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_schedule_builder(client, name, body)?;
    integrations_projects_locations_products_integrations_schedule_execute(builder)
}

/// GET v1/{+name}:test
/// Execute the integration in draft state
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_test_execute()` to send, or `integrations_projects_locations_products_integrations_test` for simplest API.

pub fn integrations_projects_locations_products_integrations_test_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaTestIntegrationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:test", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:test
/// Execute the integration in draft state
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_test_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_test_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:test
/// Execute the integration in draft state
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_test_builder()` + `integrations_projects_locations_products_integrations_test_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_test(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaTestIntegrationsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_test_builder(client, name, body)?;
    integrations_projects_locations_products_integrations_test_execute(builder)
}

/// GET v1/{+name}:download
/// Download the execution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_executions_download_execute()` to send, or `integrations_projects_locations_products_integrations_executions_download` for simplest API.

pub fn integrations_projects_locations_products_integrations_executions_download_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:download", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:download
/// Download the execution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_executions_download_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_executions_download_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadExecutionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:download
/// Download the execution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_executions_download_builder()` + `integrations_projects_locations_products_integrations_executions_download_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_download(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_executions_download_builder(
            client, name,
        )?;
    integrations_projects_locations_products_integrations_executions_download_execute(builder)
}

/// GET v1/{+name}
/// Get an execution in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_executions_get_execute()` to send, or `integrations_projects_locations_products_integrations_executions_get` for simplest API.

pub fn integrations_projects_locations_products_integrations_executions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get an execution in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_executions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_executions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecution =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get an execution in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_executions_get_builder()` + `integrations_projects_locations_products_integrations_executions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_executions_get_builder(client, name)?;
    integrations_projects_locations_products_integrations_executions_get_execute(builder)
}

/// GET v1/{+parent}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_executions_list_execute()` to send, or `integrations_projects_locations_products_integrations_executions_list` for simplest API.

pub fn integrations_projects_locations_products_integrations_executions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    filterParams_customFilter: Option<&str>,
    filterParams_endTime: Option<&str>,
    filterParams_eventStatuses: Option<&str>,
    filterParams_executionId: Option<&str>,
    filterParams_parameterKey: Option<&str>,
    filterParams_parameterPairKey: Option<&str>,
    filterParams_parameterPairValue: Option<&str>,
    filterParams_parameterType: Option<&str>,
    filterParams_parameterValue: Option<&str>,
    filterParams_startTime: Option<&str>,
    filterParams_taskStatuses: Option<&str>,
    filterParams_workflowName: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
    refreshAcl: Option<bool>,
    snapshotMetadataWithoutParams: Option<bool>,
    truncateParams: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/executions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_customFilter",
            filterParams_customFilter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_endTime",
            filterParams_endTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_eventStatuses",
            filterParams_eventStatuses
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_executionId",
            filterParams_executionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_parameterKey",
            filterParams_parameterKey
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_parameterPairKey",
            filterParams_parameterPairKey
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_parameterPairValue",
            filterParams_parameterPairValue
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_parameterType",
            filterParams_parameterType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_parameterValue",
            filterParams_parameterValue
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_startTime",
            filterParams_startTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_taskStatuses",
            filterParams_taskStatuses
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filterParams_workflowName",
            filterParams_workflowName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "refreshAcl",
            refreshAcl
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "snapshotMetadataWithoutParams",
            snapshotMetadataWithoutParams
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "truncateParams",
            truncateParams
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_executions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_executions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListExecutionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_executions_list_builder()` + `integrations_projects_locations_products_integrations_executions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    filterParams_customFilter: Option<&str>,
    filterParams_endTime: Option<&str>,
    filterParams_eventStatuses: Option<&str>,
    filterParams_executionId: Option<&str>,
    filterParams_parameterKey: Option<&str>,
    filterParams_parameterPairKey: Option<&str>,
    filterParams_parameterPairValue: Option<&str>,
    filterParams_parameterType: Option<&str>,
    filterParams_parameterValue: Option<&str>,
    filterParams_startTime: Option<&str>,
    filterParams_taskStatuses: Option<&str>,
    filterParams_workflowName: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
    refreshAcl: Option<bool>,
    snapshotMetadataWithoutParams: Option<bool>,
    truncateParams: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_executions_list_builder(
        client,
        parent,
        filter,
        filterParams_customFilter,
        filterParams_endTime,
        filterParams_eventStatuses,
        filterParams_executionId,
        filterParams_parameterKey,
        filterParams_parameterPairKey,
        filterParams_parameterPairValue,
        filterParams_parameterType,
        filterParams_parameterValue,
        filterParams_startTime,
        filterParams_taskStatuses,
        filterParams_workflowName,
        orderBy,
        pageSize,
        pageToken,
        readMask,
        refreshAcl,
        snapshotMetadataWithoutParams,
        truncateParams,
    )?;
    integrations_projects_locations_products_integrations_executions_list_execute(builder)
}

/// GET v1/{+name}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_executions_suspensions_lift_execute()` to send, or `integrations_projects_locations_products_integrations_executions_suspensions_lift` for simplest API.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_lift_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaLiftSuspensionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:lift", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_executions_suspensions_lift_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_executions_suspensions_lift_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_lift_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaLiftSuspensionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaLiftSuspensionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_executions_suspensions_lift_builder()` + `integrations_projects_locations_products_integrations_executions_suspensions_lift_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_lift(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaLiftSuspensionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaLiftSuspensionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_executions_suspensions_lift_builder(
            client, name, body,
        )?;
    integrations_projects_locations_products_integrations_executions_suspensions_lift_execute(
        builder,
    )
}

/// GET v1/{+parent}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_executions_suspensions_list_execute()` to send, or `integrations_projects_locations_products_integrations_executions_suspensions_list` for simplest API.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/suspensions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_executions_suspensions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_executions_suspensions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSuspensionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListSuspensionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_executions_suspensions_list_builder()` + `integrations_projects_locations_products_integrations_executions_suspensions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSuspensionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_executions_suspensions_list_builder(
            client, parent, filter, orderBy, pageSize, pageToken,
        )?;
    integrations_projects_locations_products_integrations_executions_suspensions_list_execute(
        builder,
    )
}

/// GET v1/{+name}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_executions_suspensions_resolve_execute()` to send, or `integrations_projects_locations_products_integrations_executions_suspensions_resolve` for simplest API.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_resolve_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaResolveSuspensionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:resolve", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_executions_suspensions_resolve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_executions_suspensions_resolve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_resolve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaResolveSuspensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaResolveSuspensionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_executions_suspensions_resolve_builder()` + `integrations_projects_locations_products_integrations_executions_suspensions_resolve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_resolve(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaResolveSuspensionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaResolveSuspensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_executions_suspensions_resolve_builder(client, name, body)?;
    integrations_projects_locations_products_integrations_executions_suspensions_resolve_execute(
        builder,
    )
}

/// GET v1/{+parent}/versions
/// Create a integration with a draft version in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_create_execute()` to send, or `integrations_projects_locations_products_integrations_versions_create` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    createSampleIntegrations: Option<bool>,
    newIntegration: Option<bool>,
    body: &GoogleCloudIntegrationsV1alphaIntegrationVersion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/versions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "createSampleIntegrations",
            createSampleIntegrations
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "newIntegration",
            newIntegration
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/versions
/// Create a integration with a draft version in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_versions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_versions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaIntegrationVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/versions
/// Create a integration with a draft version in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_create_builder()` + `integrations_projects_locations_products_integrations_versions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_create(
    client: &SimpleHttpClient,
    parent: &str,
    createSampleIntegrations: Option<bool>,
    newIntegration: Option<bool>,
    body: &GoogleCloudIntegrationsV1alphaIntegrationVersion,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_create_builder(
        client,
        parent,
        createSampleIntegrations,
        newIntegration,
        body,
    )?;
    integrations_projects_locations_products_integrations_versions_create_execute(builder)
}

/// GET v1/{+name}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_delete_execute()` to send, or `integrations_projects_locations_products_integrations_versions_delete` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_versions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_versions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_delete_builder()` + `integrations_projects_locations_products_integrations_versions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_delete_builder(
        client, name,
    )?;
    integrations_projects_locations_products_integrations_versions_delete_execute(builder)
}

/// GET v1/{+name}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_download_execute()` to send, or `integrations_projects_locations_products_integrations_versions_download` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_download_builder(
    client: &SimpleHttpClient,
    name: &str,
    fileFormat: Option<&str>,
    files: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:download", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fileFormat",
            fileFormat
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "files",
            files
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_versions_download_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_versions_download_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_download_builder()` + `integrations_projects_locations_products_integrations_versions_download_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_download(
    client: &SimpleHttpClient,
    name: &str,
    fileFormat: Option<&str>,
    files: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_download_builder(
        client, name, fileFormat, files,
    )?;
    integrations_projects_locations_products_integrations_versions_download_execute(builder)
}

/// GET v1/{+name}
/// Get a integration in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_get_execute()` to send, or `integrations_projects_locations_products_integrations_versions_get` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a integration in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_versions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_versions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaIntegrationVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a integration in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_get_builder()` + `integrations_projects_locations_products_integrations_versions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_versions_get_builder(client, name)?;
    integrations_projects_locations_products_integrations_versions_get_execute(builder)
}

/// GET v1/{+parent}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_list_execute()` to send, or `integrations_projects_locations_products_integrations_versions_list` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/versions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fieldMask",
            fieldMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_versions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_versions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_list_builder()` + `integrations_projects_locations_products_integrations_versions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_list(
    client: &SimpleHttpClient,
    parent: &str,
    fieldMask: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_list_builder(
        client, parent, fieldMask, filter, orderBy, pageSize, pageToken,
    )?;
    integrations_projects_locations_products_integrations_versions_list_execute(builder)
}

/// GET v1/{+name}
/// Update a integration with a draft version in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_patch_execute()` to send, or `integrations_projects_locations_products_integrations_versions_patch` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaIntegrationVersion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update a integration with a draft version in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_versions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_versions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaIntegrationVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update a integration with a draft version in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_patch_builder()` + `integrations_projects_locations_products_integrations_versions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaIntegrationVersion,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_patch_builder(
        client, name, updateMask, body,
    )?;
    integrations_projects_locations_products_integrations_versions_patch_execute(builder)
}

/// GET v1/{+name}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_publish_execute()` to send, or `integrations_projects_locations_products_integrations_versions_publish` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_publish_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaPublishIntegrationVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:publish", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_versions_publish_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_versions_publish_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_publish_builder()` + `integrations_projects_locations_products_integrations_versions_publish_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_publish(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaPublishIntegrationVersionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_publish_builder(
        client, name, body,
    )?;
    integrations_projects_locations_products_integrations_versions_publish_execute(builder)
}

/// GET v1/{+integrationVersion}:takeoverEditLock
/// Clears the locked_by and locked_at_timestampin the DRAFT version of this integration. It then performs the same action as the CreateDraftIntegrationVersion (i.e., copies the DRAFT version of the integration as a SNAPSHOT and then creates a new DRAFT version with the locked_by set to the user_taking_over and the locked_at_timestamp set to the current timestamp). Both the locked_by and user_taking_over are notified via email about the takeover. This RPC throws an exception if the integration is not in DRAFT status or if the locked_by and locked_at_timestamp fields are not set.The TakeoverEdit lock is treated the same as an edit of the integration, and hence shares ACLs with edit. Audit fields updated include last_modified_timestamp, last_modified_by.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_execute()` to send, or `integrations_projects_locations_products_integrations_versions_takeover_edit_lock` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_takeover_edit_lock_builder(
    client: &SimpleHttpClient,
    integrationVersion: &str,
    body: &GoogleCloudIntegrationsV1alphaTakeoverEditLockRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+integrationVersion}:takeoverEditLock",
        integrationVersion,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+integrationVersion}:takeoverEditLock
/// Clears the locked_by and locked_at_timestampin the DRAFT version of this integration. It then performs the same action as the CreateDraftIntegrationVersion (i.e., copies the DRAFT version of the integration as a SNAPSHOT and then creates a new DRAFT version with the locked_by set to the user_taking_over and the locked_at_timestamp set to the current timestamp). Both the locked_by and user_taking_over are notified via email about the takeover. This RPC throws an exception if the integration is not in DRAFT status or if the locked_by and locked_at_timestamp fields are not set.The TakeoverEdit lock is treated the same as an edit of the integration, and hence shares ACLs with edit. Audit fields updated include last_modified_timestamp, last_modified_by.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_versions_takeover_edit_lock_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_takeover_edit_lock_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTakeoverEditLockResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTakeoverEditLockResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+integrationVersion}:takeoverEditLock
/// Clears the locked_by and locked_at_timestampin the DRAFT version of this integration. It then performs the same action as the CreateDraftIntegrationVersion (i.e., copies the DRAFT version of the integration as a SNAPSHOT and then creates a new DRAFT version with the locked_by set to the user_taking_over and the locked_at_timestamp set to the current timestamp). Both the locked_by and user_taking_over are notified via email about the takeover. This RPC throws an exception if the integration is not in DRAFT status or if the locked_by and locked_at_timestamp fields are not set.The TakeoverEdit lock is treated the same as an edit of the integration, and hence shares ACLs with edit. Audit fields updated include last_modified_timestamp, last_modified_by.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_builder()` + `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_takeover_edit_lock(
    client: &SimpleHttpClient,
    integrationVersion: &str,
    body: &GoogleCloudIntegrationsV1alphaTakeoverEditLockRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTakeoverEditLockResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_versions_takeover_edit_lock_builder(
            client,
            integrationVersion,
            body,
        )?;
    integrations_projects_locations_products_integrations_versions_takeover_edit_lock_execute(
        builder,
    )
}

/// GET v1/{+name}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_unpublish_execute()` to send, or `integrations_projects_locations_products_integrations_versions_unpublish` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_unpublish_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaUnpublishIntegrationVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:unpublish", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_versions_unpublish_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_versions_unpublish_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_unpublish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_unpublish_builder()` + `integrations_projects_locations_products_integrations_versions_unpublish_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_unpublish(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaUnpublishIntegrationVersionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_unpublish_builder(
        client, name, body,
    )?;
    integrations_projects_locations_products_integrations_versions_unpublish_execute(builder)
}

/// GET v1/{+parent}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_upload_execute()` to send, or `integrations_projects_locations_products_integrations_versions_upload` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_upload_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaUploadIntegrationVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/versions:upload",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_integrations_versions_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_integrations_versions_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_upload_builder()` + `integrations_projects_locations_products_integrations_versions_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_upload(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaUploadIntegrationVersionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_upload_builder(
        client, parent, body,
    )?;
    integrations_projects_locations_products_integrations_versions_upload_execute(builder)
}

/// GET v1/{+parent}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_create_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_create` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaSfdcInstance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/sfdcInstances", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_sfdc_instances_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcInstance =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_create_builder()` + `integrations_projects_locations_products_sfdc_instances_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaSfdcInstance,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_sfdc_instances_create_builder(
        client, parent, body,
    )?;
    integrations_projects_locations_products_sfdc_instances_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes an sfdc instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_delete_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_delete` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_sfdc_instances_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an sfdc instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_delete_builder()` + `integrations_projects_locations_products_sfdc_instances_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_delete_builder(client, name)?;
    integrations_projects_locations_products_sfdc_instances_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets an sfdc instance. If the instance doesn't exist, Code.NOT_FOUND exception will be thrown.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_get_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_get` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets an sfdc instance. If the instance doesn't exist, Code.NOT_FOUND exception will be thrown.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_sfdc_instances_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcInstance =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets an sfdc instance. If the instance doesn't exist, Code.NOT_FOUND exception will be thrown.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_get_builder()` + `integrations_projects_locations_products_sfdc_instances_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_get_builder(client, name)?;
    integrations_projects_locations_products_sfdc_instances_get_execute(builder)
}

/// GET v1/{+parent}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_list_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_list` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/sfdcInstances", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_sfdc_instances_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_list_builder()` + `integrations_projects_locations_products_sfdc_instances_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_sfdc_instances_list_builder(
        client, parent, filter, pageSize, pageToken, readMask,
    )?;
    integrations_projects_locations_products_sfdc_instances_list_execute(builder)
}

/// GET v1/{+name}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_patch_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_patch` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaSfdcInstance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_sfdc_instances_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcInstance =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_patch_builder()` + `integrations_projects_locations_products_sfdc_instances_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaSfdcInstance,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_sfdc_instances_patch_builder(
        client, name, updateMask, body,
    )?;
    integrations_projects_locations_products_sfdc_instances_patch_execute(builder)
}

/// GET v1/{+parent}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaSfdcChannel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/sfdcChannels", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_builder()` + `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaSfdcChannel,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_builder(
            client, parent, body,
        )?;
    integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes an sfdc channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an sfdc channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_builder()` + `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_builder(
            client, name,
        )?;
    integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets an sfdc channel. If the channel doesn't exist, Code.NOT_FOUND exception will be thrown.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets an sfdc channel. If the channel doesn't exist, Code.NOT_FOUND exception will be thrown.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets an sfdc channel. If the channel doesn't exist, Code.NOT_FOUND exception will be thrown.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_builder()` + `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_builder(
            client, name,
        )?;
    integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_execute(builder)
}

/// GET v1/{+parent}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/sfdcChannels", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_builder()` + `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_builder(
            client, parent, filter, pageSize, pageToken, readMask,
        )?;
    integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_execute(builder)
}

/// GET v1/{+name}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaSfdcChannel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_builder()` + `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaSfdcChannel,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_builder(
            client, name, updateMask, body,
        )?;
    integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_execute(builder)
}

/// GET v1/{+parent}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_create_execute()` to send, or `integrations_projects_locations_sfdc_instances_create` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaSfdcInstance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/sfdcInstances", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_sfdc_instances_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_sfdc_instances_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcInstance =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_create_builder()` + `integrations_projects_locations_sfdc_instances_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaSfdcInstance,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_sfdc_instances_create_builder(client, parent, body)?;
    integrations_projects_locations_sfdc_instances_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes an sfdc instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_delete_execute()` to send, or `integrations_projects_locations_sfdc_instances_delete` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_sfdc_instances_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_sfdc_instances_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an sfdc instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_delete_builder()` + `integrations_projects_locations_sfdc_instances_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_delete_builder(client, name)?;
    integrations_projects_locations_sfdc_instances_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets an sfdc instance. If the instance doesn't exist, Code.NOT_FOUND exception will be thrown.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_get_execute()` to send, or `integrations_projects_locations_sfdc_instances_get` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets an sfdc instance. If the instance doesn't exist, Code.NOT_FOUND exception will be thrown.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_sfdc_instances_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_sfdc_instances_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcInstance =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets an sfdc instance. If the instance doesn't exist, Code.NOT_FOUND exception will be thrown.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_get_builder()` + `integrations_projects_locations_sfdc_instances_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_get_builder(client, name)?;
    integrations_projects_locations_sfdc_instances_get_execute(builder)
}

/// GET v1/{+parent}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_list_execute()` to send, or `integrations_projects_locations_sfdc_instances_list` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/sfdcInstances", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_sfdc_instances_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_sfdc_instances_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_list_builder()` + `integrations_projects_locations_sfdc_instances_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_list_builder(
        client, parent, filter, pageSize, pageToken, readMask,
    )?;
    integrations_projects_locations_sfdc_instances_list_execute(builder)
}

/// GET v1/{+name}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_patch_execute()` to send, or `integrations_projects_locations_sfdc_instances_patch` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaSfdcInstance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_sfdc_instances_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_sfdc_instances_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcInstance =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_patch_builder()` + `integrations_projects_locations_sfdc_instances_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaSfdcInstance,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_patch_builder(
        client, name, updateMask, body,
    )?;
    integrations_projects_locations_sfdc_instances_patch_execute(builder)
}

/// GET v1/{+parent}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_sfdc_channels_create_execute()` to send, or `integrations_projects_locations_sfdc_instances_sfdc_channels_create` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaSfdcChannel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/sfdcChannels", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_sfdc_instances_sfdc_channels_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_create_builder()` + `integrations_projects_locations_sfdc_instances_sfdc_channels_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaSfdcChannel,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_sfdc_channels_create_builder(
        client, parent, body,
    )?;
    integrations_projects_locations_sfdc_instances_sfdc_channels_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes an sfdc channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_execute()` to send, or `integrations_projects_locations_sfdc_instances_sfdc_channels_delete` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_sfdc_instances_sfdc_channels_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an sfdc channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_builder()` + `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_sfdc_instances_sfdc_channels_delete_builder(client, name)?;
    integrations_projects_locations_sfdc_instances_sfdc_channels_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets an sfdc channel. If the channel doesn't exist, Code.NOT_FOUND exception will be thrown.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_sfdc_channels_get_execute()` to send, or `integrations_projects_locations_sfdc_instances_sfdc_channels_get` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets an sfdc channel. If the channel doesn't exist, Code.NOT_FOUND exception will be thrown.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_sfdc_instances_sfdc_channels_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets an sfdc channel. If the channel doesn't exist, Code.NOT_FOUND exception will be thrown.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_get_builder()` + `integrations_projects_locations_sfdc_instances_sfdc_channels_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_sfdc_instances_sfdc_channels_get_builder(client, name)?;
    integrations_projects_locations_sfdc_instances_sfdc_channels_get_execute(builder)
}

/// GET v1/{+parent}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_sfdc_channels_list_execute()` to send, or `integrations_projects_locations_sfdc_instances_sfdc_channels_list` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/sfdcChannels", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_sfdc_instances_sfdc_channels_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_list_builder()` + `integrations_projects_locations_sfdc_instances_sfdc_channels_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_sfdc_channels_list_builder(
        client, parent, filter, pageSize, pageToken, readMask,
    )?;
    integrations_projects_locations_sfdc_instances_sfdc_channels_list_execute(builder)
}

/// GET v1/{+name}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_execute()` to send, or `integrations_projects_locations_sfdc_instances_sfdc_channels_patch` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaSfdcChannel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_sfdc_instances_sfdc_channels_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_builder()` + `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaSfdcChannel,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_sfdc_channels_patch_builder(
        client, name, updateMask, body,
    )?;
    integrations_projects_locations_sfdc_instances_sfdc_channels_patch_execute(builder)
}

/// GET v1/{+parent}/templates
/// Creates a new template
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_create_execute()` to send, or `integrations_projects_locations_templates_create` for simplest API.

pub fn integrations_projects_locations_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/templates", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/templates
/// Creates a new template
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/templates
/// Creates a new template
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_create_builder()` + `integrations_projects_locations_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaTemplate,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_create_builder(client, parent, body)?;
    integrations_projects_locations_templates_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a template
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_delete_execute()` to send, or `integrations_projects_locations_templates_delete` for simplest API.

pub fn integrations_projects_locations_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a template
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a template
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_delete_builder()` + `integrations_projects_locations_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_delete_builder(client, name)?;
    integrations_projects_locations_templates_delete_execute(builder)
}

/// GET v1/{+name}:download
/// Downloads a template. Retrieves the Template and returns the response as a string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_download_execute()` to send, or `integrations_projects_locations_templates_download` for simplest API.

pub fn integrations_projects_locations_templates_download_builder(
    client: &SimpleHttpClient,
    name: &str,
    fileFormat: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:download", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fileFormat",
            fileFormat
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:download
/// Downloads a template. Retrieves the Template and returns the response as a string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_templates_download_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_templates_download_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadTemplateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadTemplateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:download
/// Downloads a template. Retrieves the Template and returns the response as a string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_download_builder()` + `integrations_projects_locations_templates_download_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_download(
    client: &SimpleHttpClient,
    name: &str,
    fileFormat: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadTemplateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_templates_download_builder(client, name, fileFormat)?;
    integrations_projects_locations_templates_download_execute(builder)
}

/// GET v1/{+name}
/// Get a template in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_get_execute()` to send, or `integrations_projects_locations_templates_get` for simplest API.

pub fn integrations_projects_locations_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a template in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a template in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_get_builder()` + `integrations_projects_locations_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_get_builder(client, name)?;
    integrations_projects_locations_templates_get_execute(builder)
}

/// GET v1/{+name}:import
/// Import the template to an existing integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_import_execute()` to send, or `integrations_projects_locations_templates_import` for simplest API.

pub fn integrations_projects_locations_templates_import_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaImportTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:import", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:import
/// Import the template to an existing integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_templates_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_templates_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaImportTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaImportTemplateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:import
/// Import the template to an existing integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_import_builder()` + `integrations_projects_locations_templates_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_import(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaImportTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaImportTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_import_builder(client, name, body)?;
    integrations_projects_locations_templates_import_execute(builder)
}

/// GET v1/{+parent}/templates
/// Lists all templates matching the filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_list_execute()` to send, or `integrations_projects_locations_templates_list` for simplest API.

pub fn integrations_projects_locations_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/templates", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/templates
/// Lists all templates matching the filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/templates
/// Lists all templates matching the filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_list_builder()` + `integrations_projects_locations_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken, readMask,
    )?;
    integrations_projects_locations_templates_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the template by given id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_patch_execute()` to send, or `integrations_projects_locations_templates_patch` for simplest API.

pub fn integrations_projects_locations_templates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the template by given id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_templates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_templates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the template by given id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_patch_builder()` + `integrations_projects_locations_templates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudIntegrationsV1alphaTemplate,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_templates_patch_builder(client, name, updateMask, body)?;
    integrations_projects_locations_templates_patch_execute(builder)
}

/// GET v1/{+parent}/templates:search
/// Search templates based on user query and filters. This api would query the templates and return a list of templates based on the user filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_search_execute()` to send, or `integrations_projects_locations_templates_search` for simplest API.

pub fn integrations_projects_locations_templates_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    enableNaturalLanguageQueryUnderstanding: Option<bool>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/templates:search",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "enableNaturalLanguageQueryUnderstanding",
            enableNaturalLanguageQueryUnderstanding
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "query",
            query
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/templates:search
/// Search templates based on user query and filters. This api would query the templates and return a list of templates based on the user filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_templates_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_templates_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaSearchTemplatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSearchTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/templates:search
/// Search templates based on user query and filters. This api would query the templates and return a list of templates based on the user filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_search_builder()` + `integrations_projects_locations_templates_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_search(
    client: &SimpleHttpClient,
    parent: &str,
    enableNaturalLanguageQueryUnderstanding: Option<bool>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaSearchTemplatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_search_builder(
        client,
        parent,
        enableNaturalLanguageQueryUnderstanding,
        filter,
        orderBy,
        pageSize,
        pageToken,
        query,
        readMask,
    )?;
    integrations_projects_locations_templates_search_execute(builder)
}

/// GET v1/{+name}:share
/// Share a template with other clients. Only the template owner can share the templates with other projects. PERMISSION_DENIED would be thrown if the request is not from the owner.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_share_execute()` to send, or `integrations_projects_locations_templates_share` for simplest API.

pub fn integrations_projects_locations_templates_share_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaShareTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:share", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:share
/// Share a template with other clients. Only the template owner can share the templates with other projects. PERMISSION_DENIED would be thrown if the request is not from the owner.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_templates_share_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_templates_share_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_share_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:share
/// Share a template with other clients. Only the template owner can share the templates with other projects. PERMISSION_DENIED would be thrown if the request is not from the owner.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_share_builder()` + `integrations_projects_locations_templates_share_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_share(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaShareTemplateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_share_builder(client, name, body)?;
    integrations_projects_locations_templates_share_execute(builder)
}

/// GET v1/{+name}:unshare
/// Unshare a template from given clients. Owner of the template can unshare template with clients. Shared client can only unshare the template from itself. PERMISSION_DENIED would be thrown if request is not from owner or for unsharing itself.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_unshare_execute()` to send, or `integrations_projects_locations_templates_unshare` for simplest API.

pub fn integrations_projects_locations_templates_unshare_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaUnshareTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:unshare", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:unshare
/// Unshare a template from given clients. Owner of the template can unshare template with clients. Shared client can only unshare the template from itself. PERMISSION_DENIED would be thrown if request is not from owner or for unsharing itself.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_templates_unshare_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_templates_unshare_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_unshare_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:unshare
/// Unshare a template from given clients. Owner of the template can unshare template with clients. Shared client can only unshare the template from itself. PERMISSION_DENIED would be thrown if request is not from owner or for unsharing itself.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_unshare_builder()` + `integrations_projects_locations_templates_unshare_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_unshare(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaUnshareTemplateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_unshare_builder(client, name, body)?;
    integrations_projects_locations_templates_unshare_execute(builder)
}

/// GET v1/{+parent}/templates:upload
/// Uploads a template. The content can be a previously downloaded template. Performs the same function as CreateTemplate, but accepts input in a string format, which holds the complete representation of the Template content.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_upload_execute()` to send, or `integrations_projects_locations_templates_upload` for simplest API.

pub fn integrations_projects_locations_templates_upload_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaUploadTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/templates:upload",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/templates:upload
/// Uploads a template. The content can be a previously downloaded template. Performs the same function as CreateTemplate, but accepts input in a string format, which holds the complete representation of the Template content.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_templates_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_templates_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaUploadTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaUploadTemplateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/templates:upload
/// Uploads a template. The content can be a previously downloaded template. Performs the same function as CreateTemplate, but accepts input in a string format, which holds the complete representation of the Template content.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_upload_builder()` + `integrations_projects_locations_templates_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_upload(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudIntegrationsV1alphaUploadTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaUploadTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_upload_builder(client, parent, body)?;
    integrations_projects_locations_templates_upload_execute(builder)
}

/// GET v1/{+name}:use
/// Use the template to create integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_use_execute()` to send, or `integrations_projects_locations_templates_use` for simplest API.

pub fn integrations_projects_locations_templates_use_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaUseTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:use", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:use
/// Use the template to create integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `integrations_projects_locations_templates_use_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from integrations_projects_locations_templates_use_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_use_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaUseTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaUseTemplateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:use
/// Use the template to create integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_use_builder()` + `integrations_projects_locations_templates_use_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_use(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudIntegrationsV1alphaUseTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaUseTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_use_builder(client, name, body)?;
    integrations_projects_locations_templates_use_execute(builder)
}
