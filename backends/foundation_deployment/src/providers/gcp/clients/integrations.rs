//! Auto-generated API clients for gcp/integrations.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/callback:generateToken
/// Receives the auth code and auth config id to combine that with the client id and secret to retrieve access tokens from the token endpoint. Returns either a success or error message when it''s done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_callback_generate_token_execute()` to send, or `integrations_callback_generate_token` for simplest API.

pub fn integrations_callback_generate_token_builder<R>(
    client: &SimpleHttpClient<R>,
    code: &Option<Option<String>>,
    gcpProjectId: &Option<Option<String>>,
    product: &Option<Option<String>>,
    redirectUri: &Option<Option<String>>,
    state: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://integrations.googleapis.com/v1/callback:generateToken",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = code.as_ref() {
        query_parts.push(format!("code={}", val));
    }
    if let Some(val) = gcpProjectId.as_ref() {
        query_parts.push(format!("gcpProjectId={}", val));
    }
    if let Some(val) = product.as_ref() {
        query_parts.push(format!("product={}", val));
    }
    if let Some(val) = redirectUri.as_ref() {
        query_parts.push(format!("redirectUri={}", val));
    }
    if let Some(val) = state.as_ref() {
        query_parts.push(format!("state={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/callback:generateToken
/// Receives the auth code and auth config id to combine that with the client id and secret to retrieve access tokens from the token endpoint. Returns either a success or error message when it''s done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_callback_generate_token_execute()` or `integrations_callback_generate_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_callback_generate_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_callback_generate_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaGenerateTokenResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaGenerateTokenResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/callback:generateToken
/// Receives the auth code and auth config id to combine that with the client id and secret to retrieve access tokens from the token endpoint. Returns either a success or error message when it''s done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_callback_generate_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_callback_generate_token_task()`.
/// For the simplest API, use `integrations_callback_generate_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_callback_generate_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_callback_generate_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaGenerateTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_callback_generate_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_callback_generate_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsCallbackGenerateTokenArgs {
    /// Query parameter: code
    pub code: Option<Option<String>>,
    /// Query parameter: gcpProjectId
    pub gcpProjectId: Option<Option<String>>,
    /// Query parameter: product
    pub product: Option<Option<String>>,
    /// Query parameter: redirectUri
    pub redirectUri: Option<Option<String>>,
    /// Query parameter: state
    pub state: Option<Option<String>>,
}

/// GET v1/callback:generateToken
/// Receives the auth code and auth config id to combine that with the client id and secret to retrieve access tokens from the token endpoint. Returns either a success or error message when it''s done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_callback_generate_token_builder()` + `integrations_callback_generate_token_execute()`.
/// For task-level control, use `integrations_callback_generate_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_callback_generate_token(
    client: &SimpleHttpClient,
    args: &IntegrationsCallbackGenerateTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaGenerateTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_callback_generate_token_builder(
        client,
        &args.code,
        &args.gcpProjectId,
        &args.product,
        &args.redirectUri,
        &args.state,
    )?;
    integrations_callback_generate_token_execute(builder)
}

/// GET v1/connectorPlatformRegions:enumerate
/// Enumerates the regions for which Connector Platform is provisioned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_connector_platform_regions_enumerate_execute()` to send, or `integrations_connector_platform_regions_enumerate` for simplest API.

pub fn integrations_connector_platform_regions_enumerate_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://integrations.googleapis.com/v1/connectorPlatformRegions:enumerate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/connectorPlatformRegions:enumerate
/// Enumerates the regions for which Connector Platform is provisioned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_connector_platform_regions_enumerate_execute()` or `integrations_connector_platform_regions_enumerate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_connector_platform_regions_enumerate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_connector_platform_regions_enumerate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleCloudIntegrationsV1alphaEnumerateConnectorPlatformRegionsResponse,
                >,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaEnumerateConnectorPlatformRegionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/connectorPlatformRegions:enumerate
/// Enumerates the regions for which Connector Platform is provisioned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_connector_platform_regions_enumerate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_connector_platform_regions_enumerate_task()`.
/// For the simplest API, use `integrations_connector_platform_regions_enumerate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_connector_platform_regions_enumerate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_connector_platform_regions_enumerate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudIntegrationsV1alphaEnumerateConnectorPlatformRegionsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_connector_platform_regions_enumerate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/connectorPlatformRegions:enumerate
/// Enumerates the regions for which Connector Platform is provisioned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_connector_platform_regions_enumerate_builder()` + `integrations_connector_platform_regions_enumerate_execute()`.
/// For task-level control, use `integrations_connector_platform_regions_enumerate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_connector_platform_regions_enumerate(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudIntegrationsV1alphaEnumerateConnectorPlatformRegionsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_connector_platform_regions_enumerate_builder(client)?;
    integrations_connector_platform_regions_enumerate_execute(builder)
}

/// GET v1/projects/{projectsId}/clientmetadata
/// Gets the metadata info for the requested client
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_get_clientmetadata_execute()` to send, or `integrations_projects_get_clientmetadata` for simplest API.

pub fn integrations_projects_get_clientmetadata_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/clientmetadata",
        parent,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/clientmetadata
/// Gets the metadata info for the requested client
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_get_clientmetadata_execute()` or `integrations_projects_get_clientmetadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_get_clientmetadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_get_clientmetadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaGetClientMetadataResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaGetClientMetadataResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/clientmetadata
/// Gets the metadata info for the requested client
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_get_clientmetadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_get_clientmetadata_task()`.
/// For the simplest API, use `integrations_projects_get_clientmetadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_get_clientmetadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_get_clientmetadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaGetClientMetadataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_get_clientmetadata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_get_clientmetadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsGetClientmetadataArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/projects/{projectsId}/clientmetadata
/// Gets the metadata info for the requested client
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_get_clientmetadata_builder()` + `integrations_projects_get_clientmetadata_execute()`.
/// For task-level control, use `integrations_projects_get_clientmetadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_get_clientmetadata(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsGetClientmetadataArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaGetClientMetadataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_get_clientmetadata_builder(client, &args.parent)?;
    integrations_projects_get_clientmetadata_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:generateOpenApiSpec
/// Generate OpenAPI spec for the requested integrations and api triggers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_generate_open_api_spec_execute()` to send, or `integrations_projects_locations_generate_open_api_spec` for simplest API.

pub fn integrations_projects_locations_generate_open_api_spec_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}:generateOpenApiSpec",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:generateOpenApiSpec
/// Generate OpenAPI spec for the requested integrations and api triggers
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_generate_open_api_spec_execute()` or `integrations_projects_locations_generate_open_api_spec`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_generate_open_api_spec_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_generate_open_api_spec_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:generateOpenApiSpec
/// Generate OpenAPI spec for the requested integrations and api triggers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_generate_open_api_spec_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_generate_open_api_spec_task()`.
/// For the simplest API, use `integrations_projects_locations_generate_open_api_spec()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_generate_open_api_spec_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_generate_open_api_spec_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_generate_open_api_spec_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_generate_open_api_spec`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsGenerateOpenApiSpecArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:generateOpenApiSpec
/// Generate OpenAPI spec for the requested integrations and api triggers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_generate_open_api_spec_builder()` + `integrations_projects_locations_generate_open_api_spec_execute()`.
/// For task-level control, use `integrations_projects_locations_generate_open_api_spec_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_generate_open_api_spec(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsGenerateOpenApiSpecArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_generate_open_api_spec_builder(client, &args.name)?;
    integrations_projects_locations_generate_open_api_spec_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clients
/// Gets the client configuration for the given project and location resource name
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_get_clients_execute()` to send, or `integrations_projects_locations_get_clients` for simplest API.

pub fn integrations_projects_locations_get_clients_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/clients",
        parent,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clients
/// Gets the client configuration for the given project and location resource name
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_get_clients_execute()` or `integrations_projects_locations_get_clients`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_get_clients_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_get_clients_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaGetClientResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaGetClientResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clients
/// Gets the client configuration for the given project and location resource name
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_get_clients_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_get_clients_task()`.
/// For the simplest API, use `integrations_projects_locations_get_clients()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_get_clients_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_get_clients_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaGetClientResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_get_clients_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_get_clients`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsGetClientsArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clients
/// Gets the client configuration for the given project and location resource name
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_get_clients_builder()` + `integrations_projects_locations_get_clients_execute()`.
/// For task-level control, use `integrations_projects_locations_get_clients_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_get_clients(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsGetClientsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaGetClientResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_get_clients_builder(client, &args.parent)?;
    integrations_projects_locations_get_clients_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/appsScriptProjects
/// Creates an Apps Script project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_apps_script_projects_create_execute()` to send, or `integrations_projects_locations_apps_script_projects_create` for simplest API.

pub fn integrations_projects_locations_apps_script_projects_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/appsScriptProjects",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/appsScriptProjects
/// Creates an Apps Script project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_apps_script_projects_create_execute()` or `integrations_projects_locations_apps_script_projects_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_apps_script_projects_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_apps_script_projects_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/appsScriptProjects
/// Creates an Apps Script project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_apps_script_projects_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_apps_script_projects_create_task()`.
/// For the simplest API, use `integrations_projects_locations_apps_script_projects_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_apps_script_projects_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_apps_script_projects_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_apps_script_projects_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_apps_script_projects_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsAppsScriptProjectsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/appsScriptProjects
/// Creates an Apps Script project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_apps_script_projects_create_builder()` + `integrations_projects_locations_apps_script_projects_create_execute()`.
/// For task-level control, use `integrations_projects_locations_apps_script_projects_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_apps_script_projects_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsAppsScriptProjectsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_apps_script_projects_create_builder(client, &args.parent)?;
    integrations_projects_locations_apps_script_projects_create_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/appsScriptProjects:link
/// Links a existing Apps Script project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_apps_script_projects_link_execute()` to send, or `integrations_projects_locations_apps_script_projects_link` for simplest API.

pub fn integrations_projects_locations_apps_script_projects_link_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/appsScriptProjects:link",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/appsScriptProjects:link
/// Links a existing Apps Script project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_apps_script_projects_link_execute()` or `integrations_projects_locations_apps_script_projects_link`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_apps_script_projects_link_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_apps_script_projects_link_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/appsScriptProjects:link
/// Links a existing Apps Script project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_apps_script_projects_link_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_apps_script_projects_link_task()`.
/// For the simplest API, use `integrations_projects_locations_apps_script_projects_link()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_apps_script_projects_link_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_apps_script_projects_link_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_apps_script_projects_link_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_apps_script_projects_link`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsAppsScriptProjectsLinkArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/appsScriptProjects:link
/// Links a existing Apps Script project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_apps_script_projects_link_builder()` + `integrations_projects_locations_apps_script_projects_link_execute()`.
/// For task-level control, use `integrations_projects_locations_apps_script_projects_link_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_apps_script_projects_link(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsAppsScriptProjectsLinkArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_apps_script_projects_link_builder(client, &args.parent)?;
    integrations_projects_locations_apps_script_projects_link_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_auth_configs_create_execute()` to send, or `integrations_projects_locations_auth_configs_create` for simplest API.

pub fn integrations_projects_locations_auth_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    clientCertificate_encryptedPrivateKey: &Option<Option<String>>,
    clientCertificate_passphrase: &Option<Option<String>>,
    clientCertificate_sslCertificate: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/authConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientCertificate_encryptedPrivateKey.as_ref() {
        query_parts.push(format!("clientCertificate.encryptedPrivateKey={}", val));
    }
    if let Some(val) = clientCertificate_passphrase.as_ref() {
        query_parts.push(format!("clientCertificate.passphrase={}", val));
    }
    if let Some(val) = clientCertificate_sslCertificate.as_ref() {
        query_parts.push(format!("clientCertificate.sslCertificate={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_auth_configs_create_execute()` or `integrations_projects_locations_auth_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_auth_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaAuthConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_auth_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_auth_configs_create_task()`.
/// For the simplest API, use `integrations_projects_locations_auth_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_auth_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_auth_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_auth_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_auth_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsAuthConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: clientCertificate_encryptedPrivateKey
    pub clientCertificate_encryptedPrivateKey: Option<Option<String>>,
    /// Query parameter: clientCertificate_passphrase
    pub clientCertificate_passphrase: Option<Option<String>>,
    /// Query parameter: clientCertificate_sslCertificate
    pub clientCertificate_sslCertificate: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_auth_configs_create_builder()` + `integrations_projects_locations_auth_configs_create_execute()`.
/// For task-level control, use `integrations_projects_locations_auth_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsAuthConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_auth_configs_create_builder(
        client,
        &args.parent,
        &args.clientCertificate_encryptedPrivateKey,
        &args.clientCertificate_passphrase,
        &args.clientCertificate_sslCertificate,
    )?;
    integrations_projects_locations_auth_configs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/authConfigs/{authConfigsId}
/// Deletes an auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_auth_configs_delete_execute()` to send, or `integrations_projects_locations_auth_configs_delete` for simplest API.

pub fn integrations_projects_locations_auth_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/authConfigs/{authConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/authConfigs/{authConfigsId}
/// Deletes an auth config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_auth_configs_delete_execute()` or `integrations_projects_locations_auth_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_auth_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/authConfigs/{authConfigsId}
/// Deletes an auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_auth_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_auth_configs_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_auth_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_auth_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_auth_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_auth_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_auth_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsAuthConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/authConfigs/{authConfigsId}
/// Deletes an auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_auth_configs_delete_builder()` + `integrations_projects_locations_auth_configs_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_auth_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsAuthConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_auth_configs_delete_builder(client, &args.name)?;
    integrations_projects_locations_auth_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authConfigs/{authConfigsId}
/// Gets a complete auth config. If the auth config doesn''t exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_auth_configs_get_execute()` to send, or `integrations_projects_locations_auth_configs_get` for simplest API.

pub fn integrations_projects_locations_auth_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/authConfigs/{authConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authConfigs/{authConfigsId}
/// Gets a complete auth config. If the auth config doesn''t exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_auth_configs_get_execute()` or `integrations_projects_locations_auth_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_auth_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaAuthConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authConfigs/{authConfigsId}
/// Gets a complete auth config. If the auth config doesn''t exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_auth_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_auth_configs_get_task()`.
/// For the simplest API, use `integrations_projects_locations_auth_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_auth_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_auth_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_auth_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_auth_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsAuthConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authConfigs/{authConfigsId}
/// Gets a complete auth config. If the auth config doesn''t exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_auth_configs_get_builder()` + `integrations_projects_locations_auth_configs_get_execute()`.
/// For task-level control, use `integrations_projects_locations_auth_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsAuthConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_auth_configs_get_builder(client, &args.name)?;
    integrations_projects_locations_auth_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_auth_configs_list_execute()` to send, or `integrations_projects_locations_auth_configs_list` for simplest API.

pub fn integrations_projects_locations_auth_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/authConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_auth_configs_list_execute()` or `integrations_projects_locations_auth_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_auth_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListAuthConfigsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListAuthConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_auth_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_auth_configs_list_task()`.
/// For the simplest API, use `integrations_projects_locations_auth_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_auth_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_auth_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListAuthConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_auth_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_auth_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsAuthConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_auth_configs_list_builder()` + `integrations_projects_locations_auth_configs_list_execute()`.
/// For task-level control, use `integrations_projects_locations_auth_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsAuthConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListAuthConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_auth_configs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
    )?;
    integrations_projects_locations_auth_configs_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/authConfigs/{authConfigsId}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_auth_configs_patch_execute()` to send, or `integrations_projects_locations_auth_configs_patch` for simplest API.

pub fn integrations_projects_locations_auth_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    clientCertificate_encryptedPrivateKey: &Option<Option<String>>,
    clientCertificate_passphrase: &Option<Option<String>>,
    clientCertificate_sslCertificate: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/authConfigs/{authConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientCertificate_encryptedPrivateKey.as_ref() {
        query_parts.push(format!("clientCertificate.encryptedPrivateKey={}", val));
    }
    if let Some(val) = clientCertificate_passphrase.as_ref() {
        query_parts.push(format!("clientCertificate.passphrase={}", val));
    }
    if let Some(val) = clientCertificate_sslCertificate.as_ref() {
        query_parts.push(format!("clientCertificate.sslCertificate={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/authConfigs/{authConfigsId}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_auth_configs_patch_execute()` or `integrations_projects_locations_auth_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_auth_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaAuthConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/authConfigs/{authConfigsId}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_auth_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_auth_configs_patch_task()`.
/// For the simplest API, use `integrations_projects_locations_auth_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_auth_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_auth_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_auth_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_auth_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsAuthConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: clientCertificate_encryptedPrivateKey
    pub clientCertificate_encryptedPrivateKey: Option<Option<String>>,
    /// Query parameter: clientCertificate_passphrase
    pub clientCertificate_passphrase: Option<Option<String>>,
    /// Query parameter: clientCertificate_sslCertificate
    pub clientCertificate_sslCertificate: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/authConfigs/{authConfigsId}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_auth_configs_patch_builder()` + `integrations_projects_locations_auth_configs_patch_execute()`.
/// For task-level control, use `integrations_projects_locations_auth_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_auth_configs_patch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsAuthConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_auth_configs_patch_builder(
        client,
        &args.name,
        &args.clientCertificate_encryptedPrivateKey,
        &args.clientCertificate_passphrase,
        &args.clientCertificate_sslCertificate,
        &args.updateMask,
    )?;
    integrations_projects_locations_auth_configs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_certificates_create_execute()` to send, or `integrations_projects_locations_certificates_create` for simplest API.

pub fn integrations_projects_locations_certificates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/certificates",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_certificates_create_execute()` or `integrations_projects_locations_certificates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_certificates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_certificates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_certificates_create_task()`.
/// For the simplest API, use `integrations_projects_locations_certificates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_certificates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_certificates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_certificates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_certificates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsCertificatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_certificates_create_builder()` + `integrations_projects_locations_certificates_create_execute()`.
/// For task-level control, use `integrations_projects_locations_certificates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsCertificatesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_certificates_create_builder(client, &args.parent)?;
    integrations_projects_locations_certificates_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Delete a certificate
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_certificates_delete_execute()` to send, or `integrations_projects_locations_certificates_delete` for simplest API.

pub fn integrations_projects_locations_certificates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/certificates/{certificatesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Delete a certificate
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_certificates_delete_execute()` or `integrations_projects_locations_certificates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_certificates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Delete a certificate
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_certificates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_certificates_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_certificates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_certificates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_certificates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_certificates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_certificates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsCertificatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Delete a certificate
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_certificates_delete_builder()` + `integrations_projects_locations_certificates_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_certificates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsCertificatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_certificates_delete_builder(client, &args.name)?;
    integrations_projects_locations_certificates_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Get a certificates in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_certificates_get_execute()` to send, or `integrations_projects_locations_certificates_get` for simplest API.

pub fn integrations_projects_locations_certificates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/certificates/{certificatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Get a certificates in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_certificates_get_execute()` or `integrations_projects_locations_certificates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_certificates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Get a certificates in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_certificates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_certificates_get_task()`.
/// For the simplest API, use `integrations_projects_locations_certificates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_certificates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_certificates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_certificates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsCertificatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Get a certificates in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_certificates_get_builder()` + `integrations_projects_locations_certificates_get_execute()`.
/// For task-level control, use `integrations_projects_locations_certificates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsCertificatesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_certificates_get_builder(client, &args.name)?;
    integrations_projects_locations_certificates_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_certificates_list_execute()` to send, or `integrations_projects_locations_certificates_list` for simplest API.

pub fn integrations_projects_locations_certificates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/certificates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_certificates_list_execute()` or `integrations_projects_locations_certificates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_certificates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListCertificatesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListCertificatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_certificates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_certificates_list_task()`.
/// For the simplest API, use `integrations_projects_locations_certificates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_certificates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListCertificatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_certificates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_certificates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsCertificatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_certificates_list_builder()` + `integrations_projects_locations_certificates_list_execute()`.
/// For task-level control, use `integrations_projects_locations_certificates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsCertificatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListCertificatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_certificates_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
    )?;
    integrations_projects_locations_certificates_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_certificates_patch_execute()` to send, or `integrations_projects_locations_certificates_patch` for simplest API.

pub fn integrations_projects_locations_certificates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/certificates/{certificatesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_certificates_patch_execute()` or `integrations_projects_locations_certificates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_certificates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_certificates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_certificates_patch_task()`.
/// For the simplest API, use `integrations_projects_locations_certificates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_certificates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_certificates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_certificates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_certificates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsCertificatesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_certificates_patch_builder()` + `integrations_projects_locations_certificates_patch_execute()`.
/// For task-level control, use `integrations_projects_locations_certificates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_certificates_patch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsCertificatesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_certificates_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    integrations_projects_locations_certificates_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:changeConfig
/// Updates the client customer configuration for the given project and location resource name
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_change_config_execute()` to send, or `integrations_projects_locations_clients_change_config` for simplest API.

pub fn integrations_projects_locations_clients_change_config_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/clients:changeConfig",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:changeConfig
/// Updates the client customer configuration for the given project and location resource name
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_clients_change_config_execute()` or `integrations_projects_locations_clients_change_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_change_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_change_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaChangeCustomerConfigResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaChangeCustomerConfigResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:changeConfig
/// Updates the client customer configuration for the given project and location resource name
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_clients_change_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_clients_change_config_task()`.
/// For the simplest API, use `integrations_projects_locations_clients_change_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_change_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_clients_change_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaChangeCustomerConfigResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_clients_change_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_clients_change_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsClientsChangeConfigArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:changeConfig
/// Updates the client customer configuration for the given project and location resource name
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_change_config_builder()` + `integrations_projects_locations_clients_change_config_execute()`.
/// For task-level control, use `integrations_projects_locations_clients_change_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_change_config(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsClientsChangeConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaChangeCustomerConfigResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_clients_change_config_builder(client, &args.parent)?;
    integrations_projects_locations_clients_change_config_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:deprovision
/// Perform the deprovisioning steps to disable a user GCP project to use IP and purge all related data in a wipeout-compliant way.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_deprovision_execute()` to send, or `integrations_projects_locations_clients_deprovision` for simplest API.

pub fn integrations_projects_locations_clients_deprovision_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/clients:deprovision",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:deprovision
/// Perform the deprovisioning steps to disable a user GCP project to use IP and purge all related data in a wipeout-compliant way.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_clients_deprovision_execute()` or `integrations_projects_locations_clients_deprovision`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_deprovision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_deprovision_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:deprovision
/// Perform the deprovisioning steps to disable a user GCP project to use IP and purge all related data in a wipeout-compliant way.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_clients_deprovision_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_clients_deprovision_task()`.
/// For the simplest API, use `integrations_projects_locations_clients_deprovision()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_deprovision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_clients_deprovision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_clients_deprovision_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_clients_deprovision`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsClientsDeprovisionArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:deprovision
/// Perform the deprovisioning steps to disable a user GCP project to use IP and purge all related data in a wipeout-compliant way.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_deprovision_builder()` + `integrations_projects_locations_clients_deprovision_execute()`.
/// For task-level control, use `integrations_projects_locations_clients_deprovision_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_deprovision(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsClientsDeprovisionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_clients_deprovision_builder(client, &args.parent)?;
    integrations_projects_locations_clients_deprovision_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:provision
/// Perform the provisioning steps to enable a user GCP project to use IP. If GCP project already registered on IP end via Apigee Integration, provisioning will fail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_provision_execute()` to send, or `integrations_projects_locations_clients_provision` for simplest API.

pub fn integrations_projects_locations_clients_provision_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/clients:provision",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:provision
/// Perform the provisioning steps to enable a user GCP project to use IP. If GCP project already registered on IP end via Apigee Integration, provisioning will fail.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_clients_provision_execute()` or `integrations_projects_locations_clients_provision`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_provision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_provision_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:provision
/// Perform the provisioning steps to enable a user GCP project to use IP. If GCP project already registered on IP end via Apigee Integration, provisioning will fail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_clients_provision_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_clients_provision_task()`.
/// For the simplest API, use `integrations_projects_locations_clients_provision()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_provision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_clients_provision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_clients_provision_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_clients_provision`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsClientsProvisionArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:provision
/// Perform the provisioning steps to enable a user GCP project to use IP. If GCP project already registered on IP end via Apigee Integration, provisioning will fail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_provision_builder()` + `integrations_projects_locations_clients_provision_execute()`.
/// For task-level control, use `integrations_projects_locations_clients_provision_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_provision(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsClientsProvisionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_clients_provision_builder(client, &args.parent)?;
    integrations_projects_locations_clients_provision_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:provisionClientPostProcessor
/// Perform post provisioning steps after client is provisioned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_provision_client_post_processor_execute()` to send, or `integrations_projects_locations_clients_provision_client_post_processor` for simplest API.

pub fn integrations_projects_locations_clients_provision_client_post_processor_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/clients:provisionClientPostProcessor",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:provisionClientPostProcessor
/// Perform post provisioning steps after client is provisioned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_clients_provision_client_post_processor_execute()` or `integrations_projects_locations_clients_provision_client_post_processor`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_provision_client_post_processor_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_provision_client_post_processor_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:provisionClientPostProcessor
/// Perform post provisioning steps after client is provisioned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_clients_provision_client_post_processor_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_clients_provision_client_post_processor_task()`.
/// For the simplest API, use `integrations_projects_locations_clients_provision_client_post_processor()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_provision_client_post_processor_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_clients_provision_client_post_processor_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_clients_provision_client_post_processor_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_clients_provision_client_post_processor`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsClientsProvisionClientPostProcessorArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:provisionClientPostProcessor
/// Perform post provisioning steps after client is provisioned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_provision_client_post_processor_builder()` + `integrations_projects_locations_clients_provision_client_post_processor_execute()`.
/// For task-level control, use `integrations_projects_locations_clients_provision_client_post_processor_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_provision_client_post_processor(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsClientsProvisionClientPostProcessorArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_clients_provision_client_post_processor_builder(
        client,
        &args.parent,
    )?;
    integrations_projects_locations_clients_provision_client_post_processor_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:replace
/// Update run-as service account for provisioned client
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_replace_execute()` to send, or `integrations_projects_locations_clients_replace` for simplest API.

pub fn integrations_projects_locations_clients_replace_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/clients:replace",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:replace
/// Update run-as service account for provisioned client
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_clients_replace_execute()` or `integrations_projects_locations_clients_replace`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_replace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_replace_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:replace
/// Update run-as service account for provisioned client
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_clients_replace_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_clients_replace_task()`.
/// For the simplest API, use `integrations_projects_locations_clients_replace()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_replace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_clients_replace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_clients_replace_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_clients_replace`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsClientsReplaceArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:replace
/// Update run-as service account for provisioned client
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_replace_builder()` + `integrations_projects_locations_clients_replace_execute()`.
/// For task-level control, use `integrations_projects_locations_clients_replace_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_replace(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsClientsReplaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_clients_replace_builder(client, &args.parent)?;
    integrations_projects_locations_clients_replace_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:switch
/// Update client from GMEK to CMEK
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_switch_execute()` to send, or `integrations_projects_locations_clients_switch` for simplest API.

pub fn integrations_projects_locations_clients_switch_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/clients:switch",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:switch
/// Update client from GMEK to CMEK
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_clients_switch_execute()` or `integrations_projects_locations_clients_switch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_switch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_switch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:switch
/// Update client from GMEK to CMEK
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_clients_switch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_clients_switch_task()`.
/// For the simplest API, use `integrations_projects_locations_clients_switch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_switch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_clients_switch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_clients_switch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_clients_switch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsClientsSwitchArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:switch
/// Update client from GMEK to CMEK
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_switch_builder()` + `integrations_projects_locations_clients_switch_execute()`.
/// For task-level control, use `integrations_projects_locations_clients_switch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_switch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsClientsSwitchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_clients_switch_builder(client, &args.parent)?;
    integrations_projects_locations_clients_switch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:switchVariableMasking
/// Update variable masking for provisioned client
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_switch_variable_masking_execute()` to send, or `integrations_projects_locations_clients_switch_variable_masking` for simplest API.

pub fn integrations_projects_locations_clients_switch_variable_masking_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/clients:switchVariableMasking",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:switchVariableMasking
/// Update variable masking for provisioned client
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_clients_switch_variable_masking_execute()` or `integrations_projects_locations_clients_switch_variable_masking`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_switch_variable_masking_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_switch_variable_masking_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:switchVariableMasking
/// Update variable masking for provisioned client
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_clients_switch_variable_masking_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_clients_switch_variable_masking_task()`.
/// For the simplest API, use `integrations_projects_locations_clients_switch_variable_masking()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_switch_variable_masking_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_clients_switch_variable_masking_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_clients_switch_variable_masking_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_clients_switch_variable_masking`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsClientsSwitchVariableMaskingArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:switchVariableMasking
/// Update variable masking for provisioned client
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_switch_variable_masking_builder()` + `integrations_projects_locations_clients_switch_variable_masking_execute()`.
/// For task-level control, use `integrations_projects_locations_clients_switch_variable_masking_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_switch_variable_masking(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsClientsSwitchVariableMaskingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_clients_switch_variable_masking_builder(
        client,
        &args.parent,
    )?;
    integrations_projects_locations_clients_switch_variable_masking_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:toggleHttp
/// E`nable/Disable` http call for provisioned client
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_clients_toggle_http_execute()` to send, or `integrations_projects_locations_clients_toggle_http` for simplest API.

pub fn integrations_projects_locations_clients_toggle_http_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/clients:toggleHttp",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:toggleHttp
/// E`nable/Disable` http call for provisioned client
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_clients_toggle_http_execute()` or `integrations_projects_locations_clients_toggle_http`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_toggle_http_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_toggle_http_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:toggleHttp
/// E`nable/Disable` http call for provisioned client
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_clients_toggle_http_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_clients_toggle_http_task()`.
/// For the simplest API, use `integrations_projects_locations_clients_toggle_http()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_clients_toggle_http_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_clients_toggle_http_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_clients_toggle_http_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_clients_toggle_http`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsClientsToggleHttpArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clients:toggleHttp
/// E`nable/Disable` http call for provisioned client
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_clients_toggle_http_builder()` + `integrations_projects_locations_clients_toggle_http_execute()`.
/// For task-level control, use `integrations_projects_locations_clients_toggle_http_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_clients_toggle_http(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsClientsToggleHttpArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_clients_toggle_http_builder(client, &args.parent)?;
    integrations_projects_locations_clients_toggle_http_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/cloudFunctions
/// Creates a cloud function project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_cloud_functions_create_execute()` to send, or `integrations_projects_locations_cloud_functions_create` for simplest API.

pub fn integrations_projects_locations_cloud_functions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/cloudFunctions",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/cloudFunctions
/// Creates a cloud function project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_cloud_functions_create_execute()` or `integrations_projects_locations_cloud_functions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_cloud_functions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_cloud_functions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/cloudFunctions
/// Creates a cloud function project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_cloud_functions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_cloud_functions_create_task()`.
/// For the simplest API, use `integrations_projects_locations_cloud_functions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_cloud_functions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_cloud_functions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_cloud_functions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_cloud_functions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsCloudFunctionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/cloudFunctions
/// Creates a cloud function project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_cloud_functions_create_builder()` + `integrations_projects_locations_cloud_functions_create_execute()`.
/// For task-level control, use `integrations_projects_locations_cloud_functions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_cloud_functions_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsCloudFunctionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_cloud_functions_create_builder(client, &args.parent)?;
    integrations_projects_locations_cloud_functions_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/connectionSchemaMetadata
/// Lists the available entities and actions associated with a Connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_connections_get_connection_schema_metadata_execute()` to send, or `integrations_projects_locations_connections_get_connection_schema_metadata` for simplest API.

pub fn integrations_projects_locations_connections_get_connection_schema_metadata_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/connections/{connectionsId}/connectionSchemaMetadata",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/connectionSchemaMetadata
/// Lists the available entities and actions associated with a Connection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_connections_get_connection_schema_metadata_execute()` or `integrations_projects_locations_connections_get_connection_schema_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_connections_get_connection_schema_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_get_connection_schema_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaConnectionSchemaMetadata>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaConnectionSchemaMetadata =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/connectionSchemaMetadata
/// Lists the available entities and actions associated with a Connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_connections_get_connection_schema_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_connections_get_connection_schema_metadata_task()`.
/// For the simplest API, use `integrations_projects_locations_connections_get_connection_schema_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_connections_get_connection_schema_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_connections_get_connection_schema_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaConnectionSchemaMetadata>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_connections_get_connection_schema_metadata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_connections_get_connection_schema_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsConnectionsGetConnectionSchemaMetadataArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/connectionSchemaMetadata
/// Lists the available entities and actions associated with a Connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_connections_get_connection_schema_metadata_builder()` + `integrations_projects_locations_connections_get_connection_schema_metadata_execute()`.
/// For task-level control, use `integrations_projects_locations_connections_get_connection_schema_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_get_connection_schema_metadata(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsConnectionsGetConnectionSchemaMetadataArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaConnectionSchemaMetadata>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_connections_get_connection_schema_metadata_builder(
            client, &args.name,
        )?;
    integrations_projects_locations_connections_get_connection_schema_metadata_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections
/// Lists Connections in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_connections_list_execute()` to send, or `integrations_projects_locations_connections_list` for simplest API.

pub fn integrations_projects_locations_connections_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/connections",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections
/// Lists Connections in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_connections_list_execute()` or `integrations_projects_locations_connections_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_connections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListConnectionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListConnectionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections
/// Lists Connections in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_connections_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_connections_list_task()`.
/// For the simplest API, use `integrations_projects_locations_connections_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_connections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListConnectionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_connections_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_connections_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsConnectionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections
/// Lists Connections in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_connections_list_builder()` + `integrations_projects_locations_connections_list_execute()`.
/// For task-level control, use `integrations_projects_locations_connections_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsConnectionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListConnectionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_connections_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    integrations_projects_locations_connections_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/runtimeActionSchemas
/// Lists the JSON schemas for the inputs and outputs of actions, filtered by action name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_connections_runtime_action_schemas_list_execute()` to send, or `integrations_projects_locations_connections_runtime_action_schemas_list` for simplest API.

pub fn integrations_projects_locations_connections_runtime_action_schemas_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/connections/{connectionsId}/runtimeActionSchemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/runtimeActionSchemas
/// Lists the JSON schemas for the inputs and outputs of actions, filtered by action name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_connections_runtime_action_schemas_list_execute()` or `integrations_projects_locations_connections_runtime_action_schemas_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_connections_runtime_action_schemas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_runtime_action_schemas_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListRuntimeActionSchemasResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListRuntimeActionSchemasResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/runtimeActionSchemas
/// Lists the JSON schemas for the inputs and outputs of actions, filtered by action name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_connections_runtime_action_schemas_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_connections_runtime_action_schemas_list_task()`.
/// For the simplest API, use `integrations_projects_locations_connections_runtime_action_schemas_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_connections_runtime_action_schemas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_connections_runtime_action_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListRuntimeActionSchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_connections_runtime_action_schemas_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_connections_runtime_action_schemas_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsConnectionsRuntimeActionSchemasListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/runtimeActionSchemas
/// Lists the JSON schemas for the inputs and outputs of actions, filtered by action name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_connections_runtime_action_schemas_list_builder()` + `integrations_projects_locations_connections_runtime_action_schemas_list_execute()`.
/// For task-level control, use `integrations_projects_locations_connections_runtime_action_schemas_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_runtime_action_schemas_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsConnectionsRuntimeActionSchemasListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListRuntimeActionSchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_connections_runtime_action_schemas_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    integrations_projects_locations_connections_runtime_action_schemas_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/runtimeEntitySchemas
/// Lists the JSON schemas for the properties of runtime entities, filtered by entity name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_connections_runtime_entity_schemas_list_execute()` to send, or `integrations_projects_locations_connections_runtime_entity_schemas_list` for simplest API.

pub fn integrations_projects_locations_connections_runtime_entity_schemas_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/connections/{connectionsId}/runtimeEntitySchemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/runtimeEntitySchemas
/// Lists the JSON schemas for the properties of runtime entities, filtered by entity name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_connections_runtime_entity_schemas_list_execute()` or `integrations_projects_locations_connections_runtime_entity_schemas_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_connections_runtime_entity_schemas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_runtime_entity_schemas_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListRuntimeEntitySchemasResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListRuntimeEntitySchemasResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/runtimeEntitySchemas
/// Lists the JSON schemas for the properties of runtime entities, filtered by entity name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_connections_runtime_entity_schemas_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_connections_runtime_entity_schemas_list_task()`.
/// For the simplest API, use `integrations_projects_locations_connections_runtime_entity_schemas_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_connections_runtime_entity_schemas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_connections_runtime_entity_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListRuntimeEntitySchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_connections_runtime_entity_schemas_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_connections_runtime_entity_schemas_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsConnectionsRuntimeEntitySchemasListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}/runtimeEntitySchemas
/// Lists the JSON schemas for the properties of runtime entities, filtered by entity name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_connections_runtime_entity_schemas_list_builder()` + `integrations_projects_locations_connections_runtime_entity_schemas_list_execute()`.
/// For task-level control, use `integrations_projects_locations_connections_runtime_entity_schemas_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_connections_runtime_entity_schemas_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsConnectionsRuntimeEntitySchemasListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListRuntimeEntitySchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_connections_runtime_entity_schemas_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    integrations_projects_locations_connections_runtime_entity_schemas_list_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}
/// Delete the selected integration and all versions inside
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_delete_execute()` to send, or `integrations_projects_locations_integrations_delete` for simplest API.

pub fn integrations_projects_locations_integrations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}
/// Delete the selected integration and all versions inside
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_delete_execute()` or `integrations_projects_locations_integrations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}
/// Delete the selected integration and all versions inside
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}
/// Delete the selected integration and all versions inside
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_delete_builder()` + `integrations_projects_locations_integrations_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_delete_builder(client, &args.name)?;
    integrations_projects_locations_integrations_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_execute_execute()` to send, or `integrations_projects_locations_integrations_execute` for simplest API.

pub fn integrations_projects_locations_integrations_execute_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}:execute",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_execute_execute()` or `integrations_projects_locations_integrations_execute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_execute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_execute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_execute_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_execute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_execute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_execute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsExecuteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_execute_builder()` + `integrations_projects_locations_integrations_execute_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_execute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_execute(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsExecuteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_execute_builder(client, &args.name)?;
    integrations_projects_locations_integrations_execute_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:executeEvent
/// Executes an integration on receiving events from Integration Connector triggers, Eventarc or CPS Trigger. Input data to integration is received in body in json format
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_execute_event_execute()` to send, or `integrations_projects_locations_integrations_execute_event` for simplest API.

pub fn integrations_projects_locations_integrations_execute_event_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    triggerId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}:executeEvent",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = triggerId.as_ref() {
        query_parts.push(format!("triggerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:executeEvent
/// Executes an integration on receiving events from Integration Connector triggers, Eventarc or CPS Trigger. Input data to integration is received in body in json format
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_execute_event_execute()` or `integrations_projects_locations_integrations_execute_event`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_execute_event_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_execute_event_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteEventResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecuteEventResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:executeEvent
/// Executes an integration on receiving events from Integration Connector triggers, Eventarc or CPS Trigger. Input data to integration is received in body in json format
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_execute_event_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_execute_event_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_execute_event()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_execute_event_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_execute_event_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecuteEventResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_execute_event_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_execute_event`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsExecuteEventArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: triggerId
    pub triggerId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:executeEvent
/// Executes an integration on receiving events from Integration Connector triggers, Eventarc or CPS Trigger. Input data to integration is received in body in json format
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_execute_event_builder()` + `integrations_projects_locations_integrations_execute_event_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_execute_event_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_execute_event(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsExecuteEventArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecuteEventResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_execute_event_builder(
        client,
        &args.name,
        &args.triggerId,
    )?;
    integrations_projects_locations_integrations_execute_event_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_list_execute()` to send, or `integrations_projects_locations_integrations_list` for simplest API.

pub fn integrations_projects_locations_integrations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_list_execute()` or `integrations_projects_locations_integrations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_list_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_list_builder()` + `integrations_projects_locations_integrations_list_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    integrations_projects_locations_integrations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_schedule_execute()` to send, or `integrations_projects_locations_integrations_schedule` for simplest API.

pub fn integrations_projects_locations_integrations_schedule_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}:schedule",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_schedule_execute()` or `integrations_projects_locations_integrations_schedule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_schedule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_schedule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_schedule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_schedule_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_schedule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_schedule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_schedule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_schedule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_schedule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsScheduleArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_schedule_builder()` + `integrations_projects_locations_integrations_schedule_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_schedule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_schedule(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsScheduleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_schedule_builder(client, &args.name)?;
    integrations_projects_locations_integrations_schedule_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations:search
/// Searches and returns the list of integrations in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_search_execute()` to send, or `integrations_projects_locations_integrations_search` for simplest API.

pub fn integrations_projects_locations_integrations_search_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    enableNaturalLanguageQueryUnderstanding: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    query: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = enableNaturalLanguageQueryUnderstanding.as_ref() {
        query_parts.push(format!("enableNaturalLanguageQueryUnderstanding={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations:search
/// Searches and returns the list of integrations in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_search_execute()` or `integrations_projects_locations_integrations_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaSearchIntegrationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSearchIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations:search
/// Searches and returns the list of integrations in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_search_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaSearchIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: enableNaturalLanguageQueryUnderstanding
    pub enableNaturalLanguageQueryUnderstanding: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: query
    pub query: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations:search
/// Searches and returns the list of integrations in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_search_builder()` + `integrations_projects_locations_integrations_search_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_search(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaSearchIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_search_builder(
        client,
        &args.parent,
        &args.enableNaturalLanguageQueryUnderstanding,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.query,
    )?;
    integrations_projects_locations_integrations_search_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:test
/// Execute the integration in draft state
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_test_execute()` to send, or `integrations_projects_locations_integrations_test` for simplest API.

pub fn integrations_projects_locations_integrations_test_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}:test",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:test
/// Execute the integration in draft state
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_test_execute()` or `integrations_projects_locations_integrations_test`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_test_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:test
/// Execute the integration in draft state
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_test_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_test_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_test()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_test_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_test`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsTestArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}:test
/// Execute the integration in draft state
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_test_builder()` + `integrations_projects_locations_integrations_test_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_test_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_test(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsTestArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_test_builder(client, &args.name)?;
    integrations_projects_locations_integrations_test_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:cancel
/// Cancellation of an execution and associated sub-executions. This will not cancel an IN_PROCESS or completed(SUCCESSFUL, FAILED or CANCELLED) executions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_cancel_execute()` to send, or `integrations_projects_locations_integrations_executions_cancel` for simplest API.

pub fn integrations_projects_locations_integrations_executions_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:cancel
/// Cancellation of an execution and associated sub-executions. This will not cancel an IN_PROCESS or completed(SUCCESSFUL, FAILED or CANCELLED) executions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_executions_cancel_execute()` or `integrations_projects_locations_integrations_executions_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCancelExecutionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCancelExecutionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:cancel
/// Cancellation of an execution and associated sub-executions. This will not cancel an IN_PROCESS or completed(SUCCESSFUL, FAILED or CANCELLED) executions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_executions_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_executions_cancel_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_executions_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_executions_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCancelExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_executions_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_executions_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsExecutionsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:cancel
/// Cancellation of an execution and associated sub-executions. This will not cancel an IN_PROCESS or completed(SUCCESSFUL, FAILED or CANCELLED) executions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_cancel_builder()` + `integrations_projects_locations_integrations_executions_cancel_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_executions_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_cancel(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsExecutionsCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCancelExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_executions_cancel_builder(client, &args.name)?;
    integrations_projects_locations_integrations_executions_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:download
/// Download the execution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_download_execute()` to send, or `integrations_projects_locations_integrations_executions_download` for simplest API.

pub fn integrations_projects_locations_integrations_executions_download_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:download",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:download
/// Download the execution.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_executions_download_execute()` or `integrations_projects_locations_integrations_executions_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadExecutionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadExecutionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:download
/// Download the execution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_executions_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_executions_download_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_executions_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_executions_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_executions_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_executions_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsExecutionsDownloadArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:download
/// Download the execution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_download_builder()` + `integrations_projects_locations_integrations_executions_download_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_executions_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_download(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsExecutionsDownloadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_executions_download_builder(
        client, &args.name,
    )?;
    integrations_projects_locations_integrations_executions_download_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}
/// Get an execution in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_get_execute()` to send, or `integrations_projects_locations_integrations_executions_get` for simplest API.

pub fn integrations_projects_locations_integrations_executions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}
/// Get an execution in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_executions_get_execute()` or `integrations_projects_locations_integrations_executions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecution>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecution =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}
/// Get an execution in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_executions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_executions_get_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_executions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_executions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_executions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_executions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsExecutionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}
/// Get an execution in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_get_builder()` + `integrations_projects_locations_integrations_executions_get_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_executions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsExecutionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_executions_get_builder(client, &args.name)?;
    integrations_projects_locations_integrations_executions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_list_execute()` to send, or `integrations_projects_locations_integrations_executions_list` for simplest API.

pub fn integrations_projects_locations_integrations_executions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    filterParams_customFilter: &Option<Option<String>>,
    filterParams_endTime: &Option<Option<String>>,
    filterParams_eventStatuses: &Option<Option<String>>,
    filterParams_executionId: &Option<Option<String>>,
    filterParams_parameterKey: &Option<Option<String>>,
    filterParams_parameterPairKey: &Option<Option<String>>,
    filterParams_parameterPairValue: &Option<Option<String>>,
    filterParams_parameterType: &Option<Option<String>>,
    filterParams_parameterValue: &Option<Option<String>>,
    filterParams_startTime: &Option<Option<String>>,
    filterParams_taskStatuses: &Option<Option<String>>,
    filterParams_workflowName: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
    refreshAcl: &Option<Option<String>>,
    snapshotMetadataWithoutParams: &Option<Option<String>>,
    truncateParams: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/executions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = filterParams_customFilter.as_ref() {
        query_parts.push(format!("filterParams.customFilter={}", val));
    }
    if let Some(val) = filterParams_endTime.as_ref() {
        query_parts.push(format!("filterParams.endTime={}", val));
    }
    if let Some(val) = filterParams_eventStatuses.as_ref() {
        query_parts.push(format!("filterParams.eventStatuses={}", val));
    }
    if let Some(val) = filterParams_executionId.as_ref() {
        query_parts.push(format!("filterParams.executionId={}", val));
    }
    if let Some(val) = filterParams_parameterKey.as_ref() {
        query_parts.push(format!("filterParams.parameterKey={}", val));
    }
    if let Some(val) = filterParams_parameterPairKey.as_ref() {
        query_parts.push(format!("filterParams.parameterPairKey={}", val));
    }
    if let Some(val) = filterParams_parameterPairValue.as_ref() {
        query_parts.push(format!("filterParams.parameterPairValue={}", val));
    }
    if let Some(val) = filterParams_parameterType.as_ref() {
        query_parts.push(format!("filterParams.parameterType={}", val));
    }
    if let Some(val) = filterParams_parameterValue.as_ref() {
        query_parts.push(format!("filterParams.parameterValue={}", val));
    }
    if let Some(val) = filterParams_startTime.as_ref() {
        query_parts.push(format!("filterParams.startTime={}", val));
    }
    if let Some(val) = filterParams_taskStatuses.as_ref() {
        query_parts.push(format!("filterParams.taskStatuses={}", val));
    }
    if let Some(val) = filterParams_workflowName.as_ref() {
        query_parts.push(format!("filterParams.workflowName={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }
    if let Some(val) = refreshAcl.as_ref() {
        query_parts.push(format!("refreshAcl={}", val));
    }
    if let Some(val) = snapshotMetadataWithoutParams.as_ref() {
        query_parts.push(format!("snapshotMetadataWithoutParams={}", val));
    }
    if let Some(val) = truncateParams.as_ref() {
        query_parts.push(format!("truncateParams={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_executions_list_execute()` or `integrations_projects_locations_integrations_executions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListExecutionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListExecutionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_executions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_executions_list_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_executions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_executions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_executions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_executions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsExecutionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: filterParams_customFilter
    pub filterParams_customFilter: Option<Option<String>>,
    /// Query parameter: filterParams_endTime
    pub filterParams_endTime: Option<Option<String>>,
    /// Query parameter: filterParams_eventStatuses
    pub filterParams_eventStatuses: Option<Option<String>>,
    /// Query parameter: filterParams_executionId
    pub filterParams_executionId: Option<Option<String>>,
    /// Query parameter: filterParams_parameterKey
    pub filterParams_parameterKey: Option<Option<String>>,
    /// Query parameter: filterParams_parameterPairKey
    pub filterParams_parameterPairKey: Option<Option<String>>,
    /// Query parameter: filterParams_parameterPairValue
    pub filterParams_parameterPairValue: Option<Option<String>>,
    /// Query parameter: filterParams_parameterType
    pub filterParams_parameterType: Option<Option<String>>,
    /// Query parameter: filterParams_parameterValue
    pub filterParams_parameterValue: Option<Option<String>>,
    /// Query parameter: filterParams_startTime
    pub filterParams_startTime: Option<Option<String>>,
    /// Query parameter: filterParams_taskStatuses
    pub filterParams_taskStatuses: Option<Option<String>>,
    /// Query parameter: filterParams_workflowName
    pub filterParams_workflowName: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
    /// Query parameter: refreshAcl
    pub refreshAcl: Option<Option<String>>,
    /// Query parameter: snapshotMetadataWithoutParams
    pub snapshotMetadataWithoutParams: Option<Option<String>>,
    /// Query parameter: truncateParams
    pub truncateParams: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_list_builder()` + `integrations_projects_locations_integrations_executions_list_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_executions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsExecutionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_executions_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.filterParams_customFilter,
        &args.filterParams_endTime,
        &args.filterParams_eventStatuses,
        &args.filterParams_executionId,
        &args.filterParams_parameterKey,
        &args.filterParams_parameterPairKey,
        &args.filterParams_parameterPairValue,
        &args.filterParams_parameterType,
        &args.filterParams_parameterValue,
        &args.filterParams_startTime,
        &args.filterParams_taskStatuses,
        &args.filterParams_workflowName,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
        &args.refreshAcl,
        &args.snapshotMetadataWithoutParams,
        &args.truncateParams,
    )?;
    integrations_projects_locations_integrations_executions_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:replay
/// Re-execute an existing execution, with same request parameters and execution strategy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_replay_execute()` to send, or `integrations_projects_locations_integrations_executions_replay` for simplest API.

pub fn integrations_projects_locations_integrations_executions_replay_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:replay",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:replay
/// Re-execute an existing execution, with same request parameters and execution strategy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_executions_replay_execute()` or `integrations_projects_locations_integrations_executions_replay`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_replay_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_replay_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaReplayExecutionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaReplayExecutionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:replay
/// Re-execute an existing execution, with same request parameters and execution strategy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_executions_replay_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_executions_replay_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_executions_replay()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_replay_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_executions_replay_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaReplayExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_executions_replay_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_executions_replay`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsExecutionsReplayArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}:replay
/// Re-execute an existing execution, with same request parameters and execution strategy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_replay_builder()` + `integrations_projects_locations_integrations_executions_replay_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_executions_replay_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_replay(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsExecutionsReplayArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaReplayExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_executions_replay_builder(client, &args.name)?;
    integrations_projects_locations_integrations_executions_replay_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_suspensions_lift_execute()` to send, or `integrations_projects_locations_integrations_executions_suspensions_lift` for simplest API.

pub fn integrations_projects_locations_integrations_executions_suspensions_lift_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:lift",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_executions_suspensions_lift_execute()` or `integrations_projects_locations_integrations_executions_suspensions_lift`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_suspensions_lift_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_suspensions_lift_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaLiftSuspensionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaLiftSuspensionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_executions_suspensions_lift_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_executions_suspensions_lift_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_executions_suspensions_lift()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_suspensions_lift_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_executions_suspensions_lift_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaLiftSuspensionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_integrations_executions_suspensions_lift_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_executions_suspensions_lift`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsLiftArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_suspensions_lift_builder()` + `integrations_projects_locations_integrations_executions_suspensions_lift_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_executions_suspensions_lift_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_suspensions_lift(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsLiftArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaLiftSuspensionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_executions_suspensions_lift_builder(
        client, &args.name,
    )?;
    integrations_projects_locations_integrations_executions_suspensions_lift_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_suspensions_list_execute()` to send, or `integrations_projects_locations_integrations_executions_suspensions_list` for simplest API.

pub fn integrations_projects_locations_integrations_executions_suspensions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_executions_suspensions_list_execute()` or `integrations_projects_locations_integrations_executions_suspensions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_suspensions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_suspensions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSuspensionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListSuspensionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_executions_suspensions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_executions_suspensions_list_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_executions_suspensions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_suspensions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_executions_suspensions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSuspensionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_integrations_executions_suspensions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_executions_suspensions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_suspensions_list_builder()` + `integrations_projects_locations_integrations_executions_suspensions_list_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_executions_suspensions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_suspensions_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSuspensionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_executions_suspensions_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    integrations_projects_locations_integrations_executions_suspensions_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_executions_suspensions_resolve_execute()` to send, or `integrations_projects_locations_integrations_executions_suspensions_resolve` for simplest API.

pub fn integrations_projects_locations_integrations_executions_suspensions_resolve_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:resolve",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_executions_suspensions_resolve_execute()` or `integrations_projects_locations_integrations_executions_suspensions_resolve`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_suspensions_resolve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_suspensions_resolve_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaResolveSuspensionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaResolveSuspensionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_executions_suspensions_resolve_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_executions_suspensions_resolve_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_executions_suspensions_resolve()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_executions_suspensions_resolve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_executions_suspensions_resolve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaResolveSuspensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_integrations_executions_suspensions_resolve_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_executions_suspensions_resolve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsResolveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_executions_suspensions_resolve_builder()` + `integrations_projects_locations_integrations_executions_suspensions_resolve_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_executions_suspensions_resolve_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_executions_suspensions_resolve(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsResolveArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaResolveSuspensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_executions_suspensions_resolve_builder(
            client, &args.name,
        )?;
    integrations_projects_locations_integrations_executions_suspensions_resolve_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions
/// Create a integration with a draft version in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_create_execute()` to send, or `integrations_projects_locations_integrations_versions_create` for simplest API.

pub fn integrations_projects_locations_integrations_versions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    createSampleIntegrations: &Option<Option<String>>,
    newIntegration: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = createSampleIntegrations.as_ref() {
        query_parts.push(format!("createSampleIntegrations={}", val));
    }
    if let Some(val) = newIntegration.as_ref() {
        query_parts.push(format!("newIntegration={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions
/// Create a integration with a draft version in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_create_execute()` or `integrations_projects_locations_integrations_versions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaIntegrationVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions
/// Create a integration with a draft version in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_create_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: createSampleIntegrations
    pub createSampleIntegrations: Option<Option<String>>,
    /// Query parameter: newIntegration
    pub newIntegration: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions
/// Create a integration with a draft version in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_create_builder()` + `integrations_projects_locations_integrations_versions_create_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_create_builder(
        client,
        &args.parent,
        &args.createSampleIntegrations,
        &args.newIntegration,
    )?;
    integrations_projects_locations_integrations_versions_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_delete_execute()` to send, or `integrations_projects_locations_integrations_versions_delete` for simplest API.

pub fn integrations_projects_locations_integrations_versions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_delete_execute()` or `integrations_projects_locations_integrations_versions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_delete_builder()` + `integrations_projects_locations_integrations_versions_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_delete_builder(client, &args.name)?;
    integrations_projects_locations_integrations_versions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_download_execute()` to send, or `integrations_projects_locations_integrations_versions_download` for simplest API.

pub fn integrations_projects_locations_integrations_versions_download_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    fileFormat: &Option<Option<String>>,
    files: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:download",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fileFormat.as_ref() {
        query_parts.push(format!("fileFormat={}", val));
    }
    if let Some(val) = files.as_ref() {
        query_parts.push(format!("files={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_download_execute()` or `integrations_projects_locations_integrations_versions_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_download_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsDownloadArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: fileFormat
    pub fileFormat: Option<Option<String>>,
    /// Query parameter: files
    pub files: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_download_builder()` + `integrations_projects_locations_integrations_versions_download_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_download(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsDownloadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_download_builder(
        client,
        &args.name,
        &args.fileFormat,
        &args.files,
    )?;
    integrations_projects_locations_integrations_versions_download_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:downloadJsonPackage
/// Downloads an Integration version package like IntegrationVersion,Integration Config etc. Retrieves the IntegrationVersion package for a given integration_id and returns the response as a JSON.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_download_json_package_execute()` to send, or `integrations_projects_locations_integrations_versions_download_json_package` for simplest API.

pub fn integrations_projects_locations_integrations_versions_download_json_package_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    files: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:downloadJsonPackage",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = files.as_ref() {
        query_parts.push(format!("files={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:downloadJsonPackage
/// Downloads an Integration version package like IntegrationVersion,Integration Config etc. Retrieves the IntegrationVersion package for a given integration_id and returns the response as a JSON.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_download_json_package_execute()` or `integrations_projects_locations_integrations_versions_download_json_package`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_download_json_package_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_download_json_package_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadJsonPackageResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadJsonPackageResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:downloadJsonPackage
/// Downloads an Integration version package like IntegrationVersion,Integration Config etc. Retrieves the IntegrationVersion package for a given integration_id and returns the response as a JSON.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_download_json_package_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_download_json_package_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_download_json_package()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_download_json_package_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_download_json_package_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadJsonPackageResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_integrations_versions_download_json_package_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_download_json_package`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsDownloadJsonPackageArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: files
    pub files: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:downloadJsonPackage
/// Downloads an Integration version package like IntegrationVersion,Integration Config etc. Retrieves the IntegrationVersion package for a given integration_id and returns the response as a JSON.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_download_json_package_builder()` + `integrations_projects_locations_integrations_versions_download_json_package_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_download_json_package_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_download_json_package(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsDownloadJsonPackageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadJsonPackageResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_download_json_package_builder(
            client,
            &args.name,
            &args.files,
        )?;
    integrations_projects_locations_integrations_versions_download_json_package_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}
/// Get a integration in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_get_execute()` to send, or `integrations_projects_locations_integrations_versions_get` for simplest API.

pub fn integrations_projects_locations_integrations_versions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}
/// Get a integration in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_get_execute()` or `integrations_projects_locations_integrations_versions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaIntegrationVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}
/// Get a integration in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_get_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}
/// Get a integration in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_get_builder()` + `integrations_projects_locations_integrations_versions_get_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_get_builder(client, &args.name)?;
    integrations_projects_locations_integrations_versions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_list_execute()` to send, or `integrations_projects_locations_integrations_versions_list` for simplest API.

pub fn integrations_projects_locations_integrations_versions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    fieldMask: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fieldMask.as_ref() {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_list_execute()` or `integrations_projects_locations_integrations_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_list_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: fieldMask
    pub fieldMask: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_list_builder()` + `integrations_projects_locations_integrations_versions_list_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_list_builder(
        client,
        &args.parent,
        &args.fieldMask,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    integrations_projects_locations_integrations_versions_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}
/// Update a integration with a draft version in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_patch_execute()` to send, or `integrations_projects_locations_integrations_versions_patch` for simplest API.

pub fn integrations_projects_locations_integrations_versions_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}
/// Update a integration with a draft version in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_patch_execute()` or `integrations_projects_locations_integrations_versions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaIntegrationVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}
/// Update a integration with a draft version in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_patch_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}
/// Update a integration with a draft version in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_patch_builder()` + `integrations_projects_locations_integrations_versions_patch_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_patch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    integrations_projects_locations_integrations_versions_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_publish_execute()` to send, or `integrations_projects_locations_integrations_versions_publish` for simplest API.

pub fn integrations_projects_locations_integrations_versions_publish_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:publish",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_publish_execute()` or `integrations_projects_locations_integrations_versions_publish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_publish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_publish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_publish_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_publish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_publish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_publish`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsPublishArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_publish_builder()` + `integrations_projects_locations_integrations_versions_publish_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_publish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_publish(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsPublishArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_publish_builder(client, &args.name)?;
    integrations_projects_locations_integrations_versions_publish_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:test
/// Execute the integration in draft state
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_execute()` to send, or `integrations_projects_locations_integrations_versions_test` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:test",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:test
/// Execute the integration in draft state
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_test_execute()` or `integrations_projects_locations_integrations_versions_test`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:test
/// Execute the integration in draft state
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_test_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_test()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_test_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_test`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsTestArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:test
/// Execute the integration in draft state
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_builder()` + `integrations_projects_locations_integrations_versions_test_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsTestArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_test_builder(client, &args.name)?;
    integrations_projects_locations_integrations_versions_test_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_unpublish_execute()` to send, or `integrations_projects_locations_integrations_versions_unpublish` for simplest API.

pub fn integrations_projects_locations_integrations_versions_unpublish_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:unpublish",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_unpublish_execute()` or `integrations_projects_locations_integrations_versions_unpublish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_unpublish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_unpublish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_unpublish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_unpublish_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_unpublish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_unpublish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_unpublish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_unpublish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_unpublish`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsUnpublishArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_unpublish_builder()` + `integrations_projects_locations_integrations_versions_unpublish_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_unpublish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_unpublish(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsUnpublishArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_unpublish_builder(
        client, &args.name,
    )?;
    integrations_projects_locations_integrations_versions_unpublish_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_upload_execute()` to send, or `integrations_projects_locations_integrations_versions_upload` for simplest API.

pub fn integrations_projects_locations_integrations_versions_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions:upload",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_upload_execute()` or `integrations_projects_locations_integrations_versions_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_upload_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsUploadArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_upload_builder()` + `integrations_projects_locations_integrations_versions_upload_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_upload(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsUploadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_upload_builder(client, &args.parent)?;
    integrations_projects_locations_integrations_versions_upload_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases
/// Creates a new test case
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_create_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_create` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    testCaseId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = testCaseId.as_ref() {
        query_parts.push(format!("testCaseId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases
/// Creates a new test case
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_test_cases_create_execute()` or `integrations_projects_locations_integrations_versions_test_cases_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestCase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases
/// Creates a new test case
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_test_cases_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_create_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_test_cases_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_test_cases_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_integrations_versions_test_cases_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_test_cases_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsTestCasesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: testCaseId
    pub testCaseId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases
/// Creates a new test case
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_create_builder()` + `integrations_projects_locations_integrations_versions_test_cases_create_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_create_builder(
        client,
        &args.parent,
        &args.testCaseId,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}
/// Deletes a test case
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_delete_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_delete` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}
/// Deletes a test case
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_test_cases_delete_execute()` or `integrations_projects_locations_integrations_versions_test_cases_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}
/// Deletes a test case
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_test_cases_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_test_cases_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_test_cases_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_integrations_versions_test_cases_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_test_cases_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsTestCasesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}
/// Deletes a test case
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_delete_builder()` + `integrations_projects_locations_integrations_versions_test_cases_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_delete_builder(
        client, &args.name,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:download
/// Downloads a test case. Retrieves the TestCase for a given test_case_id and returns the response as a string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_download_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_download` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_download_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    fileFormat: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:download",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fileFormat.as_ref() {
        query_parts.push(format!("fileFormat={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:download
/// Downloads a test case. Retrieves the TestCase for a given test_case_id and returns the response as a string.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_test_cases_download_execute()` or `integrations_projects_locations_integrations_versions_test_cases_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadTestCaseResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadTestCaseResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:download
/// Downloads a test case. Retrieves the TestCase for a given test_case_id and returns the response as a string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_test_cases_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_download_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_test_cases_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_test_cases_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadTestCaseResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_integrations_versions_test_cases_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_test_cases_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsTestCasesDownloadArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: fileFormat
    pub fileFormat: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:download
/// Downloads a test case. Retrieves the TestCase for a given test_case_id and returns the response as a string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_download_builder()` + `integrations_projects_locations_integrations_versions_test_cases_download_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_download(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesDownloadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadTestCaseResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_test_cases_download_builder(
            client,
            &args.name,
            &args.fileFormat,
        )?;
    integrations_projects_locations_integrations_versions_test_cases_download_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases:execute
/// Executes all test cases in an integration version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_execute_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_execute` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases:execute",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases:execute
/// Executes all test cases in an integration version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_test_cases_execute_execute()` or `integrations_projects_locations_integrations_versions_test_cases_execute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteTestCasesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecuteTestCasesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases:execute
/// Executes all test cases in an integration version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_test_cases_execute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_execute_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_test_cases_execute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteTestCasesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_integrations_versions_test_cases_execute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_test_cases_execute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsTestCasesExecuteArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases:execute
/// Executes all test cases in an integration version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_execute_builder()` + `integrations_projects_locations_integrations_versions_test_cases_execute_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_execute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesExecuteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteTestCasesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_execute_builder(
        client,
        &args.parent,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_execute_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:executeTest
/// Executes functional test
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_execute_test_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_execute_test` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute_test_builder<R>(
    client: &SimpleHttpClient<R>,
    testCaseName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:executeTest",
        testCaseName,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:executeTest
/// Executes functional test
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_test_cases_execute_test_execute()` or `integrations_projects_locations_integrations_versions_test_cases_execute_test`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_execute_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute_test_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteTestCaseResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecuteTestCaseResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:executeTest
/// Executes functional test
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_test_cases_execute_test_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_execute_test_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_test_cases_execute_test()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_execute_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteTestCaseResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_test_cases_execute_test_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_test_cases_execute_test`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsTestCasesExecuteTestArgs {
    /// Path parameter: testCaseName
    pub testCaseName: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:executeTest
/// Executes functional test
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_execute_test_builder()` + `integrations_projects_locations_integrations_versions_test_cases_execute_test_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_execute_test_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_execute_test(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesExecuteTestArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteTestCaseResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_integrations_versions_test_cases_execute_test_builder(
            client,
            &args.testCaseName,
        )?;
    integrations_projects_locations_integrations_versions_test_cases_execute_test_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}
/// Get a test case
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_get_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_get` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}
/// Get a test case
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_test_cases_get_execute()` or `integrations_projects_locations_integrations_versions_test_cases_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestCase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}
/// Get a test case
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_test_cases_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_get_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_test_cases_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_test_cases_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_test_cases_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_test_cases_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsTestCasesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}
/// Get a test case
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_get_builder()` + `integrations_projects_locations_integrations_versions_test_cases_get_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_get_builder(
        client, &args.name,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases
/// Lists all the test cases that satisfy the filters.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_list_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_list` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases
/// Lists all the test cases that satisfy the filters.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_test_cases_list_execute()` or `integrations_projects_locations_integrations_versions_test_cases_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListTestCasesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListTestCasesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases
/// Lists all the test cases that satisfy the filters.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_test_cases_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_list_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_test_cases_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_test_cases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListTestCasesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_integrations_versions_test_cases_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_test_cases_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsTestCasesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases
/// Lists all the test cases that satisfy the filters.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_list_builder()` + `integrations_projects_locations_integrations_versions_test_cases_list_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListTestCasesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}
/// Updates a test case
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_patch_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_patch` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}
/// Updates a test case
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_test_cases_patch_execute()` or `integrations_projects_locations_integrations_versions_test_cases_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestCase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}
/// Updates a test case
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_test_cases_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_patch_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_test_cases_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_test_cases_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_integrations_versions_test_cases_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_test_cases_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsTestCasesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}
/// Updates a test case
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_patch_builder()` + `integrations_projects_locations_integrations_versions_test_cases_patch_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_patch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:takeoverEditLock
/// Clear the lock fields and assign them to current user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:takeoverEditLock",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:takeoverEditLock
/// Clear the lock fields and assign them to current user
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_execute()` or `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestCase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:takeoverEditLock
/// Clear the lock fields and assign them to current user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsTestCasesTakeoverEditLockArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases/{testCasesId}:takeoverEditLock
/// Clear the lock fields and assign them to current user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_builder()` + `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesTakeoverEditLockArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_builder(client, &args.name)?;
    integrations_projects_locations_integrations_versions_test_cases_takeover_edit_lock_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases:upload
/// Uploads a test case. The content can be a previously downloaded test case. Performs the same function as CreateTestCase, but accepts input in a string format, which holds the complete representation of the TestCase content.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_integrations_versions_test_cases_upload_execute()` to send, or `integrations_projects_locations_integrations_versions_test_cases_upload` for simplest API.

pub fn integrations_projects_locations_integrations_versions_test_cases_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases:upload",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases:upload
/// Uploads a test case. The content can be a previously downloaded test case. Performs the same function as CreateTestCase, but accepts input in a string format, which holds the complete representation of the TestCase content.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_integrations_versions_test_cases_upload_execute()` or `integrations_projects_locations_integrations_versions_test_cases_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUploadTestCaseResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaUploadTestCaseResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases:upload
/// Uploads a test case. The content can be a previously downloaded test case. Performs the same function as CreateTestCase, but accepts input in a string format, which holds the complete representation of the TestCase content.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_integrations_versions_test_cases_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_upload_task()`.
/// For the simplest API, use `integrations_projects_locations_integrations_versions_test_cases_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_integrations_versions_test_cases_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_integrations_versions_test_cases_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaUploadTestCaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_integrations_versions_test_cases_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_integrations_versions_test_cases_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsIntegrationsVersionsTestCasesUploadArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/integrations/{integrationsId}/versions/{versionsId}/testCases:upload
/// Uploads a test case. The content can be a previously downloaded test case. Performs the same function as CreateTestCase, but accepts input in a string format, which holds the complete representation of the TestCase content.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_integrations_versions_test_cases_upload_builder()` + `integrations_projects_locations_integrations_versions_test_cases_upload_execute()`.
/// For task-level control, use `integrations_projects_locations_integrations_versions_test_cases_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_integrations_versions_test_cases_upload(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesUploadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaUploadTestCaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_integrations_versions_test_cases_upload_builder(
        client,
        &args.parent,
    )?;
    integrations_projects_locations_integrations_versions_test_cases_upload_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_auth_configs_create_execute()` to send, or `integrations_projects_locations_products_auth_configs_create` for simplest API.

pub fn integrations_projects_locations_products_auth_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    clientCertificate_encryptedPrivateKey: &Option<Option<String>>,
    clientCertificate_passphrase: &Option<Option<String>>,
    clientCertificate_sslCertificate: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/authConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientCertificate_encryptedPrivateKey.as_ref() {
        query_parts.push(format!("clientCertificate.encryptedPrivateKey={}", val));
    }
    if let Some(val) = clientCertificate_passphrase.as_ref() {
        query_parts.push(format!("clientCertificate.passphrase={}", val));
    }
    if let Some(val) = clientCertificate_sslCertificate.as_ref() {
        query_parts.push(format!("clientCertificate.sslCertificate={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_auth_configs_create_execute()` or `integrations_projects_locations_products_auth_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_auth_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaAuthConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_auth_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_auth_configs_create_task()`.
/// For the simplest API, use `integrations_projects_locations_products_auth_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_auth_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_auth_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_auth_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_auth_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsAuthConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: clientCertificate_encryptedPrivateKey
    pub clientCertificate_encryptedPrivateKey: Option<Option<String>>,
    /// Query parameter: clientCertificate_passphrase
    pub clientCertificate_passphrase: Option<Option<String>>,
    /// Query parameter: clientCertificate_sslCertificate
    pub clientCertificate_sslCertificate: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs
/// Creates an auth config record. Fetch corresponding credentials for specific auth types, e.g. access token for OAuth 2.0, JWT token for JWT. Encrypt the auth config with Cloud KMS and store the encrypted credentials in Spanner. Returns the encrypted auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_auth_configs_create_builder()` + `integrations_projects_locations_products_auth_configs_create_execute()`.
/// For task-level control, use `integrations_projects_locations_products_auth_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsAuthConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_auth_configs_create_builder(
        client,
        &args.parent,
        &args.clientCertificate_encryptedPrivateKey,
        &args.clientCertificate_passphrase,
        &args.clientCertificate_sslCertificate,
    )?;
    integrations_projects_locations_products_auth_configs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}
/// Deletes an auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_auth_configs_delete_execute()` to send, or `integrations_projects_locations_products_auth_configs_delete` for simplest API.

pub fn integrations_projects_locations_products_auth_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}
/// Deletes an auth config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_auth_configs_delete_execute()` or `integrations_projects_locations_products_auth_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_auth_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}
/// Deletes an auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_auth_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_auth_configs_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_products_auth_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_auth_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_auth_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_auth_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_auth_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsAuthConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}
/// Deletes an auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_auth_configs_delete_builder()` + `integrations_projects_locations_products_auth_configs_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_products_auth_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsAuthConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_auth_configs_delete_builder(client, &args.name)?;
    integrations_projects_locations_products_auth_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}
/// Gets a complete auth config. If the auth config doesn''t exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_auth_configs_get_execute()` to send, or `integrations_projects_locations_products_auth_configs_get` for simplest API.

pub fn integrations_projects_locations_products_auth_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}
/// Gets a complete auth config. If the auth config doesn''t exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_auth_configs_get_execute()` or `integrations_projects_locations_products_auth_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_auth_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaAuthConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}
/// Gets a complete auth config. If the auth config doesn''t exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_auth_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_auth_configs_get_task()`.
/// For the simplest API, use `integrations_projects_locations_products_auth_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_auth_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_auth_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_auth_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_auth_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsAuthConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}
/// Gets a complete auth config. If the auth config doesn''t exist, Code.NOT_FOUND exception will be thrown. Returns the decrypted auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_auth_configs_get_builder()` + `integrations_projects_locations_products_auth_configs_get_execute()`.
/// For task-level control, use `integrations_projects_locations_products_auth_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsAuthConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_auth_configs_get_builder(client, &args.name)?;
    integrations_projects_locations_products_auth_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_auth_configs_list_execute()` to send, or `integrations_projects_locations_products_auth_configs_list` for simplest API.

pub fn integrations_projects_locations_products_auth_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/authConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_auth_configs_list_execute()` or `integrations_projects_locations_products_auth_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_auth_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListAuthConfigsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListAuthConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_auth_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_auth_configs_list_task()`.
/// For the simplest API, use `integrations_projects_locations_products_auth_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_auth_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_auth_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListAuthConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_auth_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_auth_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsAuthConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs
/// Lists all auth configs that match the filter. Restrict to auth configs belong to the current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_auth_configs_list_builder()` + `integrations_projects_locations_products_auth_configs_list_execute()`.
/// For task-level control, use `integrations_projects_locations_products_auth_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsAuthConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListAuthConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_auth_configs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
    )?;
    integrations_projects_locations_products_auth_configs_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_auth_configs_patch_execute()` to send, or `integrations_projects_locations_products_auth_configs_patch` for simplest API.

pub fn integrations_projects_locations_products_auth_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    clientCertificate_encryptedPrivateKey: &Option<Option<String>>,
    clientCertificate_passphrase: &Option<Option<String>>,
    clientCertificate_sslCertificate: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientCertificate_encryptedPrivateKey.as_ref() {
        query_parts.push(format!("clientCertificate.encryptedPrivateKey={}", val));
    }
    if let Some(val) = clientCertificate_passphrase.as_ref() {
        query_parts.push(format!("clientCertificate.passphrase={}", val));
    }
    if let Some(val) = clientCertificate_sslCertificate.as_ref() {
        query_parts.push(format!("clientCertificate.sslCertificate={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_auth_configs_patch_execute()` or `integrations_projects_locations_products_auth_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_auth_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaAuthConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_auth_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_auth_configs_patch_task()`.
/// For the simplest API, use `integrations_projects_locations_products_auth_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_auth_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_auth_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_auth_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_auth_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsAuthConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: clientCertificate_encryptedPrivateKey
    pub clientCertificate_encryptedPrivateKey: Option<Option<String>>,
    /// Query parameter: clientCertificate_passphrase
    pub clientCertificate_passphrase: Option<Option<String>>,
    /// Query parameter: clientCertificate_sslCertificate
    pub clientCertificate_sslCertificate: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/authConfigs/{authConfigsId}
/// Updates an auth config. If credential is updated, fetch the encrypted auth config from Spanner, decrypt with Cloud KMS key, update the credential fields, re-encrypt with Cloud KMS key and update the Spanner record. For other fields, directly update the Spanner record. Returns the encrypted auth config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_auth_configs_patch_builder()` + `integrations_projects_locations_products_auth_configs_patch_execute()`.
/// For task-level control, use `integrations_projects_locations_products_auth_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_auth_configs_patch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsAuthConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaAuthConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_auth_configs_patch_builder(
        client,
        &args.name,
        &args.clientCertificate_encryptedPrivateKey,
        &args.clientCertificate_passphrase,
        &args.clientCertificate_sslCertificate,
        &args.updateMask,
    )?;
    integrations_projects_locations_products_auth_configs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_certificates_create_execute()` to send, or `integrations_projects_locations_products_certificates_create` for simplest API.

pub fn integrations_projects_locations_products_certificates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/certificates",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_certificates_create_execute()` or `integrations_projects_locations_products_certificates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_certificates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_certificates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_certificates_create_task()`.
/// For the simplest API, use `integrations_projects_locations_products_certificates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_certificates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_certificates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_certificates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_certificates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsCertificatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates
/// Creates a new certificate. The certificate will be registered to the trawler service and will be encrypted using cloud KMS and stored in Spanner Returns the certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_certificates_create_builder()` + `integrations_projects_locations_products_certificates_create_execute()`.
/// For task-level control, use `integrations_projects_locations_products_certificates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsCertificatesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_certificates_create_builder(client, &args.parent)?;
    integrations_projects_locations_products_certificates_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}
/// Delete a certificate
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_certificates_delete_execute()` to send, or `integrations_projects_locations_products_certificates_delete` for simplest API.

pub fn integrations_projects_locations_products_certificates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}
/// Delete a certificate
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_certificates_delete_execute()` or `integrations_projects_locations_products_certificates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_certificates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}
/// Delete a certificate
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_certificates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_certificates_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_products_certificates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_certificates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_certificates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_certificates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_certificates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsCertificatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}
/// Delete a certificate
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_certificates_delete_builder()` + `integrations_projects_locations_products_certificates_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_products_certificates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsCertificatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_certificates_delete_builder(client, &args.name)?;
    integrations_projects_locations_products_certificates_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}
/// Get a certificates in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_certificates_get_execute()` to send, or `integrations_projects_locations_products_certificates_get` for simplest API.

pub fn integrations_projects_locations_products_certificates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}
/// Get a certificates in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_certificates_get_execute()` or `integrations_projects_locations_products_certificates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_certificates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}
/// Get a certificates in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_certificates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_certificates_get_task()`.
/// For the simplest API, use `integrations_projects_locations_products_certificates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_certificates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_certificates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_certificates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsCertificatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}
/// Get a certificates in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_certificates_get_builder()` + `integrations_projects_locations_products_certificates_get_execute()`.
/// For task-level control, use `integrations_projects_locations_products_certificates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsCertificatesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_certificates_get_builder(client, &args.name)?;
    integrations_projects_locations_products_certificates_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_certificates_list_execute()` to send, or `integrations_projects_locations_products_certificates_list` for simplest API.

pub fn integrations_projects_locations_products_certificates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/certificates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_certificates_list_execute()` or `integrations_projects_locations_products_certificates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_certificates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListCertificatesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListCertificatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_certificates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_certificates_list_task()`.
/// For the simplest API, use `integrations_projects_locations_products_certificates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_certificates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListCertificatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_certificates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_certificates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsCertificatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates
/// List all the certificates that match the filter. Restrict to certificate of current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_certificates_list_builder()` + `integrations_projects_locations_products_certificates_list_execute()`.
/// For task-level control, use `integrations_projects_locations_products_certificates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsCertificatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListCertificatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_certificates_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
    )?;
    integrations_projects_locations_products_certificates_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_certificates_patch_execute()` to send, or `integrations_projects_locations_products_certificates_patch` for simplest API.

pub fn integrations_projects_locations_products_certificates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_certificates_patch_execute()` or `integrations_projects_locations_products_certificates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_certificates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_certificates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_certificates_patch_task()`.
/// For the simplest API, use `integrations_projects_locations_products_certificates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_certificates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_certificates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_certificates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_certificates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsCertificatesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/certificates/{certificatesId}
/// Updates the certificate by id. If new certificate file is updated, it will register with the trawler service, re-encrypt with cloud KMS and update the Spanner record. Other fields will directly update the Spanner record. Returns the Certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_certificates_patch_builder()` + `integrations_projects_locations_products_certificates_patch_execute()`.
/// For task-level control, use `integrations_projects_locations_products_certificates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_certificates_patch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsCertificatesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaCertificate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_certificates_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    integrations_projects_locations_products_certificates_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/cloudFunctions
/// Creates a cloud function project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_cloud_functions_create_execute()` to send, or `integrations_projects_locations_products_cloud_functions_create` for simplest API.

pub fn integrations_projects_locations_products_cloud_functions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/cloudFunctions",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/cloudFunctions
/// Creates a cloud function project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_cloud_functions_create_execute()` or `integrations_projects_locations_products_cloud_functions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_cloud_functions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_cloud_functions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/cloudFunctions
/// Creates a cloud function project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_cloud_functions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_cloud_functions_create_task()`.
/// For the simplest API, use `integrations_projects_locations_products_cloud_functions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_cloud_functions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_cloud_functions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_cloud_functions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_cloud_functions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsCloudFunctionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/cloudFunctions
/// Creates a cloud function project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_cloud_functions_create_builder()` + `integrations_projects_locations_products_cloud_functions_create_execute()`.
/// For task-level control, use `integrations_projects_locations_products_cloud_functions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_cloud_functions_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsCloudFunctionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_cloud_functions_create_builder(
        client,
        &args.parent,
    )?;
    integrations_projects_locations_products_cloud_functions_create_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_execute_execute()` to send, or `integrations_projects_locations_products_integrations_execute` for simplest API.

pub fn integrations_projects_locations_products_integrations_execute_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:execute",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_execute_execute()` or `integrations_projects_locations_products_integrations_execute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_execute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_execute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_execute_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_execute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_integrations_execute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_execute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsExecuteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:execute
/// Executes integrations synchronously by passing the trigger id in the request body. The request is not returned until the requested executions are either fulfilled or experienced an error. If the integration name is not specified (passing -), all of the associated integration under the given trigger_id will be executed. Otherwise only the specified integration for the given trigger_id is executed. This is helpful for execution the integration from UI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_execute_builder()` + `integrations_projects_locations_products_integrations_execute_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_execute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_execute(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsExecuteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_execute_builder(client, &args.name)?;
    integrations_projects_locations_products_integrations_execute_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_list_execute()` to send, or `integrations_projects_locations_products_integrations_list` for simplest API.

pub fn integrations_projects_locations_products_integrations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_list_execute()` or `integrations_projects_locations_products_integrations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_list_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_integrations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations
/// Returns the list of all integrations in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_list_builder()` + `integrations_projects_locations_products_integrations_list_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    integrations_projects_locations_products_integrations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_schedule_execute()` to send, or `integrations_projects_locations_products_integrations_schedule` for simplest API.

pub fn integrations_projects_locations_products_integrations_schedule_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:schedule",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_schedule_execute()` or `integrations_projects_locations_products_integrations_schedule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_schedule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_schedule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_schedule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_schedule_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_schedule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_schedule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_schedule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_integrations_schedule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_schedule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsScheduleArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:schedule
/// Schedules an integration for execution by passing the trigger id and the scheduled time in the request body.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_schedule_builder()` + `integrations_projects_locations_products_integrations_schedule_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_schedule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_schedule(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsScheduleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_schedule_builder(client, &args.name)?;
    integrations_projects_locations_products_integrations_schedule_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:test
/// Execute the integration in draft state
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_test_execute()` to send, or `integrations_projects_locations_products_integrations_test` for simplest API.

pub fn integrations_projects_locations_products_integrations_test_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:test",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:test
/// Execute the integration in draft state
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_test_execute()` or `integrations_projects_locations_products_integrations_test`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_test_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTestIntegrationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:test
/// Execute the integration in draft state
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_test_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_test_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_test()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_integrations_test_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_test`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsTestArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}:test
/// Execute the integration in draft state
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_test_builder()` + `integrations_projects_locations_products_integrations_test_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_test_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_test(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsTestArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTestIntegrationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_test_builder(client, &args.name)?;
    integrations_projects_locations_products_integrations_test_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}:download
/// Download the execution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_executions_download_execute()` to send, or `integrations_projects_locations_products_integrations_executions_download` for simplest API.

pub fn integrations_projects_locations_products_integrations_executions_download_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}:download",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}:download
/// Download the execution.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_executions_download_execute()` or `integrations_projects_locations_products_integrations_executions_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_executions_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadExecutionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadExecutionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}:download
/// Download the execution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_executions_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_executions_download_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_executions_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_executions_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_executions_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_integrations_executions_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_executions_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsExecutionsDownloadArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}:download
/// Download the execution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_executions_download_builder()` + `integrations_projects_locations_products_integrations_executions_download_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_executions_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_download(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsExecutionsDownloadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadExecutionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_executions_download_builder(
            client, &args.name,
        )?;
    integrations_projects_locations_products_integrations_executions_download_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}
/// Get an execution in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_executions_get_execute()` to send, or `integrations_projects_locations_products_integrations_executions_get` for simplest API.

pub fn integrations_projects_locations_products_integrations_executions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}
/// Get an execution in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_executions_get_execute()` or `integrations_projects_locations_products_integrations_executions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_executions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecution>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaExecution =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}
/// Get an execution in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_executions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_executions_get_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_executions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_executions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_executions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_integrations_executions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_executions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsExecutionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}
/// Get an execution in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_executions_get_builder()` + `integrations_projects_locations_products_integrations_executions_get_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_executions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsExecutionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaExecution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_executions_get_builder(
        client, &args.name,
    )?;
    integrations_projects_locations_products_integrations_executions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_executions_list_execute()` to send, or `integrations_projects_locations_products_integrations_executions_list` for simplest API.

pub fn integrations_projects_locations_products_integrations_executions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    filterParams_customFilter: &Option<Option<String>>,
    filterParams_endTime: &Option<Option<String>>,
    filterParams_eventStatuses: &Option<Option<String>>,
    filterParams_executionId: &Option<Option<String>>,
    filterParams_parameterKey: &Option<Option<String>>,
    filterParams_parameterPairKey: &Option<Option<String>>,
    filterParams_parameterPairValue: &Option<Option<String>>,
    filterParams_parameterType: &Option<Option<String>>,
    filterParams_parameterValue: &Option<Option<String>>,
    filterParams_startTime: &Option<Option<String>>,
    filterParams_taskStatuses: &Option<Option<String>>,
    filterParams_workflowName: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
    refreshAcl: &Option<Option<String>>,
    snapshotMetadataWithoutParams: &Option<Option<String>>,
    truncateParams: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = filterParams_customFilter.as_ref() {
        query_parts.push(format!("filterParams.customFilter={}", val));
    }
    if let Some(val) = filterParams_endTime.as_ref() {
        query_parts.push(format!("filterParams.endTime={}", val));
    }
    if let Some(val) = filterParams_eventStatuses.as_ref() {
        query_parts.push(format!("filterParams.eventStatuses={}", val));
    }
    if let Some(val) = filterParams_executionId.as_ref() {
        query_parts.push(format!("filterParams.executionId={}", val));
    }
    if let Some(val) = filterParams_parameterKey.as_ref() {
        query_parts.push(format!("filterParams.parameterKey={}", val));
    }
    if let Some(val) = filterParams_parameterPairKey.as_ref() {
        query_parts.push(format!("filterParams.parameterPairKey={}", val));
    }
    if let Some(val) = filterParams_parameterPairValue.as_ref() {
        query_parts.push(format!("filterParams.parameterPairValue={}", val));
    }
    if let Some(val) = filterParams_parameterType.as_ref() {
        query_parts.push(format!("filterParams.parameterType={}", val));
    }
    if let Some(val) = filterParams_parameterValue.as_ref() {
        query_parts.push(format!("filterParams.parameterValue={}", val));
    }
    if let Some(val) = filterParams_startTime.as_ref() {
        query_parts.push(format!("filterParams.startTime={}", val));
    }
    if let Some(val) = filterParams_taskStatuses.as_ref() {
        query_parts.push(format!("filterParams.taskStatuses={}", val));
    }
    if let Some(val) = filterParams_workflowName.as_ref() {
        query_parts.push(format!("filterParams.workflowName={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }
    if let Some(val) = refreshAcl.as_ref() {
        query_parts.push(format!("refreshAcl={}", val));
    }
    if let Some(val) = snapshotMetadataWithoutParams.as_ref() {
        query_parts.push(format!("snapshotMetadataWithoutParams={}", val));
    }
    if let Some(val) = truncateParams.as_ref() {
        query_parts.push(format!("truncateParams={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_executions_list_execute()` or `integrations_projects_locations_products_integrations_executions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_executions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListExecutionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListExecutionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_executions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_executions_list_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_executions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_executions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_executions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_integrations_executions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_executions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsExecutionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: filterParams_customFilter
    pub filterParams_customFilter: Option<Option<String>>,
    /// Query parameter: filterParams_endTime
    pub filterParams_endTime: Option<Option<String>>,
    /// Query parameter: filterParams_eventStatuses
    pub filterParams_eventStatuses: Option<Option<String>>,
    /// Query parameter: filterParams_executionId
    pub filterParams_executionId: Option<Option<String>>,
    /// Query parameter: filterParams_parameterKey
    pub filterParams_parameterKey: Option<Option<String>>,
    /// Query parameter: filterParams_parameterPairKey
    pub filterParams_parameterPairKey: Option<Option<String>>,
    /// Query parameter: filterParams_parameterPairValue
    pub filterParams_parameterPairValue: Option<Option<String>>,
    /// Query parameter: filterParams_parameterType
    pub filterParams_parameterType: Option<Option<String>>,
    /// Query parameter: filterParams_parameterValue
    pub filterParams_parameterValue: Option<Option<String>>,
    /// Query parameter: filterParams_startTime
    pub filterParams_startTime: Option<Option<String>>,
    /// Query parameter: filterParams_taskStatuses
    pub filterParams_taskStatuses: Option<Option<String>>,
    /// Query parameter: filterParams_workflowName
    pub filterParams_workflowName: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
    /// Query parameter: refreshAcl
    pub refreshAcl: Option<Option<String>>,
    /// Query parameter: snapshotMetadataWithoutParams
    pub snapshotMetadataWithoutParams: Option<Option<String>>,
    /// Query parameter: truncateParams
    pub truncateParams: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions
/// Lists the results of all the integration executions. The response includes the same information as the [execution log](<https://cloud.google.`com/application-integration/docs/viewing-logs`>) in the Integration UI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_executions_list_builder()` + `integrations_projects_locations_products_integrations_executions_list_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_executions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsExecutionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_executions_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.filterParams_customFilter,
        &args.filterParams_endTime,
        &args.filterParams_eventStatuses,
        &args.filterParams_executionId,
        &args.filterParams_parameterKey,
        &args.filterParams_parameterPairKey,
        &args.filterParams_parameterPairValue,
        &args.filterParams_parameterType,
        &args.filterParams_parameterValue,
        &args.filterParams_startTime,
        &args.filterParams_taskStatuses,
        &args.filterParams_workflowName,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
        &args.refreshAcl,
        &args.snapshotMetadataWithoutParams,
        &args.truncateParams,
    )?;
    integrations_projects_locations_products_integrations_executions_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_executions_suspensions_lift_execute()` to send, or `integrations_projects_locations_products_integrations_executions_suspensions_lift` for simplest API.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_lift_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:lift",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_executions_suspensions_lift_execute()` or `integrations_projects_locations_products_integrations_executions_suspensions_lift`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_executions_suspensions_lift_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_lift_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaLiftSuspensionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaLiftSuspensionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_executions_suspensions_lift_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_executions_suspensions_lift_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_executions_suspensions_lift()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_executions_suspensions_lift_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_lift_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaLiftSuspensionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_integrations_executions_suspensions_lift_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_executions_suspensions_lift`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsLiftArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:lift
/// * Lifts suspension for the Suspension task. Fetch corresponding suspension with provided suspension Id, resolve suspension, and set up suspension result for the Suspension Task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_executions_suspensions_lift_builder()` + `integrations_projects_locations_products_integrations_executions_suspensions_lift_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_executions_suspensions_lift_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_lift(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsLiftArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaLiftSuspensionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_executions_suspensions_lift_builder(
            client, &args.name,
        )?;
    integrations_projects_locations_products_integrations_executions_suspensions_lift_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_executions_suspensions_list_execute()` to send, or `integrations_projects_locations_products_integrations_executions_suspensions_list` for simplest API.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_executions_suspensions_list_execute()` or `integrations_projects_locations_products_integrations_executions_suspensions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_executions_suspensions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSuspensionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListSuspensionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_executions_suspensions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_executions_suspensions_list_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_executions_suspensions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_executions_suspensions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSuspensionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_integrations_executions_suspensions_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_executions_suspensions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions
/// * Lists suspensions associated with a specific execution. Only those with permissions to resolve the relevant suspensions will be able to view them.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_executions_suspensions_list_builder()` + `integrations_projects_locations_products_integrations_executions_suspensions_list_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_executions_suspensions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSuspensionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_executions_suspensions_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )?;
    integrations_projects_locations_products_integrations_executions_suspensions_list_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_executions_suspensions_resolve_execute()` to send, or `integrations_projects_locations_products_integrations_executions_suspensions_resolve` for simplest API.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_resolve_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:resolve",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_executions_suspensions_resolve_execute()` or `integrations_projects_locations_products_integrations_executions_suspensions_resolve`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_executions_suspensions_resolve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_resolve_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaResolveSuspensionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaResolveSuspensionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_executions_suspensions_resolve_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_executions_suspensions_resolve_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_executions_suspensions_resolve()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_executions_suspensions_resolve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_resolve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaResolveSuspensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_integrations_executions_suspensions_resolve_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_executions_suspensions_resolve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsResolveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/executions/{executionsId}/suspensions/{suspensionsId}:resolve
/// * Resolves (`lifts/rejects`) any number of suspensions. If the integration is already running, only the status of the suspension is updated. Otherwise, the suspended integration will begin execution again.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_executions_suspensions_resolve_builder()` + `integrations_projects_locations_products_integrations_executions_suspensions_resolve_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_executions_suspensions_resolve_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_executions_suspensions_resolve(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsResolveArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaResolveSuspensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_executions_suspensions_resolve_builder(client, &args.name)?;
    integrations_projects_locations_products_integrations_executions_suspensions_resolve_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions
/// Create a integration with a draft version in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_create_execute()` to send, or `integrations_projects_locations_products_integrations_versions_create` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    createSampleIntegrations: &Option<Option<String>>,
    newIntegration: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = createSampleIntegrations.as_ref() {
        query_parts.push(format!("createSampleIntegrations={}", val));
    }
    if let Some(val) = newIntegration.as_ref() {
        query_parts.push(format!("newIntegration={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions
/// Create a integration with a draft version in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_versions_create_execute()` or `integrations_projects_locations_products_integrations_versions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaIntegrationVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions
/// Create a integration with a draft version in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_versions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_create_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_versions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_integrations_versions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_versions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsVersionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: createSampleIntegrations
    pub createSampleIntegrations: Option<Option<String>>,
    /// Query parameter: newIntegration
    pub newIntegration: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions
/// Create a integration with a draft version in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_create_builder()` + `integrations_projects_locations_products_integrations_versions_create_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsVersionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_create_builder(
        client,
        &args.parent,
        &args.createSampleIntegrations,
        &args.newIntegration,
    )?;
    integrations_projects_locations_products_integrations_versions_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_delete_execute()` to send, or `integrations_projects_locations_products_integrations_versions_delete` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_versions_delete_execute()` or `integrations_projects_locations_products_integrations_versions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_versions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_versions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_integrations_versions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}
/// Soft-deletes the integration. Changes the status of the integration to ARCHIVED. If the integration being ARCHIVED is tagged as "HEAD", the tag is removed from this snapshot and set to the previous non-ARCHIVED snapshot. The PUBLISH_REQUESTED, DUE_FOR_DELETION tags are removed too. This RPC throws an exception if the version being deleted is DRAFT, and if the locked_by user is not the same as the user performing the Delete. Audit fields updated include last_modified_timestamp, last_modified_by. Any existing lock is released when Deleting a integration. Currently, there is no undelete mechanism.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_delete_builder()` + `integrations_projects_locations_products_integrations_versions_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_delete_builder(
        client, &args.name,
    )?;
    integrations_projects_locations_products_integrations_versions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_download_execute()` to send, or `integrations_projects_locations_products_integrations_versions_download` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_download_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    fileFormat: &Option<Option<String>>,
    files: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:download",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fileFormat.as_ref() {
        query_parts.push(format!("fileFormat={}", val));
    }
    if let Some(val) = files.as_ref() {
        query_parts.push(format!("files={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_versions_download_execute()` or `integrations_projects_locations_products_integrations_versions_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_versions_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_download_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_versions_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_versions_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_integrations_versions_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_versions_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsVersionsDownloadArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: fileFormat
    pub fileFormat: Option<Option<String>>,
    /// Query parameter: files
    pub files: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:download
/// Downloads an integration. Retrieves the IntegrationVersion for a given integration_id and returns the response as a string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_download_builder()` + `integrations_projects_locations_products_integrations_versions_download_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_download(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsVersionsDownloadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_download_builder(
        client,
        &args.name,
        &args.fileFormat,
        &args.files,
    )?;
    integrations_projects_locations_products_integrations_versions_download_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}
/// Get a integration in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_get_execute()` to send, or `integrations_projects_locations_products_integrations_versions_get` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}
/// Get a integration in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_versions_get_execute()` or `integrations_projects_locations_products_integrations_versions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaIntegrationVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}
/// Get a integration in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_versions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_get_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_versions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_integrations_versions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}
/// Get a integration in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_get_builder()` + `integrations_projects_locations_products_integrations_versions_get_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsVersionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_get_builder(
        client, &args.name,
    )?;
    integrations_projects_locations_products_integrations_versions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_list_execute()` to send, or `integrations_projects_locations_products_integrations_versions_list` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    fieldMask: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fieldMask.as_ref() {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_versions_list_execute()` or `integrations_projects_locations_products_integrations_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_list_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_integrations_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: fieldMask
    pub fieldMask: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions
/// Returns the list of all integration versions in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_list_builder()` + `integrations_projects_locations_products_integrations_versions_list_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_list_builder(
        client,
        &args.parent,
        &args.fieldMask,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    integrations_projects_locations_products_integrations_versions_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}
/// Update a integration with a draft version in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_patch_execute()` to send, or `integrations_projects_locations_products_integrations_versions_patch` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}
/// Update a integration with a draft version in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_versions_patch_execute()` or `integrations_projects_locations_products_integrations_versions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaIntegrationVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}
/// Update a integration with a draft version in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_versions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_patch_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_versions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_integrations_versions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_versions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsVersionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}
/// Update a integration with a draft version in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_patch_builder()` + `integrations_projects_locations_products_integrations_versions_patch_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_patch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsVersionsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaIntegrationVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    integrations_projects_locations_products_integrations_versions_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_publish_execute()` to send, or `integrations_projects_locations_products_integrations_versions_publish` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_publish_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:publish",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_versions_publish_execute()` or `integrations_projects_locations_products_integrations_versions_publish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_publish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_versions_publish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_publish_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_versions_publish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_versions_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_integrations_versions_publish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_versions_publish`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsVersionsPublishArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:publish
/// This RPC throws an exception if the integration is in ARCHIVED or `ACTIVE` state. This RPC throws an exception if the version being published is DRAFT, and if the locked_by user is not the same as the user performing the Publish. Audit fields updated include last_published_timestamp, last_published_by, last_modified_timestamp, last_modified_by. Any existing lock is on this integration is released.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_publish_builder()` + `integrations_projects_locations_products_integrations_versions_publish_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_publish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_publish(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsVersionsPublishArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_publish_builder(
        client, &args.name,
    )?;
    integrations_projects_locations_products_integrations_versions_publish_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:takeoverEditLock
/// Clears the locked_by and locked_at_timestampin the DRAFT version of this integration. It then performs the same action as the CreateDraftIntegrationVersion (i.e., copies the DRAFT version of the integration as a SNAPSHOT and then creates a new DRAFT version with the locked_by set to the user_taking_over and the locked_at_timestamp set to the current timestamp). Both the locked_by and user_taking_over are notified via email about the takeover. This RPC throws an exception if the integration is not in DRAFT status or if the locked_by and locked_at_timestamp fields are not set.The TakeoverEdit lock is treated the same as an edit of the integration, and hence shares ACLs with edit. Audit fields updated include last_modified_timestamp, last_modified_by.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_execute()` to send, or `integrations_projects_locations_products_integrations_versions_takeover_edit_lock` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_takeover_edit_lock_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    integrationVersion: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:takeoverEditLock",
        integrationVersion,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:takeoverEditLock
/// Clears the locked_by and locked_at_timestampin the DRAFT version of this integration. It then performs the same action as the CreateDraftIntegrationVersion (i.e., copies the DRAFT version of the integration as a SNAPSHOT and then creates a new DRAFT version with the locked_by set to the user_taking_over and the locked_at_timestamp set to the current timestamp). Both the locked_by and user_taking_over are notified via email about the takeover. This RPC throws an exception if the integration is not in DRAFT status or if the locked_by and locked_at_timestamp fields are not set.The TakeoverEdit lock is treated the same as an edit of the integration, and hence shares ACLs with edit. Audit fields updated include last_modified_timestamp, last_modified_by.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_execute()` or `integrations_projects_locations_products_integrations_versions_takeover_edit_lock`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_takeover_edit_lock_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTakeoverEditLockResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTakeoverEditLockResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:takeoverEditLock
/// Clears the locked_by and locked_at_timestampin the DRAFT version of this integration. It then performs the same action as the CreateDraftIntegrationVersion (i.e., copies the DRAFT version of the integration as a SNAPSHOT and then creates a new DRAFT version with the locked_by set to the user_taking_over and the locked_at_timestamp set to the current timestamp). Both the locked_by and user_taking_over are notified via email about the takeover. This RPC throws an exception if the integration is not in DRAFT status or if the locked_by and locked_at_timestamp fields are not set.The TakeoverEdit lock is treated the same as an edit of the integration, and hence shares ACLs with edit. Audit fields updated include last_modified_timestamp, last_modified_by.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_versions_takeover_edit_lock()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_versions_takeover_edit_lock_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTakeoverEditLockResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_integrations_versions_takeover_edit_lock_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_versions_takeover_edit_lock`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsVersionsTakeoverEditLockArgs {
    /// Path parameter: integrationVersion
    pub integrationVersion: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:takeoverEditLock
/// Clears the locked_by and locked_at_timestampin the DRAFT version of this integration. It then performs the same action as the CreateDraftIntegrationVersion (i.e., copies the DRAFT version of the integration as a SNAPSHOT and then creates a new DRAFT version with the locked_by set to the user_taking_over and the locked_at_timestamp set to the current timestamp). Both the locked_by and user_taking_over are notified via email about the takeover. This RPC throws an exception if the integration is not in DRAFT status or if the locked_by and locked_at_timestamp fields are not set.The TakeoverEdit lock is treated the same as an edit of the integration, and hence shares ACLs with edit. Audit fields updated include last_modified_timestamp, last_modified_by.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_builder()` + `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_takeover_edit_lock_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_takeover_edit_lock(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsVersionsTakeoverEditLockArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaTakeoverEditLockResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_integrations_versions_takeover_edit_lock_builder(
            client,
            &args.integrationVersion,
        )?;
    integrations_projects_locations_products_integrations_versions_takeover_edit_lock_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_unpublish_execute()` to send, or `integrations_projects_locations_products_integrations_versions_unpublish` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_unpublish_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:unpublish",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_versions_unpublish_execute()` or `integrations_projects_locations_products_integrations_versions_unpublish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_unpublish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_unpublish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_versions_unpublish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_unpublish_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_versions_unpublish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_unpublish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_versions_unpublish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_integrations_versions_unpublish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_versions_unpublish`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsVersionsUnpublishArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions/{versionsId}:unpublish
/// Sets the status of the `ACTIVE` integration to SNAPSHOT with a new tag "PREVIOUSLY_PUBLISHED" after validating it. The "HEAD" and "PUBLISH_REQUESTED" tags do not change. This RPC throws an exception if the version being snapshot is not `ACTIVE`. Audit fields added include action, action_by, action_timestamp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_unpublish_builder()` + `integrations_projects_locations_products_integrations_versions_unpublish_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_unpublish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_unpublish(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsVersionsUnpublishArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_unpublish_builder(
        client, &args.name,
    )?;
    integrations_projects_locations_products_integrations_versions_unpublish_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_integrations_versions_upload_execute()` to send, or `integrations_projects_locations_products_integrations_versions_upload` for simplest API.

pub fn integrations_projects_locations_products_integrations_versions_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions:upload",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_integrations_versions_upload_execute()` or `integrations_projects_locations_products_integrations_versions_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_integrations_versions_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_upload_task()`.
/// For the simplest API, use `integrations_projects_locations_products_integrations_versions_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_integrations_versions_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_integrations_versions_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_integrations_versions_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_integrations_versions_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsIntegrationsVersionsUploadArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/integrations/{integrationsId}/versions:upload
/// Uploads an integration. The content can be a previously downloaded integration. Performs the same function as CreateDraftIntegrationVersion, but accepts input in a string format, which holds the complete representation of the IntegrationVersion content.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_integrations_versions_upload_builder()` + `integrations_projects_locations_products_integrations_versions_upload_execute()`.
/// For task-level control, use `integrations_projects_locations_products_integrations_versions_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_integrations_versions_upload(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsIntegrationsVersionsUploadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_integrations_versions_upload_builder(
        client,
        &args.parent,
    )?;
    integrations_projects_locations_products_integrations_versions_upload_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_create_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_create` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/sfdcInstances",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_sfdc_instances_create_execute()` or `integrations_projects_locations_products_sfdc_instances_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcInstance =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_sfdc_instances_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_create_task()`.
/// For the simplest API, use `integrations_projects_locations_products_sfdc_instances_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_sfdc_instances_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_sfdc_instances_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_sfdc_instances_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsSfdcInstancesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_create_builder()` + `integrations_projects_locations_products_sfdc_instances_create_execute()`.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsSfdcInstancesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_sfdc_instances_create_builder(
        client,
        &args.parent,
    )?;
    integrations_projects_locations_products_sfdc_instances_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}
/// Deletes an sfdc instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_delete_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_delete` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}
/// Deletes an sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_sfdc_instances_delete_execute()` or `integrations_projects_locations_products_sfdc_instances_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}
/// Deletes an sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_sfdc_instances_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_products_sfdc_instances_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_sfdc_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_sfdc_instances_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_sfdc_instances_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsSfdcInstancesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}
/// Deletes an sfdc instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_delete_builder()` + `integrations_projects_locations_products_sfdc_instances_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsSfdcInstancesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_delete_builder(client, &args.name)?;
    integrations_projects_locations_products_sfdc_instances_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}
/// Gets an sfdc instance. If the instance doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_get_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_get` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}
/// Gets an sfdc instance. If the instance doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_sfdc_instances_get_execute()` or `integrations_projects_locations_products_sfdc_instances_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcInstance =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}
/// Gets an sfdc instance. If the instance doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_sfdc_instances_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_get_task()`.
/// For the simplest API, use `integrations_projects_locations_products_sfdc_instances_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_sfdc_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_sfdc_instances_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_sfdc_instances_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsSfdcInstancesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}
/// Gets an sfdc instance. If the instance doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_get_builder()` + `integrations_projects_locations_products_sfdc_instances_get_execute()`.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsSfdcInstancesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_get_builder(client, &args.name)?;
    integrations_projects_locations_products_sfdc_instances_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_list_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_list` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/sfdcInstances",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_sfdc_instances_list_execute()` or `integrations_projects_locations_products_sfdc_instances_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_sfdc_instances_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_list_task()`.
/// For the simplest API, use `integrations_projects_locations_products_sfdc_instances_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_sfdc_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_sfdc_instances_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_sfdc_instances_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsSfdcInstancesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_list_builder()` + `integrations_projects_locations_products_sfdc_instances_list_execute()`.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsSfdcInstancesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_sfdc_instances_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
    )?;
    integrations_projects_locations_products_sfdc_instances_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_patch_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_patch` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_sfdc_instances_patch_execute()` or `integrations_projects_locations_products_sfdc_instances_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcInstance =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_sfdc_instances_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_patch_task()`.
/// For the simplest API, use `integrations_projects_locations_products_sfdc_instances_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_sfdc_instances_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_products_sfdc_instances_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_sfdc_instances_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsSfdcInstancesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_patch_builder()` + `integrations_projects_locations_products_sfdc_instances_patch_execute()`.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_patch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsSfdcInstancesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_products_sfdc_instances_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    integrations_projects_locations_products_sfdc_instances_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_execute()` or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_task()`.
/// For the simplest API, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_sfdc_instances_sfdc_channels_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_builder()` + `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_execute()`.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_builder(
            client,
            &args.parent,
        )?;
    integrations_projects_locations_products_sfdc_instances_sfdc_channels_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Deletes an sfdc channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Deletes an sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_execute()` or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Deletes an sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Deletes an sfdc channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_builder()` + `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_builder(
            client, &args.name,
        )?;
    integrations_projects_locations_products_sfdc_instances_sfdc_channels_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Gets an sfdc channel. If the channel doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Gets an sfdc channel. If the channel doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_execute()` or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Gets an sfdc channel. If the channel doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_task()`.
/// For the simplest API, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_sfdc_instances_sfdc_channels_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Gets an sfdc channel. If the channel doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_builder()` + `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_execute()`.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_builder(
            client, &args.name,
        )?;
    integrations_projects_locations_products_sfdc_instances_sfdc_channels_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_execute()` or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_task()`.
/// For the simplest API, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_sfdc_instances_sfdc_channels_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_builder()` + `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_execute()`.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
            &args.readMask,
        )?;
    integrations_projects_locations_products_sfdc_instances_sfdc_channels_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_execute()` to send, or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch` for simplest API.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_execute()` or `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_task()`.
/// For the simplest API, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/products/{productsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_builder()` + `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_execute()`.
/// For task-level control, use `integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_builder(
            client,
            &args.name,
            &args.updateMask,
        )?;
    integrations_projects_locations_products_sfdc_instances_sfdc_channels_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_create_execute()` to send, or `integrations_projects_locations_sfdc_instances_create` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/sfdcInstances",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_sfdc_instances_create_execute()` or `integrations_projects_locations_sfdc_instances_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcInstance =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_sfdc_instances_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_create_task()`.
/// For the simplest API, use `integrations_projects_locations_sfdc_instances_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_sfdc_instances_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_sfdc_instances_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_sfdc_instances_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsSfdcInstancesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances
/// Creates an sfdc instance record. Store the sfdc instance in Spanner. Returns the sfdc instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_create_builder()` + `integrations_projects_locations_sfdc_instances_create_execute()`.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsSfdcInstancesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_sfdc_instances_create_builder(client, &args.parent)?;
    integrations_projects_locations_sfdc_instances_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}
/// Deletes an sfdc instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_delete_execute()` to send, or `integrations_projects_locations_sfdc_instances_delete` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}
/// Deletes an sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_sfdc_instances_delete_execute()` or `integrations_projects_locations_sfdc_instances_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}
/// Deletes an sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_sfdc_instances_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_sfdc_instances_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_sfdc_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_sfdc_instances_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_sfdc_instances_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsSfdcInstancesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}
/// Deletes an sfdc instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_delete_builder()` + `integrations_projects_locations_sfdc_instances_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsSfdcInstancesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        integrations_projects_locations_sfdc_instances_delete_builder(client, &args.name)?;
    integrations_projects_locations_sfdc_instances_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}
/// Gets an sfdc instance. If the instance doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_get_execute()` to send, or `integrations_projects_locations_sfdc_instances_get` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}
/// Gets an sfdc instance. If the instance doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_sfdc_instances_get_execute()` or `integrations_projects_locations_sfdc_instances_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcInstance =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}
/// Gets an sfdc instance. If the instance doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_sfdc_instances_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_get_task()`.
/// For the simplest API, use `integrations_projects_locations_sfdc_instances_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_sfdc_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_sfdc_instances_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_sfdc_instances_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsSfdcInstancesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}
/// Gets an sfdc instance. If the instance doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_get_builder()` + `integrations_projects_locations_sfdc_instances_get_execute()`.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsSfdcInstancesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_get_builder(client, &args.name)?;
    integrations_projects_locations_sfdc_instances_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_list_execute()` to send, or `integrations_projects_locations_sfdc_instances_list` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/sfdcInstances",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_sfdc_instances_list_execute()` or `integrations_projects_locations_sfdc_instances_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_sfdc_instances_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_list_task()`.
/// For the simplest API, use `integrations_projects_locations_sfdc_instances_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_sfdc_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_sfdc_instances_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_sfdc_instances_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsSfdcInstancesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances
/// Lists all sfdc instances that match the filter. Restrict to sfdc instances belonging to the current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_list_builder()` + `integrations_projects_locations_sfdc_instances_list_execute()`.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsSfdcInstancesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
    )?;
    integrations_projects_locations_sfdc_instances_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_patch_execute()` to send, or `integrations_projects_locations_sfdc_instances_patch` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_sfdc_instances_patch_execute()` or `integrations_projects_locations_sfdc_instances_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcInstance =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_sfdc_instances_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_patch_task()`.
/// For the simplest API, use `integrations_projects_locations_sfdc_instances_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_sfdc_instances_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_sfdc_instances_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_sfdc_instances_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsSfdcInstancesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}
/// Updates an sfdc instance. Updates the sfdc instance in spanner. Returns the sfdc instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_patch_builder()` + `integrations_projects_locations_sfdc_instances_patch_execute()`.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_patch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsSfdcInstancesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcInstance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    integrations_projects_locations_sfdc_instances_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_sfdc_channels_create_execute()` to send, or `integrations_projects_locations_sfdc_instances_sfdc_channels_create` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_sfdc_instances_sfdc_channels_create_execute()` or `integrations_projects_locations_sfdc_instances_sfdc_channels_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_sfdc_channels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_sfdc_channels_create_task()`.
/// For the simplest API, use `integrations_projects_locations_sfdc_instances_sfdc_channels_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_sfdc_channels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_sfdc_instances_sfdc_channels_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_sfdc_instances_sfdc_channels_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Creates an sfdc channel record. Store the sfdc channel in Spanner. Returns the sfdc channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_create_builder()` + `integrations_projects_locations_sfdc_instances_sfdc_channels_create_execute()`.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_sfdc_channels_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_sfdc_channels_create_builder(
        client,
        &args.parent,
    )?;
    integrations_projects_locations_sfdc_instances_sfdc_channels_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Deletes an sfdc channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_execute()` to send, or `integrations_projects_locations_sfdc_instances_sfdc_channels_delete` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Deletes an sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_execute()` or `integrations_projects_locations_sfdc_instances_sfdc_channels_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Deletes an sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_sfdc_instances_sfdc_channels_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_sfdc_instances_sfdc_channels_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_sfdc_instances_sfdc_channels_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Deletes an sfdc channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_builder()` + `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_sfdc_channels_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_sfdc_channels_delete_builder(
        client, &args.name,
    )?;
    integrations_projects_locations_sfdc_instances_sfdc_channels_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Gets an sfdc channel. If the channel doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_sfdc_channels_get_execute()` to send, or `integrations_projects_locations_sfdc_instances_sfdc_channels_get` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Gets an sfdc channel. If the channel doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_sfdc_instances_sfdc_channels_get_execute()` or `integrations_projects_locations_sfdc_instances_sfdc_channels_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_sfdc_channels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Gets an sfdc channel. If the channel doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_sfdc_channels_get_task()`.
/// For the simplest API, use `integrations_projects_locations_sfdc_instances_sfdc_channels_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_sfdc_channels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_sfdc_instances_sfdc_channels_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_sfdc_instances_sfdc_channels_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Gets an sfdc channel. If the channel doesn''t exist, Code.NOT_FOUND exception will be thrown.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_get_builder()` + `integrations_projects_locations_sfdc_instances_sfdc_channels_get_execute()`.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_sfdc_channels_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_sfdc_channels_get_builder(
        client, &args.name,
    )?;
    integrations_projects_locations_sfdc_instances_sfdc_channels_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_sfdc_channels_list_execute()` to send, or `integrations_projects_locations_sfdc_instances_sfdc_channels_list` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_sfdc_instances_sfdc_channels_list_execute()` or `integrations_projects_locations_sfdc_instances_sfdc_channels_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_sfdc_channels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_sfdc_channels_list_task()`.
/// For the simplest API, use `integrations_projects_locations_sfdc_instances_sfdc_channels_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_sfdc_channels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_sfdc_instances_sfdc_channels_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_sfdc_instances_sfdc_channels_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels
/// Lists all sfdc channels that match the filter. Restrict to sfdc channels belonging to the current client only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_list_builder()` + `integrations_projects_locations_sfdc_instances_sfdc_channels_list_execute()`.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_sfdc_channels_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_sfdc_channels_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
    )?;
    integrations_projects_locations_sfdc_instances_sfdc_channels_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_execute()` to send, or `integrations_projects_locations_sfdc_instances_sfdc_channels_patch` for simplest API.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_execute()` or `integrations_projects_locations_sfdc_instances_sfdc_channels_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSfdcChannel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_task()`.
/// For the simplest API, use `integrations_projects_locations_sfdc_instances_sfdc_channels_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_sfdc_instances_sfdc_channels_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_sfdc_instances_sfdc_channels_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sfdcInstances/{sfdcInstancesId}/sfdcChannels/{sfdcChannelsId}
/// Updates an sfdc channel. Updates the sfdc channel in spanner. Returns the sfdc channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_builder()` + `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_execute()`.
/// For task-level control, use `integrations_projects_locations_sfdc_instances_sfdc_channels_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_sfdc_instances_sfdc_channels_patch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaSfdcChannel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_sfdc_instances_sfdc_channels_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    integrations_projects_locations_sfdc_instances_sfdc_channels_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates
/// Creates a new template
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_create_execute()` to send, or `integrations_projects_locations_templates_create` for simplest API.

pub fn integrations_projects_locations_templates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/templates",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates
/// Creates a new template
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_templates_create_execute()` or `integrations_projects_locations_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates
/// Creates a new template
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_templates_create_task()`.
/// For the simplest API, use `integrations_projects_locations_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates
/// Creates a new template
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_create_builder()` + `integrations_projects_locations_templates_create_execute()`.
/// For task-level control, use `integrations_projects_locations_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_create(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_create_builder(client, &args.parent)?;
    integrations_projects_locations_templates_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}
/// Deletes a template
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_delete_execute()` to send, or `integrations_projects_locations_templates_delete` for simplest API.

pub fn integrations_projects_locations_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/templates/{templatesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}
/// Deletes a template
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_templates_delete_execute()` or `integrations_projects_locations_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}
/// Deletes a template
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_templates_delete_task()`.
/// For the simplest API, use `integrations_projects_locations_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}
/// Deletes a template
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_delete_builder()` + `integrations_projects_locations_templates_delete_execute()`.
/// For task-level control, use `integrations_projects_locations_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_delete(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_delete_builder(client, &args.name)?;
    integrations_projects_locations_templates_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:download
/// Downloads a template. Retrieves the Template and returns the response as a string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_download_execute()` to send, or `integrations_projects_locations_templates_download` for simplest API.

pub fn integrations_projects_locations_templates_download_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    fileFormat: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/templates/{templatesId}:download",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fileFormat.as_ref() {
        query_parts.push(format!("fileFormat={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:download
/// Downloads a template. Retrieves the Template and returns the response as a string.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_templates_download_execute()` or `integrations_projects_locations_templates_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadTemplateResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaDownloadTemplateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:download
/// Downloads a template. Retrieves the Template and returns the response as a string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_templates_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_templates_download_task()`.
/// For the simplest API, use `integrations_projects_locations_templates_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_templates_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadTemplateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_templates_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_templates_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsTemplatesDownloadArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: fileFormat
    pub fileFormat: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:download
/// Downloads a template. Retrieves the Template and returns the response as a string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_download_builder()` + `integrations_projects_locations_templates_download_execute()`.
/// For task-level control, use `integrations_projects_locations_templates_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_download(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsTemplatesDownloadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaDownloadTemplateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_download_builder(
        client,
        &args.name,
        &args.fileFormat,
    )?;
    integrations_projects_locations_templates_download_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}
/// Get a template in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_get_execute()` to send, or `integrations_projects_locations_templates_get` for simplest API.

pub fn integrations_projects_locations_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/templates/{templatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}
/// Get a template in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_templates_get_execute()` or `integrations_projects_locations_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}
/// Get a template in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_templates_get_task()`.
/// For the simplest API, use `integrations_projects_locations_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}
/// Get a template in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_get_builder()` + `integrations_projects_locations_templates_get_execute()`.
/// For task-level control, use `integrations_projects_locations_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_get(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsTemplatesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_get_builder(client, &args.name)?;
    integrations_projects_locations_templates_get_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:import
/// Import the template to an existing integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_import_execute()` to send, or `integrations_projects_locations_templates_import` for simplest API.

pub fn integrations_projects_locations_templates_import_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/templates/{templatesId}:import",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:import
/// Import the template to an existing integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_templates_import_execute()` or `integrations_projects_locations_templates_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaImportTemplateResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaImportTemplateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:import
/// Import the template to an existing integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_templates_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_templates_import_task()`.
/// For the simplest API, use `integrations_projects_locations_templates_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_templates_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaImportTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_templates_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_templates_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsTemplatesImportArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:import
/// Import the template to an existing integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_import_builder()` + `integrations_projects_locations_templates_import_execute()`.
/// For task-level control, use `integrations_projects_locations_templates_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_import(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsTemplatesImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaImportTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_import_builder(client, &args.name)?;
    integrations_projects_locations_templates_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates
/// Lists all templates matching the filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_list_execute()` to send, or `integrations_projects_locations_templates_list` for simplest API.

pub fn integrations_projects_locations_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/templates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates
/// Lists all templates matching the filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_templates_list_execute()` or `integrations_projects_locations_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaListTemplatesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaListTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates
/// Lists all templates matching the filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_templates_list_task()`.
/// For the simplest API, use `integrations_projects_locations_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates
/// Lists all templates matching the filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_list_builder()` + `integrations_projects_locations_templates_list_execute()`.
/// For task-level control, use `integrations_projects_locations_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_list(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaListTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
    )?;
    integrations_projects_locations_templates_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}
/// Updates the template by given id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_patch_execute()` to send, or `integrations_projects_locations_templates_patch` for simplest API.

pub fn integrations_projects_locations_templates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/templates/{templatesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}
/// Updates the template by given id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_templates_patch_execute()` or `integrations_projects_locations_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}
/// Updates the template by given id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_templates_patch_task()`.
/// For the simplest API, use `integrations_projects_locations_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsTemplatesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}
/// Updates the template by given id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_patch_builder()` + `integrations_projects_locations_templates_patch_execute()`.
/// For task-level control, use `integrations_projects_locations_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_patch(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    integrations_projects_locations_templates_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates:search
/// Search templates based on user query and filters. This api would query the templates and return a list of templates based on the user filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_search_execute()` to send, or `integrations_projects_locations_templates_search` for simplest API.

pub fn integrations_projects_locations_templates_search_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    enableNaturalLanguageQueryUnderstanding: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    query: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/templates:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = enableNaturalLanguageQueryUnderstanding.as_ref() {
        query_parts.push(format!("enableNaturalLanguageQueryUnderstanding={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates:search
/// Search templates based on user query and filters. This api would query the templates and return a list of templates based on the user filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_templates_search_execute()` or `integrations_projects_locations_templates_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaSearchTemplatesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaSearchTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates:search
/// Search templates based on user query and filters. This api would query the templates and return a list of templates based on the user filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_templates_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_templates_search_task()`.
/// For the simplest API, use `integrations_projects_locations_templates_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_templates_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaSearchTemplatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_templates_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_templates_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsTemplatesSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: enableNaturalLanguageQueryUnderstanding
    pub enableNaturalLanguageQueryUnderstanding: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: query
    pub query: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/templates:search
/// Search templates based on user query and filters. This api would query the templates and return a list of templates based on the user filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_search_builder()` + `integrations_projects_locations_templates_search_execute()`.
/// For task-level control, use `integrations_projects_locations_templates_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_search(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsTemplatesSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaSearchTemplatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_search_builder(
        client,
        &args.parent,
        &args.enableNaturalLanguageQueryUnderstanding,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.query,
        &args.readMask,
    )?;
    integrations_projects_locations_templates_search_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:share
/// Share a template with other clients. Only the template owner can share the templates with other projects. PERMISSION_DENIED would be thrown if the request is not from the owner.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_share_execute()` to send, or `integrations_projects_locations_templates_share` for simplest API.

pub fn integrations_projects_locations_templates_share_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/templates/{templatesId}:share",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:share
/// Share a template with other clients. Only the template owner can share the templates with other projects. PERMISSION_DENIED would be thrown if the request is not from the owner.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_templates_share_execute()` or `integrations_projects_locations_templates_share`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_share_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_share_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:share
/// Share a template with other clients. Only the template owner can share the templates with other projects. PERMISSION_DENIED would be thrown if the request is not from the owner.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_templates_share_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_templates_share_task()`.
/// For the simplest API, use `integrations_projects_locations_templates_share()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_share_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_templates_share_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_templates_share_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_templates_share`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsTemplatesShareArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:share
/// Share a template with other clients. Only the template owner can share the templates with other projects. PERMISSION_DENIED would be thrown if the request is not from the owner.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_share_builder()` + `integrations_projects_locations_templates_share_execute()`.
/// For task-level control, use `integrations_projects_locations_templates_share_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_share(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsTemplatesShareArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_share_builder(client, &args.name)?;
    integrations_projects_locations_templates_share_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:unshare
/// Unshare a template from given clients. Owner of the template can unshare template with clients. Shared client can only unshare the template from itself. PERMISSION_DENIED would be thrown if request is not from owner or for unsharing itself.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_unshare_execute()` to send, or `integrations_projects_locations_templates_unshare` for simplest API.

pub fn integrations_projects_locations_templates_unshare_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/templates/{templatesId}:unshare",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:unshare
/// Unshare a template from given clients. Owner of the template can unshare template with clients. Shared client can only unshare the template from itself. PERMISSION_DENIED would be thrown if request is not from owner or for unsharing itself.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_templates_unshare_execute()` or `integrations_projects_locations_templates_unshare`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_unshare_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_unshare_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:unshare
/// Unshare a template from given clients. Owner of the template can unshare template with clients. Shared client can only unshare the template from itself. PERMISSION_DENIED would be thrown if request is not from owner or for unsharing itself.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_templates_unshare_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_templates_unshare_task()`.
/// For the simplest API, use `integrations_projects_locations_templates_unshare()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_unshare_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_templates_unshare_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_templates_unshare_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_templates_unshare`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsTemplatesUnshareArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:unshare
/// Unshare a template from given clients. Owner of the template can unshare template with clients. Shared client can only unshare the template from itself. PERMISSION_DENIED would be thrown if request is not from owner or for unsharing itself.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_unshare_builder()` + `integrations_projects_locations_templates_unshare_execute()`.
/// For task-level control, use `integrations_projects_locations_templates_unshare_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_unshare(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsTemplatesUnshareArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_unshare_builder(client, &args.name)?;
    integrations_projects_locations_templates_unshare_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates:upload
/// Uploads a template. The content can be a previously downloaded template. Performs the same function as CreateTemplate, but accepts input in a string format, which holds the complete representation of the Template content.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_upload_execute()` to send, or `integrations_projects_locations_templates_upload` for simplest API.

pub fn integrations_projects_locations_templates_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/templates:upload",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates:upload
/// Uploads a template. The content can be a previously downloaded template. Performs the same function as CreateTemplate, but accepts input in a string format, which holds the complete representation of the Template content.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_templates_upload_execute()` or `integrations_projects_locations_templates_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUploadTemplateResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaUploadTemplateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates:upload
/// Uploads a template. The content can be a previously downloaded template. Performs the same function as CreateTemplate, but accepts input in a string format, which holds the complete representation of the Template content.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_templates_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_templates_upload_task()`.
/// For the simplest API, use `integrations_projects_locations_templates_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_templates_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaUploadTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_templates_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_templates_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsTemplatesUploadArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates:upload
/// Uploads a template. The content can be a previously downloaded template. Performs the same function as CreateTemplate, but accepts input in a string format, which holds the complete representation of the Template content.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_upload_builder()` + `integrations_projects_locations_templates_upload_execute()`.
/// For task-level control, use `integrations_projects_locations_templates_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_upload(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsTemplatesUploadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaUploadTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_upload_builder(client, &args.parent)?;
    integrations_projects_locations_templates_upload_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:use
/// Use the template to create integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `integrations_projects_locations_templates_use_execute()` to send, or `integrations_projects_locations_templates_use` for simplest API.

pub fn integrations_projects_locations_templates_use_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://integrations.googleapis.com/v1/projects/{}/locations/{locationsId}/templates/{templatesId}:use",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:use
/// Use the template to create integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `integrations_projects_locations_templates_use_execute()` or `integrations_projects_locations_templates_use`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_use_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_use_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudIntegrationsV1alphaUseTemplateResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudIntegrationsV1alphaUseTemplateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:use
/// Use the template to create integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `integrations_projects_locations_templates_use_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `integrations_projects_locations_templates_use_task()`.
/// For the simplest API, use `integrations_projects_locations_templates_use()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `integrations_projects_locations_templates_use_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn integrations_projects_locations_templates_use_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaUseTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = integrations_projects_locations_templates_use_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`integrations_projects_locations_templates_use`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IntegrationsProjectsLocationsTemplatesUseArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/templates/{templatesId}:use
/// Use the template to create integration. This api would keep track of usage_count and last_used_time. PERMISSION_DENIED would be thrown if template is not accessible by client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `integrations_projects_locations_templates_use_builder()` + `integrations_projects_locations_templates_use_execute()`.
/// For task-level control, use `integrations_projects_locations_templates_use_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn integrations_projects_locations_templates_use(
    client: &SimpleHttpClient,
    args: &IntegrationsProjectsLocationsTemplatesUseArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudIntegrationsV1alphaUseTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = integrations_projects_locations_templates_use_builder(client, &args.name)?;
    integrations_projects_locations_templates_use_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaGenerateTokenResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaGenerateTokenResponse with IntegrationsCallbackGenerateTokenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsCallbackGenerateTokenArgs>
    for GoogleCloudIntegrationsV1alphaGenerateTokenResponse
{
    fn generate_resource_id(&self, input: &IntegrationsCallbackGenerateTokenArgs) -> String {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaGenerateTokenResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaGenerateTokenResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaEnumerateConnectorPlatformRegionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaEnumerateConnectorPlatformRegionsResponse with IntegrationsConnectorPlatformRegionsEnumerateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsConnectorPlatformRegionsEnumerateArgs>
    for GoogleCloudIntegrationsV1alphaEnumerateConnectorPlatformRegionsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsConnectorPlatformRegionsEnumerateArgs,
    ) -> String {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaEnumerateConnectorPlatformRegionsResponse"
            .to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaEnumerateConnectorPlatformRegionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaGetClientMetadataResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaGetClientMetadataResponse with IntegrationsProjectsGetClientmetadataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsGetClientmetadataArgs>
    for GoogleCloudIntegrationsV1alphaGetClientMetadataResponse
{
    fn generate_resource_id(&self, input: &IntegrationsProjectsGetClientmetadataArgs) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaGetClientMetadataResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaGetClientMetadataResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecResponse with IntegrationsProjectsLocationsGenerateOpenApiSpecArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsGenerateOpenApiSpecArgs>
    for GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsGenerateOpenApiSpecArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaGenerateOpenApiSpecResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaGetClientResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaGetClientResponse with IntegrationsProjectsLocationsGetClientsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsGetClientsArgs>
    for GoogleCloudIntegrationsV1alphaGetClientResponse
{
    fn generate_resource_id(&self, input: &IntegrationsProjectsLocationsGetClientsArgs) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaGetClientResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaGetClientResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectResponse with IntegrationsProjectsLocationsAppsScriptProjectsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsAppsScriptProjectsCreateArgs>
    for GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsAppsScriptProjectsCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaCreateAppsScriptProjectResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectResponse with IntegrationsProjectsLocationsAppsScriptProjectsLinkArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsAppsScriptProjectsLinkArgs>
    for GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsAppsScriptProjectsLinkArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaLinkAppsScriptProjectResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaAuthConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaAuthConfig with IntegrationsProjectsLocationsAuthConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsAuthConfigsCreateArgs>
    for GoogleCloudIntegrationsV1alphaAuthConfig
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsAuthConfigsCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaAuthConfig/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaAuthConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsAuthConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsAuthConfigsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsAuthConfigsDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaAuthConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaAuthConfig with IntegrationsProjectsLocationsAuthConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsAuthConfigsGetArgs>
    for GoogleCloudIntegrationsV1alphaAuthConfig
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsAuthConfigsGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaAuthConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaAuthConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListAuthConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListAuthConfigsResponse with IntegrationsProjectsLocationsAuthConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsAuthConfigsListArgs>
    for GoogleCloudIntegrationsV1alphaListAuthConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsAuthConfigsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListAuthConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListAuthConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaAuthConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaAuthConfig with IntegrationsProjectsLocationsAuthConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsAuthConfigsPatchArgs>
    for GoogleCloudIntegrationsV1alphaAuthConfig
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsAuthConfigsPatchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaAuthConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaAuthConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCertificate
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCertificate with IntegrationsProjectsLocationsCertificatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsCertificatesCreateArgs>
    for GoogleCloudIntegrationsV1alphaCertificate
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsCertificatesCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaCertificate/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaCertificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsCertificatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsCertificatesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsCertificatesDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCertificate
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCertificate with IntegrationsProjectsLocationsCertificatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsCertificatesGetArgs>
    for GoogleCloudIntegrationsV1alphaCertificate
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsCertificatesGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaCertificate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaCertificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListCertificatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListCertificatesResponse with IntegrationsProjectsLocationsCertificatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsCertificatesListArgs>
    for GoogleCloudIntegrationsV1alphaListCertificatesResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsCertificatesListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListCertificatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListCertificatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCertificate
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCertificate with IntegrationsProjectsLocationsCertificatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsCertificatesPatchArgs>
    for GoogleCloudIntegrationsV1alphaCertificate
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsCertificatesPatchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaCertificate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaCertificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaChangeCustomerConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaChangeCustomerConfigResponse with IntegrationsProjectsLocationsClientsChangeConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsClientsChangeConfigArgs>
    for GoogleCloudIntegrationsV1alphaChangeCustomerConfigResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsClientsChangeConfigArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaChangeCustomerConfigResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaChangeCustomerConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsClientsDeprovisionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsClientsDeprovisionArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsClientsDeprovisionArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsClientsProvisionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsClientsProvisionArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsClientsProvisionArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorResponse with IntegrationsProjectsLocationsClientsProvisionClientPostProcessorArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsClientsProvisionClientPostProcessorArgs>
    for GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsClientsProvisionClientPostProcessorArgs,
    ) -> String {
        format!("gcp::integrations::GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaProvisionClientPostProcessorResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsClientsReplaceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsClientsReplaceArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsClientsReplaceArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsClientsSwitchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsClientsSwitchArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsClientsSwitchArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsClientsSwitchVariableMaskingArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsClientsSwitchVariableMaskingArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsClientsSwitchVariableMaskingArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsClientsToggleHttpArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsClientsToggleHttpArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsClientsToggleHttpArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse with IntegrationsProjectsLocationsCloudFunctionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsCloudFunctionsCreateArgs>
    for GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsCloudFunctionsCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaConnectionSchemaMetadata
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaConnectionSchemaMetadata with IntegrationsProjectsLocationsConnectionsGetConnectionSchemaMetadataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsConnectionsGetConnectionSchemaMetadataArgs>
    for GoogleCloudIntegrationsV1alphaConnectionSchemaMetadata
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsConnectionsGetConnectionSchemaMetadataArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaConnectionSchemaMetadata/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaConnectionSchemaMetadata"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListConnectionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListConnectionsResponse with IntegrationsProjectsLocationsConnectionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsConnectionsListArgs>
    for GoogleCloudIntegrationsV1alphaListConnectionsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsConnectionsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListConnectionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListConnectionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListRuntimeActionSchemasResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListRuntimeActionSchemasResponse with IntegrationsProjectsLocationsConnectionsRuntimeActionSchemasListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsConnectionsRuntimeActionSchemasListArgs>
    for GoogleCloudIntegrationsV1alphaListRuntimeActionSchemasResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsConnectionsRuntimeActionSchemasListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListRuntimeActionSchemasResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListRuntimeActionSchemasResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListRuntimeEntitySchemasResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListRuntimeEntitySchemasResponse with IntegrationsProjectsLocationsConnectionsRuntimeEntitySchemasListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsConnectionsRuntimeEntitySchemasListArgs>
    for GoogleCloudIntegrationsV1alphaListRuntimeEntitySchemasResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsConnectionsRuntimeEntitySchemasListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListRuntimeEntitySchemasResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListRuntimeEntitySchemasResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsIntegrationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse with IntegrationsProjectsLocationsIntegrationsExecuteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsExecuteArgs>
    for GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsExecuteArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecuteEventResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecuteEventResponse with IntegrationsProjectsLocationsIntegrationsExecuteEventArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsExecuteEventArgs>
    for GoogleCloudIntegrationsV1alphaExecuteEventResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsExecuteEventArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaExecuteEventResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaExecuteEventResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListIntegrationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListIntegrationsResponse with IntegrationsProjectsLocationsIntegrationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsListArgs>
    for GoogleCloudIntegrationsV1alphaListIntegrationsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListIntegrationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListIntegrationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse with IntegrationsProjectsLocationsIntegrationsScheduleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsScheduleArgs>
    for GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsScheduleArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSearchIntegrationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSearchIntegrationsResponse with IntegrationsProjectsLocationsIntegrationsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsSearchArgs>
    for GoogleCloudIntegrationsV1alphaSearchIntegrationsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsSearchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSearchIntegrationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSearchIntegrationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestIntegrationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestIntegrationsResponse with IntegrationsProjectsLocationsIntegrationsTestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsTestArgs>
    for GoogleCloudIntegrationsV1alphaTestIntegrationsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsTestArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaTestIntegrationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaTestIntegrationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCancelExecutionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCancelExecutionResponse with IntegrationsProjectsLocationsIntegrationsExecutionsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsExecutionsCancelArgs>
    for GoogleCloudIntegrationsV1alphaCancelExecutionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsExecutionsCancelArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaCancelExecutionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaCancelExecutionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadExecutionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadExecutionResponse with IntegrationsProjectsLocationsIntegrationsExecutionsDownloadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsExecutionsDownloadArgs>
    for GoogleCloudIntegrationsV1alphaDownloadExecutionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsExecutionsDownloadArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadExecutionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadExecutionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecution
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecution with IntegrationsProjectsLocationsIntegrationsExecutionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsExecutionsGetArgs>
    for GoogleCloudIntegrationsV1alphaExecution
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsExecutionsGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaExecution/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaExecution"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListExecutionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListExecutionsResponse with IntegrationsProjectsLocationsIntegrationsExecutionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsExecutionsListArgs>
    for GoogleCloudIntegrationsV1alphaListExecutionsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsExecutionsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListExecutionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListExecutionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaReplayExecutionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaReplayExecutionResponse with IntegrationsProjectsLocationsIntegrationsExecutionsReplayArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsExecutionsReplayArgs>
    for GoogleCloudIntegrationsV1alphaReplayExecutionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsExecutionsReplayArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaReplayExecutionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaReplayExecutionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaLiftSuspensionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaLiftSuspensionResponse with IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsLiftArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsLiftArgs>
    for GoogleCloudIntegrationsV1alphaLiftSuspensionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsLiftArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaLiftSuspensionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaLiftSuspensionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListSuspensionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListSuspensionsResponse with IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsListArgs>
    for GoogleCloudIntegrationsV1alphaListSuspensionsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListSuspensionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListSuspensionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaResolveSuspensionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaResolveSuspensionResponse with IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsResolveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsResolveArgs>
    for GoogleCloudIntegrationsV1alphaResolveSuspensionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsExecutionsSuspensionsResolveArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaResolveSuspensionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaResolveSuspensionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaIntegrationVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaIntegrationVersion with IntegrationsProjectsLocationsIntegrationsVersionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsCreateArgs>
    for GoogleCloudIntegrationsV1alphaIntegrationVersion
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaIntegrationVersion/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaIntegrationVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsIntegrationsVersionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse with IntegrationsProjectsLocationsIntegrationsVersionsDownloadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsDownloadArgs>
    for GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsDownloadArgs,
    ) -> String {
        format!("gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadJsonPackageResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadJsonPackageResponse with IntegrationsProjectsLocationsIntegrationsVersionsDownloadJsonPackageArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsDownloadJsonPackageArgs>
    for GoogleCloudIntegrationsV1alphaDownloadJsonPackageResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsDownloadJsonPackageArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadJsonPackageResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadJsonPackageResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaIntegrationVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaIntegrationVersion with IntegrationsProjectsLocationsIntegrationsVersionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsGetArgs>
    for GoogleCloudIntegrationsV1alphaIntegrationVersion
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaIntegrationVersion/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaIntegrationVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse with IntegrationsProjectsLocationsIntegrationsVersionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsListArgs>
    for GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaIntegrationVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaIntegrationVersion with IntegrationsProjectsLocationsIntegrationsVersionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsPatchArgs>
    for GoogleCloudIntegrationsV1alphaIntegrationVersion
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsPatchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaIntegrationVersion/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaIntegrationVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse with IntegrationsProjectsLocationsIntegrationsVersionsPublishArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsPublishArgs>
    for GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsPublishArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestIntegrationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestIntegrationsResponse with IntegrationsProjectsLocationsIntegrationsVersionsTestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsTestArgs>
    for GoogleCloudIntegrationsV1alphaTestIntegrationsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsTestArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaTestIntegrationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaTestIntegrationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsIntegrationsVersionsUnpublishArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsUnpublishArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsUnpublishArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse with IntegrationsProjectsLocationsIntegrationsVersionsUploadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsUploadArgs>
    for GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsUploadArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestCase
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestCase with IntegrationsProjectsLocationsIntegrationsVersionsTestCasesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsTestCasesCreateArgs>
    for GoogleCloudIntegrationsV1alphaTestCase
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaTestCase/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaTestCase"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsIntegrationsVersionsTestCasesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsTestCasesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadTestCaseResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadTestCaseResponse with IntegrationsProjectsLocationsIntegrationsVersionsTestCasesDownloadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsTestCasesDownloadArgs>
    for GoogleCloudIntegrationsV1alphaDownloadTestCaseResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesDownloadArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadTestCaseResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadTestCaseResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecuteTestCasesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecuteTestCasesResponse with IntegrationsProjectsLocationsIntegrationsVersionsTestCasesExecuteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsTestCasesExecuteArgs>
    for GoogleCloudIntegrationsV1alphaExecuteTestCasesResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesExecuteArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaExecuteTestCasesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaExecuteTestCasesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecuteTestCaseResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecuteTestCaseResponse with IntegrationsProjectsLocationsIntegrationsVersionsTestCasesExecuteTestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsTestCasesExecuteTestArgs>
    for GoogleCloudIntegrationsV1alphaExecuteTestCaseResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesExecuteTestArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaExecuteTestCaseResponse/{}",
            input.testCaseName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaExecuteTestCaseResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestCase
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestCase with IntegrationsProjectsLocationsIntegrationsVersionsTestCasesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsTestCasesGetArgs>
    for GoogleCloudIntegrationsV1alphaTestCase
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaTestCase/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaTestCase"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListTestCasesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListTestCasesResponse with IntegrationsProjectsLocationsIntegrationsVersionsTestCasesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsTestCasesListArgs>
    for GoogleCloudIntegrationsV1alphaListTestCasesResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListTestCasesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListTestCasesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestCase
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestCase with IntegrationsProjectsLocationsIntegrationsVersionsTestCasesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsTestCasesPatchArgs>
    for GoogleCloudIntegrationsV1alphaTestCase
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesPatchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaTestCase/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaTestCase"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestCase
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestCase with IntegrationsProjectsLocationsIntegrationsVersionsTestCasesTakeoverEditLockArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        IntegrationsProjectsLocationsIntegrationsVersionsTestCasesTakeoverEditLockArgs,
    > for GoogleCloudIntegrationsV1alphaTestCase
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesTakeoverEditLockArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaTestCase/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaTestCase"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaUploadTestCaseResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaUploadTestCaseResponse with IntegrationsProjectsLocationsIntegrationsVersionsTestCasesUploadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsIntegrationsVersionsTestCasesUploadArgs>
    for GoogleCloudIntegrationsV1alphaUploadTestCaseResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsIntegrationsVersionsTestCasesUploadArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaUploadTestCaseResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaUploadTestCaseResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaAuthConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaAuthConfig with IntegrationsProjectsLocationsProductsAuthConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsAuthConfigsCreateArgs>
    for GoogleCloudIntegrationsV1alphaAuthConfig
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsAuthConfigsCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaAuthConfig/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaAuthConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsProductsAuthConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsAuthConfigsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsAuthConfigsDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaAuthConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaAuthConfig with IntegrationsProjectsLocationsProductsAuthConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsAuthConfigsGetArgs>
    for GoogleCloudIntegrationsV1alphaAuthConfig
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsAuthConfigsGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaAuthConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaAuthConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListAuthConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListAuthConfigsResponse with IntegrationsProjectsLocationsProductsAuthConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsAuthConfigsListArgs>
    for GoogleCloudIntegrationsV1alphaListAuthConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsAuthConfigsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListAuthConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListAuthConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaAuthConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaAuthConfig with IntegrationsProjectsLocationsProductsAuthConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsAuthConfigsPatchArgs>
    for GoogleCloudIntegrationsV1alphaAuthConfig
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsAuthConfigsPatchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaAuthConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaAuthConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCertificate
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCertificate with IntegrationsProjectsLocationsProductsCertificatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsCertificatesCreateArgs>
    for GoogleCloudIntegrationsV1alphaCertificate
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsCertificatesCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaCertificate/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaCertificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsProductsCertificatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsCertificatesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsCertificatesDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCertificate
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCertificate with IntegrationsProjectsLocationsProductsCertificatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsCertificatesGetArgs>
    for GoogleCloudIntegrationsV1alphaCertificate
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsCertificatesGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaCertificate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaCertificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListCertificatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListCertificatesResponse with IntegrationsProjectsLocationsProductsCertificatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsCertificatesListArgs>
    for GoogleCloudIntegrationsV1alphaListCertificatesResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsCertificatesListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListCertificatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListCertificatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCertificate
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCertificate with IntegrationsProjectsLocationsProductsCertificatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsCertificatesPatchArgs>
    for GoogleCloudIntegrationsV1alphaCertificate
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsCertificatesPatchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaCertificate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaCertificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse with IntegrationsProjectsLocationsProductsCloudFunctionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsCloudFunctionsCreateArgs>
    for GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsCloudFunctionsCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaCreateCloudFunctionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse with IntegrationsProjectsLocationsProductsIntegrationsExecuteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsExecuteArgs>
    for GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsExecuteArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaExecuteIntegrationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListIntegrationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListIntegrationsResponse with IntegrationsProjectsLocationsProductsIntegrationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsListArgs>
    for GoogleCloudIntegrationsV1alphaListIntegrationsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListIntegrationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListIntegrationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse with IntegrationsProjectsLocationsProductsIntegrationsScheduleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsScheduleArgs>
    for GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsScheduleArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaScheduleIntegrationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestIntegrationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTestIntegrationsResponse with IntegrationsProjectsLocationsProductsIntegrationsTestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsTestArgs>
    for GoogleCloudIntegrationsV1alphaTestIntegrationsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsTestArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaTestIntegrationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaTestIntegrationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadExecutionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadExecutionResponse with IntegrationsProjectsLocationsProductsIntegrationsExecutionsDownloadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsExecutionsDownloadArgs>
    for GoogleCloudIntegrationsV1alphaDownloadExecutionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsExecutionsDownloadArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadExecutionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadExecutionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecution
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaExecution with IntegrationsProjectsLocationsProductsIntegrationsExecutionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsExecutionsGetArgs>
    for GoogleCloudIntegrationsV1alphaExecution
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsExecutionsGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaExecution/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaExecution"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListExecutionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListExecutionsResponse with IntegrationsProjectsLocationsProductsIntegrationsExecutionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsExecutionsListArgs>
    for GoogleCloudIntegrationsV1alphaListExecutionsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsExecutionsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListExecutionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListExecutionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaLiftSuspensionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaLiftSuspensionResponse with IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsLiftArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsLiftArgs,
    > for GoogleCloudIntegrationsV1alphaLiftSuspensionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsLiftArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaLiftSuspensionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaLiftSuspensionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListSuspensionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListSuspensionsResponse with IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsListArgs,
    > for GoogleCloudIntegrationsV1alphaListSuspensionsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListSuspensionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListSuspensionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaResolveSuspensionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaResolveSuspensionResponse with IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsResolveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsResolveArgs,
    > for GoogleCloudIntegrationsV1alphaResolveSuspensionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsExecutionsSuspensionsResolveArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaResolveSuspensionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaResolveSuspensionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaIntegrationVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaIntegrationVersion with IntegrationsProjectsLocationsProductsIntegrationsVersionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsVersionsCreateArgs>
    for GoogleCloudIntegrationsV1alphaIntegrationVersion
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsVersionsCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaIntegrationVersion/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaIntegrationVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsProductsIntegrationsVersionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsVersionsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsVersionsDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse with IntegrationsProjectsLocationsProductsIntegrationsVersionsDownloadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsVersionsDownloadArgs>
    for GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsVersionsDownloadArgs,
    ) -> String {
        format!("gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadIntegrationVersionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaIntegrationVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaIntegrationVersion with IntegrationsProjectsLocationsProductsIntegrationsVersionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsVersionsGetArgs>
    for GoogleCloudIntegrationsV1alphaIntegrationVersion
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsVersionsGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaIntegrationVersion/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaIntegrationVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse with IntegrationsProjectsLocationsProductsIntegrationsVersionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsVersionsListArgs>
    for GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsVersionsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListIntegrationVersionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaIntegrationVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaIntegrationVersion with IntegrationsProjectsLocationsProductsIntegrationsVersionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsVersionsPatchArgs>
    for GoogleCloudIntegrationsV1alphaIntegrationVersion
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsVersionsPatchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaIntegrationVersion/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaIntegrationVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse with IntegrationsProjectsLocationsProductsIntegrationsVersionsPublishArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsVersionsPublishArgs>
    for GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsVersionsPublishArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaPublishIntegrationVersionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTakeoverEditLockResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTakeoverEditLockResponse with IntegrationsProjectsLocationsProductsIntegrationsVersionsTakeoverEditLockArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        IntegrationsProjectsLocationsProductsIntegrationsVersionsTakeoverEditLockArgs,
    > for GoogleCloudIntegrationsV1alphaTakeoverEditLockResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsVersionsTakeoverEditLockArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaTakeoverEditLockResponse/{}",
            input.integrationVersion
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaTakeoverEditLockResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsProductsIntegrationsVersionsUnpublishArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsVersionsUnpublishArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsVersionsUnpublishArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse with IntegrationsProjectsLocationsProductsIntegrationsVersionsUploadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsIntegrationsVersionsUploadArgs>
    for GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsIntegrationsVersionsUploadArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaUploadIntegrationVersionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcInstance
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcInstance with IntegrationsProjectsLocationsProductsSfdcInstancesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsSfdcInstancesCreateArgs>
    for GoogleCloudIntegrationsV1alphaSfdcInstance
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsSfdcInstancesCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcInstance/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcInstance"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsProductsSfdcInstancesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsSfdcInstancesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsSfdcInstancesDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcInstance
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcInstance with IntegrationsProjectsLocationsProductsSfdcInstancesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsSfdcInstancesGetArgs>
    for GoogleCloudIntegrationsV1alphaSfdcInstance
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsSfdcInstancesGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcInstance/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcInstance"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse with IntegrationsProjectsLocationsProductsSfdcInstancesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsSfdcInstancesListArgs>
    for GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsSfdcInstancesListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcInstance
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcInstance with IntegrationsProjectsLocationsProductsSfdcInstancesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsSfdcInstancesPatchArgs>
    for GoogleCloudIntegrationsV1alphaSfdcInstance
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsSfdcInstancesPatchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcInstance/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcInstance"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcChannel
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcChannel with IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsCreateArgs>
    for GoogleCloudIntegrationsV1alphaSfdcChannel
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcChannel/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcChannel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcChannel
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcChannel with IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsGetArgs>
    for GoogleCloudIntegrationsV1alphaSfdcChannel
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcChannel/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcChannel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse with IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsListArgs>
    for GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcChannel
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcChannel with IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsPatchArgs>
    for GoogleCloudIntegrationsV1alphaSfdcChannel
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsProductsSfdcInstancesSfdcChannelsPatchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcChannel/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcChannel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcInstance
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcInstance with IntegrationsProjectsLocationsSfdcInstancesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsSfdcInstancesCreateArgs>
    for GoogleCloudIntegrationsV1alphaSfdcInstance
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsSfdcInstancesCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcInstance/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcInstance"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsSfdcInstancesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsSfdcInstancesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsSfdcInstancesDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcInstance
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcInstance with IntegrationsProjectsLocationsSfdcInstancesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsSfdcInstancesGetArgs>
    for GoogleCloudIntegrationsV1alphaSfdcInstance
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsSfdcInstancesGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcInstance/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcInstance"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse with IntegrationsProjectsLocationsSfdcInstancesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsSfdcInstancesListArgs>
    for GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsSfdcInstancesListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListSfdcInstancesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcInstance
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcInstance with IntegrationsProjectsLocationsSfdcInstancesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsSfdcInstancesPatchArgs>
    for GoogleCloudIntegrationsV1alphaSfdcInstance
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsSfdcInstancesPatchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcInstance/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcInstance"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcChannel
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcChannel with IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsCreateArgs>
    for GoogleCloudIntegrationsV1alphaSfdcChannel
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcChannel/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcChannel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcChannel
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcChannel with IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsGetArgs>
    for GoogleCloudIntegrationsV1alphaSfdcChannel
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcChannel/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcChannel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse with IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsListArgs>
    for GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListSfdcChannelsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcChannel
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSfdcChannel with IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsPatchArgs>
    for GoogleCloudIntegrationsV1alphaSfdcChannel
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsSfdcInstancesSfdcChannelsPatchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcChannel/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSfdcChannel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTemplate
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTemplate with IntegrationsProjectsLocationsTemplatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsTemplatesCreateArgs>
    for GoogleCloudIntegrationsV1alphaTemplate
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsTemplatesCreateArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaTemplate/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsTemplatesDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsTemplatesDeleteArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadTemplateResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaDownloadTemplateResponse with IntegrationsProjectsLocationsTemplatesDownloadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsTemplatesDownloadArgs>
    for GoogleCloudIntegrationsV1alphaDownloadTemplateResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsTemplatesDownloadArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadTemplateResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaDownloadTemplateResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTemplate
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTemplate with IntegrationsProjectsLocationsTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsTemplatesGetArgs>
    for GoogleCloudIntegrationsV1alphaTemplate
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsTemplatesGetArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaTemplate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaImportTemplateResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaImportTemplateResponse with IntegrationsProjectsLocationsTemplatesImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsTemplatesImportArgs>
    for GoogleCloudIntegrationsV1alphaImportTemplateResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsTemplatesImportArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaImportTemplateResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaImportTemplateResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListTemplatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaListTemplatesResponse with IntegrationsProjectsLocationsTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsTemplatesListArgs>
    for GoogleCloudIntegrationsV1alphaListTemplatesResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsTemplatesListArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaListTemplatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaListTemplatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTemplate
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaTemplate with IntegrationsProjectsLocationsTemplatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsTemplatesPatchArgs>
    for GoogleCloudIntegrationsV1alphaTemplate
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsTemplatesPatchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaTemplate/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSearchTemplatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaSearchTemplatesResponse with IntegrationsProjectsLocationsTemplatesSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsTemplatesSearchArgs>
    for GoogleCloudIntegrationsV1alphaSearchTemplatesResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsTemplatesSearchArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaSearchTemplatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaSearchTemplatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsTemplatesShareArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsTemplatesShareArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsTemplatesShareArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with IntegrationsProjectsLocationsTemplatesUnshareArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsTemplatesUnshareArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsTemplatesUnshareArgs,
    ) -> String {
        format!("gcp::integrations::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaUploadTemplateResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaUploadTemplateResponse with IntegrationsProjectsLocationsTemplatesUploadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsTemplatesUploadArgs>
    for GoogleCloudIntegrationsV1alphaUploadTemplateResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsTemplatesUploadArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaUploadTemplateResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaUploadTemplateResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaUseTemplateResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudIntegrationsV1alphaUseTemplateResponse with IntegrationsProjectsLocationsTemplatesUseArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IntegrationsProjectsLocationsTemplatesUseArgs>
    for GoogleCloudIntegrationsV1alphaUseTemplateResponse
{
    fn generate_resource_id(
        &self,
        input: &IntegrationsProjectsLocationsTemplatesUseArgs,
    ) -> String {
        format!(
            "gcp::integrations::GoogleCloudIntegrationsV1alphaUseTemplateResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::integrations::GoogleCloudIntegrationsV1alphaUseTemplateResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
